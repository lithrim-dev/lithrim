__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.DS_Store

# Local data — track only the manifest pointer, not the cohort itself.
data/synthea_sample_data_csv_latest/
data/synthea/
data/synthea_*/*
!data/synthea_*/MANIFEST.txt

out/

# Live-stack credentials — do not commit
.live_env

# Dev-stack runtime — logs + pidfiles from scripts/dev/devstack.sh
.devstack/

# Local Claude Code tooling — agent worktrees (GBs), local settings (tokens), launch config.
# NEVER commit; concurrent agent sessions write here. (git-commit-pathspec-dirty-index)
.claude/

# MCP server config — may hold the Zyng API token (do not commit)
.mcp.json
