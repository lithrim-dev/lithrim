{
  "name": "ws0_default",
  "eval_profile": {
    "judges": ["risk_judge", "policy_judge", "faithfulness_judge"],
    "council_config": {
      "disposition": "compose-over-live-v2",
      "compliance_council_version": "v2",
      "endpoint": "http://localhost:8002/v1/pipeline/evaluate",
      "note": "S-BS-6 ratified: the harness baseline is compose-over-live-v2 (the prod-real v2 council over-fires; grounding cleans it). STORED ONLY here; injection into the backend PipelineRequest.council_config is WS-2. judges = the v2 trio that actually runs per runtime/council/compliance_council.py (behavior_judge / source_message_judge are declared-not-running in v2)."
    },
    "ontology_ref": "clinical/1",
    "ontology_path": "packs/healthcare/ontology.json",
    "tools": ["presence_check"],
    "kb_bindings": {},
    "severity_map_ref": "ontology:clinical/1"
  },
  "dataset": {
    "case_id": "bench_scribe_v1_inject_condition_1bd0f10dc7b5",
    "source": "out/scribe_v1.jsonl",
    "baseline": "out/scribe_v1.live_pipeline_evaluate.bench_scribe_v1_inject_condition_1bd0f10dc7b5.json",
    "mode": "replay"
  }
}
