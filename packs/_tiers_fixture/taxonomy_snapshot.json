{
  "snapshot_metadata": {
    "source": "PACK-1b non-vacuity fixture (tests/test_pack_layer1b.py)",
    "note": "SENTINEL tiers — NOT the real taxonomy. Proves the FROZEN council reads its tier sets FROM the active pack (layer 1b, the source-of-truth flip): under LITHRIM_BENCH_PACK=_tiers_fixture the council's KNOWN_TAXONOMY_CODES must become these sentinels, not the 19 healthcare codes. INERT in normal use: pack discovery is active-pack-only (lithrim_bench.packs._active_packs(active_pack())), so nothing loads this pack unless that env var selects it."
  },
  "tiers": {
    "TIER_1_NEVER_EVENTS": ["FIXTURE_SENTINEL_T1"],
    "TIER_2_HIGH_RISK": ["FIXTURE_SENTINEL_T2"],
    "TIER_3_MEDIUM": ["FIXTURE_SENTINEL_T3"]
  },
  "tier1_owners": {},
  "production_judges": ["risk_judge", "policy_judge", "faithfulness_judge"],
  "declared_but_not_running": []
}
