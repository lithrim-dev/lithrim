# Driver — `bench-salvage` phase `WS-6c-DSPy-2-live`: close the S-BS-44 prompt-parity gate, then run the cost-confirmed live prompt-vs-DSPy A/B

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — ROOTED IN `lithrim-bench` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-DSPy-2-live-ab-run-driver`
> **Version:** v1 · **Authored:** 2026-06-03
> **Last re-verified against code:** 2026-06-03 (`lithrim-bench` re-grepped at `HEAD` `317c233` of `bench-salvage/ws6c-dspy`, per MONITOR §Phase 1a)
>
> **This is the deferred live payoff of WS-6c-DSPy-2** (closed `a511012..69b3505`, audit CLEAN + fresh-critic NON-BLOCKING). DSPy-2 landed the trio's lens layer + the `ab_harness.py` substrate **offline-only** — so the prompt-vs-DSPy comparison is **not yet proven against real LLM behavior**. This cycle: (1) close the **S-BS-44 GATE** (one-line `.strip()` so the two arms get byte-identical role prompts — the A/B's like-for-like premise), then (2) run **ONE cost-confirmed live A/B** (real prompt-council vs DSPy-council) on the by-construction `proof_case` pack and persist the result. **User authorized the live spend (~$1–3) on 2026-06-03.** This is the judge-calibration USP measurement. It does **NOT** change consensus, grade, lenses, or the optimizer (that is WS-6c-DSPy-3).
>
> **⚠️ DIRTY-TREE / SHARED-BRANCH (the WS-6d/DSPy-2 landmine).** You run on `bench-salvage/ws6c-dspy` with concurrent uncommitted work (calibration `verification/*`+`grounding.py`+`SPEC_CALIBRATION_TRAINER`+`RUN_*`+`tests/verification/`; journey `apps/`; **3 STAGED paper files**). Your deliverables are in `runtime/council/` + an artifact dir (no overlap). **NEVER bare-commit. ALWAYS `git add <your-files> && git commit -- <your-files>`** and verify scope after each. The 3 staged paper files must stay staged-but-unswept.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c-DSPy-2-live
(close the S-BS-44 prompt-parity gate, then run ONE cost-confirmed live prompt-vs-DSPy A/B).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench). The A/B calls Azure DIRECTLY (the council
llm_provider + dspy.LM bound to Azure deployments) — NOT :8002, NOT Celery. DO NOT autostart any
service: the USER provides the Azure env. DO NOT change consensus / grade / lenses / the optimizer.

CRITICAL — DIRTY SHARED BRANCH: bench-salvage/ws6c-dspy carries concurrent uncommitted work
(calibration verification/* + grounding.py + SPEC_CALIBRATION_TRAINER + RUN_* + tests/verification/;
journey apps/; 3 STAGED paper files). NEVER touch those. NEVER bare-commit (it sweeps the staged
paper files). ALWAYS: git add <your-files> && git commit -- <your-files>, then git show --stat.

COST: the user authorized ~$1-3 for ONE live A/B (~42 Azure calls). Cost-confirm from a 1-2 call
smoke BEFORE the full run; halt + report if trending over $3. No second run without a fresh go.

Read in this order:
  1. .devloop/personas/EXECUTOR.md   (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-2-live_ab-run_driver.md   (this doc)
  3. .devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2-2026-06-03.json   (the substrate this runs; A3 frozen-contract posture)
  4. .devloop/sessions/critique-bench-salvage-phaseWS-6c-DSPy-2-2026-06-03.md   (S-BS-44 origin + the lower-bound/S-BS-43 caveat you must honor in the report)
  5. lithrim_bench/runtime/council/judges_dspy.py   (load_role_prompt:110-118 = the S-BS-44 fix site; build_trio:310)
  6. lithrim_bench/runtime/council/ab_harness.py   (run_live:201 confirm_cost gate; run_offline_structural:147; diff_case:80; OFFLINE_NOTE:45)
  7. lithrim_bench/runtime/council/compliance_council.py   (_load_role_prompts:523 .strip() — the parity target; _apply_consensus:1866 KEEP UNCHANGED)
  8. lithrim_bench/runtime/council/tests/test_live_smoke.py   (the env-gate pattern: LITHRIM_LLM_PROVIDER=azure + AZURE_OPENAI_*; skipif)
  9. examples/proof_case.jsonl (n=7)   (the recipe=label A/B case set)

Then post your plan-review per EXECUTOR.md — resolve the 4 PLAN-REVIEW DECISIONS in §3.
Do not write code or make any live call until I say "go". ROUTINE: inline critique at close.

Bundle ID: bench-salvage-phaseWS-6c-DSPy-2-live-ab-run-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-2-live_ab-run_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** code file:line re-grepped at `bench-salvage/ws6c-dspy` HEAD `317c233` on 2026-06-03 (CONFIRMED). Re-grep before plan-review; surface drift.

1. The DSPy-2 **session log** + **critique** (pre-flight #3/#4 above) — the substrate you exercise, the S-BS-44 origin, and the **S-BS-43 lower-bound** caveat your report MUST carry.
2. `lithrim_bench/runtime/council/judges_dspy.py` — `load_role_prompt(role)` (`:110`) reads `council_roles/<role>.txt` via `read_text` at **`:118` with NO `.strip()`**, while its docstring (`:111`) claims "the SAME prompt text the live council" uses — **that is S-BS-44**. `build_trio` (`:310`) assembles the role-prompt-bound trio.
3. `lithrim_bench/runtime/council/compliance_council.py` — `_load_role_prompts` (`:523`) does `prompts[file.stem] = file.read_text(...).strip()` (`:527`) — **the parity target**: after the fix, `build_trio()`'s prompts must equal these byte-for-byte. `_apply_consensus:1866` + the tables stay **UNCHANGED** (A3).
4. `lithrim_bench/runtime/council/ab_harness.py` — `run_live(..., confirm_cost=False)` (`:201`) **refuses without `confirm_cost=True`** (`:216-219`); `run_offline_structural:147`; `diff_case:80`; `OFFLINE_NOTE:45`. The live A/B is `run_live(confirm_cost=True)`.
5. `lithrim_bench/runtime/council/judge_metric.py` — `LENS_BY_ROLE` + `score_judge` (used per-arm). The per-judge precision is a **LOWER BOUND (S-BS-43)** — your report labels it so.
6. `lithrim_bench/runtime/council/tests/test_live_smoke.py` — the env-gate: live runs need `LITHRIM_LLM_PROVIDER=azure` + `AZURE_OPENAI_ENDPOINT/API_KEY/DEPLOYMENT_COUNCIL/_MISTRAL_LARGE_3/_LLAMA_4_MAVERICK` (precedent: sourced from `../lithrim-backend/.env`). `skipif` when unset.
7. `examples/proof_case.jsonl` (n=7) — the recipe=label A/B case set (the offline DSPy-2 set; flags incl. WRONG_DOSAGE, MISSING_ALLERGY, FABRICATED_HISTORY, VALUE_MISMATCH + clean negatives).
8. The env: run on the **`debuglithrim`** pyenv (has openai + dspy + tenacity + pytest); both arms need it. `import lithrim_bench` on default deps must stay openai/dspy-free (unchanged).

---

## 2. Deliverables (file-by-file)

1. **`lithrim_bench/runtime/council/judges_dspy.py`** (FIX — close S-BS-44, the GATE) — `load_role_prompt` returns `path.read_text(encoding="utf-8").strip()` (match `_load_role_prompts:527`); correct the docstring so "the SAME prompt text" is now true. One-line behavior change + docstring. **No other change** to `Judge`/`build_trio`/`evaluate_dspy`.
2. **`lithrim_bench/runtime/council/tests/test_trio_dspy.py`** (EXTEND — the S-BS-44 regression guard) — assert `build_trio()`'s per-role `role_prompt` equals `ComplianceCouncil()._load_role_prompts()[role]` **byte-for-byte** for all of `V2_ROLES` (so the two A/B arms are provably like-for-like). Offline, `importorskip`-gated. This test is what CLOSES S-BS-44.
3. **The live A/B run** (no new code unless the harness needs a tiny CLI shim — confirm at plan-review) — execute `ab_harness.run_live(build_trio(...), council=ComplianceCouncil(), cases=<proof_case n=7>, confirm_cost=True)` on `debuglithrim` with the Azure env set. **Cost-confirm first** (a 1–2-call smoke → per-call cost), then the full ~42-call run. Capture the diff record (per-case verdict agreement + per-role code-set agreement + None-aware calibration + per-arm `score_judge`).
4. **`out/ws6c_dspy2/ab_result.2026-06-03.json`** (NEW — the captured-run artifact; home/format = plan-review #3) — the persisted A/B diff record (a **captured live run**, NOT a byte-deterministic fixture — live LLMs vary even at temperature=0). Plus a short **REPORT** (`docs/research/REPORT_dspy2_live_ab_2026-06-03.md` or in the session log) with the headline numbers, carrying the **mandatory caveats**: per-judge precision is a **LOWER BOUND (S-BS-43)**; the run is a **captured snapshot** (not deterministic); **n=7 is small** — report the comparison, do not over-claim "DSPy beats/matches the prompt-council" beyond what n=7 supports.
5. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2-live-2026-06-03.json`** — session log incl. the S-BS-44 parity proof, the A/B numbers, the **live cost** (per-call + total), and the A3 frozen-contract re-verify (`compliance_council.py` byte-identical).

---

## 3. Plan-review checkpoint (non-negotiable)

Per `EXECUTOR.md`, post understanding + file-by-file plan + the run plan + risks + deviations. **Wait for "go". Make NO live call before "go".** Resolve:

1. **Case set + N for the live run.** Default `examples/proof_case.jsonl` (n=7), matching the DSPy-2 offline set. *(Option: add a few `data/verification_packs/` near-miss cases — more signal, more cost. Default: proof_case n=7 only.)*
2. **S-BS-44 fix + the parity assertion.** Confirm the `.strip()` fix + the byte-equality test against `_load_role_prompts:527`. This is the GATE — it lands and passes BEFORE the live run.
3. **Result artifact home + format + honesty framing.** `out/ws6c_dspy2/ab_result.<date>.json` (gitignored-or-committed? — confirm) + the REPORT home. State the three mandatory caveats (lower-bound precision / captured-run-not-deterministic / n=7-small) you will carry.
4. **Cost-confirm protocol.** The 1–2-call smoke to establish per-call cost (the WS-6c precedent: a smoke caught a payload-shape bug that would have overspent), the projected full-run total, the **$3 hard ceiling** (halt + report if trending over), and confirmation that the Azure env is the user's (no autostart; you verify reachability, you do not provision).

---

## 4. Scope guardrails — NOT in scope

- **NO change to consensus / `_apply_consensus:1866` / the tables** (A3 frozen — re-verify byte-identical at close).
- **NO grade-seam wire / `run_eval` / `report.composite` / §10** change. The A/B is a standalone measurement over `evaluate` + `evaluate_dspy`.
- **NO lens change** — use `LENS_BY_ROLE` as-is; the **co-raise-aware lens is S-BS-43 → WS-6c-DSPy-3**. Report the lower bound honestly; do not "fix" the metric here.
- **NO optimizer / BootstrapFewShot / MIPRO** — that is WS-6c-DSPy-3.
- **NO new judge / no `_TIER1_OWNERS` edit / no taxonomy re-snapshot.**
- **NO second live run** without a fresh cost-confirmed "go"; **NO autostart** of Azure or any service (the user owns the env).
- **NO concurrent-session file** touched (`verification/*`, `harness/grounding.py`, `apps/*`, `docs/PAPER_OUTLINE.md`, `docs/research/REPORT_semantic*`/`SYNTHESIS_*`/`RUN_*`, `SPEC_CALIBRATION_TRAINER.md`, `tests/verification/`). Never stage/commit them; the 3 staged paper files stay staged-but-unswept.
- **NO `../lithrim-backend` / `etlp-mapper` edit.** No drive-by reformat.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** **S-BS-44 CLOSED** — `load_role_prompt` `.strip()`s; the regression test asserts `build_trio()` role prompts equal `_load_role_prompts()[role]` byte-for-byte for all `V2_ROLES`; the docstring is now true.
- **A2.** **ONE live A/B ran** (real prompt-council vs DSPy-council, trio, proof_case n=7) and the diff record (verdict agreement / per-role code-set agreement / None-aware calibration / per-arm `score_judge`) is persisted as a committed captured-run artifact; the live **cost is within the $3 ceiling and reported** (per-call + total).
- **A3.** **Frozen contract STILL holds** — `compliance_council.py` byte-identical (re-verify diff = 0); no grade-wire; consensus unchanged.
- **A4.** **Result reported HONESTLY** — the REPORT carries all three caveats (per-judge precision = LOWER BOUND per S-BS-43; captured-run-not-deterministic; n=7 small) and makes no claim beyond what n=7 supports. The verdict-agreement %, per-role precision/recall, and calibration deltas are stated as-of-this-run.
- **A5.** **Default install unchanged + scope held** — `import lithrim_bench` openai/dspy-free; ruff clean on changed files; close-diff `git diff 317c233 HEAD --stat` = ONLY DSPy-2-live files (no concurrent-session/paper sweep); every commit pathspec'd.

### Diagnostic stats (NOT acceptance gates)
- Verdict-agreement % (prompt vs DSPy), per-role code-set agreement, per-role precision/recall (LOWER BOUND), calibration deltas — the headline calibration numbers.
- Per-call + total Azure cost.
- Any case where the two arms diverge (the interesting calibration signal) — narrate.

---

## 6. Commit structure + message templates

Atomic per-concern on `bench-salvage/ws6c-dspy`. **Explicit pathspec on every commit (the landmine).** Suggested:

1. `fix(council-dspy): load_role_prompt strips -> trio prompts byte-match the prompt-council (close S-BS-44)` — `judges_dspy.py` + `tests/test_trio_dspy.py`
2. `feat(council-dspy): live prompt-vs-DSPy A/B on proof_case (n=7, cost-confirmed) + REPORT` — the artifact + REPORT

**Commit hygiene (MANDATORY):** `git add <only your files>` → `git commit -- <only your files>` → `git show --stat --format= HEAD`. At close: `git diff 317c233 HEAD --stat` = ONLY DSPy-2-live files; if a concurrent/paper file appears, STOP (you swept it). Bodies end with `Executed per bench-salvage-phaseWS-6c-DSPy-2-live-ab-run-driver by session-…` + `See …session-…json` + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A5 PASS (S-BS-44 closed + parity test; one live A/B persisted + cost ≤ $3 reported; consensus byte-identical; report carries the 3 caveats; default install + scope held)
- [ ] `compliance_council.py` zero delta vs `317c233` (re-verify)
- [ ] No grade.py / run_eval.py / report.py / lens / `_TIER1_OWNERS` / taxonomy change
- [ ] No concurrent-session/paper file in the diff; every commit pathspec'd; the 3 staged paper files NOT swept
- [ ] Azure env was the user's (no autostart); cost-confirmed via smoke before the full run; ONE run only
- [ ] `import lithrim_bench` openai/dspy-free; ruff clean on changed files; offline tests green on `debuglithrim`
- [ ] Session log written (S-BS-44 parity proof + A/B numbers + live cost + A3 re-verify)

---

## 8. First move

1. Read `EXECUTOR.md` + the DSPy-2 session log + critique (the substrate + the S-BS-43/S-BS-44 caveats).
2. Re-grep the §1 anchors at HEAD `317c233` (`load_role_prompt:118` no-strip; `_load_role_prompts:527` strip; `run_live:201` confirm_cost; the live env-gate).
3. `git status` — confirm the dirty workbench + 3 staged paper files; internalize pathspec-only commits.
4. Read §2–§7.
5. Post plan-review per §3 (case set, the S-BS-44 fix + parity test, artifact home, the cost-confirm protocol). **Wait for "go" — no live call before then.**

---

## 9. References

- The substrate cycle: `.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2-2026-06-03.json` + `critique-bench-salvage-phaseWS-6c-DSPy-2-2026-06-03.md`
- The harness: `lithrim_bench/runtime/council/ab_harness.py` (`run_live:201`, `diff_case:80`, `OFFLINE_NOTE:45`)
- The fix site / parity target: `judges_dspy.py:load_role_prompt:118` vs `compliance_council.py:_load_role_prompts:527`
- The lens / lower-bound: `judge_metric.py` `LENS_BY_ROLE` + S-BS-43
- Live env precedent: `tests/test_live_smoke.py` + `../lithrim-backend/.env` (WS-6c live smoke)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (seams S-BS-42 / S-BS-43 / S-BS-44; First-move)
- Next: **WS-6c-DSPy-3** (the optimizer pass — BootstrapFewShot climbing `make_judge_metric` + the co-raise-aware lens S-BS-43)

---

## Hardness

- [x] **ROUTINE — monitor performs inline critique at close.** Low blast radius: a one-line `.strip()` gate fix + a single measurement run; no consensus / grade / lens / contract change (A3 re-verified). BUT the result is **paper-bearing** and **live** — so the inline critique's load-bearing focus is (a) the A3 frozen-contract re-verify, (b) **result-honesty** (the 3 caveats; no over-claim beyond n=7), and (c) the S-BS-44 parity proof. Escalate to a fresh-critic only if the A/B surfaces a surprising divergence that would change a paper claim.
