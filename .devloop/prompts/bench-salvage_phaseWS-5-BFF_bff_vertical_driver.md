# Driver — `bench-salvage` phase `WS-5-BFF`: FastAPI BFF + React↔Python bridge + ONE real `run_eval` eval-report vertical

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-5-BFF-driver`
> **Version:** v1
> **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (harness surface re-grepped on HEAD `f6d99af`)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-5-BFF
(FastAPI BFF + React<->Python bridge + ONE real run_eval eval-report vertical + first test).

This is the de-risk phase: prove the whole stack with one REAL artifact (pays down the
SPEC §9 "honest bet" — the shell has been mock-only through WS-5/WS-5b).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                  (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-5-BFF_bff_vertical_driver.md   (this doc)
  3. docs/specs/SPEC_PRODUCT_SHELL.md  §5 (the BFF bridge — load-bearing) + §5b + §10 (lock v1 endpoints) + §8 (WS-5-BFF row)
  4. docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md  (strangler-fig, sequencing B — BFF targets the harness)
  5. scripts/run_eval.py  (run():101 — the entry the BFF drives) + lithrim_bench/harness/{report,corpus,evalpack,ontology}.py
  6. apps/shell/src/{artifact.jsx,app.jsx,root.jsx}  (the eval-report ReportTab you wire to real data)
  7. .devloop/state/STREAM_bench-salvage.md  (WS-5/WS-5b done; S-BS-13/S-BS-16 floor gates; S-BS-17)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)" — resolve the
SIX PLAN-REVIEW DECISIONS in §3 (esp. lock the v1 endpoint surface + the Tauri defer + the
ONE live-run cost confirmation). Do not write code until I say "go".

Bundle ID: bench-salvage-phaseWS-5-BFF-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-5-BFF_bff_vertical_driver.md
```

---

## 1. Pre-flight reading (ordered)

1. `docs/specs/SPEC_PRODUCT_SHELL.md` **§5** (the React↔Python bridge — *the* load-bearing decision: a local FastAPI BFF; "one BFF, two packagings"; strangler-fig targets the harness) + **§5b** (gen-UI protocol — not this cycle) + **§10** ("BFF API surface — WS-5 defines v1 … Lock the first endpoints (run-eval, get-report, get-corpus, read/write-ontology) at plan-review") + **§8** (the amended WS-5-BFF row).
2. `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` — strangler-fig / sequencing B: the BFF targets the **harness** (which composes over live `:8002`/`:3031`); the Mongo backend is a strangled dev-dep, not a product component. **Product path is Mongo-free.**
3. The harness surface the BFF wraps — **re-grepped on HEAD `f6d99af`, CONFIRMED:**
   - `scripts/run_eval.py:101` — `def run(agent: Agent, *, live: bool = False, out_dir: str | Path | None = None) -> dict` — the entry. Pipeline (docstring `:4`): `load_agent(name) → grade(replay|live) → ground(ontology) → composite + calibration`. `--live` makes a real paid council call (warning at `run_eval.py:111`); **replay is default + $0**.
   - `lithrim_bench/harness/report.py:29` — `composite(grounded) -> dict` (the eval-report payload) · `report.py:159` — `calibration_check(records) -> dict`.
   - `lithrim_bench/harness/corpus.py:104` — `read_corpus(path=DEFAULT_CORPUS_PATH)` · `corpus.py:64` — `project(record, *, case_id)`.
   - `lithrim_bench/harness/evalpack.py:81/109/117` — `build_pack` / `dump_pack` / `load_pack`.
   - `lithrim_bench/harness/ontology.py` — the ontology data model (for read/write-ontology).
   - Agent config plane: `run_eval.py` `--agent` default `ws0_default`, `--config-db DEFAULT_CONFIG_DB` (`load_agent`).
4. `apps/shell/src/artifact.jsx` — `ReportTab` (`:5-61`) currently renders **mock** `TILES`/`FAILURE_MODES` from `data.jsx`; this is the surface you wire to real `run_eval.run()` composite output (in **Shell mode**, not the journey).
5. `apps/shell/src/{app.jsx,root.jsx}` — Shell mode is `<App/>`; the eval-report lives there.
6. `.devloop/state/STREAM_bench-salvage.md` — note **S-BS-13** (floor-apply offline replay) + **S-BS-16** (floor inject-param validation) are floor-gates — *inert here* (clinical_v1 declares no floor), but if the vertical exercises a floor-declaring ontology they apply. **S-BS-17** (domain split) will resurface (the real report is clinical; Shell chrome is customer-support).

> **Citation discipline:** the harness file:lines above were re-grepped on HEAD `f6d99af` at authoring. The shell is Vite/ESM; the BFF is Python importing `lithrim_bench`. If a signature has drifted on read, halt and surface it.

---

## 2. Deliverables (file-by-file)

The **de-risk vertical**: React (dev) → `fetch` localhost FastAPI BFF → `run_eval.run()` → `composite` → the real eval-report renders in pane 3.

1. **The FastAPI BFF** (location = PLAN-REVIEW DECISION #1; rec `apps/bff/`) — a small FastAPI app importing `lithrim_bench.harness` + `scripts/run_eval`:
   - `POST /v1/run-eval` `{agent?, live?}` → drives `run_eval.run(agent, live=...)` (replay default) → returns the run dict (grade + ground + **composite** + **calibration_check**).
   - `GET /v1/report/{run_id}` (or fold the report into the run-eval response) → `report.composite`.
   - `GET /v1/corpus` → `corpus.read_corpus` (projected rows).
   - `GET /v1/ontology` + `PUT /v1/ontology` → `ontology` read/write.
   - **Lock the exact v1 surface at plan-review (§10) — DECISION #2.**
2. **BFF dependency isolation** — FastAPI + uvicorn as an **optional extra** (e.g. `lithrim-bench[bff]`), NOT in the default Python install (mirror the `[verification]` extra pattern). `pyproject.toml` `[project.optional-dependencies] bff = [...]`.
3. **The React bridge client** — `apps/shell/src/bff.js` (or similar): a thin `fetch` wrapper to the BFF base URL (PLAN-REVIEW DECISION #3 — env var / vite proxy / localhost port).
4. **Wire the eval-report** — `apps/shell/src/artifact.jsx` `ReportTab`: render the real `composite` payload (replace the mock `TILES`/`FAILURE_MODES` import path) when a run result is present; keep a graceful empty/loading state. (Shell mode only; the journey stays mock.)
5. **First real test** — a BFF round-trip smoke test (pytest + httpx or FastAPI `TestClient`): start the app, `POST /v1/run-eval` (**replay**), assert the response carries a well-formed `composite` + `calibration_check`. **This is the shell's first behavioral test bar** (WS-5/WS-5b shipped none).
6. **README / run docs** — how to start the BFF (`uvicorn …`) alongside `vite dev`; the `[bff]` extra.

> **Tauri sidecar packaging = PLAN-REVIEW DECISION #4 (rec: DEFER to WS-5e).** The bridge is localhost HTTP and is fully demonstrable in dev (`uvicorn` + `vite dev`). Tauri sidecar lifecycle (PyInstaller bundle + `externalBin`) is a packaging long-pole better homed in WS-5e. If deferred, the WS-5-BFF close reconciles the SPEC §8 row (Tauri → WS-5e).

---

## 3. Plan-review checkpoint (non-negotiable)

Post understanding, file-by-file changes, the test + smoke plan, risks, and deviations. **Wait for "go".** Resolve these six decisions (monitor recs given):

1. **BFF location + dep isolation.** *Rec:* `apps/bff/` (product BFF, sibling to `apps/shell/`), Python importing `lithrim_bench`; FastAPI/uvicorn behind a `[bff]` optional extra so the default `pip install` is unchanged. (Alt: `lithrim_bench/bff/`.)
2. **Lock the v1 endpoint surface (§10).** Confirm the four endpoints + shapes (run-eval / report / corpus / ontology). *Rec:* fold the report into the `/v1/run-eval` response (the run dict already carries `composite` + `calibration_check`) + keep `GET /v1/corpus` + `GET/PUT /v1/ontology`. This is the **judge-capability API v1** — name it deliberately.
3. **How the React shell targets the BFF.** *Rec:* a `VITE_BFF_URL` env var (default `http://localhost:8787`) + a `vite` dev proxy; no hardcoded port in components.
4. **Tauri sidecar: now (minimal) vs defer to WS-5e.** *Rec:* **defer** — prove the bridge in dev this cycle; Tauri sidecar packaging at WS-5e. (If elected now, it expands scope materially.)
5. **The ONE live run (cost).** A1 is replay ($0); the de-risk also wants ONE real `--live` run. *Rec:* confirm `:8002`/`:3031` are up + the one-call cost at plan-review (per the WS-5 live-acceptance precedent); replay is the default demo path. **LLM-cost-conscious: one live call, inspected, not looped.**
6. **S-BS-17 within Shell mode.** The real `composite` is **clinical** (scribe/clinical_v1); the Shell chrome is customer-support mock. *Rec:* accept the split for the vertical (or reskin the Shell-mode mock to clinical — small). Surface; don't silently leave a jarring mix.

---

## 4. Scope guardrails — NOT in scope

- **NOT WS-5c** — generative-UI components / flag-severity editors / the Tailwind+shadcn foundation. (The eval-report is a *datapoint* render, not a gen-UI input widget.)
- **NOT WS-5d** — thickening the judge-council / config / corpus artifact tabs to real data (they stay mock until WS-5d *wires* them; this cycle wires **only the eval-report**).
- **NOT the full Tauri packaging / installers / PyInstaller bundle** — WS-5e (unless plan-review elects a minimal Tauri per decision #4).
- **NOT the journey (WS-5b)** — it stays a scripted mock demo; the real vertical is in **Shell mode**.
- **NO `../lithrim-backend` edits, NO Mongo.** The BFF targets the **harness** (which composes over live `:8002`/`:3031` per sequencing B). The Mongo backend is a strangled dev-dep, not a product component.
- **NO floor-declaring ontology** this cycle (keeps S-BS-13/S-BS-16 inert); the vertical uses `ws0_default`/clinical_v1 (floor-less).
- Drive-by formatting / dep bumps beyond the `[bff]` extra / "while I was here".

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1. The BFF serves a REAL eval-report (replay).** `POST /v1/run-eval` (replay) returns `run_eval.run()`'s dict with a well-formed `composite` + `calibration_check`; verify via the smoke test + a manual `curl`.
- **A2. The React eval-report renders real data (not mock).** In Shell mode, the `ReportTab` (`artifact.jsx`) shows the BFF's `composite` output (verdict/score/findings from the harness), not `data.jsx` `TILES`/`FAILURE_MODES`. Manual smoke at `:5180` (+ `uvicorn` running) — you will **halt and ask the user to run the dev server + BFF** (no autostart).
- **A3. ONE live run works (opt-in, cost-confirmed).** `POST /v1/run-eval {live:true}` makes exactly one real council call (services up, cost confirmed at plan-review) and renders. Replay stays the default.
- **A4. First test passes.** The BFF round-trip smoke test (replay) is green: `pytest <path>` (record the command).
- **A5. Dep isolation + scope.** Default `pip install` unchanged (FastAPI/uvicorn behind `[bff]`); `pytest` (existing harness suite) still green; diff confined to `apps/bff/` + `apps/shell/` + `pyproject.toml` + the test; no `../lithrim-backend`/Mongo, no floor ontology.

### Diagnostic stats (NOT gates)
- BFF cold-start time; `composite` payload size; the one live-run verdict vs the replay verdict (report, don't gate).

---

## 6. Commit structure + message templates

Atomic commits per logical unit. Suggested order:

1. **`feat(ws5-bff): FastAPI BFF over run_eval + harness (judge-capability API v1)`** — the BFF app + `[bff]` extra.
2. **`feat(ws5-bff): React bridge + wire the real eval-report into ReportTab`** — `bff.js` + `artifact.jsx` + vite proxy/env.
3. **`test(ws5-bff): BFF round-trip smoke (replay) — the shell's first behavioral test`** — the pytest smoke + README run docs.

Each commit body ends with:
```
Executed per bench-salvage-phaseWS-5-BFF-driver by session-YYYY-MM-DD-N.
See .devloop/sessions/session-bench-salvage-phaseWS-5-BFF-YYYY-MM-DD.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
(Per-repo atomic — all `lithrim-bench`. Do NOT auto-commit; stage + propose for "go". The A3 live run needs services up + a cost OK first.)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A5 PASS (BFF replay report; React renders real; one live run; smoke green; dep-isolated + scope held)
- [ ] All commits exist; working tree clean
- [ ] `pytest` (harness suite) + the new smoke test green; default `pip install` unchanged (BFF deps in `[bff]` only)
- [ ] Diff confined to `apps/bff/` + `apps/shell/` + `pyproject.toml` + test (+ README); no `../lithrim-backend`/Mongo; no floor ontology
- [ ] No services autostarted (you halt + ask the user to run `uvicorn` + `vite dev`); the ONE live `:8002` call was cost-confirmed at plan-review
- [ ] No pushes / publishes / tags
- [ ] Session log per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight (SPEC §5/§10 → `run_eval.run`/`report` → `artifact.jsx` ReportTab).
3. Re-read §2–§7.
4. Post plan-review per §3 — resolve the six decisions, especially the v1 endpoint lock + the Tauri defer + the live-run cost. Wait for "go".

---

## 9. References

- Spec: `docs/specs/SPEC_PRODUCT_SHELL.md` §5 (bridge) + §10 (endpoint lock) + §8 (WS-5-BFF row) · `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (strangler-fig)
- Harness: `scripts/run_eval.py:101` `run()` · `lithrim_bench/harness/{report,corpus,evalpack,ontology}.py`
- Shell consumer: `apps/shell/src/artifact.jsx` `ReportTab`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (WS-5/WS-5b closed; S-BS-13/16/17)
- Prior cycle: `.devloop/sessions/critique-bench-salvage-phaseWS-5b-2026-06-01.md` + the WS-5 critique (the §8 FE-first re-cut that created this phase)

---

## Hardness

- [ ] Routine cycle — monitor inline critique at close
- [x] **HARD GATE** — new architecture surface (the BFF = the judge-capability API contract, §10) + a live `:8002` side-effect on the A3 run. Fresh-critic close recommended (touches a contract/API surface per CRITIC.md triggers); the user may elect inline as for WS-5/WS-5b, but a fresh critic is the stronger call here because this LOCKS a public-ish API surface.
