# Driver — `bench-salvage` phase `DOGFOOD-1`: load real cases → judge-set ladder → run → reusable SDK-runnable eval pack

> **Generated by the monitor. The executor pastes the KICKOFF block (top) into a fresh
> Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseDOGFOOD-1-load-judgesets-run-pack-driver`
> **Version:** v1 · **Authored:** 2026-06-09 · **Last re-verified:** 2026-06-09 (HEAD `0757fd1`; recons `aaa89c12` [lithrim-backend salvage] + `a6d25955` [bench surface] + monitor read of `build_trio`).
> **Hardness:** HARD GATE-class (touches `build_trio`/the council assembly adjacent to the FROZEN `_apply_consensus`; runs PAID mixed-model councils; ships a new product surface [importer + eval-pack CI/CD gate]). Fresh-critic at close.

---

## KICKOFF (paste into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase DOGFOOD-1 (load real cases -> judge-set ladder -> run -> reusable SDK-runnable eval pack).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseDOGFOOD-1_load-judgesets-run-pack_driver.md (this doc)
  3. lithrim_bench/packager.py:125-231 (package_case row shape) + lithrim_bench/picklist.py:45-112 (load_case) + data/config/agents/s_bs_74_demo.json (agent+Dataset seed pattern)
  4. lithrim_bench/runtime/council/judges_dspy.py:52,65-69,205-253,319-380 (V2_ROLES, _ROLE_DEPLOYMENT, build_judge_lm, build_trio) + runtime/council/authored_stage.py:55-92 + scripts/run_eval.py:179-268,439-441
  5. lithrim_bench/harness/evalpack.py:85-127 (build_pack/dump_pack/load_pack) + lithrim_bench/harness/report.py:159-205 (calibration_check PASS/WARN)
  6. ../lithrim-backend/examples/ci_cd_gate.py (the pass/fail rule to replicate) + ../lithrim-backend/demo_dataset/*.jsonl (the cases to import) + scripts/validate_canonical_12_via_sdk.py:364-368 (the exit-code shape)

Then post your plan-review per EXECUTOR.md. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseDOGFOOD-1-load-judgesets-run-pack-driver
```

---

## 0. The goal (and the demo it proves)

End-to-end on the CURRENT app: **(1)** import a handful of real lithrim-backend cases onto the bench, **(2)** run them through a **judge-set ladder** — single Claude judge → 2 judges → 2-3 three-judge councils with different model mixes — **same flags assigned to each**, **(3)** run as evaluations (the honest composition contrast), **(4)** bundle into a **reusable eval pack runnable via an SDK/CLI with a CI/CD pass/fail exit**. This proves the **generic eval platform** works on real data, and stands up the **premium eval-pack-SDK** surface (the open-bench gate + `lithrim-sdk` parity). The cases are synthetic (Synthea, no PHI).

---

## 1. Pre-flight reading (ordered)

1. `lithrim_bench/packager.py:125-231` (the `package_case` row shape + admissibility D1/D3) + `lithrim_bench/picklist.py:45-112` (`load_case(case_id, source=)`) + `data/config/agents/{ws0_default,s_bs_74_demo}.json` (the agent+Dataset seed; `config.py:53-95,267-279`).
2. `lithrim_bench/runtime/council/judges_dspy.py:52` (`V2_ROLES` = the fixed 3 roles), `:65-69` (`_ROLE_DEPLOYMENT`), `:205-253` (`build_judge_lm` — `byo-claude`→`ClaudeCliLM`, else Azure), `:319-380` (`build_trio(*, models=, assignments=, ...)` — **loops `V2_ROLES`, builds all 3; the `roles=` subset does NOT exist yet**) + `authored_stage.py:55-92` (`build_authored_semantic_stage(models=)`) + `run_eval.py:179-268` (`run(..., models=, assignments=, in_process=)`) + `:439-441` (`main` reads `list_judges`).
3. `lithrim_bench/harness/judges.py:42-56,76,165,177` (`JudgeConfig`, the GLOBAL per-role store — S-BS-98; **NOT used for the dogfood's sets — call-time `models=` instead**).
4. `lithrim_bench/harness/evalpack.py:85-127` (`build_pack(pack_id, agents, *, live=, out_dir=, collections_db=)` — loops agents; **no `in_process`/`models` param yet**) + `lithrim_bench/harness/report.py:159-205` (`calibration_check(records)` → `status PASS/WARN` on verdict-match).
5. `../lithrim-backend/examples/ci_cd_gate.py` (**the rule:** `passed = reliability >= threshold AND never_event_count == 0`; `reliability = 100*verdict_matches/total`; default threshold 96; exit 0/1) + `scripts/validate_canonical_12_via_sdk.py:364-368` (`sys.exit(0 if pass else 1)` shape).
6. `../lithrim-backend/demo_dataset/{scribe,coding,triage,scheduling,intake}_scenarios.jsonl` + `eval_golden.jsonl` + `tests/eval_packs/council_v2_smoke.jsonl` (the cases; schema in §2).

> **Citation discipline:** re-grepped at HEAD `0757fd1`. Surface any drift in plan-review. **The lithrim-backend repo is at `../lithrim-backend` (read-only — never edit it).**

---

## 2. Deliverables (file-by-file, RANKED — guaranteed core first)

**D1 — the importer (MUST; the user's explicit "load lithrim-backend cases").** `lithrim_bench/importers/backend_demo.py` — `load_backend_record(row, *, pack) -> dict` (~80 lines): pass through `agent_type/transcript/artifacts/patient_profile` (default `allergies:[]`), `expected_compliance_verdict/expected_artifact_verdict/expected_safety_flags`; **taxonomy-lint each flag against `taxonomy/taxonomy_snapshot.json`** (`lithrim_bench/taxonomy.py Taxonomy.is_known`) — drop/quarantine + REPORT unknowns (expect a few name drifts, e.g. `PHI_WITHOUT_VERIFICATION` vs `PHI_DISCLOSURE_PRE_VERIFICATION`); synthesize `case_id=f"imported_{pack}_{scenario_id}"`, `pack`, `ground_truth_basis:"imported_demo"`, `injection_recipes:[]`, `clean_negative = not flags`, `split` (reuse `packager._split_for`), carry `notes`→`label_justification`. A CLI `scripts/import_backend_cases.py` reads the **5 starter cases** (below) → writes `examples/imported_demo_<domain>.jsonl` (**a SEPARATE file the strict lint EXEMPTS** — these carry no structured recipe, so they are NOT first-class by-construction rows; they are labeled, graded against their provided expected fields) + an agent seed per case (`data/config/agents/imported_<...>.json`, dataset→the jsonl row).
   - **5 starter cases:** `scribe_diabetes_soap_clean_compliant` + its `..._clean_violation` twin (HALLUCINATED_DETAIL) [`scribe_scenarios.jsonl`]; `coding_followup_99213_violation` (UPCODING_RISK) [`coding_scenarios.jsonl`]; `gold_scheduling_clean_booking_comp` [`eval_golden.jsonl`]; `council_v2_smoke_nka_clean` (the NKA clean that v1 false-flags FABRICATED_ALLERGY — a high-value calibration anchor) [`tests/eval_packs/council_v2_smoke.jsonl`].

**D2 — the judge-set ladder.**
   - **D2a (GUARANTEED-today core): the model-mix ladder via call-time `models=`.** Define 2-3 sets as dicts (a `data/config/judge_sets/*.json` or a small module): `set_all_azure = {}`, `set_claude_risk = {"risk_judge":"byo-claude"}`, `set_all_claude = {"risk_judge":"byo-claude","policy_judge":"byo-claude","faithfulness_judge":"byo-claude"}`. A "judge set" = `{label, models, assignments?, roles?}`. **No judge-store write** — pass `models=` straight to `run_eval.run(..., in_process=True)`.
   - **D2b (GUARDED extension: roster size 1/2/3).** Add `roles: Sequence[str] | None = None` to `build_trio` (`judges_dspy.py:319` — `for role in (roles or V2_ROLES)`), thread through `build_authored_semantic_stage` + `run_eval.run`. **PLAN-REVIEW GATE:** verify `_apply_consensus` (FROZEN) accepts a <3-vote subset. If YES → ship the 1/2/3-judge ladder. If the frozen consensus assumes exactly 3 → **DO NOT modify `_apply_consensus`**; ship D2a (model-mix over the fixed trio) only and open a seam for roster-size as a fast-follow. The demo's spine (the 3-judge mixes) survives either way.
   - **"Same flags added to each":** apply the same `assignments` (ontology flag set) uniformly across the sets, and demonstrate the authoring effect (assign → re-run → verdict moves), reusing the proven `assignments=` path.

**D3 — multi-case in_process pack.** Add `in_process: bool = False` (+ `models=`, `roles=`) to `build_pack` (`evalpack.py:85`, mirror `live`) and thread to `run()`. This unblocks a **multi-case** pack (in_process needs no captured baseline) carrying a judge-set. A pack record = `{schema_version, pack_id, pack_version, threshold, judge_set, cases[], expected_locked, outcomes[]}`.

**D4 — the SDK/CLI pack-gate (the premium surface + CI/CD parity).** `scripts/run_eval_pack.py` (and a `[project.scripts]` entry `lithrim-bench-pack`): load agents/a pack → `build_pack(in_process=…, models=…)` → compute `reliability = matches/total` (verdict-match per `report.py`/the backend rule: `expected in actual` if list else equality) + `never_events` (a Tier-1 flag fired on an expected-clean case, OR an expected Tier-1 flag missed) → `passed = reliability >= threshold AND never_events == 0` → print failed cases + never-events → `sys.exit(0 if passed else 1)`. **Replicate `../lithrim-backend/examples/ci_cd_gate.py` — Mongo-free, over the local engine** (reuse `calibration_check` where it fits; the exit shape from `validate_canonical_12_via_sdk.py:364`).

**D5 — the runbook + the live dogfood.** `docs/runbooks/DOGFOOD_eval_pack.md`: the exact commands to import → run the 3 sets × 5 cases (`in_process`, cost-gated) → build a pack per set → run the gate CLI → compare in `GET /v1/runs`. The USER drives the live run (cost-gated; `curl :8002/health` + `:8787/health` first, NO autostart). **Honest-Δ:** report the composition contrasts as measured (no "Claude wins" narrative; the BYO-Claude legs carry `confidence=None` by construction — disclose it).

---

## 3. Plan-review checkpoint (non-negotiable)

Post a plan (understanding · file-by-file · test plan · risks · deviations). **Wait for "go".** Resolve:
1. **D2b consensus gate** — does `_apply_consensus` accept a <3-vote roster? (Read it; report. If no → D2b degrades to a seam, D2a ships.) **NEVER modify `_apply_consensus` to force it.**
2. **Importer admissibility** — confirm imported rows land in a SEPARATE `examples/imported_demo_*.jsonl` exempt from the strict by-construction lint (they have no structured recipe); they are graded against their provided expected labels, not held to invariant #2. The committed first-class `examples/*.jsonl` + `taxonomy_snapshot.json` stay byte-untouched.
3. **Flag reconciliation** — list the backend flags that don't resolve against `taxonomy_snapshot.json`; drop/quarantine + report (do NOT add codes to the snapshot to make them fit — that's a separate cross-repo decision).
4. **The gate threshold + never-event definition** — adopt the backend default (reliability ≥ 96%, never_events == 0) or pick; make it a pack-manifest field (locked criteria), not hardcoded.
5. **Cost + run posture** — estimate the paid Azure spend for 3 sets × 5 cases `in_process` (BYO-Claude legs $0); the live run is a USER-RUN cost-gated gate; offer a `$0` smoke (replay/BYO-Claude-only) first.
6. **Scope/split** — if D1+D2a+D3+D4 is too much for one pass, propose a split (D4 SDK-gate is the natural fast-follow; D1+D2a+D3 + a manual compare is the demonstrable core). Flag it; don't silently drop.

---

## 4. Scope guardrails — NOT in scope / FROZEN

- **`_apply_consensus`** (the consensus seam) — BYTE-UNTOUCHED. D2b only ADDS `roles=` to `build_trio` (above the seam); if the seam can't take a subset, roster-size is a seam, not a seam-edit.
- **The accept-gate / `judge_metric.py`**, the per-judge seam dict, committed seeds (`ws0_default.json`), **`taxonomy/taxonomy_snapshot.json`** (the importer LINTS against it, never edits it), the committed first-class `examples/*.jsonl` (imported cases go in a NEW `imported_demo_*` file).
- **The full per-agent-judges refactor (S-BS-98 option a)** — OUT; the call-time `models=` set is the dogfood mechanism. (Re-key `(agent,role)` is the durable follow-on, not today.)
- **The lithrim-backend repo** — READ-ONLY; salvage by reading `../lithrim-backend`, never edit it. Do NOT bring its Mongo/`eval_external.py`/`build_canonical_pack.py` (pymongo/boto3) paths.
- **Capturing replay baselines (S-BS-13), locked WS-4b thresholds, the batched BFF in_process route** — OUT (the CLI runner sidesteps them).
- Drive-by formatting / dep bumps. `ruff format` only YOUR new files.

---

## 5. Acceptance criteria

- **A1 — import.** `scripts/import_backend_cases.py` emits `examples/imported_demo_*.jsonl` (5 starter cases) + agent seeds; taxonomy-lint runs + reports drops/renames; offline unit test for `load_backend_record` (a fixture row → the expected bench shape; an unknown-flag row → quarantined). No PHI committed (assert synthetic).
- **A2 — judge-set ladder runs.** The 3 model-mix sets each grade a case `in_process` via call-time `models=` (no judge-store write), each persisting a distinct run blob (`GET /v1/runs`). D2b: the 1/2/3 roster ladder runs IF `_apply_consensus` allows a subset (else documented as a seam, model-mix ships).
- **A3 — multi-case in_process pack.** `build_pack(in_process=True, models=set)` builds a pack over the 5 imported cases (no baseline needed); `dump_pack/load_pack` round-trips.
- **A4 — the CI/CD gate.** `scripts/run_eval_pack.py` (or `lithrim-bench-pack`) exits **0 on a passing pack and 1 on a failing one** (test both: a pack that meets reliability+never-events → 0; a perturbed pack [inject a verdict mismatch / a never-event] → 1). The rule matches `ci_cd_gate.py` (reliability ≥ threshold AND never_events == 0).
- **A5 — frozen + green.** `_apply_consensus` / `judge_metric.py` / `taxonomy_snapshot.json` / committed seeds + first-class `examples/*.jsonl` byte-untouched; full suite (debuglithrim + plain, baseline the 2 S-BS-96 failures in the MAIN checkout) + ruff clean on touched.
- **A-LIVE (USER-RUN, cost-gated) — the dogfood.** Per the runbook, import → 3 sets × 5 cases `in_process` → a pack per set → the gate CLI (exit code) → compare in run-history. Recorded honestly (the composition Δ as measured; BYO-Claude `confidence=None` disclosed). `$0` smoke first.

---

## 6. Commit structure

Atomic, pathspec-only (foreign `root.jsx` + 3 untracked — `git commit -- <files>`):
1. `feat(importer): lithrim-backend demo-case importer (taxonomy-linted, imported_demo corpus)` (D1)
2. `feat(judgeset): call-time model-mix sets + build_trio roles= (consensus-gated)` (D2)
3. `feat(evalpack): in_process/models pack build + the CI/CD pass-fail gate CLI` (D3+D4)
4. `docs+test(dogfood): runbook + importer/gate tests + session log` (D5)

Each body: `Executed per bench-salvage-phaseDOGFOOD-1-load-judgesets-run-pack-driver by session-2026-06-09-N.` + the `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` trailer.

---

## 7. Verification checklist

- [ ] A1–A5 PASS + A-LIVE runbook ready (user-run); the gate CLI exits 0/1 correctly
- [ ] `_apply_consensus` + `judge_metric.py` + `taxonomy_snapshot.json` + committed seeds/first-class examples byte-untouched
- [ ] Commits exist; tree clean except the foreign files; pathspec-only; lithrim-backend untouched
- [ ] Tests + ruff clean on touched; baseline S-BS-96 in the MAIN checkout
- [ ] No services autostarted (`curl /health` first); no pushes
- [ ] Session log per the template (incl. the importer drop/rename report + the honest dogfood Δ)

---

## 8. First move
1. Read `EXECUTOR.md`. 2. Read §1 pre-flight (esp. `build_trio` + `_apply_consensus` for the D2b gate, the package_case shape, and `ci_cd_gate.py`). 3. Re-read §2–§7. 4. Post plan-review per §3 — resolve the D2b consensus gate + the importer-admissibility + the scope/split first.

## 9. References
- Recons: `aaa89c12` (lithrim-backend salvage — the case schema/map + the ci_cd_gate pattern) + `a6d25955` (bench surface — the call-time judge-set finding + the evalpack/SDK gap).
- Parity: `../lithrim-backend/examples/ci_cd_gate.py` + `../lithrim-sdk/examples/run_eval_pack.py` (the premium SDK story to replicate Mongo-free).
- Memory: `byo-claude-provider-thesis` (the mixed-model legs + `confidence=None`), `persistence-blob-projection-architecture`, `git-commit-pathspec-dirty-index`, `self-asserting-loop-honesty-moat` (honest composition Δ).

## Hardness
- [x] **HARD GATE** — fresh-critic at close. Focus: `_apply_consensus` + accept-gate + snapshot byte-untouched (no manufactured pass); the gate CLI's pass/fail is correct + non-vacuous (a perturbed pack exits 1); imported cases are clearly second-class (not smuggled into the strict lint); the composition Δ is honest.
