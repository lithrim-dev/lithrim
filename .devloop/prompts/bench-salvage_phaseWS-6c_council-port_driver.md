# Driver — `bench-salvage` phase `WS-6c`: PORT the v2 council Mongo-free into `lithrim_bench/runtime/council/` (v2-only)

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — ROOTED IN `lithrim-bench` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-council-port-driver`
> **Version:** v1 · **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (`lithrim-backend` re-grepped on `mvp-ready` @ `493b533`, per MONITOR §Phase 1a)
>
> **This is WS-6 step c** — the first BUILD phase of the consolidation track. It **PORTS the
> validated v2 council Mongo-free** (extends the M1 v1 in-process salvage → v2) into the bench's
> in-process runtime, per `docs/specs/RECOMPOSITION_PLAN_ws6.md` §1/§5/§6 and the WS-6b ratification.
> **PORT, not rewrite** — the consensus/tier/owner/NKA/llama-veto math is validated IP; reproduce it
> verbatim. Gated-by WS-6b (ratified). Gates WS-6c-DSPy + WS-6c-AGENTIC + WS-6d.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c
(PORT the v2 council Mongo-free into lithrim_bench/runtime/council/, v2-only).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench) — the port TARGET + your session log land HERE.
You READ the sibling ../lithrim-backend repo READ-ONLY at the PINNED commit 493b533 (branch
mvp-ready). Change NO code in ../lithrim-backend. PORT FROM 493b533, NOT from the stale parked
mirror lithrim_bench/runtime/council/ (it DIFFERS from backend — S-BS-28).

Read in this order:
  1. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/personas/EXECUTOR.md   (your role)
  2. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6c_council-port_driver.md   (this doc)
  3. /Users/aregee/Workspace/github.com/lithrim-bench/docs/specs/RECOMPOSITION_PLAN_ws6.md   (§1 end-state, §5 PRESERVE-AS-IP, §6 hybrid, §Ratification = the locked decisions)
  4. ../lithrim-backend/app/services/compliance_council.py   (@493b533 — the PORT-IP source; consensus :1799/:1812/:1853, tables :176/:201/:212/:279, extract_verdict_confidence:413, build_prompt:517)
  5. ../lithrim-backend/app/models/safety_flags.py   (@493b533 — the SECOND tables file: FailureType:533, SAFETY_FLAG_TO_FAILURE_TYPE:543, FAILURE_TYPE_SEVERITY:570, get_flag_prompt_section:621)
  6. ../lithrim-backend/app/workflows/compliance_workflow.py:2206 + :2281   (the workflow-side recompute that MUST port in lockstep with _apply_consensus)
  7. ../lithrim-backend/app/services/llm_provider.py   (@493b533 — the SALVAGE infra: _resolve_model:38, get_sync/async_openai_client:97/:121, Azure deployment ids)
  8. /Users/aregee/Workspace/github.com/lithrim-bench/lithrim_bench/runtime/council/   (the stale parked package you REFRESH from 493b533) + lithrim_bench/harness/grade.py + scripts/run_eval.py (the frozen seam)

Then post your plan-review per EXECUTOR.md — resolve the 5 PLAN-REVIEW DECISIONS in §3.
Do not write code until I say "go". HARD GATE: fresh-critic at close.

Bundle ID: bench-salvage-phaseWS-6c-council-port-driver
Driver doc: …/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6c_council-port_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** every file:line below was re-grepped against
> `lithrim-backend@493b533` on 2026-06-01 (CONFIRMED unless noted). Re-grep yourself; if a line has
> drifted, surface it. **Port from `493b533`** (pin it), never from the stale parked mirror.

1. `docs/specs/RECOMPOSITION_PLAN_ws6.md` — **§5 PRESERVE-AS-IP** (the exact port-IP list + the 4 critical traps), **§6 hybrid** (the per-judge dict seam), **§Ratification** (locked decisions: v2-only, Q2 verified, agents/KB deferred). This is your contract; the driver is the index.
2. `../lithrim-backend/app/services/compliance_council.py` @493b533 — **PORT verbatim:** `_worst_of_verdicts:1799`, `_compose_council_verdict_v2` (llama-veto) `:1812`, `_apply_consensus:1853` (Tier-1 one-strike at `:2050`), tables `TIER_1_NEVER_EVENTS:176` / `TIER_2_HIGH_RISK:201` / `TIER_3_MEDIUM:212` / `KNOWN_TAXONOMY_CODES:279` / `_TIER1_OWNERS`, `extract_verdict_confidence:413` (returns None for Mistral — NEVER coerce to 1.0/0.0), `class ComplianceCouncil:453`, `build_prompt:517`, `JUDGES_V2`. **Mongo-free (CONFIRMED 0 DB hits).**
3. `../lithrim-backend/app/models/safety_flags.py` @493b533 — the **SECOND tables file** (S-BS-28 split-tables trap): `SafetyFlagCategory:19`, `SafetyFlagDefinition:28`, `FailureType:533`, `SAFETY_FLAG_TO_FAILURE_TYPE:543`, `FAILURE_TYPE_SEVERITY:570`, `get_flag_prompt_section:621`. **Both files travel together** — porting only `compliance_council.py` loses flag prompt-text + FailureType.
4. `../lithrim-backend/app/workflows/compliance_workflow.py:2206` `_recompute_consensus_from_council` + `:2281` `_recompute_final_verdict` — the defensive recompute that MUST mirror `_apply_consensus` exactly (incl. BRS-3 None-confidence tolerance). **Port in lockstep** (plan §5).
5. `../lithrim-backend/app/services/llm_provider.py` @493b533 — the **SALVAGE infra** (port-as-is): `_resolve_model:38`, `get_sync_openai_client:97`, `get_async_openai_client:121`, the Azure deployment ids `:41/:46/:57/:64`. Infra-pure (no Mongo/celery/fastapi). **Q2 VERIFIED:** the product Azure resource hosts gpt-4.1 + Mistral + Llama, accessible.
6. `lithrim_bench/runtime/council/` — the **stale parked package** you refresh (full mirror: `compliance_council.py`, `safety_flags.py`, `llm_provider.py`, `council_roles/`, `settings.py`). It DIFFERS from 493b533 (S-BS-28); **re-derive from 493b533**, do not trust it.
7. `lithrim_bench/harness/grade.py` (`grade_replay:40`, `grade_live:85`) + `scripts/run_eval.py:run():101` — the **frozen seam**. WS-6c does NOT re-point grade to in-process (that is WS-6c-AGENTIC). Keep these untouched.
8. `taxonomy/taxonomy_snapshot.json` (@493b533, re-snapshotted WS-6b) + `scripts/snapshot_taxonomy.py:69` (the `production_judges` hardcode → fix per S-BS-30).

---

## 2. Deliverables (file-by-file)

1. **`lithrim_bench/runtime/council/compliance_council.py`** — refresh from `493b533`. PORT the consensus IP verbatim: `_apply_consensus`, `_compose_council_verdict_v2` (llama-veto), `_worst_of_verdicts`, the tier tables + `KNOWN_TAXONOMY_CODES` + `_TIER1_OWNERS`, `extract_verdict_confidence` (None-tolerant), `build_prompt`, `class ComplianceCouncil`, `JUDGES_V2`. **Mongo-free** (no `app.database`, no motor). v1/v2 branch ported verbatim, **default v2** (decision #2).
2. **`lithrim_bench/runtime/council/safety_flags.py`** — refresh from `493b533/app/models/safety_flags.py` (the second tables file). FailureType + SAFETY_FLAG_TO_FAILURE_TYPE + FAILURE_TYPE_SEVERITY + get_flag_prompt_section + SafetyFlagDefinition/Category.
3. **`lithrim_bench/runtime/council/llm_provider.py` + `council_roles/`** — refresh the salvaged Azure factory (`_resolve_model`/`get_sync`/`get_async`) from `493b533`; port the **v2 role prompts** (`risk_judge.txt`, `policy_judge.txt`, `faithfulness_judge.txt`); `behavior_judge.txt` + `source_message_judge.txt` are v1/declared-not-running (carry but mark not-in-v2-trio). Config subset (the 5 `AZURE_OPENAI_DEPLOYMENT_*` + `COMPLIANCE_COUNCIL_VERSION`) into `runtime/council/settings.py`. **v2-only** (Q1).
4. **`scripts/snapshot_taxonomy.py`** (S-BS-30) — derive `production_judges` from the v2 council config (= `risk/policy/faithfulness_judge`), drop the `policy/risk/behavior_judge` hardcode at `:69`; re-snapshot so `taxonomy_snapshot.json` `production_judges` matches the v2 trio. Keep the structural_codes preservation (WS-6b fix).
5. **`lithrim_bench/runtime/council/tests/` (offline)** — the regression oracle: fixture per-judge outputs → expected consensus verdict, covering **Tier-1 one-strike** (owner OR ≥2 evidence-judges), **Tier-2 needs 2+**, **PHI-FP suppression**, **NKA exception** (NKA/NKDA ≠ FABRICATED_ALLERGY), **llama-veto**, **artifact-BLOCK override**, **None-confidence (Mistral) tolerance**. Pure-Python, no network. Plus **exactly ONE cost-confirmed live Azure smoke** (the v2 trio on one case → verdict matches the offline expectation).
6. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-2026-06-01.json`** — session log incl. the **port-fidelity diff** (the ported consensus functions vs `493b533`) + the live-smoke verdict + per-judge confidences.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `EXECUTOR.md`, post understanding + file-by-file plan + test plan + risks + deviations. **Wait for "go".** Resolve:

1. **Port target layout** — refresh the existing parked `lithrim_bench/runtime/council/` package in place (re-derive each file from `493b533`) vs a new module path? *(Default: refresh in place; it is the M1-vendored package this extends.)*
2. **v2-only handling** — strip the v1 branches during the port, or port the `COMPLIANCE_COUNCIL_VERSION` branch verbatim and default to v2 (test v2 only)? *(Default: port verbatim + default v2 — preserves IP fidelity, avoids surgical risk; v1-pruning deferred.)*
3. **The hybrid seam (§6)** — port the council's internal judge fan-out as-is and just DOCUMENT the per-judge dict boundary (`{decision, findings, confidence, evidence_spans}`) that `_apply_consensus` consumes, or refactor an explicit seam now? *(Default: port as-is + document; the explicit DSPy-judge seam lands at WS-6c-DSPy. Do NOT pre-refactor.)*
4. **S-BS-30 in this cycle** — fix `snapshot_taxonomy.py` `production_judges` derivation + re-snapshot here (v2-only makes the trio concrete), or split it out? *(Default: fix here.)*
5. **Live-validation scope + cost** — offline consensus tests + exactly ONE cost-confirmed live Azure smoke (v2 trio, one case). Confirm the cost envelope before the live call. *(Default: 1 live smoke; offline-first.)*

---

## 4. Scope guardrails — NOT in scope

- **NO LangGraph recompose, NO agents recompose** — that is **WS-6c-AGENTIC**. WS-6c lands the council PACKAGE + tests only.
- **NO DSPy judge rebuild** — that is **WS-6c-DSPy**. Port the imperative judge fan-out as-is; only DOCUMENT the per-judge dict seam.
- **NO persistence swap / repository interface / Mongo** — that is **WS-6d**. The council is Mongo-free already; keep it so.
- **NO KB/RAG / pinecone / ONNX** — that is **WS-6d-KB** (deferred per Q5).
- **NO harness grade-seam re-point** — `grade.py`/`run_eval.py` stay untouched; the in-process grade path lands at **WS-6c-AGENTIC**. WS-6c is importable + tested, not yet wired into `grade`.
- **NO v1-pruning surgery** beyond defaulting to v2 (decision #2); **PORT-not-rewrite** — reproduce the consensus IP verbatim, do not "improve" it.
- **Port FROM `lithrim-backend@493b533`**, NOT the stale parked mirror (**S-BS-28**); both tables files; never hand-edit `taxonomy_snapshot.json`.
- **NO contract change** — behind `run_eval.run` / `report.composite` / §10 (frozen).
- Change **NO code in `../lithrim-backend`** (read-only) and **NO `etlp-mapper`**.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** `lithrim_bench/runtime/council/` **imports clean on default bench deps** (council is pure-Python; the Azure client is lazy/optional) — verify offline, no network.
- **A2.** The consensus IP is a **byte-faithful PORT** of `493b533` (`_apply_consensus`, `_compose_council_verdict_v2`, `_worst_of_verdicts`, the tables, `extract_verdict_confidence`) — the session log carries a diff-vs-source showing only mechanical adaptation (imports/paths), no logic change.
- **A3.** The **offline consensus test suite is GREEN**: fixture per-judge inputs → expected verdict for Tier-1 one-strike, Tier-2 2+, PHI-FP suppression, NKA exception, llama-veto, artifact-BLOCK override, and None-confidence (Mistral) tolerance.
- **A4.** **v2-only**: the production trio is `risk/policy/faithfulness_judge`; `scripts/snapshot_taxonomy.py` derives `production_judges` from the v2 config (S-BS-30 closed); `taxonomy_snapshot.json` is consistent (re-snapshot, structural_codes preserved, lint green).
- **A5.** **One live Azure smoke**: the v2 trio produces a verdict on one case that matches the offline/replay expectation (record the verdict + per-judge confidences + the cost).
- **A6.** **No contract change** — `grade.py` / `run_eval.run` / `report.composite` / §10 untouched; the council is importable, not yet wired into the grade seam.
- **A7.** **Scope held** — no LangGraph/agents/persistence/KB/DSPy work; ported from `493b533` not the mirror; no `../lithrim-backend` or `etlp-mapper` edits.

### Diagnostic stats (NOT gates)
- The port-fidelity diff summary (ported lines vs `493b533`); the live-smoke verdict + per-judge confidences + cost; the v1-branch disposition (ported-verbatim vs pruned).

---

## 6. Commit structure + message templates

Atomic per-concern, in `lithrim-bench`. Suggested order:
1. `feat(council-port): v2 consensus IP Mongo-free (tiers/owners/veto/NKA) from 493b533` — `runtime/council/compliance_council.py` + `safety_flags.py`
2. `feat(council-port): salvaged Azure provider + v2 role prompts (v2-only)` — `runtime/council/llm_provider.py` + `council_roles/` + `settings.py`
3. `fix(taxonomy): derive production_judges from v2 config (S-BS-30) + re-snapshot` — `scripts/snapshot_taxonomy.py` + `taxonomy/taxonomy_snapshot.json`
4. `test(council-port): offline consensus oracle + 1 live Azure smoke` — `runtime/council/tests/`

Each body ends with `Executed per bench-salvage-phaseWS-6c-council-port-driver by session-…` + the `See …session-…json` line + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **Diagnose-before-edit:** any fix/root-cause claim carries a fenced evidence block. Do NOT auto-commit — stage + propose each group for "go".

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A7 PASS (imports clean offline; consensus byte-faithful; offline oracle green; v2-only + S-BS-30; 1 live smoke matches; no contract change; scope held)
- [ ] Ported FROM `493b533` (pinned), NOT the stale parked mirror; both tables files present
- [ ] `_recompute_consensus_from_council` semantics preserved (lockstep) if/where ported
- [ ] No services autostarted (user starts them); the ONE live Azure call cost-confirmed first
- [ ] No `grade.py` / `run_eval.py` / §10 change; no `../lithrim-backend` / `etlp-mapper` edit; no pushes/tags
- [ ] Session log written (incl. port-fidelity diff + live-smoke verdict)

---

## 8. First move

1. Read `EXECUTOR.md` + the plan §5/§6/§Ratification.
2. `git -C ../lithrim-backend rev-parse --short HEAD` → confirm `493b533`; re-grep the §1 anchors.
3. Read §2–§7.
4. Post plan-review per §3 (the 5 decisions + the cost envelope for the live smoke). Wait for "go".

---

## 9. References

- Plan: `docs/specs/RECOMPOSITION_PLAN_ws6.md` (§5 PRESERVE-AS-IP, §6 hybrid, §Ratification)
- Topology: `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (§WS-6c; PORT-not-rewrite; sequencing B)
- PORT-IP source @493b533: `../lithrim-backend/app/services/compliance_council.py` + `app/models/safety_flags.py` + `app/prompts/council_roles/*.txt`
- Lockstep recompute: `../lithrim-backend/app/workflows/compliance_workflow.py:2206/:2281`
- SALVAGE infra: `../lithrim-backend/app/services/llm_provider.py` + `app/config.py`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (seams S-BS-24 / S-BS-28 / S-BS-30)
- Prior cycle: `.devloop/sessions/session-bench-salvage-phaseWS-6a-2026-06-01.json` + the WS-6b ratification
- Next in track: **WS-6c-DSPy** (judge rebuild, wraps this ported consensus) ∥ **WS-6c-AGENTIC** (LangGraph recompose, calls this council in-process)

---

## Hardness

- [x] **HARD GATE** — ports validated safety-critical IP (the council consensus) into the product runtime; v2-only commits the cross-provider trio. Fresh-critic at close (spawn a session with `KICKOFF_CRITIC.md`); the diagnose-before-edit port-fidelity diff (A2) is load-bearing. User MAY elect inline, but a fresh-critic is recommended for a contract-bearing port.
