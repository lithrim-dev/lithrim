# Driver — `bench-salvage` phase `WS-6a`: backend baseline — curate + commit the pending `../lithrim-backend` WIP

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — rooted in `../lithrim-backend` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6a-backend-baseline-driver`
> **Version:** v1 · **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (`../lithrim-backend` git status re-grepped on branch `mvp-ready`)
>
> **This is WS-6 step a** — the clean-baseline precondition for the WS-6 consolidation track
> (you can't cleanly audit/strangle a dirty tree). It is a **curation cycle** — diagnose +
> commit *existing* uncommitted work — **not** a build cycle. Strangle-not-fix still holds:
> we **baseline + preserve** the work, we do **not** add new Mongo/backend features.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `../lithrim-backend`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6a
(backend baseline — curate + commit the pending ../lithrim-backend WIP).

IMPORTANT: open this session in the lithrim-backend repo
(/Users/aregee/Workspace/github.com/lithrim-backend) — that is where the commits land.
The .devloop scaffold (this driver + your session log) lives in the SIBLING bench repo
(/Users/aregee/Workspace/github.com/lithrim-bench/.devloop/).

Read in this order:
  1. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/personas/EXECUTOR.md   (your role)
  2. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6a_backend-baseline_driver.md   (this doc)
  3. ./CLAUDE.md  (the BACKEND repo's conventions + the DIAGNOSE-BEFORE-EDIT gate — load-bearing here)
  4. `git status` + `git diff HEAD` + `git log --oneline -8`  (the WIP you are curating + its commit context)
  5. /Users/aregee/Workspace/github.com/lithrim-bench/docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md  (strangle-not-fix — why this is baseline-and-preserve, not invest)

Then post your plan-review per EXECUTOR.md (resolve the FIVE PLAN-REVIEW DECISIONS in §3 —
especially the commit grouping, the gitignore set, and what to do if the structural-verdict
WIP is incomplete). Do not commit anything until I say "go".

Bundle ID: bench-salvage-phaseWS-6a-backend-baseline-driver
Driver doc: …/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6a_backend-baseline_driver.md
```

---

## 1. Pre-flight reading (ordered)

1. `../lithrim-backend/CLAUDE.md` — the backend conventions + the **diagnose-before-edit gate** (post verbatim evidence — the diff — before any "this WIP is X / safe to commit" claim; tag CONFIRMED / INFERRED / HYPOTHESIS).
2. The actual WIP — re-run `git status` + `git diff HEAD` + `git log --oneline -8` yourself. **Monitor snapshot (HEAD on `mvp-ready`, 2026-06-01) — re-verify, do not trust:**
   - **Modified (8):** `app/routes/pipeline.py` · `app/services/artifact_evaluator.py` · `app/services/pipeline/{models,orchestrator,stages}.py` · `scripts/phase4_pilot/cached_uscore_condition_validator.json` · `tests/eval_packs/council_v2_smoke.jsonl` · `tests/services/test_artifact_evaluator_severity.py` (~145 insertions). **INFERRED** (from `43b20f6` "severity-aware BLOCK on required-field failure (P0-1)" + `7017ab0` "eval-report + verification-surface remediation driver"): this is the **structural-verdict / severity remediation** continuation — the S-P1-15 / FHIR-extension fix (severity=HIGH → status=BLOCK). **Moat/paper-relevant; preserve.** Confirm by reading the diff.
   - **Untracked docs:** `docs/{CLEAN_EVAL_ENABLEMENT_PLAN, EVAL_BENCHMARK_AND_DETERMINISM_SPEC, PAPER_OUTLINE, PIPELINE_GRADING_AUDIT}.md` · `docs/dev-workflow/` · `docs/handoffs/{BACKEND_RELIABILITY_AUDIT…, E2E_ANALYZE…}.md` · `docs/research/REPORT_analyze_production_overblock…`.
   - **Untracked scripts/tests:** `scripts/{migrations/relabel_scribe_demo…, q4_analyze_overblock…, rerun_council_v2_smoke_external_validate}.py` · `tests/routes/test_audit_view_surface_fixes.py` · `tests/services/pipeline/test_council_error_flag.py`.
   - **Untracked transients:** `out/` · `test-results/` — **likely gitignore, not commit.**
   - **⚠️ Flag:** `docs/EVAL_BENCHMARK_AND_DETERMINISM_SPEC.md` + `docs/PAPER_OUTLINE.md` are **also lithrim-bench docs** — likely stray copies in the backend repo. Decide keep / remove / gitignore (§3 #3).
3. `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (bench) — **strangle-not-fix**: we are *baselining + preserving* this WIP (so the tree is clean for the WS-6b audit + the work isn't lost), **not** investing in new backend features.

> **Citation discipline:** the snapshot above is from the monitor's 2026-06-01 read. Re-grep `git status`/`git diff` yourself — if it has drifted, surface it. Every commit-vs-gitignore-vs-stash disposition must cite the diff (diagnose-before-edit).

---

## 2. Deliverables

1. **D0 — Triage manifest.** For every modified + untracked path, classify its disposition: **commit (which atomic group)** / **gitignore** / **stash (with reason)** / **remove (stray)**. Back each with a one-line read of its diff/content. This is the diagnose-before-edit artifact; it goes in the session log.
2. **D1 — Backend tests green BEFORE any commit.** Run the backend suite (per `../lithrim-backend/CLAUDE.md`). The WIP includes test changes (`test_artifact_evaluator_severity` +49, `council_v2_smoke.jsonl`, two new test files) — confirm they pass. **If the structural-verdict WIP is incomplete/red:** surface it (§3 #4) — do **NOT** build new functionality to finish it; commit-as-coherent-checkpoint or stash per plan-review.
3. **D2 — `.gitignore` the transients** (`out/`, `test-results/`, + any other generated dir the diff reveals).
4. **D3 — Resolve the stray docs** (the bench-duplicated `EVAL_BENCHMARK_AND_DETERMINISM_SPEC.md` / `PAPER_OUTLINE.md`): keep (backend-relevant) / remove (stray copy) / gitignore — per §3 #3.
5. **D4 — Atomic per-concern commits** on `mvp-ready` (per-repo atomic; user's "go"). Suggested groups (confirm at plan-review):
   - `fix(structural-verdict): …` — the `pipeline/*` + `artifact_evaluator` + severity test (the remediation; name the actual fix — likely S-P1-15).
   - `test(…): …` — the standalone new tests (`test_audit_view_surface_fixes`, `test_council_error_flag`) if separable.
   - `docs(…): …` — the audits / specs / handoffs / research reports (backend-authored).
   - `chore(…): …` — the migration/analysis scripts + `.gitignore`.
6. **D5 — Session log** at `…/lithrim-bench/.devloop/sessions/session-bench-salvage-phaseWS-6a-2026-06-01.json` (include the D0 triage manifest).

---

## 3. Plan-review checkpoint (non-negotiable)

Post understanding + the **triage manifest (D0)** + the commit plan + risks + deviations. **Wait for "go".** Resolve:

1. **Commit grouping** — how many atomic commits + the exact file→commit mapping (esp. the `fix(structural-verdict)` boundary).
2. **gitignore set** — confirm `out/` + `test-results/` (+ anything else generated).
3. **Stray bench-docs** (`EVAL_BENCHMARK_AND_DETERMINISM_SPEC.md`, `PAPER_OUTLINE.md`) — keep / remove / gitignore? (Default: if byte-identical to the bench copies, they're stray → remove or gitignore; if backend-edited, keep + commit.)
4. **Structural-verdict completeness** — is the modified code a coherent, tests-green unit? If yes → commit. If incomplete/red → **commit-as-checkpoint** (with a WIP note) vs **stash** vs **finish-the-minimal-bit-to-green**. *Strangle-tension: do NOT over-invest in completing backend features — preserve + baseline only.*
5. **Branch** — commit on `mvp-ready` (current). Confirm that's intended (not `main`).

---

## 4. Scope guardrails — NOT in scope

- **NO new backend features; NO finishing half-done WIP** beyond the minimal change to make the suite green (if something's broken, **surface it**, don't build).
- **NO de-Mongo / strangle / council-port work** — that's WS-6c/WS-6d.
- **NO `lithrim-bench` code edits** — only this cycle's session log (under bench `.devloop/`).
- **NO `etlp-mapper` edits.**
- **Do NOT "fix" pre-existing backend issues** you discover while reading — log them as seams (S-…) for a later cycle; this cycle commits *what's already there*.
- No drive-by reformatting of files you aren't otherwise touching.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** `../lithrim-backend` `git status` is **clean** — every path committed, gitignored, or explicitly stashed with a logged reason.
- **A2.** The backend test suite is **green** (record the command + result); any incomplete WIP is stashed/checkpointed with a logged reason — **nothing committed broken**.
- **A3.** Commits are **atomic per-concern** (per-repo); every commit's contents are backed by the D0 triage manifest (diagnose-before-edit — no untagged "this is X" claims).
- **A4.** Transients (`out/`, `test-results/`) are **gitignored, not committed**; the stray bench-docs are resolved.
- **A5.** **No `lithrim-bench` code touched** (only the session log); **no `etlp-mapper` touched**.

### Diagnostic stats (NOT gates)
- The D0 triage manifest (path → disposition); a one-paragraph summary of what the structural-verdict change fixes (cite the diff — likely S-P1-15).

---

## 6. Commit structure + message templates

Atomic per-concern, on `mvp-ready`. Each body ends with:
```
Executed per bench-salvage-phaseWS-6a-backend-baseline-driver by session-YYYY-MM-DD-N.
See …/lithrim-bench/.devloop/sessions/session-bench-salvage-phaseWS-6a-2026-06-01.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
**Diagnose-before-edit:** any commit body asserting a fix/root-cause carries a fenced evidence block (the diff line / test result). Do NOT auto-commit — stage + propose each group for the user's "go".

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A5 PASS (tree clean; suite green; atomic per-concern; transients gitignored; no bench/etlp edits)
- [ ] All commits exist on `mvp-ready`; `git status` clean
- [ ] Backend suite re-run green (record command); the D0 manifest justifies every commit
- [ ] No `lithrim-bench` code / no `etlp-mapper` touched
- [ ] No pushes / publishes / tags
- [ ] Session log written (incl. the triage manifest)

---

## 8. First move

1. Read `EXECUTOR.md` + the backend `CLAUDE.md` (the diagnose-before-edit gate).
2. Re-grep `git status` / `git diff HEAD` / `git log` — build the D0 triage manifest.
3. Read §2–§7.
4. Post plan-review per §3 (manifest + commit plan + the 5 decisions). Wait for "go".

---

## 9. References

- Topology spec (this is WS-6 step a): `…/lithrim-bench/docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (the consolidation track / phased breakdown)
- Backend conventions + diagnose-gate: `../lithrim-backend/CLAUDE.md`
- WIP commit context: backend `43b20f6` (severity-aware BLOCK) + `7017ab0` (eval-report + verification-surface remediation driver)
- Stream state: `…/lithrim-bench/.devloop/state/STREAM_bench-salvage.md` (WS-6 row; the S-P1-15 / structural-verdict seams in `STREAM_paper-1-copilot.md`)
- Next in the track: **WS-6b** (consolidation audit — unblocked once this baseline is clean)

---

## Hardness

- [x] **HARD GATE** — touches the backend repo + lands commits there (the STREAM WS-2 note: "backend commits need their OWN fresh-critic pass"). **But this is a curation cycle** (committing *diagnosed existing* work, not new design), so the **diagnose-before-edit evidence (the D0 manifest)** is the load-bearing discipline; an **inline critique at close likely suffices** (the contract isn't being designed). User decides fresh-critic vs inline at close. The diagnose-before-edit gate is non-negotiable either way.
