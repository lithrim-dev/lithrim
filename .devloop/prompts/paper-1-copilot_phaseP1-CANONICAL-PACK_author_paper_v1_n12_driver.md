# Driver — `paper-1-copilot` phase `P1-CANONICAL-PACK`: author `paper_v1_n12_canonical` eval pack

> **Generated by the monitor 2026-05-28.** Executor pastes the KICKOFF
> block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.
>
> **Bundle ID:** `paper-1-copilot-phaseP1-CANONICAL-PACK-author-paper-v1-n12-driver`
> **Version:** v1
> **Authored:** 2026-05-28
> **Last re-verified against code:** 2026-05-28
> **Hardness:** routine (no spec/contract files touched; backend schema extension is additive)
> **Cross-repo:** lithrim-bench (cases + author script) + lithrim-backend (Mongo writes via direct insert)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: paper-1-copilot phase P1-CANONICAL-PACK (author paper_v1_n12_canonical eval pack).

Goal: author eval_pack=paper_v1_n12_canonical in lithrim-backend's Mongo, with all 12 case_ids from the bench's canonical picklist, the Path T closing condition (picklist-taxonomy-widening contract) locked explicitly, and one calibration eval_run to confirm the pack lands intact. AFTER this cycle closes, P1-CANONICAL-N5-PILOT (a separate, queued cycle) will run the N>=5 paper-bearing dispersion measurement on C1+C2.

This cycle WRITES TO MONGO (eval_pack + 12 eval_case docs); writes are NOT API-mediated because no POST /eval-cases route exists today (gap noted as S-P1-19). Direct insert via motor/pymongo, with explicit `source_type="manual"`. NO backend code edits required if the schema-handling decision (see §3) is "use existing schema with placeholder S3 keys"; minimal backend code edits (1-2 lines) if the decision is "make transcript_s3_key Optional + add inline transcript field."

Read in this order:
  1. .devloop/personas/EXECUTOR.md      (your role)
  2. .devloop/prompts/paper-1-copilot_phaseP1-CANONICAL-PACK_author_paper_v1_n12_driver.md   (this doc; reading the kickoff already)
  3. /tmp/pilot_picklist.json           (12 picklist rows under test — exact case_ids that must land in the pack)
  4. ../lithrim-backend/app/models/eval_case.py  (the EvalCase Pydantic model — what fields are required, what's Optional)
  5. ../lithrim-backend/app/models/eval_pack.py  (the EvalPack model)
  6. docs/research/REPORT_canonical_12_validation_2026-05-28.md §2 + §5  (per-case promotion verdicts + taxonomy-widening contract source)
  7. docs/research/REPORT_s_p1_14_triage_2026-05-28.md §10 (S-P1-14 closure — confirms C1+C2 promotable)
  8. .devloop/state/STREAM_paper-1-copilot.md (current stream state + open seams S-P1-12..17 except 14,18)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do NOT write to Mongo until I say "go". Do NOT edit backend code without explicit user approval in plan-review.

Bundle ID: paper-1-copilot-phaseP1-CANONICAL-PACK-author-paper-v1-n12-driver
Driver doc: .devloop/prompts/paper-1-copilot_phaseP1-CANONICAL-PACK_author_paper_v1_n12_driver.md
```

---

## 0. Context — what just happened, what this cycle does

S-P1-14 closed 2026-05-28 (lithrim-backend `e8147d8` NKA propagation patch + lithrim-bench `bb2384c` reverify). Both clean negatives (C1, C2) lifted out of BLOCK on the patched backend — C1 to PASS, C2 to WARN/needs_review (matching offline bench exactly). On-backend FP rate on cleans is now **0/2 at single-sample measurement** — paper §5.5 corrective claim replicates.

This means the full 12-case picklist is now promotion-ready into a canonical pack: S1-S6 + S8 + M1 + M2 as PROMOTE / PROMOTE-WITH-RELAX, S7 as KEEP-AS-LIMITATION at `needs_review`, C1 + C2 as PROMOTE. No case is blocked.

The pack this cycle authors becomes the foundation for:
- Paper §5 N-per-case stability measurement (P1-CANONICAL-N5-PILOT — next cycle)
- Paper §6 worst-of composition demonstration (uses same pack)
- All future regressions against the production backend (paper-bearing CI)

**This cycle authors the pack — it does NOT run the paper-N campaign.** That's deliberately separated so the pack-authoring artifact is independently auditable before any expensive measurement runs.

### Path T closing condition (load-bearing contract)

P1-VALIDATE-12 closed on Path T (triage-relaxed A3 reading) per user election 2026-05-28. The closing condition was: **"the picklist-taxonomy-widening contract becomes an explicit P1-CANONICAL-PACK driver requirement, not implicit."**

This cycle must implement that contract. Concretely: every eval_case in the new pack must carry per-case `expected_safety_flags_strict` (the picklist's original strict list) AND `expected_safety_flags_accepted_substitutes` (per-flag substitute lists for taxonomy codes the backend semantically emits but with different labels). The harness's `_grade()` flag-match gate evolves to honor the substitutes.

Without this, the picklist contract regresses to implicit case-by-case judgment that bit P1-VALIDATE-12.

---

## 1. Pre-flight reading (ordered)

Open in this order before posting plan-review:

1. `.devloop/personas/EXECUTOR.md` — your role
2. `.devloop/prompts/paper-1-copilot_phaseP1-CANONICAL-PACK_author_paper_v1_n12_driver.md` — this driver (you're reading it)
3. **`/tmp/pilot_picklist.json`** — the 12 picklist rows. Note each row's `case_id`, `pack`, `pick_label`, `expected_compliance_verdict`, `expected_safety_flags`, `expected_structural_verdict`, `clean_negative`, `multi_defect`. These are the source-of-truth fields the pack must preserve.
4. `../lithrim-backend/app/models/eval_case.py` — full file. Note REQUIRED fields (`source_conversation_id`, `transcript_s3_key`, `expected_verdict`, `created_by`) and Optional ones (`expected_artifacts`, `artifacts_s3_key`, `metadata`). `EvalCaseSourceType = Literal["production", "simulation", "manual"]` — use `"manual"`.
5. `../lithrim-backend/app/models/eval_pack.py` — EvalPack model.
6. `docs/research/REPORT_canonical_12_validation_2026-05-28.md` §2 (per-case promotion-recommendation table) + §5 (recommended-pack composition + picklist taxonomy widening rationale per case).
7. `docs/research/REPORT_s_p1_14_triage_2026-05-28.md` §10 (S-P1-14 closure — confirms C1+C2 promotable; provides on-backend judge-vote data per case).
8. `out/canonical_12_sdk_validation.ndjson` (12 actual on-backend semantic outputs per case from P1-VALIDATE-12 — useful when designing the `accepted_substitutes` lists; you'll see exactly which taxonomy codes the backend emits per case).
9. `out/s_p1_14_c1c2_rerun.ndjson` (post-patch outputs for C1+C2).
10. `lithrim_bench/synthea_loader.py` + `lithrim_bench/encounter_spec.py` — the case-shape source. Each picklist case_id traces back through `synthea_provenance.{patient_id, encounter_id}` to a Synthea encounter.
11. `.devloop/state/STREAM_paper-1-copilot.md` — current stream + open seams.

> **Citation discipline:** every file:line above was re-verified against live code on 2026-05-28. If you spot drift on read, halt and surface it.

---

## 2. Deliverables (file-by-file)

### 2.1 Bench-side (lithrim-bench)

1. **`scripts/build_canonical_pack.py`** — NEW. Standalone author script:
   - Loads `/tmp/pilot_picklist.json`.
   - For each of 12 picks, hydrates the case from the bench pack fixture file (re-use `scripts/validate_canonical_12_via_sdk.py:_resolve_case_fixtures` logic — port or factor out into a shared module under `lithrim_bench/picklist.py` for both scripts to import).
   - Reads on-backend semantic outputs from `out/canonical_12_sdk_validation.ndjson` (and `out/s_p1_14_c1c2_rerun.ndjson` for C1+C2 post-patch) to derive each case's `accepted_substitutes` list.
   - Constructs 12 EvalCase docs + 1 EvalPack doc per the schema decision in §3.
   - Connects to Mongo via `MONGODB_URI` (default `mongodb://localhost:27017/velto`) and inserts. **NEVER deletes existing docs**; if the pack already exists, halt with an explicit error message — don't auto-overwrite (paper-grade pack should be deliberate).
   - Emits a single-line summary to stdout: `inserted: 1 eval_pack + 12 eval_case docs into velto.eval_pack / velto.eval_case`.

2. **`out/paper_v1_n12_canonical.spec.json`** — NEW. Single-source-of-truth pack manifest committed to bench. Shape:
   ```json
   {
     "pack_id": "paper_v1_n12_canonical",
     "version": "1.0.0",
     "name": "Paper 1 — Canonical N=12 (silent-confident-certification + clean-negative demonstration)",
     "authored_at": "2026-05-28",
     "authored_by": "paper-1-copilot",
     "source_picklist": "/tmp/pilot_picklist.json",
     "source_commit_bench": "<sha-of-bench-at-pack-build>",
     "source_commit_backend": "e8147d8",
     "case_count": 12,
     "cases": [
       {
         "paper_pick_label": "S1",
         "case_id": "bench_scribe_v1_hallucinate_detail_48c17bfae676",
         "source_pack": "scribe_v1",
         "pick_kind": "defect",
         "clean_negative": false,
         "multi_defect": false,
         "expected_compliance_verdict": ["needs_review", "reject"],
         "expected_artifact_verdict": "BLOCK",
         "expected_structural_verdict": "PASS",
         "expected_safety_flags_strict": ["HALLUCINATED_DETAIL"],
         "expected_safety_flags_accepted_substitutes": {
           "HALLUCINATED_DETAIL": ["FABRICATED_HISTORY", "FABRICATED_ALLERGY"]
         },
         "promotion_disposition": "PROMOTE-WITH-RELAX",
         "promotion_rationale": "Per REPORT_canonical_12_validation §3.1 — backend's faithfulness_judge emits FABRICATED_HISTORY+FABRICATED_ALLERGY for the same defect pattern; both are valid hallucination catches."
       },
       // ... 11 more cases ...
     ]
   }
   ```

   The `accepted_substitutes` dict is the **paper-bearing contract** — every paper §5 / §6 measurement reads from this manifest, not from implicit harness logic.

3. **`scripts/validate_canonical_12_via_sdk.py`** — UPDATE `_grade()` to honor `expected_safety_flags_accepted_substitutes`. New gate behavior: a flag is matched if `expected_code` OR ANY substitute in `accepted_substitutes[expected_code]` appears in the backend's emitted codes. Backwards-compatible default: if a case fixture has no `accepted_substitutes` field, fall back to strict matching (current behavior).

4. **`lithrim_bench/picklist.py`** — NEW. Shared module factoring out `_resolve_case_fixtures` from `scripts/validate_canonical_12_via_sdk.py` so `build_canonical_pack.py` can re-use it. The validation harness imports from here; old `_resolve_case_fixtures` becomes a thin wrapper for backwards-compat or is deleted.

5. **`docs/research/REPORT_paper_v1_n12_canonical_pack.md`** — NEW. Pack-author report. Required sections:
   - **§1 Summary** — what landed (paper-bearing pack now exists)
   - **§2 Per-case Path T contract** — the `accepted_substitutes` table per case; explicit + paper-citable
   - **§3 Calibration run** — single eval_run against the new pack to confirm verdict_match >= 9/12 (matches A3 floor under the relaxed contract); report each case's outcome
   - **§4 Diff vs P1-VALIDATE-12 baseline** — what changed in the on-backend signal after S-P1-14 patch + Path T contract
   - **§5 Ready for paper-N** — section explicitly stating the pack is ready for P1-CANONICAL-N5-PILOT

### 2.2 Backend-side (lithrim-backend)

**Decision required in plan-review (see §3):** choose schema-handling path before writing.

If "use existing schema with placeholder S3 keys" path chosen → NO backend code edits. `transcript_s3_key` gets value like `bench://paper_v1_n12_canonical/<case_id>/transcript.txt` (synthetic, never read by anything — eval-runner uses `transcript` from the case's `expected_artifacts` metadata path OR direct embed). Caveat: confirm with eval-runner code that this synthetic key doesn't break any S3-fetch call site (grep `transcript_s3_key` in `app/services/`).

If "extend schema" path chosen → MINIMAL edit to `app/models/eval_case.py`:
   - `transcript_s3_key: Optional[str]` (was required)
   - Add `transcript: Optional[str] = None` field for inline transcripts
   - Add `paper_pick_label: Optional[str] = None`
   - Add `expected_safety_flags_strict: Optional[List[str]] = None`
   - Add `expected_safety_flags_accepted_substitutes: Optional[Dict[str, List[str]]] = None`
   - Add `expected_compliance_verdict: Optional[List[str]] = None` (the multi-value version per Path T contract)
   - Add `expected_structural_verdict: Optional[Literal["PASS", "WARN", "BLOCK"]] = None`
   - Plus the same fields on `expected_artifacts[*]` entries if a per-artifact granularity is needed (likely not — case-level is sufficient).

Plus eval-runner update if it currently reads `transcript` only via S3:
   - `app/tasks/eval_runner.py` or wherever the transcript is loaded — fall back to inline `transcript` field when `transcript_s3_key` is None.

**Recommendation:** the "extend schema" path is paper-cleaner — the canonical pack is a paper artifact whose schema should be honest about its shape. The "synthetic S3 key" path leaves a schema-vs-content lie that's bad for paper §3 ground-truth definition. Plan-review should pick "extend" unless backend code currently does something the schema-extension would break.

### 2.3 Mongo writes (executed by `build_canonical_pack.py`)

After all upstream artifacts land + executor confirms the schema decision:

1. Insert `eval_pack` doc into `velto.eval_pack`:
   ```json
   {
     "pack_id": "paper_v1_n12_canonical",
     "name": "Paper 1 — Canonical N=12",
     "organization_id": "<org-id>",
     "version": "1.0.0",
     "case_ids": [<12 ObjectIds>],
     "created_at": "<now>",
     "created_by": "paper-1-copilot",
     "metadata": {
       "paper": "paper-1-copilot",
       "source_picklist_commit": "<bench-sha>",
       "source_backend_commit": "e8147d8"
     }
   }
   ```

2. Insert 12 `eval_case` docs into `velto.eval_case` matching the EvalCase Pydantic schema (with the optional/Path-T-contract fields populated).

3. Confirmation query: `db.eval_pack.findOne({pack_id: "paper_v1_n12_canonical"})` returns the pack; `db.eval_case.countDocuments({pack_id: "paper_v1_n12_canonical"})` returns 12.

### 2.4 Calibration run

Single eval_run via SDK against the new pack. Cost ~$0.36, runtime ~4 min. **Replaces the P1-VALIDATE-12 measurement under the new Path-T contract.** Acceptance: verdict_match >= 9/12 (matches A3 floor under the relaxed contract). If <9/12, halt and triage; the pack-authoring isn't complete until the calibration baseline holds.

### 2.5 Session log + STREAM + index

6. **`.devloop/sessions/session-paper-1-copilot-phaseP1-CANONICAL-PACK-2026-05-28.json`** — per `.devloop/templates/SESSION_LOG_TEMPLATE.json`.
7. **`.devloop/state/STREAM_paper-1-copilot.md`** — append P1-CANONICAL-PACK row, update First Move to point at P1-CANONICAL-N5-PILOT.
8. **`.devloop/state/streams.json`** — bump `current_phase` to `P1-CANONICAL-N5-PILOT` (queued) and update cycle_state_note.
9. **`.devloop/prompts/index.json`** — register the bundle.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with:

1. **Understanding** — one paragraph: what this cycle authors, what it does NOT measure (paper-N is the next cycle).
2. **Schema-handling decision** (§2.2): "synthetic S3 keys" (no backend edits) vs "extend schema" (1-2 line backend edits + transcript-loader fallback). Recommend "extend" unless something breaks. **HALT and ask user if uncertain.**
3. **Pack-creation path** (§2.3): direct Mongo insert via motor/pymongo. Confirm authority — there's no `POST /eval-cases` route (gap → opens S-P1-19 if not already). User authorized direct-insert via the canonical-pack scope.
4. **`accepted_substitutes` table preview** — for each of the 12 cases, post the proposed `expected_safety_flags_strict` + `expected_safety_flags_accepted_substitutes` pair. **User confirms case-by-case before any Mongo write.** This is the load-bearing Path T contract artifact.
5. **Calibration run plan**: one eval_run, agent_id `69e8eed80774d8129275bb4a`, against new pack. Total cost ~$0.36. C1+C2 specifically may take 1-2 runs each if Mistral content_filter fires (S-P1-13 manifestation noted in S-P1-14 closure).
6. **Risks**:
   - Mistral content_filter on C1 (S-P1-13). Mitigation: on first hit, re-run that case once.
   - Pack already exists in Mongo → halt-and-warn behavior in `build_canonical_pack.py` (don't auto-overwrite paper-grade artifact).
   - Backend schema extension may not load due to other route caching → if "extend" path, plan a backend restart in the plan-review (user must do this; not autostart).
7. **Deviations** — any change from this driver the executor proposes.

Wait for "go" before writing to Mongo or editing backend code.

---

## 4. Scope guardrails — NOT in scope

These are tempting but OUT of scope for this cycle:

- **Running the paper-N campaign.** That's P1-CANONICAL-N5-PILOT (next cycle), expressly separated so the pack is independently auditable before any expensive measurement.
- **Authoring `eval_pack=paper_v1_n12_canonical_FHIR`** or any FHIR-typed cases. That's P1-FHIR-CONFORMANCE-MINI (parallel cycle).
- **Backfilling the bench's existing packs** (`scribe_v1`, `hl7_adt_v1`, etc.) with similar contracts. Out of scope — paper measurement reads only from the new canonical pack.
- **Fixing S-P1-13** (council non-determinism). Acknowledge with the re-run mitigation; don't try to fix the root cause this cycle.
- **Fixing S-P1-15** (etlp-mapper S7 worst-of composition not firing). Out of scope; S7 stays at `needs_review` KEEP-AS-LIMITATION in the pack regardless.
- **Fixing S-P1-16** (`code: null` on structural findings) and **S-P1-17** (Mistral confidence serialized as 0.0). Both still open as low priority; not load-bearing for pack authoring.
- **Authoring eval_case via POST API** (since no such route exists). Direct Mongo insert is fine for this cycle; opening S-P1-19 (POST /eval-cases route gap) for a future API-hygiene cycle.
- **Drive-by ruff / formatting / unrelated edits** anywhere in either repo.

If something feels load-bearing-but-not-listed, halt and surface in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** `scripts/build_canonical_pack.py` runs end-to-end without errors and reports `inserted: 1 eval_pack + 12 eval_case docs`.
- **A2.** Mongo query confirms: `db.eval_pack.findOne({pack_id: "paper_v1_n12_canonical"})` returns a doc with `case_ids.length == 12`; `db.eval_case.countDocuments({pack_id: "paper_v1_n12_canonical"})` returns `12`.
- **A3.** Every one of the 12 eval_case docs has populated `paper_pick_label`, `expected_safety_flags_strict`, `expected_safety_flags_accepted_substitutes` (even if the substitutes list is empty for cleans — `{}` is valid).
- **A4.** `out/paper_v1_n12_canonical.spec.json` exists and lists all 12 cases with the Path T contract.
- **A5.** **Calibration eval_run completes; verdict_match >= 9/12 under the relaxed contract**. (Hard floor: 9; expected: 10-11.)
- **A6.** `scripts/validate_canonical_12_via_sdk.py` re-runs cleanly against the new pack (sanity check that the harness path still works on the canonical pack data shape).
- **A7.** `docs/research/REPORT_paper_v1_n12_canonical_pack.md` exists with §1-§5.
- **A8.** Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`.
- **A9.** `STREAM_paper-1-copilot.md` + `streams.json` + `prompts/index.json` updated; current_phase set to `P1-CANONICAL-N5-PILOT` (queued).

### Diagnostic stats (NOT acceptance gates)

- Per-case mean council latency on the calibration run
- Mistral content_filter incidence (expect 0-1 on 12 cases; if >1 escalate S-P1-13)
- Total LLM cost actually incurred
- Pack-load time (Mongo insert latency)

---

## 6. Commit structure + message templates

Atomic commits, in order:

1. **`feat(bench): paper_v1_n12_canonical pack author script + Path T contract spec`** (lithrim-bench)
   - Files: `scripts/build_canonical_pack.py`, `lithrim_bench/picklist.py`, `out/paper_v1_n12_canonical.spec.json`
   - Body: explain the Path T contract (strict + accepted_substitutes); reference REPORT_canonical_12_validation §5 + S-P1-14 closure.

2. **`feat(bench): harness honors expected_safety_flags_accepted_substitutes`** (lithrim-bench)
   - Files: `scripts/validate_canonical_12_via_sdk.py` (`_grade()` update)
   - Body: explain backwards-compat fallback (strict matching when no substitutes provided).

3. **`feat(eval-case): inline transcript + Path T contract fields`** (lithrim-backend, **ONLY IF "extend schema" path chosen in plan-review**)
   - Files: `app/models/eval_case.py`, possibly `app/tasks/eval_runner.py` (transcript-fallback path)
   - Body: explain additive fields are Optional; existing eval_cases unaffected; references this driver bundle + Path T closing condition.

4. **`analysis(paper-1): paper_v1_n12_canonical pack + calibration run report`** (lithrim-bench)
   - Files: `docs/research/REPORT_paper_v1_n12_canonical_pack.md`, plus the calibration-run NDJSON if you commit it (force-add past `.gitignore` per precedent of `out/canonical_12_sdk_validation.ndjson`).
   - Body: per-case promotion outcomes; A1-A6 PASS confirmation; pack ready for P1-CANONICAL-N5-PILOT.

5. **`chore(devloop): close P1-CANONICAL-PACK — session log + STREAM + state update`** (lithrim-bench)
   - Files: session log + STREAM + streams.json + prompts/index.json.

Each commit body ends with:

```
Executed per paper-1-copilot-phaseP1-CANONICAL-PACK-author-paper-v1-n12-driver
by session-2026-05-28-N.
See .devloop/sessions/session-paper-1-copilot-phaseP1-CANONICAL-PACK-2026-05-28.json
```

Plus the user's Co-Authored-By trailer.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1-A9 all PASS (per §5)
- [ ] No file in any diff outside §2 deliverables (scope held)
- [ ] If backend code touched: only the schema-extension path is in the diff; no drive-by edits
- [ ] No services were autostarted (user preference)
- [ ] No pushes / publishes / tags (user preference)
- [ ] No existing eval_pack or eval_case docs were modified or deleted; only inserts
- [ ] Mongo state verified post-run via the §5 A2 query
- [ ] Calibration run verdict_match >= 9/12 confirmed; per-case outcomes in REPORT §3
- [ ] Every CONFIRMED tag in REPORT carries a fenced evidence block per CLAUDE.md gate

---

## 8. First move

```bash
cd /Users/aregee/Workspace/github.com/lithrim-bench

# 1. Read role + driver + EvalCase model
$EDITOR .devloop/personas/EXECUTOR.md \
        .devloop/prompts/paper-1-copilot_phaseP1-CANONICAL-PACK_author_paper_v1_n12_driver.md \
        /tmp/pilot_picklist.json \
        ../lithrim-backend/app/models/eval_case.py

# 2. Confirm Mongo is reachable + check no pre-existing canonical pack
mongosh velto --quiet --eval '
  print("Existing paper_v1_n12_canonical pack: " + db.eval_pack.countDocuments({pack_id: "paper_v1_n12_canonical"}));
  print("Existing paper_v1_n12_canonical cases: " + db.eval_case.countDocuments({pack_id: "paper_v1_n12_canonical"}));
'
# Expected: 0 + 0. If ANY exist, halt + ask user before overwriting.

# 3. Pre-inspect on-backend semantic outputs per case (used to derive substitutes)
head -1 out/canonical_12_sdk_validation.ndjson | python3 -m json.tool | head -30
# Confirm the shape; you'll iterate over all 12 rows in build_canonical_pack.py

# 4. Survey transcript_s3_key call sites to decide schema-handling path
grep -rn "transcript_s3_key" ../lithrim-backend/app/ --include="*.py" | head -20
# If reads are localized to eval_runner + a single S3-fetch helper, "extend schema" is low-risk

# 5. Post plan-review per §3. Wait for "go". DO NOT write to Mongo.
```

After "go":

```bash
# 6. Implement scripts + spec; run build_canonical_pack.py
# 7. Calibration run via SDK (~$0.36, 4 min)
# 8. Author REPORT.md
# 9. Write session log + STREAM/state/index updates
# 10. Commit per §6 (5 atomic commits)
```

---

## 9. References

- Picklist (cases under test): `/tmp/pilot_picklist.json` (12 rows)
- Backend EvalCase model: `../lithrim-backend/app/models/eval_case.py`
- Backend EvalPack model: `../lithrim-backend/app/models/eval_pack.py`
- P1-VALIDATE-12 report (promotion verdicts source): `docs/research/REPORT_canonical_12_validation_2026-05-28.md`
- S-P1-14 triage + closure (confirms C1+C2 promotable): `docs/research/REPORT_s_p1_14_triage_2026-05-28.md` (esp. §10)
- Validation harness (re-used + updated): `scripts/validate_canonical_12_via_sdk.py`
- S-P1-14 reverify outputs (post-patch C1+C2 data): `out/s_p1_14_c1c2_rerun.ndjson`
- Backend NKA-patch commit (the patch that closes S-P1-14): `lithrim-backend` `e8147d8`
- Stream state: `.devloop/state/STREAM_paper-1-copilot.md`
- Open seams this cycle does NOT close: S-P1-12 (SDK schema test follow-up), S-P1-13 (council non-determinism; mitigated by re-run on Mistral content_filter), S-P1-15 (etlp-mapper S7 worst-of routing), S-P1-16 (`code:null` structural findings), S-P1-17 (Mistral confidence = 0.0)
- New seam this cycle opens: **S-P1-19** (no `POST /eval-cases` route — direct Mongo insert was the only option; flag for API-hygiene cycle)

---

## Hardness

- [x] **Routine cycle** — monitor performs inline critique at close per persona §"Spec-adherence critique pass"
- [ ] HARD GATE — n/a (no spec/contract files; backend schema extension is additive)

Inline critique focus areas at close-out:

1. **Surface fidelity** — do the 12 inserted eval_case docs match the picklist row-for-row (case_id + paper_pick_label join key)? Spot-check 3 cases.
2. **Behavioral fidelity** — does the calibration run's per-case verdict trace back to (a) picklist expected, (b) S-P1-14 reverify post-patch data, (c) the §3 Path T `accepted_substitutes` entries? Pick 3 cases and walk the chain.
3. **Out-of-scope intrusion** — `git diff --stat` against lithrim-backend should show ONLY `app/models/eval_case.py` + possibly `app/tasks/eval_runner.py` (if schema-extend path) — nothing else. `git diff --stat` against lithrim-bench should show only §2 deliverables.
4. **Spec ambiguity surfaced** — record any case where the picklist+REPORT-§5 mapping was unclear; that lands in the inline critique as Q4 OPEN-QUESTION for the next cycle's drivers to lock.
