# Driver — `bench-salvage` phase `WS-6c-DSPy-3a`: by-construction judge-calibration corpus (+ the FABRICATED_CONSENT injector, + the co-raise-aware lens) — the $0 precondition for the optimizer

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — ROOTED IN `lithrim-bench` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-DSPy-3a-judge-calibration-corpus-driver`
> **Version:** v1 · **Authored:** 2026-06-03
> **Last re-verified against code:** 2026-06-03 (`lithrim-bench` re-grepped at `HEAD` `39d535d` of `bench-salvage/ws6c-dspy`, per MONITOR §Phase 1a)
>
> **Why this exists (the diagnose-before-edit finding that re-sequenced the optimizer):** the WS-6c-DSPy-2-live A/B revealed the DSPy-judge **optimizer (WS-6c-DSPy-3b) is data-blocked** — the entire by-construction corpus has ≤1 council-judge positive per judge and **ZERO for `policy`** (no `FABRICATED_CONSENT` / `PHI_DISCLOSURE_PRE_VERIFICATION` cases). BootstrapFewShot climbs a metric over labeled positives; with 0–4 positives the gradient is noise and policy can't be optimized at all. So the optimizer's **precondition** — a real per-judge recipe=label trainset — comes first, and it is **$0 / offline / labels-true-by-construction** (the bench's core competency, `CLAUDE.md`). Authoring **policy positives also unblocks S-BS-42** (PHI recall, unmeasured) **and S-BS-45** (the policy elicitation divergence). This cycle is **$0 — no live calls.**
>
> **⚠️ CORE INVARIANT (CLAUDE.md — this is the whole reason the repo exists).** A case ships **only** if: (1) every `expected_safety_flags` code ∈ `taxonomy/taxonomy_snapshot.json` `KNOWN_TAXONOMY_CODES`; (2) every positive carries an `injection_recipes` block (`defect_type` / `mutated_projection` / `mutated_field_or_span` / `pre_value` / `post_value`) — **the recipe IS the label justification**; (3) clean negatives have `injection_recipes: null` + `expected_safety_flags: []`; (4) every flag has a `production_judges`-resident owner per `_TIER1_OWNERS`. **Do not add a case without `scripts/lint_golden_against_taxonomy.py` passing.**
>
> **⚠️ DIRTY-TREE / SHARED-BRANCH (the WS-6d/DSPy-2 landmine).** On `bench-salvage/ws6c-dspy` with concurrent uncommitted work (calibration `verification/*`+`grounding.py`+`SPEC_CALIBRATION_TRAINER`+`RUN_*`+`tests/verification/`; journey `apps/`; **3 STAGED paper files**). Your deliverables are in `lithrim_bench/injectors/`, a new pack file, `runtime/council/judge_metric.py`, tests (no overlap). **NEVER bare-commit. ALWAYS `git add <your-files> && git commit -- <your-files>`** + verify scope. The 3 staged paper files stay staged-but-unswept.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c-DSPy-3a
(by-construction judge-calibration corpus + the FABRICATED_CONSENT injector + the co-raise-aware lens).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench). This cycle is $0 / OFFLINE — NO live calls,
no Azure, no :8002. It authors by-construction cases (labels-true-by-construction) to give the
DSPy-judge optimizer (WS-6c-DSPy-3b, a later paid cycle) a real per-judge trainset.

CORE INVARIANT (CLAUDE.md): a case ships only if every expected_safety_flags code is in
taxonomy_snapshot.json, every positive has an injection_recipes block (the recipe IS the label),
clean negatives have injection_recipes:null + [], and every flag has a production-resident owner
(_TIER1_OWNERS). DO NOT add a case without scripts/lint_golden_against_taxonomy.py passing.

CRITICAL — DIRTY SHARED BRANCH: bench-salvage/ws6c-dspy carries concurrent uncommitted work
(calibration verification/* + grounding.py + SPEC_CALIBRATION_TRAINER + RUN_* + tests/verification/;
journey apps/; 3 STAGED paper files). NEVER touch those. NEVER bare-commit. ALWAYS:
git add <your-files> && git commit -- <your-files>, then git show --stat.

DO NOT change consensus / grade / lenses-as-owner-tables / the taxonomy snapshot. DO NOT run the
optimizer (that is 3b). The co-raise-aware lens is a judge_metric.py SCORING change only.

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-3a_judge-calibration-corpus_driver.md  (this doc)
  3. CLAUDE.md  (the core invariant — labels true by construction; the admissibility rules)
  4. lithrim_bench/injectors/  (the 9 existing council-judge injectors + base.py; phi_disclosure_pre_verification.py = the policy template; FABRICATED_CONSENT is MISSING — you author it)
  5. scripts/generate_proof_case.py + scripts/generate_pack.py  (the by-construction build entrypoints to reuse)
  6. scripts/lint_golden_against_taxonomy.py  (the admissibility lint = the gate)
  7. examples/proof_case.jsonl  (the case schema: injection_recipes [plural], expected_owner_map, clean_negative, multi_defect)
  8. lithrim_bench/runtime/council/judge_metric.py  (RISK/POLICY/FAITHFULNESS_JUDGE_LENS + score_judge:91 + make_judge_metric:144 = the co-raise-aware lens target, S-BS-43)
  9. lithrim_bench/runtime/council/compliance_council.py (_TIER1_OWNERS:232 = owner-residency for invariant #4)

Then post your plan-review per EXECUTOR.md — resolve the 4 PLAN-REVIEW DECISIONS in §3.
Do not write code until I say "go". HARD GATE (touches the CLAUDE.md core invariant): fresh-critic at close (user MAY elect inline).

Bundle ID: bench-salvage-phaseWS-6c-DSPy-3a-judge-calibration-corpus-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-3a_judge-calibration-corpus_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** re-grepped at `bench-salvage/ws6c-dspy` HEAD `39d535d` on 2026-06-03 (CONFIRMED). Re-grep before plan-review.

1. `CLAUDE.md` — the **core invariant** (labels-true-by-construction) + the 4 admissibility rules + "don't append to `examples/*.jsonl` without the lint passing." This is your contract.
2. `lithrim_bench/injectors/` — **the by-construction defect machinery.** The 9 council-judge injectors + their emitted code (re-verified):
   - `wrong_dosage.py` → `WRONG_DOSAGE` · `missed_escalation.py` → `MISSED_ESCALATION` (risk)
   - `phi_disclosure_pre_verification.py` → `PHI_DISCLOSURE_PRE_VERIFICATION` (`defect_type="phi_before_verify"`) (policy — **exists; this is your template for the new consent injector**)
   - `missing_allergy.py` → `MISSING_ALLERGY` · `value_mismatch.py` → `VALUE_MISMATCH` · `fabricated_history.py` → `FABRICATED_HISTORY` · `hallucinated_detail.py` → `HALLUCINATED_DETAIL` · `upcoding_risk.py` → `UPCODING_RISK` (faithfulness lens)
   - (the `hl7_*` injectors emit `STRUCTURAL_*` — **floor codes, NOT council-judge codes** — out of scope here)
   - **`FABRICATED_CONSENT` has NO injector** → you author `lithrim_bench/injectors/fabricated_consent.py` (the net-new extension; mirror `phi_disclosure_pre_verification.py` — the other policy defect). `base.py` defines the recipe (`defect_type:26`; the recipe dataclass).
3. `scripts/generate_proof_case.py` + `scripts/generate_pack.py` — **the by-construction build entrypoints to reuse** (how the existing packs are generated from injectors over a base case/Synthea; deterministic). Mirror this to build the calibration corpus.
4. `scripts/lint_golden_against_taxonomy.py` — **the admissibility lint = the gate.** Every case must pass: in-snapshot codes, recipe present on positives, clean-negative shape, owner-residency. Run it; ship nothing that fails.
5. `examples/proof_case.jsonl` — the **case schema** to mirror (keys: `case_id`, `transcript`, `artifacts`, `expected_safety_flags`, **`injection_recipes`** [plural], `expected_owner_map`, `clean_negative`, `multi_defect`, `expected_compliance_verdict`, `expected_artifact_verdict`, `severity`, `pack`, `pinned`, `ground_truth_basis`). A clean negative = `expected_safety_flags: []` + `injection_recipes: null`.
6. `lithrim_bench/runtime/council/judge_metric.py` — the **co-raise-aware lens (S-BS-43) target**: `RISK/POLICY/FAITHFULNESS_JUDGE_LENS`, `score_judge:91`, `make_judge_metric:144`. Today a judge's *correct* corroborating raise of another owner's code scores as an out-of-lens FP (the lower bound). The fix: a corroborating raise of a code that **is in the case's `expected_safety_flags` but owned by another judge** is **neutral** (not FP); only a raise of a NOT-expected code (true over-fire) or an out-of-taxonomy code is an FP.
7. `lithrim_bench/runtime/council/compliance_council.py` `_TIER1_OWNERS:232` — **owner-residency for invariant #4.** Confirmed owner-resident (v2 trio): `WRONG_DOSAGE`/`MISSED_ESCALATION`→risk; `FABRICATED_CONSENT`/`PHI_DISCLOSURE_PRE_VERIFICATION`→policy; `MISSING_ALLERGY`/`VALUE_MISMATCH`→faithfulness. (Tier-2/3 codes like `FABRICATED_HISTORY`/`HALLUCINATED_DETAIL`/`UPCODING_RISK` need 2+ corroboration, no single owner — fine.)
8. `taxonomy/taxonomy_snapshot.json` — `KNOWN_TAXONOMY_CODES` (every corpus code must be in here; `FABRICATED_CONSENT` + `PHI_DISCLOSURE_PRE_VERIFICATION` confirmed present).

---

## 2. Deliverables (file-by-file)

1. **`lithrim_bench/injectors/fabricated_consent.py`** (NEW — the net-new extension) — a `FABRICATED_CONSENT` injector mirroring `phi_disclosure_pre_verification.py`'s structure (`base.py` recipe interface). `defect_type` e.g. `"fabricate_consent"`; it injects, into the artifact, **documented consent/authorization the transcript shows was never given** (the `policy_judge.txt:19` definition); emits `expected_safety_flags=["FABRICATED_CONSENT"]`, owner = `policy_judge`, with a full `injection_recipes` block (`mutated_projection` / `mutated_field_or_span` / `pre_value` / `post_value` — the consent statement absent in source, present in artifact). The recipe must make the label self-evidently true.
2. **The judge-calibration corpus** (NEW pack — home = plan-review #3; e.g. `data/verification_packs/judge_calib_v1.jsonl` or `examples/judge_calib_v1.jsonl`) — generated by reusing `generate_pack.py`/`generate_proof_case.py` over the injectors. Coverage (resolve exact counts at plan-review #1):
   - **policy (the priority gap):** `FABRICATED_CONSENT` (new) + `PHI_DISCLOSURE_PRE_VERIFICATION` (existing) — the 0→positive unblock + S-BS-42.
   - **risk:** `WRONG_DOSAGE` + `MISSED_ESCALATION`.
   - **faithfulness:** `MISSING_ALLERGY` + `VALUE_MISMATCH` + `FABRICATED_HISTORY`.
   - **clean negatives** (to measure FP).
   - At least one **multi-defect** case that exercises cross-owner corroboration (the co-raise lens test fixture for D4).
   - Every positive carries `injection_recipes`; the pack **passes `lint_golden_against_taxonomy.py`**; byte-deterministic regen (diff==0).
3. **`scripts/generate_pack.py`** (EXTEND only if needed) — register the new pack's recipe set / wire `fabricated_consent`. Prefer reusing the existing entrypoint; if it needs a small additive hook, keep it additive. No rewrite.
4. **`lithrim_bench/runtime/council/judge_metric.py`** (EXTEND — S-BS-43, the co-raise-aware lens) — the scoring rule above: a corroborating raise of another owner's **expected** code is neutral, not FP. Keep `score_judge`'s signature stable; add the co-raise awareness behind it (e.g. an `expected_codes` arg or a per-case owner set). **Existing lens/guard tests stay green.**
5. **Tests** (offline; extend `runtime/council/tests/` + an injector test) — (a) the `fabricated_consent` injector produces an admissible case (recipe present, in-snapshot, owner=policy); (b) the corpus passes the lint (or a thin test invoking it); (c) the co-raise-aware lens: a faithfulness judge corroborating a risk-owned **expected** code scores **neutral** (was FP), while a genuine over-fire (a NOT-expected code) still scores FP; the existing lens tests (`test_trio_dspy.py` lens/guard) stay green.
6. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-3a-2026-XX-XX.json`** — session log: the coverage matrix (code × count × owner × in-snapshot), the lint PASS, the determinism proof, the co-raise lens before/after, and which gaps remain (uncovered codes deferred).

---

## 3. Plan-review checkpoint (non-negotiable)

Per `EXECUTOR.md`, post understanding + file-by-file plan + the coverage matrix + test plan + risks + deviations. **Wait for "go".** Resolve:

1. **Coverage matrix + counts.** Which codes × how many positives + how many clean negatives, and the train/held-out split 3b will use. *(Default: ~4 positives each for `FABRICATED_CONSENT`, `PHI_DISCLOSURE_PRE_VERIFICATION`, `WRONG_DOSAGE`, `MISSED_ESCALATION`, `MISSING_ALLERGY`, `VALUE_MISMATCH`, `FABRICATED_HISTORY` + ~6–8 clean negatives + ≥1 multi-defect ≈ 35–40 cases. **Prioritize the policy gap.** Defer the uncovered lens codes — `FABRICATED_ALLERGY`, `SEVERITY_ESCALATION`, the faithfulness T2/T3 — to a follow-up unless trivially injectable.)*
2. **The `fabricated_consent` injector design.** The consent-fabrication scenario + the `injection_recipes` fields (what `mutated_field_or_span` / `pre_value` / `post_value` capture). Confirm it mirrors `phi_disclosure_pre_verification.py` and that the synthesized case self-evidently warrants `FABRICATED_CONSENT` (the fresh-critic will check this).
3. **Corpus home + build path.** `data/verification_packs/judge_calib_v1.jsonl` vs `examples/judge_calib_v1.jsonl`; reuse `generate_pack.py` vs a thin new builder; confirm byte-deterministic regen + the lint integration.
4. **Co-raise-aware lens (S-BS-43) design.** The exact neutral-vs-FP rule + how `score_judge`/`make_judge_metric` learn the case's expected-owner set without breaking the existing signature/tests. *(Default: a corroborating raise of a code ∈ the case's `expected_safety_flags` but owned by another judge → neutral; a raise of a code ∉ expected → FP; out-of-taxonomy → FP. The lens still bounds the judge's own recall scope.)*

---

## 4. Scope guardrails — NOT in scope

- **NO optimizer / BootstrapFewShot / MIPRO** — that is WS-6c-DSPy-3b (paid, gated on this corpus).
- **NO live calls / Azure / :8002** — this cycle is **$0, offline** (by-construction synthesis + offline lint + offline lens tests).
- **NO consensus / grade / `_TIER1_OWNERS` / taxonomy-snapshot edit.** The corpus USES the existing owner table + snapshot; it does not change them. (If a target code is not owner-resident or not in-snapshot, halt — do not ship it.)
- **NO change to the lenses-as-code-sets** (`RISK/POLICY/FAITHFULNESS_JUDGE_LENS` membership) — the S-BS-43 work is a SCORING-rule change, not a lens-membership change.
- **NO appending to `examples/proof_case.jsonl` or the existing packs** — the calibration corpus is a NEW pack; the lint must pass on it.
- **NO touching concurrent-session files** (`verification/*`, `harness/grounding.py`, `apps/*`, `docs/PAPER_OUTLINE.md`, `docs/research/REPORT_semantic*`/`SYNTHESIS_*`/`RUN_*`, `SPEC_CALIBRATION_TRAINER.md`, `tests/verification/`). Never stage/commit them; the 3 staged paper files stay staged-but-unswept.
- **NO `../lithrim-backend` / `etlp-mapper` edit.** No drive-by reformat.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** **`fabricated_consent.py` injector** authored (mirrors `phi_disclosure_pre_verification.py`): a `defect_type`, a full `injection_recipes` block, emits `FABRICATED_CONSENT` owned by `policy_judge`; produces an admissible case.
- **A2.** **The judge-calibration corpus** exists as a new pack: every positive carries `injection_recipes` (recipe = label); clean negatives = `[]` + `injection_recipes: null`; coverage ≥ the resolved matrix **including policy** (`FABRICATED_CONSENT` + `PHI_DISCLOSURE_PRE_VERIFICATION`, the prior 0→positive); **`scripts/lint_golden_against_taxonomy.py` PASSES** on the pack (every code in-snapshot + owner-resident).
- **A3.** **Byte-deterministic** — regenerating the corpus produces an identical file (diff==0), per the offline-determinism invariant.
- **A4.** **Co-raise-aware lens (S-BS-43)** lands in `judge_metric.py`: a corroborating raise of another owner's **expected** code scores **neutral** (not FP); a genuine over-fire (not-expected code) still scores FP; the existing lens/guard tests (`test_trio_dspy.py`) stay green + a new co-raise test passes (ideally on a multi-defect corpus case).
- **A5.** **$0 + scope held** — no live call; default install unchanged; ruff clean on new files; `import lithrim_bench` unchanged; close-diff = ONLY DSPy-3a files (injector + pack + judge_metric + tests + maybe generate_pack hook + log); no concurrent-session/paper file swept; every commit pathspec'd.
- **A6.** **Unblocks the downstream** — S-BS-42 is now MEASURABLE (PHI cases exist) and WS-6c-DSPy-3b has a real per-judge recipe=label trainset incl. policy. (Enabling, not closing, those items.)

### Diagnostic stats (NOT gates)
- Coverage matrix (code × count × owner) + the train/held-out split for 3b.
- The co-raise lens before/after on a corroboration case (was FP=1 → now neutral).
- Gaps remaining (uncovered lens codes deferred).

---

## 6. Commit structure + message templates

Atomic per-concern on `bench-salvage/ws6c-dspy`. **Explicit pathspec on every commit.** Suggested:

1. `feat(injectors): FABRICATED_CONSENT injector (policy defect, by-construction recipe)` — `lithrim_bench/injectors/fabricated_consent.py` (+ test)
2. `feat(corpus): by-construction judge-calibration pack (policy/risk/faithfulness positives + cleans; lint-green)` — the new pack (+ `generate_pack.py` hook if any)
3. `feat(council-dspy): co-raise-aware lens — corroboration of another owner's expected code is neutral (S-BS-43)` — `judge_metric.py` + tests

**Commit hygiene (MANDATORY):** `git add <only your files>` → `git commit -- <only your files>` → `git show --stat --format= HEAD`. At close: `git diff 39d535d HEAD --stat` = ONLY DSPy-3a files. Bodies end with `Executed per bench-salvage-phaseWS-6c-DSPy-3a-judge-calibration-corpus-driver by session-…` + `See …session-…json` + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **Diagnose-before-edit:** the injector/recipe claims carry the recipe block as their evidence (the label justification).

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A6 PASS (consent injector admissible; corpus lint-GREEN incl. policy positives; byte-deterministic; co-raise lens neutral-not-FP + existing tests green; $0 + scope held; downstream unblocked)
- [ ] `scripts/lint_golden_against_taxonomy.py` PASSES on the new pack (paste the output in the log)
- [ ] Every positive has an `injection_recipes` block; clean negatives are `[]` + `null`; every code in-snapshot + owner-resident (`_TIER1_OWNERS`)
- [ ] No consensus / grade / `_TIER1_OWNERS` / taxonomy-snapshot / lens-membership change
- [ ] No live call (this is $0); no concurrent-session/paper file in the diff; every commit pathspec'd; the 3 staged paper files NOT swept
- [ ] `pytest -q` green on default deps + (dspy/council tests as applicable on `debuglithrim`); ruff clean on new files
- [ ] Session log written (coverage matrix + lint PASS + determinism + co-raise before/after + remaining gaps)

---

## 8. First move

1. Read `EXECUTOR.md` + `CLAUDE.md` (the core invariant + admissibility rules).
2. Re-grep §1 anchors at HEAD `39d535d` (the injector inventory + `phi_disclosure_pre_verification.py` as the consent template; `generate_pack.py`/`generate_proof_case.py`; `lint_golden_against_taxonomy.py`; the proof_case schema; `judge_metric.py` `score_judge:91`/`make_judge_metric:144`; `_TIER1_OWNERS:232`; the snapshot has `FABRICATED_CONSENT`+`PHI_DISCLOSURE_PRE_VERIFICATION`).
3. `git status` — confirm the dirty workbench + 3 staged paper files; pathspec-only commits.
4. Read §2–§7.
5. Post plan-review per §3 (the coverage matrix + the consent injector design + corpus home + the co-raise lens rule). Wait for "go".

---

## 9. References

- The core invariant: `CLAUDE.md` (labels-true-by-construction; the 4 admissibility rules)
- The injector machinery: `lithrim_bench/injectors/` (9 existing; `phi_disclosure_pre_verification.py` = the policy template; `base.py` recipe) + `scripts/generate_pack.py` / `generate_proof_case.py`
- The admissibility gate: `scripts/lint_golden_against_taxonomy.py`
- The case schema: `examples/proof_case.jsonl`
- The co-raise lens target: `lithrim_bench/runtime/council/judge_metric.py` (`score_judge:91`, `make_judge_metric:144`) — S-BS-43
- Owner-residency: `compliance_council.py` `_TIER1_OWNERS:232`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (S-BS-42 PHI-recall + S-BS-43 co-raise lens + S-BS-45 policy elicitation — this cycle enables all three)
- The motivating measurement: `docs/research/REPORT_dspy2_live_ab_2026-06-03.md` (why the optimizer is data-blocked)
- **Next: WS-6c-DSPy-3b** — the optimizer (BootstrapFewShot climbing `make_judge_metric`) on THIS corpus + the S-BS-45 policy-elicitation investigation (now that policy has positives). Paid; gated on this cycle.

---

## Hardness

- [x] **HARD GATE** — authors NEW by-construction safety cases + a NEW injector, touching the **CLAUDE.md core invariant** (labels-true-by-construction; the recipe IS the label). The admissibility lint is the automated gate, but a fresh-critic must independently confirm the `fabricated_consent` recipe self-evidently warrants the label, the cases are owner-correct, and nothing soft-passes the invariant. $0/offline lowers blast radius, but the core invariant is the repo's reason-for-being. Fresh-critic at close recommended (`KICKOFF_CRITIC.md`); **user MAY elect inline** (precedent: prior bench-salvage cycles).
