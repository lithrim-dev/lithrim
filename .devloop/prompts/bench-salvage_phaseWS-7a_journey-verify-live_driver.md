# Driver, `bench-salvage` phase `WS-7a`: Journey Phase-2 "Verify" → live BFF (the pitch becomes real)

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-7a-journey-verify-live-driver`
> **Version:** v1
> **Authored:** 2026-06-02
> **Last re-verified against code:** 2026-06-02 (per MONITOR §Phase 1a)

---

## Objective + leverage note (read first)

**WS-7** is the **product feature-completion** milestone: make the **4-act journey
(the pitch) run on REAL service calls** instead of `journeyData` mock. Shell mode is
already wired to the live BFF; the journey — the thing you demo — is 100% scripted
(S-BS-26). WS-7a is the **beachhead**: wire **Phase-2 "Verify"** to a real grade.

**The governing constraint — efficiently leverage lithrim-backend. This cycle adds
ZERO backend and ZERO BFF code.** The whole grade chain already exists and composes
over the backend:

```
journey runVerify  →  bff.js runEval({live})  →  POST /v1/run-eval  →  run_eval.run()
   → grade_replay  (captured :8002 baseline, $0 — the default)
   → grade_live    (POST :8002 /v1/pipeline/evaluate — the live backend)
   → grade_inprocess (the salvaged in-process council, WS-6c-AGENTIC)
```

`journeyData.js` is **already captured real council scores** (`:125` "the v2 council's
REAL scores… recorded run: BLOCK · WRONG_DOSAGE"). So the "mock" is a snapshot of a
real `:8002` run — wiring it live is **replacing a hardcoded snapshot with a
`runEval()` call**, default `live=false` (**$0 replay**). The pitch becomes real at
**$0 by default**, and a live toggle exercises `:8002`/in-process on demand.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-7a
(Journey Phase-2 "Verify" → live BFF; the pitch's first live act).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                          (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-7a_journey-verify-live_driver.md (this doc — read the leverage note FIRST)
  3. docs/specs/SPEC_PRODUCT_SHELL.md  §2.1 (the 4-phase journey) + §10 (the BFF surface) + §5c (scripted-default)
  4. .devloop/state/STREAM_bench-salvage.md                                 (S-BS-26 journey-acts mount; the shell-phase :5180 smoke discipline)

ROOTED IN lithrim-bench. FRONTEND cycle (apps/shell). NO ../lithrim-backend edit,
NO new BFF endpoint, NO new Python — leverage the existing runEval/run-eval/grade chain.

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)",
resolving the 4 plan-review decisions in §3. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseWS-7a-journey-verify-live-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-7a_journey-verify-live_driver.md
```

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review. Every file:line re-grepped 2026-06-02:

1. `docs/specs/SPEC_PRODUCT_SHELL.md` — §2.1 (the 4 journey phases; Phase-2 = the verify "aha"), §5c (scripted-default, zero-LLM demo), §10 (the locked v1 BFF surface — confirm `/v1/run-eval` is the grade entry).
2. `apps/shell/src/bff.js` — **the client already exists**: `runEval({agent="ws0_default", live=false})` (`:23`, `POST /v1/run-eval`, replay=`$0`), `getOntology`/`getCorpus`/`putOntology`. `BASE = VITE_BFF_URL ?? ""` (`:6`, the Tauri/VPC-ready indirection).
3. `apps/shell/src/journey/JourneyApp.jsx` — `runVerify:55` (the **mock** state machine: `verify` idle→running→done + `revealed` stagger, `:35-36`); `centerProps`/`artProps:77-78` (Phase-2 = `{verify, runVerify}` / `{verify, revealed}`). This is where the live `runEval()` call lands.
4. `apps/shell/src/journey/jp2.jsx` — `Center2({verify, runVerify}):47` (the Verify button → `runVerify`), `Artifact2({verify, revealed}):80` (the **pillar-badge reveal**, `pb-score:114`); imports `EXCHANGE, JUDGES, SCENARIOS, RUN` from `journeyData.js:5`. The pillar-badges render from `JUDGES`/`RUN` mock — swap to the live composite on the wired path.
5. `apps/shell/src/journey/journeyData.js` — `PILLARS:12`, and the **captured real scores** (`:125` recorded `BLOCK · WRONG_DOSAGE`, `:133-139` the by-construction clean-vs-injected pair). Keep these as the **idle/fallback** (the empty-state), not deleted.
6. `apps/shell/src/artifact.jsx` — `ReportTab:34` renders `runResult.composite` (verdict/`stage_verdict`/tiles, `:54-77`) + the BFF-down empty-state (`:42`). **This is the proven render pattern to lift** into the journey verify reveal (the composite → pillar shape).
7. `apps/bff/app.py` — `/v1/run-eval:115` (`run_eval_endpoint` → `run_eval.run(agent, live, out_dir)`, returns the `record`: composite/calibration/provenance) + `_council_view:27` (extracts `record["result"]["semantic"]["judge_votes"]` — the per-judge votes the pillars need). **Read-only — do not edit.**

> **Citation discipline:** every file:line above re-grepped 2026-06-02. **The leverage
> is load-bearing:** the grade chain (`runEval` → `/v1/run-eval` → `grade_replay/live/
> inprocess`) is COMPLETE; this cycle wires the journey UI to it and writes no Python.
> If you spot drift, halt and surface it (per EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

### D1, Wire `runVerify` to the live grade

1. **`apps/shell/src/journey/JourneyApp.jsx`** (`runVerify:55`) — replace the mock
   animation with: call `runEval({live})` (default `live=false` → `$0` replay), set
   the returned `record` into state, then drive the existing staggered `revealed`
   reveal from the **real** composite/judge-votes (preserve the choreography — the
   "aha" pacing is the pitch). Add a **replay/live toggle** (default replay). Handle
   the in-flight (`running`) + error states.

### D2, Render the real composite in the verify artifact

2. **`apps/shell/src/journey/jp2.jsx`** (`Artifact2:80`, `Center2:47`) — on the wired
   path, render the pillar-badges from the **live composite** (`judge_votes` → per-
   judge pillar + the composite verdict/score) instead of `journeyData` `JUDGES`/`RUN`.
   `journeyData` stays the **idle/empty-state fallback** (BFF-down or pre-run). Keep
   the existing CSS/markup (`pillar-badge`, `pb-score`) — feed it real data.

### D3, The composite → pillar mapping (shared)

3. **`apps/shell/src/lib/` (or a small shared module)** — a pure `recordToPillars(record)`
   helper mapping the `/v1/run-eval` composite + `_council_view` votes to the journey's
   pillar shape. Reusable by WS-7b (Phase-3). Lift the field reads from the proven
   `artifact.jsx ReportTab` pattern; do not re-derive.

### D4, Test (Vitest + RTL, the shell test bar)

4. **`apps/shell/src/journey/jp2.test.jsx` (or `JourneyApp.test.jsx`)** — mock `bff.js
   runEval` (the WS-5c/5d binding-test pattern): `runVerify` calls `runEval` with the
   right args; the real composite renders the pillars (real verdict, e.g. `BLOCK ·
   WRONG_DOSAGE`); `$0` replay is the default; BFF-down → the graceful empty-state, no
   crash. No network (mocked).

### D5, Visual-parity smoke + session log

5. **The `:5180` visual-parity smoke** (load-bearing per the WS-5c/5d shell discipline,
   user-run, no-autostart): Phase-2 "Verify" renders the live reveal at `:5180`,
   light/dark, no regression vs the scripted look. **`.devloop/sessions/session-
   bench-salvage-phaseWS-7a-2026-06-02.json`** — the wiring, the leverage proof (zero
   backend/BFF/Python diff), the test evidence, the `:5180` smoke attestation.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with Understanding, file-by-file
changes, test plan, risks, and any proposed deviations. **Resolve these 4 decisions:**

1. **Default grade path.** Replay (`live=false`, `$0`, the captured real scores) as the
   journey default + a live toggle (recommended — cost-conscious + the demo is
   deterministic), vs live-by-default? The live toggle exercises `grade_live(:8002)` /
   `grade_inprocess`; confirm the one-call cost envelope if a live smoke is run.
2. **Pillar mapping (D3).** The real grade has **3 `judge_votes`** (risk/policy/
   faithfulness) + a composite verdict/score; the journey shows the pillar-reveal.
   Confirm the mapping (3 judges + composite/overall as the 4th badge, vs the 4
   `PILLARS` ids) against `jp2`'s actual badge shape.
3. **Choreography.** Preserve the staggered `revealed` reveal (feed it real data) —
   recommended (the pacing is the "aha"), vs render-all-at-once.
4. **Hardness — inline vs fresh-critic.** Per SPEC §8 each shell phase is HARD-GATE-
   class; the prior shell phases ran fresh-critic (WS-5-BFF/5c/5d) or user-elected
   inline (WS-5/5b). The `:5180` visual smoke is load-bearing regardless. Your call at
   close.

Wait for "go" before writing code.

---

## 4. Scope guardrails, NOT in scope

- **NO `../lithrim-backend` edit, NO new `:8002` endpoint** — leverage the existing
  `/v1/pipeline/evaluate` via `grade_live` (the BFF `/v1/run-eval` already wraps it).
- **NO new BFF endpoint / NO new Python** — reuse `runEval` / `/v1/run-eval`. If you
  find the verify act genuinely needs a field the record doesn't carry, **halt and
  surface it** (it likely doesn't — `_council_view` already exposes the votes).
- **NO Phase-1 / Phase-3 / Phase-4 wiring** — Phase-2 only (the beachhead). Phase-3
  (Align: floor + KB) is **WS-7b**; Phase-1/4 are **WS-7c**.
- **NO KB / tools / floor integration** — that is WS-7b (the headline + paper).
- **NO Tauri / packaging** — the genuine last mile (deferred per the 2026-06-02 ship
  decision: features against the services first).
- **NO rebuild of the journey UX** — keep the scripted reveal choreography + CSS;
  feed it real data. **Do NOT delete `journeyData`** — it stays the idle/fallback.
- Drive-by reformatting / new npm deps / Tailwind churn.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1 (the pitch is real).** Journey Phase-2 "Verify" drives a **real grade** via
  `runEval` (`POST /v1/run-eval`) and renders the **real composite** (the 3 judges'
  votes + verdict, e.g. `BLOCK · WRONG_DOSAGE`) in the staggered reveal — not
  `journeyData` mock. Verify at `:5180` + the test.
- **A2 ($0 default, live on demand).** Replay (`live=false`) is the default — **$0**,
  replaying the captured `:8002` scores; a live toggle exercises `grade_live(:8002)` /
  `grade_inprocess` (cost-confirmed if run).
- **A3 (leverage held — the governing gate).** **Zero `../lithrim-backend` diff, zero
  new BFF endpoint, zero new Python** — `git diff` is confined to `apps/shell/`. The
  grade is the existing `runEval`/`/v1/run-eval`/grade chain.
- **A4 (graceful degradation).** BFF-down → the empty/CTA state (the `artifact.jsx`
  pattern) + `journeyData` idle fallback, no crash.
- **A5 (test + build + smoke).** Vitest+RTL green (the new wiring test); `npm run
  build` clean; no new npm dep; the **`:5180` visual-parity smoke PASS** (user-attested).

### Diagnostic stats (NOT acceptance gates)

- the live composite verdict + per-judge votes rendered on the one case, reported.
- any one cost-confirmed live grade (if the live toggle is exercised), reported.

---

## 6. Commit structure + message templates

Atomic, frontend commits (all `apps/shell/`):

1. **`feat(journey): wire Phase-2 Verify to the live BFF (runEval, $0 replay default)`** — D1+D2+D3.
2. **`test(journey): Phase-2 live-grade wiring + BFF-down fallback`** — D4.

Each commit body ends with:
```
Executed per bench-salvage-phaseWS-7a-journey-verify-live-driver by session-2026-06-02-N.
See .devloop/sessions/session-bench-salvage-phaseWS-7a-2026-06-02.json
```
(Add the user's Co-Authored-By trailer if used.)

**Note (concurrent-tree hazard, recorded `git-commit-pathspec-dirty-index`):** the
journey files are under active concurrent edit + the tree carries staged paper files.
**Always `git commit -- <your apps/shell files>`** (never bare-commit); verify scope
with `git diff <parent> HEAD --stat` before handing back.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance A1-A5 PASS (per §5)
- [ ] All commits exist (explicit pathspec; `apps/shell/` only); working tree's foreign files untouched
- [ ] `git diff` confined to `apps/shell/` — **zero backend/BFF/Python** (A3 the leverage gate)
- [ ] Vitest+RTL green; `npm run build` clean; no new npm dep
- [ ] `:5180` visual-parity smoke PASS (user-attested; light/dark)
- [ ] `journeyData` preserved as the idle/fallback (not deleted)
- [ ] No services autostarted; no pushes / publishes / tags
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs; confirm on your own read that the grade chain
   (`runEval`→`/v1/run-eval`→grade) is complete (zero backend work needed).
3. Re-read §2-§7 of this driver.
4. Post plan-review per §3, resolving the 4 decisions.

---

## 9. References

- Spec: `docs/specs/SPEC_PRODUCT_SHELL.md` §2.1 / §5c / §10 + `docs/design/JOURNEY_brief.md`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (S-BS-26 journey-act mount; the shell-phase `:5180` smoke discipline)
- The leverage chain: `apps/shell/src/bff.js` `runEval` → `apps/bff/app.py:115` → `lithrim_bench/harness/grade.py` (`grade_replay`/`grade_live`/`grade_inprocess`)
- The render pattern to lift: `apps/shell/src/artifact.jsx` `ReportTab:34`
- Prior shell phases (test + smoke precedent): WS-5c/5d critiques in `.devloop/sessions/`

---

## Hardness

- [ ] Routine cycle, monitor performs inline critique at close
- [x] **HARD-GATE-class** (per SPEC §8 shell phases) — **user-electable inline** (as WS-5/5b) vs fresh-critic (as WS-5-BFF/5c/5d). Decide at close.

**Monitor note.** The risk surface is low (UI wiring over a proven grade chain, no
backend), so inline is defensible. But it is the **first live journey act** (the
pitch becoming real) and **frontend visual fidelity is where the WS-5c/5d fresh-critic
earned its keep** (caught visible defects build + tests missed). So the **`:5180`
visual smoke is load-bearing regardless of the inline/fresh-critic choice** — treat it
as the gate, not optional.
