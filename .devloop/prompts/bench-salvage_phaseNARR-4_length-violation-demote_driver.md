# Driver — `bench-salvage` phase `NARR-4`: demote `LENGTH_VIOLATION` out of the floor → the `policy_judge` lens (the S-BS-NARR3-3 lock-fix)

> **Generated by the monitor in a planning session. The executor pastes the
> KICKOFF block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseNARR-4-length-violation-demote-driver`
> **Version:** v1
> **Authored:** 2026-06-16
> **Last re-verified against code:** 2026-06-16 (every load-bearing citation re-grepped this session against `bench-salvage/ws6c-dspy` HEAD `22bf3c6`)
> **Hardness:** **HARD GATE** (changes the grade-time FLOOR set — removes a deterministic check from `ground()` — and removes a by-construction label-true corpus case; fresh-critic session required at close to confirm the headline is preserved and nothing was soft-passed)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase NARR-4
(demote LENGTH_VIOLATION out of the deterministic floor -> the policy_judge lens;
the S-BS-NARR3-3 lock-fix).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseNARR-4_length-violation-demote_driver.md  (this doc)
  3. .devloop/sessions/critique-bench-salvage-phaseNARR-3-2026-06-16.md  (NON-BLOCKING #2 -> S-BS-NARR3-3, the over-fire being fixed)
  4. docs/specs/SPEC_NARRATIVE_EVAL.md  (§4.1 case record, §4.2 jute_transform emits response:clean_text, §4.3 floor/judge codes, §5 grading model)
  5. packs/narrative/floors.py  (LengthViolationTool:104 + FLOOR_EXECUTORS:213 -- what is being unattached, NOT deleted)
  6. packs/narrative/ontology.json  (verification_contracts:143-179 -- the length_violation decl to remove; the LENGTH_VIOLATION flag:43-53; question ordinal-2 policy_judge:134)
  7. packs/narrative/taxonomy_snapshot.json  (lenses.policy_judge ALREADY owns LENGTH_VIOLATION -- the demote target already exists)
  8. lithrim_bench/harness/grounding.py:506-681  (the floor dispatch -- to understand what a removed contract stops doing)
  9. tests/test_narrative_floor.py  (A1-A6; the test-local _NARRATIVE_ONT_DICT:180-241 + the unit test_length_violation_and_clean:122)
 10. tests/test_narrative_pack.py  (the contract-count invariant :93 / :275)
 11. tests/fixtures/narrative/storyworld_session.json  (the 4 enhanced scenes the over-fire false-blocks)
 12. .devloop/state/STREAM_bench-salvage.md  (the S-BS-NARR3-3 LOCK note + open seams)

Canonical env (export before running anything):
  LITHRIM_BENCH_PACK=narrative LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim
  (and re-run the regression gate under LITHRIM_BENCH_PACK=healthcare -- the canonical green bar)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseNARR-4-length-violation-demote-driver
Driver doc: .devloop/prompts/bench-salvage_phaseNARR-4_length-violation-demote_driver.md
```

---

## 0. The lock (owner decision 2026-06-16 — read before anything)

`S-BS-NARR3-3` is **LOCKED**: **demote `LENGTH_VIOLATION` out of the deterministic floor → the `policy_judge` judge lens.** The fix is **purely subtractive**.

**The diagnosis (CONFIRMED, verbatim evidence):**

```
Tool splitter re.compile(r"[.!?]+") on the FULL clean_text of
tests/fixtures/narrative/storyworld_session.json, band [3,4]:
  scene_ranger_arrival      enhanced  4 sentences  IN-BAND (the ONE scene the corpus uses)
  scene_mountain_road       enhanced  7 sentences  FALSE BLOCK
  scene_the_warning         enhanced  5 sentences  FALSE BLOCK
  scene_the_descent         enhanced  5 sentences  FALSE BLOCK
  scene_content_filtered    baseline  3 sentences  IN-BAND
```

The critic's numbers (NARR-3 critique NON-BLOCKING #2) reproduce exactly. The **decisive fact**: each enhanced scene carries only `['title','source','clean_text']` — **there is no separable preamble span**, and §4.2's `jute_transform` emits `response: $ e.value.clean_text` (the whole scene) into `artifacts[0].content`. The enhancement contract is "the AI-added **preamble** is 3–4 sentences," but the whole scene = (human-authored body, arbitrary length) + (AI preamble). Counting the whole scene against a 3–4 band false-blocks legitimate enhanced scenes. **Preamble-length is not machine-true-by-construction on the shipped record → it does not belong in the deterministic floor.** (The two options that were NOT chosen: *(b)* re-pin to a whole-scene band — feasible but a hollow check that isn't the contract; *(a)* carry a separate `preamble` span — contract-faithful but requires upstream Lenador data the fixture lacks; revisit only if real NARR-5 data carries it.)

**Why the demote is purely subtractive (CONFIRMED):** `LENGTH_VIOLATION` is **already** a `policy_judge` lens code (`packs/narrative/taxonomy_snapshot.json` `lenses.policy_judge` lists it) and the ontology already asks it as **question ordinal 2** (`policy_judge`: *"Is the added preamble 3-4 sentences and in the same language…"*). So removing the floor `verification_contract` leaves `LENGTH_VIOLATION` graded exactly like its TIER_2 judge peers (`BODY_CONTRADICTION`, `POV_VIOLATION`, … — all `owner_roles: []`, lens-owned). **The snapshot and the moat are untouched.** `BRACKET_LEAK` + `SILENT_DEGRADATION` (the day-one headline) stay deterministic floors.

**What this driver is NOT:** it is not the spec-§8 "NARR-4 = frontend demo + multi-model council." That visceral demo + the `S-BS-NARR2-1` corpus-gradeable bridge are **SPLIT to NARR-5**, to be driven after a live reassessment (per the standing "live-reassess before driver lock" practice). This NARR-4 is the floor-honesty precondition the NARR-3 handoff named — small, deterministic, offline, moat-frozen — so the floor is right on real data *before* a demo grades it.

---

## 1. Pre-flight reading (ordered) — re-grepped 2026-06-16

| # | Path | Why |
|---|---|---|
| a | `packs/narrative/ontology.json:143-179` | `verification_contracts` — 3 decls; **remove** the `length_violation` block (`flag_code: LENGTH_VIOLATION`, `contract_type: length_violation`, lines ~166-178) → leaves 2. Update `_provenance.note` (it claims "the 3 P0 deterministic FLOOR executors"). |
| b | `packs/narrative/ontology.json:43-53` + `:134` | the `LENGTH_VIOLATION` flag (stays — `gradeable:true`, `owner_roles:[]`, TIER_2) + the policy_judge question ordinal 2 (stays — the judge lens). **Do not touch** the flag or the question; the demote is *removing the floor contract only*. |
| c | `packs/narrative/floors.py:104-142` (`LengthViolationTool`), `:202-227` (`FLOOR_EXECUTORS`) | **Remove `TOOL_LENGTH_VIOLATION` from `FLOOR_EXECUTORS`** (active floor set → `bracket_leak` + `silent_degradation`). **KEEP** the `LengthViolationTool` class + `_length_reference` + `_SENTENCE_SPLIT` (retained, available for zero-code re-attach if a preamble span ever exists). Update the module docstring (line ~22) to mark `length_violation` RETAINED-BUT-UNATTACHED (demoted per S-BS-NARR3-3). |
| d | `lithrim_bench/verification/spec.py:34-59` | **Byte-UNCHANGED.** `TOOL_LENGTH_VIOLATION` stays registered in `_KNOWN_TOOLS`/`_REQUIRED_REFERENCE_KEYS` (the tool class is retained; the name registration is orthogonal to attachment, and `tests/test_narrative_floor.py:409` asserts all 3 are registered). Removing it would re-touch the CLOSED core set for no reason. |
| e | `packs/narrative/examples/narrative_v1.jsonl:4` | the `narrative_length_violation` by-construction case (`expected_safety_flags:["LENGTH_VIOLATION"]`, `expected_artifact_verdict:"BLOCK"`, a 1-sentence run-on). With the floor gone this is **no longer true-by-construction** (LENGTH_VIOLATION now needs the LLM judge). **Remove it** from the by-construction corpus → 3 cases remain (`narrative_jinn_exposure_clean`, `narrative_jinn_silent_degradation`, `narrative_bracket_leak`). The preamble-overrun *judge* demo belongs to NARR-5 (live-gated), not the offline corpus. |
| f | `tests/test_narrative_floor.py` | the consequence/test-fidelity edits — see §2.2. |
| g | `tests/test_narrative_pack.py:93,275` | `_NARR3_FLOOR_TYPES` drop `length_violation` → `{"bracket_leak","silent_degradation"}`; `assert out["n_verification_contracts"] == 3` → `== 2`. |
| h | `docs/specs/SPEC_NARRATIVE_EVAL.md` §4.3 + §11 | mechanical spec sync — see §2.3 (flip the table row + record the lock; **transcription only, no new judgment**). |

---

## 2. Deliverables

### 2.1 The demote (subtractive)
1. **`packs/narrative/ontology.json`** — delete the `length_violation` entry from `verification_contracts` (2 remain). Edit `_provenance.note`: "3 P0 deterministic FLOOR executors" → "2 deterministic FLOOR executors (bracket_leak, silent_degradation); LENGTH_VIOLATION demoted to the policy_judge lens per S-BS-NARR3-3 (no separable preamble span in the shipped record)."
2. **`packs/narrative/floors.py`** — remove the `TOOL_LENGTH_VIOLATION` stanza from `FLOOR_EXECUTORS`; keep the class + helpers; docstring note (above). `SUPPRESS_EXECUTORS = {}` unchanged.
3. **`packs/narrative/examples/narrative_v1.jsonl`** — remove the `narrative_length_violation` case.
4. **`lithrim_bench/verification/spec.py`** — **no edit** (assert 0-diff in your close-out).

### 2.2 Tests (write the RED proof first)
- **A1 (headline RED→GREEN — the over-fire is fixed):** NEW `test_real_enhanced_scenes_are_not_length_blocked` — for each `source=="enhanced"` scene in `storyworld_session.json`, build a per-scene case (`artifacts:[{type:"narrative_scene", content:clean_text}]`, `finish_reason:"stop"`, `source:"enhanced"`) and `ground(_COUNCIL_PASS, case, ontology=load_ontology())` (the **shipped** narrative ontology). Assert `g.verdict != "BLOCK"` and no floor-injected `LENGTH_VIOLATION` for **all 4** scenes (mountain_road / the_warning / the_descent currently FALSE-BLOCK → genuine RED at parent; GREEN after the demote). This is the non-vacuous proof.
- **A2 (the demote, asserted):** the shipped narrative ontology has **no** `length_violation` verification_contract and exactly 2 (`bracket_leak`, `silent_degradation`). RED at parent (3 incl. length).
- **A3 (headline preserved):** keep `test_silent_degradation_flips_pass_to_block` (`:257`) + the `test_narrative_floor_grades_under_pack_narrative` subprocess (`:395`) GREEN — `SILENT_DEGRADATION` still flips PASS→BLOCK→reject; `BRACKET_LEAK` still fires. (Reconcile the subprocess if it iterates the example corpus — it is now 3 cases.)
- **A4 (tool retained, still correct):** keep the unit `test_length_violation_and_clean` (`:122`) GREEN — `LengthViolationTool` still verifies in isolation (proves "retained, not deleted"; available for re-attach).
- **A5 (judge ownership intact):** assert `taxonomy_snapshot.json` `lenses.policy_judge` still contains `LENGTH_VIOLATION` and the ontology flag + question ordinal 2 are present (owner↔emit satisfied via the lens — no inert owner).
- **Consequence-edits:** the test-local `_NARRATIVE_ONT_DICT` (`tests/test_narrative_floor.py:208-234`) — drop its `length_violation` contract block (lines ~223-234) for fidelity with the shipped ontology; `tests/test_narrative_pack.py:93,275` per §1g.

### 2.3 Spec sync (mechanical transcription only — exact text below)
- **`docs/specs/SPEC_NARRATIVE_EVAL.md` §4.3** — change the `LENGTH_VIOLATION` row's `layer` column from `floor (det.)` to `judge` and its `check` to: `preamble not 3–4 sentences (judge lens — not machine-separable from the authored body in the shipped record, S-BS-NARR3-3)`.
- **§11 (Open questions)** — append: `RESOLVED 2026-06-16 (S-BS-NARR3-3): LENGTH_VIOLATION is a judge lens, not a deterministic floor — the shipped per-scene record carries only clean_text (no preamble span), so preamble-length is not true-by-construction. The LengthViolationTool is retained, unattached, for a future preamble-span (option a) if real data carries it.`
- Do **not** otherwise re-narrate the spec.

---

## 3. Plan-review risks (resolve before code)
- **R1 — `load_ontology()` vs the test-local dict.** A1/A2 must grade through the **shipped** `packs/narrative/ontology.json` (the thing the demote edits), not the test-local `_NARRATIVE_ONT_DICT`. Confirm `load_ontology()` resolves the narrative ontology under `pack=narrative` (mirror `test_narrative_floor_grades_under_pack_narrative:395`). The test-local dict edit is *fidelity only*, not the assertion target.
- **R2 — A1 must be genuinely RED at parent.** Before any edit, write A1 and run it at HEAD `22bf3c6`: it MUST fail for 3 of the 4 scenes (proving the over-fire). If it passes at parent, the test is vacuous — stop and re-derive. (Per diagnose-before-edit, paste the RED output in your close-out.)
- **R3 — removing the example case must not orphan a count/iteration assertion.** Grep every test that loads `narrative_v1.jsonl` or asserts a case count; reconcile to 3. Do not leave a test asserting 4.
- **R4 — the moat + core stay frozen.** `git diff 22bf3c6 HEAD --` of `runtime/council/compliance_council.py`, `signals.py`, `withstands*`, `judge_metric.py`, `tools.py`, `harness/grounding.py`, **and `verification/spec.py`** must be EMPTY. The floor de-registers entirely via the pack. No re-snapshot.

---

## 4. Scope guards / NOT in scope (→ NARR-5)
- **No** moat edit, **no** core edit (`spec.py` 0-diff), **no** `packs/healthcare/` touch, **no** re-snapshot, **no** shell/frontend, **no** council/multi-model work, **no** `load_case` bridge.
- The **S-BS-NARR2-1 corpus-gradeable bridge**, the **multi-model Claude/GPT/Llama council**, the **frontend demo**, and **option (a) preamble-span ingestion** are all **NARR-5** (a separate driver, authored after a live reassessment).

## 5. Exit
- A1–A5 GREEN under `pack=narrative`; the canonical regression gate (`pack=healthcare`) shows **0 new** failures vs parent `22bf3c6` (the documented pre-existing set only); ruff clean on touched files; `verification/spec.py` + the moat files 0-diff; RED→GREEN demonstrated for A1+A2 with the parent-RED output pasted.
- **HARD GATE:** fresh cold critic at close must confirm — the headline (`SILENT_DEGRADATION`) is preserved, the 3 real scenes now pass (the over-fire is fixed), the removed example was genuinely floor-only (not a dodge), and nothing was soft-passed.
