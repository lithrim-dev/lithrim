# Driver — `bench-salvage` phase `WS-6c-DSPy-2`: complete the DSPy judge trio (policy + faithfulness) + the prompt-vs-DSPy A/B harness

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — ROOTED IN `lithrim-bench` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-DSPy-2-complete-trio-and-ab-harness-driver`
> **Version:** v1 · **Authored:** 2026-06-03
> **Last re-verified against code:** 2026-06-03 (`lithrim-bench` re-grepped at `HEAD` of `bench-salvage/ws6c-dspy`, per MONITOR §Phase 1a)
>
> **This is WS-6 step c-DSPy-2** — the follow-on to WS-6c-DSPy (closed `95174c0..4e1d3cc`). DSPy-1
> rebuilt **one** judge (`risk_judge`) on a **role-generic** `Judge` module and proved the §6 hybrid
> (DSPy judges → ported `_apply_consensus`, byte-frozen). DSPy-2 **completes the trio** — wires
> `policy_judge` + `faithfulness_judge` through the SAME `Judge` module (the class is already generic;
> what is missing is their *lenses* + per-role prompt wiring) — and stands up the **A/B harness** that
> measures the **ported prompt-council vs the DSPy-council** on the by-construction packs. This is the
> **judge-calibration substrate** under the calibration-trainer USP (`[[calibration-trainer-is-the-product]]`).
> It does **NOT** change consensus and does **NOT** wire into the grade seam (same frozen-contract posture
> as DSPy-1). Parallel-safe with the shell/journey + calibration-trainer sessions.
>
> **⚠️ DIRTY-TREE / SHARED-BRANCH (load-bearing — this bit WS-6d).** You run on `bench-salvage/ws6c-dspy`,
> which carries **concurrent uncommitted work from other sessions** — the calibration-trainer
> (`lithrim_bench/verification/*`, `harness/grounding.py`, `docs/specs/SPEC_CALIBRATION_TRAINER.md`,
> `docs/research/RUN_*`, `tests/verification/test_dosage_floor.py`), the journey wiring (`apps/`), and
> **3 paper files STAGED in the index** (`docs/PAPER_OUTLINE.md` + two `docs/research/*semantic*`). Your
> deliverables live in `lithrim_bench/runtime/council/` (clean, no overlap), but a bare `git commit`
> would **sweep the staged paper files** into your commit. **NEVER bare-commit. ALWAYS
> `git add <your-files> && git commit -- <your-files>`** and verify scope after every commit. See §6.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c-DSPy-2
(complete the DSPy judge trio — policy + faithfulness — and build the prompt-vs-DSPy A/B harness).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench). EVERYTHING this cycle needs is in-repo:
the EXISTING role-generic DSPy Judge module (lithrim_bench/runtime/council/judges_dspy.py), the
ported council + role prompts (runtime/council/), the bench-accept metric + RISK_JUDGE_LENS
(runtime/council/judge_metric.py), and the by-construction packs. You do NOT read ../lithrim-backend
this cycle. DO NOT change the ported consensus math; DSPy lives strictly ABOVE the per-judge dict seam.

CRITICAL — DIRTY SHARED BRANCH: you are on bench-salvage/ws6c-dspy with concurrent uncommitted work
from other sessions (calibration-trainer: verification/*, harness/grounding.py, docs/specs/
SPEC_CALIBRATION_TRAINER.md, docs/research/RUN_*, tests/verification/test_dosage_floor.py; journey:
apps/; and 3 STAGED paper files). NEVER touch those files. NEVER bare-commit (it sweeps the staged
paper files — this bit WS-6d). ALWAYS: git add <your-files> && git commit -- <your-files>, then
verify with `git show --stat --format= HEAD`.

Read in this order:
  1. .devloop/personas/EXECUTOR.md   (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-2_complete-trio-and-ab-harness_driver.md   (this doc)
  3. docs/specs/RECOMPOSITION_PLAN_ws6.md   (§3 REBUILD-ON-DSPY, §6 the hybrid + the per-judge seam — re-grep section anchors)
  4. lithrim_bench/runtime/council/judges_dspy.py   (the EXISTING role-generic Judge/signature/evaluate_dspy you EXTEND — do NOT rebuild)
  5. lithrim_bench/runtime/council/judge_metric.py   (the EXISTING score_judge/make_judge_metric + RISK_JUDGE_LENS — the lens pattern to mirror)
  6. lithrim_bench/runtime/council/__init__.py:43-49   (the per-judge dict seam — the contract every judge emits)
  7. lithrim_bench/runtime/council/council_roles/{policy_judge.txt,faithfulness_judge.txt,risk_judge.txt}   (lens sources + per-role prompts)
  8. lithrim_bench/runtime/council/compliance_council.py   (_TIER1_OWNERS:232; evaluate:2413 + judge loop:2502 + _load_role_prompts:523 = the prompt-council A/B compares against; _apply_consensus:1866 = KEEP UNCHANGED)
  9. examples/proof_case.jsonl + data/verification_packs/ + lithrim_bench/picklist.py (PACK_FILES)   (the recipe=label A/B case set)

Then post your plan-review per EXECUTOR.md — resolve the 6 PLAN-REVIEW DECISIONS in §3.
Do not write code until I say "go". HARD GATE: fresh-critic at close (user MAY elect inline; precedent WS-6c-DSPy).

Bundle ID: bench-salvage-phaseWS-6c-DSPy-2-complete-trio-and-ab-harness-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-6c-DSPy-2_complete-trio-and-ab-harness_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** the code file:line below were re-grepped against
> `lithrim-bench` at the `bench-salvage/ws6c-dspy` HEAD on 2026-06-03 (CONFIRMED). **NB: `_apply_consensus`
> moved to `:1866`** (was `:1853` in the DSPy-1 driver — lines shifted when WS-6c-AGENTIC added the
> S-BS-31 owners). `RECOMPOSITION_PLAN_ws6.md` is cited by **section**, not line — re-grep its anchors.
> Re-grep yourself before plan-review; surface any drift.

1. `docs/specs/RECOMPOSITION_PLAN_ws6.md` — **§3 REBUILD-ON-DSPY** (the rebuild map) + **§6 the hybrid** (the layered-module diagram + the invariant "optimizing a judge prompt can never weaken a Tier-1 never-event rule because the rule lives *below* the DSPy layer"). DSPy-2 is the §Ratification-Q3 continuation: Q3 = "incremental, one judge first" — DSPy-1 did the one; DSPy-2 does the remaining two + the A/B.
2. `lithrim_bench/runtime/council/judges_dspy.py` — **the module you EXTEND (NOT rebuild).** Already role-generic — confirm before planning:
   - `V2_ROLES = ("risk_judge","policy_judge","faithfulness_judge")` (`:52`); `_ROLE_DEPLOYMENT` maps all three to Azure deployments (`:59-63`).
   - `class Judge(role, *, predictor, lm, role_prompt, taxonomy_context)` (`:230`) — **role-agnostic**; `forward` (`:264`) returns the exact seam dict; role specialization rides the `role_prompt` input.
   - `build_judge_lm(role, **overrides)` (`:200`) — temperature=0 + logprobs + cache, per-role Azure deployment.
   - `evaluate_dspy(judges, *, transcript, artifact, council, gate_mode)` (`:298`) — the §6 hybrid; accepts a **mixed** fan-out (live `Judge`s + pre-built seam dicts) and calls the ported `_apply_consensus` UNCHANGED (`:319`). The DSPy-council = `evaluate_dspy([Judge(r) for r in V2_ROLES])`.
   - confidence comes from `extract_verdict_confidence` (logprobs), `None` for Mistral — **never coerce** (`:278`).
3. `lithrim_bench/runtime/council/judge_metric.py` — **the EXISTING metric + the lens pattern.** `RISK_JUDGE_LENS` (`:31-39`) is the **only lens defined** — derived from `risk_judge.txt` "CODES YOU MAY RAISE". `score_judge(run_judge, cases, *, lens_codes)` (`:91`) → `{accepted, graded, precision, recall, tp, fp, fn, rows}`; `make_judge_metric(*, lens_codes)` (`:144`) the DSPy-style metric. **DSPy-2 adds `POLICY_JUDGE_LENS` + `FAITHFULNESS_JUDGE_LENS` + a `LENS_BY_ROLE` map**, mirroring `RISK_JUDGE_LENS` exactly.
4. `lithrim_bench/runtime/council/__init__.py:43-49` — **the per-judge dict seam** every judge emits (`{model, decision, confidence: float|None, findings:[{taxonomy_code, evidence_spans}], errors:[]}`). The stable boundary; the ported consensus wraps DSPy + non-DSPy judges identically (`:51-53`).
5. `lithrim_bench/runtime/council/council_roles/*.txt` — **the lens sources + the per-role prompts** the live prompt-council loads (and the DSPy `Judge` must be fed the SAME text via `role_prompt=`):
   - `risk_judge.txt` (53 lines) — `CODES YOU MAY RAISE:` (`:16`) / `CODES YOU MAY NOT RAISE:` (`:23`) → the existing `RISK_JUDGE_LENS`. Also the NKA/NKDA suppression (`:1`,`:38`).
   - `policy_judge.txt` (31 lines) — consent is the policy domain; `FABRICATED_CONSENT` (`:19`). No explicit "CODES YOU MAY RAISE" header → derive the lens (plan-review #1).
   - `faithfulness_judge.txt` (164 lines) — `VALUE_MISMATCH` (`:14`), `MISSING_ALLERGY` (`:15`); the v1-`behavior_judge`→v2-`faithfulness_judge` succession also brings its fidelity codes. Derive the lens.
6. `lithrim_bench/runtime/council/compliance_council.py` — **the prompt-council the A/B compares against (KEEP its consensus UNCHANGED):**
   - **The A/B "control" arm:** `ComplianceCouncil.evaluate(...)` (`:2413`), judge fan-out `for model in self.models[:max_models]:` (`:2502`), role prompts loaded by `_load_role_prompts` (`:523`, globs `council_roles/*.txt` → `prompts[file.stem]`) from `_ROLE_PROMPTS_DIR` (`:470`); `self.models` set `:518`. `build_prompt:530` + `_normalize_result:1474` are the imperative per-judge layer the DSPy side replaces ABOVE the seam.
   - **KEEP byte-identical (DSPy strictly above):** `_apply_consensus:1866`, the v2 llama-veto / worst-of combine, `extract_verdict_confidence` (None-tolerant), the tier tables + `_TIER1_OWNERS:232`. **Do not touch.**
   - `_TIER1_OWNERS:232` — the **post-S-BS-31** owner table: `faithfulness_judge` owns `MISSING_ALLERGY`+`VALUE_MISMATCH`; `policy_judge` owns `FABRICATED_CONSENT` (comments `:237`/`:249`/`:259`). Your lenses (D1) MUST be consistent with this for the Tier-1 codes (plan-review #1; S-BS-12 watch).
7. `examples/proof_case.jsonl` + `data/verification_packs/` + `lithrim_bench/picklist.py` (`PACK_FILES`) + `data/ontology/clinical_v1.json` — the **by-construction recipe=label** A/B case set (a judge's findings scored vs the case `expected_safety_flags`).
8. `lithrim_bench/runtime/council/tests/` (`test_judges_dspy.py`, the WS-6c oracle, `test_consensus.py`) — the **offline test pattern** (`pytest.importorskip("dspy")`, injected predictor, no network) to extend to the trio + the A/B diff.
9. `pyproject.toml` — `[verification]` (dspy) + `[council]` (openai) carry the DSPy-judge path; `testpaths` includes `runtime/council/tests`. Default install (pydantic+pandas) must stay dspy/openai-free.

---

## 2. Deliverables (file-by-file)

1. **`lithrim_bench/runtime/council/judge_metric.py`** (EXTEND) — the two missing lenses + the map.
   - Add `POLICY_JUDGE_LENS` and `FAITHFULNESS_JUDGE_LENS` (`frozenset`s, mirroring `RISK_JUDGE_LENS:31`), derived from `council_roles/{policy,faithfulness}_judge.txt` "may raise" scope, **cross-checked against `_TIER1_OWNERS:232` and `taxonomy/taxonomy_snapshot.json`** (the exact code sets resolved at plan-review #1).
   - Add `LENS_BY_ROLE = {"risk_judge": RISK_JUDGE_LENS, "policy_judge": POLICY_JUDGE_LENS, "faithfulness_judge": FAITHFULNESS_JUDGE_LENS}` so the harness/metric can look a lens up by role.
   - A short module docstring note: a lens code that is NOT in the taxonomy snapshot is surfaced (S-BS-12), not silently scored.
2. **`lithrim_bench/runtime/council/ab_harness.py`** (NEW — or `scripts/ab_council.py`; home is plan-review #2) — the **prompt-council vs DSPy-council A/B**.
   - Build the DSPy trio: `[Judge(role, role_prompt=<council_roles/role.txt text>, lm=build_judge_lm(role)) for role in V2_ROLES]` → run via `evaluate_dspy`.
   - Build the control: `ComplianceCouncil(...).evaluate(...)` (the ported prompt fan-out) on the SAME case set.
   - Emit a structured diff: **verdict agreement** (per case: prompt vs DSPy composite verdict), **per-judge findings agreement** (raised-code sets per role), **per-judge calibration** (confidence; `None`-aware), and each arm's **`score_judge` result per role** (lens from `LENS_BY_ROLE`).
   - **Two modes:** `offline-structural` (both arms fed fixtured/cached per-judge outputs → deterministic, `$0`, exercises the diff + scoring logic) and `live` (both arms call Azure → the real comparison; **cost-confirmed**, plan-review #4). Offline-first.
   - **No new grade entrypoint, no `run_eval`/`grade` import** — the harness is a standalone measurement tool over `evaluate_dspy` + `ComplianceCouncil.evaluate`.
3. **`lithrim_bench/runtime/council/judges_dspy.py`** (MINIMAL EXTEND, only if needed) — a small convenience to assemble the role-prompt-bound trio (e.g. `build_trio()` reading `council_roles/*.txt` and returning three `Judge`s), IF that helper is cleaner than inlining it in the harness. **No change to `Judge`/`evaluate_dspy`/the signature** — they are already generic; if you find a real per-role gap (e.g. the single `JudgeSignature` cannot carry policy-vs-faithfulness specialization through `role_prompt` alone), STOP and surface it in plan-review rather than editing the seam.
4. **`lithrim_bench/runtime/council/tests/`** (offline; extend the WS-6c oracle + DSPy-1 tests) — `pytest.importorskip("dspy")`, injected predictors, no network:
   - (a) the **trio** emits the seam for all three roles incl. the `policy_judge` (Mistral) `confidence=None` round-trip (never coerced) and `errors:[…]` on a simulated failure;
   - (b) `evaluate_dspy` with the **3-judge trio** + the ported `_apply_consensus` reproduces the **WS-6c oracle verdicts** (Tier-1 one-strike incl. the post-S-BS-31 `faithfulness`/`policy` owners, Tier-2 2+, PHI-FP, llama-veto, artifact-BLOCK, None-confidence) on fixtured judge outputs;
   - (c) `score_judge` runs with `POLICY_JUDGE_LENS` + `FAITHFULNESS_JUDGE_LENS` on fixtured outputs (incl. an out-of-lens raise counted as FP);
   - (d) a **lens↔snapshot↔owner guard**: every lens code ∈ `KNOWN_TAXONOMY_CODES`; for Tier-1 lens codes the role is the `_TIER1_OWNERS` owner (S-BS-12-adjacent);
   - (e) the **A/B diff logic** on fixtured both-arms (deterministic): agreement counts + per-role scores compute correctly;
   - (f) the consensus-IP-unchanged guard stays green (`test_consensus.py`).
5. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2-2026-XX-XX.json`** — session log incl. the **lens derivation + the snapshot/owner cross-check** (which codes, which source line, any S-BS-12 surfacing), the **trio seam-conformance** (per role), the **A/B result** (verdict-agreement %, per-role precision/recall on the lens, calibration deltas), the **consensus-unchanged diff** (zero logic delta vs the DSPy-1 tip), and the live A/B verdict + cost (if run). A short REPORT if the A/B numbers warrant one (paper-bearing).

---

## 3. Plan-review checkpoint (non-negotiable)

Per `EXECUTOR.md`, post understanding + file-by-file plan + test plan + risks + deviations. **Wait for "go".** Resolve:

1. **The two lens code-sets (D1).** Resolve `POLICY_JUDGE_LENS` + `FAITHFULNESS_JUDGE_LENS` exactly — derived from `council_roles/{policy,faithfulness}_judge.txt` scope, cross-checked against `_TIER1_OWNERS:232` + `taxonomy/taxonomy_snapshot.json`. State each code + its source line. **Surface any lens code NOT in the snapshot (S-BS-12) — do not silently include it.** *(Seed from the prompts: policy ≈ `{FABRICATED_CONSENT, …}`; faithfulness ≈ `{VALUE_MISMATCH, MISSING_ALLERGY, INCOMPLETE_DOCUMENTATION, FABRICATED_HISTORY, HALLUCINATED_DETAIL, …}` — confirm against the prompt text + owners, don't take this as final.)*
2. **A/B harness home + diff schema.** `runtime/council/ab_harness.py` (importable, testable) vs `scripts/ab_council.py` (a runner). *(Default: a `runtime/council/ab_harness.py` module with a thin `scripts/` runner if a CLI is wanted — the module is what the offline test imports.)* Plus the diff record shape (verdict agreement / per-judge code-set agreement / calibration / per-arm `score_judge`).
3. **Optimizer scope this cycle.** Default = **NO optimizer** — land the trio + the A/B **baseline** + the bench-accept gate (the WS-3a discipline: ship the assurance primitive, the optimizer climbs the gradient in a follow-up, WS-6c-DSPy-3). *(OPTION, only if the user wants the calibration payoff now: ONE cost-capped `BootstrapFewShot` pass on a SINGLE judge against `make_judge_metric` — confirm the Azure cost envelope explicitly; it burns many candidate calls.)*
4. **A/B case set + cost.** Which packs + N for the live arm (the by-construction packs are small — `proof_case` + the verification packs). Offline-structural first ($0). State the **cost envelope** for the ONE live A/B run and cost-confirm before it (standing rule). *(Default: offline-structural for all logic + ONE cost-confirmed live A/B on a small N, ~$1–3 envelope.)*
5. **Determinism for the trio.** temperature=0 + DSPy cache + seed across all three roles (already in `build_judge_lm`). Confirm the **Mistral `policy_judge` `None`-confidence** path holds end-to-end (no logprobs → `None`, never coerced) — the policy arm is the new None-path relative to DSPy-1's risk_judge.
6. **Branch + commit hygiene (the landmine).** STAY on `bench-salvage/ws6c-dspy` (do NOT cut a new branch — uncommitted workbench changes would follow you anyway). Confirm you will commit with **explicit pathspec only** and verify scope after each commit (§6). Confirm you will NOT stage/commit any concurrent-session file.

---

## 4. Scope guardrails — NOT in scope

- **NO change to the ported consensus IP** — `_apply_consensus:1866`, the v2 llama-veto / worst-of combine, `extract_verdict_confidence`, the tier/owner tables stay **byte-identical to the DSPy-1 tip**. DSPy lives strictly ABOVE the per-judge dict seam. (The §6 invariant.)
- **NO `Judge` / `evaluate_dspy` / signature rewrite** — they are already role-generic. If a real per-role gap appears, surface it; do not silently refactor the seam.
- **NO harness grade-seam wire / in-process grade** — `grade.py` / `run_eval.run` / `report.composite` / §10 untouched. The DSPy-council is importable + A/B-runnable, NOT wired into `grade` (same as DSPy-1; that swap is a separate, later decision).
- **NO `_TIER1_OWNERS` hand-edit, NO taxonomy re-snapshot** — S-BS-31 is CLOSED (owners reassigned at AGENTIC); `production_judges` already = the v2 trio. Lenses READ the owner table; they do not change it.
- **NO optimizer auto-harden** beyond the optional, cost-capped single pass in plan-review #3 (WS-3a §7 meta-finding: land the GATE, not the fallible auto-harden).
- **NO persistence / KB / pinecone / ONNX** (WS-6d / WS-6d-KB); **NO** `citations_used` emission (S-BS-33 is deferred to WS-6d-KB).
- **NO touching ANY concurrent-session file** — `lithrim_bench/verification/*`, `lithrim_bench/harness/grounding.py`, `apps/*`, `docs/PAPER_OUTLINE.md`, `docs/research/REPORT_semantic*`/`SYNTHESIS_*`/`RUN_*`, `docs/specs/SPEC_CALIBRATION_TRAINER.md`, `tests/verification/test_dosage_floor.py`. These belong to the calibration-trainer / journey / paper sessions. Never stage or commit them.
- Change **NO code in `../lithrim-backend`** and **NO `etlp-mapper`**. No drive-by reformatting.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** The **full trio** (`risk`, `policy`, `faithfulness`) builds via the existing `Judge` module bound to each role's `council_roles/*.txt` prompt + Azure deployment, and each emits the **exact §6 seam** (`{model, decision, confidence: float|None, findings:[{taxonomy_code, evidence_spans}], errors:[]}`) — incl. the `policy_judge` (Mistral) `confidence=None` round-trip, **never coerced**, and `errors:[…]` on a simulated failure.
- **A2.** `POLICY_JUDGE_LENS` + `FAITHFULNESS_JUDGE_LENS` + `LENS_BY_ROLE` land; each lens ⊆ `KNOWN_TAXONOMY_CODES` and is **consistent with `_TIER1_OWNERS:232`** for its Tier-1 codes; all three judges are `score_judge`-scored on a by-construction pack (recipe=label) with their lens. Any lens code outside the snapshot is surfaced (S-BS-12), not silently scored.
- **A3.** The **A/B harness** runs the ported prompt-council vs the DSPy-council on the same case set and emits the structured diff (verdict agreement, per-judge code-set agreement, per-judge calibration, per-arm `score_judge`). The **offline-structural mode is deterministic** ($0); the live mode is cost-confirmed.
- **A4.** `evaluate_dspy` with the **3-judge trio** + the ported `_apply_consensus` reproduces the **WS-6c offline oracle verdicts** (incl. the post-S-BS-31 `faithfulness`/`policy` Tier-1 one-strike) on fixtured judge outputs — the trio generalization of DSPy-1's A3.
- **A5.** The **ported consensus IP is UNCHANGED** — `_apply_consensus` / the v2 combine / `extract_verdict_confidence` / the tables byte-identical to the DSPy-1 tip (diff-vs-tip = zero logic delta in the session log); DSPy strictly above the seam.
- **A6.** **No contract change** — `grade.py` / `run_eval.run` / `report.composite` / §10 untouched; the DSPy-council is importable + A/B-runnable but **not wired into the grade seam**. `_TIER1_OWNERS` read-only (no hand-edit).
- **A7.** **Default install unchanged** — the DSPy-judge + harness path is gated behind `[verification]` (dspy) + `[council]` (openai); `pytest -q` green on default deps with the dspy tests `importorskip`-skipping; `import lithrim_bench` pulls no dspy/openai; ruff clean on new files.
- **A8.** **Scope held + commit hygiene** — no consensus-math / grade-wire / persistence / KB / backend / etlp edits; **NO concurrent-session file staged or committed**; the close diff `git diff <cycle-parent> HEAD --stat` shows ONLY DSPy-2 files (the 3 staged paper files NOT swept); every commit used an explicit pathspec.

### Diagnostic stats (NOT acceptance gates)
- The **A/B result**: verdict-agreement % (prompt vs DSPy), per-role precision/recall on the lens, per-role calibration delta — reported (paper-bearing).
- The **optimizer delta** (baseline vs optimized accept-rate) IF the optional pass is run — reported, not a gate.
- The **live A/B** verdict + per-judge confidence + cost (if run).
- Lens derivation table (code → source line → owner → in-snapshot?).

---

## 6. Commit structure + message templates

Atomic per-concern, in `lithrim-bench` on `bench-salvage/ws6c-dspy`. **Explicit pathspec on every commit (the dirty-tree landmine).** Suggested order:

1. `feat(council-dspy): policy + faithfulness lenses + LENS_BY_ROLE (snapshot/owner-checked)` — `runtime/council/judge_metric.py` (+ `judges_dspy.py` trio helper if any)
2. `feat(council-dspy): prompt-vs-DSPy A/B harness (offline-structural + cost-gated live)` — `runtime/council/ab_harness.py`
3. `test(council-dspy): trio seam + 3-judge consensus oracle + lens/owner guard + A/B diff (offline)` — `runtime/council/tests/`

**Commit hygiene (MANDATORY):**
```
git add <only your files>
git commit -- <only your files>            # NEVER a bare `git commit`
git show --stat --format= HEAD             # verify: only your files
```
At close: `git diff <cycle-parent> HEAD --stat` must show ONLY DSPy-2 files. If a concurrent file appears, STOP — you swept it; do not proceed.

Each body ends with `Executed per bench-salvage-phaseWS-6c-DSPy-2-complete-trio-and-ab-harness-driver by session-…` + `See …session-…json` + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **Diagnose-before-edit:** any fidelity/fix claim carries a fenced evidence block (lens cross-check, seam-conformance, consensus-unchanged diff). Do NOT auto-commit — stage + propose each group for "go".

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A8 PASS (trio emits the seam incl. Mistral None; 2 lenses + map, snapshot/owner-checked; A/B diff runs offline-deterministic + cost-gated live; trio reproduces the WS-6c oracle; consensus byte-identical; no contract change; default install unchanged; scope + commit hygiene held)
- [ ] The ported consensus functions show **zero logic delta** vs the DSPy-1 tip (diff in the session log)
- [ ] No `grade.py` / `run_eval.py` / `report.py` / §10 change; council still NOT wired into grade; `_TIER1_OWNERS` unedited
- [ ] **NO concurrent-session file** in the diff; `git diff <parent> HEAD --stat` = only DSPy-2 files; every commit pathspec'd; the 3 staged paper files NOT swept
- [ ] No services autostarted (user starts them); the ONE live A/B call cost-confirmed first
- [ ] No `../lithrim-backend` / `etlp-mapper` edit; no pushes/tags
- [ ] `pytest -q` green on default deps (dspy tests importorskip-skip); `import lithrim_bench` dspy/openai-free; ruff clean on new files
- [ ] Session log written (lens derivation + snapshot/owner cross-check + trio seam-conformance + A/B result + consensus-unchanged diff + live cost)

---

## 8. First move

1. Read `EXECUTOR.md` + RECOMPOSITION_PLAN §3/§6.
2. Re-grep the §1 anchors at the `ws6c-dspy` HEAD (the generic `Judge`/`evaluate_dspy` in `judges_dspy.py`; `RISK_JUDGE_LENS`/`score_judge` in `judge_metric.py`; the seam at `__init__.py:43`; `_TIER1_OWNERS:232`; `evaluate:2413`/`_load_role_prompts:523`; `_apply_consensus:1866`). Confirm `[verification]`+`[council]` carry dspy+openai.
3. `git status` — confirm the dirty workbench + the 3 staged paper files; internalize the pathspec-only commit rule (§6).
4. Read §2–§7.
5. Post plan-review per §3 (the 6 decisions, incl. the two lens code-sets + optimizer-vs-baseline + the live A/B cost). Wait for "go".

---

## 9. References

- Plan: `docs/specs/RECOMPOSITION_PLAN_ws6.md` (§3 REBUILD-ON-DSPY, §6 hybrid + per-judge seam, §Ratification Q3)
- The seam contract: `lithrim_bench/runtime/council/__init__.py:43-49`
- The module to extend: `lithrim_bench/runtime/council/judges_dspy.py` (`Judge:230`, `evaluate_dspy:298`, `build_judge_lm:200`)
- The metric + lens pattern: `lithrim_bench/runtime/council/judge_metric.py` (`RISK_JUDGE_LENS:31`, `score_judge:91`, `make_judge_metric:144`)
- The prompt-council (control arm; consensus KEEP UNCHANGED): `compliance_council.py` (`evaluate:2413`, `_load_role_prompts:523`, `_apply_consensus:1866`, `_TIER1_OWNERS:232`)
- Role prompts / lens sources: `lithrim_bench/runtime/council/council_roles/{risk,policy,faithfulness}_judge.txt`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (seams S-BS-7 / S-BS-12 / S-BS-31-closed / S-BS-33)
- Prior cycle: `.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2026-06-02.json` + critique `critique-bench-salvage-phaseWS-6c-DSPy-2026-06-02.md`
- Product framing: `[[calibration-trainer-is-the-product]]` (DSPy-judge = the calibration substrate); `docs/specs/SPEC_CALIBRATION_TRAINER.md` (the concurrent session's; READ-ONLY context — do not edit)
- Off-ramp follow-up: **WS-6c-DSPy-3** (the optimizer pass climbing `make_judge_metric`, once the A/B baseline + gate are proven)

---

## Hardness

- [x] **HARD GATE** — authors prompt wiring for two more safety-critical judges (policy + faithfulness) and stands up the A/B that informs whether the DSPy-council could one day replace the prompt-council. Lower blast radius than a consensus change (no consensus-math edit, no grade-wire), but the lens↔owner↔snapshot cross-check (A2), the trio seam-conformance (A1), and the consensus-unchanged proof (A5) are load-bearing, and an independent read of the two new lenses + the A/B diff logic earns its keep. Fresh-critic at close recommended (`KICKOFF_CRITIC.md`); **user MAY elect inline** (precedent: WS-6c, WS-6c-DSPy).
