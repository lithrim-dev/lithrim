# Driver — `bench-salvage` phase `UAP-3b-2`: complete the moat — the LIVE attestation + the withstands provenance blob (S-BS-72) + GroundingChecks as first-class entities

> **Generated by the monitor (expanded from the UAP-3b-close stub). The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseUAP-3b-2-groundingcheck-entity-llm-critique-driver`
> **Version:** v1 (expanded from v0-stub)
> **Parent:** `bench-salvage-phaseUAP-3b-withstands-gate-driver` (THE MOAT CORE landed — D1 signals + D2 gate + D4 audit, offline/$0; `8cb388b..9eadf39`)
> **Authored:** 2026-06-04 (stub by the UAP-3b executor; expanded by the monitor)
> **Last re-verified against code:** 2026-06-04 (per MONITOR §Phase 1a — all §1 citations re-grepped this session against HEAD `8bd8ac9`: `grade.py:129-170`, `run_eval.py:169/205/237/269-299`, `provenance.py:62/76`, `config.py EvalProfile`, `withstands.py:78-86` [no `critique=` param yet], `grounding.py` registries — all CONFIRMED)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase UAP-3b-2
(complete the moat: the LIVE paid attestation of the withstands-gate flip on the REAL
trio + embed the withstands ruling in the run-provenance blob [S-BS-72] + GroundingChecks
as first-class config-authored entities [the deferred UAP-3b A6]).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseUAP-3b-2_groundingcheck-entity-llm-critique_driver.md  (this doc)
  3. docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md  (§2A the withstands-gate + GroundingCheck =
     independent entity [63]; §2B audit [89/102/107]; §13 [268 locus=BOTH])
  4. .devloop/state/STREAM_bench-salvage.md  (the UAP-3b close row + S-BS-70/72/73 + the seam table)
     + .devloop/sessions/critique-bench-salvage-phaseUAP-3b-2026-06-04.md (the moat-exhibit reasoning)
     + .devloop/sessions/session-bench-salvage-phaseUAP-5a-2026-06-04.json (the LIVE-flip non-convergence lesson)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Resolve the 5 decisions in §3 (esp. D-B: the LIVE-attestation construction + halt-to-surface,
and D-A: split the LLM critique to UAP-3b-3). Do not write code until I say "go".

Bundle ID: bench-salvage-phaseUAP-3b-2-groundingcheck-entity-llm-critique-driver
Driver doc: .devloop/prompts/bench-salvage_phaseUAP-3b-2_groundingcheck-entity-llm-critique_driver.md
```

---

## 0. Why this phase (finish what UAP-3b proved)

UAP-3b proved THE MOAT **offline/$0** (A2: a deterministic signal corrects a confidently-wrong
out-of-lens judge pre-consensus → the composite flips, gate-attributable, via injected
predictors). Three things were the pre-authorized split (UAP-3b D-A/D-E):

1. **The LIVE attestation (S-BS-70's visceral finale).** The paid "watch a REAL LLM judge get
   corrected by a deterministic signal → the verdict change" is still owed. This is the whole
   point of the S-BS-70 arc — and the validation of **S-BS-73** (the gate is now ON by default
   on the authored in_process path; confirm it behaves correctly on a real trio).
2. **S-BS-72 — the withstands ruling in the run-PROVENANCE blob.** Today the ruling lands in the
   `AuditLog`/config_audit stream + the corrections NDJSON, but NOT the `SqliteProvenanceStore`
   run-blob (`/v1/runs/{id}/audit`). §2B says the processing audit is the full chain incl. "the
   critique's withstands-decisions" — finish it.
3. **GroundingChecks as first-class config-authored entities (the deferred UAP-3b A6).** Promote
   the `harness/grounding.py` floor/suppress contracts to **independent, config-declared
   GroundingCheck entities**, run at the post-consensus locus, distinguished from the
   **judge-attached** validators the UAP-3b signals bus already consumes.

**The LLM Ralph-Loop critique (UAP-3b D-A) is NOT in this driver's core** — see §3 D-A. The
deterministic moat is proven; the LLM critique's value-add is uncertain (`critique-pass-precision-not-floor`:
self-critique alone does not close the gap) and it adds cost + a new LLM pass — a separate
research cycle (**UAP-3b-3**). Monitor lean: SPLIT it.

**⚠️ Carry the UAP-5a lesson (read its session log):** UAP-5a's live flip did NOT reproduce
because the REAL trio didn't behave as a by-construction case needed. The LIVE attestation here
faces the same risk — and a SUBTLETY: the gate's out-of-lens rejection uses `effective_lens` =
base lens **+ assigned flags** (`withstands.py:121`), so **assigning a flag to a judge puts it
IN-lens → the gate keeps it.** You cannot force the out-of-lens flip by authoring. The live flip
needs the real trio to **genuinely over-fire** an out-of-lens code (the DSPy-3b failure mode) —
which is empirical and uncertain. **Pre-authorize halt-to-surface + the graded honest outcomes
in §5 A1; do NOT manufacture a flip.**

---

## 1. Pre-flight reading (ordered) — citations re-grepped 2026-06-04 (HEAD `8bd8ac9`)

1. `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md` — **§2A** (GroundingCheck = a first-class
   INDEPENDENT entity, executed per eval alongside the judges, line :63; the withstands-gate
   :66-74), **§2B** (the processing audit = the full chain incl. the critique's withstands-decisions
   :107; the `withstand/flip` action :89; the ruling shape :102), **§13** (:268 locus = BOTH —
   the independent GroundingChecks run post-consensus, the per-judge critique pre-consensus).
2. `scripts/run_eval.py` — **the wiring already built (compose, don't rebuild):** `withstands_sink`
   (`:169`) ← `build_authored_semantic_stage(…, apply_gate=True default, decisions_sink=withstands_sink)`
   (`:202-205`) → `grade_inprocess(…, provenance_store=SqliteProvenanceStore(db_path=collections_db))`
   (`:207-211`); the **post-consensus** `ground(result, case, ontology)` (`:237`) → `composite(grounded)`
   (`:238`) = the GroundingCheck locus (D3); **the withstands audit loop** (`:274-299`:
   `AuditLog.record(action withstand|flip)` + `build_withstands_correction`/`emit`) = where S-BS-72
   extends. **`:269-273` literally says the run-PROVENANCE-blob embedding is deferred to UAP-3b-2
   (needs an orchestrator seam above the frozen consensus)** — your D2 target.
3. `lithrim_bench/harness/grade.py:129-170 grade_inprocess(*, semantic_stage=, provenance_store=)` —
   `LocalPipelineBackend.evaluate_pipeline(case)` (`:166`) builds the result AND the backend saves the
   provenance blob INTERNALLY (fire-and-forget behind `save`); the returned dict is byte-identical with
   the store on/off (the frozen-contract A3). **`run_eval` has no pre-save handle** — this is the S-BS-72
   seam (decide the approach at §3 D-C).
4. `lithrim_bench/runtime/pipeline/provenance.py` — `SqliteProvenanceStore.save(provenance, …)`
   (`:62` — `provenance.model_dump(mode=json)` → `PIPELINE_RUNS.insert`) + `find_by_id(pipeline_run_id)`
   (`:76` — `PIPELINE_RUNS.get`). The S-BS-72 blob target. **Note the `created_at` re-stamp risk** on a
   post-save re-insert (S-BS-68) if you choose the patch approach.
5. `lithrim_bench/runtime/council/withstands.py:78-86` — `apply_withstands_gate(results, *, ontology,
   case, lens_by_role, assignments, http_client)` — **NO `critique=` param yet** (the LLM critique would
   ADD it; the docstring `:27-29` notes the gate is "shaped to admit it"); `effective_lens` (`:121`) =
   base lens + assignments (the in-lens subtlety above). `WithstandsDecision.to_audit_why()` (`:59`) =
   the §2B ruling. **REUSE the gate; do not rewrite its logic.**
6. `lithrim_bench/runtime/council/signals.py` — `build_judge_signals` (the validator-output signals reuse
   `grounding._build_contract` + the executors) — the GroundingCheck entity work composes over this.
7. `lithrim_bench/harness/grounding.py` — the floor/suppress contracts to promote to GroundingCheck
   entities: `_CONTRACT_EXECUTORS={presence_check,kb_grounding}` (`:290`), `_FLOOR_CONTRACT_TYPES`
   (`:298`), `ground()` (`:398`), `GroundedResult{suppressed,floor_blocks}` (`:53`). + `harness/ontology.py
   VerificationContractDecl` (`:62`) — the contracts the entities view over.
8. `lithrim_bench/harness/config.py` — `EvalProfile{judges, council_config, ontology_ref, ontology_path,
   tools, kb_bindings, severity_map_ref}` (`:53-59`) + `agent_from_dict` (`:90-111`) / `agent_to_dict`
   (`:113+`) — where a `grounding_checks` field surfaces additively (D3).
9. `harness/correction.py:build_withstands_correction` (`uap3b-withstands-correction/1`) + the `withstand`
   `AuditRecord` action (`audit.py:83`) — the UAP-3b audit substrate D2 extends.
10. **For the LIVE attestation (D1):** `apps/bff/app.py` `/v1/run-eval` (the `in_process` mode) +
    `apps/shell/src/genui/RunPanel.jsx` (the in-browser verdict/votes capture). Memory
    `browser-mcp-confirm-blocks-renderer` — **drive the PAID in_process run via the BFF API, not the
    `window.confirm()`-gated UI** (S-BS-69; the gate freezes CDP).
11. `.devloop/sessions/session-bench-salvage-phaseUAP-5a-2026-06-04.json` — **the live-flip
    non-convergence lesson** (the real trio may not behave as a by-construction case needs → halt-to-surface,
    honest fallback). Memory `judge-creation-must-be-demonstrable` (S-BS-70) + `critique-pass-precision-not-floor`
    (the LLM-critique-value caveat → split) + `persistence-blob-projection-architecture` (S-BS-72 blob tier).

> **Citation discipline:** every file:line above was re-grepped 2026-06-04 against HEAD `8bd8ac9`.
> If you spot drift on read, halt and surface it (EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

**A. D1 — The LIVE moat attestation (S-BS-70 finale + S-BS-73 validation).** *Lead deliverable.*
1. A **cost-gated real-trio `in_process` run** demonstrating the gate operating LIVE on the real v2
   Azure trio (`apply_gate=True` default). The BEST outcome = a real out-of-lens over-fire the gate
   rejects → the composite flips, captured (drive via the BFF API per S-BS-69, or in-browser via
   RunPanel). **Construct→verify, cost-confirmed each run, HALT-TO-SURFACE** (the real trio's behavior
   is the lever; you cannot force the out-of-lens flip by authoring — see §0). Record the live
   per-judge withstands ruling + the verdict. **Name the envelope at plan-review** (~$0.10–0.20/run;
   budget a few; cost-confirm before each).
2. The acceptance is GRADED (see §5 A1) — a true live flip is the prize, but a documented
   live-gate-correction (the gate corrects a real finding + the ruling renders + S-BS-73 holds: the gate
   does NOT break a legitimate real finding) is an honest PASS; a no-convergence is halt-to-surface, NOT
   a manufactured case.

**B. D2 — S-BS-72: the withstands ruling in the run-PROVENANCE blob.**
3. Embed each `WithstandsDecision` (the §2B `{signals_weighed, decision, what_failed}` ruling) into the
   `SqliteProvenanceStore` run-blob so `/v1/runs/{id}/audit` (stream-2) shows it, not just `/v1/audit`
   (stream-1). **ABOVE the frozen `_apply_consensus`** (`LocalPipelineBackend` + the provenance build are
   NON-frozen). **§3 D-C decides the approach:** (a) orchestrator-seam — thread the sink into the
   provenance build at save time (clean blob); vs (b) post-save patch in `run_eval` — `find_by_id(run_id)`
   → embed → re-save (simpler, but mind the `created_at` re-stamp, S-BS-68). Either way: NO `_apply_consensus`
   edit; the returned `result` dict stays byte-identical (A3).

**C. D3 — GroundingChecks as first-class config-authored entities (the deferred UAP-3b A6).**
4. Surface the floor/suppress contracts as **independent, config-declared GroundingCheck entities** — an
   additive `EvalProfile.grounding_checks` view over the ontology `verification_contracts` (`config.py` +
   `agent_from_dict`/`agent_to_dict`), run at the existing **post-consensus** `ground()`/`composite()`
   locus (`run_eval.py:237-238`), **distinguished from the judge-attached validators** the UAP-3b signals
   bus consumes. Each independent GroundingCheck execution is audited as its own entity (an `AuditRecord`,
   actor.type `grounding_check`). **`ground()`/`composite()` ADDITIVELY IDENTICAL** for the floor-less
   `clinical_v1` ontology (verdict + partitions byte-unchanged). *(Light surface + audit; a config-plane
   GroundingCheck CRUD UI stays OUT → follow-on.)*

**D. D4 — Tests.**
5. `tests/test_uap3b2_*.py` — (a) **S-BS-72**: a graded in_process run's provenance blob (`find_by_id`)
   carries the withstands ruling (offline/$0 via injected predictors + an injected `SqliteProvenanceStore`);
   (b) **A6**: a GroundingCheck entity runs at the post-consensus locus + is audited; `ground()` additively
   identical for `clinical_v1` (the suppress/floor partitions byte-unchanged); (c) **frozen seam 0-delta**;
   (d) the gate STILL cannot relabel a true case (the A5 guard, re-pinned). The LIVE attestation (D1) is a
   cost-gated manual/recorded run, NOT an automated test.

**E. D5 — Docs.** Note S-BS-72 closed + the GroundingCheck-entity surface + the S-BS-73 disposition (live-validated
   or carried) + the LLM-critique split → UAP-3b-3 (write the stub). `SPEC_PRODUCT_SHELL.md §10` only if the BFF
   surface changes (a `grounding_checks` field on the eval-profile read may touch it — note if so).

**DEFER → UAP-3b-3 (D-A split):** the LLM Ralph-Loop critique pass (the `critique=` hook + a cost-gated
gpt-4.1 reasoned withstands-ruling). Write the stub so it's deferred-not-dropped.

---

## 3. Plan-review checkpoint (non-negotiable)

Post: understanding · file-by-file · test plan · risks · **the 5 decisions** · any deviations.

**The 5 load-bearing decisions:**
- **D-A. LLM Ralph-Loop critique — IN or SPLIT?** Monitor lean: **SPLIT → UAP-3b-3.** The deterministic moat
  is proven; the LLM critique's value over it is uncertain (`critique-pass-precision-not-floor`) + it adds
  cost + a new LLM pass. UAP-3b-2 = operationalize (D3) + audit (D2) + attest (D1). Confirm or argue to pull
  it in.
- **D-B. The LIVE-attestation construction + cost + halt-to-surface (the hard one).** How will you get the
  REAL trio to exhibit a gate-correctable finding (a real out-of-lens over-fire is the gate-attributable prize
  but cannot be forced by authoring; a validator-disprovable finding is correctable but the composite flips
  via `ground()` too, so it's a CONSENSUS-coherence demo not a composite-flip)? Name the case, the cost
  envelope, and the halt-to-surface fallback (per the UAP-5a lesson). **Do NOT manufacture a flip.**
- **D-C. The S-BS-72 approach.** Orchestrator-seam (thread the sink into the provenance build) vs post-save
  patch in `run_eval` (`find_by_id`→embed→re-save). Confirm A3 (no `_apply_consensus` edit; returned dict
  byte-identical) + the `created_at` handling (S-BS-68) if you patch.
- **D-D. The GroundingCheck entity surface (D3).** Light (`EvalProfile.grounding_checks` view + audit) vs a
  full config-plane CRUD (monitor lean: **light**, defer CRUD).
- **D-E. SPLIT confirm.** Is D1+D2+D3+D4 a full cycle (with the LLM critique → UAP-3b-3)? If even this is too
  big, propose splitting D3 (GroundingChecks) OR keeping D1 (live attestation) + D2 (S-BS-72) as the core
  (flag, don't drop).

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

- **The LLM Ralph-Loop critique pass** (the `critique=` hook + the gpt-4.1 reasoned ruling) → **UAP-3b-3**
  (if D-A splits — monitor lean). The deterministic gate is the floor; the LLM critique is enrichment.
- **Optimize / calibration-in-UI (R5)** → UAP-4 (S-BS-49).
- **The conversational shell / chat / SSE (R11)** → UAP-5b.
- **Live `:8002` per-judge signal injection** → S-BS-64 (WS-2 backend, paused). The attestation is in_process.
- **A full config-plane GroundingCheck CRUD UI** → follow-on (D3 is light surface + audit).
- **The withstands-gate LOGIC** (`withstands.py` `apply_withstands_gate`) is **REUSED, not rewritten** —
  D1/D2/D3 compose over it; only the LLM critique would add the `critique=` param (and that's split out).
- **Re-snapshotting the ontology `owner_roles` (S-BS-59)** — separate pass.
- **FROZEN:** `compliance_council._apply_consensus` + the per-judge seam dict (`judges_dspy.py`) +
  `judge_metric.py` + the committed seeds. New demo cases/agents are additive.
- Drive-by formatting / dep bumps.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1. The LIVE attestation (GRADED — name the outcome honestly in the session log):**
  - **PASS-prize:** a real-trio `in_process` run where the gate rejects a genuine out-of-lens over-fire →
    the composite flips live (the full S-BS-70 visceral demo), cost-confirmed, captured.
  - **PASS-honest:** a real-trio run where the gate corrects a real finding (the per-judge withstands ruling
    renders live) AND S-BS-73 holds (the gate does NOT suppress a legitimate real finding) — the moat working
    live, even without a composite flip on that case.
  - **HALT-TO-SURFACE:** no convergence within the cost envelope → surface it (NOT a manufactured case); the
    offline A2 (UAP-3b) stands as the mechanism proof. **A manufactured flip is a FAIL, not a pass.**
- **A2. S-BS-72:** a graded in_process run's `SqliteProvenanceStore` blob (`find_by_id(run_id)` /
  `/v1/runs/{id}/audit`) carries the per-judge withstands ruling `{signals_weighed, decision, what_failed}`.
  Verify offline/$0 (injected predictors + injected store).
- **A3. A6 — GroundingChecks first-class:** declared as independent config-authored entities, run at the
  post-consensus locus, each audited; `ground()`/`composite()` **additively identical** for `clinical_v1`
  (verdict + suppress/floor partitions byte-unchanged — test-pinned).
- **A4. Frozen seam 0-delta:** `git diff` over `_apply_consensus` + `judges_dspy` + `judge_metric` + the
  committed seeds == **0 lines**. (D2's provenance work is above the frozen seam.)
- **A5. By-construction guard + green bar:** the gate STILL cannot relabel a true case (re-pinned);
  ruff-clean changed files; default + `debuglithrim` + council + BFF + Vitest re-run; default-deps import-clean;
  pathspec-only; any paid run cost-confirmed.

### Diagnostic stats (NOT gates)
- The live verdict/finding/ruling the attestation produces (illustrative; session log).
- Live in_process cost actually spent (report; the gate is "cost-confirmed", not a number).

---

## 6. Commit structure + message templates

Atomic commits per logical unit (pathspec-only). Suggested order:
1. **`feat(provenance): embed the withstands ruling in the run-blob (S-BS-72, above the frozen seam)`**
2. **`feat(grounding): GroundingChecks as first-class config-authored entities (UAP-3b A6)`**
3. **`test(uap-3b-2): S-BS-72 provenance ruling + GroundingCheck-entity + ground() additive-identical + frozen 0-delta`**
4. **`docs(spec): S-BS-72 closed + the GroundingCheck-entity surface + the LLM-critique split (UAP-3b-3 stub)`**
   *(the LIVE attestation D1 is a cost-gated run recorded in the session log, not a code commit unless a demo case/agent is added — then a `feat(examples): …` commit, additive)*

Each commit body ends with:
```
Executed per bench-salvage-phaseUAP-3b-2-groundingcheck-entity-llm-critique-driver by session-2026-06-04.
See .devloop/sessions/session-bench-salvage-phaseUAP-3b-2-2026-06-04.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1 LIVE attestation outcome NAMED honestly (prize / honest / halt-to-surface) + cost-confirmed
- [ ] A2 S-BS-72 ruling in the provenance blob (offline test) · A3 GroundingCheck entity + `ground()` additive-identical
- [ ] **A4 frozen seam 0-delta** (`wc -l == 0`); no file outside §2 deliverables (scope held)
- [ ] A5 the gate cannot relabel a true case (guard re-pinned); green bar re-run (commands + counts)
- [ ] No services autostarted; pathspec-only (foreign untracked `.claude/`, `KICKOFF_CRITIC_…`,
      `REPORT_fhir_agentbench…` NOT swept); the PAID live run driven via the BFF API (S-BS-69), cost-confirmed
- [ ] No pushes / publishes / tags
- [ ] UAP-3b-3 stub written (the LLM critique deferred-not-dropped)
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight in order; re-verify the §1 citations on open (esp. `run_eval.py:269-299` [the S-BS-72
   pointer + the existing withstands loop] + `grade.py:129-170` [the provenance seam] + the UAP-5a session
   log [the live-flip lesson]).
3. Re-read §2–§7.
4. Post plan-review per §3 — resolve the 5 decisions (D-B the live attestation + D-A the LLM-critique split
   are the calls; do NOT manufacture a live flip).

---

## 9. References
- Spec (LOCKED): `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md` §2A (GroundingCheck :63 + withstands-gate
  :66-74) · §2B (:107 the processing-audit chain) · §13 (:268 locus=BOTH)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (the UAP-3b close row + S-BS-70/72/73)
- Parent cycle: `.devloop/sessions/session-bench-salvage-phaseUAP-3b-2026-06-04.json` +
  `critique-bench-salvage-phaseUAP-3b-2026-06-04.md`
- The live-flip lesson: `.devloop/sessions/session-bench-salvage-phaseUAP-5a-2026-06-04.json`
- Memory: `judge-creation-must-be-demonstrable` (S-BS-70) · `critique-pass-precision-not-floor` (the
  LLM-critique-value caveat → split) · `browser-mcp-confirm-blocks-renderer` (drive PAID via the API, S-BS-69) ·
  `persistence-blob-projection-architecture` (the S-BS-72 blob tier) · `git-commit-pathspec-dirty-index`

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session required; trigger via `/devloop-critique bench-salvage UAP-3b-2`. *Reason: the LIVE paid attestation is a labels-true-by-construction claim on the REAL trio (a manufactured flip = a FAIL; the by-construction invariant + the UAP-5a non-convergence lesson); D2 embeds into the provenance blob ADJACENT to the frozen consensus (A4 byte-0-delta load-bearing); D3 changes the eval-profile surface + the post-consensus grading path. Live in_process runs are cost-gated.*
