# Driver — `bench-salvage` phase `WS-6b`: consolidation audit (the GATE) — map the v2-council Mongo-coupling + persistence surface, produce the port plan

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6b-consolidation-audit-driver`
> **Version:** v1 · **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (`../lithrim-backend` re-grepped on branch `mvp-ready` at the WS-6a baseline `493b533`, per MONITOR §Phase 1a)
>
> **This is WS-6 step b** — the **GATE** for the consolidation track. It is a **READ-ONLY audit** that
> produces a **port plan**; it changes **no code in either repo**. Unblocked by WS-6a's clean
> `../lithrim-backend` tree. Strangle-not-fix holds: the plan is for *porting validated IP*
> (PORT, not rewrite), not investing in the legacy backend.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6b
(consolidation audit — map the v2-council Mongo-coupling + persistence surface, produce the port plan).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench) — the deliverable (the PORT PLAN doc) and your
session log land HERE. The audit READS the sibling ../lithrim-backend repo READ-ONLY
(/Users/aregee/Workspace/github.com/lithrim-backend, branch mvp-ready at 493b533). Change NO code in
EITHER repo this cycle.

Read in this order:
  1. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/personas/EXECUTOR.md   (your role)
  2. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6b_consolidation-audit_driver.md   (this doc)
  3. /Users/aregee/Workspace/github.com/lithrim-bench/docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md   (the consolidation track; §WS-6 table lines 38/47; PORT-not-rewrite; sequencing B)
  4. ../lithrim-backend/app/services/compliance_council.py  (the v2 council — read the v2 branches; CONFIRM Mongo-free)
  5. ../lithrim-backend/app/services/pipeline/provenance.py + app/services/pipeline/stages.py  (persistence interface + the orchestrator council call)
  6. /Users/aregee/Workspace/github.com/lithrim-bench/.devloop/sessions/HANDOFF_bench-salvage_phaseWS-6b_kickoff_2026-06-01.md  (WS-6a close context + the S-BS-24 suite caveat)

Then post your plan-review per EXECUTOR.md (resolve the 5 plan-review decisions in §3). Do not write
the plan doc until I say "go".

Bundle ID: bench-salvage-phaseWS-6b-consolidation-audit-driver
Driver doc: …/lithrim-bench/.devloop/prompts/bench-salvage_phaseWS-6b_consolidation-audit_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** the file:lines below were re-grepped against
> `../lithrim-backend` on `mvp-ready` at `493b533` on 2026-06-01. Re-grep them yourself; if drifted,
> surface it. Every separability/coupling claim in the plan MUST cite a `../lithrim-backend` file:line
> and be tagged **CONFIRMED / INFERRED / HYPOTHESIS** (diagnose-before-edit).

1. `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (bench) — the consolidation track. Lines **38** (the GATE step: confirm v2-council Mongo-coupling [INFERRED] + map the persistence surface) and **47** (the WS-6b row: "read-only ... Produces the port plan"). PORT-not-rewrite (line **36**); sequencing B (line **23**).
2. `../lithrim-backend/app/services/compliance_council.py` — the v2 council. `class ComplianceCouncil` at **:453**; v2 branches at **:463**, **:2347**, **:2699** (the logprob-confidence override — see PIPELINE_GRADING_AUDIT §3); `build_prompt` at **:517**. **Monitor grep (CONFIRM):** the file has NO `get_database` / `motor` / `db[` / `collection` DB access (the lone "collection" at **:524** is the prose phrase "PHI collection"). So the council is **Mongo-free** → cleanly portable.
3. `../lithrim-backend/app/services/pipeline/stages.py` — `_run_council_and_map` at **:686** (the orchestrator's council call; `get_sync_openai_client(purpose="council")` at **:610**). **Monitor grep:** also Mongo-free (no `get_database`/`db[`). The whole grading path is Mongo-free.
4. `../lithrim-backend/app/services/pipeline/provenance.py` — the `pipeline_runs` persistence. `ProvenanceStore` **Protocol** at **:25**; `MongoProvenanceStore` at **:37**; `COLLECTION_NAME = "pipeline_runs"` at **:22**; Mongo isolated at **:46-60** (`get_database()` + `db[COLLECTION_NAME].insert_one`), `find_one` at **:90**, `compliance_report` read at **:119**. **A repository interface ALREADY exists here.**
5. `../lithrim-backend/app/database.py` — the Mongo entry point: `from motor.motor_asyncio import AsyncIOMotorClient` (**:3**), client+db init (**:20-21**), `get_database()` (**:32**). Config: `app/config.py:16-17` (`MONGODB_URL`, `MONGODB_DB_NAME = "velto"`), `COMPLIANCE_COUNCIL_VERSION` at **:211** (default `v1`; live `:8002` runs `v2`).
6. `../lithrim-backend/app/routes/artifact_profiles.py` — `artifact_profiles` direct `db["artifact_profiles"]` at **:39/93/122/146/183** (indexes ensured `app/main.py:127-128`). **No repository interface.**
7. `../lithrim-backend/app/routes/eval.py` + `app/tasks/eval_runner_tasks.py` + `app/tasks/eval_external_results_task.py` — `eval_packs`/`eval_cases`/`eval_run`: `eval_pack_catalog.py:17` (`get_database`), `eval_runner_tasks.py:623` (`eval_cases`), `eval_external_results_task.py:221` (`db["eval_run"]`), plus **S3** (`eval_runs/{id}/case_N` at `eval_runner_tasks.py:208/218`). **Scattered; no interface; has an S3 dependency.**
8. `.devloop/state/STREAM_bench-salvage.md` — open seams (esp. **S-BS-24**: the backend suite is RED, NOT a green gate; **S-BS-7** the calibration exhibit; **S-BS-11** the prompt-templating coupling).

---

## 2. Deliverables (file-by-file)

**This cycle writes exactly TWO files, both in `lithrim-bench`** (the audit reads `../lithrim-backend` read-only):

1. **`docs/specs/PORT_PLAN_v2_council_consolidation.md`** (bench; new — name/location is plan-review decision #1) — the port plan. Sections:
   - **D0 — v2-council Mongo-coupling confirmation.** Read the v2 branches (`compliance_council.py:463/2347/2699` + `build_prompt:517`) and the orchestrator call (`stages.py:686`). Resolve the topology-spec **INFERRED→CONFIRMED** (or surface any hidden state/DB coupling). Enumerate the **v2-vs-v1 deltas** to preserve under PORT-not-rewrite (cross-provider trio + llama-veto-approve + calibration + NKA patches + prompt deltas). Cite file:lines, tag CONFIRMED/INFERRED/HYPOTHESIS.
   - **D1 — persistence-surface map (per collection).** For `pipeline_runs` (`provenance.py` — already behind `ProvenanceStore` Protocol), `artifact_profiles` (`routes/artifact_profiles.py` — direct `db[]`), `eval_packs`/`eval_cases`/`eval_run` (`eval.py`/`eval_runner_tasks.py` — scattered + **S3**), and `compliance_report` (`provenance.py:119`): record read sites, write sites, indexes, doc shape, and **whether a repository interface exists**.
   - **D2 — the phased port plan.** What moves in-process (the council; PORT not rewrite; behind the frozen SPEC §10 BFF contract). What needs a repository interface (pipeline_runs already has `ProvenanceStore` → 6d adds `SqliteProvenanceStore`; `artifact_profiles` + `eval_*` need one introduced). The order (6c council-port → 6d persistence swap → 6e ETLP sidecar). The **S3 dependency** in the eval path and its desktop/VPC story. Risks (incl. **S-BS-24**: cannot use the backend suite as a green regression gate during the port).
2. **`.devloop/sessions/session-bench-salvage-phaseWS-6b-2026-06-01.json`** (bench; new) — session log incl. the audit findings + the citation-tagged evidence.

---

## 3. Plan-review checkpoint (non-negotiable)

Post understanding + the audit approach + the 5 decisions. **Wait for "go".** Resolve:

1. **Plan doc location/name** — `docs/specs/PORT_PLAN_v2_council_consolidation.md` (new) vs `docs/research/REPORT_ws6b_*` vs append a section to `SPEC_PRODUCT_SERVICE_TOPOLOGY.md`. (Default: new `docs/specs/PORT_PLAN_*`.)
2. **v2-council read depth** — confirm Mongo-free via the grep (monitor already did) vs read the full v2 branch logic for hidden state/global coupling before declaring CONFIRMED. (Default: read the three v2 branches end-to-end before CONFIRMED.)
3. **Repository-interface depth** — sketch the interface for `artifact_profiles` + `eval_*` at plan level, or defer interface *design* to 6d and only *map the need* here. (Default: map the need + a thin sketch; full design is 6d.)
4. **S3 in the eval path** — how the plan treats `eval_runner_tasks` S3 writes for desktop (SQLite) / VPC (PG) packaging.
5. **Citation depth** — every separability claim file:line'd vs a representative sample. (Default: every separability claim, per diagnose-before-edit.)

---

## 4. Scope guardrails — NOT in scope

- **NO code changes in EITHER repo.** Read-only audit + two doc/log files in `lithrim-bench`. Porting the council is **WS-6c**; the persistence swap is **WS-6d**; the ETLP sidecar is **WS-6e**.
- **NO actual `SqliteProvenanceStore` / repository implementation** — only map that `ProvenanceStore` (Protocol) exists and what a new impl must satisfy.
- **NO de-Mongo of the legacy hosted backend** — Mongo is retired from the **product path** only; the legacy `:8002` backend may keep it as dev scaffolding.
- **NO `etlp-mapper` edits; NO `lithrim_bench` harness code edits.**
- **Do NOT "fix" pre-existing backend issues** found while reading (e.g. the S-BS-21..25 test-rot/suite-health). Log as seams.
- No drive-by edits in `../lithrim-backend` (it is read-only this cycle).

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** The PORT PLAN doc exists in `lithrim-bench/docs`; **every separability/coupling claim cites a `../lithrim-backend` file:line** and is tagged CONFIRMED/INFERRED/HYPOTHESIS (diagnose-before-edit).
- **A2.** The v2-council Mongo-coupling surface is resolved **INFERRED→CONFIRMED** (or any coupling is surfaced) with cited evidence from the v2 branches (`compliance_council.py:463/2347/2699`).
- **A3.** The persistence surface is mapped **per collection** (`pipeline_runs`, `artifact_profiles`, `eval_packs`/`eval_cases`/`eval_run`, `compliance_report`): read sites, write sites, interface-or-not, swap approach.
- **A4.** The plan names the **phased order** (6c/6d/6e), the **v2 IP deltas to preserve** (PORT-not-rewrite), the **S3 eval dependency**, and the **repository-interface introductions** needed.
- **A5.** **READ-ONLY:** `git status` in `../lithrim-backend` is **clean** (no changes), and no `lithrim_bench` harness code changed (only the plan doc under `docs/` + the session log under `.devloop/`).

### Diagnostic stats (NOT gates)
- Count of Mongo collections on the product path + which are already behind an interface vs direct/scattered.
- The enumerated v2-vs-v1 council deltas (for the 6c PORT-not-rewrite scope).

---

## 6. Commit structure + message templates

This is a docs-only cycle in `lithrim-bench`. Atomic:

1. **`docs(bench-salvage): WS-6b consolidation audit — v2-council + persistence port plan`** — the PORT PLAN doc.
2. (session log lands with the close commit, per `/devloop-close-phase`.)

Each commit body ends with:
```
Executed per bench-salvage-phaseWS-6b-consolidation-audit-driver by session-YYYY-MM-DD-N.
See .devloop/sessions/session-bench-salvage-phaseWS-6b-2026-06-01.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```
Do NOT auto-commit; stage + propose for "go". **Heed the concurrency note** ([[devloop-parallel-cycles-staging]]): this stream runs cycles in parallel on `main` — `git status` before staging, and commit atomically with an explicit pathspec.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A5 PASS (plan doc cited end-to-end; v2 council CONFIRMED; persistence mapped per collection; phased order + deltas + S3 + interfaces named; read-only)
- [ ] `git -C ../lithrim-backend status` is clean (read-only honored)
- [ ] No `lithrim_bench` harness code touched (only the plan doc + session log); no `etlp-mapper` touched
- [ ] Every separability claim carries a `../lithrim-backend` file:line + a CONFIRMED/INFERRED/HYPOTHESIS tag
- [ ] No pushes / publishes / tags
- [ ] Session log written

---

## 8. First move

1. Read `EXECUTOR.md` + the topology spec (§WS-6 lines 38/47).
2. Re-grep the §1 citations against `../lithrim-backend` at `493b533`; build the surface map.
3. Read §2–§7.
4. Post plan-review per §3 (audit approach + the 5 decisions). Wait for "go".

---

## 9. References

- Consolidation track: `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (§WS-6 table lines 38/47; PORT-not-rewrite line 36; sequencing B line 23)
- Prior cycle close: `.devloop/sessions/session-bench-salvage-phaseWS-6a-2026-06-01.json` + handoff `HANDOFF_bench-salvage_phaseWS-6b_kickoff_2026-06-01.md`
- Backend conventions + diagnose-gate: `../lithrim-backend/CLAUDE.md`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (seams S-BS-1..25; S-BS-24 = suite-not-a-green-gate)
- Next in the track: **WS-6c** (council-port, PORT-not-rewrite, behind the frozen SPEC §10 contract) — gated by this plan

---

## Hardness

- [x] **HARD GATE** — read-only (no commits to `../lithrim-backend`, no harness code), **but this is "the GATE"** (topology spec): the PORT PLAN is the design contract WS-6c/6d depend on, and the v2-council separability claim gates the whole port. **Citation discipline (every separability claim file:line'd, CONFIRMED/INFERRED/HYPOTHESIS) is the load-bearing discipline.** A **fresh-critic on the plan's separability claims** is recommended before 6c relies on them; the user may elect inline at close (consistent with the curation-cycle precedent). Either way the diagnose-before-edit citation gate is non-negotiable.
