# Driver: `bench-salvage` phase `COUNCIL-EVIDENCE-GATE`: `consensus-evidence-gate`

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-consensus-evidence-gate-driver`
> **Version:** v1
> **Authored:** 2026-05-30
> **Last re-verified against code:** 2026-05-30 (monitor re-grepped every §1 + §2 citation against `lithrim_bench/runtime/council/compliance_council.py` and the three persistence files; see §0)
> **Target repo:** `lithrim-bench`
> **Estimated effort:** ~3-4 hours (two coordinated code changes + plan-review + the validating batch run + writeup)
> **LLM cost:** ~$5 for the validating batch (the full 30-case `scribe_v1` sweep is ~90 gpt-4.1 calls; the fix-validation FOLDS INTO that one sweep, see §6). Two near-zero-cost pre-checks gate the paid run.
> **Depends on:** `M1` (DONE, salvaged council + sync `/v1/pipeline/evaluate` orchestrator run in-process under `LocalPipelineBackend`, BYOK Azure gpt-4.1, council v1; persisted-judge-reasoning enabler landed)
> **Blocks:** `P1` (SQLite data layer + SME-Question Ontology, the data-driven flag/dimension bindings must be built on corrected aggregation, not on a known-FP baseline)
> **Hardness:** **HARD GATE**, this is an aggregation-code change to the council that alters the flag set on EVERY case (output-contract + consensus behavior). Fresh-critic session required at close; trigger via `/devloop-critique bench-salvage COUNCIL-EVIDENCE-GATE`.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase COUNCIL-EVIDENCE-GATE (consensus-evidence-gate).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                                                          (your role)
  2. .devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md               (this doc)
  3. docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md                                           (the detailed fix spec, §1-8; the R3(d) design, validation protocol, confidence ledger)
  4. lithrim_bench/runtime/council/compliance_council.py:517-519, 645-651, 685, 742-752                      (build_prompt: scribe branch + the evidence gate + the dual-shape output contract)
  5. lithrim_bench/runtime/council/compliance_council.py:1461-1466, 1507-1548, 1555                          (_normalize_result: validated_findings build + the derive-from-findings guard + the ungated return)
  6. lithrim_bench/runtime/council/compliance_council.py:1853-1857, 1907, 1962-1971                          (_apply_consensus: format selector + the legacy judge-level-evidence branch)
  7. lithrim_bench/runtime/council/compliance_council.py:63, 2539                                            (_eval_seed def + use, the per-(case,judge) determinism the A/B relies on)
  8. .devloop/state/STREAM_bench-salvage.md                                                                  (current stream state + open seams)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-consensus-evidence-gate-driver
Driver doc: .devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md
```

---

## 0. Why this cycle exists (the diagnosis is FIXED: do not re-derive)

M1 proved the salvaged council runs in-process on the shipped path: `scribe_v1` case 1 (`bench_scribe_v1_inject_condition_1bd0f10dc7b5`) returns `compliance_verdict=reject == expected reject` and correctly catches `FABRICATED_HISTORY`. **CONFIRMED** (`out/scribe_v1.local.ndjson`; `HANDOFF_BENCH_SALVAGE_2026-05-29.md` lines 91-112).

The same case also emits a second HIGH flag, `MEDICATION_NOT_IN_TRANSCRIPT`, which is a **CONFIRMED** false positive: the medication (zidovudine 300 MG) is verbatim in the transcript and was never mutated (the only injected defect is the fabricated diabetes PMH). The judge itself knows the med is present:

> `behavior_judge.reason` verbatim (`out/scribe_v1.local.guardfix.ndjson`): "The artifact contains fabricated medical history and conditions not discussed in the transcript. Only sprain and zidovudine were mentioned; all other history is invented."

A judge that says "zidovudine was mentioned" and still emits `MEDICATION_NOT_IN_TRANSCRIPT` is a **padding/aggregation artifact, not a presence-reasoning error** (**CONFIRMED**). The reasoning-captured re-run proves the flag rode the legacy judge-level-evidence consensus path with empty per-finding spans:

> `findings_rich` (`out/scribe_v1.local.guardfix.ndjson`): `MEDICATION_NOT_IN_TRANSCRIPT (judges=2)` spans=`[]` and `FABRICATED_HISTORY (judges=3)` spans=`[]`; `per_judge` now carries `reason` + `confidence=1.0`.

**Prompt-only fix already tested-and-rejected (CONFIRMED).** A behavior-judge carryforward guard was added 2026-05-30; the FP persisted (because the judge already knows the med is present), and the guard was reverted. Verified absent: a grep for `clinical incongruity` / `transcript absence` / `carryforward` in `compliance_council.py` returns no matches (the monitor re-ran this grep at authoring, ABSENT-confirmed). **A prompt cannot close a gate that lives in the aggregation code.** That disproof is why §4 forbids re-attempting any scribe-prompt fix.

The full diagnosis (CONFIRMED/INFERRED/HYPOTHESIS-tagged) lives in `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` §1-4. **Do not re-derive it; do not "improve" it.** Your job is to execute the corrective work the spec specifies (the R3(d) fix). The spec is the authority; this driver is the runnable execution wrapper around it.

> **One HYPOTHESIS the spec leaves open (do NOT block on it, but resolve it cheaply, see §6 pre-check P0):** that the v1 judges emitted PURE-LEGACY output (a flat `violations_found` with no `findings[]`), as opposed to findings-first that got fully discarded by the spans-gate at `:1518-1525`. The council does not persist each judge's raw LLM JSON, so which shape each judge emitted is **HYPOTHESIS**. The **CONFIRMED** part is only that the flag surfaced via the judge-level-evidence legacy path (empty consensus spans + `(judges=N)` detail). The fix is correct either way (it gates the legacy branch and tightens the contract); the pre-check only tells us *which* lever did the work.

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review:

1. `.devloop/personas/EXECUTOR.md`, your role
2. `.devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md`, this driver
3. `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md`, the detailed fix spec (§1 defect / §2 mechanism + §2b the pure-legacy HYPOTHESIS / §3 v2 open question / §4 the R3(d) design + blast radius / §5 validation protocol / §6 persistence enabler that already landed / §8 the consensus citations). **This driver does not duplicate the spec; it operationalizes it.**
4. `lithrim_bench/runtime/council/compliance_council.py:517-519`, `build_prompt` def (the prompt this cycle tightens), **CONFIRMED** at authoring
5. `lithrim_bench/runtime/council/compliance_council.py:645-651`, the `elif agent_category == "scribe":` branch (context: this is the scribe rules block; the output-contract change is NOT here, it is the shared JSON-shape block below), **CONFIRMED** (handoff had cited `:597-655`; current scribe `elif` is `:645`, logged as a CITATION-DRIFT correction)
6. `lithrim_bench/runtime/council/compliance_council.py:685`, the evidence gate: `"CRITICAL: A finding WITHOUT evidence_spans is INVALID and will be discarded."`, **CONFIRMED**
7. `lithrim_bench/runtime/council/compliance_council.py:742-752`, the dual-shape ambiguity: the output JSON requests a bare `"violations_found"` list (`:742`) AND the backward-compat reconciliation note (`:748-750`, `"The 'violations_found' array should list the taxonomy_codes from your findings (for backward compatibility)."`), **CONFIRMED**
8. `lithrim_bench/runtime/council/compliance_council.py:1461-1466`, `_normalize_result` def + `violations_found = raw.get("violations_found", [])` passthrough source, **CONFIRMED**
9. `lithrim_bench/runtime/council/compliance_council.py:1507-1530`, `validated_findings` build (spans-discard for findings missing evidence at `:1518-1525`) + the derive-from-findings guard `if findings and not violations_found:` at `:1530` (the line R3(d) changes), **CONFIRMED**
10. `lithrim_bench/runtime/council/compliance_council.py:1555`, the `_normalize_result` return passing `violations_found` through ungated, **CONFIRMED**
11. `lithrim_bench/runtime/council/compliance_council.py:1853-1857`, `_apply_consensus` def, **CONFIRMED**
12. `lithrim_bench/runtime/council/compliance_council.py:1907`, the format selector `if raw_findings and isinstance(raw_findings, list):` (routes to the per-finding-evidence branch), **CONFIRMED**
13. `lithrim_bench/runtime/council/compliance_council.py:1962-1971`, the LEGACY branch: `violations = set(result.get("violations_found") or [])`, `has_evidence = bool(primary_ev) or bool(supporting_ctx)`, one judge-level flag broadcast to all violations with `violation_spans[v] = []` (where the un-gated companion flag currently survives), **CONFIRMED**
14. `lithrim_bench/runtime/council/compliance_council.py:63` + `:2539`, `_eval_seed(case_id, judge_role)` def (`sha256(case_id|judge_role)`) and use (`judge_seed = _eval_seed(case_id, model.name) if case_id else 42`), the per-(case,judge) determinism that makes the validating run a clean A/B, independent of the prompt/consensus code you touch, **CONFIRMED**
15. `lithrim_bench/backends/base.py:32` + `lithrim_bench/backends/local_pipeline.py:118` + `lithrim_bench/eval_runner.py:62, 79-80`, the persisted-reasoning surface (`JudgeOutput.reason`; `reason=jv.reason or ""`; `_verdict_row` persists per-judge `{verdict,flags,confidence,reason}` + `findings_rich` + `structural_findings_rich`). This is the M1 enabler, DO NOT re-touch it; you READ from it for the pre-check., **CONFIRMED**
16. `.devloop/state/STREAM_bench-salvage.md`, stream state + open seams (update the R3(d) row at close)

> **Citation discipline:** every file:line above was re-grepped by the monitor against current code on the "Last re-verified" date and matched (one drift corrected: scribe `elif` is `:645`, not the handoff's `:597-655`). If you spot further drift on read, STOP, re-grep, log it as a CITATION-DRIFT deviation, and use the actual lines (per EXECUTOR.md §"Citation drift"). If a cited symbol is gone, halt and ask.
>
> **Diagnose-before-edit gate (per `CLAUDE.md`):** the §0 diagnosis and the spec §1-4 are evidence-tagged and FIXED. Do not re-open the root cause. Before opening the file to edit you do not need a fresh diagnosis, it exists. Your plan-review must, however, tag every NEW causal claim you make (e.g. about blast radius on a specific case) CONFIRMED / INFERRED / HYPOTHESIS.

---

## 2. Deliverables (file-by-file)

The R3(d) fix is **two coordinated changes**, both in `lithrim_bench/runtime/council/compliance_council.py`. The principle (spec §4): a taxonomy code counts toward the verdict only if the judge supplied evidence specifically for THAT code. The fix lives in consensus aggregation, so it is **judge-agnostic** and composes with R3(a) pre-stage normalization.

### Deliverable 1: OUTPUT CONTRACT: require findings-first (retire the dual-shape ambiguity)

1. **`lithrim_bench/runtime/council/compliance_council.py`**, in `build_prompt` (def `:517`), tighten the output-contract block at `:742` + `:748-750`. Today the prompt requests BOTH the `findings[]` block (with the evidence gate at `:685`, "A finding WITHOUT evidence_spans is INVALID and will be discarded") AND a bare `"violations_found"` list (`:742`) with a backward-compat note (`:748-750`) that lets a judge return a padded flat list. Change the contract to state **explicitly** that:
   - every flag MUST be a `findings[]` entry carrying its own `evidence_spans` (direct quote);
   - `violations_found` is **DERIVED from `findings[]`, not authoritative**, a code appearing only in `violations_found` with no matching evidenced finding will be dropped and will not affect the verdict.
   - Keep the evidence gate at `:685` intact; this change makes the JSON-shape instruction consistent with it rather than contradicting it.

> **Do NOT touch the scribe-rules block at `:645-651`.** That is the scribe evaluation-rules text, not the output contract. The scribe-prompt fix was disproven (§0); the output-contract change is the shared JSON-shape block (`:742`/`:748-750`), which is judge-agnostic. Editing `:645-651` would re-attempt the disproven lever and is OUT of scope (§4).

### Deliverable 2: AGGREGATION: make evidenced findings authoritative; gate the legacy path

2. **`lithrim_bench/runtime/council/compliance_council.py`**, change `_normalize_result` (def `:1461`) so the derive-from-findings reconciliation at `:1530` ALWAYS reconciles instead of firing only when `violations_found` is empty:
   - Today: `if findings and not violations_found:` rebuilds `violations_found` from finding codes ONLY when `violations_found` is empty (`:1530-1535`).
   - Change to: build `evidenced_codes = {f.get("taxonomy_code") for f in validated_findings}` (the `validated_findings` list is built at `:1507-1527`; the var name `evidenced_codes` is **CONFIRMED ABSENT** in the tree, so it is a clean new name), then **always** drop any `violations_found` code lacking an evidenced finding: `violations_found = [c for c in violations_found if c in evidenced_codes]`. Preserve the existing empty-case behavior (when there are findings but no `violations_found`, deriving from findings is equivalent to the filter, confirm this equivalence in plan-review). The return at `:1555` then passes the now-gated `violations_found` through unchanged (no edit needed at `:1555` itself).

3. **`lithrim_bench/runtime/council/compliance_council.py`**, change the LEGACY branch in `_apply_consensus` (def `:1853`) at `:1962-1971`. The format selector at `:1907` routes judges with `raw_findings` to the per-finding-evidence branch; the `else` branch at `:1962-1971` handles a judge that emitted ONLY legacy `violations_found` (`violations = set(result.get("violations_found") or [])`, `has_evidence = bool(primary_ev) or bool(supporting_ctx)`, one judge-level flag broadcast to all with `violation_spans[v] = []`). For a judge emitting ONLY legacy output, **a code with no per-code evidence must not drive a BLOCK vote**, flag-only / non-decision-driving at most (mirrors the 2026-05-28 Finding-2 nuance: "structured-detail asymmetry must not drive a BLOCK vote"). Implement the gate here in consensus (judge-agnostic), not per judge. The exact mechanism (e.g. retaining the code as an informational flag while excluding it from the block-vote tally, or requiring per-code evidence before the code enters `violations`) is yours to propose in plan-review, constrained by: it must suppress the un-evidenced companion flag's decision weight without dropping genuinely-evidenced legacy flags.

> **Scope-held boundary:** these are the ONLY file changes. The close-out scope check greps the diff for any file outside this list. `compliance_council.py` is the single source file touched; the two scripts/tests below are NEW deliverables.

### Deliverable 3: the validating batch run + its artifacts

4. **`out/scribe_v1.local.r3d.ndjson`** (NEW, run artifact), the validating `scribe_v1` batch (the BYOK run command is in §6). One row per case, full `BackendVerdict` JSON including `per_judge[*].{verdict,flags,confidence,reason}` and `findings_rich`. This run DOUBLES as the deferred pack-level `scribe_v1` sweep (verdict accuracy, flag precision/recall, false-block rate on cleans).

5. **`out/scribe_v1.local.r3d.summary.md`** (NEW, analysis), a short human-readable report computed OFFLINE from the NDJSON (no extra paid calls): the acceptance table (§5 A1-A4), the per-defect `FABRICATED_HISTORY` recall delta vs the M1 baseline, the false-block rate on the two clean negatives, and the pre-check result (which shape the v1 judges emitted). Cite `out/scribe_v1.local.ndjson` (M1 baseline) and `out/scribe_v1.local.guardfix.ndjson` (reasoning-captured re-run) as the A/B anchors.

### Deliverable 4: tests

6. **`tests/test_consensus_evidence_gate.py`** (NEW), unit tests for the aggregation change, no LLM calls (construct fake `_normalize_result` / `_apply_consensus` inputs):
   - a judge result with `violations_found=["MEDICATION_NOT_IN_TRANSCRIPT", "FABRICATED_HISTORY"]` but `findings=[]` (pure-legacy, no evidence) → asserts `MEDICATION_NOT_IN_TRANSCRIPT` does NOT drive a block vote / is filtered from the authoritative set;
   - a judge result with an evidenced `findings[]` entry for `FABRICATED_HISTORY` → asserts `FABRICATED_HISTORY` is retained and authoritative;
   - a judge result where `violations_found` contains a code WITH a matching evidenced finding → asserts the code survives `evidenced_codes` filtering (no false drop of genuine legacy flags);
   - backward-compat: `findings[]` present, `violations_found` empty → asserts the derived set matches the finding codes (the `:1530` equivalence).
   - The existing persistence-capture test `tests/test_lithrim_pipeline_confidence_capture.py` (M1, 2 passed) must stay green; do NOT modify it.

> **Test-count note:** the full suite is currently **82 passed** (`pytest -q`, py3.10/debuglithrim), verified by the monitor at authoring. The spec/handoff cite "38/38", which was the count when the M1 persistence enabler landed; the current count is 82. Use the live count, not the stale one.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with:
- **Understanding**, one paragraph: the FP rides the legacy `violations_found` per-flag bypass in consensus; the fix makes evidenced `findings[]` authoritative and gates the legacy branch; prompt-only was disproven.
- **File-by-file changes**, 1 source file modified (`compliance_council.py`, three edits: contract `:742`/`:748-750`, `_normalize_result` `:1530`, `_apply_consensus` `:1962-1971`); 2 new outputs (`out/scribe_v1.local.r3d.ndjson`, `.summary.md`); 1 new test file. Spell out the exact `_apply_consensus` legacy-branch mechanism you propose (Deliverable 2 item 3 leaves it to you within the stated constraint).
- **Test plan**, the new `test_consensus_evidence_gate.py` cases + the unchanged persistence-capture test + full `pytest -q` stays at 82 (or higher with the new tests).
- **Pre-check plan**, confirm you will run the two near-zero-cost pre-checks in §6 BEFORE the paid sweep (the pure-legacy shape log, and the offline v2-NDJSON re-inspection) and report their results in plan-review or before the run.
- **Risks**, see §4 (BLAST RADIUS especially: making findings authoritative changes the flag set on EVERY case; a legitimate flag emitted ONLY via legacy `violations_found` with no `findings[]` would also drop). State how the batch verifies no such legitimate flag is lost.
- **Determinism note**, confirm the per-(case,judge) seed `sha256(case_id|judge_role)` at `:63` is independent of the code you touch, so the run is a clean A/B vs the M1 baseline.
- **Any proposed deviations**, especially if the `_apply_consensus` change cannot be made judge-agnostic without touching the per-finding branch at `:1907+` (surface and halt before coding).

Wait for "go" before writing code. This is a HARD GATE: do not begin the paid run until the user has confirmed both the plan AND the two pre-check results.

---

## 4. Scope guardrails: NOT in scope

The following are tempting but OUT of scope for this cycle:

- **Any scribe-prompt edit (the `:645-651` block, or any per-judge coaching/guard).** The prompt-only lever was tested 2026-05-30 and DISPROVEN, the FP persisted because the judge already knows the med is present (`behavior_judge.reason` "zidovudine were mentioned"), and the guard was reverted (verified absent). A prompt cannot close a gate that lives in aggregation code. Re-attempting this is the single most likely wrong turn; do not take it.
- **SQLite / data-layer work.** `eval_runner` writes NDJSON to `out/`; the doc-shim-vs-relational decision belongs to P1, not here.
- **The SME-Question Ontology / data-driving the hardcoded council constants** (`TIER_1/2/3_*`, `_TIER1_OWNERS`, reliability dims). That is P1.
- **Local vector store / per-question retrieval** (`retrieval.py` stub). sqlite-vec is blocked; that is P1-VEC.
- **Audio** (`analyzeWithAudio`, whisper loader, `audio_bytes` use-after-delete). M2 sub-track; scribe is text+artifact.
- **v2 council fidelity / cross-provider sweep** (Mistral/Llama). M1 + this cycle use v1 (3× gpt-4.1). The v1-vs-v2 question is P2-COUNCIL. **You may READ the existing 2026-05-28 v2 NDJSON offline for the §6 pre-check, but issue NO new v2/cross-provider calls.**
- **Mirroring the fix upstream into `lithrim-backend`.** This cycle edits the vendored `lithrim_bench/runtime/council/` only. Upstream mirroring is a separate, later decision.
- **Re-touching the M1 persistence enabler** (`base.py:32`, `local_pipeline.py:118`, `eval_runner.py:62`). It is DONE and you READ from it; do not modify it.
- **Computing or "fixing" anything in the v2 over-fire class beyond the offline span-check.** Whether the v2 over-fires ride the same legacy path is an open HYPOTHESIS to RECORD (cheap test in §6), not to fix here.
- **Drive-by formatting / dependency bumps / "while I was here" edits / ruff reflow of untouched code.**

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS (each gets a row in the session log's `acceptance` array):

- **A1.** **FP suppressed on case 1.** In `out/scribe_v1.local.r3d.ndjson`, the row for `bench_scribe_v1_inject_condition_1bd0f10dc7b5` no longer carries `MEDICATION_NOT_IN_TRANSCRIPT` in `flags` / `findings_rich`. Verify by inspecting that row in the NDJSON.
- **A2.** **True defect retained.** The same row still carries `FABRICATED_HISTORY`. Verify in the same row.
- **A3.** **Verdict unchanged.** The same row still has `compliance_verdict == "reject"` (`== expected reject`). Verify in the same row.
- **A4.** **No true-defect regression on the batch.** Across the `scribe_v1` batch, `FABRICATED_HISTORY` emission stays high where genuinely warranted, it is NOT dropped on the true-fabrication cases (e.g. the all batch cases whose expected_safety_flags include FABRICATED_HISTORY) where a v1 judge may have emitted it only via legacy `violations_found`. Per-defect recall unchanged within tolerance vs the M1 baseline. Verify via the `out/scribe_v1.local.r3d.summary.md` recall-delta table. (This is the BLAST-RADIUS check, it is the reason a 1-case run is insufficient and the batch is required.)
- **A5.** **False-block on cleans unchanged-or-improved.** The two clean negatives in `scribe_v1` are not newly blocked (the metric the FP most affects). Verify via the summary false-block-rate line.
- **A6.** **Determinism preserved.** The run is a clean A/B: the per-(case,judge) seed at `:63` is unchanged, so verdict + flag-set + confidence are reproducible against the M1 baseline. Reproducibility bar = **verdict + flag-set + confidence, NOT byte-identical reasoning** (per `feedback_eval_reproducibility_framing`). Verify by confirming the seed path was not edited and the baseline rows that SHOULD be stable are stable.
- **A7.** **Tests green.** `tests/test_consensus_evidence_gate.py` passes; the M1 persistence-capture test still passes; full `pytest -q` is at strictly greater than 82 (the current 82 tests plus the new evidence-gate tests) (with the new tests). Verify via `PYENV_VERSION=debuglithrim pyenv exec python -m pytest -q`.

### Diagnostic stats (NOT acceptance gates)

- **D1.** Pure-legacy pre-check result: did the v1 judges emit a flat `violations_found` with no `findings[]` (pure-legacy confirmed), or a `findings[]` that got discarded by the spans-gate (findings-first discard)? Report from the §6 pre-check log.
- **D2.** v2 over-fire span-check result (offline, no new calls): do the 2026-05-28 v2 over-fired codes carry per-finding `evidence_spans` (substantive → R3(a) still needed) or are they span-less (same legacy bypass → R3(d) partially fixes v2)? Report from re-inspecting the existing 2026-05-28 v2 NDJSON.
- **D3.** Pack-level numbers the sweep produces (it doubles as the deferred sweep): verdict accuracy, flag precision/recall, false-block rate on clean negatives.
- **D4.** Wallclock + estimated LLM cost (from `duration_ms` × council price; expected ~90 gpt-4.1 calls for the full 30-case run).

---

## 6. Validation protocol + the exact BYOK run command

**Cost discipline (per `feedback_llm_cost_conscious`): two near-zero-cost pre-checks gate the one paid sweep. Do the offline analysis first; run once; inspect before any rerun.**

### Pre-check P0 (near-zero cost: settles the pure-legacy HYPOTHESIS)

On a SINGLE re-run (the seed is deterministic, so this is no extra paid calls beyond the one run you are about to do anyway), add a one-line debug log of each judge's raw `findings[]` / `violations_found` before mapping. If judges return a non-empty `findings[]` with spans, the flag came via findings-first discard; if they return only `violations_found`, pure-legacy is confirmed (D1). Remove the debug log before commit (or guard it behind a flag), it is not a deliverable.

### Pre-check P1 (zero cost: settles whether R3(d) helps v2)

Re-inspect the EXISTING 2026-05-28 v2 NDJSON (`out/p1_canonical_n5_pilot.ndjson` C1 dumps, `out/fhir_mini_harness_results.ndjson` case-A dump) for per-finding `evidence_spans` on the over-fired codes (`FABRICATED_HISTORY` / `FABRICATED_CONSENT` / `IMPLICIT_CONFIRMATION_OF_RECORD`). No new run needed, per-judge `reason` + consensus spans are persisted. Span-less ⇒ same legacy bypass (R3(d) partially fixes both); spans present ⇒ substantive (R3(a) pre-stage normalization still required). Record as D2.

### The paid validating batch (folds in the deferred pack-level sweep)

Run on the `scribe_v1` batch and measure: FP suppressed (A1-A3); no true-defect regression (A4); false-block on cleans unchanged-or-improved (A5); determinism preserved (A6). This run DOUBLES as the deferred pack-level `scribe_v1` sweep (verdict accuracy, flag precision/recall, false-block rate). The A/B is clean for free because the per-(case,judge) seed `sha256(case_id|judge_role)` at `:63` is independent of the prompt/consensus code R3(d) touches.

**Exact BYOK run command** (export ONLY the 5 LLM/Azure keys, do NOT source the whole backend `.env`; it has complex Settings fields like `HIPAA_ELIGIBLE_LLM_PROVIDERS` that pydantic-settings JSON-parses and fails on):

```bash
cd lithrim-bench
for k in LITHRIM_LLM_PROVIDER AZURE_OPENAI_API_KEY AZURE_OPENAI_ENDPOINT AZURE_OPENAI_API_VERSION AZURE_OPENAI_DEPLOYMENT_COUNCIL; do
  v=$(grep -E "^$k=" ../lithrim-backend/.env | head -1 | cut -d= -f2-); v="${v%\"}"; v="${v#\"}"; export "$k=$v"; done

# Cheap first: --limit 1 reproduces the M1 FP (got=reject exp=reject; flags=[FABRICATED_HISTORY, MEDICATION_NOT_IN_TRANSCRIPT]).
# Use this for pre-check P0 (one run, deterministic seed).
PYENV_VERSION=debuglithrim COMPLIANCE_COUNCIL_VERSION=v1 PYTHONPATH=. \
  pyenv exec python scripts/run_local_scribe.py --limit 1

# The validating batch (doubles as the deferred 30-case pack-level sweep; ~90 gpt-4.1 calls).
# The runner also takes --n for N-seed repeats if robustness bars are later wanted.
PYENV_VERSION=debuglithrim COMPLIANCE_COUNCIL_VERSION=v1 PYTHONPATH=. \
  pyenv exec python scripts/run_local_scribe.py --limit 30
```

> **VERIFIED at authoring:** `../lithrim-backend/.env` exists and all 5 keys are present; `scripts/run_local_scribe.py` exists. Point the batch output at `out/scribe_v1.local.r3d.ndjson` (per the runner's output convention; if the runner hardcodes a different output name, surface in plan-review and either pass an output flag or copy the artifact to the deliverable path).
>
> **Owner gate on the paid run:** do NOT launch the `--limit 30` batch until the user has said "go" on the plan AND confirmed the two pre-check results. The user is LLM-cost-conscious; the `--limit 1` reproduction is fine to run under the plan-review "go", but the 30-case sweep is the paid step that needs explicit confirmation.

### Reverify bar

Case-1 FP lifts while `FABRICATED_HISTORY` stays; cleans' false-block unchanged-or-better; defect cases' recall stable within tolerance. Reproducibility = verdict + flag-set + confidence, not byte-identical reasoning.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance criteria PASS (per §5, A1-A7)
- [ ] All commits exist (per §8 below); working tree clean
- [ ] Tests pass + linters clean (`PYENV_VERSION=debuglithrim pyenv exec python -m pytest -q` ⇒ 82+; `ruff check .` clean; record commands used)
- [ ] No file in the diff outside §2 deliverables (scope held, grep the diff: only `compliance_council.py` + the new test + the two `out/` artifacts; NO scribe-prompt `:645-651` edit, NO data-layer/ontology/retrieval/audio/v2 files, NO `lithrim-backend` files, NO re-touch of the M1 persistence enabler)
- [ ] **No services were autostarted** (user preference), this cycle runs the council BYOK in-process; no Mongo/Pinecone/Celery/etlp-mapper/uvicorn started. If anything needs a service, halt and surface.
- [ ] **No pushes / publishes / tags** (user preference), staged for the user; no auto-commit beyond plan-review "go"
- [ ] The paid `--limit 30` sweep was run ONLY after explicit user confirmation of plan + pre-checks
- [ ] Session log written per the SESSION_LOG template at `.devloop/sessions/session-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.json`
- [ ] `STREAM_bench-salvage.md` updated: the R3(d) / COUNCIL-EVIDENCE-GATE row marked shipped (pending critic verdict); blast-radius open-decision resolved with the batch result; P1 unblocked
- [ ] **HARD GATE:** fresh-critic session triggered (`/devloop-critique bench-salvage COUNCIL-EVIDENCE-GATE`), this cycle CANNOT close on inline monitor critique alone (it is an aggregation-contract change touching the council's flag set on every case)

---

## 8. Commit structure + message templates

Atomic, per-repo commits per logical unit (NOT one mega-commit). All in `lithrim-bench`:

1. **`fix(council): require findings-first output contract, retire dual-shape violations_found ambiguity`**
   - Files: `lithrim_bench/runtime/council/compliance_council.py` (the `build_prompt` `:742`/`:748-750` contract change).
   - Body: explains WHY (the bare `violations_found` request contradicted the `:685` evidence gate and let a judge return a padded flat list); names the user-visible contract change (judges are now told `violations_found` is derived, not authoritative); references this driver bundle ID.

2. **`fix(council): make evidenced findings authoritative; gate legacy violations_found in consensus (R3(d))`**
   - Files: `lithrim_bench/runtime/council/compliance_council.py` (the `_normalize_result` `:1530` always-reconcile via `evidenced_codes`, and the `_apply_consensus` `:1962-1971` legacy-branch gate).
   - Body: cites the §0 evidence (the FP rode the legacy judge-level-evidence path, empty consensus spans); names the behavior change (a legacy-only code with no per-code evidence no longer drives a BLOCK vote); references `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` §4 + this driver bundle ID; lists the seam(s) opened/closed.
   - Files (same commit OR a paired test commit): `tests/test_consensus_evidence_gate.py`.

3. **`analysis(bench): validate R3(d) on scribe_v1, FP suppressed, no true-defect regression`**
   - Files: `out/scribe_v1.local.r3d.ndjson`, `out/scribe_v1.local.r3d.summary.md`.
   - Body: includes the headline numbers (case-1 FP gone, `FABRICATED_HISTORY` recall delta, clean false-block rate) so they are discoverable from `git log -p`. **Check `.gitignore`** before committing, if `out/*.ndjson` is gitignored, commit with `git add -f` and call out the deliberate force-add in the body (the file is an intentionally-tracked validation artifact, not a transient sweep output).

Each commit body should end with:

```
Executed per bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-consensus-evidence-gate-driver by session-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.
See .devloop/sessions/session-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.json
```

(Add the user's Co-Authored-By trailer if used.)

---

## 9. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` in full, it is the authority; this driver wraps it.
3. Read §1 pre-flight citations in order; re-grep each against current code and log any CITATION-DRIFT.
4. **Run the two near-zero-cost pre-checks (§6 P0 + P1) BEFORE any code and BEFORE the paid sweep.** P0 needs one `--limit 1` run (deterministic seed); P1 is purely offline on the existing 2026-05-28 v2 NDJSON.
5. Re-read §2-§8 of this driver.
6. Post plan-review per §3, including the proposed `_apply_consensus` legacy-branch mechanism and the two pre-check results.
7. Wait for "go" on the plan; wait for explicit confirmation before the paid `--limit 30` batch.

---

## 10. References

- **Spec (the authority):** `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` (§1 defect / §2 + §2b mechanism + pure-legacy HYPOTHESIS / §3 v2 open question / §4 R3(d) design + blast radius / §5 validation protocol / §6 persistence enabler / §8 consensus citations)
- **Convergence report (R3(d) is additive to its R1-R4):** `docs/research/REPORT_council_overfire_convergence_2026-05-28.md`, the 2026-05-28 report is v2 cross-provider, clean artifacts, a SUBSTANTIVE reasoning over-fire; R3(d) is v1 (3× gpt-4.1), a DEFECTIVE artifact, a STRUCTURAL bypass in `_apply_consensus`'s legacy path. R3(d) composes with R3(a) pre-stage normalization.
- **M1 close-out:** `HANDOFF_BENCH_SALVAGE_2026-05-29.md` lines 91-112 (the proven spine + the FP that motivates this cycle)
- **M1 artifacts (A/B anchors):** `out/scribe_v1.local.ndjson` (baseline, `per-judge reason absent/None`), `out/scribe_v1.local.guardfix.ndjson` (reasoning-captured re-run, `findings_rich` with `(judges=N)` + empty spans)
- **Determinism:** `lithrim_bench/runtime/council/compliance_council.py:63` (`_eval_seed`, `sha256(case_id|judge_role)`) + `:2539` (use)
- **Persistence enabler (M1, READ-only here):** `lithrim_bench/backends/base.py:32` + `lithrim_bench/backends/local_pipeline.py:118` + `lithrim_bench/eval_runner.py:62, 79-80`
- **Stream state:** `.devloop/state/STREAM_bench-salvage.md`
- **Run command target:** `scripts/run_local_scribe.py` (`--limit`, `--n`); keys from `../lithrim-backend/.env` (5 keys only)
- **Paper guardrail:** `docs/PAPER_OUTLINE.md`, R3(d) does NOT weaken the locked one-claim; scribe is semantic-only by construction (paper semantic-baseline characterization, never thesis validation, which is HL7). R3(d) sharpens the false-block-on-cleans measurement.

---

## Hardness

- [ ] Routine cycle, monitor performs inline critique at close
- [x] **HARD GATE**, fresh-critic session required; trigger via `/devloop-critique bench-salvage COUNCIL-EVIDENCE-GATE`

*(This is a HARD GATE because it is an aggregation-CONTRACT change to the compliance council: it alters the output contract judges are held to AND the consensus path that turns flags into a BLOCK vote, which changes the flag set on EVERY case. Per `persona_rules`, any cycle touching a spec/API contract, and any whose blast radius is "every case", requires a fresh-critic session that reads the spec cold and diffs via `git show` against the closing commits, not the executor's prose. The executor must also surface, not silently absorb, any case where a legitimate legacy-only flag would drop.)*
