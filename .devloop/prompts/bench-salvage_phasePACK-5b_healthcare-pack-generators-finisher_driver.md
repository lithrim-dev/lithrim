# Driver — `bench-salvage` phase `PACK-5b`: healthcare-realm-as-pack, layer 5b (dataset generation → pack, the 4 remaining agent-types — THE CORE-BOUNDARY FINISHER)

> **Generated by the monitor 2026-06-09 from the user's "drive to finish" + the "Unify, split 5a/5b" decision ([[healthcare-realm-as-pack]]). PACK-5a relocated the scribe agent-type + built the `load_pack_generators` interface. PACK-5b relocates the LAST 4 agent-types (coding/hl7/scheduling/triage) + the `_pmh` residual + thins core `packs.py` to a resolver — after which `grep lithrim_bench/` carries NO relocatable clinical CODE, leaving only the intentionally-FROZEN council (the 1b/2b endgame) + minor docstring/carve-out residue.**
>
> **Bundle ID:** `bench-salvage-phasePACK-5b-healthcare-pack-generators-finisher-driver`
> **Version:** v1 · **Status:** READY (HARD GATE)

---

## KICKOFF (paste into a fresh executor rooted in lithrim-bench)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-5b
(healthcare-realm-as-pack, LAYER 5b: relocate the 4 remaining clinical agent-type generators —
coding/hl7/scheduling/triage — + the _pmh residual out of the core into the active `healthcare`
pack THROUGH the PROVEN load_pack_generators interface, then thin core packs.py to a resolver.
This FINISHES the core-boundary relocation: after 5b, lithrim_bench/ carries no relocatable
clinical generation CODE — only the FROZEN council remains).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-5b_healthcare-pack-generators-finisher_driver.md (this)
  3. .devloop/prompts/bench-salvage_phasePACK-5a_healthcare-pack-generators-layer_driver.md (the PROVEN
     pattern you are REPEATING — load_pack_generators, active_packs(), the package loader, the
     by-construction byte-identity gate) + packs/healthcare/generators/__init__.py (the registration surface)
  4. The pre-flight §1 — esp. lithrim_bench/packs.py (_CORE_PACKS + the 4 non-scribe PackDefinitions +
     active_packs()), injectors/__init__.py (the agent-type registries to drop), and the per-agent-type
     move-list §2 (the helper map is EXACT — every helper is single-agent-type)
  5. CLAUDE.md ("labels are true by construction" — EACH of the 4 agent-type corpora must regenerate byte-identical)

Then post plan-review. The SCOPE (5b = the 4 agent-types + _pmh + thin core; unify) is DECIDED — do
NOT re-litigate. Do NOT write code until the monitor says "go". HARD GATE — it touches the
by-construction label path × 4 corpora + empties the core generation registry.

Bundle ID: bench-salvage-phasePACK-5b-healthcare-pack-generators-finisher-driver
```

---

## 0. Why this cut — the finisher

PACK-5a proved `load_pack_generators` + `active_packs()` + the multi-file package loader + the by-construction byte-identity gate on the scribe agent-type. 5b is the SAME PATTERN applied to the remaining 4 agent-types, plus the core-thinning that 5a deferred. After 5b: `injectors/` = the generic base only, `synthesizers/` = empty, `packs.py` = a thin resolver (`PackDefinition` + `active_packs()`, `_CORE_PACKS = {}`), and ALL generation content lives in `packs/healthcare/generators/`. The boundary completion test (`grep lithrim_bench/` carries no relocatable clinical CODE) then holds — the residue is (a) the FROZEN council (`runtime/council`, ~303 hits — 1b/2b, intentional), (b) the PACK-3 enumerated carve-out (`harness`/`verification`, ~55), (c) generic-engine docstring EXAMPLES (`runtime/pipeline`/`backends`, ~33 — a trivial scrub, not relocatable code).

---

## 1. Pre-flight reading (re-verify on open)
1. `.devloop/prompts/bench-salvage_phasePACK-5a_…driver.md` + `critique-bench-salvage-PACK-5a-2026-06-09.md` — the proven mechanism + the citation-drift lesson (**re-grep every helper PER-PATTERN incl. ABSOLUTE imports** — the monitor's combined-grep slip is why `_pmh` was mis-mapped in 5a).
2. `lithrim_bench/packs.py` — `_CORE_PACKS` (`:87`) = the 4 non-scribe `PackDefinition`s (`SCHEDULING`/`CODING`/`TRIAGE`/`HL7_ADT_PACK`, `:53–91`) + `active_packs()` (`:109`). 5b moves the 4 instances → the pack, sets `_CORE_PACKS = {}`, keeps the `PackDefinition` class + `active_packs()` (the thin resolver).
3. `lithrim_bench/injectors/__init__.py` — drops `SCHEDULING/CODING/TRIAGE/HL7_ADT_INJECTORS` + `ALL_INJECTORS` + the agent-type imports; keeps the `base` re-exports (`DefectInjector`/`InjectionRecipe`/`InjectionResult`).
4. `packs/healthcare/generators/__init__.py` — the registration surface; 5b adds the 4 recipes (PACKS gains coding/hl7/scheduling/triage) + the moved synthesizer/injector imports.
5. The CONSUMERS (re-grep): `scripts/generate_pack.py` + `generate_judge_calib.py` (already on `active_packs()` from 5a — but `generate_judge_calib.py` DEEP-IMPORTS `FabricatedConsentInjector` [scheduling, moves in 5b] — repoint it to the loader) + `tests/test_{coding,hl7,scheduling,triage}_pack.py` + any agent-type injector/synthesizer tests + `tests/_factories.py` (used by `test_value_mismatch_injector` etc.).
6. CLAUDE.md — the by-construction invariant: each of `coding_v1`/`hl7_adt_v1`/`scheduling_v1`/`triage_v1` must regenerate byte-identical.

---

## 2. The move-list (EXACT — every helper is single-agent-type, comprehensively re-greped)

All move into `packs/healthcare/generators/` (byte-verbatim `git mv`; imports re-point: core primitives → absolute `lithrim_bench.*`, intra-pack helpers → relative `.`):

- **HL7:** `injectors/{hl7_invalid_field_format,hl7_malformed_date,hl7_missing_required_field,hl7_missing_segment,hl7_trigger_event_mismatch}.py` + `injectors/_hl7.py` (consumed only by the 5 HL7 injectors) + `synthesizers/{hl7_adt_artifact,hl7_adt_transcript}.py` + `HL7_ADT_PACK`.
- **Coding:** `injectors/upcoding_risk.py` + `synthesizers/{coding_artifact,coding_note,coding_transcript}.py` + `synthesizers/_coding_dx.py` + `synthesizers/_icd10_map.py` (consumed only by `_coding_dx` + `upcoding_risk` — both coding) + `CODING_PACK`.
- **Scheduling:** `injectors/{fabricated_consent,phi_disclosure_pre_verification}.py` + `synthesizers/{scheduling_artifact,scheduling_transcript}.py` + `SCHEDULING_PACK`.
- **Triage:** `injectors/missed_escalation.py` + `synthesizers/{triage_artifact,triage_transcript}.py` + `synthesizers/_triage_scenarios.py` (triage-only) + `TRIAGE_PACK`.
- **Scribe residual:** `synthesizers/_pmh.py` → `packs/healthcare/generators/_pmh.py`; the pack's scribe synthesizers (`transcript.py`/`scribe_artifact.py`) flip their `from lithrim_bench.synthesizers._pmh import …` → relative `from ._pmh import …` (pack-internal; closes **S-BS-121**).

**Helper map (re-greped relative+absolute, all trees):** `_hl7`→HL7-only · `_coding_dx`/`_icd10_map`→coding-only · `_triage_scenarios`→triage-only · `_pmh`→scribe-only (pack-only consumer). No cross-agent-type sharing → every helper moves cleanly with its agent-type.

---

## 3. Deliverables (5b)

**D1 — relocate the 4 agent-type bundles + `_pmh` (git mv, byte-verbatim).** Per §2; re-point imports only (core primitives absolute, intra-pack helpers relative). The recipe/label logic byte-untouched.
**D2 — the pack registration surface absorbs all 5 recipes.** `packs/healthcare/generators/__init__.py` `PACKS` gains `coding_v1`/`hl7_adt_v1`/`scheduling_v1`/`triage_v1` (the 4 `PackDefinition`s moved from `packs.py`, now importing the pack-resident synthesizers/injectors).
**D3 — thin core `packs.py` to a resolver.** `_CORE_PACKS = {}`; the 4 `PackDefinition` instances + their synthesizer/injector imports REMOVED; keep `PackDefinition` (the class) + `active_packs()` (now resolving entirely from the pack). `active_packs()` behavior-identical (still yields all 5 recipes — now all pack-sourced).
**D4 — empty the core generation dirs.** `injectors/__init__.py` → only the `base` re-exports (drop all agent-type registries + `ALL_INJECTORS`); after the moves `injectors/` = `base.py` + `__init__.py`. `synthesizers/` → all files moved; leave a minimal `__init__.py` (or remove the dir if nothing imports it — verify).
**D5 — repoint the consumers/tests.** `generate_judge_calib.py`'s direct `FabricatedConsentInjector` → the loader; the 4 `test_*_pack.py` + agent-type injector/synthesizer tests → resolve via `load_pack_generators()`/`active_packs()` (the 5a repoint pattern).
**D6 — the boundary + 4-corpus by-construction test.** Extend `tests/test_pack_layer5b.py` (or layer5a): (a) NO agent-type injector/synthesizer CODE in `lithrim_bench/` (injectors/ = base only, synthesizers/ empty); (b) `active_packs()` yields all 5 recipes, all pack-sourced (`_CORE_PACKS == {}`); (c) **the by-construction gate for EACH of the 4 corpora** (regenerate coding/hl7/scheduling/triage byte-identical to a parent baseline — the frozen-timestamp builder per S-BS-120, or normalize `generated_at`); (d) no core→pack leak (AST).
**D7 — docs.** `SPEC_PLUGIN_ARCHITECTURE` Layer-5b entry: the core-boundary finisher; the residue map (frozen council + carve-out + docstring); the path to `grep → empty`.

---

## 4. Plan-review — the load-bearing decisions
- **D-A (one cut vs per-agent-type split).** Land 5b as ONE cut (4 bundles + `_pmh` + core-thinning), in per-agent-type SUB-COMMITS for reviewability (commit per bundle + one for the thinning). *Monitor lean: one cut — the interface is proven, the moves are mechanical repetitions of 5a.* **Escape hatch:** if it can't land green in one cycle, split to 5b-1..5b-4 (one agent-type each) — surface at plan-review, don't thrash mid-build.
- **D-B (the 4-corpus byte-identity — the HARD-GATE crux).** EACH of coding/hl7/scheduling/triage regenerates byte-identical (the recipe = the label). 4 gates, not 1. Use the frozen-timestamp path (S-BS-120) or normalize `generated_at`. NO label/recipe edits.
- **D-C (the thin-`packs.py` shape).** `_CORE_PACKS = {}`, `PackDefinition` class + `active_packs()` stay; confirm `active_packs()` yields the identical 5-recipe dict (now all pack-sourced) — behavior-0-delta vs pre-5b.
- **D-D (helper relativity).** Intra-pack helper imports become RELATIVE (`._hl7`, `._coding_dx`, `._pmh`) so the package is self-contained; core-primitive imports stay absolute (`lithrim_bench.injectors.base`, `lithrim_bench.encounter_spec`) — pack→core. NO core→pack import.
- **D-E (green-throughout + the residue honesty).** After 5b, `grep lithrim_bench/` for clinical is NOT literally empty — the FROZEN council (1b/2b) + the PACK-3 carve-out + generic-engine docstring examples remain. A1 asserts the relocatable GENERATION CODE is gone (injectors/=base, synthesizers/empty, packs.py thin), NOT a blanket word-sweep (the 5a/PACK-3 precedent).

---

## 5. Scope guardrails — NOT in scope
- **Layers 1b/2b** — the council un-freeze (the endgame, the monitor brings it as an explicit decision AFTER 5b).
- **The docstring-scrub** of `runtime/pipeline`/`backends` generic examples (a trivial follow-on cleanup; not relocatable code).
- **FROZEN:** the council seam; the pack ontology/taxonomy (additive only); the generic base (`DefectInjector`/`InjectionRecipe` — 0-delta); **every generated LABEL byte-identical across all 4 corpora** (a MOVE, not an edit).

## 6. Acceptance
- **A1.** No agent-type generation CODE in `lithrim_bench/`: `injectors/` = `base.py` + `__init__.py` (generic re-exports only), `synthesizers/` empty, `packs.py` `_CORE_PACKS == {}`; all 5 agent-type injectors/synthesizers/recipes PRESENT in `packs/healthcare/generators/` (D6 grep, relocation≠deletion).
- **A2.** **By-construction byte-identity × 4:** coding/hl7/scheduling/triage corpora each regenerate byte-identical pre/post; every `injection_recipe` + `expected_safety_flags` unchanged.
- **A3.** `active_packs()` yields the identical 5-recipe set (now entirely pack-sourced); a no-`generators` pack degrades to `{}` (no core fallback); pack→core only (no cycle); `import lithrim_bench.packs` heavy-dep-free.
- **A4.** Generic framework 0-delta (`injectors/base.py`/`encounter_spec.py`/`packager.py`/`synthea_*` + the `PackDefinition` class byte-untouched). `_pmh` residual closed (S-BS-121).
- **A5.** Green both envs, 0-new at parent (baseline first; the S-BS-117/119 worktree caveat — judge by 0-new-at-parent); ruff clean; pathspec-only; NOT pushed.

## 7. Hardness
- [x] **HARD GATE** — fresh-critic required, `isolation:'worktree'` (re-spawn on infra stall, per PACK-5a). *Reason: it touches the by-construction label path across 4 corpora + empties the core generation registry + thins `packs.py`. The critic verifies the moved code is byte-verbatim, ALL 4 corpora regenerate byte-identical, `active_packs()` is behavior-0-delta + non-vacuous + fail-clean, the generic base is 0-delta, and no core→pack leak — and confirms the boundary milestone (injectors/=base, synthesizers/empty).*

## 8. References
- [[healthcare-realm-as-pack]] (the layered plan; the finisher) · the PROVEN PACK-5a driver + critique (the pattern + the citation-drift lesson) · `SPEC_PLUGIN_ARCHITECTURE` (the unified pack model) · CLAUDE.md (labels true by construction). Pattern: `harness/pack.py load_pack_generators` + `packs.active_packs()` (PACK-5a).
