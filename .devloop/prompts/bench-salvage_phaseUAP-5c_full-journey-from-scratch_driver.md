# Driver — `bench-salvage` phase `UAP-5c`: the FULL Domain→Judge→Flag→Run→Review-from-scratch conversational journey (R11, grow the UAP-5b spine over the complete tool set)

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseUAP-5c-full-journey-from-scratch-driver`
> **Version:** v1 (expanded from the UAP-5b-close stub `v0-stub`)
> **Authored:** 2026-06-05 (from the LOCKED SPEC §13/R11 + the index stub + the UAP-5b close-out: the proven loop SPINE + the two carried seams S-BS-82/S-BS-81 + NB-2/NB-3)
> **Last re-verified against code:** 2026-06-05 (per MONITOR §Phase 1a — ALL citations re-grepped against the live UAP-5b modules: `apps/bff/agent/tools.py:130-154` `_TOOL_SPECS` (3 CORE tools) + `RUN_EVAL_SCHEMA:40` + `PAID_KEYS:43`; `apps/bff/agent/loop.py:49` `allowed` + `:53` `permission_mode="bypassPermissions"` + `:28-36` `_SYSTEM_PROMPT` + `:55` `max_turns=8`; `apps/bff/agent/adapter.py:22-59` `judge_part`/`verdict_part`; `apps/bff/app.py:911-959` `_build_tool_context` (the binding) + `:942-943` `_get_judge` (**the S-BS-82 bug**) + `:962-963` `POST /v1/chat`. The endpoints the NEW tools wrap, all CONFIRMED present + FROZEN: `put_ontology_endpoint:714-715` (PUT /v1/ontology) · `put_agent_endpoint:420-421` (PUT /v1/agent) · `get_agent_endpoint:410-411` (GET /v1/agent) · `eval_pack_run_endpoint:375-376` (POST /v1/eval-pack/run, `EvalPackRunRequest.live:372`) · `list_runs_endpoint:821-822` (GET /v1/runs) · `get_run_audit_endpoint:834-835` · `get_audit_endpoint:758-759`. `apps/shell/src/genui/registry.js:23-33` `KNOWN_TOOLS` (9 cards — every card the new tools need ALREADY exists; **no new card types**). 0 DRIFTED, 0 MISSING.)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase UAP-5c
— the FULL Domain→Judge→Flag→Run→Review-from-scratch conversational journey (R11). UAP-5b shipped
the proven LOOP SPINE + a 3-tool CORE (author_judge / get_judge / run_eval-REPLAY) over POST /v1/chat
(SSE) + the parts-adapter + the wired chat pane + the in-DOM CostModal. A-SAFE was attested AIRTIGHT and
A-LIVE was attested LIVE. THIS cycle GROWS that spine to the complete journey driven from a blank agent
by wrapping the REMAINING existing audited ops as in-process SDK-MCP tools — each a THIN wrapper over a
FROZEN op, adapted to an EXISTING gen-UI card.

TWO non-negotiable carries from UAP-5b close (do these — they are deliverables, not optional):
  S-BS-82  the live get_judge BREAK: the bound _get_judge (app.py:943) omits assigned_flags, so it
           inherits the Query(None,…) FieldInfo sentinel (app.py:559) → app.py:581 `.split` crashes
           live. The offline 8/8 tests MISSED it. FIX the binding (pass assigned_flags=None), keep the
           OP frozen (A6 0-delta), and add the regression test that would have caught it.
  S-BS-81  A-SAFE is safe-by-ALLOWLIST, not safe-by-mode (critic NB-1). permission_mode is
           "bypassPermissions" — safe ONLY while allowed_tools stays the bounded mcp__lithrim__* set.
           You are ADDING tools. ADD the allowlist-bounds test: allowed_tools ⊆ {mcp__lithrim__<name> :
           name ∈ _TOOL_SPECS}, NO built-in tool (Bash/Read/Write/Edit/WebFetch), and NO tool schema
           carries a paid knob (generalize the run_eval A-SAFE assertion across ALL tools).

A-SAFE invariants HOLD unchanged: the agent PROPOSES a paid run; only the human's in-DOM CostModal
confirms it. The agent can NEVER silently fire a paid run, relabel a case, or bypass an author-gate;
every config write is audited (the conversation IS the audit log, R0). Any wrapper over a paid-capable
op (eval-pack `live`, run live/in_process) HARDCODES the $0 path.

RIGHT-SIZE (the UAP-4 anti-over-engineering lesson + the live-reassess discipline): build the SMALLEST
demonstrable-live cut of the full journey. Monitor-candidate CORE = author_flag (PUT /v1/ontology) +
review_runs (GET /v1/runs + /runs/{id}/audit) + get_agent (GET /v1/agent, the $0 Domain read), riding
the existing author_judge + run_eval. The heavier legs — assemble_agent (PUT /v1/agent, a WRITE) and
eval-pack batch (POST /v1/eval-pack/run) — are OPTIONAL and SPLIT to UAP-5c-2 if the spine balloons.
Resolve the final set in plan-review (D-A). Do NOT wrap every op reflexively.

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseUAP-5c_full-journey-from-scratch_driver.md  (this doc)
  3. .devloop/prompts/bench-salvage_phaseUAP-5b_conversational-shell_driver.md       (the proven spine you grow)
  4. .devloop/sessions/critique-bench-salvage-phaseUAP-5b-2026-06-05.md              (NB-1→S-BS-81, NB-2, NB-3; the A-SAFE 5-break analysis)
  5. docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md §13 + R11 (:216/:281) + OQ-6 (:285, resolved B)
     + docs/specs/SPEC_PRODUCT_SHELL.md §10 (ratify the grown tool set — the S-BS-51 discipline)
  6. apps/bff/agent/{tools,adapter,loop}.py + apps/bff/app.py (the SSE route + _build_tool_context + the FROZEN endpoints to wrap)
  7. .devloop/state/STREAM_bench-salvage.md (skim; the UAP arc + the open seams)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)". Resolve the OPEN decisions
in §3 (D-A the final tool set · D-B the adapter mapping · D-C the S-BS-82 fix + the generalized
Query/Header-explicit rule · D-D the S-BS-81 allowlist-bounds test · D-E NB-2 model-bind · D-F the
assemble/eval-pack SPLIT · D-G the A-LIVE cost envelope).
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseUAP-5c-full-journey-from-scratch-driver
Driver doc: .devloop/prompts/bench-salvage_phaseUAP-5c_full-journey-from-scratch_driver.md
```

---

## 0. Why this phase (the capstone, completed — talk the WHOLE journey from scratch)

UAP-5b proved the **loop** — talk → an in-process SDK-MCP tool → an audited config write → the EXISTING gen-UI card → the conversation IS the audit log (R0). It shipped a 3-tool CORE (`author_judge` / `get_judge` / `run_eval`-replay) and was attested **A-SAFE-airtight** (the fresh-critic tried five break-paths and failed) and **A-LIVE** (the loop ran end-to-end on BYO-Claude). UAP-5c is the **fast-follow the spine was built for**: grow the tool set + the orchestration so a user can drive the **entire** product journey — **Domain → Judge → Flag → Run → Review** — *from a blank agent, by talking*. Each new tool is a thin wrapper over an op that already exists and is already audited, adapted to a card that already renders. The spine is proven; this cycle is cheap *per tool*, and its value is the complete, demonstrable, conversational journey.

**Right-sized (the UAP-4 anti-over-engineering lesson + `live-reassess-before-driver-lock`):** the "complete tool set" is tempting to wrap wholesale. Don't. The smallest demonstrable-live cut of the full journey is `author_flag` (Flag) + `review_runs` (Review) + a `get_agent` Domain read, riding the existing `author_judge` (Judge) + `run_eval` (Run). The heavier **WRITE** legs — `assemble_agent` (a new agent from scratch) and the `eval-pack` batch — are split-able to **UAP-5c-2** if the core spine already fills a cycle. Resolve at plan-review (D-A/D-F).

**The two carried seams are first-class deliverables, not afterthoughts:**
- **S-BS-82** (the live `get_judge` break) is a *known live regression* in the very spine we grow — fix it FIRST, and add the test the offline suite missed.
- **S-BS-81** (A-SAFE is safe-by-allowlist) is the load-bearing safety property *and we are widening the tool surface* — the allowlist-bounds test is what keeps `bypassPermissions` safe as the set grows.

---

## 1. Pre-flight reading (ordered) — citations re-grepped 2026-06-05

1. **The proven spine you grow** — `apps/bff/agent/tools.py` (`_TOOL_SPECS:130-154` the 3 CORE tools; `ToolContext:46-66` the bound-ops dataclass; `AUTHOR_JUDGE_SCHEMA:34` / `GET_JUDGE_SCHEMA:39` / `RUN_EVAL_SCHEMA:40` plain-dict schemas; `PAID_KEYS:43` the A-SAFE tuple; `build_sdk_tools:157` the lazy `@tool` wrap) · `apps/bff/agent/loop.py` (`_SYSTEM_PROMPT:28-36`; `_build_options:43`; `allowed:49` = `[f"mcp__lithrim__{name}" for _, name, *_ in _TOOL_SPECS]`; `permission_mode="bypassPermissions":53`; `max_turns=8:55`; `run_chat:70` the SSE-event loop) · `apps/bff/agent/adapter.py` (`_part:22`; `judge_part:26`; `verdict_part:31-59` — **NB-3:** agreement/confidence default to `"—"` on the replay path).
2. **The binding + the S-BS-82 bug** — `apps/bff/app.py` `_build_tool_context:911-959` binds the FROZEN endpoint functions into the `ToolContext`. **`_get_judge:942-943`** calls `get_judge_endpoint(role, agent=…, db_path=…, workdir=…)` WITHOUT `assigned_flags` → it inherits the **`Query(None,…)` FieldInfo** default (`get_judge_endpoint:559`), which is `not None`, so `assigned_flags.split(",")` (`:581`) raises `AttributeError` live. `_author_judge:931-940` + `_run_eval_replay:945-952` avoid the trap because they pass every non-`Depends` param explicitly. **The fix lives in the binding closure, NOT in the op** (keep `get_judge_endpoint` byte-identical → A6 0-delta).
3. **The FROZEN endpoints the NEW tools wrap** (wrap, never modify):
   - `put_ontology_endpoint:714-715` — **`PUT /v1/ontology`** (the **Flag** leg / `author_flag`): audited WRITE to a non-committed working copy; `_validate_ontology:691` → 422 on malformed/snapshot-violating; params `agent:717`, `rationale=Query:718`, `db_path/workdir/actor=Depends`, `x_actor=Header:722`.
   - `put_agent_endpoint:420-421` — **`PUT /v1/agent`** (the heavier **Domain** assemble / `assemble_agent`, OPTIONAL): audited WRITE; `agent_from_dict` round-trip → 422; `rationale=Query:423`, `db_path/actor=Depends`, `x_actor=Header:426`.
   - `get_agent_endpoint:410-411` — **`GET /v1/agent`** (the $0 **Domain** read / `get_agent`): `name:412`, `db_path=Depends`; 404 on unknown.
   - `eval_pack_run_endpoint:375-376` — **`POST /v1/eval-pack/run`** (batch, OPTIONAL): `EvalPackRunRequest{pack_id, agents[], live:372}`. **`live` IS a paid knob** — a wrapper MUST hardcode `live=False` (the A-SAFE generalization).
   - `list_runs_endpoint:821-822` — **`GET /v1/runs`** (the **Review** leg): newest-first run history; `limit=Query:823`, `collections_db=Depends`.
   - `get_run_audit_endpoint:834-835` — **`GET /v1/runs/{run_id}/audit`**: the run-provenance report; `collections_db=Depends`; clean 404 on unknown.
   - `get_audit_endpoint:758-759` — **`GET /v1/audit`** (the config-change audit, "show me what I changed"): `actor/target_type/target_id/since=Query`, `db_path=Depends`.
4. `apps/shell/src/genui/registry.js:23-33` — **`KNOWN_TOOLS` (9 cards):** `tool-flag_editor`, `tool-contract_builder`, `tool-kb_picker`, `tool-verdict_card`, `tool-calibration_chart`, `tool-agent_editor`, `tool-audit_log`, `tool-judge_editor`, `tool-run_panel`. **Every card the new tools need already exists** — `author_flag`→`tool-flag_editor`, `assemble_agent`/`get_agent`→`tool-agent_editor`, `review_runs`→`tool-audit_log`/`tool-run_panel`. **No new card types** (reuse guardrail).
5. `apps/shell/src/panes.jsx` + `apps/shell/src/bff.js` — the UAP-5b-wired chat pane + the SSE client. UAP-5c renders the NEW tool-parts here (they ride the same `renderTool` path; likely zero or minimal shell change beyond verifying the new parts render).
6. `apps/bff/app.py:962-988` `chat_endpoint` (`POST /v1/chat`) + `_build_tool_context` — the SSE route already builds the loop; UAP-5c extends the `ToolContext` + the `_TOOL_SPECS`, not the route shape.
7. `tests/test_uap5b_chat.py` — the test pattern to extend (the **stub source** that drives the loop `$0`, the audited-write assertion, the A-SAFE no-paid-knob assertion, the import-isolation test). UAP-5c adds the full-journey stub run + the S-BS-81 allowlist-bounds test + the S-BS-82 regression.
8. Memories: `unified-authoring-product-frozen-journey` (the 4-act journey is FROZEN — this drives the PRODUCT shell, not the demo acts) · `browser-mcp-confirm-blocks-renderer` (S-BS-69 in-DOM modal for the paid gate; drive A-LIVE via Chrome MCP) · `shell-journey-real-wired` (the BFF must be a persistent process for the live attestation) · `self-asserting-loop-honesty-moat` (surface honestly; never manufacture a win) · `live-reassess-before-driver-lock` (smallest demonstrable-live cut; split speculative backend) · `git-commit-pathspec-dirty-index` (pathspec-only on this dirty shared branch).

> **Citation discipline:** re-grepped 2026-06-05, all CONFIRMED. If anything drifts on read, halt and surface it.

---

## 2. Deliverables (file-by-file)

> Scope = grow the proven spine with the journey-completing tools + close the two carried seams. **A SPLIT is pre-authorized** (§3 D-F): the heavier `assemble_agent` (WRITE) + `eval-pack` batch legs → **UAP-5c-2** if the CORE spine fills a cycle.

**A. D1 — FIX S-BS-82 (the live `get_judge` break; do FIRST — it's a known live regression in the spine).**
1. `apps/bff/app.py` `_get_judge` (`:943`): pass **`assigned_flags=None`** explicitly so the bound plain-function call does not inherit the `Query(None,…)` FieldInfo sentinel. **Keep `get_judge_endpoint` (`:556-587`) byte-identical — the fix is in the wrapper, the op stays FROZEN (A6 0-delta).**
2. **Generalize the lesson:** audit *every* closure in `_build_tool_context` AND every NEW wrapper you add — each MUST pass all `Query(...)` / `Header(...)` params explicitly when calling an endpoint as a plain function (FastAPI only resolves those defaults through the router, not on a direct call). Record this as a binding-layer rule (a short comment near `_build_tool_context`).
3. **The regression test the offline 8/8 missed:** drive `get_judge` through the loop (or call the bound `ctx.get_judge`) and assert a summary dict comes back, NOT an exception. (This is the test whose absence the live attestation caught.)

**B. D2 — the journey-completing SDK-MCP tools (the CORE set; D-A resolves the final list).** Add to `apps/bff/agent/tools.py` `_TOOL_SPECS` + bind in `_build_tool_context`. Each = a THIN wrapper → the FROZEN op; each schema is SDK-free (the A-SAFE tests exercise them without the SDK):
   - **`author_flag`** → `put_ontology_endpoint` (PUT /v1/ontology, `app.py:714`) — audited WRITE — the **Flag** leg. The wrapper passes `agent` + `rationale` + `db_path` + `workdir` + `actor` + `x_actor` explicitly (the S-BS-82 rule). A malformed/snapshot-violating body 422s → the handler surfaces it (like `author_judge`), never bypasses. Adapter → `tool-flag_editor`.
   - **`review_runs`** → `list_runs_endpoint` (GET /v1/runs, `app.py:821`) + `get_run_audit_endpoint` (GET /v1/runs/{id}/audit, `app.py:834`) — **$0 READ** — the **Review** leg. Adapter → `tool-audit_log` (or `tool-run_panel`).
   - **`get_agent`** → `get_agent_endpoint` (GET /v1/agent, `app.py:410`) — **$0 READ** — the **Domain** leg (establish the domain/agent before authoring). Adapter → `tool-agent_editor`.
   - **OPTIONAL / SPLIT to UAP-5c-2 (D-F):** **`assemble_agent`** → `put_agent_endpoint` (PUT /v1/agent, `app.py:420`, audited WRITE, the heavier Domain-assembly) · **`run_eval_pack`** → `eval_pack_run_endpoint` (POST /v1/eval-pack/run, `app.py:375`) — **the wrapper MUST hardcode `EvalPackRunRequest(..., live=False)`** (the batch's per-agent `live` call is paid; no paid knob reaches the agent).

**C. D3 — the A-SAFE hardening (closes S-BS-81 / NB-1; load-bearing).** In `tests/` (+ a tiny structural assertion):
   - **The allowlist-bounds test:** assert `loop._build_options(ctx)` (or the `allowed` literal at `loop.py:49`) yields EXACTLY `{f"mcp__lithrim__{name}"` for each `name` in `_TOOL_SPECS}` — no built-in tool name (`Bash`/`Read`/`Write`/`Edit`/`WebFetch`/`Glob`/`Grep`), no wildcard. This is what keeps `permission_mode="bypassPermissions"` (`loop.py:53`) safe as the set grows.
   - **Generalize the paid-knob assertion across ALL tools** (today the A-SAFE test only checks `RUN_EVAL_SCHEMA`): assert NO schema in `_TOOL_SPECS` contains any `PAID_KEYS` (`confirm`/`in_process`/`live`), and that any wrapper over a paid-capable op (eval-pack `live`, run live/in_process) hardcodes the $0 path — inject the paid keys into the tool args and assert they're dropped.
   - Outcome: A-SAFE is now **safe-by-explicit-bound**, not safe-by-current-allowlist (the exact NB-1 ask).

**D. D4 — the parts-adapter extension.** `apps/bff/agent/adapter.py`: add a part-builder for each new tool-result → an EXISTING `KNOWN_TOOLS` card (no new types): `author_flag`→`tool-flag_editor` (`{agent}`; the card self-fetches the ontology), `get_agent`/`assemble_agent`→`tool-agent_editor`, `review_runs`→`tool-audit_log`/`tool-run_panel`. Flat-spread `output` (the S-BS-19 convention). **Optional (NB-3):** harden `verdict_part` (`:31-59`) so agreement/confidence don't default to `"—"` when the replay record DOES carry votes — cosmetic, decide in plan-review.

**E. D5 — grow the agent loop (orchestration).** `apps/bff/agent/loop.py`: extend `_SYSTEM_PROMPT` (`:28-36`) to name the new tools + the **Domain→Judge→Flag→Run→Review** order + the standing invariant (paid runs are the human's; surface errors plainly, never claim un-got success). Consider `max_turns` (`:55`, currently 8) for the longer journey — **bump only if the A-LIVE attestation shows truncation** (a diagnostic, not a gate; don't pre-inflate). **D-E / NB-2 (decide):** thread an optional `model` into `author_judge`'s schema + the `_author_judge` wrapper (today `model=""` is hardcoded at `app.py:932`, so a conversationally-authored judge is model-unbound) — or stay unbound and flag it forward.

**F. D6 — tests (offline/`$0` via the stub source).** Extend `tests/test_uap5b_chat.py` (or a new `tests/test_uap5c_journey.py`): drive the **full journey** via the stub provider — `get_agent` (Domain) → `author_flag` (Flag) → `author_judge` (Judge) → `run_eval` (Run, replay `$0`) → `review_runs` (Review) — asserting: (a) each config-writing tool → an audited record at `GET /v1/audit`; (b) each parts-adapter shape resolves to a real `KNOWN_TOOLS` card; (c) **S-BS-81** the allowlist bound; (d) **S-BS-82** the get_judge regression; (e) **A-SAFE** no paid knob in ANY schema + no built-in tool + eval-pack/live hardcoded `False`; (f) import-isolation still holds (`claude_agent_sdk` not pulled into default deps). Vitest: the chat pane renders the new tool-parts (`tool-flag_editor` / `tool-agent_editor` / `tool-audit_log`). **The A-LIVE full-journey slice is a cost-gated user-run, NOT an automated test.**

**G. D7 — docs + the A-LIVE attestation.** `docs/specs/SPEC_PRODUCT_SHELL.md §10` + `SPEC_UNIFIED_AUTHORING_PRODUCT.md §13`: note the GROWN tool set under `POST /v1/chat` (the S-BS-51 ratify-don't-grow discipline). Mark **S-BS-82 CLOSED** + **S-BS-81 CLOSED** in the close-out. + `.devloop/sessions/session-bench-salvage-phaseUAP-5c-2026-06-05.json`. **A-LIVE (BLOCKING, USER-RUN):** the full from-scratch journey on `:5180` — built fully driveable (in-DOM modal, Chrome-MCP-driveable); the monitor schedules the user-run attestation at close.

---

## 3. Plan-review checkpoint (non-negotiable)

Post: understanding · file-by-file · test plan · risks · the decisions · any deviations.

### RESOLVED upstream (build to these — do NOT re-litigate)
- **OQ-6 = (B) Python Agent SDK in the BFF** (settled UAP-5b; the loop, SSE route, parts-adapter, `[agent]` extra all exist).
- **PROVIDER = BYO local-CLI; paid tools stay confirm-gated.** Never multi-tenant. The agent proposes a paid run; only the human's in-DOM CostModal (S-BS-69) fires it. The agent can NEVER silently spend, relabel a case, or bypass an author-gate.
- **A-SAFE + A6 0-delta are load-bearing** (the UAP-5b gate-deciders) and carry forward unchanged — the new tools wrap FROZEN ops; the S-BS-82 fix lives in the WRAPPER, not the op.

### Still OPEN — resolve in plan-review
- **D-A. The final tool set.** Monitor candidate CORE = `author_flag` + `review_runs` + `get_agent`. Pick the set that makes "talk the whole journey from scratch" demonstrable end-to-end.
- **D-B. The adapter mapping** for each new tool → which EXISTING card (+ the `output` shape that card destructures).
- **D-C. The S-BS-82 fix** — confirm the wrapper-level fix (op frozen) + the generalized "pass every `Query`/`Header` explicitly" binding rule + the regression test.
- **D-D. The S-BS-81 allowlist-bounds test** shape (assert the exact `mcp__lithrim__*` set; no built-ins; no paid knob across all schemas).
- **D-E. NB-2 model-bind** — thread a `model` into `author_judge`, or stay unbound + flag forward?
- **D-F. The SPLIT** — `assemble_agent` (WRITE) + `eval-pack` batch stay this cycle, or → UAP-5c-2 if the CORE spine already fills a cycle? (Default: split if it balloons.)
- **D-G. The A-LIVE cost envelope** — the BYO-Claude loop (~`$0`) + `$0` replay tools are the core journey; any optional paid run stays human-confirmed. Halt-and-ask before any paid run.

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

- **New gen-UI card types** — reuse the `KNOWN_TOOLS` registry (the adapter maps to them); a new card is a follow-on.
- **API-key / hosted / multi-tenant provider** — BYO-Claude/desktop only (the licensing guardrail). Hosted/VPC is a FUTURE concern.
- **A backend live-grading / WS-2 path** — the tools wrap the EXISTING BFF ops only; no new `:8002` injection.
- **The FROZEN 4-act journey** — this drives the PRODUCT shell (`CenterPane`), NOT the demo journey acts.
- **`assemble_agent` (WRITE) + `eval-pack` batch** — OPTIONAL; SPLIT to **UAP-5c-2** if the CORE spine fills a cycle (D-F). Don't force them in.
- **FROZEN:** `compliance_council._apply_consensus` + the per-judge seam dict + `judge_metric` + the UAP entity model / withstands-gate / audit primitives + the committed seeds + the EXISTING BFF ops (`get_judge_endpoint` / `put_judge_endpoint` / `run_eval_endpoint` / `put_ontology_endpoint` / `put_agent_endpoint` / `eval_pack_run_endpoint` / the runs/audit endpoints). This cycle is a **DRIVER OVER** them — wrap + stream, never modify (A6 0-delta). **The S-BS-82 fix is the one allowed touch — and it is in the WRAPPER `_get_judge`, not in `get_judge_endpoint`.**
- Drive-by formatting / dep bumps (the `[agent]` extra already exists).

If something feels load-bearing-but-not-listed, halt and surface it.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1. The FULL journey works end-to-end:** talk in the shell → SSE → the agent drives **Domain → Flag → Judge → Run → Review**, each step rendering its EXISTING gen-UI card inline in `CenterPane`. (The `:5180` smoke is A-LIVE, user-run.)
- **A2. The conversation IS the audit log (R0):** EACH config-writing tool (`author_flag`, `author_judge`, and `assemble_agent` if included) → an actor-attributed `AuditRecord`; a tool-call → `GET /v1/audit` shows it. No un-audited writes.
- **A3. The new tools drive real ops:** `author_flag` performs the audited `PUT /v1/ontology` (malformed/snapshot-violating → 422 surfaced, NOT bypassed); `review_runs` reads real run history + provenance; `get_agent` reads the real domain.
- **A4. The parts-adapter reuses existing cards:** every new tool-result renders via `renderTool` as `{type:"tool-<name>", state:"output-available", output}` — NO new card types.
- **A-FIXED (S-BS-82):** the bound `get_judge` returns a summary (no `FieldInfo.split` crash); a regression test guards it; the generalized "pass `Query`/`Header` explicitly" rule is applied to every binding closure.
- **A-SAFE (load-bearing — GROWN, closes S-BS-81):** the agent CANNOT (i) fire a paid run without the in-DOM confirm, (ii) relabel a case / bypass an author-gate, or (iii) write un-audited config — each negative-tested. PLUS: `allowed_tools` is EXACTLY the `mcp__lithrim__*` set from `_TOOL_SPECS` (no built-in tool); NO tool schema carries a paid knob; any paid-capable op is wrapped `$0`-hardcoded (eval-pack `live=False`).
- **A5. BYO-Claude + import-isolated:** the loop runs on local-CLI/BYO-Claude auth (no API key); `claude_agent_sdk` stays behind the `[agent]` extra, no leak into default-deps (re-verify the isolation test).
- **A6. Frozen 0-delta:** `git diff` over `_apply_consensus` + the seam dict + `judge_metric` + the EXISTING BFF ops + the committed seeds == **0 lines**. (The S-BS-82 fix is in the wrapper `_get_judge`; `get_judge_endpoint` is byte-identical.)
- **A7. Green bar:** ruff-clean changed files; default + `debuglithrim` (the `[agent]` suite) + BFF + Vitest re-run; default-deps import-clean; pathspec-only.
- **A-LIVE (BLOCKING, USER-RUN):** the live `:5180` full-journey slice — BFF up + BYO-Claude — talk a domain → a flag → a judge → a run → a review, end-to-end, each card rendering, cost-gated (the journey itself is `$0`: BYO-Claude loop + `$0` replay). Built fully driveable (in-DOM modal); the monitor schedules the user-run attestation at close.

### Diagnostic stats (NOT gates)
- The final tool set wired + which card each renders; the loop's turn count on the attestation; the BYO-Claude cost actually spent (report, honestly labeled per `loop.COST_LABEL`); whether NB-2 model-bind + NB-3 verdict-projection were taken or deferred.

---

## 6. Commit structure + message templates

Atomic, pathspec-only (foreign untracked `.claude/`, `KICKOFF_CRITIC_…`, `REPORT_fhir_…` NOT swept — memory `git-commit-pathspec-dirty-index`; verify scope with `git diff <parent> HEAD --stat`). Suggested order:
1. **`fix(bff): S-BS-82 — bind get_judge with assigned_flags=None (the live break; the op stays frozen) + regression test`**
2. **`feat(bff): UAP-5c SDK-MCP tools — author_flag + review_runs + get_agent (thin audited/$0 wrappers; paid-free)`**
3. **`feat(bff): parts-adapter maps the new tools to existing gen-UI cards (no new card types)`**
4. **`feat(bff): grow the agent loop — system prompt + the Domain→Judge→Flag→Run→Review orchestration`**
5. **`feat(shell): render the new tool-parts in the chat pane`** *(only if a shell change is needed — verify first)*
6. **`test(uap-5c): full-journey stub loop + S-BS-81 allowlist-bounds + A-SAFE no-paid-knob across all tools + S-BS-82 regression + A6 0-delta`**
7. **`docs(spec): note the grown tool set (§10/§13); close S-BS-82 + S-BS-81; session log`**

Each body ends with:
```
Executed per bench-salvage-phaseUAP-5c-full-journey-from-scratch-driver by session-2026-06-05.
See .devloop/sessions/session-bench-salvage-phaseUAP-5c-2026-06-05.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] **S-BS-82 FIXED:** bound `get_judge` returns a summary (regression-tested); the Query/Header-explicit rule applied to every binding closure
- [ ] A1 the FULL journey end-to-end (Domain→Flag→Judge→Run→Review, each card renders); A2 the conversation IS the audit log (each write → `/v1/audit`)
- [ ] A3 the new tools drive real audited/$0 ops; A4 parts-adapter reuses existing cards (no new types)
- [ ] **A-SAFE GROWN (S-BS-81):** allowlist == the exact `mcp__lithrim__*` set, no built-ins; NO paid knob in ANY schema; eval-pack/live hardcoded `False`; gate-violation surfaced not bypassed; no un-audited write — all negative-tested
- [ ] **A6 frozen 0-delta** (`wc -l == 0` over `_apply_consensus` + seam dict + `judge_metric` + the EXISTING BFF ops + seeds; `get_judge_endpoint` byte-identical); no file outside §2 (scope held)
- [ ] A5 BYO-Claude + import-isolation re-verified; A7 green bar (counts recorded); pathspec-only; no pushes
- [ ] A-LIVE built driveable (in-DOM modal) — flagged for the user-run `:5180` full-journey attestation
- [ ] D-F SPLIT decision recorded (assemble_agent / eval-pack in or → UAP-5c-2); NB-2 + NB-3 dispositions recorded
- [ ] Tests pass + linters clean (re-run); no services autostarted; session log written per the template

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md`.
2. Read the UAP-5b driver + the UAP-5b critique (the spine you grow + NB-1→S-BS-81 / NB-2 / NB-3 / the A-SAFE 5-break analysis).
3. **Reproduce S-BS-82** (the bound `get_judge` `FieldInfo.split` crash) so the fix + the regression test are grounded in the actual failure, then read §1 pre-flight in order and re-verify the citations.
4. Post plan-review per §3 — resolve D-A…D-G. The locus + provider + A-SAFE/A6 are RESOLVED; right-size (smallest demonstrable-live cut; split the heavy WRITE legs if needed); the agent never spends/relabels/bypasses without the gate.

---

## 9. References
- Spec (LOCKED): `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md` §13 + R11 (:216/:281) + OQ-6 (:285, resolved B) · `SPEC_PRODUCT_SHELL.md §10` (ratify the grown tool set)
- The proven spine: `.devloop/prompts/bench-salvage_phaseUAP-5b_conversational-shell_driver.md` + `apps/bff/agent/{tools,adapter,loop}.py` + `apps/bff/app.py` (`_build_tool_context` + `POST /v1/chat`)
- Prior cycle: session `.devloop/sessions/session-bench-salvage-phaseUAP-5b-2026-06-05.json` · critique `.devloop/sessions/critique-bench-salvage-phaseUAP-5b-2026-06-05.md` (NB-1→S-BS-81, NB-2, NB-3)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (the UAP arc; open seams S-BS-80/81/82 + the standing forks S-BS-46/49/74/76)
- Memory: `unified-authoring-product-frozen-journey` · `browser-mcp-confirm-blocks-renderer` (S-BS-69) · `shell-journey-real-wired` · `self-asserting-loop-honesty-moat` · `live-reassess-before-driver-lock` · `git-commit-pathspec-dirty-index`

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session required; trigger via `/devloop-critique bench-salvage UAP-5c`. *Reason: the agent's action surface WIDENS (new SDK-MCP tools driving real audited config writes — `author_flag`/`put_ontology`, optionally `assemble_agent`) under `permission_mode="bypassPermissions"`, so the A-SAFE boundary must be re-proven safe-by-explicit-bound (S-BS-81); the loop fixes a live regression in the frozen-adjacent binding (S-BS-82) while A6 0-delta over the wrapped ops stays load-bearing; multi-step orchestration from a blank agent + the BYO-Claude licensing guardrail (BYO/desktop only, never multi-tenant) remain in force. Live runs are BYO-Claude + cost-gated.*
