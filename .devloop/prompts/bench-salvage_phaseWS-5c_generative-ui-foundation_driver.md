# Driver — `bench-salvage` phase `WS-5c`: generative-UI components + Tailwind/shadcn foundation

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-5c-generative-ui-foundation-driver`
> **Version:** v1
> **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (per MONITOR §Phase 1a — shell freshly built, all citations CONFIRMED, 0 drift)
> **Hardness:** HARD-GATE (SPEC §8: every shell phase is HARD-GATE-class; introduces two contracts — Tailwind/`@theme` foundation + the gen-UI registry protocol). Fresh-critic close recommended; user may elect inline (WS-5/WS-5b precedent).

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-5c
(generative-UI components + Tailwind v4/@theme/shadcn foundation + mode-switch
integration + first React<->BFF test + S-BS-17 reconcile).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                          (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-5c_generative-ui-foundation_driver.md
                                                            (this doc)
  3. docs/specs/SPEC_PRODUCT_SHELL.md  §2 / §2.1 / §4 / §5b / §5c / §6 / §8 / §10
  4. apps/shell/src/{cards,panes,root,artifact,app,bff,data}.jsx + styles.css
  5. ../v0-lithrim-landing-page/app/globals.css   (the Tailwind v4 / @theme pattern reference)
  6. .devloop/state/STREAM_bench-salvage.md        (current state; S-BS-17 + S-BS-18)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go". Resolve the 5 plan-review decisions in §3 first.

Bundle ID: bench-salvage-phaseWS-5c-generative-ui-foundation-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-5c_generative-ui-foundation_driver.md
```

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review:

1. `docs/specs/SPEC_PRODUCT_SHELL.md` — the design-of-record. Key sections:
   - §2 (`:20-26`) the 3-pane model; §2.1 (`:28-37`) the 4-phase journey
   - §4 (`:54-63`) the stack + **the 2026-06-01 CSS/Tailwind sequencing decision** (`:61`: Tailwind v4 foundation + `@theme` bridge + shadcn land **at WS-5c**, incremental, chrome CSS not rewritten)
   - **§5b (`:87-89`)** the generative-UI `tool-<name>` ↔ component protocol — *the contract this phase implements*
   - §5c (`:91-93`) scripted-default (zero-LLM) — no LLM host this phase
   - §6 (`:95-100`) the four shell contracts (plugin / gen-UI protocol / artifacts-pane / engine)
   - §8 (`:125-134`) the WS-5c row (`:129`); §10 (`:142-146`) PUT-ontology deferred to WS-5c/d (`:144`)
2. `apps/shell/src/cards.jsx` — the inline cards to promote: `ConfigCard:7`, `VerdictCard:60`, `CalibrationChart:94`
3. `apps/shell/src/panes.jsx` — `CenterPane:68` renders the cards inline (`:95` ConfigCard, `:112` VerdictCard, `:121` CalibrationChart)
4. `apps/shell/src/root.jsx` — `ModeSwitch:16` (the fixed-position floating overlay `:22`) + `RootApp:51` (default `"journey"`) — the WS-5b NB to integrate into chrome
5. `apps/shell/src/bff.js` — the bridge client: `runEval:23`, `getCorpus:26`, `getOntology:27` (GET reads; the S-BS-18 binding-test surface)
6. `apps/shell/src/artifact.jsx` — `ReportTab:22` (the real-composite render) + `ArtifactPane:197` (tabs `:220-223`) — the S-BS-18 test target
7. `apps/shell/src/data.jsx` — the S-BS-17 demo-domain mock: `THREADS:3` "Support Agent v4", `STEPS:12` "Customer support", `CONFIG_YAML:50` "customer_support"; + `apps/shell/src/app.jsx:14` `acme-support`
8. `apps/shell/src/styles.css` — `:root` brand tokens `:5-34` + `[data-theme="dark"]` `:36-58` (the single token source the `@theme` bridge consumes)
9. `../v0-lithrim-landing-page/app/globals.css` — `:1` `@import 'tailwindcss'` + `:57` `@theme inline { --color-* : var(--*) }` — the proven in-family Tailwind v4/`@theme` pattern to mirror
10. `.devloop/state/STREAM_bench-salvage.md` — S-BS-17 (`:82`, demo-domain split) + S-BS-18 (`:83`, React↔BFF binding manual-smoke-only)

> **Citation discipline:** every file:line above was re-grepped 2026-06-01 (the shell was built 2026-06-01, so no inter-cycle drift). If you spot drift on read, halt and surface it (per EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

### D0 — Tailwind v4 + `@theme` token bridge + shadcn foundation (SPEC §4:61 / §129)

1. **`apps/shell/package.json`** (`:12-19`) — add **dev** deps only (frontend-isolated; Python install untouched): `tailwindcss@4`, `@tailwindcss/vite`, `class-variance-authority`, `clsx`, `tailwind-merge`, + the Radix primitives shadcn copy-ins need. Add a `test` script (D6).
2. **`apps/shell/vite.config.js`** — register the `@tailwindcss/vite` plugin (keep the existing `/v1`→`:8787` proxy at `:12-13`).
3. **`apps/shell/src/theme.css`** (NEW) — `@import "tailwindcss"` + `@theme inline { --color-bg: var(--bg); … }` bridging the **existing** `styles.css` `:root` (`:5-34`) + `[data-theme="dark"]` (`:36-58`) tokens, so brand consistency holds across both styling systems (mirror v0 `globals.css:1/:57`). Map `@custom-variant dark` → `[data-theme="dark"]` (the shell's existing dark selector, NOT `.dark`).
4. **`apps/shell/src/main.jsx`** — import `theme.css` (additive; keep `styles.css` + `journey.css`).
5. **`apps/shell/components.json`** + **`apps/shell/src/lib/utils.js`** (NEW) — shadcn config + the `cn()` helper.

> Incremental: do **NOT** rewrite the bespoke chrome CSS (`styles.css`, `journey.css`) into utilities (SPEC §4 — high churn / regression risk / low value).

### D1 — generative-UI component registry (SPEC §5b:87-89 / §6.2)

6. **`apps/shell/src/genui/registry.js`** (NEW) — the `tool-<name>` → React component map + `renderTool(part)` resolving `part.state === 'output-available'` (assistant-ui `makeAssistantToolUI` / AI SDK message-`parts` pattern). The 5 §5b tools: `tool-flag_editor`, `tool-contract_builder`, `tool-kb_picker`, `tool-verdict_card`, `tool-calibration_chart`. Unknown tool → graceful fallback.

### D2 — input components (config widgets, shadcn + Tailwind)

7. **`apps/shell/src/genui/FlagEditor.jsx`** (NEW) — flag/severity-map editor over ontology flags/tiers/owners + `severity_map`; reads via `bff.js getOntology:27` (**GET, read-only**); returns the edited config into conversation/UI state (no PUT this phase).
8. **`apps/shell/src/genui/ContractBuilder.jsx`** (NEW) — verification-contract builder (claim → tool-query → verdict; the WS-3a structural-floor contract shape).
9. **`apps/shell/src/genui/KbPicker.jsx`** (NEW) — KB-binding picker (`EvalProfile.kb_bindings`).

### D3 — datapoint cards on the foundation

10. **`apps/shell/src/genui/`** — promote `cards.jsx VerdictCard:60` + `CalibrationChart:94` into the registry as `tool-verdict_card` / `tool-calibration_chart` (re-home on Tailwind/shadcn). `ConfigCard:7` stays the inline summary or folds into `tool-flag_editor`. Keep `panes.jsx CenterPane:68` rendering them via the registry (`:95/:112/:121`).

### D4 — integrate the mode-switch into the chrome (WS-5b NB)

11. **`apps/shell/src/root.jsx`** — move `ModeSwitch:16` out of the fixed-position floating overlay (`:22`) into the titlebar/chrome (`app.jsx TopBar:9` / journey `chrome.jsx TopBarJ`). Shell↔Journey still toggles sharing `theme` (`RootApp:51`).

### D5 — S-BS-17 demo-domain reconcile (plan-review decision)

12. **`apps/shell/src/data.jsx`** (+ `app.jsx:14`) — converge the shell mock (`THREADS:3` / `STEPS:12` / `CONFIG_YAML:50` / `acme-support`) with the journey + harness `clinical_v1` per the plan-review decision (clinical/Scribe vs neutral). Close or re-disposition S-BS-17.

### D6 — S-BS-18: first React↔BFF binding test

13. **`apps/shell/`** — stand up Vitest + React Testing Library (the shell's first JS test infra; `package.json` `test` script) + a component test for `bff.js` → `artifact.jsx ReportTab:22` binding (mock the fetch; assert the real-composite shape renders). The Python BFF smoke (`tests/test_ws5_bff.py`) already guards the server side; this guards the React side.

### D7 — tests + docs

14. **`apps/shell/src/genui/*.test.jsx`** — render + return-result tests for the registry + the 3 input components (covers A2/A3).
15. **`apps/shell/README.md`** — status update (gen-UI + Tailwind/shadcn landed; WS-5d next).

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with Understanding / file-by-file changes / test plan / risks / proposed deviations, and **resolve these 5 decisions** before "go":

1. **S-BS-17 demo-domain** — converge the shell mock to clinical/Scribe (matches `JOURNEY_brief` + harness `clinical_v1`) **vs** keep a neutral customer-support demo. (This is the "shell mock is next touched" moment S-BS-17 was deferred to.)
2. **shadcn scope** — which primitives to copy in (button / slider / dialog / select / …); keep minimal — only what D2 input components + D3 cards need.
3. **Mode-switch placement** — titlebar segmented control vs left-rail; stays a dev/demo toggle vs starts gating real first-run activation.
4. **gen-UI registry shape** — assistant-ui `makeAssistantToolUI` vs AI SDK message-`parts`; pick one and justify (scripted-default per §5c — no live LLM).
5. **JS test runner** — confirm Vitest + React Testing Library (first shell test infra).

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

- **PUT `/v1/ontology` write-wiring / ontology-editor persistence** — WS-5d (SPEC §8:131 + §10:144). WS-5c reads ontology via **GET only**; locking an unexercised write that clobbers committed `clinical_v1.json` was rejected at the WS-5-BFF plan-review.
- **Wiring `artifact.jsx` JudgeTab/ConfigTab to the BFF** (they stay mock) — WS-5d.
- **Corpus/flywheel view** (`GET /v1/corpus` render) — WS-5d.
- **Tauri / sidecar / installers / VPC / offline-license** — WS-5e.
- **LLM-driven conversation** — scripted-default holds (§5c); no LLM host this phase.
- **Rewriting existing chrome CSS into Tailwind utilities** — SPEC §4: incremental adoption, NOT a rewrite. `styles.css` / `journey.css` stay.
- **Any `lithrim_bench/`, `apps/bff/`, or `../lithrim-backend` edit**; backend `:8002` config injection (WS-2).
- Drive-by formatting / dependency bumps outside D0 / "while I was here" edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** Tailwind v4 + `@theme` bridge builds clean (`npm run build`); brand tokens resolve through **both** the bespoke `:root` CSS and `@theme` (existing chrome visually unchanged — build clean + manual parity); Python default install (pydantic+pandas) unchanged (frontend deps isolated). Verify: `cd apps/shell && npm run build` + `pip install -e .` diff check.
- **A2.** The `tool-<name>` registry renders each of the 5 §5b components from a typed tool-part; an unknown tool degrades gracefully. Verify: `npm test` (registry test).
- **A3.** FlagEditor / ContractBuilder / KbPicker collect input + return a result object; ontology read via `GET /v1/ontology` only (no PUT). Verify: `npm test` (component tests).
- **A4.** Mode-switch integrated into the chrome (no longer a fixed floating overlay); Shell↔Journey toggles sharing theme. Verify: manual smoke at `:5180` + grep `root.jsx` for the removed fixed overlay.
- **A5.** S-BS-17 reconciled per plan-review (shell / journey / harness demo-domain agree, or explicitly recorded neutral); seam closed or re-dispositioned in the session log.
- **A6.** S-BS-18 — shell JS test infra exists; `bff.js` → `ReportTab` binding test passes. Verify: `npm test`.
- **A7.** `npm run build` clean; diff confined to `apps/shell/` (+ shadcn copy-ins); NO `apps/bff/`, `lithrim_bench/`, or `../lithrim-backend` edits; no new Python deps. Verify: `git diff --stat` + `git status`.

### Diagnostic stats (NOT acceptance gates)

- shadcn primitives introduced (count + names) — reported in REPORT/session log; NOT a gate.
- Tailwind utility adoption is incremental (existing chrome CSS NOT rewritten) — reported; NOT a gate.

---

## 6. Commit structure + message templates

Atomic commits per logical unit. Suggested order:

1. **`build(shell): Tailwind v4 + @theme token bridge + shadcn foundation`** (D0)
2. **`feat(shell): generative-UI tool-<name> component registry`** (D1)
3. **`feat(shell): flag/severity editor + contract builder + KB picker`** (D2)
4. **`refactor(shell): promote verdict + calibration cards into the gen-UI registry`** (D3)
5. **`feat(shell): integrate the Shell↔Journey mode-switch into the chrome`** (D4)
6. **`chore(shell): reconcile demo-domain (S-BS-17)`** (D5)
7. **`test(shell): Vitest infra + first React↔BFF binding test (S-BS-18)`** (D6 + D7)
8. **`docs(shell): README status — gen-UI + Tailwind landed`** (D7)

Each commit body should end with:

```
Executed per bench-salvage-phaseWS-5c-generative-ui-foundation-driver by session-2026-06-01-N.
See .devloop/sessions/session-bench-salvage-phaseWS-5c-2026-06-01.json
```

(Add the user's Co-Authored-By trailer if used.)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance criteria PASS (per §5)
- [ ] All commits exist; working tree clean
- [ ] `npm run build` clean + `npm test` green (re-run; record commands used)
- [ ] No file in the diff outside §2 deliverables / `apps/shell/` (scope held)
- [ ] No `apps/bff/` / `lithrim_bench/` / backend edits; no new Python deps (frontend-isolated)
- [ ] No services were autostarted (BFF is only needed if you smoke A4/A6 against live; tests mock fetch)
- [ ] No pushes / publishes / tags (user preference)
- [ ] S-BS-17 disposition + S-BS-18 closure recorded in the session log
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order (SPEC §5b is the load-bearing contract; v0 `globals.css` is the Tailwind pattern).
3. Re-read §2–§7 of this driver.
4. Post plan-review per §3 — resolve the 5 decisions before "go".

---

## 9. References

- Spec: `docs/specs/SPEC_PRODUCT_SHELL.md` §4 (`:61` Tailwind decision) / §5b (`:87-89` gen-UI protocol) / §6 (`:95-100` contracts) / §8 (`:129` WS-5c row) / §10 (`:144` PUT deferred)
- Service topology: `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (strangler-fig; BFF targets the harness)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (first-move §1; S-BS-17 `:82`; S-BS-18 `:83`)
- Prior cycle session log: `.devloop/sessions/session-bench-salvage-phaseWS-5-BFF-2026-06-01.json`
- Prior cycle critiques: `.devloop/sessions/critique-bench-salvage-phaseWS-5b-2026-06-01.md` (mode-switch NB) + `critique-bench-salvage-phaseWS-5-BFF-2026-06-01.md` (S-BS-18)
- Tailwind pattern reference: `../v0-lithrim-landing-page/app/globals.css` (`:1` / `:57`)

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session recommended; trigger via `/devloop-critique bench-salvage WS-5c` (or a fresh-critic kickoff). User MAY elect inline (WS-5/WS-5b precedent — UI port, close-time decision). Two contracts introduced: the Tailwind/`@theme` foundation + the §5b gen-UI registry protocol.
