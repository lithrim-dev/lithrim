# Driver — `bench-salvage` phase `NARR-5`: the narrative-eval demo — offline foundation + LIVE 3-model council run

> **Generated by the monitor from a code-scan reassessment (run `wf_9cd6359c-326`) + a LIVE reassessment (2026-06-17, services up). Every load-bearing anchor re-grepped DIRECTLY; the live capability re-confirmed by curl.**
>
> **Bundle ID:** `bench-salvage-phaseNARR-5-narrative-demo-offline-cut-driver` *(filename retains "offline-cut" for git continuity; scope EXPANDED 2026-06-17 — see §0)*
> **Version:** v2 (v1 = offline cut; v2 folds in the live 3-model run after the owner confirmed services + Azure deployments are live)
> **Authored:** 2026-06-17
> **Last re-verified against code + live services:** 2026-06-17 (vs `bench-salvage/ws6c-dspy` HEAD `ac7f746`; BFF/UI/backend/jute all 200; `/v1/packs` shows narrative `tier:core`; all 3 Azure council deployments in `.env`; `claude` CLI at `~/.local/bin/claude`)
> **Hardness:** **HARD GATE** + a **PAID live Azure 3-model run** (D5, owner-approved). Touches the grade-input path `picklist.load_case`; makes a public "multi-model council is config-only" claim AND proves it LIVE. Fresh-critic at close. NO moat file edited.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase NARR-5
(the narrative-eval demo — offline foundation D1-D4 + a LIVE 3-model council run D5).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseNARR-5_narrative-demo-offline-cut_driver.md  (this doc)
  3. .devloop/sessions/HANDOFF_bench-salvage_NARR-4_2026-06-16.md  (NARR-5 first-move + S-BS-NARR2-1 + S-BS-NARR4-1)
  4. docs/specs/SPEC_NARRATIVE_EVAL.md  (§3 loop, §5 grading model, §6 P1, §8)
  5. lithrim_bench/picklist.py  (load_case:101-112, resolve_case_fixtures:45-63, PACK_FILES:20 — the D1 gap + S-BS-9 precedence)
  6. lithrim_bench/harness/workspace.py  (get_active_workspace().out_dir — SAME core layer as picklist; lazy import, no layering violation)
  7. apps/bff/app.py:540 (load_case(case_id, source=ag.source_abspath())) + :1964 (ws.out_dir/"ingested_cases.jsonl") + :760 (POST /v1/workspaces {pack}) + :411 (/v1/run-eval re-derives models from judge config)
  8. lithrim_bench/verification/jute_extractor.py:63 (_to_envelope — ingested record shape)
  9. lithrim_bench/harness/report.py:51-92  (composite() emits floor_adjustments:87 + floor_block_count:91 — D2's data exists)
 10. apps/shell/src/artifact.jsx:96-130  (Active findings :96-110 + Grounded adjustments :111-130 — floor_adjustments rendered NOWHERE; the D2 gap)
 11. lithrim_bench/runtime/council/authored_stage.py:148-190 (build_authored_semantic_stage models=/roles=) + judges_dspy.py:329-405 (build_trio models=) + :205-264 (build_judge_lm, _ROLE_DEPLOYMENT:65-69) + byo_claude_lm.py (ClaudeCliLM) — the multi-model seam
 12. tests/test_narrative_floor.py  (pack=narrative grade patterns; subprocess :373)

Canonical env (export before running):
  LITHRIM_BENCH_PACK=narrative LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim
  Regression green bar: LITHRIM_BENCH_PACK=healthcare LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim
  UI tests: cd apps/shell && npx vitest run

SERVICES ARE UP (verified): BFF :8787 / UI :5180 / backend :8002 / jute :3031 all 200. curl /health BEFORE use; NEVER autostart. D1-D4 are offline/$0. D5 is a PAID live Azure run (owner-approved) — run it DELIBERATELY, log the cost, honest-Δ only.

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code or run the paid live council until I say "go".

Bundle ID: bench-salvage-phaseNARR-5-narrative-demo-offline-cut-driver
Driver doc: .devloop/prompts/bench-salvage_phaseNARR-5_narrative-demo-offline-cut_driver.md
```

---

## 0. Scope (read first) — the live demo is UNBLOCKED; the owner folded the live run in

A code-scan reassessment (run `wf_9cd6359c-326`, HIGH confidence) + a LIVE reassessment (2026-06-17) established:
- **Headline: the multi-model Claude/GPT/Llama council is ALREADY config-only** — `build_trio(models={role: provider})` (judges_dspy.py:329) → `build_judge_lm` (:205) routes per-role to `ClaudeCliLM` or Azure via `_ROLE_DEPLOYMENT` (:65-69); `build_authored_semantic_stage(models=, roles=)` (authored_stage.py:148) threads it through and wraps the **frozen** `_apply_consensus`; `/v1/run-eval` re-derives models from persisted judge config (app.py:411). **No engine work for a mixed council** (proven on healthcare, BYOC-1).
- **The two genuine offline gaps** (narrow, moat-clean): `load_case` (picklist.py:112) has no workspace-ingested-corpus path (`S-BS-NARR2-1` bridge); `composite()` emits `floor_adjustments` (report.py:87) but `artifact.jsx` renders it NOWHERE.
- **LIVE capability CONFIRMED 2026-06-17:** all 4 services 200; narrative pack `tier:core` selectable; all 3 Azure council deployments in `.env` (`AZURE_OPENAI_DEPLOYMENT_COUNCIL`/`_MISTRAL_LARGE_3`/`_LLAMA_4_MAVERICK` + key/endpoint/version); `claude` CLI present. **A real 3-model council can run live now.**

**OWNER DECISION 2026-06-17: FOLD IN THE LIVE 3-MODEL RUN.** So **NARR-5 = the offline foundation (D1-D4) + a real live multi-model council run on a narrative case (D5 — the visceral honest-Δ proof; PAID Azure, confirm-gated).** **SPLIT to NARR-6:** ONLY the polished browser-driven walkthrough (Claude_in_Chrome: drop-JSON → `:3031` live ingest → grade) + the zyng-narrated proof-capsule VIDEO.

**Map-drift corrected (do not repeat):** `narrative_v1.jsonl` has **3** cases (clean, silent_degradation, bracket_leak), NOT 5. Pack examples are ALREADY discoverable via `pack_cases` (pack.py:190) — the bridge is for the INGESTED workspace corpus. `picklist.py` is `lithrim_bench/picklist.py` (NOT `harness/`); `workspace.py` is `lithrim_bench/harness/workspace.py` (same core layer). The live active pack is `healthcare`/`demo-clinical` — D5 must create/select a NARRATIVE workspace first.

---

## 1. Pre-flight reading — re-verified DIRECTLY 2026-06-17

| # | Anchor | Status |
|---|---|---|
| a | `lithrim_bench/picklist.py:101-112` `load_case(case_id, *, source=None)` — `source` reads any jsonl by id; else `resolve_case_fixtures` (PACK_FILES-only). | VERIFIED — the D1 gap + S-BS-9 PACK_FILES-first precedence. |
| b | `apps/bff/app.py:540` `case = load_case(ag.dataset.case_id, source=ag.source_abspath())` — the live UI caller. | VERIFIED — trace whether an ingested case's `source_abspath()` points at `ingested_cases.jsonl` or the gap is the dropdown ENUMERATION (R1). |
| c | `apps/bff/app.py:1964` `ws.out_dir/"ingested_cases.jsonl"` + `lithrim_bench/harness/workspace.py` `get_active_workspace().out_dir`. | VERIFIED — same core layer; lazy import avoids a cycle. |
| d | `lithrim_bench/verification/jute_extractor.py:63` `_to_envelope(record)` — ingested §4.1 record shape. | VERIFIED — the case shape D1's test feeds. |
| e | `lithrim_bench/harness/report.py:87` `"floor_adjustments"` + `:91 "floor_block_count"` (each adj = `{flag, action: floor_block\|floor_inconclusive, contract_type, contract, conforms, disposition}`). | VERIFIED — D2's data is ALREADY emitted. |
| f | `apps/shell/src/artifact.jsx:96-110` "Active findings" + `:111-130` "Grounded adjustments"; `comp.floor_adjustments` read NOWHERE. | VERIFIED — the D2 insertion point. |
| g | `authored_stage.py:148` `build_authored_semantic_stage(models=, roles=)` → `judges_dspy.py:329` `build_trio(models=)` → `:205` `build_judge_lm` → `byo_claude_lm.py` `ClaudeCliLM`; `_ROLE_DEPLOYMENT:65-69`. | VERIFIED — the multi-model seam (D3 offline + D5 live). |
| h | `apps/bff/app.py:760` `POST /v1/workspaces {pack}` + `:411` `/v1/run-eval` (re-derives models from judge config) + `:353` `_grade_via_subprocess LITHRIM_BENCH_PACK`. | VERIFIED (live) — the D5 narrative-workspace + run path. |

---

## 2. Deliverables

**D1 — the `S-BS-NARR2-1` corpus-gradeable bridge (production; small; offline).** Make an ingested workspace-corpus case (`ws.out_dir/ingested_cases.jsonl`, `_to_envelope` shape) first-class gradeable through the picklist/`load_case` path, preserving S-BS-9 PACK_FILES-first precedence. **TRACE the UI→grade path (anchor b) first** to locate the real break (a `load_case` workspace-corpus fallback after `resolve_case_fixtures` with a lazy `workspace` import, and/or the case-dropdown enumeration). **RED-first integration test:** an ingest-enveloped case → `load_case` → `grade_inprocess` (injected predictors, $0) → `composite` verdict; assert resolvable + grades + PACK_FILES-first collision order.

**D2 — the ReportTab Floor Blocks section (production; small; offline).** Render `comp.floor_adjustments` (report.py:87) in `apps/shell/src/artifact.jsx` between "Active findings" (:110) and "Grounded adjustments" (:111), color-coded `floor_block` (verdict-flip — red) vs `floor_inconclusive` (muted), showing `flag/contract_type/conforms/disposition`. Make the SILENT_DEGRADATION flip visceral. **Hand-format JSX (NO prettier).** vitest assertion/snapshot with a fixtured `runResult`; handle empty/all-inconclusive gracefully (no false "BLOCK" when `injected_finding` is null).

**D3 — the multi-model-seam OFFLINE proof (test-only; small; $0 — run BEFORE the paid D5).** Hermetic test: `build_authored_semantic_stage(ontology=narrative, models={'risk_judge':'byo-claude'}, predictors={the others})` assembles a mixed-provider trio — assert `build_judge_lm` routes `risk_judge` to `ClaudeCliLM` (injected runner, no shell-out) and the others to injected predictors, and the stage emits `{consensus, models, evidence_summary}` via the frozen `_apply_consensus`. No Azure creds, $0. **Test name + doc copy MUST say this proves the SEAM/WIRING, not judge quality.** This is the cheap gate that de-risks D5 before spending.

**D4 — the honest LENGTH_VIOLATION positive-catch OFFLINE proof (test-only; small; mock).** Inject an out-of-band preamble into a narrative scene fixture; run with a `policy_judge` **predictor** that fires LENGTH_VIOLATION; assert `policy_judge.findings` carries the code with an evidence span. **Test name MUST say MOCK** (the subtractive proof is `test_narrative_floor.py:440`; the REAL live catch is D5). honest-Δ.

**D5 — the LIVE 3-model council run (PAID; confirm-gated; honest-Δ; THE VISCERAL PROOF).** After D1-D4 are GREEN (the wiring proven offline at $0):
1. **Set up a narrative workspace + agent** (live active pack is `healthcare`/`demo-clinical`): create/select a narrative workspace (`POST /v1/workspaces {pack: narrative}` app.py:760) + seed a narrative agent over a narrative case; bind the trio's per-judge models for a genuine multi-model council (Azure GPT-4 `risk_judge` via `AZURE_OPENAI_DEPLOYMENT_COUNCIL`, Mistral-Large `policy_judge`, Llama-4-Maverick `faithfulness_judge`; OR a `byo-claude` role for a $0 contrast leg) via authored judge config (distinct `model` values), NOT engine edits.
2. **Run the live council** (in_process Azure or via `:8002`) on `narrative_jinn_silent_degradation` (the headline — should flip PASS→BLOCK→reject via the SILENT_DEGRADATION floor) AND a clean case. Run it DELIBERATELY (paid); do NOT loop/retry.
3. **CAPTURE the honest-Δ proof:** the run JSON showing per-judge votes carrying **DISTINCT `.model` values** (one GPT, one Mistral, one Llama — or the Claude contrast) + the floor-block + the composite verdict; whether the live `policy_judge` fires `LENGTH_VIOLATION` on a genuine over-length scene (the REAL `S-BS-NARR4-1` proof vs D4's mock — **report HONESTLY whether it fires**). Write a proof capsule `docs/research/PROOF_narrative_live_council_2026-06-17.md` (per the proof-capsule convention; the zyng VIDEO is NARR-6).
4. **HONEST-Δ ONLY:** report exactly what the live council does. A non-flip or a LENGTH_VIOLATION no-fire is a VALID, publishable result (honest loss = a proof point for honesty-is-the-moat). NEVER tune the case/prompt to manufacture a win.

---

## 3. Plan-review risks (resolve before code; the paid D5 before "go")
- **R1 — D1 mechanism (TRACE first).** Resolution fallback vs dropdown enumeration vs both? Trace `app.py:540` `ag.source_abspath()` for an ingested case before choosing.
- **R2 — D1 circularity + precedence.** Lazy in-fn `workspace` import (+ try/except); the workspace fallback runs AFTER `resolve_case_fixtures` (PACK_FILES-first, S-BS-9); assert a both-places `case_id` resolves PACK_FILES-first.
- **R3 — honest-Δ (D3/D4 are MOCKS; D5 is REAL).** D3/D4 prove seam/wiring, NOT judge quality — say so in test names. D5 is the real council; report what it actually does.
- **R4 — moat + core frozen.** `git diff ac7f746 HEAD --` of `lithrim_bench/runtime/council/{compliance_council,signals,withstands,judge_metric}.py` + `harness/grounding.py` + `verification/{spec,tools}.py` + `apps/bff/agent/tools.py` must be EMPTY. D1 touches `picklist.load_case` (NOT frozen); D3/D4/D5 only CALL the seam, never edit it. No re-snapshot.
- **R5 — D2 JSX discipline.** Hand-compact, no prettier; `git diff --stat artifact.jsx` small; vitest 0-new vs S-BS-145.
- **R6 — PAID live run (D5).** Real Azure $ (owner-approved). Run DELIBERATELY, log the cost, honor the confirm-gate (in-DOM CostModal S-BS-69 via UI; if via BFF/in_process, note the spend). NO loop/retry of paid runs. Prove the wiring offline (D3) FIRST.
- **R7 — narrative workspace setup (D5).** Live active pack is `healthcare`/`demo-clinical`; `/v1/judges` errored on `ws0_default` not in that config DB. D5 must create/select a NARRATIVE workspace + seed a narrative agent + per-judge model bindings (authored config-plane: distinct `model` values), NOT engine edits, before the run.
- **R8 — honest-Δ on the live result (D5).** The live council may NOT flip, or LENGTH_VIOLATION may not fire — a VALID, publishable result. Capture + report whatever happens; never tune to manufacture a flip.

---

## 4. Scope guards / NOT in scope (→ NARR-6)
- **No** moat/core edit; **no** `packs/healthcare/` touch; **no** re-snapshot; **no** autostart of any service (they're up — curl first).
- **NARR-6 (deferred):** the POLISHED browser-driven walkthrough (Claude_in_Chrome: drop-JSON → `:3031` live ingest → grade → floor-block + multi-model verdict, all 4 services) + the zyng-narrated proof-capsule VIDEO + per-judge-model provenance roster UI (`S-BS-97`) + a run-time UI control to pick per-judge providers (the BFF `runEval` body doesn't thread `models/roles`; persisted judge config already works). **The live 3-model run itself is now IN NARR-5 (D5).**

## 5. Exit
- D1-D4 GREEN under `pack=narrative` (+ vitest for D2); the canonical regression gate (`pack=healthcare`) shows **0 new** vs `ac7f746`; ruff clean on touched `.py`; vitest 0-new vs S-BS-145; moat + core 0-diff; RED→GREEN demonstrated for D1's integration test + D2's vitest with parent-RED pasted.
- **D5 captured:** a real live multi-model council run on a narrative case — the run JSON with **distinct per-judge `.model` values** + the SILENT_DEGRADATION floor-block flip (PASS→BLOCK→reject) + the honest LENGTH_VIOLATION live result — written as an honest-Δ proof capsule (`docs/research/PROOF_narrative_live_council_2026-06-17.md`). The cost is logged.
- **The demonstrable cut:** an ingested narrative case loads + grades end-to-end, the floor-block renders in the ReportTab, AND a real 3-model council graded a narrative case live with the verdict captured honestly.
- **HARD GATE:** fresh cold critic at close must confirm — D1 preserves S-BS-9 precedence; D2 renders the flip honestly (no false BLOCK on clean); D3/D4 are EXPLICIT mocks; **D5 is a REAL live run reported honestly (distinct per-judge models, no manufactured win)**; moat + core 0-diff.
