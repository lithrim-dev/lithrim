# Driver — `bench-salvage` phase `NARR-5`: the narrative-eval demo — offline demonstrable-live cut (bridge + floor-block UI + multi-model seam proof)

> **Generated by the monitor from a code-scan reassessment (run `wf_9cd6359c-326`, 4 readers + synthesis, confidence HIGH; every load-bearing anchor re-grepped DIRECTLY by the monitor afterward — the Explore agents drifted on 2 paths, corrected below).**
>
> **Bundle ID:** `bench-salvage-phaseNARR-5-narrative-demo-offline-cut-driver`
> **Version:** v1
> **Authored:** 2026-06-17
> **Last re-verified against code:** 2026-06-17 (vs `bench-salvage/ws6c-dspy` HEAD `7be383f`)
> **Hardness:** **HARD GATE** (touches the grade-input path `picklist.load_case` + makes a public "multi-model council is config-only" claim; fresh-critic session required at close — though NO moat file is edited)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase NARR-5
(the narrative-eval demo — offline demonstrable-live cut: the corpus-gradeable bridge,
the floor-block UI, and the multi-model-seam offline proof).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseNARR-5_narrative-demo-offline-cut_driver.md  (this doc)
  3. .devloop/sessions/HANDOFF_bench-salvage_NARR-4_2026-06-16.md  (NARR-5 first-move + S-BS-NARR2-1 + S-BS-NARR4-1)
  4. docs/specs/SPEC_NARRATIVE_EVAL.md  (§3 loop, §6 P1, §8 — the spec-§8 NARR-4 "demo" SPLIT: this is the offline cut; the live demo is NARR-6)
  5. lithrim_bench/picklist.py  (load_case:101-112, resolve_case_fixtures:45-63, PACK_FILES:20 — the D1 gap + S-BS-9 precedence)
  6. lithrim_bench/harness/workspace.py  (get_active_workspace().out_dir — SAME core layer as picklist; lazy import, no layering violation)
  7. apps/bff/app.py:540 (the live caller load_case(case_id, source=ag.source_abspath())) + app.py:1964 (corpus_path = ws.out_dir / "ingested_cases.jsonl")
  8. lithrim_bench/verification/jute_extractor.py:63 (_to_envelope — the ingested record shape)
  9. lithrim_bench/harness/report.py:51-92  (composite() ALREADY emits floor_adjustments:87 + floor_block_count:91 — D2's data exists)
 10. apps/shell/src/artifact.jsx:96-130  (Active findings :96-110 + Grounded adjustments :111-130 — floor_adjustments is rendered NOWHERE; the D2 gap)
 11. lithrim_bench/runtime/council/authored_stage.py:148-190 (build_authored_semantic_stage models=/roles=) + judges_dspy.py:329-405 (build_trio models=) + :205-264 (build_judge_lm) + byo_claude_lm.py (ClaudeCliLM injected runner) — the D3 multi-model seam
 12. tests/test_narrative_floor.py  (the pack=narrative grade-test patterns; A5 subprocess:373)

Canonical env (export before running anything):
  LITHRIM_BENCH_PACK=narrative LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim
  Regression green bar: LITHRIM_BENCH_PACK=healthcare LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim
  UI tests: cd apps/shell && npx vitest run
ALL SERVICES ARE DOWN. This cut is OFFLINE/$0 — do NOT autostart :8787/:5180/:8002/:3031. The live checks are NARR-6.

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseNARR-5-narrative-demo-offline-cut-driver
Driver doc: .devloop/prompts/bench-salvage_phaseNARR-5_narrative-demo-offline-cut_driver.md
```

---

## 0. The reassessment result (read before anything) — what's real, what's the cut

A code-scan reassessment (services were down; live-UI pass deferred to you when they're up) ground-truthed the spec-§8 "NARR-4 = visceral demo." **The headline finding: the multi-model Claude/GPT/Llama council is ALREADY config-only.** `build_trio(models={role: provider})` (judges_dspy.py:329) → `build_judge_lm` (judges_dspy.py:205) routes per-role to `ClaudeCliLM` or Azure via `_ROLE_DEPLOYMENT`; `build_authored_semantic_stage(models=, roles=)` (authored_stage.py:148) threads it through and wraps the **frozen** `_apply_consensus`; `/v1/run-eval` re-derives models from persisted judge config (app.py:411). **No engine work is needed for a mixed council** — it was proven on healthcare (BYOC-1).

**The only two genuine gaps are narrow, offline-testable, and touch NO moat file:**
1. `load_case` (picklist.py:112) has no path to a workspace's ingested corpus → a pinned/ingested case isn't first-class gradeable (the `S-BS-NARR2-1` D-C bridge).
2. `composite()` ALREADY emits `floor_adjustments` (report.py:87) but `apps/shell/src/artifact.jsx` renders it NOWHERE → the SILENT_DEGRADATION floor-flip is invisible in the UI.

**THE CUT (this NARR-5 — offline, $0, no services, moat-clean):** D1 the bridge · D2 the floor-block UI · D3 the multi-model-seam offline proof · D4 the honest LENGTH_VIOLATION positive-catch proof. **SPLIT to NARR-6 (gated on services up + Azure Mistral/Llama deployments + creds):** the live real-Azure 3-model council run, the browser walkthrough, the live `:3031` ingest leg, the real-LM LENGTH_VIOLATION catch, per-judge-model provenance UI (`S-BS-97`).

**Map-drift corrected (do not repeat):** `narrative_v1.jsonl` has **3** cases (clean, silent_degradation, bracket_leak), NOT 5. Pack examples are ALREADY discoverable via `pack_cases` (pack.py:190) — the bridge is specifically for the INGESTED workspace corpus, not pack examples. `picklist.py` is at `lithrim_bench/picklist.py` (NOT `harness/`); `workspace.py` is `lithrim_bench/harness/workspace.py` (same core layer).

---

## 1. Pre-flight reading — re-verified DIRECTLY 2026-06-17 (not the Explore excerpts)

| # | Anchor | Status |
|---|---|---|
| a | `lithrim_bench/picklist.py:101-112` `load_case(case_id, *, source=None)` — `source` branch reads any jsonl by id; else `resolve_case_fixtures({case_id})` (PACK_FILES-only). | VERIFIED — the D1 gap + the S-BS-9 PACK_FILES-first precedence (resolve runs first). |
| b | `apps/bff/app.py:540` `case = load_case(ag.dataset.case_id, source=ag.source_abspath())` — the live UI caller. | VERIFIED — trace whether an ingested case's `source_abspath()` points at `ingested_cases.jsonl`, or whether the gap is the case-dropdown ENUMERATION (plan-review R1). |
| c | `apps/bff/app.py:1964` `corpus_path = ws.out_dir / "ingested_cases.jsonl"` (the NARR-2 write target) + `lithrim_bench/harness/workspace.py` `get_active_workspace().out_dir`. | VERIFIED — same core layer as picklist; a lazy in-fn import avoids any cycle. |
| d | `lithrim_bench/verification/jute_extractor.py:63` `_to_envelope(record)` — the ingested §4.1 record shape (artifacts/context/expected_safety_flags). | VERIFIED — the case shape D1's test feeds. |
| e | `lithrim_bench/harness/report.py:87` `"floor_adjustments": [...]` + `:91 "floor_block_count"` (each adj = `{flag, action: floor_block|floor_inconclusive, contract_type, contract, conforms, disposition}`). | VERIFIED — D2's data is ALREADY emitted by `composite()`. |
| f | `apps/shell/src/artifact.jsx:96-110` "Active findings" + `:111-130` "Grounded adjustments" (gated on `.length>0`); `comp.floor_adjustments` read NOWHERE (no grep hit in `apps/shell/src`). | VERIFIED — the D2 insertion point (between the two sections). |
| g | `authored_stage.py:148` `build_authored_semantic_stage(models=, roles=)` → `judges_dspy.py:329` `build_trio(models=, roles=)` → `:205` `build_judge_lm` → `byo_claude_lm.py` `ClaudeCliLM` (injectable runner, $0). | VERIFIED — D3's seam; the multi-model claim is real + offline-provable. |

---

## 2. Deliverables (the offline cut; tests-first, RED captured)

**D1 — the `S-BS-NARR2-1` corpus-gradeable bridge (production; small).** Make an ingested workspace-corpus case (`ws.out_dir/ingested_cases.jsonl`, the `_to_envelope` shape) first-class gradeable through the picklist/`load_case` path the UI uses — preserving the S-BS-9 PACK_FILES-first precedence (a `case_id` in both resolves PACK_FILES-first). Plan-review must TRACE the actual UI→grade path (anchor b) to decide whether the fix is a `load_case` workspace-corpus fallback (after `resolve_case_fixtures`, before `return None`, lazy `workspace` import) and/or the case-dropdown enumeration. **RED-first integration test:** an ingest-enveloped case → `load_case` → `grade_inprocess` (with injected predictors, $0) → `composite` verdict; assert the case is resolvable AND grades, and assert the PACK_FILES-first collision order.

**D2 — the ReportTab Floor Blocks section (production; small).** Render `comp.floor_adjustments` (report.py:87) in `apps/shell/src/artifact.jsx` between "Active findings" (:110) and "Grounded adjustments" (:111), color-coded `floor_block` (verdict-flip — red) vs `floor_inconclusive` (muted), showing `flag / contract_type / conforms / disposition`. Make the SILENT_DEGRADATION PASS→BLOCK flip visceral. **Hand-format JSX (NO prettier — `shell-no-prettier` memory).** Offline-renderable with a fixtured `runResult`; a **vitest** assertion/snapshot covers it. Handle empty/all-inconclusive `floor_adjustments` gracefully (no false "BLOCK" styling when `injected_finding` is null — a clean scene ran the floor but injected nothing).

**D3 — the multi-model-seam offline proof (test-only; small).** Hermetic offline test: `build_authored_semantic_stage(ontology=narrative, models={'risk_judge':'byo-claude'}, predictors={the other two})` assembles a mixed-provider trio — assert `build_judge_lm` routes `risk_judge` to `ClaudeCliLM` (injected runner, no shell-out) and the others to injected predictors, and the stage emits `{consensus, models, evidence_summary}` via the frozen `_apply_consensus`. No Azure creds, $0. Proves the Claude/GPT/Llama headline is config-only on `pack=narrative`. **Test name + any doc copy MUST say this proves the SEAM/WIRING, not judge quality.**

**D4 — the honest LENGTH_VIOLATION positive-catch proof (test-only; small).** Inject an out-of-band preamble (1-sentence or 6-sentence) into a narrative scene fixture; run `build_authored_semantic_stage` with a `policy_judge` **predictor** that fires LENGTH_VIOLATION; assert `policy_judge.findings` carries the code with an evidence span. Documents the demote is honest in BOTH directions (the subtractive proof is `test_narrative_floor.py:440`). **Predictor-driven — the test name MUST say it is a MOCK, not a real-LM result (manufactured-win = the commercial-moat violation; honest-Δ is non-negotiable).**

---

## 3. Plan-review risks (resolve before code — from the reassessment, monitor-verified)
- **R1 — D1 mechanism (TRACE first).** Is the gap a `load_case` resolution fallback, the case-dropdown enumeration, or both? Trace `app.py:540` `ag.source_abspath()` for an ingested case before choosing. Don't add a fallback if `source=` already covers it — find the real break.
- **R2 — D1 import-circularity + precedence.** `picklist.py` doesn't import `workspace`; add a LAZY in-fn import (+ try/except) so a cycle can't form. The workspace fallback MUST run AFTER `resolve_case_fixtures` (PACK_FILES-first, S-BS-9); assert a both-places `case_id` resolves PACK_FILES-first.
- **R3 — honest-Δ (D3/D4 are MOCKS).** Injected predictors prove the seam + wiring, NOT judge quality. Test names + any demo copy say so explicitly. A predictor-driven LENGTH_VIOLATION catch must never be presented as a real-judge result.
- **R4 — moat + core frozen.** `git diff 7be383f HEAD --` of `lithrim_bench/runtime/council/{compliance_council,signals,withstands,judge_metric}.py` + `lithrim_bench/harness/grounding.py` + `lithrim_bench/verification/{spec,tools}.py` + `apps/bff/agent/tools.py` must be EMPTY. D1 touches `picklist.load_case` (NOT frozen); D3/D4 only CALL `build_trio`/`build_authored_semantic_stage`/`_apply_consensus`, never edit them. No re-snapshot.
- **R5 — D2 JSX discipline.** Hand-compact JSX, no prettier; verify `git diff --stat` on `artifact.jsx` is small (only the new section). vitest green (`cd apps/shell && npx vitest run`); 0-new vs the pre-existing S-BS-145.

---

## 4. Scope guards / NOT in scope (→ NARR-6, gated on services + Azure creds)
- **No** moat/core edit; **no** `packs/healthcare/` touch; **no** re-snapshot; **no** autostart of any service.
- **NARR-6 (deferred):** the live real-Azure 3-model council run (GPT-4 / Mistral-Large / Llama-4-Maverick — `_ROLE_DEPLOYMENT` `ValueError`s when unset, judges_dspy.py:65-69); the browser-driven walkthrough (drop-JSON → `:3031` live ingest → grade → floor-block + multi-model verdict, needs all 4 services); the live `:3031` extractor-convergence leg; the real-LM LENGTH_VIOLATION catch (paid Azure call); per-judge-model provenance roster UI (`S-BS-97`); a UI control to pick per-judge providers at run-time (the BFF `runEval` body doesn't thread `models/roles` — persisted judge config already works).

## 5. Exit
- D1+D2+D3+D4 GREEN under `pack=narrative` (+ vitest green for D2); the canonical regression gate (`pack=healthcare`) shows **0 new** failures vs `7be383f` (the documented pre-existing set only); ruff clean on touched `.py`; vitest 0-new vs S-BS-145; the moat + core files 0-diff; RED→GREEN demonstrated for D1's integration test + D2's vitest with the parent-RED output pasted.
- **The demonstrable cut:** an ingested narrative case loads + grades end-to-end ($0 in_process) and the SILENT_DEGRADATION floor-block renders in the ReportTab — visible, honest, offline.
- **HARD GATE:** fresh cold critic at close must confirm — the bridge preserves S-BS-9 precedence, the floor-block UI renders the flip honestly (no false BLOCK on clean), the multi-model + LENGTH_VIOLATION proofs are explicitly MOCKS (no manufactured win), and the moat/core are 0-diff.
