# Driver — `bench-salvage` phase `NARR-3`: the deterministic narrative floor executors + ATTACH-VALIDATORS

> **Generated by the monitor in a planning session. The executor pastes the
> KICKOFF block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseNARR-3-narrative-floor-executors-driver`
> **Version:** v1
> **Authored:** 2026-06-16
> **Last re-verified against code:** 2026-06-16 (per MONITOR §Phase 1a — every load-bearing citation re-grepped this session)
> **Hardness:** **HARD GATE** (touches the grade-time floor / `ground()` verdict-flip path + a moat-adjacent core file `verification/spec.py`; fresh-critic session required at close)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase NARR-3
(the deterministic narrative floor executors + ATTACH-VALIDATORS).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseNARR-3_narrative-floor-executors_driver.md  (this doc)
  3. docs/specs/SPEC_NARRATIVE_EVAL.md  (§4.1 case record, §4.3 floor codes, §5 grading model, §8 NARR-3, §9 test plan)
  4. docs/specs/SPEC_GROUNDING_TOOL_LAYER.md  (the floor/contract layer)
  5. lithrim_bench/harness/grounding.py:506-681  (the REAL floor dispatch: _artifact_content, _run_floor, ground() floor block)
  6. lithrim_bench/verification/spec.py  (the _KNOWN_TOOLS registry blocker + Claim/VerificationSpec/VerificationResult)
  7. lithrim_bench/verification/tools.py:44-149  (the VerificationTool protocol + a verify() to mirror)
  8. ../lithrim-pack-healthcare/healthcare/floors.py  (the pack floors.py TEMPLATE — DosageGroundingTool is the pure-stdlib floor to mirror)
  9. tests/verification/test_grounding_floor.py  (the floor-flips-verdict test pattern)
 10. tests/test_narrative_pack.py  (the NARR-1 subprocess pack=narrative grade pattern)
 11. .devloop/state/STREAM_bench-salvage.md  (current state + open seams)

Canonical env (export before running anything):
  LITHRIM_BENCH_PACK=narrative LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseNARR-3-narrative-floor-executors-driver
Driver doc: .devloop/prompts/bench-salvage_phaseNARR-3_narrative-floor-executors_driver.md
```

---

## 0. The load-bearing correction (read before anything — the handoff was half-wrong)

The NARR-2 handoff said *"attach the floor executors at `_CONTRACT_EXECUTORS` (grounding.py:295)."* **That is the wrong registry.** Re-grepped 2026-06-16:

- `_CONTRACT_EXECUTORS` (grounding.py:295-298) is the **SUPPRESS** registry — per-finding contracts that *disprove a confident-but-wrong finding* (clear a flag the council raised). Its comment (grounding.py:279-294) is explicit: *"the structural FLOOR direction (artifact-level contracts that inject a BLOCK the council missed) is a categorically different shape — keyed by `floor_contract_types()` and run by `_run_floor`, not here."*
- The narrative codes (`SILENT_DEGRADATION` / `BRACKET_LEAK` / `LENGTH_VIOLATION`) are **FLOOR-direction**: a deterministic check that *injects a BLOCK the council missed*. They register in the **FLOOR** registry via the active pack's `FLOOR_EXECUTORS` (grounding.py:`floor_executors()` 386-390 merges `_core_floor_executors()` ∪ the pack's `FLOOR_EXECUTORS` from `_pack_registries` 356-368). So they ship in **`packs/narrative/floors.py`**, mirroring how the clinical floor relocated to `packs/healthcare/floors.py` (PACK-3).

**The one core edit you DO need (owner decision 2026-06-16, Option A — "minimal core registration"):**
`_run_floor` builds `VerificationSpec(tool=contract_type, …)` (grounding.py:545-551), and `VerificationSpec.__post_init__` (spec.py:106-116) **hard-rejects any `tool` not in the CORE, CLOSED `_KNOWN_TOOLS` set** (spec.py:34-41) and `KeyError`s on `_REQUIRED_REFERENCE_KEYS` (spec.py:44-59). The clinical `dosage_grounding` ships its *executor* in the pack but its **tool-name + required-keys live in core `spec.py`** — so a pack floor cannot register a NEW `contract_type` without a small additive core edit. **Option A is chosen:** add the 3 narrative tool-names to `_KNOWN_TOOLS` + `_REQUIRED_REFERENCE_KEYS`, mirroring `dosage_grounding` exactly. (Option B — generalizing `spec.py` to be pack-extensible — was explicitly NOT chosen; do not refactor the registry. The "no engine edits per domain" design-debt is an acknowledged open question, not a NARR-3 deliverable.)

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review. Each line was re-grepped against current code on 2026-06-16:

1. `docs/specs/SPEC_NARRATIVE_EVAL.md` — §4.1 (eval-case record, lines 78-89), §4.2 (the `jute_transform` emits `response`/`finish_reason`/`source`, lines 89-120), §4.3 (floor codes table, lines 121-135), §5 (grading model — the day-one `SILENT_DEGRADATION` proof, lines 137-144), §8 NARR-3 (lines 184-186), §9 (test plan, lines 190-197).
2. `lithrim_bench/harness/grounding.py` — `_artifact_content` (506-511: `case["artifacts"][0]["content"]`), `_run_floor` (514-559: builds `VerificationSpec`+`Claim(claim_type=STRUCTURAL_CONFORMANCE, subject=artifact, source=case)`, calls `tool.verify`), `ground()` floor block (647-668: `vr.conforms is False` → injects `{code: params["inject_flag_code"], severity: params["inject_severity"], _floor: True}` into `active`, re-scores; `None` → surfaced, never flips; `True` → no record). `FloorExecutor` (302-314), `_core_floor_executors` (316-353), `_pack_registries`/`load_pack_floors` (356-368), `floor_executors` (386-390), `_SERVICE_CONTRACT_TYPES` (401 — **do NOT add pure-stdlib floors here**).
3. `lithrim_bench/verification/spec.py` — `_KNOWN_TOOLS` (34-41), `_REQUIRED_REFERENCE_KEYS` (44-59), `Claim` (62-70), `VerificationResult` (73-87, tri-state `conforms`), `VerificationSpec.__post_init__` (106-123, the validation that forces the core edit).
4. `lithrim_bench/verification/tools.py` — `VerificationTool(ABC)` (44-51: `name`, `handles`, `verify(claim, spec) -> VerificationResult`); `StructuralJuteTool.verify` (89-149) as the return-shape to mirror.
5. `../lithrim-pack-healthcare/healthcare/floors.py` — the pack `floors.py` TEMPLATE: module-level `FLOOR_EXECUTORS` dict (`contract_type -> FloorExecutor(tool_factory, reference_builder)`), `from lithrim_bench.harness.grounding import FloorExecutor`, a `reference_builder(params)` (`_dosage_reference`), and **`DosageGroundingTool`** — the pure-stdlib floor `VerificationTool` (regex, no httpx) you mirror three times. (External repo; re-verify line numbers on read.)
6. `lithrim_bench/harness/ontology.py` — `VerificationContractDecl` (65-71: `flag_code/question/contract_type/params/version`), `SeverityMap.rescore` (90-96: reads each active finding's `severity` via `weights`; `>= block_at_or_above` → `BLOCK`).
7. `packs/narrative/ontology.json` — `verification_contracts: []` (line 143, EMPTY — you populate it); `severity_map` (144-152: `block_at_or_above=0.5`, `HIGH=1.0`/`MEDIUM=0.5`/`LOW=0.2`); the 3 floor flags already declared (`BRACKET_LEAK` TIER_1 owner policy_judge :13-19; `SILENT_DEGRADATION` TIER_1 owner risk_judge :22-31; `LENGTH_VIOLATION` TIER_2 :44-53).
8. `packs/narrative/pack.json` — 8 keys, **NO `floors` key** (you add `"floors": "floors.py"`).
9. `packs/narrative/taxonomy_snapshot.json` — tiers confirm `BRACKET_LEAK`,`SILENT_DEGRADATION` ∈ TIER_1_NEVER_EVENTS; `LENGTH_VIOLATION` ∈ TIER_2_HIGH_RISK.
10. `tests/fixtures/narrative/storyworld_session.json` — the StoryWorld session: 5 `enhanced_scenes`; the degraded scene `scene_content_filtered_fallback` has `source: "baseline"`, and `llm_calls` carries its `finish_reason: "content_filter"` (clean scenes: `source: "enhanced"`, `finish_reason: "stop"`). This is the day-one `SILENT_DEGRADATION` data.
11. `tests/verification/test_grounding_floor.py` — `test_floor_flips_pass_to_block_and_emits_correction` (the ontology-with-verification_contracts + `ground()` PASS→BLOCK pattern) and `test_floor_inconclusive_never_flips` (`conforms is None` guardrail). (Your narrative floors are pure-stdlib → `http_client=None`, no `_ReplayHttp` needed.)
12. `tests/test_narrative_pack.py` — the NARR-1 subprocess pattern (`LITHRIM_BENCH_PACK=narrative` in a fresh interpreter + `sys.addaudithook` for the 0-`packs/healthcare`-reads assertion). Mirror for the admissibility test.

> **Citation discipline:** every file:line above was re-grepped on 2026-06-16. The external-pack (`healthcare/floors.py`) + the two test-file line numbers are from the anchor-map workflow; re-confirm them on read and halt if drifted (per EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

**Tests are authored FIRST** (per `EXECUTOR.md` §"Tests first"): write the acceptance tests, watch them fail (RED — the floor tools + the `_KNOWN_TOOLS` entries + the contracts don't exist yet, so `VerificationSpec` raises "unknown tool" and the executors are absent), commit them, then implement to GREEN.

1. **`tests/test_narrative_floor.py`** (NEW) — the acceptance tests, written first. Covers A1–A6 (§5). Mix of:
   - **Unit** (call `tool.verify(claim, spec)` directly, $0/offline, no subprocess): one pass + one violation per floor.
   - **Integration** (`ground()` with a narrative ontology carrying the 3 floor contracts): a `SILENT_DEGRADATION` violation flips a clean council PASS → `BLOCK` (composite `reject`); a clean case stays PASS; an inconclusive case (`conforms is None`) never flips.
   - **Admissibility** (subprocess `LITHRIM_BENCH_PACK=narrative`, mirror `tests/test_narrative_pack.py`): a narrative case grades through `pack=narrative` with **0 `packs/healthcare/` reads**.
   - Must fail meaningfully before code exists (RED).

2. **`lithrim_bench/verification/spec.py`** — `feat`, the ONLY core edit (Option A, additive, regression-safe):
   - Add 3 tool-name constants: `TOOL_BRACKET_LEAK = "bracket_leak"`, `TOOL_LENGTH_VIOLATION = "length_violation"`, `TOOL_SILENT_DEGRADATION = "silent_degradation"`.
   - Add them to `_KNOWN_TOOLS` (34-41).
   - Add `_REQUIRED_REFERENCE_KEYS` entries (44-59). Recommended minimal SME-pinnable shape (finalize at plan-review): `bracket_leak: set()`, `length_violation: {"min_sentences", "max_sentences"}`, `silent_degradation: set()`. (Empty set is valid — `set() - set(ref)` is empty.)
   - **Nothing else in this file.** Do NOT touch `VerificationSpec.__post_init__` logic, the clinical tool entries, or `STRUCTURAL_CONFORMANCE`.

3. **`packs/narrative/floors.py`** (NEW) — the 3 pure-stdlib floor tools + the `FLOOR_EXECUTORS` registry, mirroring `healthcare/floors.py` `DosageGroundingTool` + `_dosage_reference`:
   - `from lithrim_bench.harness.grounding import FloorExecutor` and the verification value objects (`VerificationTool`, `VerificationResult`, `Claim`, `VerificationSpec`).
   - `BracketLeakTool(VerificationTool)` — `name = "bracket_leak"`; `verify` reads `claim.subject` (the scene text), returns `conforms=False` if a bracketed instruction marker (`[...]`) is present, else `True`. Pure-stdlib `re`.
   - `LengthViolationTool(VerificationTool)` — `name = "length_violation"`; reads `claim.subject`, counts sentences, `conforms=False` if outside `[min_sentences, max_sentences]` (from `spec.reference`), else `True`. (Empty/no text → `conforms=None`, inconclusive.)
   - `SilentDegradationTool(VerificationTool)` — `name = "silent_degradation"`; reads provenance from **`claim.source`** (the full case row — `finish_reason`, `source`). `conforms=False` iff `finish_reason != "stop"` AND `source == "baseline"` (silently demoted yet shipped). If the provenance fields are absent → `conforms=None` (never flip by silence). **This is the day-one proof point.**
   - `FLOOR_EXECUTORS = { "bracket_leak": FloorExecutor(tool_factory=lambda http_client: BracketLeakTool(), reference_builder=...), ... }` — pure-stdlib tools ignore `http_client` (the lambda still accepts it).
   - **Do NOT declare `SERVICE_CONTRACT_TYPES`** (these are `in_process`, not `:3031`).
   - `SUPPRESS_EXECUTORS = {}` (narrative has no suppress executors).

4. **`packs/narrative/pack.json`** — add `"floors": "floors.py"` (so `load_pack_floors` discovers the module). One key, nothing else.

5. **`packs/narrative/ontology.json`** — populate `verification_contracts` (currently `[]`, line 143) with the 3 floor declarations. Each: `{flag_code, question, contract_type, version, params:{...}}` where `params` carries `inject_flag_code` (the UPPERCASE taxonomy code), `inject_severity`, and the tool's required reference keys. Recommended `inject_severity`: `BRACKET_LEAK`→`HIGH`, `SILENT_DEGRADATION`→`HIGH` (both TIER_1 never-events → flip to BLOCK at `block_at_or_above=0.5`); `LENGTH_VIOLATION`→`MEDIUM` (TIER_2; `0.5` still blocks per the SeverityMap "a lone MEDIUM blocks" rule — finalize block-vs-warn at plan-review). Keep the ontology prose **needle-free** (no clinical wording — the `_provenance.note` invariant).

6. **`packs/narrative/examples/narrative_v1.jsonl`** (extend, OPTIONAL-but-recommended) — add 1–3 by-construction violation eval-cases (a `content_filter`/`baseline` case for `SILENT_DEGRADATION`; a bracket-leak case; a short-preamble case), each carrying the provenance/content the floor reads, with `expected_safety_flags` set to the injected code. If `load_case` does not preserve the provenance fields where the tool reads them, surface it at plan-review (see §3 risk).

> **Trust-model boundary (carry from NARR-2):** these floor executors CAN flip a verdict (that is the point — they are the floor). The NARR-2 `jute_transform` extractor stays OUT of any grounding registry (ingestion ≠ grounding). Do not touch `jute_extractor.py` or `ingest_cases`.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with: Understanding (one paragraph), file-by-file changes, test plan (the RED tests you'll write first), risks, and any proposed deviations. Wait for "go" before writing code.

**Must resolve at plan-review (load-bearing decisions / risks):**
- **R1 — SILENT_DEGRADATION data path (the #1 risk).** The floor reads provenance via `claim.source` (= the case row). Confirm WHERE `load_case` puts `finish_reason`/`source` for an admissible eval-case (top-level case fields per the §4.2 `jute_transform`, vs `artifacts[0].metadata`). If `load_case` strips extra fields, the violation example case must carry them where the tool reads them. State the exact path you'll read + show the case shape.
- **R2 — `inject_severity` calibration** for `LENGTH_VIOLATION` (TIER_2): BLOCK (`MEDIUM`) vs WARN (`LOW`). TIER_1 floors are firmly `HIGH`/BLOCK.
- **R3 — `_REQUIRED_REFERENCE_KEYS` shape** for the 3 tools (empty vs SME-pinned bounds).
- **R4 — the floor only runs when `_artifact_content(case)` is non-None** (grounding.py:530-532). Confirm the narrative cases carry `artifacts[0].content` (the `response`) so `_run_floor` doesn't bail before reaching `SILENT_DEGRADATION` (which reads `claim.source`, not the artifact). Show the case has both.

---

## 4. Scope guardrails — NOT in scope

- **The judge-layer codes** (`BODY_CONTRADICTION`, `MODE_INAPPROPRIATE`, `TONE_INFIDELITY`, `PERSONALIZATION_MISS`) — LLM-judge criteria, P1/NARR-4. Floor codes only.
- **`POV_VIOLATION` / `LANGUAGE_DRIFT` / `CLICHE_OVERUSE` / `REPETITION`** — deferred floor codes; NARR-3 ships exactly the 3 P0 floors (`SILENT_DEGRADATION`, `BRACKET_LEAK`, `LENGTH_VIOLATION`).
- **Option B (generalizing `spec.py` to be pack-extensible)** — explicitly NOT chosen. Do not refactor `_KNOWN_TOOLS`/`_REQUIRED_REFERENCE_KEYS` into a pack-merge. Additive entries only.
- **The frontend demo / multi-model council** — NARR-4.
- **The D-C corpus bridge** (`S-BS-NARR2-1`: ingested cases gradeable through `load_case`/the picklist) — NARR-4; do not wire it here.
- **The withstands-gate mechanism** (`runtime/council/signals.py`, `withstands.py`) + the MOAT (`compliance_council`, `_apply_consensus`, `extract_verdict_confidence`, `judge_metric.LENS_BY_ROLE`, `tools.py`) — **byte-frozen**. The floor runs in `ground()`, downstream of the frozen council.
- **Re-snapshotting** the narrative taxonomy, or editing `scripts/snapshot_taxonomy.py`.
- **Any `packs/healthcare/` or external-pack edit.**
- Drive-by formatting / dependency bumps / "while I was here" edits. No prettier/ruff-format sweeps beyond your touched lines.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

Each is a **test written first** (the deterministic gate the cycle + critic hang on). Test names are suggestions; keep them stable in the session log `acceptance` array.

- **A1 — `bracket_leak` fires.** `tests/test_narrative_floor.py::test_bracket_leak_violation_and_clean` — `BracketLeakTool.verify` returns `conforms=False` on text containing a `[...]` instruction marker, `True` on clean prose.
- **A2 — `length_violation` fires.** `::test_length_violation_and_clean` — `conforms=False` on a 1-sentence (or ≥6-sentence) preamble, `True` on a 3–4-sentence one, `None` on empty.
- **A3 — `silent_degradation` fires (the headline).** `::test_silent_degradation_violation_clean_and_inconclusive` — `conforms=False` when `finish_reason != "stop"` AND `source == "baseline"`; `True` on `finish_reason="stop"`/`source="enhanced"`; `None` when the provenance fields are absent. Drive from the `storyworld_session.json` degraded scene values (`content_filter`/`baseline`).
- **A4 — the floor flips the verdict via `ground()`.** `::test_silent_degradation_flips_pass_to_block` — a clean council result (PASS, no findings) + a degraded case + the narrative ontology (3 floor contracts) → `ground()` injects the `SILENT_DEGRADATION` BLOCK finding, `grounded.verdict == "BLOCK"`, `composite(...).verdict == "reject"`, `floor_blocks` carries the injected finding; a clean case stays PASS/`approve`; `floor_blocks == []`.
- **A5 — admissibility, pack-bound, zero leakage.** `::test_narrative_floor_grades_under_pack_narrative` (subprocess `LITHRIM_BENCH_PACK=narrative`) — a narrative violation case grades to a verdict through the authored path; **`healthcare_reads == []`** (mirror `tests/test_narrative_pack.py`). Confirms the new `contract_type`s resolve via `floor_executors()` under `pack=narrative` with zero engine edits beyond the `spec.py` registration.
- **A6 — moat + clinical floor regression-clean.** `::test_known_tools_additive_and_clinical_floor_intact` (or fold into Gate 0) — `_KNOWN_TOOLS` still contains all 6 prior tools (additive, not replaced); the clinical floor tests (`tests/verification/test_grounding_floor.py`) + the moat guard tests stay green under the canonical env. The `spec.py` diff is purely additive (3 constants + 3 dict entries).

### Diagnostic stats (NOT acceptance gates)
- The `inject_severity` chosen per floor + the resulting verdict (BLOCK/WARN) — reported in the session log; the calibration is a tunable, not a gate.
- Number of by-construction violation example cases added to `narrative_v1.jsonl`.

---

## 6. Commit structure + message templates

Atomic commits per logical unit. **Tests commit first** so the diff shows RED→GREEN. Suggested order:

1. **`test(narrative): NARR-3 floor executors acceptance — red`** — `tests/test_narrative_floor.py`. Body notes the RED run (floor tools + `_KNOWN_TOOLS` entries absent → `VerificationSpec` raises "unknown tool").
2. **`feat(verification): register the 3 narrative floor tool-names (NARR-3, Option A)`** — the additive `spec.py` edit. Body: cites the owner's Option-A decision + why (the closed `_KNOWN_TOOLS` registry; mirrors `dosage_grounding`); names the additive-only contract.
3. **`feat(narrative): deterministic floor executors — bracket/length/silent-degradation`** — `packs/narrative/floors.py` + `pack.json` `"floors"` key.
4. **`feat(narrative): attach the floor contracts in the pack ontology`** — `packs/narrative/ontology.json` `verification_contracts` + (optional) the violation example cases in `narrative_v1.jsonl`.

Each commit body ends with:
```
Executed per bench-salvage-phaseNARR-3-narrative-floor-executors-driver by session-2026-06-NN-N.
See .devloop/sessions/session-bench-salvage-phaseNARR-3-2026-06-NN.json

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```
Use pathspec-scoped commits (`git commit -- <your-files>`) — the index may carry foreign files from concurrent sessions.

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] Acceptance tests were written first and failed before code (RED run recorded; the "unknown tool" raise + absent executors are the RED).
- [ ] `test(...)` commit precedes the implementation commits it covers.
- [ ] All acceptance criteria A1–A6 PASS.
- [ ] Full suite passes + ruff clean (re-run from clean state; record commands). **Canonical env: `LITHRIM_BENCH_PACK=narrative LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare PYENV_VERSION=debuglithrim`** for the narrative tests; ALSO run once with `LITHRIM_BENCH_PACK=healthcare` to prove the clinical floor + the rest of the suite are 0-new-failure (the `spec.py` edit is additive). Note the pre-existing failures honestly (the `S-BS-NARR2-4` pack-activation + observation order-pollution set).
- [ ] `spec.py` diff is purely additive (3 constants + 3 `_REQUIRED_REFERENCE_KEYS` entries); `__post_init__` + the 6 prior tools byte-unchanged.
- [ ] MOAT byte-frozen: `git diff` shows 0 changes to `runtime/council/{compliance_council.py,signals.py,withstands.py}`, `judge_metric.py` `LENS_BY_ROLE`, `apps/bff/agent/tools.py`. Floor logic lives only in the pack + `ground()` (unchanged).
- [ ] No file in the diff outside §2 deliverables (scope held — grep for the §4 NOT-in-scope items).
- [ ] No services autostarted (`:3031` not needed — these floors are pure-stdlib `in_process`).
- [ ] No pushes / publishes / tags.
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`.

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §0 (the registry correction) then §1 pre-flight docs in order — especially `grounding.py:506-681`, `spec.py`, and `healthcare/floors.py` (`DosageGroundingTool`).
3. Re-read §2–§7 of this driver.
4. Post plan-review per §3 — resolve R1 (the SILENT_DEGRADATION data path) explicitly.

---

## 9. References

- Spec: `docs/specs/SPEC_NARRATIVE_EVAL.md` §4.1/§4.3/§5/§8/§9 + `docs/specs/SPEC_GROUNDING_TOOL_LAYER.md`
- Stream state: `.devloop/state/STREAM_bench-salvage.md`
- Prior cycle session log: `.devloop/sessions/session-bench-salvage-phaseNARR-2-2026-06-16.json`
- Prior cycle handoff: `.devloop/sessions/HANDOFF_bench-salvage_NARR-2_2026-06-16.md`
- Anchor map (this cycle's pre-work): workflow `wf_2ac1c863-404` (4 readers; the floor-dispatch / pack-mirror / fixture / test-path reports)
- The clinical floor mirror: `../lithrim-pack-healthcare/healthcare/floors.py` (`DosageGroundingTool`, `_dosage_reference`, `FLOOR_EXECUTORS`)
- The floor-flip test pattern: `tests/verification/test_grounding_floor.py`
- The pack=narrative subprocess pattern: `tests/test_narrative_pack.py`

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session required at close (touches the grade-time floor verdict-flip path + the moat-adjacent core `verification/spec.py`). Trigger via a fresh critic session per `.devloop/prompts/KICKOFF_CRITIC.md`; verify the moat byte-freeze + the `spec.py` additive-only claim non-vacuously.
