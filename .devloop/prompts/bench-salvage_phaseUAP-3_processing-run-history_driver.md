# Driver — `bench-salvage` phase `UAP-3`: processing surface + run-history + the authored→flip wiring (R4+R6, S-BS-63)

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseUAP-3-processing-run-history-driver`
> **Version:** v1
> **Authored:** 2026-06-04 (from the LOCKED SPEC §5 R4/R6 + the UAP-3 handoff — no prior stub; pack backfilled per memory `devloop-task-pack-stale-uap-from-spec`)
> **Last re-verified against code:** 2026-06-04 (9 citation groups re-grepped; 9 CONFIRMED, 0 DRIFTED, 0 MISSING)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase UAP-3
(processing surface + run-history + the authored→flip wiring, R4+R6 / S-BS-63).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseUAP-3_processing-run-history_driver.md   (this doc)
  3. docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md  (§5 R4/R6, §3.3 Stage-3 processing, §4 BFF surface, §13 surface context)
  4. .devloop/sessions/HANDOFF_bench-salvage_phaseUAP-3_kickoff_2026-06-04.md
  5. .devloop/state/STREAM_bench-salvage.md  (the UAP-2 row + seams S-BS-52/55/56/63)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseUAP-3-processing-run-history-driver
Driver doc: .devloop/prompts/bench-salvage_phaseUAP-3_processing-run-history_driver.md
```

---

## 1. Pre-flight reading (ordered)

1. `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md` — **§5 R4** (`RunPanel` + Stage-3 processing) + **R6** (`POST /v1/eval-pack/run` + `GET /v1/runs` — batch + run-history) · **§3.3** (Stage-3 net-new UI surface) · **§4 BFF surface** + **gen-UI registry** contract · **§13** (the conversational surface UAP-5 will layer on — keep tools registry-clean, but do NOT build it here).
2. `scripts/run_eval.py:101-156` — `run(agent, *, live, in_process, out_dir, ontology_path)` + the 3-way grade dispatch (in_process:125-142 / live:143-153 / replay:155). **The S-BS-63 thread-point.**
3. `lithrim_bench/harness/grade.py:129-170` — `grade_inprocess(case, *, org_id, semantic_stage, provenance_store)`; council built via `LocalPipelineBackend`→orchestrator→`ComplianceCouncil()` **default config** (`lithrim_bench/runtime/pipeline/stages.py:708-730` `_run_council_and_map`) — **never reads assignments (S-BS-63).**
4. `lithrim_bench/runtime/council/judges_dspy.py:298-304` — `build_trio(*, predictors, taxonomy_context, ontology, assignments)` — **already accepts `assignments: dict[str, Sequence[str]]`** (role→flag codes). The plumbing exists; the gap is threading the persisted assignments into the in-process path.
5. `lithrim_bench/harness/judges.py:119-128` — `load_judge(role, *, db_path) -> JudgeConfig | None` (carries `assigned_flags`) — the loader to assemble the assignments dict.
6. `apps/bff/app.py` — `run_eval_endpoint:222` + `_council_view:256` + the response build `252-257` (NO `pipeline_run_id` → **S-BS-56**); `/v1/runs/{run_id}/audit:669-688` (404s on replay `684-686`). **No `GET /v1/runs` yet.**
7. `lithrim_bench/runtime/pipeline/provenance.py:38-79` — `SqliteProvenanceStore.save:62` / `find_by_id:76` (async). Replay + live DON'T persist; only in_process when the store is injected (`run_eval.py:141`) → **S-BS-52** (replay not auditable).
8. `lithrim_bench/harness/evalpack.py:81-119` — `build_pack(pack_id, agents, *, live, out_dir)` / `dump_pack:109` / `load_pack:117` (R6 batch primitive). `lithrim_bench/harness/corpus.py:64-112` — `project`/`build_corpus`/`read_corpus`.
9. `apps/shell/src/genui/registry.js:21-30` — `KNOWN_TOOLS` (8; no `tool-run_panel`) + `apps/shell/src/panes.jsx:79-204` `CenterPane` (the mount: `renderTool` at 146-150; VerdictCard 128 / CalibrationChart 137 / agent_editor+audit_log 162-163) + `apps/shell/src/bff.js` (`runEval` exists; add `getRuns`).

> **Citation discipline:** every file:line above was re-grepped 2026-06-04 (all CONFIRMED). If you spot drift on read, halt and surface it (EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

> **UAP-3 is large (R4 + R6 + the wiring gap). The P0 core is A/B below — the authored→flip loop + run ids; C/D/E/F complete the surface. If the cycle overflows, plan-review may propose splitting R6-batch (C) to a follow-on — flag it, don't silently drop.**

**A. S-BS-63 — thread authored assignments into the in-process grade (THE load-bearing gap; closes "authoring feels inert").**
1. **`scripts/run_eval.py`** (`run`, 101-156) — for the agent's trio, `load_judge(role)` (judges.py:119) → assemble `assignments: dict[role, tuple[flag,...]]` → thread into the in_process (and live) grade so the council is built with `build_trio(ontology=, assignments=)`. Replay path unchanged (no live council).
2. **`lithrim_bench/harness/grade.py`** (`grade_inprocess`, 129-170) — accept an `assignments=` (or a pre-built trio-backed `semantic_stage=`) and route it to the council construction. **Decide the seam at plan-review:** (i) inject a `semantic_stage` built from `build_trio(assignments=)` via the existing `semantic_stage=` param (preferred — no orchestrator edit), vs (ii) extend the orchestrator path to read assignments. **Constraint: `compliance_council._apply_consensus` + the per-judge seam dict stay byte-frozen (A1 of the whole DSPy track); this is council-CONSTRUCTION/injection, above the frozen seam.**
3. **`apps/bff/app.py`** (`run_eval_endpoint`, 222) — ensure the authored agent's assignments flow through (it already `_load_agent`s; verify the assignments reach the grade).

**B. Run ids + run-history + replay-provenance (R6 read half; S-BS-56 + S-BS-52).**
4. **`apps/bff/app.py`** — surface `pipeline_run_id` in the `/v1/run-eval` response (**S-BS-56**; also unblocks the A8 run→audit leg) + add **`GET /v1/runs`** (list persisted runs from the provenance store / `PIPELINE_RUNS`, newest-first, id+verdict+agent+grade_path+ts projection). `/v1/runs/{id}/audit` already exists (669).
5. **`scripts/run_eval.py` + `lithrim_bench/harness/grade.py`** — **S-BS-52:** persist a provenance blob for the **replay** path too (thread `SqliteProvenanceStore` into `grade_replay`, or document a scoped deferral if the replay record lacks a `PipelineProvenance`). Decide at plan-review; if deferred, run-history is in_process+live-only this phase and S-BS-52 stays open with a written reason.

**C. R6 — eval-pack batch processing (`POST /v1/eval-pack/run`).**
6. **`apps/bff/app.py`** — `POST /v1/eval-pack/run` over `evalpack.build_pack` (evalpack.py:81) — batch a pack of agents/cases (replay default; live/in_process cost-gated) → a frozen pack + the run ids. The "did it move the number?" loop.

**D. R4 — the RunPanel UI surface.**
7. **`apps/shell/src/genui/RunPanel.jsx`** (new) + register `tool-run_panel` in **`registry.js`** (KNOWN_TOOLS→9) — a run surface: trigger `/v1/run-eval` (replay default; live/in_process opt-in, **cost-gated confirm**), show status + the composite verdict + the realized `_council_view` votes + a run-history list (`GET /v1/runs`). Follow the flat-spread `part.output` prop convention (registry.js:59-64).
8. **`apps/shell/src/panes.jsx`** (`CenterPane`, 79-204) — mount `tool-run_panel` like the other output tools (162-163). **`apps/shell/src/bff.js`** — add `getRuns()` (and `getRun`/`runId` plumbing); `runEval` exists.

**E. Tests.**
9. **`tests/test_uap3_*.py`** (Python, the `tests/test_ws5_bff.py` direct-handler pattern) — (a) S-BS-63: author a judge with an assigned flag → in_process grade → the council reflects the authored lens (the flip); (b) `_apply_consensus` + per-judge seam byte-0-delta; (c) `/v1/runs` lists + `pipeline_run_id` surfaced + a run_id round-trips to `/v1/runs/{id}/audit`; (d) replay-provenance (or the documented deferral); (e) `POST /v1/eval-pack/run`.
10. **`apps/shell/src/genui/RunPanel.test.jsx`** (Vitest+RTL, the WS-5c/5d `bff.js`-mock pattern) — RunPanel renders + triggers a run + lists history.

**F. Docs.**
11. **`docs/specs/SPEC_PRODUCT_SHELL.md` §10** — ratify the new v1 BFF routes (`GET /v1/runs`, `POST /v1/eval-pack/run`, the `pipeline_run_id` field) — the **S-BS-51 pattern** (don't grow the surface unratified). Note S-BS-63 closed + S-BS-56/52 disposition in the session log.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with:
- Understanding (one paragraph)
- File-by-file changes
- Test plan
- Risks
- **The 4 load-bearing decisions:** (1) the S-BS-63 injection seam — `semantic_stage` injection [preferred] vs orchestrator-reads-assignments; (2) S-BS-52 replay-provenance — persist now vs scoped-defer-with-reason; (3) cost posture — any live/in_process acceptance run must be cost-confirmed ($0 replay is the default; name the envelope, ~$0.10-0.20/in_process case per the WS-6c-AGENTIC line); (4) scope — is R6-batch (C) in this cycle or split to a follow-on?
- Any proposed deviations from this driver

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

- **The withstands-gate / signals orchestration (§2A)** — that is **UAP-3b** (the moat). UAP-3 wires authored config → the *existing* council grade; it does NOT add GroundingChecks or the Ralph-Loop critique. Do not touch.
- **Optimize / calibration in-UI (R5)** — **UAP-4**, gated **S-BS-49**.
- **Shipping a floor/KB from a committed ontology** — **S-BS-13/16/41** stay as gates; clinical_v1 is floor-less; do NOT trip them.
- **Authoring-assist (describe→card, R10)** — **UAP-5a**; no LLM-draft path here.
- **The `JudgeEditor` shell mount (S-BS-62)** — the spawned fix-task / UAP-5a line-item; keep it out unless the RunPanel mount makes it a one-line freebie (then flag, don't silently fold).
- **Re-snapshotting the stale ontology `owner_roles` (S-BS-59)** — separate pass.
- **FROZEN:** `compliance_council._apply_consensus` + the per-judge seam dict + the committed seeds (`clinical_v1.json`, `data/config/agents`, `council_roles/*.txt`). This cycle is council-CONSTRUCTION + BFF/UI, not consensus math.
- Drive-by formatting / dep bumps / "while I was here" edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1. Authored→flip (S-BS-63 closed):** assign a flag to a judge via `PUT /v1/judges`, run `/v1/run-eval {in_process}` (or the Python grade path) → the council votes reflect the authored lens (a verdict/finding change vs the default-config grade). Verify via the Python test (E-a) + the $0 `JudgeEditor` preview parity.
- **A2. Frozen seam:** `git diff` over `compliance_council.py` (esp. `_apply_consensus`) + the per-judge seam dict = **0 lines**. Verify via `git diff … | wc -l`.
- **A3. Run ids + history (S-BS-56):** `/v1/run-eval` response carries `pipeline_run_id`; `GET /v1/runs` lists persisted runs newest-first; a listed `run_id` round-trips to `GET /v1/runs/{id}/audit` (no 404 for a persisted run).
- **A4. Replay-provenance (S-BS-52):** a replay run is persisted + auditable — OR a written, scoped deferral in the session log + S-BS-52 stays open (plan-review decision).
- **A5. Eval-pack batch (R6):** `POST /v1/eval-pack/run` runs a pack via `build_pack` and returns the frozen pack + run ids (or split-to-follow-on per plan-review, flagged).
- **A6. RunPanel:** `tool-run_panel` renders in `CenterPane`, triggers a run (replay $0 default; live/in_process cost-gated), shows verdict + votes + history. Vitest green. The **`:5180` visual smoke** (user-run, no-autostart) is the load-bearing A8-class gate — OWED at close.
- **A7. Green bar:** ruff-clean on changed files; default suite + `debuglithrim` suite + council + BFF + Vitest re-run + reproduced; default-deps import-clean (no dspy/openai/fastapi leak into core). Pathspec-only commits (foreign untracked: `.claude/`, `KICKOFF_CRITIC_…`, `REPORT_fhir_agentbench…`).

### Diagnostic stats (NOT acceptance gates)
- The specific verdict/finding delta the authored lens produces (illustrative; report in the session log, not a gate).
- `pipeline_run_id` count / run-history length after the test runs.
- Any live/in_process cost actually spent (report; the gate is "cost-confirmed", not a number).

---

## 6. Commit structure + message templates

Atomic commits per logical unit (pathspec-only). Suggested order:

1. **`feat(harness): thread authored judge assignments into the in-process grade (S-BS-63)`** — run_eval + grade.py + the assignments dict.
2. **`feat(bff): surface pipeline_run_id + GET /v1/runs run-history (S-BS-56)`**
3. **`feat(harness): persist replay provenance for auditability (S-BS-52)`** — or `docs: scope-defer S-BS-52 with reason`.
4. **`feat(bff): POST /v1/eval-pack/run batch processing (R6)`**
5. **`feat(shell): RunPanel gen-UI (tool-run_panel) + run-history (R4)`**
6. **`test(uap-3): authored→flip + run-history + eval-pack + RunPanel`**
7. **`docs(spec): ratify /v1/runs + /v1/eval-pack/run in SHELL §10 (S-BS-51)`**

Each commit body ends with:

```
Executed per bench-salvage-phaseUAP-3-processing-run-history-driver by session-2026-06-04.
See .devloop/sessions/session-bench-salvage-phaseUAP-3-2026-06-04.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance criteria PASS (per §5); A6 `:5180` smoke flagged OWED if user hasn't run it
- [ ] All commits exist; working tree clean; `git diff <parent> HEAD --stat` = only §2 files
- [ ] Tests pass + linters clean (re-run; record commands + counts)
- [ ] No file in the diff outside §2 deliverables (scope held); frozen seam 0-delta (A2)
- [ ] No services autostarted; no foreign files swept (pathspec-only)
- [ ] No pushes / publishes / tags
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order; re-verify the §1 citations on open.
3. Re-read §2-§7 of this driver.
4. Post plan-review per §3 (resolve the 4 load-bearing decisions).

---

## 9. References

- Spec (LOCKED): `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md` §5 R4/R6 · §3.3 · §4 · §13 (surface context)
- Handoff: `.devloop/sessions/HANDOFF_bench-salvage_phaseUAP-3_kickoff_2026-06-04.md`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (UAP-2 row + seams S-BS-52/55/56/63)
- Prior cycle session log: `.devloop/sessions/session-bench-salvage-phaseUAP-2-2026-06-04.json`
- Prior cycle critique: `.devloop/sessions/critique-bench-salvage-phaseUAP-2-2026-06-04.md`
- Memory: `unified-authoring-product-frozen-journey` · `judge-creation-must-be-demonstrable` · `git-commit-pathspec-dirty-index` · `devloop-task-pack-stale-uap-from-spec` · `persistence-blob-projection-architecture` (S-BS-52/56 = the projection/run-history tier)

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session required; trigger via `/devloop-critique bench-salvage UAP-3`. *Reason: closes the load-bearing authored→flip wiring (S-BS-63) through the grade path adjacent to the frozen consensus seam (A2 byte-0-delta is load-bearing), grows the v1 BFF surface (§10 ratify), and adds run-provenance persistence — the calibration/audit-bearing core of the product. Live/in_process acceptance runs are cost-gated.*
