# Driver — `bench-salvage` phase `CE-STANDALONE-1`: the generic-CE walking-skeleton

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseCE-STANDALONE-1-generic-pack-walking-skeleton-driver`
> **Version:** v1
> **Authored:** 2026-06-11
> **Last re-verified against code:** 2026-06-11 (per MONITOR §Phase 1a — monitor spot-verification at HEAD a4f6548)

**The demonstrable "the Core runs STANDALONE as a generic CE" proof.** Build a genuinely
independent **non-clinical** pack + a headless end-to-end test that grades a non-clinical
case through the **authored path**, with the healthcare pack UNLOADED and `:8002` down,
asserting **zero clinical leakage** + a verdict. This *falsifies* the decoupling claim
(PACK-2c swapped a pack's DATA but reused healthcare's PROMPTS+ontology — see
`SPEC_STANDALONE_CORE_VALIDATION.md` §1) and becomes the regression guard for the
`build_prompt` retire (6b). **Non-frozen — touches NO core council file.**

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase CE-STANDALONE-1 (generic-pack-walking-skeleton).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseCE-STANDALONE-1_generic-pack-walking-skeleton_driver.md  (this doc)
  3. docs/specs/SPEC_STANDALONE_CORE_VALIDATION.md  (the demarcation + §2 audit evidence + §3 design)
  4. CLAUDE.md  (§"Core invariant" + §"Taxonomy snapshot is the contract")

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseCE-STANDALONE-1-generic-pack-walking-skeleton-driver
Driver doc: .devloop/prompts/bench-salvage_phaseCE-STANDALONE-1_generic-pack-walking-skeleton_driver.md
```

---

## 1. Pre-flight reading (ordered) — all file:lines monitor-verified 2026-06-11

**The pack contract (what an independent pack supplies):**
1. `lithrim_bench/harness/pack.py:61-196` — `active_pack()` `:61`, `_manifest()` `:67`, `pack_tiers()` `:106`, `pack_tier1_owners()` `:122`, `pack_lenses()` `:137`, `pack_production_judges()` `:154`, `pack_prompts_path()` `:?`, `pack_ontology_path()` `:192`. The accessors your pack must satisfy.
2. `packs/story_audit/{pack.json,taxonomy_snapshot.json}` — the closest existing 2nd pack, BUT it REUSES `packs/healthcare/council_roles` + `ontology` (the gap this cycle closes). Use as a manifest-shape reference ONLY; your pack must be fully independent.
3. `packs/healthcare/{pack.json,ontology.json,taxonomy_snapshot.json,council_roles/}` — the reference shapes to mirror with non-clinical content.

**The ontology schema (so the non-clinical `ontology.json` validates):**
4. `lithrim_bench/harness/ontology.py:144-178` — `load_ontology` required fields: `flags[]` (`flag/category/definition/when_to_use/when_NOT_to_use/owner_roles/tier/gradeable/reliability_pillar`), `questions[]` (`role/ordinal/text`), `verification_contracts[]` (`flag_code/question/contract_type/params/version`), `severity_map{weights,block_at_or_above,warn_above}`, `ontology_version`, `domain`.

**The authored (domain-agnostic) grade path:**
5. `scripts/run_eval.py:172-302` — `run()`; `:257` `if assignments or models or roles:` → `build_authored_semantic_stage` (`:259-273`). The authored path is selected by passing **assignments**. `:222` ontology resolves via `agent.ontology_abspath()` (now S-BS-128-self-healing).
6. `lithrim_bench/runtime/council/judge_assignment.py:46-101` `render_role_questions(ontology, role, assigned_flags=)` — the pack-loaded, domain-agnostic prompt (`load_role_prompt` base + ontology lens). **Experiment proved 0 clinical leakage on a non-clinical pack.**
7. `lithrim_bench/runtime/council/authored_stage.py` `build_authored_semantic_stage` + `judges_dspy.py:322-398` `build_trio` (the in-process trio over the authored prompts). `build_judge_lm` (`judges_dspy.py:205-256`) — the provider seam (Azure / BYO-Claude / mock).

**The `$0` test mechanism + the proof pattern:**
8. `lithrim_bench/runtime/council/tests/test_trio_dspy.py` + `test_judge_metric.py` — find the **fake/mock LM** pattern (a DSPy predictor double) so the E2E grades for `$0` in CI without paid Azure.
9. `tests/test_pack_layer2c.py:143-243` — the `LITHRIM_BENCH_PACK=<pack>` **subprocess** proof pattern to mirror (module-level `__import__` pack resolution is frozen at import → the cross-pack assertion MUST run in a subprocess).
10. `tests/fixtures/ws0/case.*.jsonl` — the case shape (`case_id` + `artifacts[0].content` + `expected_compliance_verdict`); the schema is generic (SPEC §2.3).

> **Citation discipline:** verified at HEAD `a4f6548` on 2026-06-11. Drift on read → halt + surface.

---

## 2. Deliverables (file-by-file)

### D1 — a genuinely independent non-clinical pack `packs/<generic>/`

**Domain (plan-review):** a neutral, legible non-clinical domain — monitor default **`support_ticket_qa`** (a support-agent QA: did the reply fabricate policy, make unsupported claims, skip a required disclosure, leave the issue unresolved). Surface your pick in plan-review.

- `pack.json` — `pack_id`, `tier: core` (the OSS sample tier; OQ-2), own `ontology`/`flags_ref`/`council_roles` (NO `packs/healthcare/` paths), `judges` ⊆ `_ROLE_DEPLOYMENT` = reuse role NAMES `["risk_judge","policy_judge","faithfulness_judge"]` (the PACK-2c OQ-2 boundary — packs subset known deployable identities; do NOT introduce a new deployable judge).
- `ontology.json` — 4–6 non-clinical flags (per §1.4 schema), internally consistent: each `gradeable` flag's code ∈ a tier; `owner_roles` set for Tier-1; `severity_map` + `questions` + `verification_contracts: []` (no `kb_grounding` — keeps `:8002` off the path) + versions + `domain`.
- `taxonomy_snapshot.json` — `tiers`/`tier1_owners`/`production_judges`/`lenses` over the non-clinical codes (lens ⊆ owners; codes ⊆ tiers; the snapshot is the council's source-of-truth).
- `council_roles/{risk_judge,policy_judge,faithfulness_judge}.txt` — **non-clinical** role prompts (no HIPAA/clinical prose).
- a tiny non-clinical **case** (committed under the pack or `tests/fixtures/standalone/`): `case_id` + an `artifacts[0].content` support-reply text + `expected_compliance_verdict` (pick a case that should `reject`/`needs_review` on a planted defect).

### D2 — the headless standalone E2E `tests/test_standalone_ce.py`

Mirror `test_pack_layer2c.py`'s subprocess pattern, `LITHRIM_BENCH_PACK=<generic>`, **no `:8002`**:

- **A-STANDALONE-1 (0 clinical leakage):** under the generic pack, render the authored judge prompts (`render_role_questions(ontology, role, assigned_flags=...)` for each production judge) and assert **0** clinical-needle hits (`HIPAA|patient|medication|dosage|allerg|clinical|SOAP|escalat|consent|transcript`, case-insensitive). (This is the §2.1 experiment, inverted-green.)
- **A-STANDALONE-2 (grade E2E → verdict):** grade the non-clinical case through the **authored path** (pass `assignments` = each judge's pack lens so `run()` selects the authored stage) → a verdict object. `$0` via the mock LM (per §1.8) in CI; the real-provider confirmation is a SEPARATE env-gated live smoke (don't spend in CI).
- **A-STANDALONE-3 (`:8002` down):** the grade completes with `:8002` unreachable (the generic ontology declares no `kb_grounding` contract). Assert no crash + a verdict.
- **A-STANDALONE-4 (healthcare UNLOADED):** assert the run reads **no `packs/healthcare/` path** (a path-trace, an import guard, or assert `active_pack() == "<generic>"` + the resolved prompt/ontology paths are under `packs/<generic>/`).
- **A-STANDALONE-5 (provider seam):** the judge binds to a provider via env (`build_judge_lm`) — the mock/offline LM for CI; the gated live smoke proves a real BYO provider produces a sane verdict.

### D3 — docs

- `SPEC_STANDALONE_CORE_VALIDATION.md` — flip §5 acceptance line 1 ("a real independent non-clinical pack exists") to **DONE** + cite `packs/<generic>/` + `tests/test_standalone_ce.py`. Add a one-line CLAUDE.md note that a `tier: core` non-clinical sample pack exists (the standalone-CE fixture / OSS sample).

---

## 3. Plan-review checkpoint (non-negotiable)

Post: Understanding · the non-clinical domain + the flag set + tier/owner/lens consistency · the `$0` grade mechanism (mock LM vs `$0`-replay — OQ-3) · how A-STANDALONE-4 proves healthcare-unloaded · the subprocess structure · Test plan · Risks · deviations. **Wait for "go".**

---

## 4. Scope guardrails — NOT in scope

- **The frozen council** — `compliance_council.py`, `judges_dspy.py` consensus seam. This cycle adds a PACK + a TEST; it touches **no** core council file. (The `build_prompt` retire is **6b**, a later cycle.)
- **Retiring / routing `build_prompt`** — that's CE-PACK-6b-ROUTE. This cycle uses the authored path EXPLICITLY (via `assignments`), because 6b-ROUTE (authored-only default) has NOT landed yet.
- **Deleting `safety_flags.py` / relocating flag defs** — 6b-CLEAN.
- **A new deployable judge role** — packs subset KNOWN roles (OQ-2). Reuse risk/policy/faithfulness names.
- **BYO-vector / KB-VENDOR-1** — CE-STANDALONE-2. The generic ontology declares no `kb_grounding`.
- **Paid CI** — the CI grade is mock-LM `$0`; the live smoke is env-gated, run by the user.
- Drive-by formatting / dep bumps.

If something feels load-bearing-but-not-listed, halt and surface it.

---

## 5. Acceptance criteria

- **A1–A5** = A-STANDALONE-1..5 above (each a row in the session log `acceptance`).
- **A6 (suite):** full suite **0-new vs parent `a4f6548`** (debuglithrim; the 2 pre-existing S-BS-96 observation guards don't count). `ruff check .` + `ruff format --check .` clean on touched files.
- **A7 (boundary):** `grep -rl 'packs/healthcare' packs/<generic>/` → **empty** (the pack is genuinely independent, no healthcare reuse).

### Diagnostic stats (NOT gates)
- The live-smoke verdict + the per-judge findings (reported; the live confirmation, not a CI gate).

---

## 6. Commit structure + message templates

Atomic, pathspec-scoped (memory `git-commit-pathspec-dirty-index`):
1. `feat(pack): support_ticket_qa — a tier:core independent non-clinical pack (CE-STANDALONE-1 D1)`
2. `test(ce): headless standalone walking-skeleton — authored path, healthcare unloaded, :8002 down (CE-STANDALONE-1 D2)`
3. `docs(spec): mark the independent non-clinical pack DONE in SPEC_STANDALONE_CORE_VALIDATION (D3)`

Each body ends with:
```
Executed per bench-salvage-phaseCE-STANDALONE-1-generic-pack-walking-skeleton-driver by session-2026-06-11-N.
See .devloop/sessions/session-bench-salvage-phaseCE-STANDALONE-1-2026-06-11.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before session log)

- [ ] A1–A7 PASS
- [ ] Commits exist; tree clean for the cycle pathspec (foreign staged files untouched)
- [ ] Tests + linters clean (record commands + env)
- [ ] No file outside §2 deliverables; NO core council file touched
- [ ] No services autostarted; no pushes/tags; CI grade is `$0` (mock LM)
- [ ] Session log per template; REPORT documents the `$0` mechanism + the (gated) live-smoke verdict

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md`.
2. Read §1 pre-flight in order — **especially #8** (find the mock-LM double so the CI grade is `$0`) and #6 (confirm `render_role_questions` is the clean path).
3. Re-read §2–§7. Post plan-review per §3.

---

## 9. References

- Spec: `docs/specs/SPEC_STANDALONE_CORE_VALIDATION.md` (the demarcation §0, audit §2, design §3, sequencing §4)
- Subprocess proof precedent: `tests/test_pack_layer2c.py:143-243`
- Memory: `generic-ce-demarcation`, `healthcare-realm-as-pack`, `git-commit-pathspec-dirty-index`

---

## Hardness

- [x] **Routine cycle** — monitor inline critique at close. Non-frozen (a new pack + a test; no core council edit). *But it's the demonstrable-CE proof* — the executor must make A-STANDALONE-1 (0 leakage) + A-STANDALONE-4 (healthcare unloaded) genuinely airtight (not a vacuous pass). If the executor ends up needing ANY core change to make a non-clinical pack run, escalate — that's an undiscovered coupling and a finding, not a quiet edit.
