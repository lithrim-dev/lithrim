# Driver — `bench-salvage` phase `GROUND-FLOOR-1b`: the honest live grounding-flip

> **Generated by the monitor 2026-06-09 — the paid live verdict-flip SPLIT from GROUND-FLOOR-1's D5 (the offline core shipped, `8c45ffc..1d5a4a5`). Deferred-not-dropped: this is that "not-dropped" made into a real artifact.**
>
> **Bundle ID:** `bench-salvage-phaseGROUND-FLOOR-1b-honest-live-flip-driver`
> **Version:** v1
> **Status:** 🅿️ **PARKED** — written + runnable, but do NOT run until the activation gate clears.

---

## 🅿️ PARKED — activation gate

**Activate ONLY when a specific pitch/demo needs the *visceral live* grounding verdict-flip** — "watch the deterministic floor correct a confidently-wrong judge council, live, BLOCK→PASS." Until then it stays parked, because:

- **The mechanism is already proven without it.** GROUND-FLOOR-1's `record_presence` floor flips BLOCK→PASS **offline 16/16** (fresh-critic `a4e8ba1418e7cfd49` verified non-vacuous), and run `8a41ef3e` shows a **live** record/KB grounding suppression (BLOCK→WARN). That is the Paper-2 Faithfulness keystone, in hand.
- **The naive version is a documented likely-loss.** S-BS-74 already ran an engineered clean-negative live and the council **unanimously approved it** (0 findings → no FP → no flip; run `6c431bf2`). [[live-overfire-context-primed]] explains why: the over-fire is *context-primed*, so a spotless case lacks the trigger.

So running it now most likely yields an honest loss + ~$0.01 spend with **near-zero marginal proof** over what's already attested. Worth it only when the visceral *live* flip is the literal headline of a pitch and the offline-twin + `8a41ef3e` won't carry it.

**Monitor's standing recommendation: stay parked until a pitch demands it.** The user can override and activate at any time.

---

## KICKOFF (paste when activated)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase GROUND-FLOOR-1b
(the HONEST live grounding-flip — design + run a live case where the record-grounding
floor corrects a confidently-wrong council BLOCK->PASS; honest-Δ).

This cycle was PARKED. Confirm with the monitor that the activation gate has cleared
(a specific pitch needs the visceral live flip) before doing anything.

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseGROUND-FLOOR-1b_honest-live-flip_driver.md (this)
  3. memory: live-overfire-context-primed  (WHY the naive version loses)
  4. .devloop/state/STREAM_bench-salvage.md  (S-BS-74 the loss, S-BS-100 the re-scope, S-BS-70 the finale)

The DESIGN is the hard part (§2). Post plan-review; resolve D-A (which of the 3 case
designs). Do NOT spend a paid run until the monitor confirms the design + says "go".

Bundle ID: bench-salvage-phaseGROUND-FLOOR-1b-honest-live-flip-driver
```

---

## 0. Why this is a DESIGN task, not a re-run

The split D5 was naively scoped as "provoke a `FABRICATED_HISTORY` over-fire on the clean twin (`clean_negative_aaecd73c3bcf`), then `ground()` flips it BLOCK→PASS." We now KNOW that most likely won't fire — S-BS-74 ran the exact pattern and the council unanimously approved (0 findings). So the real deliverable is **DESIGN an honest live case** that genuinely demonstrates the flip, then run it once. If no admissible design flips live, that is an honest outcome (option c), not a failure to engineer harder.

## 1. Pre-flight reading
1. memory `live-overfire-context-primed` — the over-fire is context-primed; the naive case loses.
2. `docs/research/PROOF_bench-salvage_S-BS-74_2026-06-08.md` — the documented honest-loss + the offline mechanism that DOES hold.
3. `8a41ef3e` evidence (`docs/research/RUN_uap5a_a8_live_2026-06-06.json`) — the live suppression BLOCK→WARN (the partial live flip already in hand).
4. `lithrim_bench/harness/grounding.py` — `RecordPresence` + `ground()` (the offline-proven floor; do NOT modify).
5. STREAM seams S-BS-70 (the visceral finale), S-BS-100 (re-scopes it → this driver), S-BS-74 (the loss).

## 2. Deliverable — design + run ONE (the load-bearing decision D-A)

Design an honest live case where the floor flips a **real, live** council verdict. Pick ONE in plan-review; the monitor confirms before any paid run:

- **(a) Near-miss surface-mismatch.** The artifact asserts the same fact as the patient record but in a DIFFERENT surface form (brand vs generic, abbreviation, a SNOMED FSN vs a synonym). The council mis-reads it as a fabrication/discrepancy and BLOCKs, but it grounds clean by record/code → the floor flips it. Most natural "live FP the floor corrects"; the design risk is making the surface-mismatch genuinely fool the *live* council (not a strawman).
- **(b) Held-block WARN→PASS.** A multi-finding case the council BLOCKs; the floor suppresses the ONE grounded false finding and the residual is only WARN/PASS — `8a41ef3e`'s BLOCK→WARN pushed to PASS. Lower design risk, less visceral.
- **(c) Accept the offline twin + `8a41ef3e`.** Conclude the single-FP live flip is not naturally provocable (the honest finding) and let the demo be the offline 16/16 + the live BLOCK→WARN suppression. A legitimate close — no further paid run.

## 3. Honest-Δ (NON-NEGOTIABLE)
A no-flip is a **documented PASS**, not a manufactured win. Do NOT engineer a case so contrived it isn't admissible (lint-green by construction or it does not ship), and do NOT dress a loss as a win. If (a)/(b) don't flip live, the outcome IS (c), honestly recorded. [[proof-capsule-convention]]: a narrated video ONLY if it genuinely flips.

## 4. Scope guardrails — NOT in scope
- Touching the offline floor (`RecordPresence`/`ground()`/`_CONTRACT_EXECUTORS`) — GROUND-FLOOR-1 is closed + proven; this is live-attestation only.
- The FROZEN council seam (`_apply_consensus`/`judges_dspy`/`judge_metric`/agent seeds/`taxonomy_snapshot`).
- Widening the ontology or the gradeable flag set; any non-admissible "demo" case.
- More than ONE paid live run without re-authorization.

## 5. Acceptance
- **A1.** A designed admissible case (a or b) + ONE user-authorized live run → EITHER a genuine live BLOCK→PASS (or BLOCK→WARN→PASS) flip (→ proof capsule + video) OR a documented honest loss (→ outcome (c), capsule without video). **A manufactured win = FAIL.**
- **A2 (if c).** GROUND-FLOOR-1b closes "mechanism proven; the visceral single-FP live flip is not naturally reproducible" — the offline 16/16 + `8a41ef3e` stand as the demo; no further spend.

## 6. Hardness
- [x] **User-authorized paid run** (~$0.01; the user fires it per standing prefs; drive via the BFF API, not the confirm-gated UI [[browser-mcp-confirm-blocks-renderer]]).
- [ ] Not a code/ontology/verdict-floor change → **routine critique at close**, NOT a fresh-critic HARD-GATE (this attests an already-proven floor live; the offline floor was the HARD-GATE in GROUND-FLOOR-1). The **DESIGN (§2, D-A) is the load-bearing part**; the monitor confirms it before the paid run.

## 7. References
- Authority: GROUND-FLOOR-1 driver + `docs/specs/SPEC_GROUNDING_TOOL_LAYER.md` (Phase 1 done).
- Paper tie: the 2026-06-02 semantic-moat amendment in `docs/PAPER_OUTLINE.md` (the record-grounded floor = Paper-2 Faithfulness keystone; the demo is the channel, not the paper).
- Memory: [[live-overfire-context-primed]] · [[proof-capsule-convention]] · [[grounding-floor-is-the-moat-next]] · [[browser-mcp-confirm-blocks-renderer]].
