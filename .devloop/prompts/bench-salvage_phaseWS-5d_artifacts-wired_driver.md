# Driver — `bench-salvage` phase `WS-5d`: artifacts pane wired (judge-council + config/ontology + corpus) + PUT /v1/ontology + S-BS-19

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-5d-artifacts-wired-driver`
> **Version:** v1
> **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (per MONITOR §Phase 1a — shell + BFF re-grepped at HEAD `e960234`, all citations CONFIRMED, 0 drift)
> **Hardness:** HARD-GATE (SPEC §8: every shell phase is HARD-GATE-class; this phase lands the deferred `PUT /v1/ontology` **write** surface on the locked v1 BFF API — §10:144 — a contract-touching change). Fresh-critic close recommended.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-5d
(artifacts pane wired — judge-council + config/ontology tabs to the BFF +
corpus/flywheel view + the deferred PUT /v1/ontology write surface + S-BS-19
scripted-host wiring + datapoint prop-convention lock).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                          (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-5d_artifacts-wired_driver.md
                                                            (this doc)
  3. docs/specs/SPEC_PRODUCT_SHELL.md  §2 / §3 / §5 / §5b / §6 / §8 / §10
  4. apps/bff/app.py  +  apps/shell/src/{artifact,bff.js,panes,data}.jsx
  5. apps/shell/src/genui/{registry.js,FlagEditor,ContractBuilder,KbPicker,VerdictCard,CalibrationChart}.jsx
  6. apps/shell/src/journey/JourneyApp.jsx  (the scripted host that does NOT yet mount tool-parts)
  7. lithrim_bench/harness/corpus.py  +  scripts/run_eval.py  +  lithrim_bench/harness/config.py
  8. .devloop/state/STREAM_bench-salvage.md  (current state; S-BS-19, S-BS-20, and the OWED :5180 smoke)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go". Resolve the 5 plan-review decisions in §3 first.

Bundle ID: bench-salvage-phaseWS-5d-artifacts-wired-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-5d_artifacts-wired_driver.md
```

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review:

1. `docs/specs/SPEC_PRODUCT_SHELL.md` — the design-of-record. Key sections:
   - §2 (`:18-26`) the 3-pane model + §3 (`:39-52`) the **journey → primitive mapping** (the artifact tabs in this phase ARE the right-hand column of that table: judge-council view, ontology-config editor, corpus/flywheel view)
   - §5 (`:65-85`) the React↔BFF bridge; §5b (`:87-89`) the gen-UI `tool-<name>` protocol (the input widgets this phase finally mounts)
   - §6 (`:95-100`) the four shell contracts — §6.3 artifacts-pane `{id, type, payload}` is what the new corpus tab extends
   - §8 (`:125-134`) the **WS-5d row** (`:131`: "Artifacts pane wired … wire the already-built judge-council view + config/ontology editor … to the BFF + add the corpus/flywheel view")
   - §10 (`:142-146`) the **BFF API surface** — `:144`: `PUT`/write-ontology was **DEFERRED to WS-5c/d** (this is the phase that wires an ontology editor). The locked v1 read surface (`POST /v1/run-eval`, `GET /v1/corpus`, `GET /v1/ontology`, `/health`) is the contract this phase ADDS a `PUT` to — additively.
2. `apps/bff/app.py` — the BFF v1 surface. `run_eval_endpoint:94` (the per-case record — the judge-council data source), `corpus_endpoint:119` (`GET /v1/corpus` already live), `ontology_endpoint:125` (`GET /v1/ontology`, read-only) + the **PUT-deferred docstring `:15-17`** (the seam this phase fills). The clobber-safety constraint lives here.
3. `apps/shell/src/artifact.jsx` — the three artifact tabs. `ReportTab:22` (the **WS-5-BFF wiring template** — real BFF data via props), `JudgeTab:130` (**mock** — renders `JUDGES`), `ConfigTab:167` (**mock** — renders `CONFIG_YAML`), `ArtifactPane:197` + the tab strip `:220-223` (`report`/`judges`/`config` — the corpus tab adds a 4th).
4. `apps/shell/src/bff.js` — the bridge client: `runEval:23` (POST template), `getCorpus:26`, `getOntology:27` (GET). This phase adds a `putOntology` writer + (per plan-review) a judges fetch.
5. `apps/shell/src/data.jsx` — the mock the tabs replace: `JUDGES:35` (JudgeTab source), `CONFIG_YAML:50` (ConfigTab source).
6. `apps/shell/src/genui/registry.js` — `renderTool(part, handlers):56` (passes `part.output` as props + threads `handlers` like `onResult` — the host contract for S-BS-19).
7. `apps/shell/src/genui/FlagEditor.jsx` — `FlagEditor:40` reads `getOntology:49` (GET), `apply:86-98` returns via `onResult` only (**no PUT yet** — this phase wires persistence). `ContractBuilder.jsx onResult:34/:54`, `KbPicker.jsx onResult:27/:42` — the other two input widgets, mounted by no host today.
8. `apps/shell/src/genui/VerdictCard.jsx:22` (`{ data }`) vs `CalibrationChart.jsx:10` (`{ points, ece, brier }`) — the **inconsistent datapoint `part.output` prop convention** (fresh-critic Ambiguity-2 / S-BS-19) this phase LOCKS to one shape.
9. `apps/shell/src/panes.jsx` — `CenterPane:69` mounts the 2 datapoint cards via `renderTool` (`:113`, `:122`) but mounts **no input tool-parts** — the S-BS-19 host site (Shell mode). `apps/shell/src/journey/JourneyApp.jsx:26` (`phase` state machine `:27`; `Center`/`Artifact` per phase `:75-78`) — the journey host (does not mount tool-parts either; grep-confirmed).
10. `lithrim_bench/harness/corpus.py` — `read_corpus:104` + `project:64` / `build_corpus:90` (the `corpus-row/1` shape the corpus tab renders). `scripts/run_eval.py run():101` (the committed-JSON ontology send `~:118`) — the PUT writes the same on-disk JSON this reads. `lithrim_bench/harness/config.py Agent.ontology_abspath:70` (the PUT target path resolution).
11. `.devloop/state/STREAM_bench-salvage.md` — first-move §1 (WS-5d row), S-BS-19 (`:85`), S-BS-20 (`:86`), and the **OWED WS-5c `:5180` visual-parity smoke** now standing practice for every shell phase.

> **Citation discipline:** every file:line above was re-grepped 2026-06-01 at HEAD `e960234` (the shell + BFF were built fresh in WS-5-BFF/WS-5c, so no inter-cycle drift; 0 DRIFTED, 0 MISSING). If you spot drift on read, halt and surface it (per EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

### D0 — BFF: judge-council data source for the JudgeTab (SPEC §3 / §8:131)

1. **`apps/bff/app.py`** — surface the **real** judge-council data the JudgeTab needs (per-judge roles / votes / confidences from the council run). Per plan-review decision #1, either (a) **fold** the per-judge breakdown into the existing `/v1/run-eval` response (`run_eval_endpoint:94` — the record already carries per-judge data from the live/replay grade; the WS-5-BFF "ReportTab-over-/v1/run-eval" pattern extended), or (b) a derived **`GET /v1/judges`** reading the eval-profile `council_config`. Additive to the locked v1 surface; default replay path stays `$0`.

### D1 — BFF: the deferred `PUT /v1/ontology` write surface (SPEC §10:144)

2. **`apps/bff/app.py`** — add `PUT /v1/ontology` (the §10:144 deferred write). **Clobber-safety is load-bearing (plan-review #2):** the committed `data/ontology/clinical_v1.json` seed is the source-of-truth and MUST NOT be silently overwritten — write to an agent-scoped working copy / out-dir (or a copy-on-write path), not the committed seed. **Validate the write** before it lands: round-trip through the existing ontology-load path (`Ontology.from_dict` / `load_ontology`) and respect the CLAUDE.md core invariant + the S-BS-10/S-BS-12 snapshot lint (a `gradeable` flag outside `taxonomy/taxonomy_snapshot.json` is REJECTED, loudly) — **import-only** use of `lithrim_bench`/`scripts.seed_ontology`, do not edit them. Replace the PUT-deferred docstring `:15-17`.
3. **`apps/shell/src/bff.js`** — add `putOntology(agent, ontology)` (`PUT`, JSON body) next to `getOntology:27`.

### D2 — Shell: wire the JudgeTab + ConfigTab off mock data (SPEC §8:131)

4. **`apps/shell/src/artifact.jsx`** — `JudgeTab:130`: render the real judge-council data (from D0) instead of `data.jsx JUDGES:35`; loading/empty/error states like `ReportTab:22`. `ConfigTab:167`: render the real ontology config from `GET /v1/ontology` (`getOntology:27`) instead of `data.jsx CONFIG_YAML:50` (flags/tiers/owners/questions/`verification_contracts` + `severity_map` — the §3 "ontology config editor" view; read via GET, edits go through the FlagEditor/PUT path in D4, not a raw textarea). Thread the BFF data in via `ArtifactPane:197` props (the `ReportTab` prop pattern).

### D3 — Shell: corpus / flywheel artifact view (SPEC §3 / §8:131)

5. **`apps/shell/src/artifact.jsx`** — add a 4th artifact tab `corpus` to `ArtifactPane:197` (`titles:198-202` + the tab strip `:220-223`) rendering `getCorpus:26` (`GET /v1/corpus` → `corpus.read_corpus:104` rows, `corpus-row/1` shape: `case_id` / `action` (suppress|floor) / `flag_code` / `verdict_before→verdict_after` / `contract` / `owner_roles` / `rollout_ref`). Empty-state handled (clinical_v1 is suppress-only / may be empty — render "no corrections yet", not a crash).

### D4 — Shell: FlagEditor persistence (wire `onResult` → `PUT /v1/ontology`)

6. **`apps/shell/src/genui/FlagEditor.jsx`** — `apply:86-98` currently returns via `onResult` only; wire an explicit persist action through `putOntology` (D1) so the editor becomes **read-write** (was GET-only in WS-5c). Keep `onResult` for the in-conversation/config-plane return (D5); persistence is the new, distinct path. Surface the PUT's accept/reject (the validation in D1) in the widget footer.

### D5 — S-BS-19: scripted-host wiring + datapoint prop-convention lock

7. **`apps/shell/src/panes.jsx`** (and/or `apps/shell/src/journey/*` per plan-review #3) — wire the scripted host to emit **input** `tool-parts` that mount `FlagEditor`/`ContractBuilder`/`KbPicker` (today only the 2 datapoint cards mount, `panes.jsx:113/:122`); thread each widget's `onResult` into config-plane / UI state (the §3 "the conversation writes the config plane" loop). `renderTool(part, handlers):56` already passes handlers — supply `onResult`.
8. **`apps/shell/src/genui/VerdictCard.jsx:22` + `CalibrationChart.jsx:10`** — **LOCK the datapoint `part.output` prop convention** to ONE shape (fresh-critic Ambiguity-2): pick `{data}` vs spread and make BOTH cards conform; update `panes.jsx` `renderTool` call sites + the registry test. Document the locked shape in `registry.js`.

### D6 — tests + docs

9. **`apps/bff/`** — extend `tests/test_ws5_bff.py` (the existing Python BFF suite): `PUT /v1/ontology` accepts a valid write to the working copy + **rejects** a snapshot-violating / malformed write (the clobber-safety + validation gate) + the judge-council surface (D0).
10. **`apps/shell/src/*.test.jsx`** — React binding tests: JudgeTab + ConfigTab render real BFF data (mock fetch); the corpus tab renders `corpus-row/1` rows + empty-state; the FlagEditor persist path calls `putOntology`; the locked datapoint prop shape (D5). (jsdom layout limits per S-BS-20 still hold — assert via default/value paths.)
11. **`apps/shell/README.md`** — status update (artifacts wired; corpus view + PUT-ontology landed; WS-5e next).

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with Understanding / file-by-file changes / test plan / risks / proposed deviations, and **resolve these 5 decisions** before "go":

1. **Judge-council data source (D0)** — fold the per-judge breakdown into the existing `/v1/run-eval` response (no new endpoint; reuses the live/replay record) **vs** a derived `GET /v1/judges` off the eval-profile `council_config`. (Note the run record carries per-judge votes from the grade; the configured council comes from `council_config`. State which the JudgeTab shows: realized votes vs configured roster.)
2. **`PUT /v1/ontology` clobber-safety + validation (D1)** — write target = an agent-scoped working copy / out-dir (rec — **never** overwrite the committed `clinical_v1.json` seed) vs in-place. Validation depth: JSON-schema round-trip only, vs also the S-BS-10/S-BS-12 snapshot lint + core-invariant check (rec — reject a `gradeable` flag outside the snapshot, loudly; import-only use of `lithrim_bench`).
3. **S-BS-19 host placement (D5)** — mount input tool-parts in the Shell-mode `CenterPane` (panes.jsx) vs the journey acts (`journey/*`) vs both; how `onResult` threads into config-plane state (local state vs a Zustand store — SPEC §4 names Zustand for pane/journey UI state).
4. **Datapoint `part.output` prop convention (D5)** — lock `{data}` (VerdictCard's shape) vs spread (`{points, ece, brier}`, CalibrationChart's shape) vs a new `{output}` wrapper; pick ONE and conform both cards + the registry.
5. **Corpus view placement (D3)** — a 4th artifact tab (rec — matches §3 "rendered as artifact in pane 3") vs a center datapoint card. Plus: confirm whether a live `/v1/run-eval` call is needed to populate a non-empty corpus for the demo, or the committed `out/ws4a/corpus.ndjson` suffices (replay/$0 default holds).

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

- **Tauri / sidecar packaging / installers / VPC / offline-license** — WS-5e. The PUT lands in dev over localhost HTTP; packaging is the next phase.
- **The deferred Tauri sidecar** (from WS-5-BFF plan-review #4) — WS-5e.
- **Radix Select/Slider popover/drag E2E** (S-BS-20) — WS-5e (Playwright/E2E; jsdom can't).
- **The eval-pack loop + LOCKED calibration gate + `/eval-runs/compare`** — WS-4b (cross-repo). The corpus view READS the corpus; it does not preregister or lock a gate.
- **LLM-driven conversation** — scripted-default holds (§5c); no LLM host this phase.
- **Backend `:8002` config injection / any `../lithrim-backend` edit** (WS-2, HARD-GATE-paused).
- **Rewriting the bespoke chrome CSS into Tailwind utilities** — SPEC §4 incremental adoption; `styles.css` / `journey.css` stay (the WS-5c invariant — do not rewrite them).
- **`lithrim_bench/harness/` edits** — the BFF + widgets COMPOSE over the harness (import-only); do not edit `corpus.py`/`config.py`/`run_eval.py`/`seed_ontology.py`. If the PUT genuinely needs a harness change, halt and surface it (likely a sign the working-copy path should live in the BFF, not the harness).
- Drive-by formatting / dependency bumps / "while I was here" edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** `JudgeTab` renders the **real** judge-council data from the BFF (D0); `ConfigTab` renders the **real** ontology config from `GET /v1/ontology` — neither reads `data.jsx JUDGES`/`CONFIG_YAML` for its live content. Verify: `npm test` (tab binding tests) + grep `artifact.jsx` for the removed mock imports on the wired paths.
- **A2.** A `corpus` artifact tab renders `GET /v1/corpus` rows in the `corpus-row/1` shape; the empty corpus renders a clean empty-state (no crash). Verify: `npm test` (corpus tab test, populated + empty).
- **A3.** `PUT /v1/ontology` lands additively on the locked v1 surface, writes to a **non-committed** working copy (the committed `clinical_v1.json` is byte-unchanged after a PUT), **validates** the write (a snapshot-violating / malformed ontology is rejected with a 4xx), and the FlagEditor persist path round-trips (a PUT then `GET` reflects the edit). Verify: `pytest apps/bff` (PUT accept + reject + no-clobber) + `npm test` (FlagEditor persist).
- **A4.** S-BS-19 closed: a scripted host mounts the 3 input tool-parts (FlagEditor/ContractBuilder/KbPicker); each widget's `onResult` threads into config-plane/UI state; the datapoint `part.output` prop convention is locked to ONE shape and both `VerdictCard` + `CalibrationChart` conform. Verify: `npm test` (host-mount + prop-shape tests) + the disposition recorded in the session log.
- **A5.** `npm run build` clean + `npm test` green + `pytest apps/bff` green (re-run; record commands). The shell's React test count grows (new bindings); the BFF suite covers the PUT.
- **A6.** Diff confined to `apps/shell/` + `apps/bff/`; **NO** `lithrim_bench/`, `scripts/`, or `../lithrim-backend` edits; Python default install unchanged (BFF deps stay in the `[bff]` extra; no new default dep). Verify: `git diff --stat` + `git status` + `pip install -e .` diff check.
- **A7.** The **`:5180` visual-parity smoke** (the standing per-shell-phase human-eyeball gate, OWED since WS-5c): light/dark × Shell/Journey renders unchanged AND the 4 wired artifact tabs (report/judges/config/corpus) render correctly. **User-run under no-autostart** (`cd apps/shell && npm run dev` → `:5180`); recorded as a verification item, not auto-PASSed.

### Diagnostic stats (NOT acceptance gates)

- Corpus size + suppress/floor action split rendered in the corpus tab — reported; NOT a gate (clinical_v1 is suppress-only / may be empty per WS-4a).
- Judge-council realized-votes vs configured-roster shape (whichever decision #1 lands) — reported; NOT a gate.

---

## 6. Commit structure + message templates

Atomic commits per logical unit. Suggested order:

1. **`feat(bff): judge-council data surface for the JudgeTab`** (D0)
2. **`feat(bff): PUT /v1/ontology write surface (clobber-safe + validated)`** (D1)
3. **`feat(shell): wire JudgeTab + ConfigTab off mock to the BFF`** (D2)
4. **`feat(shell): corpus / flywheel artifact view (GET /v1/corpus)`** (D3)
5. **`feat(shell): FlagEditor persistence via PUT /v1/ontology`** (D4)
6. **`feat(shell): mount input tool-parts + lock datapoint prop convention (S-BS-19)`** (D5)
7. **`test(shell,bff): tab bindings + PUT round-trip + prop-shape`** (D6)
8. **`docs(shell): README status — artifacts wired + PUT-ontology landed`** (D6)

Each commit body should end with:

```
Executed per bench-salvage-phaseWS-5d-artifacts-wired-driver by session-2026-06-01-N.
See .devloop/sessions/session-bench-salvage-phaseWS-5d-2026-06-01.json
```

(Add the user's Co-Authored-By trailer if used.)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance criteria PASS (per §5)
- [ ] All commits exist; working tree clean
- [ ] `npm run build` clean + `npm test` green + `pytest apps/bff` green (re-run; record commands used)
- [ ] No file in the diff outside `apps/shell/` + `apps/bff/` (scope held); no `lithrim_bench/` / `scripts/` / backend edits; no new Python default deps
- [ ] The committed `data/ontology/clinical_v1.json` is byte-unchanged after a PUT (clobber-safety verified)
- [ ] No services were autostarted (the BFF is needed for live A3/A7 smoke; tests mock fetch / use TestClient) (user preference)
- [ ] The `:5180` visual-parity smoke run by the user (A7) — light/dark × Shell/Journey + the 4 tabs (record the outcome; do NOT auto-PASS)
- [ ] No pushes / publishes / tags (user preference)
- [ ] S-BS-19 closure (+ S-BS-20 still-open → WS-5e) recorded in the session log
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order (SPEC §3 journey→primitive mapping + §10:144 PUT-deferral are the load-bearing context; `apps/bff/app.py` is the contract surface this phase extends).
3. Re-read §2–§7 of this driver.
4. Post plan-review per §3 — resolve the 5 decisions (esp. PUT clobber-safety + the prop-convention lock) before "go".

---

## 9. References

- Spec: `docs/specs/SPEC_PRODUCT_SHELL.md` §3 (`:39-52` journey→primitive mapping) / §5 (`:65-85` bridge) / §5b (`:87-89` gen-UI protocol) / §6 (`:95-100` contracts) / §8 (`:131` WS-5d row) / §10 (`:144` PUT-deferred → now landed)
- Service topology: `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (strangler-fig; BFF targets the harness — the PUT writes a working copy, not Mongo)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (first-move §1; S-BS-19 `:85`; S-BS-20 `:86`; the OWED `:5180` smoke)
- Prior cycle driver: `.devloop/prompts/bench-salvage_phaseWS-5c_generative-ui-foundation_driver.md`
- Prior cycle session log: `.devloop/sessions/session-bench-salvage-phaseWS-5c-2026-06-01.json`
- Prior cycle critique: `.devloop/sessions/critique-bench-salvage-phaseWS-5c-2026-06-01.md` (Ambiguity-2 prop-convention lock; the stray-`}` lesson → the `:5180` smoke is load-bearing)
- Handoff: `.devloop/sessions/HANDOFF_bench-salvage_phaseWS-5d_kickoff_2026-06-01.md`
- BFF API of record: SPEC §10:144 (ratified WS-5-BFF; this phase adds the `PUT`)

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — fresh-critic session recommended; trigger via `/devloop-critique bench-salvage WS-5d` (or a fresh-critic kickoff). User MAY elect inline (WS-5/WS-5b precedent — UI wiring, close-time decision). The contract-touching reason: this lands `PUT /v1/ontology` (a **write** on the locked v1 BFF API, §10:144) — the WS-5-BFF fresh-critic ratified the read surface; the write needs its own pass. The WS-5c lesson with teeth: the `:5180` visual-parity smoke (A7) is the one gate build-green + unit-tests-green cannot cover — do not skip it.
