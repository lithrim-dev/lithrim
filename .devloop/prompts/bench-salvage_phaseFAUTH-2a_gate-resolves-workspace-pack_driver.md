# Driver — `bench-salvage` phase `FAUTH-2a`: the gate must resolve the ACTIVE WORKSPACE's pack, not the BFF process pack (BLOCKING fix)

> **Generated by the monitor after the FAUTH-2 cold critic returned BLOCKING DRIFT
> (S-BS-FAUTH2-2, live-proven). This is the fix-cycle. The executor pastes the KICKOFF
> block into a fresh session, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseFAUTH-2a-gate-resolves-workspace-pack-driver`
> **Version:** v1
> **Authored:** 2026-06-20
> **Parent:** FAUTH-2 HEAD `48162ad` (the 5 FAUTH-2 commits stay; this ADDS the fix on top)
> **Hardness:** **HARD GATE** — touches the moat-adjacent `grounding.py` accessors (owner-blessed,
> additive) + the audited write path. Fresh cold critic at close.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase FAUTH-2a
(fix the FAUTH-2 BLOCKING bug: the author-time gate + GET /v1/grounding-contract/types resolve the
BFF PROCESS pack (_core) instead of the ACTIVE WORKSPACE's grade pack (healthcare), so they
false-reject the clinical floors record_presence/snomed_subsumption/dosage_grounding on a healthcare
workspace — live-proven by the cold critic with a real HTTP 422 on clinverdict_clean).

You are rooted in the repo: /Users/aregee/Workspace/github.com/lithrim-bench

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                   (your role)
  2. .devloop/prompts/bench-salvage_phaseFAUTH-2a_gate-resolves-workspace-pack_driver.md  (THIS doc)
  3. .devloop/sessions/critique-bench-salvage-phaseFAUTH-2-2026-06-20.md  (the BLOCKING finding + the failing test the critic specified + the live 422 repro)
  4. lithrim_bench/harness/grounding.py                              (the accessors suppress_executors()/floor_executors() 377-395 — you ADD an optional pack= arg here, OWNER-BLESSED; _pack_registries(pack) 356-368 already takes a pack; _active_pack() 371-374 = the process env)
  5. lithrim_bench/harness/pack.py                                   (active_pack() 83-85 = os.environ["LITHRIM_BENCH_PACK"] or "_core"; workspace.get_active_workspace().pack is the WORKSPACE pack)
  6. apps/bff/app.py                                                 (the gate in _put_grounding_contract ~2408-2429; the endpoint grounding_contract_types_endpoint 1687-1700 + its WRONG docstring 1696; the PRECEDENT pack_lenses endpoint 1376 = pack_mod.pack_lenses(workspace.get_active_workspace().pack); the subprocess pack bind _grade_via_subprocess ~559-566)
  7. runtime/council/signals.py                                      (the withstands-gate's no-arg suppress_executors() call — it must stay BEHAVIOR-IDENTICAL; this is the moat read)
  8. tests/bff/test_grounding_contract_gate.py                       (the 5 existing FAUTH-2 gate tests — keep green; ADD the critic's failing test)
  9. .devloop/state/STREAM_bench-salvage.md                          (FAUTH-2 + seam S-BS-FAUTH2-2)

Canonical env (export before running anything):
  PYENV_VERSION=debuglithrim LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare
  (shell tests: cd apps/shell && npx vitest run)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Resolve R1 (agent↔workspace pack), R2 (the no-arg grade path stays behavior-identical), R3 (A-LIVE
for real this time). Do not write code until I say "go".

Bundle ID: bench-salvage-phaseFAUTH-2a-gate-resolves-workspace-pack-driver
Driver doc: .devloop/prompts/bench-salvage_phaseFAUTH-2a_gate-resolves-workspace-pack_driver.md
```

---

## 0. What this cycle is

**The bug (CONFIRMED, live-proven — S-BS-FAUTH2-2):** the FAUTH-2 author-time gate
(`_put_grounding_contract`, `app.py:~2424`) and the `GET /v1/grounding-contract/types` endpoint
(`app.py:1700`) compute the registered executor set from `grounding.suppress_executors()` /
`floor_executors()`, which internally read `_active_pack()` = the **BFF process** env
`LITHRIM_BENCH_PACK` (live = `_core`, 4 keys). But a non-`_core` workspace grades in a **subprocess**
bound to `LITHRIM_BENCH_PACK=ws.pack` (`app.py:~559-566`). On the live `clinverdict_clean` workspace
(`pack: healthcare`, 7 keys) the gate consults `_core` and **422-rejects `record_presence` /
`snomed_subsumption` / `dosage_grounding`** — the clinical floors FAUTH exists to author — and the
inline `ContractBuilder` never offers them. The critic captured the real 422.

**The fix (owner-chosen: the clean grounding.py API):**
1. **`grounding.py` (additive, owner-blessed):** give `suppress_executors()` / `floor_executors()` (and
   `floor_contract_types()` if it's reached) an optional `pack: str | None = None` param defaulting to
   `_active_pack()`. The bodies already delegate to `_pack_registries(pack)` (which takes a pack arg) —
   so the change is `_pack_registries(pack or _active_pack())`. **The no-arg call is byte-behavior-identical**
   (it still defaults to `_active_pack()`), so the withstands-gate (`signals.py`) and `ground()` (which
   call no-arg, at grade time, in the correct-pack subprocess) are UNAFFECTED.
2. **`app.py` (the two call sites):** resolve `ws_pack = workspace.get_active_workspace().pack` (the
   in-repo precedent at `app.py:1376`) and pass it: `suppress_executors(ws_pack)` / `floor_executors(ws_pack)`.
   - the endpoint returns `{"contract_types": sorted(registered), "pack": ws_pack}`.
   - the gate in `_put_grounding_contract` admits against the workspace pack's set.
   - **fix the WRONG docstring** at `app.py:1696` ("the process-global active pack ... exactly what
     ground() reads at grade time" — false for any non-`_core` workspace) to state it resolves the
     active workspace's grade pack.

**What this cycle is NOT:** no new tool (tool count stays 20); no change to the consensus/withstands
MECHANISM (`_apply_consensus`, `withstands.py`, the `signals.py` withstands READ stay byte-frozen — you
only ADD an optional param to two registry ACCESSORS, the no-arg behavior unchanged); no change to
`ground()`; no FAUTH-2b oracle-marker work; no shell change beyond what the bug requires (the UI already
fetches the endpoint — fixing the endpoint fixes the offered list, so likely ZERO shell code change —
confirm). No taxonomy re-snapshot.

---

## 1. Plan-review checkpoint (non-negotiable) — risks to resolve

- **R1 — agent↔workspace pack.** The gate writes to a specific `agent`'s ontology; the pack must match
  the pack that agent will be GRADED under (the subprocess binds `ws.pack`). Confirm
  `workspace.get_active_workspace().pack` IS the right pack for the agent being written (the live
  single-active-workspace model: the agent is within the active workspace). If an agent could belong to
  a different workspace than the active one, state it + how you resolve the agent's OWN workspace pack.
  Use the `app.py:1376` `pack_lenses` precedent.
- **R2 — the grade path stays behavior-identical (the moat read).** Prove the no-arg `suppress_executors()`
  call in `signals.py` (the withstands-gate) and any no-arg call in `ground()` are byte-unchanged and
  behave identically (the optional param defaults to `_active_pack()`). The consensus/withstands MECHANISM
  must be 0-diff; only the two accessor `def` lines + their one-line bodies change. Show the `git diff` of
  `grounding.py` is EXACTLY the additive param (no logic change).
- **R3 — A-LIVE for real (the FAUTH-2 miss).** The FAUTH-2 commit titled A-LIVE "discharged" but it was
  never run against the failing topology (it used `presence_check`, a core type present in both packs).
  THIS cycle's A-LIVE MUST: on the live BFF (process pack `_core`) with the active workspace = a
  healthcare workspace, confirm `GET /v1/grounding-contract/types` returns the 7-key healthcare set
  (incl. `record_presence`) AND a `record_presence` contract PERSISTS (HTTP 200, not 422) + is audited.
  No autostart (curl /health first); if services are down, say A-LIVE is OWED honestly — do NOT title it
  discharged.

---

## 2. Deliverables

**Tests FIRST (RED):**
1. **`tests/bff/test_grounding_contract_gate.py`** — ADD the critic's
   `test_gate_resolves_the_active_workspace_pack_not_the_process_pack`: with `LITHRIM_BENCH_PACK` unset
   (process = `_core`) but the active workspace pinned to `healthcare`, assert (a) the endpoint returns
   the healthcare set incl. `record_presence`, and (b) the gate PERSISTS a `record_presence` contract on
   a healthcare flag (200, not 422). RED today, GREEN after the fix. Keep the existing 5 tests green.
   Optionally add a `grounding.py`-level unit test: `suppress_executors("healthcare")` ⊋
   `suppress_executors("_core")` while `suppress_executors()` (no-arg) == `suppress_executors(_active_pack())`.

**Then GREEN:**
2. **`lithrim_bench/harness/grounding.py`** — the optional `pack=` param on `suppress_executors()` /
   `floor_executors()` (+ `floor_contract_types()` if reached). Additive only.
3. **`apps/bff/app.py`** — resolve `workspace.get_active_workspace().pack` in the endpoint + the gate;
   pass it to the accessors; fix the docstring.

> If the shell needs NO change (the endpoint fix alone corrects the offered list), say so explicitly and
> do not touch the JSX. If a shell change IS needed, scope it minimally + add a vitest.

---

## 3. Acceptance criteria (each a test, written first where it can be)

- **A1 — the workspace-pack regression is fixed, non-vacuous.** The critic's failing test passes:
  process=`_core` + workspace=`healthcare` → endpoint includes `record_presence`; the gate persists a
  `record_presence` contract (200). RED at parent `48162ad`, GREEN at HEAD.
- **A2 — the gate still rejects prose (no regression of FAUTH-2's win).** A bogus `openevidence_judge`
  is STILL 422-rejected on a healthcare workspace, and a genuinely unregistered-for-this-pack type is
  rejected. The 5 existing gate tests stay green.
- **A3 — the grade path is byte-behavior-identical (the moat).** `git diff` of `grounding.py` is EXACTLY
  the additive `pack=` param (no logic/mechanism change); `_apply_consensus`, `withstands.py`, and the
  `signals.py` withstands read are 0-diff; the no-arg accessor behavior is unchanged (unit-asserted).
- **A4 — A-LIVE actually run** (per R3) OR honestly marked OWED (no false "discharged").
- **A5 — frozen + green bar + A-SAFE.** Tool count == 20; allowlist == `_TOOL_SPECS`; deny hook 0-diff;
  the consensus/withstands MECHANISM 0-diff; full suite + vitest + ruff clean vs the parent baseline
  (28==28 etc. — run it, don't claim it).

---

## 4. Commit structure

1. **`test(fauth): FAUTH-2a workspace-pack gate regression — red`** — the new test(s).
2. **`fix(grounding): suppress/floor executors accept an optional pack arg (FAUTH-2a)`** — `grounding.py`
   (additive; body notes the owner-blessed freeze relaxation + that the no-arg path is behavior-identical).
3. **`fix(bff): resolve the active workspace pack for the grounding-contract gate + types (FAUTH-2a)`** —
   `app.py` (gate + endpoint + docstring).

Each body ends with the standard `Executed per … / See .devloop/sessions/session-…json` + the
`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>` trailer. Pathspec-scoped commits
(`git commit -- <files>`); verify scope with `git diff 48162ad HEAD --stat`.

---

## 5. Verification checklist

- [ ] Test RED before the fix (the critic's test failed at `48162ad`).
- [ ] A1–A5 PASS.
- [ ] `grounding.py` diff is EXACTLY the additive param (no mechanism change); consensus/withstands 0-diff.
- [ ] Happy path intact: prose still 422; every type registered for the WORKSPACE pack now persists.
- [ ] A-LIVE run for real on a healthcare workspace (record_presence persists) OR honestly OWED.
- [ ] Tool count 20; allowlist == `_TOOL_SPECS`; deny hook 0-diff.
- [ ] Scope held; no foreign-WIP sweep; no push/autostart.
- [ ] Session log written (`.devloop/sessions/session-bench-salvage-phaseFAUTH-2a-2026-06-20.json`).

---

## 6. References

- The BLOCKING critique: `.devloop/sessions/critique-bench-salvage-phaseFAUTH-2-2026-06-20.md` (S-BS-FAUTH2-2 + the live 422 + the failing test)
- The precedent: `app.py:1376` `pack_lenses(workspace.get_active_workspace().pack)`
- The accessors: `grounding.py:377-395` (+ `_pack_registries(pack)` 356-368 already pack-parameterized)
- The moat read that must stay identical: `runtime/council/signals.py` (no-arg `suppress_executors()`)

---

## Hardness

- [x] **HARD GATE** — fresh cold critic at close (`/devloop-critique bench-salvage FAUTH-2a`). The critic
      re-proves: A1 fixes the live regression (record_presence persists on healthcare with process=_core),
      A2 prose still rejected, `grounding.py` is additive-only with the consensus/withstands MECHANISM
      0-diff (the owner blessed ONLY the optional-param relaxation), tool count 20, and A-LIVE was actually
      run (not falsely titled).
