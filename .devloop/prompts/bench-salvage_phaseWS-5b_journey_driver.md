# Driver — `bench-salvage` phase `WS-5b`: conversational journey layer (4-act activation; port `jp1–jp4`)

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-5b-journey-driver`
> **Version:** v1
> **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (per MONITOR §Phase 1a — every prototype + shell citation below opened this session)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-5b
(conversational journey layer — port the 4-act activation journey into apps/shell/).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                          (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-5b_journey_driver.md   (this doc)
  3. docs/specs/SPEC_PRODUCT_SHELL.md  §2.1 (the 4-phase journey) + §8 (amended FE-first; the WS-5b row)
  4. docs/design/JOURNEY_brief.md                           (the canonical 4-act brief + brand)
  5. apps/shell/src/{app,panes,artifact,cards,brand,icons}.jsx + src/{styles,journey}.css  (the WS-5 shell you extend)
  6. .devloop/state/STREAM_bench-salvage.md                 (WS-5 row, S-BS-17, open seams)

The journey PROTOTYPE source to port from lives at:
  /tmp/lithrim_design/x/lithrim-bench/project/{journeydata,journeyrail,journeyapp,jp1,jp2,jp3,jp4}.jsx
  (if /tmp is gone, re-fetch the tar.gz from api.anthropic.com/v1/design/h/pIAWkwsx2ASYYEZ-7lHzjQ)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)" — in particular
resolve the four PLAN-REVIEW DECISIONS in §3 below. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseWS-5b-journey-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-5b_journey_driver.md
```

---

## 1. Pre-flight reading (ordered)

1. `docs/specs/SPEC_PRODUCT_SHELL.md` §2.1 (the 4-phase activation journey) + §8 (amended 2026-06-01 — the **WS-5b** row; note WS-5b is a *plain-CSS port*, Tailwind stays deferred to WS-5c).
2. `docs/design/JOURNEY_brief.md` — the canonical 4-act journey (First contact → Reveal → Calibration → Own it), the hero beats, and the brand tokens. **This is the content source of truth; the prototype realizes it.**
3. The journey **prototype** (port FROM these), all at `/tmp/lithrim_design/x/lithrim-bench/project/`:
   - `journeydata.jsx` — content (ACTS, PILLARS, AGENT_TYPES, PACK, EXCHANGE, SCENARIOS, ALIGN, JUTE, SDK_LINES, PRO_FEATURES); ends in `Object.assign(window, …)`.
   - `journeyrail.jsx` — `LeftRailJ`, `TopBarJ`, `StatusBarJ`, `PhaseFoot`.
   - `journeyapp.jsx` — `JourneyApp` composition: phase state machine (1–4), `runVerify` (staggered 4-pillar reveal, `journeyapp.jsx:44-49`), `runCalib` (before→after, `:51-54`), resize (`:56-62`), keyboard ←/→ nav (`:30-39`), `ART_META` (`:6-11`). **Ends in its own `ReactDOM.createRoot(...).render(<JourneyApp/>)` — drop that on port.**
   - `jp1.jsx` — `AgentMsg` (shared by all phases — `jp1.jsx:4-17`), `Center1`, `Artifact1`.
   - `jp2.jsx` — `ScribeExchange` + `WAVE` + `Center2` + `Artifact2` (HERO: the verify reveal).
   - `jp3.jsx` — `JuteBlock` + `Center3` + `Artifact3` (HERO: calibration before/after).
   - `jp4.jsx` — `Center4` + `Artifact4`.
4. The **target** shell (port INTO this; extend, don't rewrite): `apps/shell/src/{main,app,panes,artifact,cards,brand,icons}.jsx`, `src/{styles,journey}.css`. Note `main.jsx:3` currently imports **only** `styles.css`.
5. `.devloop/state/STREAM_bench-salvage.md` — WS-5 close row, **S-BS-17** (demo-domain split), the WS-5-BFF row.

> **Citation discipline.** Every file:line above was opened by the monitor on 2026-06-01. The prototype is a **no-build `window.*`-globals** composition (script tags); the target is **Vite/ESM**. Your port's central mechanical task is converting `window.X` references into ESM `import`/`export`. If a prototype detail has drifted from what this driver says, halt and surface it.

### Pre-verified integration facts (monitor, 2026-06-01) — you can rely on these

- **All icons the journey uses already exist** in `apps/shell/src/icons.jsx` (`shield, grid, scale, wand, gauge, mic, play, pause, arrowR, book, note, key, star, lock, link, plus, refresh, copy, chevD, chevR, search, dots, sun, moon, panel, expand, minimize, close, check, layers, flag`). **No icon additions needed.**
- **All journey CSS classes already exist**: 29/30 in `apps/shell/src/journey.css` (262 lines, the prototype's verbatim CSS); the 30th, `.h-sub`, is in `styles.css:286`. **No CSS authoring needed** — but `journey.css` is **not yet imported** (see deliverable 9).
- `Mark` + `Wordmark` are exported from `apps/shell/src/brand.jsx`; `Icon` from `apps/shell/src/icons.jsx`. The journey's `window.Mark`/`window.Icon` map to these.

---

## 2. Deliverables (file-by-file)

Create the `apps/shell/src/journey/` subtree (ESM ports of the prototype) + a top-level mode switch.

1. **`apps/shell/src/journey/journeyData.js`** — port `journeydata.jsx`.
   - Export each const (`ACTS, PILLARS, AGENT_TYPES, PACK, EXCHANGE, SCENARIOS, ALIGN, JUTE, SDK_LINES, PRO_FEATURES`) as a named ESM export. Drop the trailing `Object.assign(window, …)`. Pure data — `.js`, no JSX.

2. **`apps/shell/src/journey/chrome.jsx`** — port `journeyrail.jsx` + the shared `AgentMsg`.
   - `LeftRailJ`, `TopBarJ`, `StatusBarJ`, `PhaseFoot` + `AgentMsg` (lifted out of `jp1.jsx` since `jp2/jp3/jp4` all use it via `window.AgentMsg`).
   - `import { Icon } from "../icons.jsx"`, `import { Mark } from "../brand.jsx"`, data from `./journeyData.js`.

3. **`apps/shell/src/journey/jp1.jsx`** — `Center1` + `Artifact1` (ESM; `AgentMsg` now imported from `./chrome.jsx`).

4. **`apps/shell/src/journey/jp2.jsx`** — `ScribeExchange` (+ the `WAVE` array) + `Center2` + `Artifact2`. Preserve the audio-wave play state + the per-pillar staggered-reveal rendering driven by the `revealed` prop.

5. **`apps/shell/src/journey/jp3.jsx`** — `JuteBlock` + `Center3` + `Artifact3`. Preserve the plain-English rule textarea, the Jute block, and the before/after compare table driven by `calib`.

6. **`apps/shell/src/journey/jp4.jsx`** — `Center4` + `Artifact4`.

7. **`apps/shell/src/journey/JourneyApp.jsx`** — port `journeyapp.jsx` composition.
   - The phase state machine (`phase` 1–4), `runVerify` / `runCalib` hero timers, resize handles, keyboard ←/→ nav, `ART_META`, theme.
   - Resolve `Center{n}`/`Artifact{n}` via **imports** (a small map), not `window.*`.
   - `export function JourneyApp()`. **Remove** the prototype's `ReactDOM.createRoot(...).render(...)` line.

8. **`apps/shell/src/root.jsx`** *(new)* — the **Shell↔Journey mode switch** (PLAN-REVIEW DECISION #1).
   - Holds `mode` state (`"shell" | "journey"`), renders `<App/>` or `<JourneyApp/>`, plus a minimal switch control. Default per the plan-review decision.

9. **`apps/shell/src/main.jsx`** — mount `<RootApp/>` (from `root.jsx`) instead of `<App/>`; **add `import "./journey.css";`** alongside `import "./styles.css";`.

10. **`apps/shell/README.md`** — update "Status & next": WS-5b journey landed; next = WS-5c (generative-UI + the Tailwind/shadcn foundation).

> **No tests in this cycle** (the shell has no test infra; acceptance is `vite build` + manual smoke at `:5180`, per the §8 amendment which moves the first test bar to WS-5-BFF). Do **not** stand up a test harness here — that's scoped to WS-5-BFF.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with: understanding, file-by-file changes, the smoke-test plan, risks, and any proposed deviations. **Wait for "go" before writing code.** Resolve these four decisions explicitly in the plan-review (monitor recommendations given):

1. **Shell↔Journey switch mechanism + default mode.** How does the user toggle Shell (the WS-5 eval workspace) ↔ Journey (this activation arc)? *Monitor rec:* a minimal top-level toggle in `root.jsx` (e.g. a small segmented control in the titlebar, or a dev switch), **default to Journey** for the demo. Keep it a thin dev/demo switch — the real first-run activation gating is product logic for a later phase, NOT this cycle.
2. **S-BS-17 — the demo-domain split.** The journey is **clinical/Scribe** (`acme-health`, Healthcare Scribe Pack, hypertension/lisinopril); the WS-5 shell skeleton is **customer-support** (`acme-support`, `support_transcripts.jsonl`). *Monitor rec:* port the journey **clinical as-is** (faithful) and **defer** reconciling the shell skeleton's mock content (leave S-BS-17 open, or fold a small `app.jsx`/`data.jsx` re-skin into this cycle only if the user wants them unified now). Two distinct modes with two domains is acceptable short-term.
3. **`AgentMsg` + shared-parts placement.** `AgentMsg` is defined in `jp1.jsx` but used by all four phases. *Monitor rec:* lift it into `chrome.jsx` (or a `journey/parts.jsx`).
4. **`journey/` file layout + naming** (e.g. `journeyData.js` vs `data.js`; `chrome.jsx` vs `rail.jsx`). Confirm the names in deliverable §2 or propose alternatives.

---

## 4. Scope guardrails — NOT in scope

- **NO BFF / no `run_eval` wiring / no network / no live data.** The journey runs entirely on `journeyData.js` representative content — exactly like the WS-5 shell skeleton. The real bridge + vertical is **WS-5-BFF**.
- **NO Tailwind / shadcn.** Deferred to **WS-5c** (decision 2026-06-01). This cycle is a **plain-CSS port** on the existing `styles.css` + `journey.css`.
- **NO Tauri** — WS-5-BFF / WS-5e.
- **NO generative-UI `tool-<name>`↔component protocol** (SPEC §5b) — WS-5c.
- **NO test infrastructure** — manual smoke only this cycle (tests land at WS-5-BFF acceptance).
- **NO rewriting** the existing shell chrome CSS / `styles.css`, and **NO new icons** (`icons.jsx` is already complete — confirmed).
- **NO converging** the shell skeleton's customer-support mock to clinical beyond what the switch needs (S-BS-17 — plan-review decision #2).
- Drive-by formatting / dependency bumps / "while I was here" edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1. `npm run build` (`vite build`) is clean** — no new errors; the bundle builds (the WS-5 build-verified bar). Verify: `cd apps/shell && npm run build`.
- **A2. The journey renders all four acts** at `:5180` and the hero interactions work — verify manually + record observations:
  - rail-click + keyboard ←/→ navigate acts 1↔4;
  - Act 2 **Verify** runs the staggered 4-pillar reveal → PASS verdict (8.6);
  - Act 3 **Re-run pack & compare** flips agreement `0.62 → 0.91` + the per-scenario before→after table (`SCENARIOS`).
- **A3. The Shell↔Journey switch** toggles between `<App/>` (eval workspace) and `<JourneyApp/>` (activation) with **no reload**, sharing the window chrome + theme.
- **A4. Diff is confined to `apps/shell/`** (+ this cycle's own `.devloop/` close-out). **No new npm deps** (react/react-dom + vite only). **No Tailwind/shadcn, no network code.** Verify: `git -C <root> diff --stat`.

### Diagnostic stats (NOT gates)

- Pixel/visual parity vs the `/tmp` prototype (eyeball the four acts + dark theme) — reported, not gated.

---

## 6. Commit structure + message templates

Atomic commits per logical unit. Suggested order:

1. **`feat(ws5b): journey data + chrome (ESM port of the activation journey)`** — `journey/journeyData.js` + `journey/chrome.jsx`.
2. **`feat(ws5b): the four activation acts (jp1–jp4)`** — `journey/jp1.jsx … jp4.jsx`.
3. **`feat(ws5b): JourneyApp composition + Shell↔Journey switch`** — `journey/JourneyApp.jsx` + `root.jsx` + `main.jsx` + README.

Each commit body ends with:

```
Executed per bench-salvage-phaseWS-5b-journey-driver by session-YYYY-MM-DD-N.
See .devloop/sessions/session-bench-salvage-phaseWS-5b-YYYY-MM-DD.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

(Per-repo atomic — all `lithrim-bench`. Do NOT auto-commit; stage + propose for the user's "go".)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A4 PASS (build clean; four acts render; hero interactions work; switch toggles; diff confined; no new deps)
- [ ] All commits exist; working tree clean
- [ ] `vite build` re-run clean (record the command)
- [ ] No file in the diff outside §2 deliverables (scope held — no Tailwind, no BFF, no network, no new icons/CSS rewrite)
- [ ] No services autostarted; the dev server is the user's to run
- [ ] No pushes / publishes / tags
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order (spec §2.1/§8 → JOURNEY_brief → the 7 prototype files → the WS-5 shell).
3. Re-read §2–§7 of this driver.
4. Post plan-review per §3 — resolve the four decisions. Wait for "go".

---

## 9. References

- Spec: `docs/specs/SPEC_PRODUCT_SHELL.md` §2.1 (journey) + §8 (amended FE-first; WS-5b row)
- Journey brief: `docs/design/JOURNEY_brief.md`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (WS-5 close, S-BS-17, WS-5-BFF)
- Prior cycle critique (WS-5 HARD-GATE): `.devloop/sessions/critique-bench-salvage-phaseWS-5-2026-06-01.md`
- Prototype source: `/tmp/lithrim_design/x/lithrim-bench/project/` (re-fetch from `api.anthropic.com/v1/design/h/pIAWkwsx2ASYYEZ-7lHzjQ` if gone)
- Shell README: `apps/shell/README.md`

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [x] **HARD GATE** — per SPEC §8 ("each HARD-GATE-class"). Fresh-critic close nominally required (`/devloop-critique` → spawn fresh critic per `KICKOFF_CRITIC.md`); the user may elect inline as they did for WS-5 (a UI port), but that's a close-time decision. The HARD-GATE classification stands.
