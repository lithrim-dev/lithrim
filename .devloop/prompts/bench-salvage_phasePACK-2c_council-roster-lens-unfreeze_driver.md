# Driver — `bench-salvage` phase `PACK-2c`: council ROSTER + LENS_BY_ROLE un-freeze (the decoupling endgame)

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phasePACK-2c-council-roster-lens-unfreeze-driver`
> **Version:** v1
> **Authored:** 2026-06-11
> **Last re-verified against code:** 2026-06-11 (per MONITOR §Phase 1a — recon agent + monitor spot-verification at HEAD f4655d8)

**This is the strangler-fig ENDGAME.** PACK-1b/2b pack-resolved the council's clinical
DATA (taxonomy + owners). PACK-2c un-freezes the last two domain-coupled things:
the **lens authority** (`LENS_BY_ROLE` — what each role may assert; the MOAT authority)
and the **roster identity** (which judges run). After 2c + the second-pack proof, the
core compliance council carries only the **domain-agnostic mechanism**; ALL domain
specifics live in the pack. This is what makes the OSS-core / Pro-pack split real.

**HARD GATE.** Touches the moat (`LENS_BY_ROLE` via `withstands.py`) and the FROZEN
`compliance_council.py`. Fresh-critic with `isolation: worktree` required at close.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-2c (council-roster-lens-unfreeze). HARD GATE.

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-2c_council-roster-lens-unfreeze_driver.md  (this doc)
  3. CLAUDE.md  (§"Taxonomy snapshot is the contract" + §"Core invariant")
  4. tests/_seam_freeze.py  (the council freeze guard you must extend — read it whole)
  5. .devloop/state/STREAM_bench-salvage.md  (PACK-1b/2b close history, S-BS-125, the carve-out precedent)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
This is a HARD GATE — do not write code until I say "go", and expect a fresh-critic pass at close.

Bundle ID: bench-salvage-phasePACK-2c-council-roster-lens-unfreeze-driver
Driver doc: .devloop/prompts/bench-salvage_phasePACK-2c_council-roster-lens-unfreeze_driver.md
```

---

## 1. Pre-flight reading (ordered) — all file:lines recon-verified 2026-06-11

**The two couplings:**
1. `lithrim_bench/runtime/council/judge_metric.py:110-114` — `LENS_BY_ROLE = {"risk_judge": RISK_JUDGE_LENS, "policy_judge": POLICY_JUDGE_LENS, "faithfulness_judge": FAITHFULNESS_JUDGE_LENS}` + the component constants `:54-106` (`RISK_JUDGE_LENS`/`POLICY_JUDGE_LENS`/`FAITHFULNESS_JUDGE_LENS`). **The lens half. `judge_metric.py` is NOT under the freeze guard — confirmed.**
2. `lithrim_bench/runtime/council/compliance_council.py:461-492` — the roster construction (v2 trio `:461-486` with `model=settings.AZURE_OPENAI_DEPLOYMENT_*` + capability flags + `prompt_role`; v1 legacy `:488-492` in the `else`). `CouncilModel` dataclass `:348-373`. **The roster half — the FROZEN file.**

**The moat (what reads `LENS_BY_ROLE` — do NOT change its behavior):**
3. `lithrim_bench/runtime/council/withstands.py:38,83,121,144-152` — the withstands-gate; `:144 if code and code not in lens:` is the moat scope-check. `signals.py:40,85,148` reads it too. `ab_harness.py:38,138` + `judge_optimize.py:39,251,283,285,363` (scoring/optimizer readers).

**The pack contract + accessors to mirror:**
4. `packs/healthcare/taxonomy_snapshot.json` — keys: `tiers`, `tier1_owners`, `production_judges` (`["risk_judge","policy_judge","faithfulness_judge"]` — **this IS the pack-side identity list**), `declared_but_not_running`, `structural_codes`. **NO `lenses` field — you add one.**
5. `lithrim_bench/harness/pack.py:106-119` `pack_tiers()` + `:122-134` `pack_tier1_owners()` — the accessor precedents to mirror for `pack_lenses()` + `pack_production_judges()`. `:80` `council_known_codes()` (active) + `:183-212` `council_roster()` (canonical/`DEFAULT_PACK`) — the **S-BS-125 asymmetry** you inherit (see §2-D). `:61` `active_pack()`, `:62 _manifest()`.

**The carve-out + freeze guard (the roster half must extend this):**
6. `compliance_council.py:196` (`_PACK_TIERS = __import__(...).pack_tiers()`), `:239` (`_TIER1_OWNERS = dict(__import__(...).pack_tier1_owners())`), `:446` (`_ROLE_PROMPTS_DIR = ...pack_prompts_path()`) — the 3 inline-`__import__` carve-out precedents.
7. `tests/_seam_freeze.py:152-161` `_COUNCIL_AUTHORIZED_MARKERS` + `:164-168` `_COUNCIL_REQUIRED_CARVEOUTS` + `:171-239` `assert_compliance_council_carveouts_only` / `assert_council_carveouts_only` (the difflib upper-bound + S-BS-124 per-line marker hardening `:224-233` + the revert-detection lower-bound `:235-238`). **3 carve-outs today → 4 after the roster un-freeze.**

**The second-pack proof precedents:**
8. `packs/_tiers_fixture/pack.json` + `packs/_tiers_fixture/taxonomy_snapshot.json` (SENTINEL tier sets/owners — the non-vacuity fixture pattern). `tests/test_pack_layer2b.py:213-243` (the subprocess `LITHRIM_BENCH_PACK=…` proof pattern to mirror). `tests/test_pack_layer1b.py` (equivalence + non-vacuity test shape).

**Behavior-pinning tests (must stay green; one deliberate edit):**
9. `lithrim_bench/runtime/council/tests/test_trio_dspy.py:302-305` — `assert set(LENS_BY_ROLE) == set(V2_ROLES)` + `LENS_BY_ROLE["risk_judge"] is RISK_JUDGE_LENS` (**the `is`-identity check → must become `==` once lenses are pack-resolved**). `test_consensus.py` (the consensus oracle — the determinism net). `test_judge_metric.py` (lens-scoped scoring).

> **Citation discipline:** every file:line above was recon-greped against HEAD `f4655d8` on 2026-06-11. If you spot drift on read, halt and surface it.

---

## 2. Deliverables (file-by-file)

### LENS HALF (clean, non-frozen — `judge_metric.py`)

**D1 — `packs/healthcare/taxonomy_snapshot.json`:** add a `"lenses"` object: `{role: [codes]}` carrying the CURRENT healthcare lens values (risk/policy/faithfulness → their code sets, copied verbatim from `judge_metric.py:54-106`). This is now the single source of truth for the lens authority (mirrors how `tier1_owners` works). Do NOT hand-edit beyond adding this block; if a re-snapshot would clobber it, note it (the snapshot is normally generated — flag whether `scripts/snapshot_taxonomy.py` needs a parallel update to emit `lenses`, as an OQ).

**D2 — `lithrim_bench/harness/pack.py`:** add `pack_lenses(pack=None) -> dict[str, frozenset[str]]` reading `snap["lenses"]` (mirror `pack_tier1_owners()` `:122-134` exactly — same active-pack resolution, same shape coercion to `frozenset`).

**D3 — `lithrim_bench/runtime/council/judge_metric.py`:** resolve `LENS_BY_ROLE` FROM `pack_lenses()` via the inline-`__import__` carve-out pattern (`__import__("lithrim_bench.harness.pack", fromlist=["pack_lenses"]).pack_lenses()`). The component constants (`RISK_JUDGE_LENS` etc.): **plan-review decision** — either keep them as references derived from the resolved map (`RISK_JUDGE_LENS = LENS_BY_ROLE["risk_judge"]`, preserving importers) or remove + update importers. Recommend the derived-reference form to minimize blast radius. `effective_lens()` and all readers unchanged.

**D4 — `lithrim_bench/runtime/council/tests/test_trio_dspy.py:302-305`:** change the `is`-identity assertions to `==` (value equality against `pack_lenses()`), keeping the lens⊆owners invariant intact. This is the one deliberate test edit; justify it in the REPORT.

### ROSTER HALF (the frozen `compliance_council.py` — a 4th carve-out, identity↔deployment SPLIT)

**D5 — `lithrim_bench/harness/pack.py`:** add `pack_production_judges(pack=None) -> list[str]` reading `snap["production_judges"]` (the identity list; mirror the accessors). **S-BS-125 inheritance (§2-D below) — resolve the canonical-vs-active question here.**

**D6 — `lithrim_bench/runtime/council/compliance_council.py`:** the identity↔deployment split.
- Extract the per-role DEPLOYMENT params (provider, model-source, `supports_logprobs`, `supports_response_format_json`, `prompt_role`) from `:461-492` into a **core-side** `_ROLE_DEPLOYMENT` table keyed by role name. **Deployment config stays in core — a pack must NOT carry Azure deployment ids.**
- Build the roster by iterating the **pack identity list** (`pack_production_judges()` via a NEW inline-`__import__` carve-out) and binding each role through `_ROLE_DEPLOYMENT`. A pack judge with no `_ROLE_DEPLOYMENT` entry → fail-clean with a surfaced error (the domain↔infra boundary: packs SELECT/subset known deployable identities; introducing a brand-new deployable judge needs core deployment support — document as a boundary + OQ).
- **v1/v2 OQ (§2-E):** the v2 branch (`:461-486`) reads the pack identity; the v1 legacy `else` (`:488-492`, v2-disabled) — recommend **leaving frozen/hardcoded** (it's the legacy non-pack path) and documenting it, to minimize blast radius. Surface your decision in plan-review.
- Behavior 0-delta: with the healthcare pack in v2 mode, the resolved roster MUST equal the `acc4973` roster (same 3 judges, same deployment, same order).

**D7 — `tests/_seam_freeze.py`:** extend `_COUNCIL_AUTHORIZED_MARKERS` (+ `production_judges`/`pack_production_judges` + the roster carve-out symbol) and `_COUNCIL_REQUIRED_CARVEOUTS` (+ the new `__import__(...).pack_production_judges()` signature). Carve-out count 3→4. Keep the S-BS-124 per-line hardening intact; prove the guard stays non-vacuous (a smuggled non-marker line in the new hunk must FAIL).

### SECOND-PACK PROOF (the decoupling gate — the whole point)

**D8 — `packs/story_audit/`** (a minimal 2nd real pack; NOT a sentinel-only fixture):
- `pack.json` — own `pack_id`, own `flags_ref` → its own `taxonomy_snapshot.json`, `judges` = a DIFFERENT set (e.g. `["risk_judge","policy_judge"]` — a strict subset, MUST be ⊆ `_ROLE_DEPLOYMENT`), reuse healthcare `council_roles`/`ontology` if convenient.
- `taxonomy_snapshot.json` — own `tiers` + `tier1_owners` + `production_judges` (the subset) + **own `lenses`** (a DIFFERENT role→codes map from healthcare).

**D9 — `tests/test_pack_layer2c.py`** (new) — mirror `test_pack_layer2b.py:213-243`:
- **Equivalence:** under the healthcare pack, resolved `LENS_BY_ROLE` == the `acc4973` lens values AND resolved roster == the `acc4973` roster (behavior 0-delta).
- **Non-vacuity / second-pack proof:** a subprocess with `LITHRIM_BENCH_PACK=story_audit` → the council assembles a roster of EXACTLY story_audit's judge subset AND `LENS_BY_ROLE` == story_audit's lenses, **with zero core edits**. This is the A-DECOUPLE gate.
- **Moat-untouched:** the withstands-gate still gates on the (now pack-resolved) lens identically for healthcare (consensus oracle + lens⊆owners green).

**D10 — docs:** SPEC `healthcare-realm-as-pack` LANDED entry for layer-2c + a CLAUDE.md note (the council now resolves roster identity + lens authority from the pack; deployment binding stays core). Update the §"Taxonomy snapshot is the contract" line to add `lenses` + `production_judges`-as-roster-identity to the runtime-resolved set.

### §2-D — S-BS-125 inheritance (resolve in this cycle)

S-BS-125 = `council_roster()` reads canonical/`DEFAULT_PACK` while `council_known_codes()` reads active. **2 halves, both due here per the seam's OQ-1:** (a) `council_roster()` must NOT validate a 2nd judge-bearing pack against healthcare's roster — re-point it to the active pack's identity where correct; (b) `council_known_codes()`'s now-vacuous self-check should become a real cross-pack check (or be retired). State your resolution in plan-review; closing S-BS-125 is an acceptance item (A7).

### §2-E — Open questions to resolve in plan-review

- **OQ-1 (v1/v2):** does the pack identity drive only v2, with v1 left frozen-legacy? (recommended) Or both?
- **OQ-2 (deployment boundary):** confirm a pack judge ∉ `_ROLE_DEPLOYMENT` fails-clean; document that packs subset known identities (don't introduce new deployable judges).
- **OQ-3 (snapshot generation):** does `scripts/snapshot_taxonomy.py` need to emit `lenses` so a future re-snapshot doesn't drop it? (S-BS-117-class portability of the contract.)
- **OQ-4 (component lens constants):** keep `RISK_JUDGE_LENS` as derived references or remove?

---

## 3. Plan-review checkpoint (non-negotiable)

Post: Understanding · the identity↔deployment split design (the `_ROLE_DEPLOYMENT` table shape) · file-by-file · the second-pack (`story_audit`) shape · resolutions to OQ-1..4 + S-BS-125 · how you prove behavior 0-delta (healthcare) AND the second-pack decoupling · Test plan · Risks. **Wait for "go".** Expect a fresh-critic worktree pass at close.

---

## 4. Scope guardrails — NOT in scope

- **The council MECHANISM** — `_apply_consensus`, the consensus oracle, the withstands-gate LOGIC, evidence validation. You move WHERE the roster/lenses come FROM; you do NOT change how votes are tallied or how the gate decides. Behavior 0-delta on healthcare is the proof.
- **The v1 legacy roster** (if OQ-1 resolves "v2 only") — leave it frozen.
- **Deployment config into the pack** — `_ROLE_DEPLOYMENT` (provider/model/Azure ids/capability flags) STAYS in core. A pack carrying Azure deployment ids is a scope violation.
- **Refactoring `CouncilModel`** beyond what the split needs — no new fields, no dataclass restructure.
- **The other carve-outs** (tiers, owners, prompts) — untouched.
- **`signals.py`/`withstands.py` moat logic** — they READ the resolved lens; their logic is frozen.
- Drive-by formatting / dep bumps / "while I was here".

If something feels load-bearing-but-not-listed, halt and surface it.

---

## 5. Acceptance criteria

- **A1 (lens equivalence).** Under healthcare, resolved `LENS_BY_ROLE` == the `acc4973` lens values (per-role frozenset equality). Verify in `test_pack_layer2c.py`.
- **A2 (roster equivalence / 0-delta).** Under healthcare v2, resolved roster == `acc4973` roster (same judges, deployment, order). Verify via test + a diff against the pre-2c construction.
- **A3 (DECOUPLE — the gate).** `LITHRIM_BENCH_PACK=story_audit` (subprocess) → roster == story_audit's judge subset AND `LENS_BY_ROLE` == story_audit's lenses, **zero core edits**. The decoupling proof.
- **A4 (moat untouched).** `test_consensus.py` + the lens⊆owners invariant (`test_trio_dspy.py`) green; withstands-gate behavior on healthcare unchanged.
- **A5 (freeze guard).** Exactly 4 authorized carve-outs in `compliance_council.py`; `assert_compliance_council_carveouts_only` green AND proven non-vacuous (a smuggled non-marker line in the roster hunk FAILS). `roster/_apply_consensus/withstands/ontology` otherwise byte-frozen vs `acc4973`.
- **A6 (suite).** Full suite **0-new vs parent `f4655d8`** (debuglithrim; documented S-BS-96 observation guards don't count). `ruff check .` + `ruff format --check .` clean on touched files.
- **A7 (S-BS-125).** Both halves resolved; the asymmetry closed or explicitly re-scoped with rationale.

### Diagnostic stats (NOT gates)
- Carve-out hunk count 3→4; lens authority relocated (count of codes per role moved to the snapshot).

---

## 6. Commit structure + message templates

Atomic, pathspec-scoped (dirty index — memory `git-commit-pathspec-dirty-index`):
1. `feat(pack): pack_lenses() + snapshot lenses field — the lens authority source (PACK-2c D1/D2)`
2. `refactor(council): resolve LENS_BY_ROLE from the pack (PACK-2c D3/D4)` — judge_metric.py carve-out + the `is`→`==` test.
3. `feat(pack): pack_production_judges() (PACK-2c D5)`
4. `refactor(council): un-freeze the roster — identity from pack, deployment in core (PACK-2c D6)` — the 4th carve-out + `_ROLE_DEPLOYMENT`.
5. `test(pack): freeze-guard 4th carve-out + story_audit second-pack decoupling proof (PACK-2c D7/D8/D9)`
6. `docs(pack): SPEC layer-2c LANDED + CLAUDE.md roster/lens runtime note (PACK-2c D10)`

Each body ends with:
```
Executed per bench-salvage-phasePACK-2c-council-roster-lens-unfreeze-driver by session-2026-06-11-N.
See .devloop/sessions/session-bench-salvage-phasePACK-2c-2026-06-11.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before session log)

- [ ] A1–A7 PASS
- [ ] All commits exist; tree clean for the cycle pathspec (foreign staged files untouched)
- [ ] Tests + linters clean (record commands + env)
- [ ] No file outside §2 deliverables; the council mechanism byte-frozen vs `acc4973` except the 4th carve-out
- [ ] No services autostarted; no pushes/tags
- [ ] Session log per template; REPORT documents OQ-1..4 + S-BS-125 resolutions + the deployment↔domain boundary
- [ ] Diagnose-before-edit: every "the roster/lens resolves to X" claim backed by a verbatim evidence block (CONFIRMED/INFERRED/HYPOTHESIS tagged)

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight in order — **read `tests/_seam_freeze.py` and `compliance_council.py:348-492` whole** before proposing the split.
3. Trace the moat: `withstands.py:144` — confirm exactly how the lens is consumed so your pack-resolution is behavior-identical.
4. Re-read §2-§7. Post plan-review per §3 with OQ-1..4 + S-BS-125 resolved.

---

## 9. References

- Recon findings: this driver's §1 (recon agent + monitor spot-verification, HEAD `f4655d8`, 2026-06-11)
- Precedents: PACK-1b (`test_pack_layer1b.py`), PACK-2b (`test_pack_layer2b.py:213-243` subprocess proof)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (S-BS-125, the carve-out history)
- Memory: `healthcare-realm-as-pack`, `git-commit-pathspec-dirty-index`, `live-reassess-before-driver-lock`

---

## Hardness

- [x] **HARD GATE** — fresh-critic session with `isolation: worktree` required at close (touches the moat + the frozen council; the FIRST un-freeze that is NOT moat-untouched). Trigger via a fresh critic kickoff, not inline critique.
