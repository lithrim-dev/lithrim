# Driver — `bench-salvage` phase `S-BS-49-WIN`: the optimize-win attempt (corpus-deepening + a demo-selection interleave)

> **Generated by the monitor in a planning session. The executor pastes the
> KICKOFF block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseS-BS-49-WIN-optimize-win-attempt-driver`
> **Version:** v1
> **Authored:** 2026-06-08
> **Last re-verified against code:** 2026-06-08 (HEAD `212d02b`; per MONITOR §Phase 1a — recon `ad821b5e6f35b0af9` + monitor spot-greps of `order_positive_first`/`compile_judge`/the optimize route; all CONFIRMED).
> **Hardness:** HARD GATE — optimize is a PAID labels-true-by-construction claim; the held-out Δ must be HONEST (the `run_optimize` contract + 2 prior honest losses); this cycle CHANGES the demo-SELECTION adjacent to the FROZEN accept-gate; fresh-critic at close.

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase S-BS-49-WIN (the optimize-win attempt: corpus-deepening + a demo-selection interleave).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                                  (your role)
  2. .devloop/prompts/bench-salvage_phaseS-BS-49-WIN_optimize-win-attempt_driver.md (this doc)
  3. docs/research/RUN_uap4_optimize_live_2026-06-05.json + .devloop/sessions/critique-bench-salvage-phaseUAP-4-2026-06-05.md  (the prior HONEST LOSS + the over-fire that sank it)
  4. lithrim_bench/runtime/council/judge_optimize.py:119-146 (order_positive_first) + :255-294 (compile_judge, the selection insertion + the FROZEN accept-gate) + :333-439 (run_optimize)
  5. lithrim_bench/runtime/council/judge_metric.py:54-114 (LENS_BY_ROLE/RISK_JUDGE_LENS) + :188-275 (score_judge accept-gate — FROZEN, NEVER loosen)
  6. scripts/generate_judge_calib.py (WIDEN_TARGETS + the widening loop — the additive clean-cohort template) + examples/judge_calib_v1.jsonl + tests/test_uap4_corpus_superset.py (the corpus guards)
  7. apps/bff/app.py:232-236 (OptimizeRequest.confirm) + :740-783 (POST /v1/judges/{role}/optimize — the route 422s WITHOUT confirm:true; the live run is {confirm:true})
  8. docs/specs/SPEC_CALIBRATION_TRAINER.md §4.1 (the demo lever is NON-MONOTONIC) + .devloop/templates/PROOF_CAPSULE_TEMPLATE.md (honest-Δ only)

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseS-BS-49-WIN-optimize-win-attempt-driver
Driver doc: .devloop/prompts/bench-salvage_phaseS-BS-49-WIN_optimize-win-attempt_driver.md
```

---

## 0. What this cycle is (and the honest odds)

**The genuine attempt to land a REAL optimize held-out WIN.** The optimize loop (compile few-shot demos for `risk_judge` over the calibration split, measure precision/recall Δ on the FROZEN held-out `test` split) has LOST twice, honestly. UAP-4's live run: held-out graded **0.80→0.70** (precision 0.7143→0.625) — a single new FP, `WRONG_DOSAGE` raised on the dose-rich clean negative `bench_scribe_v1_clean_negative_5074ab5a99d4`. Root cause: the 4 bootstrapped demos were all `WRONG_DOSAGE` positives (the S-BS-49 `order_positive_first` fix put positives first), which primed the judge to over-fire dose mentions (**S-BS-76**, the §4.1 non-monotonic lever on the DEMO axis).

**The win lever (this cycle):** interleave ≥1 **clean negative** (a dose-rich note the teacher correctly judges SILENT) into the 4 compiled demos, so the few-shot judge learns "a dose mentioned ≠ `WRONG_DOSAGE`" and **stops over-firing on the held-out clean** → fp drops → precision up → a POSITIVE Δ. This requires (a) deepening the clean-negative cohort so there ARE dose-rich cleans to interleave, and (b) a **selection change** so a clean is reachable within the 4-demo cap (today `order_positive_first` + the cap means cleans are never reached).

**Honest odds: UNCERTAIN, lean LOW.** The demo lever is non-monotonic (the thesis: the deterministic floor is the reliable lever, demo-tuning is exploratory). The breaking case `5074ab5a` lives in the FROZEN held-out split and the win must come from the compiled program *generalizing* to stop over-firing it — which few-shot may or may not achieve. **Honest-Δ is the LAW:** a positive held-out Δ = the WIN; a non-positive Δ = a DOCUMENTED LOSS (a PASS — it reconfirms the floor-over-optimize thesis). A **manufactured win** (loosening the accept-gate, relabeling a row, moving/editing the frozen test case, or iterating paid runs until one lifts) = a **FAIL**. The deepened corpus has standalone value regardless.

---

## 1. Pre-flight reading (ordered)

1. `docs/research/RUN_uap4_optimize_live_2026-06-05.json` + `critique-bench-salvage-phaseUAP-4-2026-06-05.md` — the prior loss: baseline vs optimized tp/fp/fn, the over-firing held-out case `5074ab5a`, `n_positive_demos 0→4`.
2. `lithrim_bench/runtime/council/judge_optimize.py` — `order_positive_first` :129-146 (the FROZEN-no-longer selection: `positives + rest`; its docstring explains the 4-cap → cleans unreached); `compile_judge` :255-294 (the selection insertion at :282-283; `max_bootstrapped_demos=4`; the metric :285 is the FROZEN accept-gate, `trace`→hard-accept so ONLY a teacher-nailed case becomes a demo); `run_optimize` :333-439 (splits by `row["split"]`×role-relevant; returns baseline/optimized/delta); `build_judge_program` :158-206 (the `$0` offline path via a fake `predictor`).
3. `lithrim_bench/runtime/council/judge_metric.py` — `RISK_JUDGE_LENS` :54-62; `score_judge` :188-246 (`accepted = fp_total==0 and fn_total==0`); `make_judge_metric` :249-275. **This whole file is FROZEN — the win comes from demos, never a weaker gate.**
4. `scripts/generate_judge_calib.py` — `WIDEN_TARGETS` :103-110 + the widening loop :280-309 (pins `row["split"]="calibration"` via `WIDEN_SPLIT`) — the template a `CLEAN_WIDEN_TARGETS` block mirrors. `packager.py:_split_for` :60-70 (split = `sha1` hash unless pinned) + `package_case` (emits `injection_recipes: []`, `clean_negative: true`, `expected_compliance_verdict: "approve"` for a recipe-less row).
5. `examples/judge_calib_v1.jsonl` — 83 rows (53 calibration / 30 test); risk-lens trainset 24 (19 in-lens positives vs only 5 cleans); only 3 cleans in the held-out. `tests/test_uap4_corpus_superset.py` — the guards (see §4).
6. `apps/bff/app.py:232-236` (`OptimizeRequest{confirm, limit}`) + :740-783 (`POST /v1/judges/{role}/optimize`; **422 without `confirm:true`**; calls `run_optimize(..., confirm_cost=True, coverage_aware=True)`). **NOTE:** this route DOES have a confirm cost-gate (unlike `/v1/run-eval`) — fire `{confirm:true}`.
7. `docs/specs/SPEC_CALIBRATION_TRAINER.md` §4.1 (NON-MONOTONIC lever) + `.devloop/templates/PROOF_CAPSULE_TEMPLATE.md`.

> **Citation discipline:** every file:line was re-grepped at HEAD `212d02b`. If you spot drift, halt and surface it in plan-review.

---

## 2. Deliverables (file-by-file)

1. **`scripts/generate_judge_calib.py`** — a `CLEAN_WIDEN_TARGETS` block + loop, mirroring `WIDEN_TARGETS`, pinned `split="calibration"`: **dose-rich `scribe_v1` clean negatives** (mirror the `5074ab5a` over-fire template — medication/dose-rich notes with NO injected defect) + ~2 `triage_v1` cleans. Each is a clean negative the teacher should judge SILENT. Then **regenerate `examples/judge_calib_v1.jsonl` byte-deterministically.**

2. **`examples/judge_calib_v1.jsonl`** — the regenerated corpus (the +N pinned-calibration clean rows). NEVER edit/relabel an existing row; the `test` split stays byte-identical.

3. **`lithrim_bench/runtime/council/judge_optimize.py`** — the **selection change (the win lever)**: a new coverage mode (e.g. `order_balanced` / reserve-a-slot) that makes ≥1 teacher-nailable clean negative **reachable within the `max_bootstrapped_demos` cap** — e.g. reserve the 4th of 4 slots for a clean, or interleave so the bootstrap sees `[pos, pos, pos, clean, …]`. Wire it into `compile_judge` + thread it through `run_optimize` + the BFF route's `coverage_aware=True` call (or a new flag). **The accept-gate (`make_judge_metric`/`score_judge`) is UNTOUCHED** — a clean is only bootstrapped if the teacher nails it silent. Keep `max_bootstrapped_demos=4` unless plan-review justifies raising it.

4. **`tests/test_uap4_corpus_superset.py`** — relax `test_added_rows_are_positives_pinned_to_calibration` to permit pinned clean-negatives (added = positive **OR** calibration-pinned clean-neg); KEEP the byte-identity + held-out-frozen guards. Add an assertion that the new cleans are in `calibration` + `expected_safety_flags: []`.

5. **`tests/test_s_bs_49_win_selection.py`** (NEW) — the offline `$0` proof + dry-run: (a) **reachability, non-vacuous** — with the new selection + a perfect-teacher stub, ≥1 clean negative lands in the compiled demos (and WITHOUT the change, 0 cleans reached); (b) the held-out Δ measured offline via `build_judge_program(predictor=…)` / `evaluate_program` where feasible (the dry-run before the paid run); (c) the accept-gate is provably untouched (a row with an fp still fails `score_judge`).

6. **`docs/research/RUN_s_bs_49_win_optimize_live_2026-06-08.json`** (from A-LIVE) — the one live optimize run blob (baseline/optimized/delta + compile_config showing the demo mix).

7. **`docs/research/PROOF_bench-salvage_S-BS-49-WIN_2026-06-08.md`** — the proof capsule per the template. **Honest-Δ:** a positive Δ = the WIN, documented; a non-positive Δ = the LOSS, documented (+ the deepened corpus + the reachability proof still stand).

---

## 3. Plan-review checkpoint (non-negotiable)

Post a plan (understanding · file-by-file · test plan · risks · deviations). **Wait for "go".** Resolve these:

1. **The selection mechanism** — `order_balanced` (interleave) vs reserve-the-Nth-slot-for-a-clean vs a pos/neg quota. **Recommend the minimal:** reserve ≥1 of the 4 slots for a teacher-nailable clean, keeping `max_bootstrapped_demos=4`. Name exactly how the trainset is reordered/partitioned and how it stays a single compile pass (no extra teacher calls beyond the cap).
2. **Teacher-nailable cleans** — a clean is only bootstrapped if the teacher produces `[]` AND the gate passes. How do you ensure ≥1 dose-rich clean in the cohort is one the real teacher nails silent? (Offline uses a perfect-teacher stub; the LIVE run depends on the real teacher — name the risk.)
3. **Cohort size + composition** — how many cleans, dose-rich `scribe_v1`-weighted (mirroring `5074ab5a`)? (Recommend dose-rich scribe-weighted, a handful.)
4. **`max_bootstrapped_demos`** — keep 4 (reserve a slot) vs raise it. Recommend keep 4 (minimal, comparable to the UAP-4 baseline).
5. **The live-run gate** — fire the ONE paid run ONLY after D5's offline proof shows the clean is reached AND the offline Δ is non-negative (don't spend if the offline measure shows it can't help). Budget ≤ ~$0.40, ONE run, NO iteration.
6. **SPLIT confirm** — the bind→re-run round-trip stays **UAP-4-opt** (BLOCKED on a +Δ; if THIS cycle lands a +Δ it becomes unblockable, but binding is still its own cycle); the faithfulness arm + `VALUE_MISMATCH` (S-BS-46) stay OUT (logged-not-padded).

---

## 4. Scope guardrails — NOT in scope / FROZEN

- **The ACCEPT-GATE — `judge_metric.py` / `make_judge_metric` / `score_judge` / `LENS_BY_ROLE` — BYTE-UNTOUCHED. NEVER loosen** (`accepted = fp==0 and fn==0`). The win MUST come from better demo selection, not a weaker gate. This is the labels-true-by-construction line.
- **The FROZEN held-out `test` split** — 30 cases, identical ids, **including the breaking case `5074ab5a`**. The win must come from the compiled program GENERALIZING to stop over-firing it — never from editing/moving/relabeling the test set.
- **Existing 83 corpus rows** — never edit/relabel. Additive append only (the new cleans, pinned calibration, via the generator — byte-deterministic, never hand-edited).
- **The per-judge seam** (`judges_dspy.py`, `compliance_council.py`, role prompts / `council_roles/`), committed seeds (`ws0_default.json`), `taxonomy/taxonomy_snapshot.json` — byte-untouched.
- **The bind→re-run round-trip** (`bind_compiled_demos` production-load) — UAP-4-opt; OUT.
- **The faithfulness arm / `VALUE_MISMATCH` (S-BS-46)** — OUT (logged-not-padded); this cycle targets the `risk_judge` lens.
- **The prompt-axis lever** — OUT (already shown non-monotonic).
- Drive-by formatting / dep bumps. (Run `ruff format` on YOUR new/changed files so they ship format-clean — but do NOT whole-file-format pre-existing format-dirty files.)

If something feels load-bearing-but-not-listed, halt and surface it.

---

## 5. Acceptance criteria

- **A1 — admissible deepened corpus.** The corpus is regenerated by the generator (byte-identical to a fresh `python scripts/generate_judge_calib.py` run); `scripts/lint_golden_against_taxonomy.py --golden examples/judge_calib_v1.jsonl` exits 0; the new rows are `clean_negative:true`, `expected_safety_flags:[]`, `split:"calibration"`.
- **A2 — the selection reaches a clean (non-vacuous).** Offline (`tests/test_s_bs_49_win_selection.py`): with the new selection + a perfect-teacher stub, ≥1 clean negative is in the compiled demos; WITHOUT the change, 0 cleans are reached. The reachability flip is attributable to the selection change.
- **A3 — the accept-gate + test split are FROZEN.** `git diff <parent>..HEAD -- lithrim_bench/runtime/council/judge_metric.py` is EMPTY; the `test`-split ids are byte-identical (incl. `5074ab5a`); no existing corpus row relabeled. A row with an fp still fails `score_judge` (gate-untouched proof).
- **A4 — green bar, no regression.** Canonical `PYENV_VERSION=debuglithrim python -m pytest -q` (baseline the 2 S-BS-96 observation failures in the MAIN checkout) + plain `python -m pytest -q`; `test_uap4_corpus_superset` + `test_judge_calib_corpus` + `test_judge_optimize` + `test_judge_metric` green; ruff check + format clean on touched files.
- **A5 — frozen-seam 0-delta** — the per-judge seam / seeds / snapshot byte-untouched.
- **A-LIVE — ONE cost-gated optimize run (honest-Δ).** After `curl :8002/health` + `:8787/health` (no autostart), fire `POST :8787/v1/judges/risk_judge/optimize {confirm:true}`. Capture the blob. **PASS = an HONEST Δ, recorded either way:** WIN = a POSITIVE held-out Δ (precision/graded up; the genuine optimize win — a real result) → proof capsule; LOSS = a non-positive Δ → documented honestly (the demo lever is non-monotonic; the floor remains the reliable lever) → proof capsule. **A manufactured win = FAIL.** ≤1 run, ≤~$0.40; do NOT iterate.

### Diagnostic stats (NOT gates)
- baseline vs optimized tp/fp/fn + the demo mix (how many cleans vs positives bootstrapped) — reported; NOT a gate.
- Whether `5074ab5a`'s FP cleared — reported; NOT a gate (the gate is the honest Δ + frozen invariants).

---

## 6. Commit structure + message templates

Atomic, pathspec-only (the shared dirty branch carries foreign `root.jsx` + 3 untracked — `git commit -- <your files>`, never bare):

1. **`feat(calib): dose-rich clean-negative cohort (additive, calibration-pinned, byte-deterministic)`** (D1+D2)
2. **`feat(optimize): demo-selection interleaves a clean negative within the cap (accept-gate FROZEN)`** (D3)
3. **`test(s-bs-49-win): corpus-guard relax + offline reachability/Δ proof (non-vacuous)`** (D4+D5)
4. **`docs(s-bs-49-win): live optimize run blob + honest proof capsule (WIN or documented LOSS)`** (D6+D7)

Each body ends with `Executed per bench-salvage-phaseS-BS-49-WIN-optimize-win-attempt-driver by session-2026-06-08-N.` + the `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` trailer.

---

## 7. Verification checklist (run at end, before the session log)

- [ ] A1–A5 PASS + A-LIVE recorded honestly (WIN or documented LOSS)
- [ ] `judge_metric.py` (accept-gate) byte-untouched; the `test` held-out split byte-identical; no existing corpus row relabeled
- [ ] All commits exist; tree clean except the known foreign files; pathspec-only
- [ ] Tests + ruff (check + format) clean on touched; baseline S-BS-96 in the MAIN checkout
- [ ] No services autostarted (`curl /health` first); no pushes
- [ ] Session log per the template (incl. the honest A-LIVE Δ + the demo mix)

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight in order (esp. the UAP-4 loss blob + `compile_judge`/`order_positive_first` + the accept-gate).
3. Re-read §2–§7.
4. Post plan-review per §3 — RESOLVE the 6 decisions (esp. #1 the selection mechanism + #2 teacher-nailable cleans + #5 the live-run gate).

---

## 9. References

- Seams: S-BS-49 (selection — this cycle changes it), S-BS-76 (positive-demo over-fire — this cycle targets it), S-BS-46 (faithfulness `VALUE_MISMATCH` cap — OUT), S-BS-48/UAP-4-opt (bind→re-run — OUT, gated on +Δ). `.devloop/state/STREAM_bench-salvage.md`.
- Prior: `docs/research/RUN_uap4_optimize_live_2026-06-05.json` + `REPORT_dspy3b_optimizer_2026-06-03.md` (the loss + S-BS-49 root cause); `critique-bench-salvage-phaseUAP-4-2026-06-05.md`.
- Honest-Δ: `.devloop/templates/PROOF_CAPSULE_TEMPLATE.md`; memory `calibration-trainer-is-the-product`, `critique-pass-precision-not-floor`, `self-asserting-loop-honesty-moat`, `live-overfire-context-primed`, `git-commit-pathspec-dirty-index`.
- Recon: agent `ad821b5e6f35b0af9` (2026-06-08; the feasibility read + the decisive 4-cap/positives-first reachability finding this driver is built on).

---

## Hardness

- [x] **HARD GATE** — fresh-critic at close. Focus: the accept-gate (`judge_metric.py`) + the FROZEN held-out `test` split are byte-untouched (the win is NOT manufactured); the selection-reaches-a-clean proof is non-vacuous; the corpus is byte-deterministic + additive (no relabel); the A-LIVE Δ is HONEST (a positive Δ is a real win; a non-positive Δ is an accepted documented loss; a loosened gate / relabeled row / moved test case / iterated run is a FAIL).
