# Driver — `bench-salvage` phase `PACK-1b`: un-freeze the council's taxonomy (read `KNOWN_TAXONOMY_CODES` / tier sets FROM the active pack)

> **Generated by the monitor 2026-06-10 from the user's authorization to begin the council un-freeze endgame + the "1b first, 2b follows" sequencing decision, grounded in a live code-scan reassessment (agent `aa92fc754a83859aa`) + monitor spot-verification of the load-bearing citations at HEAD `75507a2`. PACK-1a relocated the taxonomy DATA into the pack but left the council reading a hardcoded copy behind a `pack ⊆ council` consistency gate. PACK-1b inverts that: the FROZEN council reads its taxonomy FROM the active pack, so the pack snapshot becomes the single source of truth and the ⊆ gate becomes an equivalence pin. This is the first edit INSIDE the frozen seam beyond the PACK-2 path-only carve-out — hence the freeze guard itself must be relaxed (carefully).**
>
> **Bundle ID:** `bench-salvage-phasePACK-1b-council-taxonomy-unfreeze-driver`
> **Version:** v1 · **Status:** READY (HARD GATE)
> **Scope decision (LOCKED by the user 2026-06-10):** 1b = the TAXONOMY un-freeze ONLY. The roster / `_TIER1_OWNERS` / `LENS_BY_ROLE` un-freeze is **2b**, a separate follow-on driver (re-greped against 1b's changes). Do NOT pull 2b forward.

---

## KICKOFF (paste into a fresh executor rooted in lithrim-bench)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-1b
(healthcare-realm-as-pack, LAYER 1b: un-freeze the council's TAXONOMY — make the FROZEN
compliance_council.py read KNOWN_TAXONOMY_CODES + the TIER_1/2/3 sets FROM the active pack
[via the proven PACK-2 inline-__import__ carve-out] instead of hardcoded literals, so the
pack snapshot is the single source of truth. This is the FIRST substantive edit inside the
frozen seam — the freeze guard in tests/_seam_freeze.py must be carefully relaxed to admit
the authorized taxonomy hunk(s), and the AST reader that parses the council's literal sets
[harness/pack.py council_known_codes] must be re-pointed atomically [it literal_evals the
tier sets and WILL break]. TAXONOMY ONLY — the roster / _TIER1_OWNERS / LENS_BY_ROLE are 2b,
explicitly OUT of scope).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-1b_council-taxonomy-unfreeze_driver.md (this)
  3. The PACK-2 carve-out precedent you are REPEATING: compliance_council.py:469-470
     (_ROLE_PROMPTS_DIR inline-__import__) + tests/_seam_freeze.py:144-179
     (assert_compliance_council_prompts_dir_relocated_only — the guard you must relax)
  4. The pre-flight §1 — esp. compliance_council.py:176-292 (the 3 tier sets + the union +
     the readers), harness/pack.py:68-125 (council_known_codes [BREAKS], _pack_taxonomy_codes,
     assert_pack_council_consistent, the gated path accessors), and the determinism net
     (runtime/council/tests/test_consensus.py — the consensus oracle that must stay green)
  5. CLAUDE.md ("taxonomy_snapshot is THE contract" — now the pack's contract; the invariant
     MOVES into the pack, it does not weaken)

Then post plan-review. The SCOPE (taxonomy-only; 2b deferred) is DECIDED — do NOT re-litigate.
Do NOT write code until the monitor says "go". HARD GATE — it edits the FROZEN council +
relaxes the freeze guard. The plan-review must resolve D-A (the carve-out hunk shape + the
exact guard relaxation) / D-B (retire vs re-point council_known_codes) / D-C (the equivalence
gate that replaces byte-identity) / D-D (behavior-0-delta proof: same VALUES, different source).

Bundle ID: bench-salvage-phasePACK-1b-council-taxonomy-unfreeze-driver
```

---

## 0. Why this cut — the source-of-truth flip

PACK-1a relocated the taxonomy DATA into `packs/healthcare/taxonomy_snapshot.json` (`tiers` = the same 19 codes) but the live council still owns a **hardcoded copy** (`compliance_council.py` `TIER_1_NEVER_EVENTS`/`TIER_2_HIGH_RISK`/`TIER_3_MEDIUM` → `KNOWN_TAXONOMY_CODES`). PACK-1a bridged the two with a **one-directional consistency gate** (`assert_pack_council_consistent`: pack codes ⊆ council codes) so the council could stay byte-frozen. That gate's own docstring (`pack.py:45-46`) says *"1b removes the gate by reading the council's codes FROM the pack."*

PACK-1b does exactly that: the council reads taxonomy from the active pack via the **proven PACK-2 carve-out pattern** (`compliance_council.py:470`'s inline `__import__` of a `harness.pack` accessor). After 1b the pack snapshot is the **single source of truth**; the ⊆ gate becomes a **two-way equivalence pin**. Behavior is **0-delta** — the resolved values are identical (the snapshot mirrors the literals today), only the *source* changes. This is the smaller, cleaner half of the endgame; 2b (roster/owners/lens) follows.

---

## 1. Pre-flight reading (re-verify on open — citations grepped at authoring 2026-06-10, HEAD `75507a2`)

1. **The 3 tier sets + the union (the 1b targets):** `compliance_council.py:176` `TIER_1_NEVER_EVENTS` (8 codes), `:201` `TIER_2_HIGH_RISK` (7), `:212` `TIER_3_MEDIUM` (4), `:292` `KNOWN_TAXONOMY_CODES = TIER_1 | TIER_2 | TIER_3`. Total = **19**.
2. **The readers (why the symbols must survive as module-level names):** the discard-unknown-code gate `compliance_council.py:1525` (`if code not in KNOWN_TAXONOMY_CODES: … continue`); the consensus engine reads the individual tier sets (≈`:2063`/`:2183`/`:2205`); `judges_dspy.py:43-46` imports `KNOWN_TAXONOMY_CODES` + the 3 tier sets, and `judges_dspy.py:142` is the DSPy finding-filter mirror; `default_taxonomy_context()` (`judges_dspy.py:95-107`) lists the tier codes in the judge PROMPT — so preserving the VALUES preserves the prompt → no live-judge drift.
3. **The carve-out template (REPEAT this):** `compliance_council.py:469-470` — `_ROLE_PROMPTS_DIR = Path(__import__("lithrim_bench.harness.pack", fromlist=["pack_prompts_path"]).pack_prompts_path())`. Inline `__import__` (no top-level import of `harness.pack` — keeps the frozen file dependency-light), resolved at class/module-body evaluation.
4. **The pack surface (what 1b ADDS):** `harness/pack.py` has `_pack_taxonomy_codes(pack)` (`:99`, PRIVATE, returns the union frozenset from the snapshot `tiers`) but **NO public accessor returning the 3 named tier sets**. The council needs `TIER_1/2/3` as separate symbols, so 1b adds a public `pack_tiers(pack=None) -> dict[str,frozenset]` (and/or `pack_taxonomy_codes()` for the union). `lithrim_bench/taxonomy.py` already loads tiers via `load_taxonomy()` — reuse it to avoid a second parser.
5. **THE ATOMIC-COUPLING LANDMINE — `council_known_codes()` BREAKS:** `harness/pack.py:69-83` AST-parses the council and does `ast.literal_eval(node.value)` on each tier-set assignment (`:79`). Once `TIER_1_NEVER_EVENTS = pack_tiers()[…]` (a Call/Subscript, not a literal), `literal_eval` RAISES. So `council_known_codes()` MUST be retired or re-pointed **in the same change**. Its callers: `assert_codes_known` (`:91`, the ⊆ gate) and `test_pack_layer1a.py:101-103` (`tax.known_codes == council_known_codes()`).
6. **The freeze guard you must relax:** `tests/_seam_freeze.py:144-179` `assert_compliance_council_prompts_dir_relocated_only` — pins `compliance_council.py` byte-identical to baseline `_SEAM_BASELINE = "acc4973"` (`:23`) EXCEPT **exactly one** changed hunk (`:168` `assert len(changed) == 1`) that must be a `replace` (`:173`) containing `_ROLE_PROMPTS_DIR` (`:176`). Invoked from `tests/test_uap3b2_provenance.py:85` + `tests/test_uap3b_withstands.py:239`. Its docstring (`:150-152`) explicitly names `KNOWN_TAXONOMY_CODES` as frozen — that line updates with 1b.
7. **The determinism net (must stay green):** `runtime/council/tests/test_consensus.py` — the offline consensus oracle drives `_apply_consensus` with synthesized per-judge dicts (no LLM), locking verdict + tier bookkeeping. It tests BEHAVIOR not bytes → it survives the un-freeze unchanged and is the guarantee that replaces byte-identity. Runs under the `debuglithrim` pyenv (`importorskip("openai")`).
8. **The equivalence-pin hook:** `tests/test_pack_layer1a.py:101-103` (`tax.known_codes == council_known_codes()`) + `:110` (`== 19`). CLAUDE.md: the taxonomy_snapshot is THE contract (now the pack's).

---

## 2. The seam map (what 1b touches — taxonomy only)

| Site | Today | After 1b |
|---|---|---|
| `compliance_council.py:176/201/212` | 3 literal `set` bodies | resolved from `pack_tiers()` via inline `__import__` (symbols + values preserved) |
| `compliance_council.py:292` | `KNOWN_TAXONOMY_CODES = TIER_1 \| TIER_2 \| TIER_3` | unchanged (union of the now-pack-resolved sets) |
| `harness/pack.py` (new) | `_pack_taxonomy_codes` private | + public `pack_tiers()` / `pack_taxonomy_codes()` |
| `harness/pack.py:69` `council_known_codes()` | AST `literal_eval` of council tier sets — **BREAKS** | retire OR re-point to the pack snapshot (D-B) |
| `harness/pack.py:108` `assert_pack_council_consistent` | pack ⊆ council (now circular) | equivalence pin OR no-op (D-C) |
| `tests/_seam_freeze.py:144` | exactly-1-hunk (`_ROLE_PROMPTS_DIR`) | relaxed to enumerate the authorized taxonomy hunk(s) + prompts-dir, each marker-checked, otherwise byte-frozen |
| `tests/test_pack_layer1a.py:101` | `tax.known_codes == council_known_codes()` | the post-1b equivalence pin (council-resolved == snapshot == taxonomy loader) |

**FROZEN / OUT (2b or untouched):** `_TIER1_OWNERS` (`:232`), the `CouncilModel` roster (`:485-516`), `LENS_BY_ROLE` (`judge_metric.py:110`), `_apply_consensus` (the consensus logic), the ontology (`clinical_v1.json` / pack `ontology.json`).

---

## 3. Deliverables (1b)

**D1 — add the public pack accessor.** `harness/pack.py`: `pack_tiers(pack=None) -> dict[str, frozenset[str]]` returning `{TIER_1_NEVER_EVENTS, TIER_2_HIGH_RISK, TIER_3_MEDIUM}` from the active pack's snapshot `tiers` (reuse `_pack_taxonomy_codes`/`taxonomy.load_taxonomy()`; do not add a manifest field — `flags_ref` already carries `tiers`). Keep `import lithrim_bench.harness.pack` heavy-dep-free (no openai pulled).
**D2 — the council carve-out.** `compliance_council.py`: the 3 tier-set symbols (`:176/201/212`) resolve from `pack_tiers()` via the `:470` inline-`__import__` pattern; `KNOWN_TAXONOMY_CODES` (`:292`) stays the union. Minimal hunks; the 3 tier symbols + `KNOWN_TAXONOMY_CODES` stay module-level names (readers in §1.2 depend on them). NO other line in the file changes. Preserve the explanatory comments (they document the codes' clinical rationale — keep them or relocate verbatim to the pack snapshot; do not delete clinical provenance silently).
**D3 — retire/re-point `council_known_codes()` (atomic with D2).** It `literal_eval`s the council's literals and breaks. **Monitor lean (D-B): re-point** it to read the pack snapshot (`_pack_taxonomy_codes(active_pack())`) — post-1b the council reads from the pack, so "the council's known codes" == the pack's; this keeps `test_pack_layer1a:101` meaningful (taxonomy-loader == snapshot) and `assert_codes_known` self-consistent. Update the `pack.py` module docstring (`:12-18`) — the AST-parse-the-council technique no longer applies to taxonomy.
**D4 — relax the freeze guard (the delicate one).** `tests/_seam_freeze.py`: `assert_compliance_council_prompts_dir_relocated_only` → admit the authorized 1b taxonomy hunk(s) IN ADDITION to the `_ROLE_PROMPTS_DIR` hunk. Keep the baseline `acc4973`. Every changed hunk must contain ONLY an authorized marker (`_ROLE_PROMPTS_DIR` OR one of `TIER_1_NEVER_EVENTS`/`TIER_2_HIGH_RISK`/`TIER_3_MEDIUM`/`KNOWN_TAXONOMY_CODES`/the `pack_tiers` accessor name); everything OUTSIDE the authorized hunks stays byte-identical to `acc4973`. **Non-vacuous: an unauthorized edit anywhere else, or reverting the carve-out, still FAILS.** Rename the function (it is no longer "prompts_dir only"). Update its callers in `test_uap3b2_provenance.py` + `test_uap3b_withstands.py`.
**D5 — the equivalence gate (replaces byte-identity for taxonomy).** A test asserting the council's RESOLVED `TIER_1/2/3` + `KNOWN_TAXONOMY_CODES` **==** the pack snapshot `tiers` **==** `taxonomy.load_taxonomy().known_codes` (== 19 for healthcare). Plus a **non-vacuity** check: with `LITHRIM_BENCH_PACK` pointing at a fixture pack whose `tiers` differ, the council's resolved codes FOLLOW the pack (proving it reads from the pack, not a hardcode). Confirm `test_consensus.py` (the consensus oracle) stays green — behavior 0-delta.
**D6 — docs.** Update `SPEC_PLUGIN_ARCHITECTURE` (the layer-1b entry: taxonomy un-frozen, source-of-truth flip, the equivalence gate replacing byte-identity) + the `pack.py` module docstring + the `_seam_freeze.py` comments. Note CLAUDE.md's "taxonomy_snapshot is THE contract" is now load-bearing at runtime (the council reads it).

---

## 4. Plan-review — the load-bearing decisions
- **D-A (carve-out hunk shape + guard relaxation — the crux).** Minimize the changed hunks in `compliance_council.py`: prefer resolving the 3 tier sets from a single `pack_tiers()` call (e.g. one helper assignment + 3 `TIER_x = _PACK_TIERS["…"]` lines, ideally contiguous) so the relaxed guard enumerates the FEWEST authorized hunks. The guard relaxation must stay auditable + non-vacuous — propose the exact assertion shape (enumerate-and-marker-check vs. an allowlist of hunk signatures) at plan-review.
- **D-B (retire vs re-point `council_known_codes`).** *Monitor lean: re-point to the pack snapshot* (keeps the equivalence pin + callers working). Retiring it means updating `assert_codes_known` + `test_pack_layer1a:101` — surface the choice.
- **D-C (the now-circular ⊆ gate).** `assert_pack_council_consistent` becomes vacuous (pack ⊆ pack). Replace with the D5 equivalence pin, or keep as a cheap self-consistency no-op. Decide + state.
- **D-D (behavior-0-delta proof).** The whole safety claim is "same VALUES, different SOURCE." Show it: the consensus oracle green, the `:1525` discard-gate unchanged, the DSPy prompt's taxonomy listing byte-identical, the resolved sets `==` the literals that were at `acc4973`.

---

## 5. Scope guardrails — NOT in scope
- **2b** — `_TIER1_OWNERS` (`:232`), the `CouncilModel` roster (`:485-516`), `LENS_BY_ROLE` (`judge_metric.py`). Explicitly FROZEN this cycle (the user locked 1b-first). Do NOT touch `council_roster()` either (it AST-parses `_TIER1_OWNERS`, which stays a literal in 1b — leave it working).
- **The consensus logic** (`_apply_consensus`) — untouched; only the tier-set SOURCE changes.
- **The ontology** (`clinical_v1.json` / pack `ontology.json`) — untouched; `assert_clinical_ontology_seam_frozen` stays green.
- **The moat** (`signals.py`/`withstands.py`/`LENS_BY_ROLE`) — untouched; 1b is orthogonal to it (verified: the gate reads the suppress registry + `LENS_BY_ROLE`, never `KNOWN_TAXONOMY_CODES`).
- **No new manifest field** — the snapshot's `tiers` is the source.
- **No re-snapshot / no taxonomy VALUE change** — 1b is a source flip, not a content edit. The 19 codes stay identical.

## 6. Acceptance
- **A1.** The council resolves `KNOWN_TAXONOMY_CODES` + the 3 tier sets FROM the active pack (no hardcoded literal set bodies remain at `:176/201/212`); `import compliance_council` still works in the `[council]` env; `import lithrim_bench.harness.pack` stays heavy-dep-free.
- **A2.** **Behavior 0-delta:** the consensus oracle (`test_consensus.py`) green unchanged; the resolved tier sets `==` the `acc4973` literals (== the snapshot, == `load_taxonomy().known_codes`, == 19); the `:1525` discard-unknown-code gate still discards the same codes; the DSPy taxonomy prompt listing unchanged.
- **A3.** **Non-vacuity (both directions):** (i) point `LITHRIM_BENCH_PACK` at a fixture pack with different `tiers` → the council's resolved codes follow the pack (proves it reads from the pack); (ii) the relaxed freeze guard still FAILS on an unauthorized council edit AND on reverting the carve-out.
- **A4.** **Frozen-set 0-delta (2b + moat):** `_TIER1_OWNERS`, the `CouncilModel` roster, `_apply_consensus`, `LENS_BY_ROLE`, `signals.py`, `withstands.py`, the ontology — byte-unchanged (grep + the surviving freeze guards green: `assert_judges_dspy_consensus_seam_frozen`, `assert_clinical_ontology_seam_frozen`, `assert_council_roles_relocated_only`).
- **A5.** Green both envs (default py3.12 + `debuglithrim` py3.10.15), **0-new at parent** (`75507a2`; the S-BS-117/119 worktree caveat — judge by 0-new-at-parent in the same env); ruff clean; pathspec-only; NOT pushed.

## 7. Hardness
- [x] **HARD GATE** — fresh-critic required, `isolation:'worktree'` (re-spawn on infra stall, per PACK-5a/5b). *Reason: this is the FIRST substantive edit inside the FROZEN `compliance_council.py` (beyond the PACK-2 path-only carve-out) AND it relaxes the freeze guard that is currently the only thing forbidding arbitrary council edits. The critic must verify: the carve-out is value-preserving (resolved == `acc4973` literals), behavior is 0-delta (consensus oracle), the relaxed guard is still non-vacuous (unauthorized edits + carve-out-revert both fail), the AST-reader re-point is correct, 2b + the moat are byte-frozen, and the source flip reads genuinely from the pack (the fixture-pack non-vacuity test).*

## 8. References
- [[healthcare-realm-as-pack]] (the layered plan; 1b = the council taxonomy un-freeze) · the PACK-2 carve-out (`compliance_council.py:470` + `_seam_freeze.py:144`) = the proven pattern + the guard to relax · `runtime/council/tests/test_consensus.py` (the determinism net that survives) · `SPEC_PLUGIN_ARCHITECTURE` · CLAUDE.md (the taxonomy_snapshot contract). Reassessment: agent `aa92fc754a83859aa` (2026-06-10), monitor-spot-verified.
