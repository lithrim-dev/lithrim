# Driver — `bench-salvage` phase `PACK-1`: healthcare-realm-as-pack, layer 1 (ontology + taxonomy load)

> **Generated by the monitor 2026-06-09 from the user's "the entire healthcare realm loads as a pack, not hardcoded" decision ([[healthcare-realm-as-pack]]). This is the FIRST concrete cut of `SPEC_PLUGIN_ARCHITECTURE` Phase-1 ∩ `SPEC_EVAL_SCENARIOS` SCENARIO-1 — extract the foundational layer (ontology + taxonomy) out of the core into a loadable `healthcare` pack.**
>
> **Bundle ID:** `bench-salvage-phasePACK-1-healthcare-pack-ontology-layer-driver`
> **Version:** v1 · **Status:** READY (HARD GATE)

---

## KICKOFF (paste into a fresh executor)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-1
(healthcare-realm-as-pack, LAYER 1: make the core LOAD its ontology + taxonomy from an
active `healthcare` pack instead of the hardcoded clinical_v1.json / taxonomy_snapshot.json,
so the core's ontology-load path is domain-agnostic and the boundary is grep-verifiable).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-1_healthcare-pack-ontology-layer_driver.md (this)
  3. docs/specs/SPEC_PLUGIN_ARCHITECTURE.md (§3 packs, the Pack manifest, §Build sequencing Phase-1)
  4. docs/specs/SPEC_EVAL_SCENARIOS.md (D-C per-domain admissibility; SCENARIO-1)
  5. memory healthcare-realm-as-pack (the layered plan + the frozen-seam tension)

Then post plan-review. The LOAD-BEARING decision is D-A (the frozen council's
KNOWN_TAXONOMY_CODES): this cut stays ABOVE the frozen seam (consistency gate, defer the
un-freeze to layer 1b). Do NOT write code until I confirm the 4 decisions + say "go".
This is HARD GATE — it touches taxonomy_snapshot, the CLAUDE.md core invariant.

Bundle ID: bench-salvage-phasePACK-1-healthcare-pack-ontology-layer-driver
```

---

## 0. Why this cut (and why ONLY layer 1a)

Today the clinical realm is hardcoded into the core: `harness/ontology.py:38`
`DEFAULT_ONTOLOGY_PATH = .../data/ontology/clinical_v1.json`; `taxonomy.py:13` `_SNAPSHOT_PATH`
→ `taxonomy/taxonomy_snapshot.json`; `load_ontology()` defaults to clinical everywhere
(`correction.py`, `grounding.py:575`, `config.py:11`). The goal ([[healthcare-realm-as-pack]]):
the core LOADS the ontology/taxonomy from an **active pack**; `healthcare` is the first pack;
the core has no clinical hardcoding — **verifiable by grep**.

**Layer 1 SPLITS, and this cut is ONLY 1a.** `KNOWN_TAXONOMY_CODES` is hardcoded in
`compliance_council.py:292` (TIER_1/2/3 sets) — **inside the FROZEN council seam**, enforced at
`judges_dspy.py:142` / `judge_metric.py`. Un-hardcoding THAT (layer **1b**) touches the frozen
seam and needs explicit un-freeze authorization — **OUT of scope here**. This cut moves only the
**load path** (above the seam) + adds a **consistency gate** bridging to the still-frozen council.

---

## 1. Pre-flight reading (re-verify on open)
1. `docs/specs/SPEC_PLUGIN_ARCHITECTURE.md` — §3 "Packs as manifest bundles" + the **Pack manifest** (`pack_id:"healthcare"`, `ontology`, `flags_ref:"taxonomy_snapshot"`, `tier:pro`) + the load-time gate + Phase-1 ("formalize the existing registries… pure refactor"). This cut REALIZES the pack-load half of Phase-1 on the ontology layer.
2. `lithrim_bench/harness/ontology.py` — `DEFAULT_ONTOLOGY_PATH` (`:38`), `load_ontology(path=…)` (`:182`), `_load_ontology_cached` (`:197`), `VerificationContractDecl`. **The wiring site.**
3. `lithrim_bench/taxonomy.py` — `_SNAPSHOT_PATH` (`:13`) + the snapshot loader. The taxonomy half.
4. `lithrim_bench/runtime/council/compliance_council.py:292` — `KNOWN_TAXONOMY_CODES` (the FROZEN hardcode — **read, do NOT touch**; D-A's consistency gate validates AGAINST it).
5. `lithrim_bench/harness/config.py:11` + `correction.py` + `grounding.py:575` — the `load_ontology()` call sites that must resolve via the active pack (not a hardcoded path).
6. `scripts/seed_ontology.py` — seeds `clinical_v1.json`; the seeder becomes the **healthcare pack's** seeder (relocated/retargeted, not deleted).
7. `apps/bff/app.py:125/165` — `DEFAULT_ONTOLOGY_WORKDIR`; how the BFF resolves the working ontology (the active-pack thread may surface here).
8. CLAUDE.md — "the taxonomy snapshot is the contract" + "never hand-edit / re-snapshot, don't soft-pass." The snapshot's CONTENT stays byte-identical; only its HOME moves into the pack.

---

## 2. Deliverables (layer 1a)

**D1 — the pack manifest + `healthcare` pack layout.** A `packs/healthcare/pack.json` (matching the spec's Pack manifest) declaring the pack's `ontology` + `flags_ref` (taxonomy). A minimal pack-resolution helper (`harness/pack.py` or similar): `active_pack()` → the pack id (default `healthcare`); `pack_ontology_path(pack)` / `pack_taxonomy_path(pack)`.
**D2 — relocate the content into the pack (D-B).** Move `data/ontology/clinical_v1.json` → `packs/healthcare/ontology.json` and `taxonomy/taxonomy_snapshot.json` → `packs/healthcare/taxonomy_snapshot.json` (**byte-identical** moves), OR (lower-risk first step) a manifest that REFERENCES them in place. Decide in D-B; either way the CORE no longer names `clinical_v1` directly — it resolves via the active pack.
**D3 — pack-aware load.** `load_ontology()` resolves the path via `active_pack()` (not the hardcoded `DEFAULT_ONTOLOGY_PATH`); `taxonomy.py` resolves the snapshot via the active pack. All call sites (`config.py`/`correction.py`/`grounding.py`) go through this — no hardcoded clinical path survives in the core.
**D4 — the consistency gate (the bridge to the still-frozen council).** On load, assert the loaded pack's taxonomy codes ⊆ `compliance_council.KNOWN_TAXONOMY_CODES` (the council stays frozen; the gate guarantees the loaded pack is council-compatible). A mismatch fails closed with a clear error. This is what lets 1a ship without touching the frozen seam (1b removes this gate by reading the council's codes FROM the pack).
**D5 — the boundary test.** A test that greps the **core** (`lithrim_bench/`, excluding `packs/` + the frozen council's `KNOWN_TAXONOMY_CODES` which is 1b) for `clinical_v1` / a hardcoded ontology path → **asserts none in the load path**. This is the verifiable-boundary proof, scoped to layer 1a.
**D6 — docs.** Note layer-1a landed in `SPEC_PLUGIN_ARCHITECTURE` (Phase-1 first cut) + the 1b/2–5 follow-ons.

---

## 3. Plan-review — the 4 load-bearing decisions
- **D-A (the frozen-seam line).** Confirm this cut stays ABOVE the frozen council: keep `KNOWN_TAXONOMY_CODES` as-is + the D4 consistency gate; **defer un-hardcoding the council to layer 1b** (a separately-authorized cycle). *Monitor lean: yes — 1a only.* Touching `compliance_council.py` here is a HARD STOP without explicit go.
- **D-B (relocate vs reference).** Physically move the two JSONs into `packs/healthcare/` (cleanest boundary) vs a manifest referencing them in place (lower first-step risk). Monitor lean: **move** (the boundary is the point) — but byte-identical, and the snapshot move must keep the CLAUDE.md re-snapshot path working (the pack's snapshot is still refreshed from lithrim-backend).
- **D-C (call-site coverage).** Which `load_ontology()` consumers route through the active pack this cut (all of them, or just the default)? Lean: all, since the goal is zero hardcoded clinical path in the core.
- **D-D (green-throughout).** The active pack DEFAULTS to `healthcare`, so the clinical product is byte-behavior-identical (same ontology, same codes, same verdicts) — only the SOURCE moved. Prove it: the full suite green + a before/after eval on `ws0_default` identical.

---

## 4. Scope guardrails — NOT in scope
- **Layer 1b** — un-hardcoding `KNOWN_TAXONOMY_CODES` from the frozen `compliance_council.py` (needs explicit un-freeze authorization; the consistency gate defers it).
- **Layers 2–5** — judges, floors/verification-contracts, journey literals (`data.jsx`/`cards.jsx`), dataset → their own cycles.
- **The story/second pack**, the licensing gate (Phase 3), the `frontend`-kind plugin, HPACK.
- **FROZEN:** `compliance_council._apply_consensus` + `KNOWN_TAXONOMY_CODES` + the `judges_dspy` seam + `judge_metric` + the agent seeds. The **snapshot CONTENT** stays byte-identical (only its home moves). No ontology CONTENT edits (flags/owners/severity byte-identical — this is a MOVE, not an edit).

---

## 5. Acceptance
- **A1.** The core resolves the ontology + taxonomy via an **active pack** (`healthcare` default); no hardcoded `clinical_v1`/snapshot path survives in the core load path (D5 grep asserts it).
- **A2.** **Byte-behavior-identical clinical product:** `ws0_default` (and the suite) produce identical ontology + identical verdicts before/after — the MOVE changed the source, not the behavior.
- **A3.** The **consistency gate** holds (loaded pack codes ⊆ the frozen council's `KNOWN_TAXONOMY_CODES`); a deliberately-mismatched pack fails closed (test-pinned).
- **A4.** Content byte-identity: `packs/healthcare/{ontology,taxonomy_snapshot}.json` are byte-identical to the moved originals; `git` shows renames/moves, not content deltas; `scripts/lint_golden_against_taxonomy.py` green; the re-snapshot path still works against the pack location.
- **A5.** FROZEN set 0-delta (`compliance_council` incl. `KNOWN_TAXONOMY_CODES` + seam + judge_metric + agent seeds); green bar (default + debuglithrim); ruff clean; pathspec-only.

## 6. Hardness
- [x] **HARD GATE** — fresh-critic required (`/devloop-critique bench-salvage PACK-1`). *Reason: it touches `taxonomy_snapshot` (the CLAUDE.md core invariant + the only lithrim-backend coupling point) and the admissibility path; the move must be byte-behavior-identical (A2) and must NOT silently relax the by-construction gate or breach the frozen council (D-A). The critic verifies the boundary is real, the move is content-identical, and the consistency gate is non-vacuous.*

## 7. References
- [[healthcare-realm-as-pack]] (the committed approach + the layered plan) · [[conversational-first-core-plugin-line]] · `SPEC_PLUGIN_ARCHITECTURE` (Pack manifest, Phase-1) · `SPEC_EVAL_SCENARIOS` (D-C, SCENARIO-1) · CLAUDE.md (taxonomy snapshot = the contract).
