# Driver — `bench-salvage` phase `LAUNCH-PREP`: v1 standalone + release prep

> **Generated by the monitor. The executor pastes the KICKOFF block (top) into a fresh
> Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseLAUNCH-PREP-v1-standalone-and-release-prep-driver`
> **Version:** v1 · **Authored:** 2026-06-09 · **Re-verified against code:** 2026-06-09 (HEAD `8f87843`; monitor reads of `README.md`, `apps/bff/app.py:224-228` RunEvalRequest, `apps/shell/src/bff.js:25` + `app.jsx:32/95`, `pyproject.toml` extras).
> **Hardness:** ROUTINE-with-care — a **live standalone smoke** + inline critique. NOT HARD-GATE (it does NOT touch the A-SAFE conversational gate; confirm that stays byte-identical). The **standalone topology (D1)** is the load-bearing item; plan-review it carefully.

---

## KICKOFF (paste into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase LAUNCH-PREP (v1 standalone + release prep).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phaseLAUNCH-PREP_v1-standalone-and-release-prep_driver.md (this doc)
  3. apps/bff/app.py:224-228 (RunEvalRequest: live -> :8002 backend; in_process -> the bundled v2 council) + the run_eval endpoint + how run_eval.run routes live vs in_process vs replay
  4. lithrim_bench/runtime/council/ (the WS-6c PORTED v2 council — Mongo-free, importable, in_process-capable) + lithrim_bench/runtime/council/judges_dspy.py build_judge_lm (Azure default | byo-claude) — CONFIRM it imports self-contained (no lithrim-backend / no :8002 / no Mongo)
  5. apps/shell/src/bff.js:25 (runEval posts {agent, live, in_process}) + app.jsx:32/95 ("Run live" -> live=true -> :8002 today)
  6. README.md (engine-only today — no product quickstart) + pyproject.toml [project.optional-dependencies] (bff/council/agent) + [project.scripts] (lithrim-bench-pack)
  7. .devloop/state/STREAM_bench-salvage.md (the open seams S-BS-98/101/102/104/105/106/107/108 — for the triage table) + memory conversational-first-core-plugin-line (the v1-launch pivot + the CUT)

Then post your plan-review per EXECUTOR.md. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseLAUNCH-PREP-v1-standalone-and-release-prep-driver
```

---

## 0. The goal

Make the existing product **launchable as v1** — the conversational, tool-grounded clinical eval platform. v1 is ~built (shell + 12 chat tools + the multi-provider v2 council + the grounding-floor moat + audit + the CI/CD gate, live-attested + CHATBIND-2). The launch gap is **packaging, not features**: today a live run needs **lithrim-backend on `:8002`** (+ its Mongo), and the README is **engine-only** (no product quickstart). LAUNCH-PREP closes that so a person can install + run the OSS core with a **BYO Azure/Claude key, no lithrim-backend, no Mongo**.

**Scope discipline (the user's "don't digress / launch ASAP"):** this cycle is packaging/stabilize/release ONLY. **NOT in scope:** SCENARIO-1 / FHIR / StoryWorld, IMPORT-1 (data ingestion), the provider picker UI, the plugin refactor — all parked post-v1 (`SPEC_EVAL_SCENARIOS`, `SPEC_PLUGIN_ARCHITECTURE`).

---

## 1. Pre-flight reading
1. `apps/bff/app.py:224-228` — `RunEvalRequest` (`live` → `:8002` backend [paid HTTP]; `in_process` → the bundled v2 council [paid Azure/BYO-Claude]; both default `False` = `$0` replay). Trace `run_eval.run` / the endpoint: how does it route live vs in_process vs replay, and where is the `:8002` URL read?
2. `lithrim_bench/runtime/council/` — the WS-6c ported v2 council. **CONFIRM (Phase-1a, load-bearing): the `in_process` grade path imports self-contained** — no `lithrim-backend` import, no `:8002` call, no Mongo. (WS-6c was Mongo-free + importable; WS-6d swapped persistence to SQLite; the in_process A-LIVE ran in the BFF — so this should hold; verify it.)
3. `apps/shell/src/bff.js:25` + `app.jsx:32/95` — "Run live" → `live=true` → `:8002`. The shell does not drive `in_process` today (S-BS-105).
4. `README.md` — engine-only (Synthea → cases). No "run the conversational eval product" quickstart.
5. `pyproject.toml` — extras `[bff]`/`[council]`/`[agent]`/`[verification]`; `[project.scripts]` `lithrim-bench-pack`. The install surface.

---

## 2. Deliverables (file-by-file)

**D1 — Standalone council topology (load-bearing; the OSS-release enabler).** Make the product run a real council **without lithrim-backend (`:8002`) and without Mongo** — via the bundled `in_process` v2 council (BYO Azure/Claude key). Recommended mechanism (plan-review confirms): a **council-backend selector** (an env flag, e.g. `LITHRIM_COUNCIL_BACKEND=in_process|http`, default **`in_process`** for the OSS core) read in the BFF run path so a non-replay run routes to the bundled council; the shell's "Run live" then drives the configured backend (BYO-key), no `:8002` required. **A-SAFE: the chat stays replay-only / `$0` — D1 changes only the council BACKEND for the human's explicit paid run, NOT the chat's deny-hook/allowlist (confirm byte-identical).** Verify the `in_process` verdict is equivalent-or-stricter than `:8002` (WS-6c-AGENTIC established the orchestrator is ≥-strict).

**D2 — Product quickstart (the install-friction fix).** Add a **QUICKSTART** (README section or `docs/QUICKSTART.md`) for the conversational eval *product*: install the extras (`pip install -e .[bff,council,agent]` + the shell `npm i`), set a **BYO Azure or Claude key** (the env vars), run the BFF + the shell + the in_process council; the `$0` replay default; the paid live run is the human's cost-confirmed button. A clean-clone walk-through the executor actually follows.

**D3 — Green bar + seam triage.** The full suite green under `debuglithrim` (document the **credential-gated** council tests — they need a BYO Azure/Claude key, not a regression). A short **seam-triage table** (S-BS-98/101/102/104/105/106/107/108): each → `launch-blocking? (expected: none) / post-v1`. `ruff`/`prettier` clean on touched.

**D4 — Release checklist + notes.** A `RELEASE.md` (or `docs/RELEASE_v1.md`): what ships in the **OSS-core v1** (the engine + the conversational platform + the CI/CD gate + the clinical sample agent/cases), the **BYO-key / airgapped / no-us-hosted** framing, and the **push/PR/tag steps as a checklist the OWNER executes** (the executor PREPARES it + drafts release notes; **do NOT push** — owner-gated).

---

## 3. Plan-review checkpoint (non-negotiable)
Post a plan (understanding · file-by-file · test plan · risks · deviations). **Wait for "go".** Resolve:
1. **D1 mechanism:** the council-backend selector — env flag vs request param vs a startup mode. Cite the `:8002`-URL read site + the in_process route. **Confirm the in_process council imports self-contained** (no lithrim-backend/`:8002`/Mongo) — the load-bearing feasibility check.
2. **D1 A-SAFE:** confirm the change is to the council BACKEND only; the chat's deny-hook/allowlist/replay-only contract stays byte-identical (no new paid path for the agent).
3. **D1 default:** is `in_process` the OSS default (recommended), with `:8002` opt-in for a lithrim-backend deployment? Confirm verdict-equivalence (in_process ≥-strict).
4. **Scope:** LAUNCH-PREP only — NOT IMPORT-1 / the provider picker / scenarios / the plugin refactor. The push is owner-gated.

---

## 4. Scope guardrails — NOT in scope
- **SCENARIO-1 / FHIR / StoryWorld / IMPORT-1 / the provider picker / the plugin refactor** — parked post-v1.
- **The actual push / PR / tag / publish** — owner-gated; the executor PREPARES the checklist + notes only.
- **A-SAFE** — the chat's deny-hook + allowlist + the replay-only `$0` contract stay byte-identical; D1 touches the human's paid-run council backend, not the agent's gate.
- **The frozen grading engine / consensus seam** — untouched (D1 routes to it, does not modify it).
- Drive-by formatting. `ruff`/`prettier` only touched files. Pathspec-only commits (foreign `root.jsx` + untracked stay out).

---

## 5. Acceptance criteria
- **A1 (standalone)** — the product runs end-to-end with **NO `:8002`/lithrim-backend and no Mongo** (the in_process council, BYO Azure/Claude key); a non-replay run produces the correct verdict.
- **A2 (quickstart)** — a clean-clone install + run works from the QUICKSTART (the executor verifies each step); the `$0` replay default works with no key, the paid run needs the BYO key.
- **A3 (green bar)** — full suite green under `debuglithrim` (cred-gated council tests documented as BYO-key); ruff clean; the seam-triage table shows no HARD launch-blocker.
- **A4 (release)** — `RELEASE.md`/checklist + draft release notes; the OSS-core scope + BYO-key framing stated; push steps listed for the owner.
- **A-LIVE (USER-RUN)** — on `:5180` with lithrim-backend STOPPED, a run completes via the in_process council (BYO-key) — proving the standalone v1.

---

## 6. Commit structure
Atomic, pathspec-only (`git commit -- <files>`):
1. `feat(bff): standalone in_process council backend (no :8002/Mongo dependency) + the shell route`
2. `docs(launch): product QUICKSTART + RELEASE v1 checklist`
3. `chore(launch): green-bar + the seam-triage table` (+ any green-bar fix)

Each body: `Executed per bench-salvage-phaseLAUNCH-PREP-v1-standalone-and-release-prep-driver by session-2026-06-09-N.` + the `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` trailer.

---

## 7. Verification checklist
- [ ] A1–A4 PASS + A-LIVE ready (user-run, lithrim-backend stopped)
- [ ] D1 A-SAFE: the chat deny-hook/allowlist/replay-only contract byte-identical; the in_process council imports self-contained
- [ ] Commits exist; tree clean except the foreign files; pathspec-only
- [ ] Tests + lint clean on touched; the frozen engine/consensus untouched; the seam-triage table present
- [ ] No services autostarted; NO push (owner-gated)
- [ ] Session log per the template

## 8. First move
1. Read `EXECUTOR.md`. 2. Read §1 (esp. `app.py:224-228` + `runtime/council/` self-containment + the `:8002`-URL read site). 3. Re-read §2–§7. 4. Post plan-review per §3 — resolve the D1 council-backend mechanism + CONFIRM the in_process council is self-contained (no lithrim-backend) + the A-SAFE-untouched bound, before any code.

## 9. References
- The v1-launch pivot: memory `conversational-first-core-plugin-line` (the CUT + the critical path) + `gtm-launch-and-journey-thesis` (open-core + BYO-key + no-us-hosted; install-friction is the named risk).
- The in_process council: WS-6c (the PORT, Mongo-free) + WS-6c-AGENTIC (grade_inprocess, verdict ≥-strict) + WS-6d (SQLite persistence) — memory `ws6c-agentic-grade-wire-and-recompose-pattern`, `persistence-blob-projection-architecture`.
- A-SAFE: memory `conversational-authoring-surface-complete` (the chat stays replay-only `$0`).
- Unblocks: the v1 release (push owner-gated). PENDING (parallel, non-blocking): the v1-scope-line (pitch/early-access vs self-serve — the latter layers IMPORT-1 + the provider picker AFTER this).

## Hardness
- [ ] ROUTINE-with-care — inline critique + a **live standalone smoke** (A1/A-LIVE). NOT HARD-GATE, BUT: if plan-review finds D1 must touch the A-SAFE conversational gate (it should NOT), escalate to a fresh-critic.
