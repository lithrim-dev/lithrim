# Driver — `bench-salvage` phase `WS-6c-DSPy`: rebuild the per-judge layer on DSPy, wrapping the ported v2 consensus (the §6 hybrid)

> **Generated by the monitor in a planning session. The executor pastes the KICKOFF block
> (top) into a fresh Claude Code session — ROOTED IN `lithrim-bench` — reads the rest,
> posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-DSPy-judge-rebuild-driver`
> **Version:** v1 · **Authored:** 2026-06-02
> **Last re-verified against code:** 2026-06-02 (`lithrim-bench` re-grepped at `HEAD` of `bench-salvage/ws6c-council-port`, per MONITOR §Phase 1a)
>
> **This is WS-6 step c-DSPy** — the second BUILD phase of the consolidation track and the FIRST
> DSPy-rebuild. It **rebuilds the council's per-judge prompt-and-parse layer as DSPy modules**, each
> emitting the EXACT per-judge dict seam that the WS-6c-ported `_apply_consensus` already consumes, so
> the **validated consensus IP wraps DSPy judges UNCHANGED** (the §6 hybrid). Per
> `docs/specs/RECOMPOSITION_PLAN_ws6.md` §3/§6/§7 and the WS-6b Ratification (Q3 = **incremental, one
> judge first**). Gated-by WS-6c (closed `12281e1..6bae3c0`). Lower blast radius than WS-6c: it does
> **NOT** change the consensus math and does **NOT** wire into the grade seam (that is WS-6c-AGENTIC).
> Parallel-safe with WS-6c-AGENTIC and the shell track.

---

## KICKOFF (paste into a fresh executor session — ROOTED IN `lithrim-bench`)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c-DSPy
(rebuild the per-judge layer on DSPy, wrapping the ported v2 consensus — the §6 hybrid).

IMPORTANT: open this session ROOTED IN lithrim-bench
(/Users/aregee/Workspace/github.com/lithrim-bench). EVERYTHING this cycle needs is in-repo:
the ported council (lithrim_bench/runtime/council/), the by-construction packs, and the
throwaway DSPy prior art (a worktree). You do NOT read ../lithrim-backend this cycle — the
council IP was already ported + verified byte-faithful to 493b533 at WS-6c. DO NOT change the
ported consensus math; DSPy lives strictly ABOVE the per-judge dict seam.

Read in this order:
  1. .devloop/personas/EXECUTOR.md   (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-6c-DSPy_dspy-judge-rebuild_driver.md   (this doc)
  3. docs/specs/RECOMPOSITION_PLAN_ws6.md   (§3 REBUILD-ON-DSPY, §6 the hybrid + the per-judge seam diagram, §7 the WS-6c-DSPy row, §Ratification Q3)
  4. lithrim_bench/runtime/council/__init__.py   (the per-judge dict seam — the contract DSPy must emit, :38-54)
  5. lithrim_bench/runtime/council/compliance_council.py   (the imperative seam DSPy replaces: per-judge dict built :1550-1567, consumed by _apply_consensus:1853; evaluate fan-out :2400/:2489)
  6. lithrim_bench/verification/jute_dspy.py   (the bench-as-acceptance-oracle pattern to REUSE: score_template:130, make_bench_metric:200, build_generator:333, lazy `import dspy`)
  7. .claude/worktrees/agent-adefc36309f77ed1b/experiments/dspy_council_smoke/{council_v2.py,council_dspy.py}   (DSPy prior art — REFERENCE-ONLY, do NOT import; S-BS-27)
  8. data/ontology/clinical_v1.json + examples/proof_case.jsonl + lithrim_bench/picklist.py (PACK_FILES:20)   (the recipe=label scoring source)

Then post your plan-review per EXECUTOR.md — resolve the 6 PLAN-REVIEW DECISIONS in §3.
Do not write code until I say "go". HARD GATE: fresh-critic at close (user MAY elect inline).

Bundle ID: bench-salvage-phaseWS-6c-DSPy-judge-rebuild-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-6c-DSPy_dspy-judge-rebuild_driver.md
```

---

## 1. Pre-flight reading (ordered)

> **Citation discipline (MONITOR §Phase 1a):** every file:line below was re-grepped against
> `lithrim-bench` at the WS-6c tip on 2026-06-02 (CONFIRMED unless noted). Re-grep yourself before
> plan-review; if a line has drifted, surface it.

1. `docs/specs/RECOMPOSITION_PLAN_ws6.md` — **§3 REBUILD-ON-DSPY** (the rebuild map + the two prior-art proofs), **§6 the hybrid** (the layered-module diagram + "DSPy owns the prompts; the tier math stays ported as the safety floor" + the closing invariant "optimizing a judge prompt can never weaken a Tier-1 never-event rule because the rule lives *below* the DSPy layer"), **§7 the WS-6c-DSPy row** (scope + gated-by 6c), **§Ratification Q3** (= incremental, one judge first; the hybrid wraps DSPy or non-DSPy judges identically). This is your contract; the driver is the index.
2. `lithrim_bench/runtime/council/__init__.py:38-54` — **the per-judge dict seam** (the §6 hybrid boundary). Verbatim shape DSPy MUST emit:
   ```python
   {
       "model": <judge role name>,        # e.g. "risk_judge"
       "decision": "approve"|"needs_review"|"reject",
       "confidence": float | None,        # None for Mistral (no logprobs) — NEVER coerce to 1.0/0.0
       "findings": [{"taxonomy_code": str, "evidence_spans": [{...}]}],
       "errors": [],                      # non-empty ⇒ judge excluded from consensus
   }
   ```
   "This is the stable boundary a future DSPy-rebuilt judge layer (WS-6c-DSPy) must emit; the ported consensus math wraps DSPy or non-DSPy judges identically." (`:51-54`).
3. `lithrim_bench/runtime/council/compliance_council.py` — **the imperative seam you replace (above), the ported consensus you keep UNCHANGED (below):**
   - **Replace (DSPy owns this):** the per-judge dict is built imperatively today at `:1550` (`"model"`) / `:1556` (`"findings"`) / `:1567` (`"errors"`), one per judge, inside the `evaluate()` fan-out (`def evaluate:2400`, judge loop `for model in self.models[:max_models]:2489`, via `_invoke_model:1406`). DSPy modules produce this same dict.
   - **Keep verbatim (DSPy is strictly ABOVE this):** `_apply_consensus:1853` (reads `decision` `:1986`, `findings`/`taxonomy_code`/`evidence_spans` `:1899/:1911/:1914`, `confidence` `:1979`, `errors` `:1578`, `model` `:1985`), `_compose_council_verdict_v2` (llama-veto) `:1812`, `_worst_of_verdicts:1799`, `extract_verdict_confidence:413` (None-tolerant), the tier tables + `_TIER1_OWNERS:232`. **Do not touch these.**
   - `class ComplianceCouncil` `:453`; `__init__` `:459`; the v2 trio is built **inline** at `:463` (`if settings.COMPLIANCE_COUNCIL_VERSION == "v2":`) — there is **no `JUDGES_V2` constant** (only a comment at `:381`). *(D3 close-out fix 2026-06-02: was `:459` for the class line.)*
4. `lithrim_bench/verification/jute_dspy.py` — **the bench-as-acceptance-oracle pattern to REUSE** (RECOMPOSITION_PLAN §3 "prior art #2" — "this makes the bench the acceptance oracle for tool authoring — the exact pattern judge-prompt optimization follows"): `score_template:130`, `make_bench_metric:200` (the hard-accept metric: 0-FP gates the optimizer), `build_generator:333` (the seeded generate→test→refine loop), `JuteValidatorSignature:311`, lazy `import dspy` inside the fns (`:309/:351` — heavy import guarded). Mirror this shape for a **judge** metric (findings-vs-recipe), do not reuse the structural-conformance metric directly.
5. `.claude/worktrees/agent-adefc36309f77ed1b/experiments/dspy_council_smoke/council_dspy.py` + `council_v2.py` — **DSPy prior art, REFERENCE-ONLY, do NOT import (S-BS-27, a throwaway worktree).** It proved the 3-judge DSPy wiring (28-row ledger; clean-negative reject→APPROVE flip) but **deliberately dropped the consensus IP** (`council_dspy.py:17-21` "DELIBERATE SIMPLIFICATION … none of that is reproduced") AND emits a **simplified, non-conforming** per-judge shape (`council_v2.py:42-66`: `{decision, violations:[str], confidence, reason, error}`). **The gap you close:** `violations:[str]` → `findings:[{taxonomy_code, evidence_spans}]` (the spans are what Tier-1 owner one-strike + 2-evidence-judge corroboration READ); singular `error` → `errors:[]`; add the `model` key; keep `confidence` None-tolerant. Re-author clean in the package wrapping the REAL ported consensus.
6. `data/ontology/clinical_v1.json` (23 flags / 16 questions / 1 verification_contract, floor-less) + `examples/proof_case.jsonl` + `lithrim_bench/picklist.py:PACK_FILES:20` (`scribe_v1:21`) + `data/verification_packs/` — the **by-construction recipe=label** scoring source. A judge's findings are scored against the case's `expected_safety_flags` (the recipe IS the label, CLAUDE.md core invariant).
7. `pyproject.toml` — `[project.optional-dependencies]`: `verification = [dspy>=3.2, httpx>=0.27]` (`:24-26`) already carries DSPy; `council = [openai>=1.0, tenacity>=8.0]` (`:42-44`) carries the Azure provider; `testpaths` includes `lithrim_bench/runtime/council/tests` (`:57`). The DSPy judge needs **both** extras (dspy to define the module, openai to bind the live LM). Default install (pydantic+pandas) must stay unchanged.
8. `lithrim_bench/runtime/council/llm_provider.py` (`get_sync`/`get_async`/`_resolve_model`) — the salvaged Azure factory the DSPy judges bind their LM to (the `council_v2.py:36` `pred.set_lm(lms[role])` pattern). `lithrim_bench/harness/grade.py` (`grade_replay:40`/`grade_live:85`) + `scripts/run_eval.py:run():101` + `lithrim_bench/harness/report.py:composite:29` — **the frozen seam; WS-6c-DSPy does NOT touch it** (A6).

---

## 2. Deliverables (file-by-file)

1. **`lithrim_bench/runtime/council/judges_dspy.py`** (NEW) — the DSPy judge layer.
   - A `dspy.Signature` for one judge, **findings-first**: inputs `(transcript/artifact, role_key_questions, taxonomy_context)`; outputs `decision` + `findings` (each `{taxonomy_code, evidence_spans}`) + `confidence` + `reason`.
   - A `Judge` `dspy.Module` (one `dspy.Predict` per role, bound to its Azure deployment via the salvaged `llm_provider` factory — the `council_v2.py:30-37` pattern) whose `forward` returns the **EXACT per-judge dict seam** (`{model, decision, confidence: float|None, findings:[{taxonomy_code, evidence_spans}], errors:[]}`, `runtime/council/__init__.py:45`). Per-judge `try/except` → `errors:[…]` (never abort the fan-out). `confidence` stays `None`-tolerant (do NOT coerce).
   - **Incremental (Q3):** rebuild **ONE judge first** (the role the packs score most cleanly — resolve at plan-review); the other two roles are pluggable behind the same signature, ported-imperative until rebuilt. The hybrid wraps DSPy + non-DSPy judges identically, so a mixed fan-out is valid.
   - Heavy `dspy` import **lazy/guarded** (mirror `jute_dspy.py:309/:351`); the module must not import `dspy` at package-load on default deps.
2. **`lithrim_bench/runtime/council/judge_metric.py`** (NEW, or folded into `judges_dspy.py`) — the **bench-accept metric for judges** (mirror `jute_dspy.make_bench_metric`/`score_template`): score a judge's `findings` against a by-construction case's `expected_safety_flags` (recipe=label) → precision/recall + a hard-accept (e.g. 0 false-positive flags on clean negatives, the judge analogue of jute's 0-FP). This makes the **bench the acceptance oracle** for judge-prompt authoring/optimization.
3. **The §6 hybrid wiring** (in `judges_dspy.py` — a `CouncilDSPy`/`evaluate_dspy` thin path, NOT a new grade entrypoint) — fan out the DSPy judge(s), collect the per-judge dict list, and call the **ported `_apply_consensus` UNCHANGED**. Prove the seam closes: the same fixtured per-judge dicts produce an identical verdict whether built imperatively or by the DSPy module. **No edit to `compliance_council.py`'s consensus functions.**
4. **`lithrim_bench/runtime/council/tests/` (offline; extend the WS-6c oracle)** — `pytest.importorskip("dspy")`, mock LM / injected predictor (the `jute_dspy` test pattern), no network:
   - (a) the DSPy judge emits the exact seam shape incl. a `confidence=None` round-trip (never coerced) and `errors:[…]` on a simulated judge failure;
   - (b) DSPy judges + the ported `_apply_consensus` reproduce the **WS-6c oracle verdicts** (Tier-1 one-strike, Tier-2 2+, PHI-FP, llama-veto, artifact-BLOCK, None-confidence) on fixtured judge outputs; *(NKA is a role-prompt suppression, not a consensus rule — verified at the prompt/live layer, not the oracle.)*
   - (c) the `judge_metric` runs offline on a fixtured judge output vs a recipe=label case;
   - (d) a guard that the consensus IP is unchanged (the existing `test_consensus.py` stays green).
5. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2026-XX-XX.json`** — session log incl. the **seam-conformance proof** (key-by-key vs `_apply_consensus` reads), the **pack score** for the rebuilt judge, the **consensus-unchanged diff** (the ported functions vs the WS-6c tip = zero logic delta), and the live-smoke verdict + cost. Optional short REPORT if the pack numbers warrant one.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `EXECUTOR.md`, post understanding + file-by-file plan + test plan + risks + deviations. **Wait for "go".** Resolve:

1. **Which judge first (Q3 incremental).** Rebuild ONE role first — the one the by-construction packs score most cleanly. *(Default: `risk_judge` — it owns `WRONG_DOSAGE` (Tier-1), the most-exercised Tier-1 code across the `scribe_v1` + `proof_case` packs. D1 close-out fix 2026-06-02: `risk_judge` is **prohibited from `FABRICATED_HISTORY`** — that is `behavior_judge`'s domain per `risk_judge.txt:24`; the metric must CREDIT `risk_judge` for correctly staying silent on it. The other two stay ported-imperative until a follow-up.)* vs all three at once.
2. **Module layout.** `runtime/council/judges_dspy.py` as a sibling to the ported council (it wraps that consensus) vs a new `runtime/judges/` package. *(Default: sibling in `runtime/council/`.)*
3. **The judge metric home + shape.** A thin council-specific `judge_metric` (findings-vs-recipe precision/recall + 0-FP-on-cleans hard-accept) mirroring `jute_dspy.make_bench_metric`, vs reusing the structural jute metric directly. *(Default: a council-specific metric; the jute metric is structural-conformance, judges need flag precision/recall.)*
4. **Optimizer scope this cycle.** Land the **bench-accept GATE + a seeded/hand-authored prompt only** (the WS-3a discipline: ship the assurance primitive, NOT the LLM auto-harden loop), vs run a DSPy optimizer (BootstrapFewShot/MIPRO) now. *(Default: GATE + seeded prompt first; an optimizer pass is a follow-up once the seam + metric are proven. If an optimizer IS run, confirm the LLM cost envelope — it burns Azure calls.)*
5. **Determinism contract.** How the DSPy judge honors the harness's per-(case,judge) reproducibility (e.g. `temperature=0` + DSPy cache + a seed pass-through), so a fixtured/replay case is byte-deterministic and the verdict-reproducibility signal survives. *(Default: temperature=0 + deterministic cache; document the approach. A DSPy reimpl that ignores seeding breaks reproducibility — §5 trap.)*
6. **Live-smoke scope + cost.** Exactly ONE cost-confirmed live Azure smoke (the rebuilt judge on one case → seam dict → consensus verdict). Confirm the envelope before the call. *(Default: 1 live smoke; offline/replay-first.)*

---

## 4. Scope guardrails — NOT in scope

- **NO change to the ported consensus IP** — `_apply_consensus`, `_compose_council_verdict_v2`, `_worst_of_verdicts`, `extract_verdict_confidence`, the tier/owner tables stay **byte-identical to the WS-6c tip**. DSPy lives strictly ABOVE the per-judge dict seam. (The §6 invariant: optimizing a prompt must never weaken a Tier-1 rule.)
- **NO harness grade-seam wire / in-process grade** — `grade.py` / `run_eval.py` / `report.composite` / §10 untouched. The DSPy judges are importable + tested, NOT wired into `grade`. The in-process grade path is **WS-6c-AGENTIC**.
- **NO LangGraph recompose, NO agents recompose** — that is **WS-6c-AGENTIC**.
- **NO S-BS-31 resolution** — the orphaned-Tier-1-owner decision (reassign owner vs ratify corroboration-only) is a backend-IP/clinical call that **gates WS-6c-AGENTIC** (where the council scores real cases). It stays **inert** this cycle (the DSPy judges grade no real cases through the seam). Do NOT hand-edit `_TIER1_OWNERS`.
- **NO persistence swap / repository interface / Mongo** (WS-6d); **NO KB/RAG / pinecone / ONNX** (WS-6d-KB).
- **NO importing the throwaway worktree DSPy code** (`council_v2.py`/`council_dspy.py` are S-BS-27 reference-only; re-author clean — they emit a non-conforming seam shape and dropped the consensus IP).
- **NO LLM auto-hardening loop** beyond the bench-accept gate (WS-3a §7 meta-finding: land the GATE, not the fallible auto-harden).
- **NO taxonomy re-snapshot / hand-edit** (done at WS-6c; `production_judges` already = the v2 trio).
- Change **NO code in `../lithrim-backend`** and **NO `etlp-mapper`**. No drive-by reformatting of `compliance_council.py` outside the (zero) consensus edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** The DSPy judge module emits the **EXACT per-judge dict seam** (`{model, decision, confidence: float|None, findings:[{taxonomy_code, evidence_spans}], errors:[]}`) — verified key-by-key against what `_apply_consensus` reads (`compliance_council.py:1985/1986/1899/1911/1914/1979/1578`), incl. a `confidence=None` round-trip that is **never coerced** and `errors:[…]` on a simulated judge failure.
- **A2.** **At least one judge** (incremental, Q3) is rebuilt on DSPy and **scored by a by-construction pack** (recipe=label) via the `judge_metric` (the reused `jute_dspy` bench-accept pattern); the pack score (precision/recall + hard-accept) is recorded.
- **A3.** **DSPy judges + the ported `_apply_consensus` reproduce the WS-6c offline oracle verdicts** (Tier-1 one-strike, Tier-2 2+, PHI-FP suppression, llama-veto, artifact-BLOCK, None-confidence) on fixtured judge outputs — proving the consensus wraps DSPy judges identically. *(Critique fix 2026-06-02: NKA was listed here but is a **role-prompt suppression** (`risk_judge.txt:1,38`), a judge-EMISSION behavior, not a consensus rule — so it is NOT reproducible on fixtured judge outputs through the oracle. NKA coverage belongs to the prompt-authoring + live-smoke layer; the consensus oracle correctly omits it.)*
- **A4.** The **ported consensus IP is UNCHANGED** — `_apply_consensus` / `_compose_council_verdict_v2` / `_worst_of_verdicts` / `extract_verdict_confidence` / the tables are byte-identical to the WS-6c tip (diff-vs-tip = zero logic delta in the session log); DSPy is strictly above the seam.
- **A5.** **Determinism preserved** — the DSPy judge honors the reproducibility contract (temperature=0 + cache + seed per the resolved plan-review #5), so a fixtured/replay case is byte-deterministic and the harness verdict-reproducibility signal holds.
- **A6.** **No contract change** — `grade.py` / `run_eval.run` / `report.composite` / §10 untouched; the DSPy judges are importable + tested but **not wired into the grade seam** (WS-6c-AGENTIC's job). S-BS-31 stays inert.
- **A7.** **Default install unchanged** — the DSPy judge path is gated behind `[verification]` (dspy) + `[council]` (openai); `pytest -q` green on default deps with the dspy tests `importorskip`-skipping cleanly; ruff clean.
- **A8.** **Scope held** — no AGENTIC / persistence / KB / consensus-math / backend / etlp edits; the throwaway worktree code is referenced, not imported.

### Diagnostic stats (NOT acceptance gates)
- The rebuilt judge's **pack score** (precision/recall on recipe=label) — reported.
- The **optimizer delta** (raw-prompt baseline vs DSPy-optimized accept-rate on the pack) IF an optimizer pass is run — reported, not a gate.
- **One cost-confirmed live Azure smoke** (the rebuilt judge, one case → seam dict → consensus verdict) — verdict + per-judge confidence + cost.
- Which judge was rebuilt first + the plan for the remaining two.

---

## 6. Commit structure + message templates

Atomic per-concern, in `lithrim-bench` (on the `bench-salvage/ws6c-council-port` branch or a fresh `bench-salvage/ws6c-dspy` branch — confirm at plan-review). Suggested order:

1. `feat(council-dspy): per-judge DSPy module emitting the §6 seam (findings-first, None-tolerant)` — `runtime/council/judges_dspy.py`
2. `feat(council-dspy): bench-accept judge metric over recipe=label packs` — `runtime/council/judge_metric.py`
3. `test(council-dspy): seam-conformance + DSPy-judges-wrap-ported-consensus + metric (offline)` — `runtime/council/tests/`

Each body ends with `Executed per bench-salvage-phaseWS-6c-DSPy-judge-rebuild-driver by session-…` + the `See …session-…json` line + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. **Diagnose-before-edit:** any fidelity/fix claim carries a fenced evidence block (the seam-conformance proof, the consensus-unchanged diff). Do NOT auto-commit — stage + propose each group for "go".

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] A1–A8 PASS (seam shape exact incl. None round-trip; ≥1 judge pack-scored; DSPy-judges reproduce the WS-6c oracle; consensus byte-identical to tip; determinism held; no contract change; default install unchanged; scope held)
- [ ] The ported consensus functions show **zero logic delta** vs the WS-6c tip (diff in the session log)
- [ ] No `grade.py` / `run_eval.py` / `report.py` / §10 change; council still NOT wired into grade
- [ ] No services autostarted (user starts them); the ONE live Azure call cost-confirmed first
- [ ] Throwaway worktree DSPy code referenced, NOT imported; no `../lithrim-backend` / `etlp-mapper` edit; no pushes/tags
- [ ] `pytest -q` green on default deps (dspy tests importorskip-skip); ruff clean
- [ ] Session log written (seam-conformance proof + pack score + consensus-unchanged diff + live-smoke verdict/cost)

---

## 8. First move

1. Read `EXECUTOR.md` + RECOMPOSITION_PLAN §3/§6/§7/§Ratification-Q3.
2. Re-grep the §1 anchors at the WS-6c tip (the seam at `runtime/council/__init__.py:45`; the imperative dict at `compliance_council.py:1550-1567`; `_apply_consensus:1853`; `jute_dspy` metric symbols). Confirm the `[verification]`+`[council]` extras carry dspy+openai.
3. Read §2–§7.
4. Post plan-review per §3 (the 6 decisions, incl. which judge first + optimizer-vs-gate + the live-smoke cost). Wait for "go".

---

## 9. References

- Plan: `docs/specs/RECOMPOSITION_PLAN_ws6.md` (§3 REBUILD-ON-DSPY, §6 hybrid + per-judge seam, §7 WS-6c-DSPy row, §Ratification Q3)
- Topology: `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (PORT-not-rewrite; sequencing B; frozen-contract rule)
- The seam contract: `lithrim_bench/runtime/council/__init__.py:38-54`
- The ported consensus (keep UNCHANGED): `lithrim_bench/runtime/council/compliance_council.py` (`_apply_consensus:1853`, `_compose_council_verdict_v2:1812`, `_worst_of_verdicts:1799`, `extract_verdict_confidence:413`)
- The bench-accept pattern to reuse: `lithrim_bench/verification/jute_dspy.py` (`make_bench_metric:200`, `score_template:130`, `build_generator:333`)
- DSPy prior art (reference-only, S-BS-27): `.claude/worktrees/agent-adefc36309f77ed1b/experiments/dspy_council_smoke/{council_v2.py,council_dspy.py}`
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (seams S-BS-7 / S-BS-27 / S-BS-31)
- Prior cycle: `.devloop/sessions/session-bench-salvage-phaseWS-6c-2026-06-01.json` + critique `critique-bench-salvage-phaseWS-6c-2026-06-01.md` + handoff `HANDOFF_bench-salvage_phaseWS-6c-DSPy_kickoff_2026-06-01.md`
- Next in track: **WS-6c-AGENTIC** (LangGraph recompose + grade-seam wire; S-BS-31-GATED) → **WS-6d** persistence (+**WS-6d-KB**) → **WS-6e** ETLP

---

## Hardness

- [x] **HARD GATE** — establishes the DSPy-judge ↔ ported-consensus contract (the §6 hybrid seam) that WS-6c-AGENTIC builds on, and authors prompt text feeding safety-critical judges. Lower blast radius than WS-6c (no consensus-math change, no grade-wire), but the seam-conformance + consensus-unchanged proofs (A1/A4) are load-bearing and an independent read of the judge prompts earns its keep. Fresh-critic at close recommended (`KICKOFF_CRITIC.md`); **user MAY elect inline** (precedent: WS-6c).
