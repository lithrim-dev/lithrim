# Driver — `bench-salvage` phase `S-BS-74`: the LIVE grounding-correction demo (floor-locus)

> **Generated by the monitor in a planning session. The executor pastes the
> KICKOFF block (top) into a fresh Claude Code session, reads the rest, posts
> plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseS-BS-74-live-correction-demo-driver`
> **Version:** v1
> **Authored:** 2026-06-08
> **Last re-verified against code:** 2026-06-08 (HEAD `63d4d65`; per MONITOR §Phase 1a — recon `ac9a1c501480c3455` + monitor spot-greps. **CORRECTION 2026-06-08 (executor plan-review caught it):** a monitor spot-grep MISREAD `OptimizeRequest.confirm` (`app.py:236`) as a run-eval cost-gate; `RunEvalRequest` (`:224-229`) has NO `confirm` field. The recon's original "no confirm field" was RIGHT. See §1.)
> **Hardness:** HARD GATE (a live paid attestation that a deterministic floor CORRECTS a confidently-wrong LLM judge and FLIPS the composite verdict — a labels-true-by-construction claim; touches the core invariant; fresh-critic at close).

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase S-BS-74 (the LIVE grounding-correction demo, floor-locus).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                             (your role)
  2. .devloop/prompts/bench-salvage_phaseS-BS-74_live-correction-demo_driver.md (this doc)
  3. CLAUDE.md                                                                  (the core invariant: labels true by construction; the snapshot is the contract)
  4. lithrim_bench/harness/grounding.py:398-508 (ground), :118-175 (PresenceCheck), :426 (suppress_decls)
  5. lithrim_bench/harness/ontology.py:86-92 (rescore) + lithrim_bench/harness/report.py:81-92 (composite)
  6. examples/proof_case.jsonl (the clean-negative TEMPLATE: bench_scribe_v1_clean_negative_aaecd73c3bcf) + tests/test_uap3b_withstands.py:257-337 (the MOAT EXHIBIT flip pattern)
  7. apps/bff/app.py:224-229 (RunEvalRequest — agent/live/in_process, NO confirm) + :276-331 (POST /v1/run-eval, no API cost-gate) + docs/research/RUN_uap5a_a8_live_2026-06-06.json (the live over-fire+suppress, run 8a41ef3e)
  8. .devloop/state/STREAM_bench-salvage.md (open seams: S-BS-70/73/74/75) + .devloop/templates/PROOF_CAPSULE_TEMPLATE.md

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: bench-salvage-phaseS-BS-74-live-correction-demo-driver
Driver doc: .devloop/prompts/bench-salvage_phaseS-BS-74_live-correction-demo_driver.md
```

---

## 0. What this cycle is (and is NOT)

**Is:** the visceral, LIVE "watch grounding correct a confidently-wrong judge → the verdict flips" demo. The deterministic floor + withstands-gate is ALREADY BUILT and proven OFFLINE/$0 (the MOAT exhibit) and LIVE-SAFE (suppression fired live in run `8a41ef3e`, but on a multi-defect case where it couldn't flip the verdict). S-BS-74 engineers a **by-construction CLEAN-NEGATIVE** case where the live prompt-council over-fires `MEDICATION_NOT_IN_TRANSCRIPT` on a medication that is **verbatim in the transcript**, the post-hoc `ground()` PresenceCheck disproves+suppresses it, and — because nothing else of HIGH/MEDIUM severity is active — the composite verdict flips **BLOCK→PASS / reject→approve**, attested LIVE.

**Locus = FLOOR (decided by the user, do NOT re-litigate):** the LIVE attestation targets the post-hoc `ground()` floor on the **prompt-council** (`live=true, in_process=false` → `:8002`) — the over-fire that already reproduced live (`8a41ef3e`), best odds. The OFFLINE flip test (D2) proves BOTH loci (floor + the pre-consensus withstands-gate) deterministically.

**Is NOT a release-blocker; honest-Δ is the law.** Feasibility is LOW–MEDIUM (the live trio is trigger-happy on scribe artifacts; it may over-fire other codes or null-code hallucinations that hold the block). **If the live flip does NOT reproduce, it ships as a DOCUMENTED LOSS + an honest proof capsule — NEVER a manufactured win** (CLAUDE.md; `self-asserting-loop-honesty-moat`; the PROOF_CAPSULE template). The mechanism is already proven; this is pitch-upside.

---

## 1. Pre-flight reading (ordered)

1. `CLAUDE.md` — "Core invariant: labels are true by construction" + "Taxonomy snapshot is the contract". The new case must be admissible: a clean-negative ⇒ `injection_recipes: null`, `expected_safety_flags: []`, `expected_compliance_verdict: approve`. The over-fire is a genuine LLM **false positive** the floor corrects — never a manufactured defect.
2. `lithrim_bench/harness/grounding.py` — `ground()` :398; `PresenceCheck` :118-175 (suppresses `"X not in transcript"` only on a **positive** presence match, never on a failed extraction); `suppress_decls`/`_CONTRACT_EXECUTORS` :426/:290; `GroundedResult{active, suppressed, skipped_non_gradeable, floor_blocks}` :52-87.
3. `lithrim_bench/harness/ontology.py:86-92` `rescore()` (severity-driven: `block_at_or_above=0.5`, MEDIUM=0.5, HIGH=1.0 → a single active MEDIUM/HIGH ⇒ BLOCK) + `lithrim_bench/harness/report.py:81-92` `composite()`.
4. `examples/proof_case.jsonl` — the clean-negative TEMPLATE `bench_scribe_v1_clean_negative_aaecd73c3bcf` (keys: `case_id, agent_type, artifacts, clean_negative, expected_safety_flags, expected_compliance_verdict, expected_artifact_verdict, expected_owner_map, injection_recipes, ground_truth_basis, generated_at`). `zidovudine 300 MG Oral Tablet` is present verbatim in both the med list and the transcript — the canonical presence-disprovable scaffold.
5. `tests/test_uap3b_withstands.py:257-337` — `test_MOAT_EXHIBIT_gate_flips_composite_and_ground_alone_does_not` (the gate-locus double-assertion) + the consensus-flip; `tests/test_uap5a_flip_demo.py:68-95` — the offline injected-predictor pattern.
6. `data/ontology/clinical_v1.json:385-419` — the ONE committed suppress contract (`presence_check` for `MEDICATION_NOT_IN_TRANSCRIPT`). **This is sufficient — do NOT add a contract.**
7. `apps/bff/app.py:224-229` — `RunEvalRequest{agent, live, in_process}` (**NO `confirm` field**) + :276-331 `POST /v1/run-eval` (**NO API-level cost gate** — `live=true` is itself the explicit paid opt-in; replay/`$0` is the default). The `confirm` 422-gate (`:232-236` `OptimizeRequest` + `:761-768`) is on the OPTIMIZE route, not run-eval. `docs/research/RUN_uap5a_a8_live_2026-06-06.json` — the live over-fire+suppress evidence (run `8a41ef3e`, `live=true`).
8. `.devloop/state/STREAM_bench-salvage.md` (open seams S-BS-70/73/74/75) + `.devloop/templates/PROOF_CAPSULE_TEMPLATE.md` (honest-Δ only).

> **⚠️ CITATION NOTE (corrected 2026-06-08, executor plan-review):** an earlier monitor note here claimed `RunEvalRequest` carries a `confirm` cost-gate. **That was a misread and is RETRACTED** — `RunEvalRequest` (`app.py:224-229`) is `{agent, live, in_process}` with **NO `confirm` field**; the `confirm: bool = False` at `:236` belongs to `OptimizeRequest`, and the only 422-without-confirm gate (`:761-768`) is on `/v1/judges/{role}/optimize`. `run_eval_endpoint` has **no** confirm check. So the LIVE paid run is fired via `POST :8787/v1/run-eval {agent, live:true}` — **no `confirm`** (a phantom `confirm:true` is silently ignored). `/v1/run-eval` has no API-level cost gate; `live=true` IS the explicit paid opt-in (replay/`$0` is the default), and cost safety is OPERATIONAL: `curl /health` first, the `$0` dry-run green first, exactly ONE deliberate run. The shell's in-DOM modal (S-BS-69) gates the UI path; the API path trusts the explicit `live=true`.

> **Citation discipline:** every file:line above was re-grepped at HEAD `63d4d65`. If you spot further drift on read, halt and surface it in plan-review.

---

## 2. Deliverables (file-by-file)

1. **`examples/s_bs_74_med_overfire_demo.jsonl`** (NEW file — additive, never edit existing packs) — ONE by-construction **clean-negative** case, schema mirroring `bench_scribe_v1_clean_negative_aaecd73c3bcf`:
   - `clean_negative: true`, `injection_recipes: null` (or `[]` per the template's exact shape — match it), `expected_safety_flags: []`, `expected_compliance_verdict: "approve"`.
   - The scribe **artifact** is engineered so the live council is LIKELY to raise `MEDICATION_NOT_IN_TRANSCRIPT` (e.g. the artifact's medication line is reworded/abbreviated/restructured enough that the LLM may judge it unsupported) **while the drug token stays verbatim in the transcript** (so `PresenceCheck` positively disproves it).
   - **Otherwise faithful:** no fabricated PMH/allergy/consent, no dosage drift, nothing that would draw another HIGH/MEDIUM coded finding OR a null-code HIGH hallucination (those survive `ground()` and would hold the BLOCK). The artifact must be spotless except for the one invitable MED-FP.
   - `ground_truth_basis` documents WHY it's clean (the construction note).

2. **`data/config/agents/s_bs_74_demo.json`** (NEW file — additive demo agent, à la `uap5a_flip_demo`; do NOT edit `ws0_default.json`) — an Agent whose `Dataset` points at the new case so `run_eval` grades it. EvalProfile uses the committed `clinical/1` ontology (which carries the presence_check contract). *(Plan-review decides: dedicated demo agent [preferred] vs the eval-pack path.)*

3. **`tests/test_s_bs_74_live_correction.py`** (NEW) — the OFFLINE deterministic flip (the live attestation's $0 twin), proving BOTH loci:
   - **Floor-locus (net-new, the live twin):** feed a council result with a single MEDIUM `MEDICATION_NOT_IN_TRANSCRIPT` finding (a faked over-fire) over the new case → assert `ground()` **OFF/bypassed** ⇒ active set non-empty ⇒ `rescore`/`composite` = BLOCK/reject; `ground()` **ON** ⇒ the MED finding in `suppressed`, `active==[]`, `composite` = PASS/approve. Suppression-attributable double-assertion.
   - **Gate-locus (reuse-assert):** confirm `tests/test_uap3b_withstands.py::test_MOAT_EXHIBIT_…` still GREEN (the pre-consensus withstands-gate flip is already covered — do not duplicate; assert it stays green as the second locus).
   - A **$0 dry-run helper** (open decision §3.3): run the new case's artifact through `ground()` with a faked MED over-fire predictor to CONFIRM the active-set-drops-to-empty math BEFORE any paid run.

4. **`docs/research/RUN_s_bs_74_live_2026-06-08.json`** (NEW, from D-LIVE) — the captured live run blob (the council result + the `ground()` suppression record + the before/after verdict). Produced by ONE cost-gated paid run (§5 A-LIVE).

5. **`docs/research/PROOF_bench-salvage_S-BS-74_2026-06-08.md`** (NEW) — the proof capsule per `.devloop/templates/PROOF_CAPSULE_TEMPLATE.md`. **Honest-Δ only:** flip = the win, documented; no-flip = the loss, documented (what over-fired that held the block; the mechanism still stands on the offline twin + `8a41ef3e`). Optional: a zyng-narrated video (`out/zyng_narrate/`) — offline `$0`.

---

## 3. Plan-review checkpoint (non-negotiable)

Post a plan with: Understanding (one paragraph) · file-by-file changes · test plan · risks · proposed deviations. **Wait for "go".** Resolve these open decisions IN the plan-review:

1. **Case wiring (D2):** a dedicated additive demo agent (`s_bs_74_demo.json`, preferred — mirrors `uap5a_flip_demo`) vs the eval-pack path. Confirm it does NOT edit `ws0_default.json` or any frozen seed.
2. **Artifact-engineering approach:** HOW the artifact reliably invites the live MED-FP while staying otherwise-spotless and keeping the drug token verbatim in the transcript. Name the specific rewording lever (abbreviation? a restructured med section? a paraphrase the LLM mis-reads as unsupported?). This is the load-bearing construction risk.
3. **$0 dry-run BEFORE the paid run** (`live-reassess-before-driver-lock`): confirm via a faked-over-fire `ground()` replay that suppressing the lone MED finding drops `active` to empty → composite flips. Spend the paid run ONLY after the math is proven.
4. **The no-iterate honesty rule:** if the live trio does not over-fire MED (or over-fires other codes that hold the block) on the FIRST 1–2 paid runs, **accept the honest loss — do NOT iterate paid runs hunting for a flip** (cost + manufacturing risk). State the budget cap (≤ ~$2–3, each run ~$0.10–0.40) and the stop rule up front.
5. **SPLIT confirmation:** `kb_grounding`-contract enrichment + any multi-code disprovable axis are explicitly OUT (would edit the FROZEN `clinical_v1.json` + trip the seam-freeze; a separate follow-on). S-BS-74 = one new clean-negative + one paid run.

---

## 4. Scope guardrails — NOT in scope

- **`data/ontology/clinical_v1.json`** — BYTE-UNTOUCHED. The existing `presence_check` contract is sufficient. Do **NOT** add a `kb_grounding` (or any) contract — it would trip the seam-freeze (`tests/_seam_freeze.py` / `test_uap3b_withstands.py` frozen-set pin) and widen scope. → SPLIT follow-on.
- **`taxonomy/taxonomy_snapshot.json`** — BYTE-UNTOUCHED (the contract).
- **The frozen consensus seam** — `compliance_council._apply_consensus`, the per-judge seam dict (`judges_dspy.py`), `judge_metric.py` `LENS_BY_ROLE`, committed seeds (`ws0_default.json`, committed predictor seeds), `council_roles/` — BYTE-UNTOUCHED (asserted by the seam-freeze tests). This cycle ADDS a case + a demo agent + a test; it changes NO grading logic.
- **Existing golden rows** — never edit/relabel a row in `proof_case.jsonl` / `uap5a_flip_demo.jsonl` / `judge_calib_v1.jsonl`. The new case is an additive, lint-GREEN append to a NEW jsonl.
- **The withstands-gate / grounding engine code** — REUSE as-is. No new suppress mechanism. If the floor doesn't flip, that's data, not a code gap.
- Drive-by formatting / dependency bumps / "while I was here" edits.

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS (a documented honest LOSS on A-LIVE still satisfies the cycle — see A-LIVE):

- **A1 — admissible case.** `scripts/lint_golden_against_taxonomy.py --golden examples/s_bs_74_med_overfire_demo.jsonl` exits 0; the case is a clean-negative (`expected_safety_flags: []`, `injection_recipes` null, `expected_compliance_verdict: approve`). True by construction.
- **A2 — offline floor-flip (net-new), non-vacuous.** `tests/test_s_bs_74_live_correction.py` proves: a single MEDIUM `MEDICATION_NOT_IN_TRANSCRIPT` over-fire on the new case ⇒ `ground()` OFF = BLOCK, `ground()` ON = PASS (MED in `suppressed`, `active==[]`). Suppression-attributable (the verdict change is caused by the suppression, not the case).
- **A3 — both loci proven offline.** A2 (floor) green + `test_uap3b_withstands.py::test_MOAT_EXHIBIT_…` (gate) still green.
- **A4 — green bar, no regression.** Full suite — canonical `PYENV_VERSION=debuglithrim python -m pytest -q` (baseline the 2 pre-existing S-BS-96 observation failures — read the count in the MAIN checkout, not a worktree) + plain `python -m pytest -q`; ruff clean on touched files. No new failures.
- **A5 — frozen-seam 0-delta.** `git diff <parent>..HEAD` touches NO file in the frozen set (§4); `taxonomy_snapshot.json` + `clinical_v1.json` byte-untouched.
- **A-LIVE — ONE cost-gated live attestation (honest-Δ).** Fire `POST :8787/v1/run-eval {agent: s_bs_74_demo, live:true}` (**NO `confirm`** — run-eval has no API cost-gate; `live=true` is the explicit paid opt-in. Services UP — `curl :8002/health` + `:8787` first; do NOT autostart). Capture `RUN_s_bs_74_live_2026-06-08.json`. **PASS = an HONEST result, recorded either way:** WIN = the live council over-fired MED, `ground()` suppressed it, composite flipped BLOCK→PASS (the visceral demo → proof capsule); LOSS = it didn't flip (the council didn't over-fire MED, or over-fired/hallucinated something else that held the block) → documented honestly in the capsule, mechanism still stands on A2 + `8a41ef3e`. **A manufactured win = FAIL.** Stop after ≤1–2 paid runs (≤ ~$2–3); do NOT iterate for a flip.

### Diagnostic stats (NOT gates)
- Whether the live flip reproduced (win/loss) — reported in the capsule; NOT a gate (the gate is HONESTY).
- The live council's full finding set on the new case (what else fired) — reported; NOT a gate.

---

## 6. Commit structure + message templates

Atomic, pathspec-only (the shared dirty branch `bench-salvage/ws6c-dspy` carries a foreign `root.jsx` + 3 untracked — `git commit -- <your files>`, never bare). Suggested order:

1. **`feat(s-bs-74): by-construction clean-negative MED-overfire demo case + demo agent`** (D1 + D2)
2. **`test(s-bs-74): offline floor-flip (non-vacuous) + both-loci proven`** (D3)
3. **`docs(s-bs-74): live attestation run blob + honest proof capsule`** (D-LIVE + D5; the capsule states win OR loss honestly)

Each commit body ends with:
```
Executed per bench-salvage-phaseS-BS-74-live-correction-demo-driver by session-2026-06-08-N.
See .devloop/sessions/session-bench-salvage-S-BS-74-2026-06-08.json

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
```

---

## 7. Verification checklist (run at end, before the session log)

- [ ] A1–A5 PASS + A-LIVE recorded honestly (win OR documented loss)
- [ ] All commits exist; working tree clean except the known foreign files
- [ ] Tests pass + linters clean (record commands; baseline S-BS-96 in the MAIN checkout)
- [ ] No file in the diff outside §2 deliverables; `clinical_v1.json` + `taxonomy_snapshot.json` byte-untouched
- [ ] No services autostarted (`curl /health` first; halt + ask if down)
- [ ] No pushes / publishes / tags
- [ ] Session log per `.devloop/templates/SESSION_LOG_TEMPLATE.json` (incl. the honest A-LIVE Δ)

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order (esp. the clean-negative TEMPLATE case + the MOAT exhibit test + `ground()`/`rescore`).
3. Re-read §2–§7 of this driver.
4. Post plan-review per §3 — RESOLVE the 5 open decisions (esp. #2 the artifact-engineering lever + #3 the $0 dry-run + #4 the no-iterate honesty rule).

---

## 9. References

- Seams: S-BS-74 (this cycle), S-BS-70 (the visceral live-flip, entangled), S-BS-73 (gate on-by-default, live-validated), S-BS-75 (pre/post-consensus PresenceCheck locus). `.devloop/state/STREAM_bench-salvage.md`.
- Prior live evidence: `docs/research/RUN_uap5a_a8_live_2026-06-06.json` (run `8a41ef3e` — over-fire+suppress LIVE, SAFE direction, no flip).
- Prior cycle critique (HARD GATE pattern): `.devloop/sessions/critique-bench-salvage-FLAG-1-2026-06-08.md`.
- Honest-Δ: `.devloop/templates/PROOF_CAPSULE_TEMPLATE.md`; memory `self-asserting-loop-honesty-moat`, `calibration-trainer-is-the-product`, `proof-capsule-convention`, `live-reassess-before-driver-lock`, `git-commit-pathspec-dirty-index`.
- Recon: agent `ac9a1c501480c3455` (2026-06-08; the feasibility read + reuse map this driver is built on).

---

## Hardness

- [x] **HARD GATE** — fresh-critic session required at close (focus: the new case is admissible/clean-negative + lint-green; the offline floor-flip is non-vacuous [suppression-attributable]; the frozen seam + `clinical_v1.json` + snapshot are byte-untouched; the A-LIVE Δ is HONEST — a documented loss is a pass, a manufactured win is a FAIL).
