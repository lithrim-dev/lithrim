# Driver — `bench-salvage` phase `PACK-2b`: un-freeze the council's owner-map (read `_TIER1_OWNERS` FROM the active pack)

> **Generated by the monitor 2026-06-10 from the user's "draft 2b and get it done" + a live code-scan reassessment (agent `a39572611af5948e2`) + monitor spot-verification at HEAD `92f1689`. PACK-1b un-froze the taxonomy (the 3 tier sets → `pack_tiers()`). PACK-2b is the direct analogue for the consensus owner-map: `_TIER1_OWNERS` resolves FROM the active pack's snapshot via the same inline-`__import__` carve-out. After 2b the FROZEN council's clinical DATA (taxonomy + owners) is fully pack-resolved.**
>
> **Bundle ID:** `bench-salvage-phasePACK-2b-council-owners-unfreeze-driver`
> **Version:** v1 · **Status:** READY (HARD GATE)
> **SCOPE NARROWED BY THE REASSESSMENT (the monitor owns this; it overrides the earlier "owners + roster" lean):** 2b = the `_TIER1_OWNERS` un-freeze **ONLY**. The `CouncilModel` roster and `LENS_BY_ROLE` are **deferred** — see §0 for why (the roster is an infra tangle, not a value-preserving carve-out; LENS is the moat's authority + the snapshot carries no lenses).

---

## KICKOFF (paste into a fresh executor rooted in lithrim-bench)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-2b
(healthcare-realm-as-pack, LAYER 2b: un-freeze the council's OWNER-MAP — make the FROZEN
compliance_council.py read _TIER1_OWNERS FROM the active pack's taxonomy_snapshot via the
SAME inline-__import__ carve-out PACK-1b used for the tier sets [pack_tier1_owners()],
flipping the source of truth to the pack. This is the DIRECT 1b analogue for the consensus
one-strike owner-map. OWNER-MAP ONLY — the CouncilModel roster (an infra tangle) and
LENS_BY_ROLE (the moat's authority; the snapshot has no lenses) are DEFERRED, OUT of scope).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-2b_council-owners-unfreeze_driver.md (this)
  3. The PROVEN 1b pattern you are REPEATING: git show 856deba -- lithrim_bench/harness/pack.py
     (the pack_tiers() accessor) + git show 1038f5f -- lithrim_bench/runtime/council/compliance_council.py
     tests/_seam_freeze.py (the _PACK_TIERS carve-out + the relaxed assert_compliance_council_carveouts_only)
     + git show 678afb9 -- scripts/seed_ontology.py (the S-BS-123 AST-reader re-point)
  4. The pre-flight §1 — esp. compliance_council.py:214-245 (_TIER1_OWNERS, the carve-out target) +
     :2077 (the consensus one-strike, the only live reader — MEMBERSHIP) + harness/pack.py:99-112
     (pack_tiers, the template) + :161-194 (council_roster, the AST-reader that BREAKS) +
     tests/_seam_freeze.py:150-218 (the guard you extend)
  5. CLAUDE.md (the taxonomy_snapshot is the contract; the owner-map is now read FROM it at runtime)

Then post plan-review. The SCOPE (owner-map only; roster + LENS deferred) is DECIDED — do NOT
re-litigate. Do NOT write code until the monitor says "go". HARD GATE — it edits the FROZEN
council + relaxes the freeze guard AGAIN. Plan-review resolves D-A (the carve-out + guard
extension) / D-B (the 2 AST-reader re-points: council_roster + seed_ontology) / D-C (the
fixture sentinel-owner for a POSITIVE non-vacuity signal) / D-D (behavior-0-delta proof) /
D-E (harden the S-BS-124 guard residual now, or carry).

Bundle ID: bench-salvage-phasePACK-2b-council-owners-unfreeze-driver
```

---

## 0. Why owner-map ONLY — the reassessment overrode the "owners + roster" prior
The reassessment (agent `a39572611af5948e2`) found the `CouncilModel` roster is **not** a 1b-shaped carve-out:
- Each `CouncilModel(...)` (`:467-498`) tangles **clinical identity** (`name`/`prompt_role`) with **deployment infra** (`provider`, `model=settings.AZURE_OPENAI_DEPLOYMENT_*`, `supports_logprobs`) in one call; the v1/v2 branch carries two *different* rosters; there is no standalone identity list to swap.
- Un-freezing it means either moving infra into the pack (WRONG — infra ∉ a domain pack) or a `name→infra` lookup-table refactor of the frozen `__init__` (a different kind of change, not a value-preserving literal→accessor flip).
- The trio's clinical identity is **already** externalized as `snapshot.production_judges`; the runtime roster (`self.models`) is correctly an infra concern.

So the clean 2b = the **owner-map** un-freeze (the exact 1b analogue: a value-preserving literal→`pack_*()` carve-out, oracle-pinned, snapshot-equivalent). `LENS_BY_ROLE` defers too (it's in `judge_metric.py`, not the frozen file; it's the moat's sole authority; the snapshot has no lens data — un-freezing it needs a new pack surface = a separate cut). **After 2b the frozen council's clinical DATA is fully pack-resolved.** The roster-infra-refactor + LENS-pack-surface are optional, lower-priority follow-ons (a future "2c"), NOT required to call the clinical-data un-freeze done.

---

## 1. Pre-flight reading (re-verify on open — citations grepped at authoring 2026-06-10, HEAD `92f1689`; the 1b carve-out shifted line numbers, these are CURRENT)
1. **The carve-out target:** `compliance_council.py:214` `_TIER1_OWNERS: Dict[str, set] = {…}` (8 entries, ends `:245`), with heavy clinical/S-BS-31 provenance comments (`:208-243`) — **preserve verbatim** (the 1b precedent: relocate to a comment block at the carve-out site, do not delete).
2. **The only LIVE reader (the 0-delta basis):** `compliance_council.py:2077` `owners = _TIER1_OWNERS.get(violation, set())` → `:2078` `if not owners or (first_model in owners):` — pure MEMBERSHIP (the consensus one-strike). Every other repo hit is a comment/docstring/import EXCEPT the 2 AST-readers (§1.5).
3. **The carve-out template (REPEAT it):** `harness/pack.py:99-112` `pack_tiers()` (reads `snapshot["tiers"]` directly, stdlib-only, ungated, acyclic) — 2b adds the owner twin. And `compliance_council.py:196` `_PACK_TIERS = __import__("lithrim_bench.harness.pack", fromlist=["pack_tiers"]).pack_tiers()` — the exact carve-out shape.
4. **The pack equivalence target:** `packs/healthcare/taxonomy_snapshot.json` `tier1_owners` (8 entries) — **EXACT match** to `_TIER1_OWNERS` (reassessment verified code-for-code, 0 mismatches). `taxonomy.py:25,67` already loads it into `Taxonomy.tier1_owners`.
5. **The AST-readers that BREAK (must re-point atomically — the S-BS-123 pattern, now expected):**
   - `harness/pack.py:188-189` `council_roster()` — `ast.literal_eval(node.value)` on the `_TIER1_OWNERS` literal → **RAISES** when it becomes a `pack_tier1_owners()` call. Re-point the OWNER leg to read `snapshot["tier1_owners"]` (the `CouncilModel`-name AST-collect at `:172-179` STAYS — the roster is still a literal; do not break it).
   - `scripts/seed_ontology.py:207` `ast.literal_eval(_module_assign(source, "_TIER1_OWNERS"))` → **RAISES**. 1b re-pointed this function's TIER read to the snapshot (`:197`) but left `_TIER1_OWNERS` council-sourced (the `:195-196` comment says "still a council literal (2b)"). Re-point the owner read to the snapshot too (the function already loads it at `:197`).
   - `scripts/snapshot_taxonomy.py:107` — reads the **UPSTREAM BACKEND** council (`backend_path/app/services/...`), not this repo, not the snapshot it writes → **NOT circular, NO change**. (Confirm this on open; do not "fix" it.)
6. **The guard you extend:** `tests/_seam_freeze.py:150-163` `_COUNCIL_AUTHORIZED_MARKERS` + `_COUNCIL_REQUIRED_CARVEOUTS`; `:166-218` `assert_compliance_council_carveouts_only` + the pure predicate `assert_council_carveouts_only`. (Already generically named from 1b — 2b extends the constants, no rename.) **The docstring `:171` carries a STALE `:485-516` roster citation — fix it to `:467-498`.**
7. **The determinism net + moat:** `runtime/council/tests/test_consensus.py` (the one-strike oracle — membership-driven, survives a value-preserving flip); `signals.py`/`withstands.py` read **only** `LENS_BY_ROLE`, **never** `_TIER1_OWNERS` (reassessment confirmed — neither imports `compliance_council`) → owners-2b touches the CONSENSUS, NOT the moat; deferring LENS leaves the withstands-gate 100% untouched. The lens⊆owners invariant `tests/test_trio_dspy.py:317` is **value-stable** (the 8 Tier-1 lens codes' roles are all owners in the snapshot; `snapshot.tier1_owners == _TIER1_OWNERS`).

---

## 2. The seam map (what 2b touches — owner-map only)

| Site | Today | After 2b |
|---|---|---|
| `compliance_council.py:214-245` | `_TIER1_OWNERS = {…}` literal | resolved from `pack_tier1_owners()` via inline `__import__` (1 hunk; values + the `:2077` membership read 0-delta) |
| `harness/pack.py` (new) | `_pack_taxonomy_codes`/`pack_tiers` | + `pack_tier1_owners()` (snapshot `tier1_owners` → `dict[str, frozenset]`) |
| `harness/pack.py:188-189` `council_roster()` | AST `literal_eval` of `_TIER1_OWNERS` — **BREAKS** | the owner leg reads `snapshot["tier1_owners"]`; the `CouncilModel`-name leg unchanged |
| `scripts/seed_ontology.py:207` | AST `literal_eval` of `_TIER1_OWNERS` — **BREAKS** | reads `tier1_owners` from the snapshot (already loaded at `:197`) |
| `tests/_seam_freeze.py:150-163` | 2 carve-outs (prompts-dir + tiers) | + `_TIER1_OWNERS`/`pack_tier1_owners` marker + the new required carve-out signature (3 carve-outs); fix the stale `:485-516` docstring |
| `tests/test_pack_layer2b.py` (new) | — | equivalence + non-vacuity (mirror `test_pack_layer1b.py`) |
| `packs/_tiers_fixture/taxonomy_snapshot.json` | `tier1_owners: {}` | a SENTINEL owner entry (positive non-vacuity signal — D-C) |

**FROZEN / OUT (deferred or untouched):** the `CouncilModel` roster (`:467-498`), `LENS_BY_ROLE` (`judge_metric.py:110`), `_apply_consensus`, the tier-set carve-out (1b — untouched), the ontology, the moat (`signals`/`withstands`).

---

## 3. Deliverables (2b)
**D1 — add `pack_tier1_owners()`.** `harness/pack.py`: `pack_tier1_owners(pack=None) -> dict[str, frozenset[str]]` reading the active pack's `snapshot["tier1_owners"]` (JSON arrays → frozensets). Mirror `pack_tiers()` EXACTLY (stdlib-only, ungated, no `taxonomy.load_taxonomy()` call → no gate re-entry; the council imports it during its own module import).
**D2 — the council carve-out.** `compliance_council.py:214-245`: replace the literal with `_TIER1_OWNERS: Dict[str, set] = dict(__import__("lithrim_bench.harness.pack", fromlist=["pack_tier1_owners"]).pack_tier1_owners())` (the `dict()` wrap keeps the `Dict[str, set]` shape; frozenset values are membership-0-delta at `:2077` — confirm no reader mutates, grep-verified the only read is `:2077`). Preserve the clinical/S-BS-31 provenance comments verbatim. **One authorized hunk.** Nothing else in the file changes.
**D3 — re-point the 2 breaking AST-readers (atomic with D2).** `council_roster()` (the `_TIER1_OWNERS` leg → snapshot; the roster-name leg stays) + `seed_ontology.parse_tiers_and_owners` (owner read → snapshot). Update their docstrings. `snapshot_taxonomy.py` = NO change (verify it reads the upstream backend).
**D4 — relax the freeze guard (extend, don't rewrite).** `tests/_seam_freeze.py`: add `"_TIER1_OWNERS"` + `"pack_tier1_owners"` to `_COUNCIL_AUTHORIZED_MARKERS`; add `'__import__("lithrim_bench.harness.pack", fromlist=["pack_tier1_owners"]).pack_tier1_owners()'` to `_COUNCIL_REQUIRED_CARVEOUTS`; update the docstring (3 carve-outs now) + **fix the stale `:485-516` → `:467-498`** roster citation. Non-vacuity preserved both directions (the lower-bound now requires all 3 carve-out signatures).
**D5 — the equivalence + non-vacuity tests.** New `tests/test_pack_layer2b.py` mirroring `test_pack_layer1b.py`: (a) `[council]`-env equivalence — imported `_TIER1_OWNERS == snapshot.tier1_owners == load_taxonomy().tier1_owners` (8 entries, owner-sets == the `acc4973` literal); (b) the subprocess non-vacuity under `_tiers_fixture` (the council's `_TIER1_OWNERS` follows the pack — D-C sentinel); (c) the relaxed-guard tamper + revert tests (unauthorized edit fails; reverting the owners carve-out fails; the tiers + prompts carve-outs still required); (d) the consensus oracle (`test_consensus.py`) green unchanged; (e) the lens⊆owners invariant green with owners-pack-resolved.
**D6 — docs.** `SPEC_PLUGIN_ARCHITECTURE` Layer-2b entry (the owner-map un-freeze; roster/LENS deferred + why); note the snapshot's `tier1_owners` is now load-bearing at runtime. (CLAUDE.md already notes the snapshot is runtime-load-bearing from 1b — extend only if needed.)

---

## 4. Plan-review — the load-bearing decisions
- **D-A (carve-out + guard).** The `dict(__import__(...).pack_tier1_owners())` one-hunk carve-out + the 3-carve-out guard extension. Confirm exactly 1 NEW hunk vs `acc4973` (so 3 total: prompts-dir + tiers + owners), each marker-checked.
- **D-B (the 2 AST-reader re-points).** `council_roster()` (owner leg only — don't break the roster-name collect) + `seed_ontology` (owner read → snapshot). Confirm `snapshot_taxonomy.py` is genuinely the upstream-backend reader (no change).
- **D-C (fixture sentinel-owner).** Add a sentinel `tier1_owners` entry to `packs/_tiers_fixture/taxonomy_snapshot.json` so the subprocess non-vacuity proves a POSITIVE "follows the pack" signal (not the empty `{}` it has today). *Monitor lean: yes — a positive signal is stronger.*
- **D-D (behavior-0-delta proof).** The consensus oracle green unchanged + resolved `_TIER1_OWNERS` == `acc4973` literal == snapshot == `load_taxonomy().tier1_owners` + the `:2077` membership unchanged + lens⊆owners green.
- **D-E (S-BS-124 — harden now or carry).** The guard's inside-an-authorized-hunk residual (a marker-bearing hunk can hide an extra line) — *monitor lean: harden it now* (3 carve-outs is the right point to tighten before any further un-freeze; e.g. bound each authorized hunk to ONLY the accessor-assignment lines). Acceptable to carry as S-BS-124 if the hardening proves fiddly — surface at plan-review.

## 5. Scope guardrails — NOT in scope
- **The `CouncilModel` roster** (`:467-498`) — DEFERRED (infra tangle; identity already in `production_judges`). Do NOT touch `__init__` or `self.models`. (`council_roster()`'s roster-name AST-collect must keep working — the roster stays a literal.)
- **`LENS_BY_ROLE`** (`judge_metric.py:110`) — DEFERRED to a 2c (moat authority; no snapshot lens data). Do NOT touch `judge_metric.py` lens defs.
- **`_apply_consensus`** + the consensus logic — untouched (only the owner-map SOURCE changes).
- **The 1b tier-set carve-out** (`:196`), the ontology, the moat (`signals`/`withstands`) — untouched.
- **No re-snapshot / no owner-VALUE change** — 2b is a source flip; the 8 owner entries stay identical.

## 6. Acceptance
- **A1.** The council resolves `_TIER1_OWNERS` FROM the active pack (no hardcoded literal body at `:214`); `import compliance_council` works in `[council]`; `import lithrim_bench.harness.pack` stays heavy-dep-free.
- **A2.** **Behavior 0-delta:** the consensus oracle (`test_consensus.py`) green unchanged; resolved `_TIER1_OWNERS` == `acc4973` literal == snapshot `tier1_owners` == `load_taxonomy().tier1_owners` (8 entries); the `:2077` one-strike membership unchanged; the lens⊆owners invariant green.
- **A3.** **Non-vacuity both directions:** (i) `LITHRIM_BENCH_PACK=_tiers_fixture` → the council's `_TIER1_OWNERS` follows the pack (D-C sentinel, disjoint from the real 8); (ii) the relaxed guard FAILS on an unauthorized council edit AND on reverting the owners carve-out (the tiers + prompts carve-outs still required).
- **A4.** **Frozen-set 0-delta (deferred + moat):** the `CouncilModel` roster, `_apply_consensus`, `LENS_BY_ROLE`, `signals.py`, `withstands.py`, the ontology, the 1b tier carve-out — byte-unchanged; surviving freeze guards green (`assert_judges_dspy_consensus_seam_frozen`, `assert_clinical_ontology_seam_frozen`, `assert_council_roles_relocated_only`).
- **A5.** Green both envs (default py3.12 + `debuglithrim` py3.10.15), **0-new at parent** (`92f1689`; the S-BS-117/119 worktree caveat — the live-key tests fail in a bare worktree but pass credentialed, both 0-new; judge by 0-new-at-parent); ruff clean; pathspec-only; NOT pushed.

## 7. Hardness
- [x] **HARD GATE** — fresh-critic required, `isolation:'worktree'` (re-spawn on infra stall, per PACK-5a/5b/1b). *Reason: the 3rd substantive edit inside the FROZEN `compliance_council.py` + it relaxes the freeze guard again. The critic verifies: the carve-out is value-preserving (resolved == `acc4973` literal == snapshot), behavior 0-delta (the consensus oracle), the relaxed guard still non-vacuous (unauthorized edit + each carve-out revert all fail), the 2 AST-reader re-points correct + complete (re-grep for a 3rd owner-literal reader), the roster/LENS/moat byte-frozen, and the source genuinely reads from the pack (the fixture-pack subprocess).*

## 8. References
- [[healthcare-realm-as-pack]] (the layered plan; 2b = the council owner-map un-freeze; roster/LENS deferred) · the PROVEN PACK-1b driver + `critique-bench-salvage-PACK-1b-2026-06-10.md` (the pattern + the S-BS-123 AST-reader lesson) · `SPEC_PLUGIN_ARCHITECTURE` · CLAUDE.md. Reassessment: agent `a39572611af5948e2` (2026-06-10), monitor-spot-verified (`_TIER1_OWNERS:214`, consensus `:2077`, guard `:150-218`).
