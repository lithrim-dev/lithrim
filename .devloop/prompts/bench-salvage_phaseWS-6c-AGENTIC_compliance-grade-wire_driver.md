# Driver, `bench-salvage` phase `WS-6c-AGENTIC`: compliance grade-wire milestone

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-6c-AGENTIC-compliance-grade-wire-driver`
> **Version:** v1
> **Authored:** 2026-06-02
> **Last re-verified against code:** 2026-06-02 (per MONITOR §Phase 1a)

---

## Re-scope note (read first)

This phase was **re-scoped against current code** before authoring. The §7
plan's AGENTIC headline ("recompose `compliance_workflow` → straight-line
async fns") is **already built** as the parked M1 in-process spine
(`8f9beae`): `runtime/pipeline/orchestrator.py:187`
(`PipelineOrchestrator.evaluate() -> PipelineResult`), `stages.py` (the 7
recomposed stages), `backends/local_pipeline.py:42` (`LocalPipelineBackend`).
So WS-6c-AGENTIC is **not** a greenfield recompose. It is the **compliance
grade-wire milestone**: resolve the S-BS-31 gate, upgrade the M1 spine from
v1 to the ported v2 trio, verify the behavior contracts survive, and wire the
in-process council into the harness grade seam so the council scores real
cases for the first time. The **observation_workflow + 6 KPI agents recompose
is split out to `WS-6c-OBS`** (greenfield; not in the compliance grade path).

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-6c-AGENTIC
(compliance grade-wire milestone).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                            (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-6c-AGENTIC_compliance-grade-wire_driver.md  (this doc)
  3. docs/specs/RECOMPOSITION_PLAN_ws6.md  §4 (recompose-in-process + the behavior contracts), §5 (lockstep recompute trap), §6 (the hybrid + grade seam), §7 (AGENTIC row), §8 risk 1 (the regression oracle)
  4. .devloop/state/STREAM_bench-salvage.md                                   (phase table + seams S-BS-1..33 + the S-BS-31 decision)
  5. .devloop/sessions/HANDOFF_bench-salvage_phaseWS-6c-AGENTIC_kickoff_2026-06-02.md   (the carried-forward context)

ROOTED IN lithrim-bench. One cross-repo edit only: the S-BS-31 fix (deliverable
D0) edits ../lithrim-backend on mvp-ready, then re-ports to the bench. Everything
else is in-bench.

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)",
resolving the 6 plan-review decisions in §3. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseWS-6c-AGENTIC-compliance-grade-wire-driver
Driver doc: .devloop/prompts/bench-salvage_phaseWS-6c-AGENTIC_compliance-grade-wire_driver.md
```

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review:

1. `docs/specs/RECOMPOSITION_PLAN_ws6.md`, §4 (recompose-in-process + the 4 behavior-preservation contracts), §5 (the IP-port lockstep-recompute trap), §6 (the hybrid + the grade-seam swap, narrative point 4), §7 (the AGENTIC row), §8 risk 1 (regression oracle = by-construction packs + replay-baseline diff). **Note the re-scope above: the compliance recompose already exists; §4/§7 describe it as future. A §4/§7 amendment ships with this cycle's monitor close-out, do not re-build what `runtime/pipeline/` already has.**
2. `lithrim_bench/runtime/pipeline/orchestrator.py:187`, `PipelineOrchestrator.evaluate()`; the existing straight-line async compliance pipeline you extend (NOT rebuild).
3. `lithrim_bench/runtime/pipeline/stages.py`, the recomposed stages; `:28` already imports the ported `..council.compliance_council`.
4. `lithrim_bench/backends/local_pipeline.py:42`, `LocalPipelineBackend`; M1 docstring pins `COMPLIANCE_COUNCIL_VERSION=v1`, structural/artifact skipped, retrieval stubbed. This is the v1→v2 upgrade target.
5. `lithrim_bench/runtime/council/compliance_council.py`, `_TIER1_OWNERS:232` (the S-BS-31 fix site, bench copy), v2 trio construction `:463`, `_apply_consensus:1853`, Tier-1 escalation `:2050-2110`, `_recompute` lockstep parity targets.
6. `../lithrim-backend/app/services/compliance_council.py` (mvp-ready), `_TIER1_OWNERS:232` (the S-BS-31 IP source of truth); the workflow reference `../lithrim-backend/app/workflows/compliance_workflow.py` (7 nodes; the behavior contracts).
7. `lithrim_bench/harness/grade.py` (`grade_replay:40`, `grade_live:85`, the `:8002` POST `:121`) + `scripts/run_eval.py:101` (`run()`) + `lithrim_bench/harness/report.py:29` (`composite`), the frozen grade seam you wire the in-process path behind.
8. `.devloop/state/STREAM_bench-salvage.md`, seams S-BS-31 (DECIDED: option (a) reassign-by-domain), S-BS-32 (resolves with it), S-BS-33 (inert until KB), S-BS-24 (suite not a green gate).

> **Citation discipline:** every file:line above was re-grepped 2026-06-02. If you spot drift on read, halt and surface it (per EXECUTOR.md §"Citation drift"). **One known DRIFT is already recorded:** §4's contract-2 node names `check_hipaa_compliance`/`evaluate_artifacts` do **not** exist in the live `compliance_workflow.py`, it has 7 nodes (`retrieve_context, run_safety_prescreening, run_confidence_gate, run_council, extract_evidence, store_report, handle_error`). Re-derive the fatal/non-fatal map from those 7 (see D2).

---

## 2. Deliverables (file-by-file)

### D0, S-BS-31 fix (the gate; DECIDED option (a) reassign-by-domain). Cross-repo.

The orphaned-Tier-1-owner decision is **made** (monitor + user, 2026-06-02):
reassign by clinical domain so single-judge one-strike is restored for the 3
codes orphaned under v2. **This is a per-repo two-commit deliverable** (backend
IP source of truth first, then the byte-faithful re-port to bench).

1. **`../lithrim-backend/app/services/compliance_council.py:232`** (`_TIER1_OWNERS`), add the v2-trio owners:
   - `"MISSING_ALLERGY"`: add `"faithfulness_judge"` **and** `"risk_judge"` (fidelity + patient-safety; mirrors `FABRICATED_ALLERGY` co-ownership). Keep the existing declared-but-not-running owners.
   - `"VALUE_MISMATCH"`: add `"faithfulness_judge"`.
   - `"FABRICATED_CONSENT"`: add `"policy_judge"` (consent is policy's domain per `risk_judge.txt:26` + `policy_judge.txt:12`).
2. **`../lithrim-backend/app/prompts/council_roles/faithfulness_judge.txt`**, add `VALUE_MISMATCH` to the emit list (it covers the *concept* at `:44-50` but does not name the code; `MISSING_ALLERGY` is already at `:14`, no change needed there).
3. **`../lithrim-backend/app/prompts/council_roles/policy_judge.txt`**, add `FABRICATED_CONSENT` to the emit list (consent scope is named at `:12`; the code is not).
4. **`lithrim_bench/runtime/council/compliance_council.py:232`**, re-port the `_TIER1_OWNERS` change byte-faithfully (PORT-not-rewrite; keep the bench copy a faithful mirror of the backend). Re-port the two prompt files into `lithrim_bench/runtime/council/council_roles/`.
5. **`taxonomy/taxonomy_snapshot.json`**, re-snapshot via `scripts/snapshot_taxonomy.py --backend-path ../lithrim-backend` so `tier1_owners` reflects the new residency; lint stays GREEN. **Do not hand-edit.**

Closes **S-BS-31**; **S-BS-32** (the 3 RED packager/triage tests) goes green with it.

### D1, Upgrade the in-process spine v1 → v2 trio

6. **`lithrim_bench/backends/local_pipeline.py:42`** (`LocalPipelineBackend`), stop forcing `COMPLIANCE_COUNCIL_VERSION=v1`; drive the **v2 Azure trio** (the ported default; `settings.py:50` already defaults `v2`, trio built at `compliance_council.py:463`). Supply the Azure deployment config the trio needs (`AZURE_OPENAI_DEPLOYMENT_MISTRAL_LARGE_3`, `AZURE_OPENAI_DEPLOYMENT_LLAMA_4_MAVERICK`). Un-skip the structural and artifact stages **per plan-review decision #1** (the M1 backend skips both; the milestone scope for un-skipping is a plan-review call, semantic-only-v2 first vs full structural+artifact).
7. **`lithrim_bench/runtime/pipeline/orchestrator.py:187` / `stages.py`**, only the changes the v2 trio + un-skipped stages require; **no consensus-math edits** (the ported `_apply_consensus`/veto/tables stay byte-identical, the §6 invariant).

### D2, Verify the behavior contracts survive the recompose (no silent verdict drift)

8. **`lithrim_bench/runtime/pipeline/orchestrator.py` / `stages.py`**, confirm (and complete where missing) the §4/§5 contracts in the existing recompose, each checked against the backend reference `../lithrim-backend/app/workflows/compliance_workflow.py`:
   - **Two-phase disposition** (§4 #1): preliminary `_determine_disposition` in `run_council` (`compliance_workflow.py:859`, the "preliminary disposition without evidence_spans / recomputed in extract_evidence" note ~`:955`) then recompute in `extract_evidence` (`:1034`, recompute ~`:1211`). Preserve the ordering.
   - **Fatal vs non-fatal errors** (§4 #2; **contract DRIFTED, re-derive**): the live workflow has 7 nodes; map which node failures are swallowed best-effort (HIPAA retrieval in `retrieve_context`; artifact eval) vs fatal (routed to `handle_error:1514`). Do **not** trust the stale `check_hipaa_compliance`/`evaluate_artifacts` names.
   - **Council concurrency** (§4 #3): the `COMPLIANCE_COUNCIL_MAX_CONCURRENT_LLM` semaphore (`compliance_council.py:58-60`, bench copy) survives.
   - **Eval isolation** (§4 #4): the `::eval::<eval_run_id>` `context_hash` upsert behavior (`compliance_workflow.py:1372`) is preserved through the in-process path (provenance stays `NoOpProvenanceStore` this cycle; real store is WS-6d).
   - **Gate safety policy** (§5): P0-3 artifact-guard (`compliance_workflow.py:767`, artifacts ALWAYS escalate to council), FAST PATH (`:782`, gate can ONLY approve), regex protocol checks (`:722`). Preserve in the recomposed gate stage.
   - **Lockstep recompute** (§5 CRITICAL): if the in-process path uses `_recompute_consensus_from_council` / `_recompute_final_verdict` (`compliance_workflow.py:2206`/`:2281`), they must mirror `_apply_consensus` exactly incl. BRS-3 None-confidence tolerance.

### D3, Wire the in-process council into the grade seam

9. **`lithrim_bench/harness/grade.py`**, add a `grade_inprocess(case, ...)` (sibling to `grade_replay:40` / `grade_live:85`) that runs `LocalPipelineBackend` (v2) and returns the **same `PipelineResult` dict shape** so `ground()`/`composite()` stay path-agnostic (the §6/§7 frozen-contract property).
10. **`scripts/run_eval.py:101`** (`run()`), add the in-process path selector (a `--in-process` flag or backend selector alongside `--live`/replay), returning the same record shape. **`report.composite` (`report.py:29`) and the §10 BFF stay untouched.**

### D4, Regression oracle (the §8-risk-1 guard against silent verdict drift)

11. **`lithrim_bench/runtime/pipeline/tests/` (or `tests/`)**, diff in-process **v2** verdicts against the captured `:8002` replay baselines (`tests/fixtures/ws0/baseline.*.json`) over the by-construction packs **including `examples/proof_case.jsonl`** (which now exercises the S-BS-31 codes `MISSING_ALLERGY` + `VALUE_MISMATCH`). The diff is the contract-preservation proof: verdict + findings + confidence must reproduce (per memory `eval_reproducibility_framing`, verdict/findings/confidence, not byte-identical reasoning). Where the v2 in-process verdict legitimately differs from a v1-era baseline, document why (it is a v1→v2 delta, not a regression).

### D5, Session log

12. **`.devloop/sessions/session-bench-salvage-phaseWS-6c-AGENTIC-2026-06-02.json`**, the S-BS-31 before/after (single-judge one-strike restored on the 3 codes; S-BS-32 green), the v2-trio in-process verdict on proof_case, the contract-preservation diff-vs-replay-baseline, the grade-wire proof (PipelineResult shape unchanged), and the one live Azure smoke verdict + cost.

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with Understanding, file-by-file
changes, test plan, risks, and any proposed deviations. **Resolve these 6 decisions:**

1. **Un-skip scope (D1).** Drive v2 **semantic-only first** (structural/artifact stay skipped, fastest path to "council scores real cases"), or un-skip structural (`etlp-mapper :3031`, the permanent sidecar) + artifact this cycle? (Recommended: semantic-only-v2 first; structural/artifact a fast-follow if the packs need them.)
2. **Grade-wire mechanism (D3).** A `run_eval.py --in-process` flag returning the same record (recommended, smallest), or a backend-selector abstraction over `grade_live/replay/inprocess`?
3. **Replay-baseline coverage (D4).** Which captured `:8002` baselines are the regression set, and how to treat legitimate v1→v2 deltas (document-and-accept vs re-capture v2 baselines).
4. **Contract-2 re-derivation (D2).** Confirm the fatal/non-fatal node map read off the live 7 nodes (the §4 names are stale).
5. **Live-smoke scope + cost (D5).** Exactly ONE cost-confirmed live Azure v2-trio call (confirm the envelope before the call; the WS-6c-DSPy single call was \$0.0067, the WS-6c trio smoke ~\$0.15-0.25). User is LLM-cost-conscious: offline/replay-first.
6. **Persistence posture.** Confirm `NoOpProvenanceStore` stays this cycle (real repository interface is WS-6d) while the `::eval::` isolation contract is still honored in-memory.

Wait for "go" before writing code.

---

## 4. Scope guardrails, NOT in scope

- **NO observation_workflow / 6-KPI-agent recompose**, that is the split-out **`WS-6c-OBS`** phase (greenfield; not in the compliance grade path).
- **NO change to the ported consensus IP**, `_apply_consensus` / `_compose_council_verdict_v2` / `_worst_of_verdicts` / `extract_verdict_confidence` / tables stay byte-identical (the §6 invariant: a recompose/upgrade must never weaken a Tier-1 rule). D0 edits `_TIER1_OWNERS` **data**, not the consensus functions.
- **NO persistence swap / repository interface / Mongo** (WS-6d); **NO KB/RAG/Pinecone/ONNX** (WS-6d-KB). Provenance stays `NoOpProvenanceStore`.
- **NO DSPy judge changes** (WS-6c-DSPy shipped `risk_judge`; the remaining two judges are a separate track). The grade-wire is judge-impl-agnostic (the §6 hybrid wraps DSPy/imperative identically).
- **NO §10 BFF / `report.composite` / `run_eval.run` signature change**, wire the in-process path **behind** the frozen seam, same `PipelineResult` shape.
- **NO S-BS-33 resolution**, the `citations_used` seam-doc gap stays inert until KB lands (WS-6d-KB).
- **S-BS-24 is not a green gate**, the backend `not slow` suite is RED pre-existing; do not chase it. The bench default-deps suite has S-BS-32 (resolved by D0).
- Drive-by formatting / dependency bumps / "while I was here" edits. The orchestrator/stages already exist, **extend, do not rewrite.**

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A0 (S-BS-31 closed).** After D0, a single v2 judge firing `MISSING_ALLERGY` / `VALUE_MISMATCH` / `FABRICATED_CONSENT` with grounded evidence **one-strike rejects** (no longer downgrades to `needs_review`); verify via the offline consensus oracle + a unit test per code. **S-BS-32**'s 3 packager/triage tests go GREEN — but NOT all from the re-snapshot alone *(fresh-critic correction 2026-06-02)*: the re-snapshot greens only `test_multi_defect_worst_of_verdict` (the `MISSING_ALLERGY` `ValueError`). `test_single_defect_pack` (WRONG_DOSAGE) + `test_missed_escalation` (MISSED_ESCALATION) fail an `AssertionError` because they hard-code the now-dormant `behavior_judge` as an expected production owner — fix is a **principled test-assertion update** to expect the v2 production owner (`risk_judge`, which owns+emits both), NOT a broadening of D0's reassignment; the assertion must keep its discriminating power (still require the correct owner). Verify: `pytest tests/test_packager.py tests/test_triage_pack.py` + the council oracle.
- **A1 (v2 in-process).** `LocalPipelineBackend` runs the **v2 trio** in-process and produces a council verdict on `examples/proof_case.jsonl` (no `:8002`, no Celery). Verify via the in-process path.
- **A2 (contracts preserved).** The two-phase disposition, the (re-derived) fatal/non-fatal map, the concurrency semaphore, the `::eval::` isolation, and the gate safety policy all hold in the recomposed orchestrator, demonstrated by the D4 diff-vs-replay-baseline reproducing verdict+findings+confidence (legitimate v1→v2 deltas documented).
- **A3 (grade-wired).** `run_eval.run` can score a case through the in-process v2 council and the record flows through `ground`→`composite` unchanged; `PipelineResult` shape identical to `grade_live`/`grade_replay`. This is the first time the council scores real cases through the harness. Verify end-to-end on one case (per memory `validate_via_single_e2e`).
- **A4 (consensus unchanged).** `git diff` over the ported consensus functions/tables vs the WS-6c-DSPy tip = **zero logic delta** (only `_TIER1_OWNERS` data + path/config changes). Recorded in the session log.
- **A5 (one live smoke).** Exactly ONE cost-confirmed live Azure v2-trio call: in-process verdict on one case matches the offline/replay expectation (record verdict + per-judge confidences + cost). Mistral `confidence=None` tolerated end-to-end.
- **A6 (scope held).** No observation/KPI recompose; no persistence/KB; no consensus-math edit; no §10/`report.composite` change; orchestrator/stages extended not rewritten; the only backend edit is the D0 `_TIER1_OWNERS` + 2 prompt files.
- **A7 (default install).** `pytest -q` green on default deps (dspy tests `importorskip`-skip); `ruff check .` clean.

### Diagnostic stats (NOT acceptance gates)

- in-process v2 verdict distribution vs the captured `:8002` replay baselines (v1→v2 delta summary), reported, not a gate.
- per-judge confidences + the live-smoke cost, reported, not a gate.
- proof_case score (recipe=label) under the in-process v2 path, reported, not a gate (calibration is report-only per §8 risk 6).

---

## 6. Commit structure + message templates

Atomic, **per-repo** commits (MONITOR pref #5, no cross-repo single commit). Suggested order:

1. **`fix(council): reassign 3 orphaned Tier-1 owners to the v2 trio (S-BS-31)`**, in `../lithrim-backend` (mvp-ready): `_TIER1_OWNERS` + the 2 prompt emit-lines. Body: the S-BS-31 decision (option a, reassign-by-domain, monitor+user 2026-06-02); the evidence (faithfulness emits MISSING_ALLERGY; consent is policy's domain); closes S-BS-31.
2. **`feat(council): re-port the v2 Tier-1 owner residency + re-snapshot taxonomy`**, in `lithrim-bench`: the byte-faithful re-port + `snapshot_taxonomy.py` re-run; closes S-BS-32.
3. **`feat(pipeline): drive the in-process spine on the v2 trio`**, D1.
4. **`feat(harness): wire the in-process council into the grade seam`**, D3.
5. **`test(pipeline): contract-preservation diff vs :8002 replay baselines`**, D2 + D4.

Each bench commit body ends with:
```
Executed per bench-salvage-phaseWS-6c-AGENTIC-compliance-grade-wire-driver by session-2026-06-02-N.
See .devloop/sessions/session-bench-salvage-phaseWS-6c-AGENTIC-2026-06-02.json
```
(Add the user's Co-Authored-By trailer if used.)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance A0-A7 PASS (per §5)
- [ ] All commits exist (both repos); working trees clean
- [ ] Tests pass + `ruff` clean (record commands used); S-BS-32 GREEN
- [ ] No file in the diff outside §2 deliverables (scope held); orchestrator/stages extended not rewritten
- [ ] No observation/KPI recompose leaked (WS-6c-OBS)
- [ ] No consensus-math edit; `PipelineResult` / `report.composite` / `run_eval.run` signature unchanged
- [ ] No services autostarted; no pushes / publishes / tags (user preferences)
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order; confirm the re-scope (the M1 spine exists) on your own read.
3. Re-read §2-§7 of this driver.
4. Post plan-review per §3, resolving the 6 decisions.

---

## 9. References

- Spec: `docs/specs/RECOMPOSITION_PLAN_ws6.md` §4/§5/§6/§7/§8 (with the M1-spine amendment shipping this cycle) + `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (the frozen-contract rule)
- Stream state: `.devloop/state/STREAM_bench-salvage.md` (S-BS-31 DECIDED; S-BS-32/33; S-BS-24)
- Handoff: `.devloop/sessions/HANDOFF_bench-salvage_phaseWS-6c-AGENTIC_kickoff_2026-06-02.md`
- Prior cycle session log: `.devloop/sessions/session-bench-salvage-phaseWS-6c-DSPy-2026-06-02.json`
- Prior cycle critique: `.devloop/sessions/critique-bench-salvage-phaseWS-6c-DSPy-2026-06-02.md`
- The ported council: `lithrim_bench/runtime/council/` (consensus byte-frozen below the §6 seam)
- The existing in-process spine: `lithrim_bench/runtime/pipeline/` + `lithrim_bench/backends/local_pipeline.py`

---

## Hardness

- [ ] Routine cycle, monitor performs inline critique at close
- [x] **HARD GATE**, **fresh-critic session REQUIRED** (genuinely separate session, `KICKOFF_CRITIC.md`).

**Why fresh-critic is mandatory here (not user-electable inline):** this is the
cycle where the council **first scores real cases** through the grade seam. The
genuinely-fresh-critic independence was deliberately deferred from WS-6c (inline,
user-elected) and WS-6c-DSPy (inline-by-continuation) precisely so it lands here,
where a fidelity miss in the recompose or the S-BS-31 fix would score real cases
wrong. Trigger via `/devloop-critique bench-salvage WS-6c-AGENTIC` in a fresh session.
