# Driver — `bench-salvage` phase `PACK-DIST-2`: finish the CE/pack clinical split

> **Generated by the monitor in a planning session (scoped via the `pack-dist-2-scope`
> workflow, ground-truthed against both repos). The executor pastes the KICKOFF block
> into a fresh Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phasePACK-DIST-2-finish-ce-pack-clinical-split-driver`
> **Version:** v1
> **Authored:** 2026-06-14
> **Last re-verified against code:** 2026-06-14 (scope workflow `wpdecwvhk`)
> **Hardness:** **HARD-GATE** (fresh-critic required — see bottom)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-DIST-2
(finish the CE/pack clinical split).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-DIST-2_finish-ce-pack-clinical-split_driver.md  (this doc)
  3. CLAUDE.md — the PACK-DIST-1/2 sections + the moat byte-freeze invariant
  4. conftest.py — the root demarcation ledger (the move+shrink must be exact against it)
  5. lithrim_bench/harness/pack.py — the discovery seam + the moat-safe carve-out boundary

Then post your plan-review per EXECUTOR.md. Do NOT write code until I say "go".
Two repos: lithrim-bench (CE) + ../lithrim-pack-healthcare (PACK). Commit each repo
separately with `git commit -- <paths>` (dirty-index hazard). Neither repo is pushed.

Bundle ID: bench-salvage-phasePACK-DIST-2-finish-ce-pack-clinical-split-driver
```

---

## 0. North star + the split

PACK-DIST-1 made the clinical realm physically external (`../lithrim-pack-healthcare`).
PACK-DIST-2 **finishes** the split: CE still carries ~20 clinical scripts (10 default to
gone in-repo paths), ~22 clinical test modules (skip-when-absent via the conftest ledger),
the `ws0` clinical house-fixture that ~21 plumbing tests hardcode, a runbook ambiguity, and
a non-hermetic standalone-CE subprocess. **Done = CE is genuinely clinical-free on
scripts+tests+fixtures, the pack repo is self-contained + CI-green, the canonical invocation
is locked, and bare-CE has zero over-skip / zero collection errors.**

**THE MOAT IS NOT TOUCHED.** All work sits ABOVE the frozen discovery seam — in `scripts/`
defaults, test location, fixtures, docs, and pack-repo CI. `compliance_council.py` /
`_apply_consensus` / `signals.py` withstands read stay byte-frozen vs `acc4973`.

**SPLIT decision (locked):** the history-preserving git subtree/filter-repo extraction is
**OUT of scope → gated PACK-DIST-3.** Rationale: the move renamed paths, the pack repo
already has 5 orphan TOOL-2 commits with no CE ancestry, and a history rewrite touching
shared seam files risks the byte-frozen-moat hash attestations. Provenance is archaeology,
not load-bearing — gate it alone.

**Order (deliverables are mostly independent; this is the safe sequence):**
`D1` + `D2` + `D3` can run early/parallel (none need the pack repo) → `D4` (move scripts) →
`D5` (move tests + shrink ledger, **lockstep**, after D3+D4) → `D6` (pack CI, after D4+D5) →
`D7` (docs triage, anytime). Each `Dn` is one logical commit unit; `D4`/`D5` span both repos.

---

## 1. Pre-flight reading (ordered)

1. `CLAUDE.md` — PACK-DIST-1/2 sections (discovery seam, what relocated, what's deferred, the moat invariant)
2. `conftest.py` — the root ledger: `_clinical_content_present`/`_healthcare_discoverable`, `_IGNORE_MODULES_WHEN_RELOCATED`, `_IGNORE_MODULES_WHEN_BARE_CE`, `_RELOCATED_FUNCS`, `_NEEDS_PACK_FUNCS`, `pytest_ignore_collect`/`pytest_collection_modifyitems`
3. `lithrim_bench/harness/pack.py` — `active_pack()` (~83), `_pack_root()` (~109), `assert_codes_known()` (~263), `assert_pack_licensed()` + R-GUARD (~362-397), `pack_taxonomy_path(pack=None)` (~403)
4. `README.md` — the Open-core fenced block (~line 24-36): the canonical invocation to lock + the caveat to add
5. `tests/test_standalone_ce.py` — `_run_subprocess` (~60-81) for the S-BS-139 env fix; the council conftest leak at `lithrim_bench/runtime/council/tests/conftest.py:16` it currently depends on
6. `tests/fixtures/standalone/case._core_fabricated_claim.jsonl` — the PROVEN neutral content-review pattern to model the new `_core` house fixture on
7. `tests/fixtures/ws0/` (case + baseline + a5_live + README) — the clinical house fixture being genericized; the baseline STRUCTURE the `_core` baseline clones
8. `../lithrim-pack-healthcare/README.md` + `pyproject.toml` — the pack repo's local-is-SSOT install note + entry point (`healthcare=healthcare`); the basis for D6 CI
9. `../lithrim-pack-healthcare/tests/test_specificity_flip.py` + `test_snomed_grounding.py` — the pack-repo path convention (`parents[1]/healthcare`, `parents[1]/examples`) the moved tests follow
10. `scripts/snapshot_taxonomy.py` + `scripts/lint_golden_against_taxonomy.py` — the `--out`/`--snapshot` defaults (~75-79 / ~33-37) re-pointed in D1
11. Monitor memory: `clinical-ontology-reseed-owner-drift` (do NOT full-re-seed `seed_ontology.py`) · `git-commit-pathspec-dirty-index` (always `git commit -- <paths>`) · `debuglithrim-bff-extra-httpx-align` (the `[bff]`/dspy env for the gated relocated tests)
12. `tests/test_6bclean_seam_guard.py` + `test_6bclean_attestation.py` + `test_pack_dist.py` — the moat-freeze + clinical-free-data-surface attestations to re-run after every deliverable

> **Citation discipline:** verify each `file:line` on read; halt + surface drift.

---

## 2. Deliverables (file-by-file)

### D1 — Re-point the 3 CE-resident generic admissibility tools off gone paths
`scripts/snapshot_taxonomy.py` · `scripts/lint_golden_against_taxonomy.py` · `scripts/build_label_owner_matrix.py` (+ verify `scripts/optimize_judge.py --corpus`)
- These 3 stay in CE (generic engines) but hardcode `REPO_ROOT/packs/healthcare/taxonomy_snapshot.json` (gone). Fix: default the arg to `None`, let the discovery seam resolve it.
- `lint_golden` + `build_label_owner_matrix`: default `--snapshot` to `None`, call `load_taxonomy(args.snapshot or None)` (`load_taxonomy(None)` already resolves via `pack_taxonomy_path()`).
- `snapshot_taxonomy.py`: default `--out` to `None`, resolve via `pack.pack_taxonomy_path('healthcare')` → writes to `../lithrim-pack-healthcare/healthcare/taxonomy_snapshot.json` under `LITHRIM_BENCH_PACKS_DIR`. The preserve-prior-fields logic at `args.out.exists()` survives.
- **Document the new fail-closed mode:** a bare-CE run now `FileNotFoundError`s instead of silently writing a gone dir — CORRECT.

### D2 — Lock the canonical invocation + make standalone-CE hermetic
`README.md` · `tests/test_standalone_ce.py` · `lithrim_bench/harness/pack.py` (optional)
- **S-BS-138 (doc):** README fenced block (~line 28) already has `LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare LITHRIM_BENCH_PACK=healthcare python -m pytest`. LOCK it as THE canonical line + add a one-line caveat: PACKS_DIR-alone leaves the active pack on `_core` → the council binds `_core` codes at import → 12 healthcare modules fail collection with `PackConsistencyError` (fail-closed-correct). Doc-only.
- **S-BS-139 (test):** in `_run_subprocess` (env dict ~66-67, currently only `LITHRIM_BENCH_PACK`) add `env.setdefault('OPENAI_API_KEY','test-offline-key')` + `env.setdefault('LITHRIM_LLM_PROVIDER','openai')` (**setdefault**, so a real live-smoke env wins). Removes dependence on the council-conftest leak / suite order / repo-root `.env`. **Do NOT lazy the FROZEN council client.**
- **OPTIONAL S-BS-138 clearer-error:** in `assert_codes_known` (pack.py ~263, stdlib-only, above the seam, NOT a council inline-`__import__` carve-out target → moat-safe): when `extra` is non-empty AND `active_pack() != pack`, append a hint naming the missing `LITHRIM_BENCH_PACK={pack}` pin (pure message text; grep the `'declares taxonomy codes'` literal in tests first).

### D3 — Genericize the `ws0` house-fixture to a neutral `_core` fixture behind one chokepoint
NEW: `tests/fixtures/_core/{case._core_house.jsonl, baseline._core_house.json, ontology._core_house.json, README.md}` · `tests/_house_fixture.py` · re-mint `tests/fixtures/ws4a/corpus.example.ndjson` · genericize ~18 plumbing tests (`test_uap3_bff`, `test_ws5_bff`, `test_uap5b_chat`, `test_chatbind_active_agent`, `test_chatbind2_pane`, `test_ws2`, `test_ws4a`, `test_audit`, `test_onb0_memory`, `test_asafe_tool_gate`, `test_uap5c_journey`, `test_uap3b2_grounding_check`, `test_ws6d`, `test_uap3_grade`, `test_byoc_provider`, …) · `conftest.py` (drop now-green funcs from `_NEEDS_PACK_FUNCS`)
- Model the `_core` fixture on the proven `tests/fixtures/standalone/case._core_fabricated_claim.jsonl`: (1) a domain-neutral content-review case (FABRICATED_CLAIM by-construction + a generic source-facts field for `presence_check`); (2) a baseline cloning the `ws0` STRUCTURE with a NEW fixed `HOUSE_RUN_ID`, **one standing finding (verdict=BLOCK) + one suppressible finding** (so `grounded_adjustments` is non-empty — the suppression-shape parity 5 tests need); (3) a fixture ontology = `packs/_core` flags + ONE generic `presence_check` contract (PresenceCheck is core-generic, reused unchanged); (4) README.
- `tests/_house_fixture.py` is THE chokepoint: exports `HOUSE_CASE_ID`/`HOUSE_FIXTURES_DIR`/`HOUSE_CASE_PATH`/`HOUSE_BASELINE_PATH`/`HOUSE_ONTOLOGY_PATH`/`HOUSE_RUN_ID` + a `house_agent(name, **overrides)` factory. Every genericized test imports from it instead of redefining `CASE_ID=`/`FIXTURES=`/`ONTOLOGY_SEED=` locally.
- Genericize the PLUMBING funcs (run-id/verdict round-trip/audit-shape/suppression-wired); KEEP genuinely-clinical funcs (authored-assignment grades, `domain=='clinical'`, `flags==23`, the FABRICATED_HISTORY demo-pair flip) as RELOCATED/NEEDS_PACK.
- **FIX the latent bug:** `test_ground_floor1` ERRORS at collection (module-level `load_pack_floors()` → `None._decode_artifact_soap`) despite its ignore entry — defer the module-level pack load so the ignore fires before import (body stays MOVE-to-pack in D5).
- Independent of the pack repo; runnable in parallel with D1/D2. **Must be dual-mode green** (bare CE + dev-discoverable).

### D4 — Move the ~17 clinical scripts into the pack repo + re-point constants
NEW `../lithrim-pack-healthcare/scripts/` ← `scripts/{seed_ontology, import_backend_cases, generate_judge_calib, generate_proof_case, generate_uap5a_flip_demo, generate_pack, run_ws0, run_local_scribe, build_fhir_*_pack, build_transaction_pack, build_canonical_pack, calibrate_canonical_pack, calibrate_judges, falsifier_fabricated_history_*}.py` (+ the paper-experiment/demo/HL7/onboarding scripts — see scope notes)
- `mkdir ../lithrim-pack-healthcare/scripts/` and move the clinical majority, re-pointing each script's `REPO_ROOT`-relative constants to the pack root. Confirmed clinical = reads `packs/healthcare/{ontology,council_roles,safety_flags_seed,taxonomy_snapshot}` or writes the relocated `examples/*.jsonl` or walks the Synthea cohort.
- `seed_ontology.py`: re-point `SAFETY_FLAGS_SEED_PY`/`ROLE_DIR`/`OUT_PATH`/`SNAPSHOT_PATH`; **do NOT full-re-seed in CE** (`clinical-ontology-reseed-owner-drift` sweeps additive `owner_roles`). Verify the byte-identical `ontology.json` rebuild after re-point.
- **LEAVE the generic entrypoints** in CE: `run_eval.py`, `run_eval_pack.py`, `analyze_runs.py`, `run_determinism.py`, `scripts/dev/*`. **Borderline (confirm reuse before moving):** `analyze_council_confidence.py`, `reconcile_golden.py`, `optimize_judge.py` — re-point/leave if they shape arbitrary run/golden NDJSON.

### D5 — Relocate RELOCATED-class clinical tests + shrink the CE conftest ledger (LOCKSTEP)
NEW `../lithrim-pack-healthcare/tests/conftest.py` · `conftest.py` (shrink) · MOVE whole modules (`test_judge_calib_corpus`, `test_ground_floor1`, `test_uap5a_flip_demo`, `test_uap5a_projection`, `test_s_bs_74_live_correction`, `test_uap4_corpus_superset` + `fixtures/uap4/` row, `test_ws0`, `test_ws1`, the bare-CE-ignored generator/floor modules `test_coding_pack`/`hl7_pack`/`scheduling`/`triage`/`*_injector`/`test_packager`/`verification/test_dosage_floor`/`test_toolbox`) · EXTRACT named RELOCATED funcs from MIXED modules (`test_audit`, `test_ws1`, `test_ws5_bff`, `test_pack_layer3`, `test_pack_layer5a/5b`) · MOVE `tests/fixtures/ws0/`
- NEW pack `tests/conftest.py` (session-scope): `os.environ.setdefault('LITHRIM_BENCH_PACK','healthcare')` — **without this, discovery-based tests silently grade `_core` and mis-pass** (non-vacuity test required). Optionally set `LITHRIM_BENCH_PACKS_DIR=parents[1]`.
- Re-point `parent.parent/examples → parents[1]/examples` and `packs/healthcare → parents[1]/healthcare` in moved modules.
- **GENERICIZE-on-move** `test_pack_layer1a/2`: their RELOCATED funcs assert the literal `'packs/healthcare'` in `DEFAULT_ONTOLOGY_PATH`/`_ROLE_PROMPTS_DIR` — STALE post-PACK-DIST-1; rewrite to assert `== pack._pack_root('healthcare')/…`, not the literal.
- **GUARD** the 3 byte-identity regen funcs (`test_uap4` regen, `test_pack_layer5a/5b` regen) skip-when-scripts/cohort-absent (they subprocess D4 scripts over `data/synthea_*`).
- **SHRINK** `conftest.py`: empty/delete `_IGNORE_MODULES_WHEN_RELOCATED` + `_RELOCATED_FUNCS` + the relocated branch in `pytest_ignore_collect`/`pytest_collection_modifyitems`. **KEEP `_NEEDS_PACK_FUNCS` + the `bare_ce` branch byte-unchanged.** Do NOT over-delete a NEEDS_PACK func — the frozen-seam zero-delta funcs in `test_uap3b2`/`test_uap3b_withstands` are NEEDS_PACK, NOT RELOCATED; `test_uap3_grade` has NO RELOCATED entry (do not move).
- **The ledger and the moves change together** — dead skip-markers or over-shrink (re-collecting moved tests that then ERROR) are the failure modes.

### D6 — Pack-repo CI + Makefile
NEW `../lithrim-pack-healthcare/{.github/workflows/ci.yml, Makefile}` · `pyproject.toml`
- Thin `Makefile` + `ci.yml`: `pip install -e ../lithrim-bench` (the CE core — local SSOT, a pinned ref or local path, **NOT** PyPI) → `pip install -e .` (registers the `healthcare` entry point) → `ruff check` → `pytest` (in-pack + the moved clinical tests via `LITHRIM_BENCH_PACKS_DIR=$PWD LITHRIM_BENCH_PACK=healthcare` pointed at a CE checkout).
- **Scope CI to the OFFLINE/deterministic suite first** (defer Hermes `:3031` / KB `:8002` live-service tests). `pyproject`: `[tool.pytest.ini_options] testpaths=['tests']` + declare dev deps (pytest + dspy/openai + the `[bff]` extra). Mirror the `importorskip` guards the source modules use.

### D7 — Triage the clinical docs residual
`docs/` triage (per-file, NOT a regex sweep) · relocate deeply-clinical artifacts to `../lithrim-pack-healthcare/docs/`
- **KEEP in CE:** all `docs/specs/*` (post-pivot domain-agnostic platform docs that merely cite clinical as an example).
- **RELOCATE** the deeply-clinical + dated artifacts (`SALVAGE_FINDINGS_*`, `LIVE_PIPELINE_SMOKE_*`, `PHASE1_CLOSE`, the ~30 `docs/research/RUN_*`/`REPORT_*`/`PROOF_*` clinical-run capsules). **`PAPER_OUTLINE.md`/`PAPER_FRAMING`/paper_draft — CONFIRM WITH USER** (may stay in CE for research framing).
- Lowest-risk; independent; anytime.

---

## 3. Plan-review checkpoint (non-negotiable)

Post understanding + file-by-file changes + test plan + risks + proposed deviations. Wait for "go". **Recommend executing in batches** (D1+D2+D3 first → re-gate → D4+D5 → re-gate → D6+D7) given the HARD-GATE.

---

## 4. Scope guardrails — NOT in scope

- **PACK-DIST-3** — the history-preserving subtree/filter-repo extraction + graft (rename-replay, history rewrite, reconciling the pack's 5 orphan TOOL-2 commits). Gated to its own phase.
- **Provisioning the Synthea cohort** into the pack repo (so the moved generators + 3 byte-identity regen tests can RUN there) — separate data-relocation; until then those regen tests guard-skip.
- **Re-snapshotting the taxonomy / editing `taxonomy_snapshot.json` content** — never hand-edit; D1 changes only WHERE `snapshot_taxonomy.py` writes.
- **Lazying/refactoring the `ComplianceCouncil` OpenAI-client** (compliance_council.py ~448) — FROZEN moat; the S-BS-139 fix stays at the test subprocess env.
- **Removing the council conftest's `OPENAI_API_KEY=test-offline-key` setdefault** — correct for the in-process oracle tests; the standalone fix stops DEPENDING on it.
- Publishing `lithrim-bench` to a public index; pack CI for the live-service (Hermes/KB) tests; rewriting dormant tests' grounding assertions (record_presence→snomed_subsumption); touching `run_eval.py`/`run_eval_pack.py` behavior or the active-pack pin MECHANISM; adding contract types to `packs/_core/ontology.json`; HPACK/new Pro content; frontend/shell changes.
- Drive-by formatting / dep bumps / "while I was here" edits. If something feels load-bearing-but-not-listed, halt + surface in plan-review.

---

## 5. Acceptance criteria (ALL must PASS)

- **A1 — Clinical-free scripts:** after D1+D4, `grep -rnE 'packs/healthcare|REPO_ROOT.*examples|REPO_ROOT.*taxonomy_snapshot'` over the scripts REMAINING in CE returns ZERO hardcoded gone-path defaults; every clinical pack-builder no longer lives in `lithrim-bench/scripts/`.
- **A2 — Clinical-free fixtures:** `git ls-files tests/fixtures/ | xargs grep -l 'bench_scribe_v1\|patient\|medication\|allergy\|SOAP\|HIPAA\|diabetes'` returns EMPTY; `tests/fixtures/ws0/` gone; `tests/fixtures/_core/` holds the neutral fixture; `grep -rn 'bench_scribe_v1_inject_condition_1bd0f10dc7b5' tests/*.py` returns only MOVE-tagged files (or empty).
- **A3 — Clinical-free tests + ledger:** after D5, `conftest.py` `_IGNORE_MODULES_WHEN_RELOCATED == set()` and `_RELOCATED_FUNCS == {}` (or the relocated branch deleted); `_NEEDS_PACK_FUNCS` byte-unchanged; the `bare_ce` skip path still works; no dead skip-markers; no re-collected-then-erroring test.
- **A4 — Bare-CE green, 0 over-skip:** bare CE (`LITHRIM_BENCH_PACK` unset, pack undiscoverable) runs pytest GREEN with the generic core suite + ZERO clinical collection ERRORS (`ground_floor1` ignore fixed); the previously-RED 8 plumbing tests now PASS on `_core`; clinical funcs are ABSENT (moved), not skip-stubs.
- **A5 — Dev-CE green:** `PYENV_VERSION=debuglithrim LITHRIM_BENCH_PACKS_DIR=../lithrim-pack-healthcare LITHRIM_BENCH_PACK=healthcare python -m pytest --co -q` collects with 0 errors; the full dev suite is green with moved files/funcs absent and every retained NEEDS_PACK func still running.
- **A6 — Pack repo self-contained + CI-green:** `cd ../lithrim-pack-healthcare && make test` (or the CI workflow) pip-installs the CE core + pack, runs `ruff check` + pytest GREEN on in-pack + moved tests via `LITHRIM_BENCH_PACKS_DIR`, with `LITHRIM_BENCH_PACK=healthcare` set by the new `tests/conftest.py`; moved hermetic/$0 tests pass offline + without the cohort; byte-identity regen funcs pass (scripts+cohort present) or SKIP (never error).
- **A7 — Non-vacuity:** at least one moved discovery-based test (e.g. the `ground_floor1` demo-pair flip) FAILS if `LITHRIM_BENCH_PACK` is unset in the pack conftest (proving the env is load-bearing); standalone-CE passes as a NARROW subset in a clean env with NO `OPENAI_API_KEY` and no repo-root `.env` (proving hermeticity, not the conftest leak).
- **A8 — Canonical invocation locked:** README names the canonical dev/CI line + the PACKS_DIR-alone caveat.
- **A9 — Moat untouched:** `git diff` shows ZERO changes to `compliance_council.py` / `_apply_consensus` / `signals.py` withstands; `test_6bclean_seam_guard` + `test_6bclean_attestation` + the frozen-seam zero-delta funcs stay GREEN; `test_pack_dist.py` A2 still passes (strengthened).
- **A10 — Docs triaged:** every `docs/specs/*` generic platform doc REMAINS in CE; deeply-clinical+dated artifacts relocated or user-confirmed-to-stay; `grep -rlE 'HIPAA|scribe|synthea' docs/` in CE returns only generic example-domain citations.
- **A11 — Scope hygiene:** `git diff --stat` in BOTH repos shows only this cycle's files; every commit uses `git commit -- <paths>`; no foreign content swept in.

### Diagnostic stats (NOT gates)
- bare-CE collected/skipped counts before→after; dev-CE collected count (target 676, 0 errors vs 596+12-PackConsistencyError with PACKS_DIR-alone); pack-repo test count.

---

## 6. Commit structure (atomic per Dn; both-repo deliverables commit each repo separately)

1. `chore(scripts): re-point snapshot/lint/owner-matrix off the gone in-repo taxonomy path (D1)` [CE]
2. `docs+test(ce): lock the canonical invocation + standalone-CE subprocess hermeticity (D2)` [CE]
3. `test(fixtures): neutral _core house fixture + chokepoint; genericize the plumbing tests (D3)` [CE]
4. `chore(scripts): relocate the clinical pack-builders to the pack repo (D4)` — [PACK: add] + [CE: remove], two commits
5. `test(dist): relocate the RELOCATED clinical tests + shrink the conftest ledger (D5)` — [PACK: add tests+conftest] + [CE: remove + shrink], two commits
6. `ci(pack): pack-repo CI + Makefile + pyproject test config (D6)` [PACK]
7. `docs(dist): triage the clinical docs residual (D7)` — [PACK: add] + [CE: remove], two commits

Each commit body: `Executed per bench-salvage-phasePACK-DIST-2-…-driver by session-YYYY-MM-DD.` + the Co-Authored-By trailer.

---

## 7. Verification checklist (end, before the session log)

- [ ] A1–A11 PASS (re-run the moat guard trio after EACH deliverable)
- [ ] Two pytest modes verified: bare CE + dev-discoverable; plus a scrubbed-env (no `.env`, no `OPENAI_API_KEY`) hermeticity check for A7
- [ ] Both repos: working tree clean; `git diff --stat` scoped; `git commit -- <paths>` used (no foreign files)
- [ ] No services autostarted; no pushes/tags
- [ ] Session log written per `SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `EXECUTOR.md` end-to-end. 2. Read §1 pre-flight in order. 3. Re-read §2–§7.
4. Decide the batch plan (recommend D1+D2+D3 → re-gate → D4+D5 → re-gate → D6+D7).
5. Post plan-review per §3. Wait for "go".

---

## 9. References

- Scope source: workflow `wpdecwvhk` (`pack-dist-2-scope`), 2026-06-14 — the ground-truthed per-area file lists + risks
- Seams: S-BS-135 (scripts), S-BS-136 (tests MOVE), S-BS-137 (`ws0` fixture), S-BS-138 (invocation), S-BS-139 (standalone hermeticity)
- Stream state: `.devloop/state/STREAM_bench-salvage.md`
- Prior cycle: `.devloop/sessions/session-bench-salvage-phaseGROUND-FLIP-2026-06-14.json` + `HANDOFF_bench-salvage_phaseGROUND-FLIP_kickoff_2026-06-14.md`
- CLAUDE.md PACK-DIST-1/2 sections

---

## Risks (carry into plan-review)

- **Conftest ledger drift (D5 — highest in-cycle):** the CE root conftest IS the auditable enumeration. Move without shrinking → dead skip-markers; over-shrink → re-collect moved tests that then ERROR. Lockstep. The frozen-seam zero-delta funcs are NEEDS_PACK, NOT RELOCATED — easy to over-delete.
- **Suppression-shape parity (D3):** 5 tests assert `grounded_adjustments`/a SUPPRESSED finding — the `_core` baseline+ontology MUST reproduce the two-finding shape (one standing→reject, one suppressible via the generic presence_check) or they regress.
- **Latent collection bug (D3):** `test_ground_floor1` module-level `load_pack_floors()` None-deref ERRORS at collection despite its ignore entry → aborts the whole pytest invocation. Defer the module-level load.
- **Active-pack env (D5):** discovery-based tests resolve `_core` unless `LITHRIM_BENCH_PACK=healthcare` is set; the new pack conftest MUST set it (non-vacuity test).
- **Deferred-script dep (D5↔D4):** 3 regen funcs subprocess D4 scripts over `data/synthea_*` (not in the pack repo) → ERROR unless guarded skip-when-absent OR D4 lands first.
- **Dual-mode green (D3):** genericized tests must pass in BOTH dev (healthcare pinned) AND bare CE.
- **setdefault not assignment (D2):** verify in a `.env`-free/scrubbed env, not the dev box where the repo-root `.env` masks the regression; `LITHRIM_LLM_PROVIDER=openai` must accompany the key.
- **Core-as-installed coupling (D6):** pack CI must reproduce the local-SSOT editable install (pinned ref/local path), not PyPI; the `[bff]`/dspy/openai extras must be present.
- **Script coupling (D4):** `seed_ontology.py`'s byte-identical rebuild can break on careless re-point — verify.
- **Docs over-relocation (D7):** per-file class triage only; a blanket sweep guts `docs/specs/*`. `PAPER_OUTLINE` — confirm with user.
- **Dirty-index hazard (all):** always `git commit -- <paths>`; verify scope with `git diff --stat` in both repos.

---

## Hardness

- [x] **HARD GATE** — fresh-critic session required. Spans two repos + the conftest demarcation ledger (the SSOT of the "CE is clinical-free" release invariant) + sits adjacent to the byte-frozen moat (must not edit it; the optional `assert_codes_known` hint must avoid the council inline-`__import__` carve-out targets). **Recommend a fresh-critic gate before D5's ledger surgery + D3's fixture authoring** (the canonical close-phase HARD-GATE critic, re-pointed to the mechanism anchor per GROUND-FLIP). Verify in two pytest modes + a scrubbed-env hermeticity check.
