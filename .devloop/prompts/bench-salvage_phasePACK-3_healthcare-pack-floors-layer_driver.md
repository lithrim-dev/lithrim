# Driver — `bench-salvage` phase `PACK-3`: healthcare-realm-as-pack, layer 3 (floors / verification-contracts → pack)

> **Generated by the monitor 2026-06-09, continuing the strangler-fig from PACK-2 ([[healthcare-realm-as-pack]]). This is the FIRST packs-as-CODE step: relocate the clinical grounding *executors* out of the core into the `healthcare` pack, behind a pack executor-registration interface, so the grounding engine is domain-agnostic and the floor layer of the boundary is grep-verifiable.**
>
> **Bundle ID:** `bench-salvage-phasePACK-3-healthcare-pack-floors-layer-driver`
> **Version:** v1 · **Status:** READY (HARD GATE)
>
> **⚠️ Scope decision RESOLVED (user, 2026-06-09): FULL layer-3 in one cycle** — the registration interface + relocate BOTH the suppress executor (`RecordPresence`) AND the floor executor (`dosage_grounding`/`DosageGroundingTool`) + refactor the inline `_run_floor` to registry-dispatch + merge both registries (moat-visible). Deliver a grep-clean grounding engine in one shot. (The 3a/3b split was NOT chosen.) The executor keeps the standard plan-review escape hatch: if the multi-file extraction genuinely cannot land green in one cycle, propose a split AT PLAN-REVIEW — but the DEFAULT is full.

---

## KICKOFF (paste into a fresh executor)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase PACK-3
(healthcare-realm-as-pack, LAYER 3: relocate the clinical grounding EXECUTORS out of the
core into the active `healthcare` pack behind a pack executor-registration interface, so the
grounding ENGINE [harness/grounding.py + the generic verification toolbox] is domain-agnostic
and the floor layer of the boundary is grep-verifiable. This is the FIRST packs-as-CODE step
[PACK-1/2 were packs-as-DATA]).

Read in this order:
  1. .devloop/personas/EXECUTOR.md
  2. .devloop/prompts/bench-salvage_phasePACK-3_healthcare-pack-floors-layer_driver.md (this)
  3. .devloop/prompts/bench-salvage_phasePACK-2_healthcare-pack-judges-layer_driver.md (the prior layer + the pack-resolution pattern)
  4. memory healthcare-realm-as-pack + conversational-first-core-plugin-line (the FE+BE plugin interface this realizes on the BE floor side)
  5. lithrim_bench/harness/grounding.py + lithrim_bench/verification/tools.py (the engine; the clinical executors to relocate)

Then post plan-review. The SCOPE (full layer-3, one cycle) is DECIDED. Plan-review resolves the
INTERFACE design (D-A registration shape / D-B the merge mechanism + the circular-import hazard /
D-C the verification/tools.py partition / D-D moat-visibility via signals.py / D-E green-throughout
+ the split escape-hatch). The LOAD-BEARING hazard: signals.py (THE MOAT, UAP-3b withstands-gate)
reads grounding._CONTRACT_EXECUTORS — the pack-merged registry MUST stay moat-visible and
behavior-identical. Do NOT write code until the monitor confirms the decisions + says "go".
This is HARD GATE — it touches the moat-visible registry + the verdict-feeding floor dispatch +
introduces the first in-process pack CODE (a trust-surface change).

Bundle ID: bench-salvage-phasePACK-3-healthcare-pack-floors-layer-driver
```

---

## 0. Why this cut (and why it's the first packs-as-CODE)

PACK-1 relocated the ontology+taxonomy (data); PACK-2 relocated the role prompts (data). The
floor-layer **contract DECLARATIONS** already moved with the ontology in PACK-1 (`verification_contracts`,
the additive `_seam_freeze` carve-out). What's still clinical-in-the-core is **executor CODE**:

- **`harness/grounding.py`** (658 lines, 46 clinical-symbol hits): `RecordPresence` (`:282-382`, the
  GROUND-FLOOR-1 *suppress* executor — grounds documented PMH vs `patient_profile.conditions` by SNOMED)
  + `_decode_artifact_soap` (`:448`) + the inline `dosage_grounding` branch in `_run_floor` (`:503`).
- **`verification/tools.py`** (765 lines, 19 clinical hits): `InRowTool` (`:110`, what `RecordPresence`
  wraps — `patient_profile.conditions`/`snomed_core`/`soap_pmh_items`) + `DosageGroundingTool` (`:171`)
  + the clinical extractors `extract_pmh_items` (`:63`) / `extract_plan_dose_tokens` (`:85`) / `_norm_dose` (`:81`).

The **generic engine stays core**: `ground()` (`:548`, the orchestrator), `_build_contract` (`:427`),
`_run_floor` (`:472`, the dispatcher — refactored to be registry-driven, not relocated), `PresenceCheck`
(`:119`), `KbGrounding` (`:179`), and in `verification/`: the `VerificationTool` base (`:41`), `StructuralJuteTool`,
`KbRagTool`, `RecordRagTool`, `JuteGenValidatorTool`, the `_dig`/`_norm` helpers, the Jute machinery.

**The two complications that make this bigger than PACK-2:**
1. **The moat reads the registry.** `runtime/council/signals.py:182-184` (the UAP-3b withstands-gate) consumes
   `grounding._CONTRACT_EXECUTORS` + `grounding._build_contract`. Any pack-registered executor MUST be visible
   to the moat, not just to `ground()` — and behavior-identically (A2/A4).
2. **Packs carry no code yet.** `harness/pack.py` resolves data *paths* only (`pack_ontology_path`/`_taxonomy_path`/`_prompts_path`).
   Layer-3 introduces the first executor-registration interface — the pack contributes in-process Python. For the
   OSS-core/Pro-pack model this is the BE half of `SPEC_PLUGIN_ARCHITECTURE`'s plugin interface (a real trust/packaging
   surface — flag it in docs; the security posture is a plan-review note, not a blocker for a local pack).

The win (same as PACK-1/2): `grep -riE 'snomed|pmh|dosage|patient_profile|soap_pmh' lithrim_bench/harness lithrim_bench/verification` → **empty** in the engine; the clinical executors live only under `packs/healthcare/`.

---

## 1. Pre-flight reading (re-verify on open — re-grep every line cite)
1. `lithrim_bench/harness/grounding.py` — `_CONTRACT_EXECUTORS` (`:412`, the suppress registry: presence_check/kb_grounding/record_presence) + `_FLOOR_CONTRACT_TYPES` (`:424`: structural_jute/jute_gen/dosage_grounding) + `_build_contract` (`:427`) + `_run_floor` (`:472-545`, the inline `if/elif` dispatcher) + `ground()` (`:548`) + `RecordPresence` (`:282-382`, **clinical, relocate**) + `_decode_artifact_soap` (`:448`, **clinical, relocate**) + `PresenceCheck` (`:119`, generic, STAY) + `KbGrounding` (`:179`, generic, STAY) + `_artifact_content` (`:440`, generic, STAY).
2. `lithrim_bench/verification/tools.py` — `VerificationTool` ABC (`:41`, base, STAY) + `_dig`/`_norm`/`_core` (`:96/:54/:58`, generic helpers, STAY — the clinical tools import them) + `InRowTool` (`:110`, **clinical, relocate**) + `DosageGroundingTool` (`:171`, **clinical, relocate**) + `extract_pmh_items` (`:63`)/`extract_plan_dose_tokens` (`:85`)/`_norm_dose` (`:81`) (**clinical extractors, relocate**) + `StructuralJuteTool`/`KbRagTool`/`RecordRagTool` (generic, STAY).
3. `lithrim_bench/verification/__init__.py:50-57` + `:73-80` — the tool exports (drop the relocated clinical tools, or re-export from the pack — D-C) + `spec.py:24/:28/:33` (`RECORD_PRESENCE`/`TOOL_IN_ROW`/`TOOL_DOSAGE_GROUNDING` — the contract-type NAME constants: the interface vocabulary; decide stay-vs-move in D-C).
4. **`lithrim_bench/runtime/council/signals.py:182-184`** — `if decl.contract_type not in grounding._CONTRACT_EXECUTORS:` + `grounding._build_contract(decl, http_client=http_client)`. **THE MOAT READ — must stay visible + behavior-identical.** (signals.py is NOT whole-file-frozen but is moat code; the A2/A4 guard pins its grounding behavior.)
5. `lithrim_bench/harness/pack.py` — the infra to EXTEND: `active_pack()` `:48`, `_manifest`/`_resolve` `:53-63`, `pack_prompts_path` `:112` (the path-resolution pattern). **Add the floor-LOADING helper** (`load_pack_floors`/`pack_floor_executors` — importlib the pack's floors module).
6. `packs/healthcare/pack.json` — the manifest (declares ontology/flags_ref/council_roles/judges); **add a `floors` module ref**.
7. `lithrim_bench/harness/__init__.py:20` (re-exports `PresenceCheck`/`ground`/etc.) + `report.py:24` + `grounding_check.py:28` (consume `GroundedResult`, generic — confirm untouched).
8. The GROUND-FLOOR-1 close + memory `grounding-floor-is-the-moat-next` — `record_presence` is the proven suppress floor; its relocation must keep the offline 16/16 suppress behavior identical.

---

## 2. Deliverables (full layer-3)

**D1 — the pack floor-executor REGISTRATION interface.** `packs/healthcare/pack.json` gains `"floors": "packs/healthcare/floors.py"`. `harness/pack.py` gains `load_pack_floors(pack=None)` (importlib-load the module from the manifest path, cached; return its executor registrations). Decide the contract (D-A): the pack module exposes `SUPPRESS_EXECUTORS: dict[str, factory]` + `FLOOR_EXECUTORS: dict[str, executor]` (lean), OR a `register(suppress, floor)` fn. The pack module must be **import-clean** (pure-stdlib at import; the relocated executors are pure-stdlib already — no httpx/dspy).
**D2 — the suppress registry merges the pack (moat-visible).** The core keeps `_CONTRACT_EXECUTORS` as the GENERIC dict (`presence_check`, `kb_grounding`). Add a merge accessor `suppress_executors()` = `{**_CONTRACT_EXECUTORS, **pack_suppress_executors()}` (lazy + cached → avoids the circular import, see D-B). Route `_build_contract` + `ground()` **and `signals.py:182`** through it. The `record_presence` executor comes FROM the pack now.
**D3 — the `_run_floor` registry refactor.** Replace the inline `if/elif`-by-contract_type (`:503-529`) with a `_FLOOR_EXECUTORS` registry: each floor executor owns its `reference`-from-`params` builder + its tool. Core-register the GENERIC floors (`structural_jute`→StructuralJuteTool, `jute_gen`→JuteGenValidatorTool); the pack registers `dosage_grounding`→DosageGroundingTool. `_FLOOR_CONTRACT_TYPES` becomes `core ∪ pack`. Behavior-identical dispatch (A2).
**D4 — relocate the clinical executors → `packs/healthcare/floors.py`.** Move `RecordPresence` + `_decode_artifact_soap` (from grounding.py) + `InRowTool` + `DosageGroundingTool` + `extract_pmh_items`/`extract_plan_dose_tokens`/`_norm_dose` (from verification/tools.py) into the pack module. They import the core primitives they need (`VerificationContract`/`VerificationTool` base, `_dig`, `_artifact_content`) FROM the core — the dependency points pack→core (never core→pack at import; D-B). Update `verification/__init__.py` exports (D-C: drop or re-export-from-pack). The relocated code is **behavior-identical** (same logic; the move is mechanical).
**D5 — the boundary test + the moat-behavior guard.** (i) `tests/test_pack_layer3.py` (mirror layer1a/2): the boundary grep — `grep clinical/snomed/pmh/dosage` over `harness/grounding.py` + `verification/tools.py` → empty; a pack with NO `floors` → the core engine runs with only generic executors; the pack's `record_presence`/`dosage_grounding` register + run; the gate fails closed on an unknown contract_type. (ii) **the moat-behavior guard** — `signals.py`'s grounding read sees the pack's `record_presence` (the withstands-gate still suppresses a false FABRICATED_HISTORY) AND `ground()` produces a byte-identical `GroundedResult` on the GROUND-FLOOR-1 demo pair (the offline 16/16 suppress + a real fabrication still stands). Non-vacuous: unregister the pack floors → the moat loses the suppress (proves the wiring is real).
**D6 — docs.** Record layer-3 in `SPEC_PLUGIN_ARCHITECTURE` (the BE plugin interface = pack executor registration; the first packs-as-code) + the trust/packaging note (Pro packs ship in-process Python) + the layers 4–5 + 2b/1b follow-ons. Update the `healthcare-realm-as-pack` memory.

---

## 3. Plan-review — the load-bearing decisions
- **D-A (the registration-interface shape).** The pack `floors.py` exposes `SUPPRESS_EXECUTORS`/`FLOOR_EXECUTORS` dicts (lean — declarative, no execution at import) vs a `register(...)` callback. *Monitor lean: module-level dicts* (simplest, inspectable, import-clean).
- **D-B (the merge mechanism + the circular-import hazard).** grounding.py imports pack.py (stdlib, no cycle); but the pack's `floors.py` imports core primitives (`VerificationTool`/`VerificationContract`/`_dig`) — so loading floors AT grounding-import-time risks a cycle (grounding→pack→floors→grounding/verification). *Monitor lean: LAZY merge* — `suppress_executors()`/`floor_executors()` load+cache the pack on FIRST USE (inside `_build_contract`/`_run_floor`/`ground()`/the signals read), by which point all core modules are imported. Keep `_CONTRACT_EXECUTORS` as the core-generic constant; the accessors do the merge. (Confirm `lru_cache` keying on `active_pack()`.)
- **D-C (the verification/tools.py partition + the contract-type names).** Which helpers move (the clinical extractors `extract_pmh_items`/`extract_plan_dose_tokens`/`_norm_dose`) vs stay (generic `_dig`/`_norm`/`_core` — verify the generic tools don't depend on the clinical ones). The `spec.py` contract-type NAME constants (`RECORD_PRESENCE`/`TOOL_DOSAGE_GROUNDING`/`TOOL_IN_ROW`) — *lean: STAY* (they're the interface vocabulary the pack registers against), only the tool CLASSES move. Decide whether `verification/__init__.py` drops the moved tools or re-exports them from the pack (lean: drop from core exports; the pack owns them).
- **D-D (moat-visibility — the signals.py touch).** Confirm the one-line `signals.py:182` change (`grounding._CONTRACT_EXECUTORS` → `grounding.suppress_executors()`) is behavior-preserving (same registry contents, now pack-merged) + covered by the D5 moat-behavior guard. signals.py isn't frozen-guarded, but treat it as moat-adjacent: minimal touch, A2/A4 pinned.
- **D-E (green-throughout + the split escape-hatch).** Active pack defaults to `healthcare` ⇒ byte-behavior-identical (the same executors run, sourced from the pack). Prove: full suite green (default + debuglithrim) + `ground()`/the moat byte-identical on the GROUND-FLOOR-1 demo + a before/after on `ws0_default`. **Escape hatch:** if the verification/tools.py extraction + the `_run_floor` refactor cannot land green in one cycle, propose a 3a (suppress/RecordPresence via the existing registry) / 3b (floor/dosage refactor) split at plan-review — but the DEFAULT, per the user, is full.

---

## 4. Scope guardrails — NOT in scope
- **The generic engine STAYS core:** `ground()`, `_build_contract`, the refactored `_run_floor` dispatcher, `PresenceCheck`, `KbGrounding`, `VerificationTool` (base), `StructuralJuteTool`, `KbRagTool`, `RecordRagTool`, `JuteGenValidatorTool`, the Jute machinery, `_dig`/`_artifact_content`. Only the CLINICAL executors move.
- **Layers 4–5** (journey literals + FE fixtures; dataset) → their own cycles. **Layer 2b** (un-freeze the council roster/lenses/owners) + **layer 1b** (un-freeze `KNOWN_TAXONOMY_CODES`) still deferred behind explicit authorization.
- **FROZEN (0-delta):** the consensus seam (`compliance_council._apply_consensus` + the PACK-2 `_ROLE_PROMPTS_DIR` carve-out + roster/owners/`KNOWN_TAXONOMY_CODES`), `judge_metric.py`, the `judges_dspy` consensus seam, the agent seeds. The ontology `verification_contracts` are ALREADY pack-resident (PACK-1) + additive — do NOT edit existing contracts (the `assert_clinical_ontology_seam_frozen` carve-out). The relocation is **behavior-identical executor CODE moving home** — no logic edits.
- **NOT a new floor / NOT optimize.** Relocate the existing executors; do not add coverage or change grounding behavior. The withstands-gate logic (`signals.py`/`withstands.py`) is reused, not rewritten — only the registry READ becomes pack-merged.

---

## 5. Acceptance
- **A1 — boundary grep-clean.** `grep -riE 'snomed|pmh|dosage|patient_profile|soap_pmh|active_medications' lithrim_bench/harness lithrim_bench/verification` → **empty** (the clinical executors live only under `packs/healthcare/`); the core engine resolves floor executors via the active pack. The D5 boundary test asserts it.
- **A2 — byte-behavior-identical.** `ground()` + the withstands-gate produce identical results before/after on the GROUND-FLOOR-1 demo pair AND `ws0_default` (same suppress, same floor blocks, same verdict). The executors moved home; the behavior did not change.
- **A3 — the registration interface is non-vacuous + fail-closed.** A pack with no `floors` runs the core-generic engine (no clinical executors); the `healthcare` pack registers `record_presence` + `dosage_grounding` and they run; an unknown contract_type still raises (`ground()`'s partition guard). Test-pinned, non-vacuous (unregister → the suppress/floor disappears).
- **A4 — moat-visible.** `signals.py`'s withstands-gate sees the pack's `record_presence` (suppresses a false FABRICATED_HISTORY pre-consensus, the GROUND-FLOOR-1 mechanism) — proven via the moat-behavior guard, NOT just `ground()`.
- **A5 — frozen 0-delta + green.** The consensus seam + `judge_metric` + the `judges_dspy` seam + the PACK-2 carve-out + agent seeds byte-identical; the ontology `verification_contracts` unchanged (additive-only guard holds). Green bar (default + debuglithrim — the live-council + grounding suites); ruff net-new 0; pathspec-only commits; parent = the PACK-2 close HEAD.

## 6. Hardness
- [x] **HARD GATE** — fresh-critic required (`/devloop-critique bench-salvage PACK-3`, **spawn with `isolation:'worktree'`** per [[devloop-critic-worktree-isolation]]; scan the close-commit for `[detached HEAD]`). *Reason: (i) it touches the **moat-visible** suppress registry that `signals.py` (the UAP-3b withstands-gate) reads + the **verdict-feeding** `_run_floor` dispatch — A2/A4 byte-behavior is a labels-true-by-construction claim; (ii) it introduces the **first in-process pack CODE** (a trust-surface change — the critic confirms the dependency points pack→core, the pack module is import-clean, no circular import, and a no-floors pack degrades cleanly); (iii) the relocation must be behavior-identical executor code (the critic diffs the moved classes for logic edits + re-runs the GROUND-FLOOR-1 16/16 suppress + a real-fabrication-still-blocks defeat-test).*

## 7. References
- [[healthcare-realm-as-pack]] (the layered plan) · [[conversational-first-core-plugin-line]] (the FE+BE plugin interface — this is the BE floor half) · [[grounding-floor-is-the-moat-next]] (record_presence = the proven suppress floor) · [[devloop-critic-worktree-isolation]] · [[council-runtime-test-env]] (debuglithrim for the live-council/grounding suite).
- The PACK-1 + PACK-2 drivers (the pack-resolution + carve-out patterns) · `tests/test_pack_layer1a.py` / `test_pack_layer2.py` (the boundary-test shape) · `SPEC_PLUGIN_ARCHITECTURE` (Phase-1; the pack manifest; the plugin interface) · CLAUDE.md (labels true by construction; the frozen council).
