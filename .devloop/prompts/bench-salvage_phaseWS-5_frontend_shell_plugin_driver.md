# Driver — `bench-salvage` phase `WS-5`: frontend shell + one plugin UI (bring-data journey)

> **Generated by the monitor. The executor pastes the KICKOFF block into a
> fresh Claude Code session, reads the rest, posts plan-review, then implements.**
>
> **Bundle ID:** `bench-salvage-phaseWS-5-frontend-shell-plugin-driver`
> **Version:** v1
> **Authored:** 2026-06-01
> **Last re-verified against code:** 2026-06-01 (WS-4a consumed surface CONFIRMED on HEAD `5c1d3d1`)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase WS-5 (frontend shell + one plugin UI).

Read in this order:
  1. .devloop/personas/EXECUTOR.md                                  (your role)
  2. .devloop/prompts/bench-salvage_phaseWS-5_frontend_shell_plugin_driver.md  (this doc)
  3. CLAUDE.md  §"What this repo is NOT"  (the "Not a UI. CLI + JSONL until phase 4" boundary — WS-5 IS that boundary)
  4. scripts/run_eval.py                   (run:101 — the vertical entry; `live` flag)
  5. lithrim_bench/harness/evalpack.py     (build_pack:81, dump_pack:109, load_pack:117)
  6. lithrim_bench/harness/corpus.py       (read_corpus:104, project:64, DEFAULT_CORPUS_PATH:38)
  7. lithrim_bench/harness/report.py       (composite:29, calibration_check:159)
  8. ../lithrim-ui/  (BEHAVIOR REFERENCE ONLY — do NOT code-salvage; study the journey, not the code)
  9. .devloop/state/STREAM_bench-salvage.md + the WS-4a driver + critique (the vertical you're surfacing)

This cycle is HARD GATE. Post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)"
— it MUST resolve the three decisions in §3 below. Do not write code until I say "go".

Bundle ID: bench-salvage-phaseWS-5-frontend-shell-plugin-driver
```

---

## 0. Why this cycle (one paragraph)

The backend vertical is complete (WS-0→WS-4a: `grade → ground(suppress + structural floor) → correction corpus → thin eval-pack → calibration check`, all offline). WS-5 stands up the **first UI layer** — the phase-4 boundary `CLAUDE.md` names ("Not a UI. CLI + JSONL until phase 4"). Scope is deliberately **thin: one shell + ONE plugin + ONE case** — turn the scripts+ndjson vertical into something a person can *see and drive*, not build a UI platform. The data path reuses the existing `live` flag (default **replay, $0**); the "must be live" acceptance is one `live=True` run.

---

## 1. Pre-flight reading (ordered)

1. `CLAUDE.md` §"What this repo is NOT" — WS-5 crosses the "CLI + JSONL until phase 4" line deliberately; keep the UI thin.
2. `scripts/run_eval.py` — `run(agent, *, live=False, out_dir=None):101` — the vertical entry the plugin drives. **`live` defaults to replay ($0).**
3. `lithrim_bench/harness/evalpack.py` — `build_pack(…, live=False, out_dir=None):81` (forwards `live` to `run`), `dump_pack:109`, `load_pack:117` — the pack the plugin renders.
4. `lithrim_bench/harness/corpus.py` — `read_corpus:104`, `project:64`, `DEFAULT_CORPUS_PATH = out/ws4a/corpus.ndjson:38` — the corpus-row/1 provenance the plugin shows.
5. `lithrim_bench/harness/report.py` — `composite:29` (verdict + suppress/floor adjustments), `calibration_check:159` (verdict-match + ECE summary, report-only).
6. `../lithrim-ui/` — **BEHAVIOR REFERENCE ONLY.** Study the bring-data / case-view / report journey for UX cues; do **not** copy code (task-pack guardrail).
7. `.devloop/state/STREAM_bench-salvage.md` (milestone arc + open seams) + `.devloop/prompts/bench-salvage_phaseWS-4a_flywheel_slice_driver.md` + its critique — the vertical you're surfacing.

> **Citation discipline:** the WS-4a symbols above were re-grepped on HEAD `5c1d3d1`. The frontend itself is net-new — no citations to drift. If a WS-4a symbol has moved on read, halt and surface it.

---

## 2. Deliverables (file-by-file — thin)

1. **Shell + minimal plugin contract** (NEW dir, location is a plan-review decision) — a shell that discovers + loads UI plugins behind a **minimal** interface (e.g. a plugin declares an id + a render entry). One plugin registered for now; do NOT build a plugin marketplace.

2. **ONE plugin = the bring-data journey** — pick/ingest a case → run the WS-4a vertical (`evalpack.build_pack` / `run_eval.run`, default `live=False`) → render four views: (a) the **graded + grounded verdict** (`composite()` — original→adjusted, suppress + floor adjustments), (b) the **corpus-row/1** provenance rows (`corpus.read_corpus`), (c) the **calibration_check** summary (`report.calibration_check`, report-only PASS/WARN), (d) the case itself (transcript/artifact + expected).

3. **Data path = the existing `live` flag** — default **replay ($0)** for dev/CI; the acceptance demo (A3) runs **one case `live=True`** (user's `:8002` up + cost confirmed at plan-review). Do NOT fork a separate live/replay codepath — it's one flag.

4. **Smoke/tests** — the shell loads the plugin + the plugin renders the replay path offline + deterministically; frontend deps isolated from the Python default install.

> Net-new layout (dir names, where the shell lives, build tooling) is a **plan-review decision** — propose the lightest thing.

---

## 3. Plan-review checkpoint (non-negotiable — resolve these three)

1. **Frontend tech stack.** No UI exists in lithrim-bench (CLI+JSONL). Survey + propose the **lightest** thing that demos the journey (a small local web app, or a static HTML/JSON renderer over the WS-4a outputs — NOT a heavy SPA framework). State how frontend deps stay isolated from the Python default install. `../lithrim-ui` is behavior-reference-only.
2. **Minimal plugin contract.** Propose the smallest plugin interface that the shell loads — enough for one plugin, extensible later, not over-engineered.
3. **Data path + cost.** Confirm `:8002` is up (do NOT autostart) and confirm the **one live-call cost** for the A3 live-acceptance run (~$0.05–0.30). Confirm replay is the default dev path. If you'd rather ship A1/A2 replay-only this cycle and defer the live A3 to a follow-up (WS-5b), say so — that's an electable narrowing.

Plus: Understanding, file-by-file plan, test plan, risks, any deviations. Wait for "go".

---

## 4. Scope guardrails — NOT in scope

- **No `../lithrim-backend` edit; no WS-2 dependency.** The plugin composes over the existing harness only.
- **No code-salvage of `../lithrim-ui`** — behavior reference only (task-pack guardrail).
- **No heavy frontend framework / build pipeline** — keep it walking-skeleton-thin.
- **One plugin, one case.** Multi-plugin shell, multi-case dashboards, the full live journey beyond one case → WS-5b / WS-6.
- **Don't autostart services** — the user runs `:8002`; confirm up at plan-review.
- No drive-by changes to the Python harness beyond what surfacing it requires (if the plugin needs a thin read-only adapter, that's in scope; refactoring the harness is not).

If something feels load-bearing-but-not-listed, halt and surface it in plan-review.

---

## 5. Acceptance criteria

- **A1.** The shell loads the registered plugin (verify: shell starts, plugin appears/renders).
- **A2.** The plugin runs the WS-4a vertical on one case via the **replay** path (`live=False`) and renders all four views (graded+grounded verdict, corpus-row/1 rows, calibration_check summary, the case) — offline, deterministic.
- **A3.** The plugin runs **one case LIVE** (`live=True`) end-to-end from the shell and cites that run (the task-pack "must be live" mandate). Gated on `:8002` up + the cost confirmed at plan-review. *(Electable: deferred to WS-5b if plan-review chooses replay-only this cycle.)*
- **A4.** Offline tests/smoke green; frontend deps isolated (Python default install `['pydantic>=2.6','pandas>=2.1']` unchanged); no heavy-framework bloat.

### Diagnostic stats (NOT gates)
- Time-to-first-render on the replay path; the one live-run latency + cost (A3).

---

## 6. Commit structure + message templates

Atomic per unit. Suggested:
1. `feat(ws5): minimal shell + plugin contract [D0]`
2. `feat(ws5): bring-data plugin — run WS-4a vertical + render the four views [D1/D2]`
3. `test(ws5): shell+plugin smoke (replay, offline) [D4]`

Each body ends with:
```
Executed per bench-salvage-phaseWS-5-frontend-shell-plugin-driver by session-2026-06-01-N.
See .devloop/sessions/session-bench-salvage-phaseWS-5-2026-06-01.json
```
(Add the Co-Authored-By trailer.)

---

## 7. Verification checklist (run at end, before the session log)

- [ ] A1–A4 PASS (A3 per the plan-review data-path decision)
- [ ] All commits exist; working tree clean
- [ ] Offline smoke/tests green; Python default install unchanged; frontend deps isolated
- [ ] No file outside §2 (no backend, no WS-2, no lithrim-ui code-salvage)
- [ ] No services autostarted; the only live call is the A3 acceptance run (cost recorded); nothing pushed
- [ ] Session log per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight in order (incl. a survey of `../lithrim-ui` for UX, not code).
3. Re-read §2–§7.
4. Post plan-review per §3 — resolve the three decisions. Wait for "go".

---

## 9. References

- Stream state: `.devloop/state/STREAM_bench-salvage.md` (milestone arc; WS-5 row)
- Task pack: `.devloop/tasks/TASK_PACK_bench-salvage.json#WS-5`
- The vertical being surfaced: `.devloop/prompts/bench-salvage_phaseWS-4a_flywheel_slice_driver.md` + `.devloop/sessions/critique-bench-salvage-phaseWS-4a-2026-06-01.md`
- Behavior reference (NOT code): `../lithrim-ui/WORKFLOW_UI_IMPLEMENTATION.md`

---

## Hardness

- [ ] Routine
- [x] **HARD GATE** — first UI layer + a new plugin-contract surface + a live `:8002` side-effect (A3); fresh-critic close (`/devloop-critique bench-salvage WS-5`, NOT inline) + user sign-off before commits land.

> If plan-review scopes this **replay-only** (defers A3 live to WS-5b) AND the plugin contract is judged non-durable, it could drop to routine — monitor decides at close.
