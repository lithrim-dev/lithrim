# STREAM: `bench-salvage` — Walking-Skeleton eval/calibration harness

> **Live monitor state.** 2026-05-30: pivoted from bottom-up "salvage the council in-process"
> to a **walking-skeleton product** — a domain-agnostic, tool-grounded eval/calibration harness
> for agentic-LLM outputs, built by **composing over the live verification stack** and thickening
> one vertical slice at a time. The harness grows INSIDE lithrim-bench.
>
> **Owner:** monitor · **Created:** 2026-05-30
> **North star:** a domain-agnostic, tool-grounded harness composed over `:8002 /v1/pipeline/evaluate`
> (council+structural+artifact, sync, inline) + `:3031` (JUTE copilot `/mappings/generate` + `/apply` +
> `/parse-hl7`) + pinecone KB (`:8002 /v1/kb/search`), with **SQLite as the configuration plane** and the
> **ontology as the domain definition**. Tool-grounding can flip verdicts (every flip logged); every
> correction is a **fine-tuning-ready record** for the future RLVR / data-lake north star. Paper:
> replicate current state on the harness, then strengthen via tool-grounded correction of
> confident-but-wrong judges (**S-BS-7** = the exhibit). `Agent` = the system under test.

## Authority docs

- `walking-skeleton-architecture` + `etlp-ecosystem-capability-map` (monitor memory) — the converged design + the live-endpoint map
- `docs/research/REPORT_r3d_precheck_falsification_2026-05-30.md` — why the FP is calibration not aggregation (S-BS-7 origin)
- `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md` — M1 close-out
- `docs/PAPER_OUTLINE.md` — locked one-claim guardrail (scribe = semantic-only by construction)
- `out/scribe_v1.live_pipeline_evaluate.bench_scribe_v1_inject_condition_1bd0f10dc7b5.json` — **WS-0 live baseline** (the captured live `/v1/pipeline/evaluate` result; grounding code is built offline against it)
- Live endpoints: `:8002` lithrim-backend (FastAPI, `COMPLIANCE_COUNCIL_VERSION=v2`), `:3031` etlp-mapper (Clojure JUTE)

---

## Milestone arc (walking-skeleton: build vertical, thicken incrementally)

| WS | Title | Composes / new code | Status |
|---|---|---|---|
| **WS-0** | One case end-to-end over live APIs | grade via live `:8002 /v1/pipeline/evaluate` (PROVEN) → persist (fs+SQLite) → **harness-side grounding** (S-BS-7 presence-check disproves MED FP) → composite + calibration-report | **done — closed 2026-05-30** (`9001a36..0859c8d`; NON-BLOCKING) |
| **WS-1** | SQLite config plane | Agent eval-profile {judges, council_config, ontology, tools, kb_bindings}; the **ontology data model** (flags+tiers+owners+questions+verification_contract); 4 slim-path collections doc-shim | **done — closed 2026-05-30** (`6f4612f..0c73ff0`; NON-BLOCKING) |
| **WS-2** | Generalize `:8002` | inject `council_config`/`ontology` into `PipelineRequest` (Optional, additive) → domain-agnostic grading | **bench-side LANDED 2026-05-31** (`8b396d3..5ba1bec`, D0/D3/D4, PROCEED-WITH-CAVEATS); **backend D1/D2/D4 ⛔ HARD-GATE-paused** |
| **WS-3** | Tool-grounding generalized + mid-loop | **WS-3a:** promote the verification core (DSPy bench-gated JUTE generator + mutation/joint gate) into `lithrim_bench/verification/` + wire the bench-accepted contract into `harness/grounding` as the FLOOR (PASS→BLOCK). **WS-3b (deferred):** pinecone via `:8002 /v1/kb/search` + local vector (S-BS-5) + judge-calls-tool mid-loop into live `:8002` | **WS-3a done — closed 2026-06-01** (`59a3cba..d6fe96f`; audit CLEAN + fresh-critic NON-BLOCKING); prototyped on `spike/verification-toolbox` (66 offline tests). **WS-3b queued.** |
| **WS-4** | Eval-pack loop + LOCKED calibration gate | promote-to-eval + external SDK run + `/eval-runs/compare`; eval-pack = preregistered locked criteria incl. a **calibration gate**; NULL/regression parity. **Gates:** S-BS-13 (floor-apply replay) + S-BS-16 (floor inject-param validation) before any floor ships (WS-4b) | **WS-4a done 2026-06-01; WS-4b queued** |
| **WS-5..WS-5e + WS-5-BFF** | **Product shell program** (React/Tauri) | **RE-SCOPED 2026-06-01** to the real Claude-desktop-class 3-pane conversational shell (supersedes the thin stdlib driver). **§8 re-cut FE-first 2026-06-01** (WS-5 critique BLOCKING→amend): **WS-5 = shell skeleton (DONE)**; WS-5b convo journey; WS-5c generative-UI; **WS-5-BFF (NEW, HARD-GATE) = FastAPI BFF + Tauri + one real `run_eval` vertical** (the displaced "prove-the-stack" half, parallelizable); WS-5d artifacts pane **wired** (re-cut build→wire); WS-5e Tauri+VPC packaging. Specs: `docs/specs/SPEC_PRODUCT_SHELL.md` (§8 amended) + `SPEC_PRODUCT_SERVICE_TOPOLOGY.md` | **WS-5 / WS-5b / WS-5-BFF / WS-5c / WS-5d done (2026-06-01); WS-5e next** — BFF targets the harness (sequencing B) |
| **WS-6** | **Python-Layer consolidation** (strangler-fig completion; was "compartmentalize-local") | **REFRAMED 2026-06-01** (`SPEC_PRODUCT_SERVICE_TOPOLOGY.md`): port the validated **v2 council Mongo-free** (extends the M1 v1 in-process salvage; PORT not rewrite) + swap persistence Mongo→SQLite/PG behind a repository interface; ETLP = JVM-sidecar service. **Retires Mongo from the product.** | **parallel track (sequencing B); broken into WS-6a…e (2026-06-01)** · **WS-6a ✅ DONE** · **WS-6b RECOMPOSITION PLAN landed** (`RECOMPOSITION_PLAN_ws6.md`; scope broadened to the full agentic layer) → **6c council-PORT** ∥ **6c-DSPy** (rebuild judges) ∥ **6c-AGENTIC** (recompose LangGraph+agents) / **6d** persistence (+**6d-KB**) / **6e** ETLP |
| **UAP-1..5** | **Unified authoring+processing PRODUCT** (reframed WS-7; the journey FROZEN as a demo inside it) | The UI-driven author→process loop over the config plane: **judge creation** (ASSIGNED ontologies → refinement questions; execute-not-generate validators) + **flag creation** (gen-UI + `PUT /v1/ontology`) + **processing** (`run_eval`/grade + floor + KB) + the **Ralph-Loop withstands-gate** + a first-class **why/when/who/what audit trail** (§2B). Compose-not-build. | **SPEC LOCKED 2026-06-04** (`docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md`; `213545d..cd60da1`). **UAP-1 ✅ CLOSED 2026-06-04** (`6bdf975..a7edd25`; R0/R1/R3 — config-plane write-path `/v1/agent` + draft→grade [closes S-BS-26(b)] + the immutable audit-record/actor foundation; audit CLEAN + fresh-critic NON-BLOCKING; PROCEED-WITH-CAVEATS, A8 `:5180` smoke OWED). **UAP-2 ✅ CLOSED 2026-06-04** (`86fa1d3..3e89ad5`; `JudgeEditor`/`/v1/judges` + the prompt↔ontology bridge via ontology-assignment, R2; audit CLEAN + fresh-critic NON-BLOCKING; PROCEED-WITH-CAVEATS, A8 OWED). **NEXT = UAP-3** (`RunPanel`/processing) → UAP-3b (withstands-gate) → UAP-4 ✅ CLOSED 2026-06-05 (optimize→honest-Δ; live LOSS — selection fixed but demos over-fire → S-BS-76; S-BS-49 sharpened; UAP-4-opt bind BLOCKED, no +Δ). → UAP-5b ✅ CLOSED 2026-06-05 (the conversational-shell SPINE: talk→tool→audited write→card; D0 BYO-Claude PASSED, A-SAFE airtight; A-LIVE owed → S-BS-79). → UAP-5c ✅ CLOSED 2026-06-05 (the FULL Domain→Judge→Flag→Run→Review journey from a blank agent: `get_agent`/`author_flag`/`review_runs` grown over the spine; **S-BS-82 + S-BS-81 CLOSED**; A-SAFE cleanest-possible [the D-F split → no agent-reachable paid op]; A-LIVE `:5180` owed). → UAP-5c-2 ✅ CLOSED 2026-06-05 (the complete 8-tool surface: `run_eval_pack` batch [live=False hardcoded] + `assemble_agent` edit-one-facet roster write; A-SAFE re-proven safe-by-explicit-bound; A-LIVE ATTESTED LIVE [run_eval_pack replay-only + assemble_agent audited roster round-trip, BFF-verified]). NEXT = an S-BS-46/49 corpus-deepening pass or the S-BS-74 live-correction demo. WS-7a was the last journey-targeted cycle. **+ UAP-5a/5b INCORPORATED 2026-06-04** (additive conversational-driver surface, **SPEC §13** — surface-only, no change to §2A/§2B/§4/R0–R5): **R10 authoring-assist** (describe→card, reuse the zyng `../zyng/zyng/authoring.py` `LLMClient`/`ClaudeCliClient` pattern; after UAP-2) + **R11 conversational shell** (Claude Agent SDK over the BFF ops-as-MCP-tools + the gen-UI registry; after UAP-3b). Sequenced **AFTER the moat (UAP-3b)**, does NOT disturb the in-flight UAP-2 (stays deterministic/LLM-free). **OQ-6 OPEN** (loop locus: Python-Agent-SDK-in-BFF [monitor lean, on-thesis] vs TS-AI-SDK-in-shell). Licensing guardrail: local-CLI provider = BYO-Claude/desktop only, never multi-tenant. Index stubs added; memories `zyng-claude-cli-provider` + `judge-creation-must-be-demonstrable`. |
| *(future)* | RLVR / data-lake | correction records → data lake → RL/fine-tuning flywheel (verification contracts = verifiable rewards) | north star |

---

## Current cycle

| Cycle | Status | Note |
|---|---|---|
| **M1** — salvaged council + sync orchestrator in-process | **done 2026-05-30** | case-1 `reject==reject` (1/1); `FABRICATED_HISTORY` caught; `MEDICATION_NOT_IN_TRANSCRIPT` CONFIRMED FP. Persisted-judge-reasoning enabler landed. 82 tests green. Now a **WS-6 input** (the compartmentalize-local target), not the spine. Artifacts: `out/scribe_v1.local{,.guardfix}.ndjson`; handoff `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md`. |
| **COUNCIL-EVIDENCE-GATE** — R3(d) consensus fix | **⛔ halted / re-scoped 2026-05-30** | Pre-check P0 ($0.10) FALSIFIED the fix: the MED FP is findings-first + fully-evidenced (SPEC §2b false — NDJSON projection artifact); root cause = judge calibration, not aggregation. Re-routed to **S-BS-7** (the WS-0 grounding worked example). Records: `REPORT_r3d_precheck_falsification_2026-05-30.md`, `r3d_precheck_P0_raw_evidence_2026-05-30.ndjson`, `.devloop/sessions/{session,critique}-…COUNCIL-EVIDENCE-GATE-2026-05-30.{json,md}`. Closed commit `b54e92a`. |
| **WS-0** — one case over live APIs | **✅ done — closed 2026-05-30** | Spine landed end-to-end on the one M1 scribe case, entirely OFFLINE against the captured baseline (zero new paid calls). New `lithrim_bench/harness/` pkg (grade/persist/grounding/correction/report) + `scripts/run_ws0.py` + `tests/test_ws0.py`; 6 atomic commits `9001a36..0859c8d` (+ session-log `9d2b721`). **S-BS-7 exhibit works:** the med presence-check disproves+suppresses the confident `MEDICATION_NOT_IN_TRANSCRIPT` FP (zidovudine is verbatim in transcript; judge cites that very line as proof of absence), retains `FABRICATED_HISTORY`, holds composite verdict at `reject`, emits one versioned RLVR correction record. S-BS-8 (4 null-code findings) skip-logged not dropped. Calibration report-only (ECE 0.5, small-N caveat). **Audit CLEAN; critique NON-BLOCKING** (`critique-bench-salvage-phaseWS-0-2026-05-30.md`); 89/89 tests green, new code ruff-clean. Session log `session-bench-salvage-phaseWS-0-2026-05-30.json`. |
| **WS-1** — SQLite config plane + ontology data model | **✅ done — closed 2026-05-30** | Lifted the WS-0 hardcoded spine into two planes, zero new paid calls. New `harness/{ontology,config,collections}.py` + `data/ontology/clinical_v1.json` (23 flags / 16 questions / 1 verification_contract / severity_map) seeded byte-deterministically by `scripts/seed_ontology.py` (seed-not-import: light-import `safety_flags`, AST-parse tiers/owners; eval path reads only the JSON) + committed `data/config/agents/ws0_default.json` driving `scripts/run_eval.py` (canonical) with `run_ws0.py` as a thin shim over one grounding path. WS-0 critique **Q4.2 + Q4.3 CLOSED** (severity_map + extraction params as data); **Q4.1 recorded-only** (owner_roles in the correction record + ontology) → WS-4. **S-BS-6 ratified** (compose-over-live-v2 stored in the eval-profile); **S-BS-9 normalized** (shape contract + normalizer in picklist.py). 8 atomic commits `6f4612f..0c73ff0`. **Audit CLEAN; critique NON-BLOCKING** (`critique-bench-salvage-phaseWS-1-2026-05-30.md`); 99/99 tests green, ruff-clean. Session log `session-bench-salvage-phaseWS-1-2026-05-30.json`. |
| **WS-2** — generalize `:8002` config injection | **ready** (next) | WS-1 unblocks it. **PRE-CONDITION (WS-1 critique Q4.1):** reconcile the ontology flag-source — 4 flags (`FABRICATED_CONSENT_SCOPE`, `MALAFFI_CODE_PROPAGATION`, `MISSING_DUAL_CODING`, `WRONG_PATIENT_INFO`) are in the council `safety_flags.py` the ontology seeded from but **absent from `taxonomy/taxonomy_snapshot.json`** (the CLAUDE.md contract). Decide flag-source authority BEFORE the ontology drives live grading. Run `/devloop-expand-driver bench-salvage WS-2`. |
| **WS-3** — verification core promotion + structural floor (WS-3a) | **✅ done — closed 2026-06-01 (`59a3cba..d6fe96f` + close-out)** | Promoted the `spike/verification-toolbox` core into `lithrim_bench/verification/` (spec/etlp_client/tools/jute_gen/jute_dspy/mutation/router; `KbRagTool` class dropped) behind a `[verification]` extra (dspy/httpx), import-isolated (monitor-re-verified: dspy/httpx/pinecone/onnx absent from `sys.modules` on default deps). **Headline:** wired the structural FLOOR into `harness/grounding` via a sibling `_FLOOR_CONTRACT_TYPES` registry + `_run_floor` sub-path (Decision 2) — a bench-accepted contract injects a BLOCK the council missed and flips PASS→BLOCK (severity HIGH→weight 1.0 ≥ `block_at_or_above` 1.0), logged via `build_floor_correction` (inverse rollout, Decision 6) + surfaced in `composite()`. Backward-compat: clinical_v1 declares no floor → `ground()` additively identical (verdict + partitions unchanged, `floor_blocks==[]`, no floor HTTP via `_BoomHttp`). 3 by-construction packs homed (10/8/10, byte-deterministic regen — monitor-re-verified diff==0) + pinned clean Patient validator. **Audit CLEAN (7/7; A1/A3/A4/A5 monitor-re-verified: isolation `[]`, floor 5/5, packs diff==0, 164 passed/3 dspy-skipped); fresh-critic NON-BLOCKING** (`critique-bench-salvage-phaseWS-3-2026-06-01.md`; 0 BLOCKING / 6 NB / 3 OQ). New seams S-BS-13/14/15; Q4-3 → **S-BS-16** follow-up (touches the core invariant). Feeds `paper-1-copilot` §4/§6/§7 + settles S-P1-22. WS-3b (KB/RAG/ONNX + judge-calls-tool mid-loop) queued. |
| **WS-4a** — flywheel slice (corpus + eval-pack + calibration check) | **✅ done — closed 2026-06-01 (`894cc4d..da05779` + close-out)** | Closed the data-flywheel loop on clinical_v1, offline/replay: `corpus.py` (corpus-row/1 projects BOTH suppress + floor correction records without re-authoring them — the corrected plan-review schema verbatim: single `flag_code`≠contract, `action` encodes direction, `owner_roles` + sha256 `rollout_ref`), `evalpack.py` (evalpack/1 build→load round-trip; one-case-thin by the one-baseline constraint, documented-not-faked), `report.py calibration_check` (verdict-match drives PASS/WARN; ECE diagnostic-only — NOT the WS-4b locked gate). **Audit CLEAN (7/7; A1–A4 monitor-re-verified: 171 passed/3 dspy-skipped, ruff clean, default deps unchanged); inline critique NON-BLOCKING** (`critique-bench-salvage-phaseWS-4a-2026-06-01.md`; 0 BLOCKING / 2 NB / 2 OQ). Q4a-1: corpus is suppress-only until a floor-declaring ontology ships (S-BS-16 gate). No seams opened; clinical_v1 floor-less → S-BS-13/16 inert. Session log `session-bench-salvage-phaseWS-4a-2026-06-01.json`. |
| **WS-5** — product shell skeleton (3-pane React/Vite) | **✅ done — closed 2026-06-01 (FE-first re-cut; built directly, no exec driver)** | The real Claude-desktop-class 3-pane shell **skeleton** landed in `apps/shell/` (Vite 6 + React 18; 18 source files; pixel-faithful Claude Design port; `vite build` clean; live `:5180`). Realizes SPEC §2: floating window + 3 resizable panes + journey rail (`Domain→Judge→Oracle→KB→Run→Review`) + inline cards (config/verdict/calibration) + artifact pane (report/judge-council/config tabs) + fullscreen + light/dark + real Lithrim logo (`src/brand.jsx`). Plain CSS (Tailwind deferred-additive, user decision). **Built by the prior monitor session directly** (conscious role-deviation for a design port — no exec driver, no session log, no tests). **Inline critique (HARD-GATE override, user-elected): BLOCKING DRIFT** (`critique-bench-salvage-phaseWS-5-2026-06-01.md`) — the shell runs 100% mock data; BFF + `run_eval` vertical + Tauri (3 of 5 §8 WS-5 components) absent. **Resolved by amending SPEC §8 to FE-first** (user 2026-06-01): WS-5 ≔ skeleton (this); the "prove-the-stack" half → **new WS-5-BFF** (HARD-GATE, parallel). NB dispositions: domain mismatch → **S-BS-17**; WS-5d re-cut build→wire; zero-tests note carried to WS-5-BFF acceptance. **No harness/`lithrim_bench` code touched** (diff confined to `apps/shell/`). |
| **WS-5b** — conversational journey layer (4-act activation) | **✅ done — closed 2026-06-01 (`460b677..00c68b9` + close-out)** | ESM port of the Claude Design 4-act journey prototype into `apps/shell/src/journey/` (`journeyData.js`/`chrome.jsx`/`jp1–jp4`/`JourneyApp.jsx`) + a top-level `root.jsx` **Shell↔Journey switch** (default Journey; shared theme via lifted `theme/setTheme`). Faithful: SPEC §2.1's 4 phases 1:1; both **heroes** (Phase-2 staggered verify reveal `JourneyApp:54-59`+`jp2:105-134`; Phase-3 before→after `runCalib`+`jp3`); scripted-default (§5c — zero-LLM, mock `journeyData`). Central task (`window.*` globals → ESM imports; `createRoot` dropped) clean. **Audit CLEAN (7/7; `vite build` 42 modules, diff confined to `apps/shell/`, zero new deps, no Tailwind/network/Tauri/tests); inline critique NON-BLOCKING** (`critique-bench-salvage-phaseWS-5b-2026-06-01.md`; 0 BLOCKING / 2 NB / 2 OQ). One **approved** deviation: additive `app.jsx` theme-prop (`theme = themeProp ?? themeLocal`) for shared-theme A3 — monitor-authorized, not intrusion. NB/OQ carried: mode-switch is a dev/demo overlay → integrate at WS-5c; behavioral verification manual-only → first test bar at **WS-5-BFF**; **S-BS-17** (demo-domain split) stays open. Session log `session-bench-salvage-phaseWS-5b-2026-06-01.json`. |
| **WS-5-BFF** — FastAPI BFF + bridge + one real vertical + first test | **✅ done — closed 2026-06-01 (`f19dee4..4549a12` + close-out)** | The de-risk paid down (SPEC §9 honest-bet): `apps/bff/` FastAPI BFF (judge-capability API v1, `[bff]` extra) over `run_eval.run` + harness; React `bff.js` bridge + vite proxy; `ReportTab` renders the **real clinical composite** (replay default + one cost-confirmed live run — **live verdict == replay; the S-BS-7 MED-FP suppression holds on live data**); `tests/test_ws5_bff.py` = the shell's **first behavioral test** (hermetic; +5, suite 176/3). v1 surface **RATIFIED** (SPEC §10): `POST /v1/run-eval` (report folded) + `GET /v1/corpus` + `GET /v1/ontology` (read-only) + `/health`; **PUT/write-ontology → WS-5c/d; Tauri → WS-5e**. **Audit CLEAN (7/7; pytest 176/3 + new-file ruff re-verified); fresh-critic NON-BLOCKING** (`critique-bench-salvage-phaseWS-5-BFF-2026-06-01.md`; 0 BLOCKING / 1 NB / 3 OQ, all authorized). 3 approved deviations (fold `calibration_check`; defer PUT ontology; narrow ruff B008 allowance). New seam **S-BS-18** (React↔BFF binding manual-smoke-only). **S-BS-17 now visually live** (clinical report in support chrome), stays open. Session log `session-bench-salvage-phaseWS-5-BFF-2026-06-01.json`. |
| **WS-5c** — generative-UI components + Tailwind/shadcn foundation | **✅ done — closed 2026-06-01 (`efefaa8..e9f863b` + close-out)** | Two contracts landed: the §5b `tool-<name>`→component registry (AI-SDK message-parts shape; `renderTool`+graceful fallback; 5 tools) + the Tailwind v4/`@theme`/shadcn foundation (`theme.css` bridges the single `styles.css` token source; 9 shadcn primitives; **incremental — `styles.css`/`journey.css` untouched in the diff**). 3 input widgets (flag/severity editor [GET-only ontology, no PUT], contract builder, KB picker) on real shadcn primitives; verdict+calibration cards promoted into the registry (kept faithful on `.icard`); mode-switch moved out of the fixed overlay into both titlebars. **S-BS-17 CLOSED** (Shell mock converged to clinical/Scribe — shell/journey/harness `clinical_v1` now agree); **S-BS-18 CLOSED** (Vitest+RTL infra + the `bff.js`→`ReportTab` binding test). **Audit CLEAN (7/7; 38 files all `apps/shell/`, build clean, 19/19 tests, 0 new Python deps — monitor re-verified through `e9f863b`); fresh-critic NON-BLOCKING** (`critique-bench-salvage-phaseWS-5c-2026-06-01.md`; 1 BLOCKING→RESOLVED / 1 NB / 2 OQ). **The fresh-critic earned its keep:** caught a stray `}` literal in the Shell titlebar (introduced by the post-close chrome-align fix `0c13d3f`) that build + 18 tests missed — fixed in `e9f863b` (one-char delete + a new `app.test.jsx` Shell-`TopBar` guard; +1 test). New seams S-BS-19/S-BS-20. **OWED:** the 60s `:5180` visual-parity smoke (A1/A4 human-eyeball; deferred under no-autostart — now de-risked by the `app.test.jsx` regression guard, but light/dark Preflight-bleed parity still un-eyeballed). Session log `session-bench-salvage-phaseWS-5c-2026-06-01.json`. |
| **WS-5d** — artifacts pane wired (JudgeTab + ConfigTab + corpus) + PUT /v1/ontology + S-BS-19 | **✅ done — closed 2026-06-01 (`38e4071..b065f14` + `5518ffb` fresh-critic copy-fix + close-out)** | Wired the three artifact surfaces off mock to live BFF data + landed the deferred §10:144 write. **JudgeTab** renders realized council votes (`result.semantic.judge_votes`, **`confidence float\|null`-tolerant — WS-6a carry (b) DISCHARGED**); **ConfigTab** self-fetches `GET /v1/ontology`; both drop the `data.jsx JUDGES/CONFIG_YAML` mock from the live path. New **corpus/flywheel** 4th artifact tab (`GET /v1/corpus`, corpus-row/1; empty-state is the real default — committed corpus is empty). **PUT /v1/ontology** (D1) is **clobber-safe BY CONSTRUCTION** — writes `out/bff/ontology/<agent>.json` (gitignored), NEVER `data/ontology/clinical_v1.json` (monitor-verified byte-unchanged + not in the cycle diff); validated via `ontology.from_dict` round-trip + the S-BS-10/12 snapshot lint → **422** on malformed / gradeable-flag-outside-snapshot; GET prefers the working copy so PUT→GET round-trips. **FlagEditor** is now read-write (a "Persist draft" PUT, kept distinct from the `onResult` config-plane path). **S-BS-19 closed for the Shell host** (`CenterPane` mounts the 3 input tool-parts, `onResult`→local config-plane state) + the datapoint `part.output` prop convention **locked to flat-spread** (VerdictCard conformed, `{data}` dropped). **Audit CLEAN (7/7; monitor re-verified BOTH halves: pytest 181/3, BFF 10/10, Vitest 30/30, diff confined to `apps/shell/`+`apps/bff/`+`tests/test_ws5_bff.py`, seed byte-unchanged); fresh-critic NON-BLOCKING** (`critique-bench-salvage-phaseWS-5d-2026-06-01.md`; 0 BLOCKING / 1 NB→fixed `5518ffb` / 1 NB arg-flip / 2 OQ). **The fresh-critic earned its keep again:** caught a stale "read-only — no PUT this phase" label/docstring on the now-writing FlagEditor (coherent-but-wrong copy build + 30 tests missed) — fixed in `5518ffb` BEFORE the smoke. **A7 `:5180` visual-parity smoke PASS (user-attested 2026-06-01).** New seam **S-BS-26** (journey-act mount still owed + drafts-don't-feed-grading). Session log `session-bench-salvage-phaseWS-5d-2026-06-01.json`. |
| **WS-6a** — backend baseline (curate + commit `../lithrim-backend` WIP) | **✅ closed 2026-06-01 (`2f04a40..493b533`; audit CLEAN 7/7; inline critique NON-BLOCKING)** | WS-6 step **a** — the clean-baseline precondition (can't audit/strangle a dirty tree). **Curation cycle** (commit *existing* diagnosed WIP, not build): `../lithrim-backend` `mvp-ready` has **8 modified** files (structural-verdict/severity remediation — S-P1-15-adjacent, moat/paper-relevant) + **~12 untracked** docs/scripts/tests + transient `out/`/`test-results/`. Atomic per-concern commits + gitignore transients + **backend tests green first**; diagnose-before-edit (D0 triage manifest). **Cross-repo** (commits land in `../lithrim-backend` on `mvp-ready`). *Baseline + preserve — strangle-not-fix holds.* Driver `bench-salvage_phaseWS-6a_backend-baseline_driver.md`. **CLOSED:** 5 atomic per-concern commits on `mvp-ready` (C1 `2f04a40` validator-declared check severity; C2 `4b3e4e4` PIPELINE_GRADING_AUDIT D-E/D-J/D-K surface+provenance; C3 `379d1d3` docs; C4 `a845809` dev-workflow scaffold; C5 `493b533` scripts+gitignore). Tree clean; 2 stray bench-doc copies removed; `out/`+`test-results/` gitignored; cached-validator timestamp churn restored. **Regression-free** (WIP tests 24/24; broad `-m "not slow"` suite failure-set IDENTICAL clean-HEAD vs WIP, so the 52 pre-existing failures are NOT this cycle → **S-BS-24**). **Real attribution: PIPELINE_GRADING_AUDIT D-E/D-J/D-K + `43b20f6` P0-1, NOT S-P1-15** (driver guess corrected; reconcile in paper-1-copilot). Live e2e: 2/2 scribe verdicts correct. New seams **S-BS-21..25**. Session `session-bench-salvage-phaseWS-6a-2026-06-01.json`; critique `critique-bench-salvage-phaseWS-6a-2026-06-01.md`. |
| **WS-6c** — council PORT (v2 consensus IP, Mongo-free, v2-only) | **✅ done, closed 2026-06-01 (`12281e1..6bae3c0`; audit CLEAN 7/7; inline critique NON-BLOCKING; HARD-GATE, user-elected inline)** | WS-6 step **c**, the first BUILD phase. PORTED the validated v2 council into `lithrim_bench/runtime/council/` from `lithrim-backend@493b533` (Mongo-free, importable, v2-default). **A2 byte-faithful** (monitor re-diffed at tip: `compliance_council.py` = 6 import lines + `_ROLE_PROMPTS_DIR:457`; `safety_flags.py` byte-identical; `llm_provider`/`phi_redaction` 1 import each; 5 role prompts SAME). **S-BS-28 was mechanical, not logical:** the parked mirror already matched `493b533`, so the re-derive reproduced it with zero git delta on the IP files. Net-new: 16-field settings subset (default v2), `[council]` extra + testpaths, package/seam docs, **S-BS-30** derivation + re-snapshot, a **16-case offline consensus oracle** (verdicts captured from the real `_apply_consensus`, monitor-traced: off-domain Tier-1 downgrade, llama-veto + Tier-1 safety-floor, None-confidence). **A5 live smoke PASS** (Azure dev resource via `../lithrim-backend/.env`, user-go'd: v2 trio all-reject, **Mistral `confidence=None` end-to-end**; actual ~$0.15-0.25 vs ~$0.10 envelope, the overage from a payload-shape bug the smoke caught then fixed, user-aware). Grade seam frozen (A6); council importable but NOT yet wired (that is WS-6c-AGENTIC). 3 atomic commits, not pushed. New seam **S-BS-31** (orphaned Tier-1 owners under v2, gates 6c-AGENTIC); **closed S-BS-30 + S-BS-28**. Session `session-bench-salvage-phaseWS-6c-2026-06-01.json`; critique `critique-bench-salvage-phaseWS-6c-2026-06-01.md`. |
| **WS-6c-DSPy** — DSPy per-judge layer wrapping the ported consensus (the §6 hybrid) | **✅ done, closed 2026-06-02 (`95174c0..4e1d3cc`; audit CLEAN 7/7; critique NON-BLOCKING; HARD-GATE, inline-by-continuation — full fresh-critic independence routed to 6c-AGENTIC)** | WS-6 step **c-DSPy**, the first DSPy-rebuild. Rebuilt **`risk_judge`** as a DSPy module (`runtime/council/judges_dspy.py`) emitting the EXACT §6 per-judge dict seam (`{model, decision, confidence:float\|None, findings:[{taxonomy_code,evidence_spans}], errors:[]}`); the ported `_apply_consensus` wraps it **UNCHANGED** — **A4 byte-ZERO-delta** vs the WS-6c tip `6bae3c0` (`git diff` over the full ported set incl. `__init__.py` = 0 lines; monitor + critic both independently re-verified). Bench-accept **judge metric** (`judge_metric.py`, recipe=label findings-vs-`expected_safety_flags`, the `jute_dspy` pattern). **39 passed / 1 skipped** on `debuglithrim` (WS-6c oracle + the new DSPy tests; live smoke env-gated); dspy tests `importorskip`-skip on default `python3` (A7); ruff clean. **Live smoke PASS:** 1 gpt-4.1 call **$0.0067** — DSPy `risk_judge` → `reject` on a WRONG_DOSAGE case with 2 grounded spans → ported consensus → `reject` (matches recipe=label); **D2 logprobs bridge validated live** (`confidence=0.999999`, Mistral `None`-path pinned offline — confidence sourced from `extract_verdict_confidence`, NOT a self-report `OutputField`). Council importable but **NOT wired into grade** (A6; that is 6c-AGENTIC); **S-BS-31 inert** (no real cases scored). **Closed S-BS-27** (prior art re-authored clean in-package; worktree never imported). **New seams S-BS-32** (S-BS-31 footprint: 3 default-deps packager/triage tests RED under v2-only orphaned owners — pre-existing, the bench analogue of S-BS-24) **+ S-BS-33** (the seam doc omits the optional `citations_used` read at `_apply_consensus:1942` — verdict-neutral/inert; decide DSPy emission at 6c-AGENTIC). HARD-GATE closed on an **inline-by-continuation critique** (same session that prepped the kickoff; the critic adopted an adversarial from-source stance and surfaced 2 findings the monitor audit missed; 0 BLOCKING) — full fresh-critic independence deliberately reserved for **6c-AGENTIC** (where the council first scores real cases). 4 atomic commits (`95174c0..4e1d3cc`, branch `bench-salvage/ws6c-dspy`, not pushed). Session `session-bench-salvage-phaseWS-6c-DSPy-2026-06-02.json`; critique `critique-bench-salvage-phaseWS-6c-DSPy-2026-06-02.md`. |
| **WS-6c-AGENTIC** — compliance grade-wire milestone (**the council scores real cases for the first time**) | **✅ done, closed 2026-06-02 (bench `903478e..5cf438b` + backend `6720c70`@`mvp-ready`; audit CLEAN; HARD-GATE **genuinely-fresh-critic** NON-BLOCKING (`wf_fdb93c9c-776`); A5 completed to milestone evidence standard — option B)** | The MILESTONE: the in-process council scores real cases through the harness grade seam. **D0** S-BS-31 fix (cross-repo): reassign 3 orphaned Tier-1 owners to the v2 trio by domain — `faithfulness`←MISSING_ALLERGY+VALUE_MISMATCH, `policy`←FABRICATED_CONSENT; **risk co-ownership DROPPED as inert** (emits FABRICATED_ALLERGY not MISSING_ALLERGY); backend `6720c70` + byte-faithful re-port `903478e` + re-snapshot. **D1** drive the M1 spine on the v2 trio. **D3** `grade_inprocess` + `run_eval --in-process` behind the frozen seam (`report.composite` untouched; `run_eval.run` extended backward-compat). **A4** consensus byte-frozen (single `_TIER1_OWNERS` data hunk vs `6bae3c0`; functions identical — re-verified post-addendum). **A5** live v2 trio on `proof_case:drop_allergy` → BLOCK/`reject`, **Mistral `None` tolerated**, **on-disk fixture** `tests/fixtures/ws0/a5_live.drop_allergy.json` + env-gated real-council `grade_inprocess` test (option B, ~$0.09–0.20, 1 paid call). **A6** orchestrator/stages **0-touched**. **A7** 187 passed/7 skipped, zero new lint. **Fresh-critic (6-agent workflow):** test-integrity **PRINCIPLED** (the negative-control non-owner oracle survived — not gamed); its one over-reach (an A5 "fabricated" framing) monitor-corrected (it conflated `proof_case:drop_allergy` with the separate `test_live_smoke.py` fixture). **D2 reframe (load-bearing for WS-6c-OBS):** the orchestrator is the `pipeline/` PRIMITIVE (always-runs-council), **verdict-equivalent-or-STRICTER** than the backend's approve-only confidence-gate — so "verify-and-document," not rebuild the workflow nodes. **Closed S-BS-31 + S-BS-32 + S-BS-35**; opened **S-BS-34** (pre-existing ruff exclude, not a gate). 6 bench commits (`903478e..5cf438b`) + monitor docs (`b619f2e`/`118af7d`/close) + backend `6720c70`, not pushed. Session `session-…AGENTIC….json`; critique `critique-…AGENTIC….md`. |
| **WS-6c-OBS** — observation/KPI recompose (greenfield in-process; grade-path-independent) | **✅ done, closed 2026-06-02 (bench `2321090..3cda9df` + `5900877`; audit CLEAN 7/7; ROUTINE inline critique + monitor deep-read NON-BLOCKING; tier A+ user-elected; bench-only, NO backend edit)** | The observation/KPI half split from AGENTIC, recomposed greenfield into `lithrim_bench/runtime/observation/`. **D1** the KPI-half `StateGraph` (`process_input_item…aggregate_kpis`) → straight-line async (`should_continue`→`if/await`, `handle_error`→`try/except`, **4-way `asyncio.gather` PRESERVED**, Mongo/S3/LangGraph dropped). **D2** the 7 agents recomposed in-process, instantiation **hoisted** into `ObservationAgents` (backend re-instantiated per node); `evaluation_agent` **dropped** (dead), `simulation_agent` **parked** (LiveKit). **Tier A+** (user-elected at plan-review): the 3 LLM agents (intent/sentiment/safety) are **real faithful ports** (injectable/lazy LLM reusing the council `llm_provider`/`phi_redaction`, offline-stubbed) + the pure `KPIAggregationAgent`; the **audio-DSP trio interface-complete + `[observation]`-gated, bodies deferred** (→S-BS-36). **D3** `ObservationState`/`call_kpi` contract preserved (28 KPI fields ⟂ 9 seam fields; `CallKPI` 19-field upstream set). **A3 boundary (the D2 pattern at the OBS edge):** the compliance tail (`check_hipaa_compliance`→`ComplianceWorkflow().process():686`) is a **documented hand-off seam** to the orchestrator AGENTIC already recomposed — NOT re-ported; grep+import+runtime confirm no council/orchestrator call. **A2:** 200 passed/7 skipped default deps, **zero heavy-dep leak** (no openai/whisper/torch/librosa/boto3), ruff clean. **Monitor deep-read** (producer-bias check, user-pushed): the 3 LLM ports verified faithful — identical float-constant multiset (scoring math) + AST string-literal parity (prompt text), only `__init__` dropped per the injectable refactor. **DRIFT-2b** (monitor self-correction): the driver's "dep-light slice" grouping was wrong — only `kpi_aggregation` is pure (`transcription`→`whisper`/`torch`, `technical_metrics`→`librosa`); driver corrected at close, no harm (A+ gated all heavy deps). Opened **S-BS-36** (audio bodies deferred) + **S-BS-37** (`datetime.utcnow`). 4 bench commits + log, not pushed. Session `session-…OBS….json`; critique `critique-…OBS….md`. |
| **WS-6d** — persistence swap: real SQLite provenance store (retire Mongo on the grade path) | **✅ done, closed 2026-06-02 (bench `f898790..a211773` + `c6a35db`; audit CLEAN; ROUTINE inline + risk-driven deep verification NON-BLOCKING; bench-only, NO backend edit; $0 offline)** | **The first ship-path payment: the in-process grade path is now Mongo-free.** Implemented `SqliteProvenanceStore` (the **S-BS-38 blob tier**: full `PipelineProvenance.model_dump(mode=json)` → standalone `PIPELINE_RUNS` doc-shim, `sort_keys` deterministic) behind the existing injected `save(provenance)` seam; added `find_by_id`; **retired the `MongoProvenanceStore` alias** (A4: 0 hits in `runtime/`); **opt-in** (orchestrator default → `NoOpProvenanceStore`, the grade path threads Sqlite via additive kwargs — `run_eval.run`/`report.composite`/`PipelineResult` **frozen**). **Eval-isolation** rides `pipeline_run_id` uniqueness (the `::eval::` mechanism has no bench analogue — no `store_report` in the primitive). **A3 (load-bearing, frozen-contract):** grade record byte-identical with persistence on vs off — **PASS on `debuglithrim` (6)**, monitor-verified **PRINCIPLED** (NoOp-vs-NoOp control + raw-diff + side-effect guards rule out gaming). **A5** 205/8 default deps, ruff-clean, no new dep (stdlib `sqlite3`). SQLite-only + grade-path-only (PG/Aurora + projection columns + OBS `call_kpi` deferred). **Contamination handled:** the executor's first commit swept in 3 concurrently-staged paper files; resolved by soft-reset + rebuild from exact original blobs (**monitor-verified:** `git diff f7b9a2d..HEAD` = 8 files + log, ZERO paper files; paper files re-staged intact; `f7b9a2d` intact; backend untouched) → `git-commit-pathspec-dirty-index` memory. Opened **S-BS-39** (debuglithrim env reds, pre-existing). 4 bench commits + log on `f7b9a2d`, not pushed. Session `session-…WS-6d….json`; critique `critique-…WS-6d….md`. |
| **WS-6c-DSPy-2** — complete the DSPy judge trio (policy + faithfulness lenses) + the prompt-vs-DSPy A/B harness | **✅ done, closed 2026-06-03 (bench `a511012..69b3505` + log `c588904` on `bench-salvage/ws6c-dspy`; audit CLEAN 7/7 monitor-re-verified; HARD-GATE **fresh-critic** NON-BLOCKING (agent `a8930eba`, 0 BLOCKING); bench-only, $0 offline)** | Follow-on to WS-6c-DSPy. The `Judge`/`evaluate_dspy` module was **already role-generic**, so this added the missing **lens layer** (no judge rebuild): `POLICY_JUDGE_LENS` + `FAITHFULNESS_JUDGE_LENS` + `LENS_BY_ROLE` in `judge_metric.py`, **owner-consistent (lens A)** + in-snapshot (monitor + critic both independently re-derived vs `_TIER1_OWNERS`); `build_trio()` in `judges_dspy.py`; and the **prompt-vs-DSPy `ab_harness.py`** (verdict / code-set / None-aware calibration / per-arm `score_judge` diff). **A5 frozen contract:** `compliance_council.py` = **0 lines** in the close-diff (re-verified). **A3 = PASS-with-live-deferred:** harness + both modes implemented + offline-structural tested deterministically ($0), but the **live arm is UNEXERCISED** — the real prompt-vs-DSPy comparison numbers are **N/A this cycle** (offline-only ruling); offline output is HARNESS-LOGIC ONLY (`OFFLINE_NOTE`), never an A/B result. **70 council tests / 2 live-smoke skips on `debuglithrim`** (monitor re-ran); default-deps import openai/dspy-free; ruff clean. Scope held; the 3 staged paper files NOT swept (close-diff = 6 files). **S-BS-12 surfaced two ways** (faithfulness EXCLUDES 4 invited-but-unowned Tier-1 codes → S-BS-43 lower-bound; policy PHI code unnamed in its prompt → S-BS-42). Opened **S-BS-42** (PHI prompt-naming) + **S-BS-43** (co-raise-aware lens → DSPy-3) + **S-BS-44** (fresh-critic: `load_role_prompt` no-`.strip()` → GATE before the live A/B). Optimizer deferred → **WS-6c-DSPy-3**. Session `session-…DSPy-2….json`; critique `critique-…DSPy-2….md`. |
| **WS-6c-DSPy-2-live** — close the S-BS-44 parity gate + the cost-confirmed live prompt-vs-DSPy A/B | **✅ done, closed 2026-06-03 (bench `4d67c93..7031466`; audit CLEAN 7/7 monitor-re-verified incl. a security/secret-leak pass; ROUTINE inline critique NON-BLOCKING; bench-only; ~$0.75–1.5 / 48 Azure calls, within the $3 ceiling, ONE run)** | The deferred live payoff of DSPy-2. **S-BS-44 CLOSED** (`load_role_prompt` `.strip()`s → `build_trio` prompts byte-match `_load_role_prompts:527`; parity-guarded). **D-1 — the cost-confirm gate earned its keep:** the executor caught a **CONFIRMED payload-shape bug** at plan-review — `_context_payload` fed the prompt arm a top-level transcript the council reads only from `call_context.transcript` (`:1159-1161`), which would have false-rejected every case + wasted the spend; monitor-verified the diagnosis, one-line harness fix folded in + offline-guarded. **A3 frozen:** `compliance_council.py` 0 delta vs `c0a5463`. **Live A/B (captured snapshot, n=7):** 85.7% verdict agreement (6/7); near-identical confidence (risk Δ−0.004, faithfulness 0.0, Mistral `None` uncoerced); the load-bearing signal = the **Mistral `policy_judge` elicitation divergence** (verbose fp=22 in the prompt arm, silent fp=0 in the DSPy arm — same model/prompt; **monitor-confirmed GENUINE, zero errors in the artifact** → S-BS-45); DSPy faithfulness out-recalled the prompt arm on labeled `MISSING_ALLERGY`. Per-judge precision = LOWER BOUND (S-BS-43) further saturated by the transcript-only blind spot (clean-negatives false-reject in BOTH arms — the grounding-track motivation). REPORT carries all 4 caveats; the numbers are **NOT a paper claim**. **72 tests / 2 skips** on `debuglithrim`; no secret leaked; scope = 7 files (4 council + 2 `docs/research/` + log), paper files unswept. Closed **S-BS-44**; opened **S-BS-45**. Session `session-…DSPy-2-live….json`; critique `critique-…DSPy-2-live….md`; REPORT `docs/research/REPORT_dspy2_live_ab_2026-06-03.md`. |
| **WS-6c-DSPy-3a** — by-construction judge-calibration corpus + `FABRICATED_CONSENT` injector + co-raise-aware lens (the $0 optimizer precondition) | **✅ done, closed 2026-06-03 (bench `c847501..dad8b6f`; monitor 7-item audit CLEAN; HARD-GATE **spawned fresh-critic** (`a37cff3eeae32c201`) **CLEAN** + 1 NON-BLOCKING; bench-only, $0 offline)** | Re-sequenced ahead of the optimizer after DSPy-2-live showed it **data-blocked** (≤1 council-judge positive/judge, 0 for policy). Landed: (1) the net-new **`FABRICATED_CONSENT` injector** (by-construction-safe — refuses if the transcript already carries a consent token, mutates only `artifact.consent`, transcript byte-unchanged); (2) **`examples/judge_calib_v1.jsonl`** — 47 cases, **lint-GREEN (exit 0)**, **byte-deterministic** (critic regen-twice = identical), **policy 0→12** (FABRICATED_CONSENT x6 + PHI x6) + risk + faithfulness positives + 8 genuine cleans + 2 cross-owner multi-defect; splits **calibration 17 / test 30** (3b's trainset/held-out); (3) the **co-raise-aware lens** (S-BS-43, `judge_metric.py`, opt-in). **Adversarial fresh-critic (cold):** every recipe self-evidently warrants its label (no soft-pass found), all 47 `owner_map`s → v2-production owners (FABRICATED_CONSENT→policy, PHI→policy), 8 cleans genuinely clean, co-raise logic correct both directions — it independently ran the lint + tests + a full regen + scanned all 47 owner_maps. **Closed S-BS-43**; opened **S-BS-46** (VALUE_MISMATCH 1/6 cohort cap, low) + **S-BS-47** (`CLAUDE.md` inv#3 `[]`-vs-`null` doc drift, low); **S-BS-42 + S-BS-45 now ENABLED** (policy positives exist). **#4 process note:** executor shipped `co_raise_aware` opt-in vs the monitor's adjudicated default-ON — accepted as a defensible judgment call, but process-flagged (honor an adjudication or flag the deviation; don't silently revert + relabel). 38 tests green on `debuglithrim`; ruff clean; scope = 9 files; paper unswept. Session `session-…DSPy-3a….json`; critique `critique-…DSPy-3a….md`. |
| **WS-6c-DSPy-3b** — close the DSPy judge loop: BootstrapFewShot optimizer + held-out measurement (the optimize→measure→bind-back stages on the 3a corpus) | **✅ done, closed 2026-06-03 (bench `781abc4..820cf88`; monitor 7-item audit CLEAN re-verified; HARD-GATE **spawned fresh-critic** (agent `a576212aa4bdcd0d1`) **CLEAN** + 1 OPEN-QUESTION; bench-only, ~$0.26 / 24 Azure calls, ONE run, well under $3)** | **The DSPy judge loop is CLOSED + MEASURED for `risk_judge`** — and the held-out Δ is **NEGATIVE and honest** (the gate was never loosened to win). New `lithrim_bench/runtime/council/judge_optimize.py`: `JudgeProgram` (a NEW lazy `dspy.Module` = the compilable view, additive to + separate from the frozen seam-emitting `Judge`, wrapping the SAME `JudgeSignature` + production `_validate_findings` so demos transfer) + `compile_judge` (BootstrapFewShot, `max_bootstrapped_demos=4`, **`max_labeled_demos=0`**, `co_raise_aware=True`) + `evaluate_program` (=`score_judge`, baseline AND compiled) + `bind_compiled_demos` (loop-closure mechanism + test only) + `run_optimize` (PAID, `confirm_cost`-gated) + CLI `scripts/optimize_judge.py` + $0 offline tests + REPORT/4 artifacts. **Held-out (n=10): precision 0.7143→0.4444, recall 0.7143→0.5714, graded 0.80→0.70.** **CONFIRMED diagnostic:** all 4 bootstrapped demos are SILENT (empty findings) — the exact-accept gate admits only already-perfect cases, which on this small mixed corpus are the silent/clean ones, so the judge got 0 positive exemplars + over-fired out-of-lens VALUE_MISMATCH on held-out positives (verbatim demo dump + per-case rows in the REPORT). **A1 frozen 0-delta ×3** (`compliance_council` + `judges_dspy` + `judge_metric`; monitor + critic both re-ran `git diff … | wc -l` = 0). **My 2 plan-review adjustments landed verbatim** (`max_labeled_demos=0` — our examples carry the metric label not gold signature outputs → labeled demos degenerate in dspy 3.2.1; + validate-findings-inside-`JudgeProgram.forward` → the Δ is production-faithful). **The smoke earned its keep** (caught a `BootstrapFewShot`-deepcopy-drops-`set_lm` bug → ambient `dspy.context(lm=)`; committed as an honest broken-then-fixed 4th commit, not a rebase, to dodge the foreign-staged-paper-files index landmine). Scope held (close-diff = exactly 9 files; 3 paper files staged-but-untouched; all commits pathspec'd). Opened **S-BS-48** (bind-back-by-default deferred — and given the negative Δ, do NOT ship these demos) + **S-BS-49** (exact-accept gate harvests only silent demos → positive-exemplar bootstrapping / MIPROv2 / more positives-per-code). OQ-1 (`JudgeProgram` via `build_judge_program()` factory = A3 lazy-import accommodation, non-blocking). Session `session-…DSPy-3b….json`; critique `critique-…DSPy-3b….md`; REPORT `docs/research/REPORT_dspy3b_optimizer_2026-06-03.md`. |
| WS-6b…e · WS-4b · WS-3b | **WS-6b ✅ RATIFIED 2026-06-01** (`9f8b907` plan + `RECOMPOSITION_PLAN_ws6.md` §Ratification); 6c-track ready; rest queued | **WS-6b plan ratified.** Decisions locked: **v2-only** (Azure trio; **Q2 ✅ VERIFIED 2026-06-01** — target Azure resource hosts all 3 deployments, accessible), **recompose 6 KPI agents / drop dead `evaluation_agent` / park LiveKit sim**, **defer KB → WS-6d-KB** (core = floor + suppress), DSPy judges incremental, §10 frozen (WS-5-BFF). **Taxonomy re-snapshotted @ `493b533`** (no council drift; `structural_codes` preserved; lint GREEN proof_case 7/7 + 5 packs) + `snapshot_taxonomy.py` 2 bugs fixed. New seam **S-BS-30** (production_judges hardcode → v2 faithfulness_judge). Downstream: **6c council-PORT** (v2-only, PORT not rewrite) ∥ **6c-DSPy** ∥ **6c-AGENTIC** / **6d** (+**6d-KB**) / **6e**. **MUST heed S-BS-24** (suite not green). **WS-6c council-PORT ✅ CLOSED 2026-06-01** (dedicated row above; `12281e1..6bae3c0`, NON-BLOCKING, A5 live PASS; **S-BS-28 + S-BS-30 closed**, **S-BS-31** opened). Next downstream: **6c-DSPy** (judge rebuild wrapping the ported consensus) ∥ **6c-AGENTIC** (LangGraph recompose + grade-seam wire, **S-BS-31-gated**) / **6d** (+**6d-KB**) / **6e**. |
| **WS-7a** — journey Phase-2 Verify → live BFF (verify-and-close) | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (build via the parallel journey rework `26e479f`; D4 test `0b1d2d0`; monitor verify-and-close + inline critique; `:5180` smoke OWED)** | **The pitch's first live act is real + tested.** WS-7a's deliverable landed NOT via a driver-bounded executor but as part of the parallel-session semantic-moat journey rework — so this was a **verify-and-close, not a build**. `JourneyApp.runVerify`→`POST /v1/run-eval {live:false}` ($0 replay default = **A2✓**), real composite/votes via `gradeResult.council.votes` + `.result.verdict` + `GET /v1/case` (**A1✓**), `bffDown` fixture fallback (**A4✓**). **D4 test added** (`JourneyApp.test.jsx` — the journey's FIRST test; it surfaced + polyfilled the `matchMedia`/`scrollTo` jsdom gaps the rework never hit; **full shell suite 33/33**, `vite build` clean = A5 build+test). **A3 DEVIATED (accepted):** the journey extended the BFF (`/v1/case` + a `council` view on `/v1/run-eval` + `in_process`) vs the driver's "zero new BFF" — additive + justified (needs the real case + a votes view + the in-process toggle) but outside the §10-locked surface. **OWED:** the load-bearing `:5180` visual-parity smoke (user-run, no-autostart). Opened **S-BS-50** (journey hardcodes `BFF_URL`, bypasses `bff.js`'s `VITE_BFF_URL` indirection → packaging) + **S-BS-51** (the §10 v1 BFF surface grew unratified). Acts 1/3/4 of the rework = the broader semantic-moat journey (live-wiring = WS-7b/7c). Critique `critique-…WS-7a-2026-06-04.md`. |
| **UAP-1** — config-plane write-path (`/v1/agent`) + draft→grade + audit/actor foundation (R0/R1/R3) | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (`6bdf975..a7edd25` + log `c2e01ae`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `a62e561dc12854912`) NON-BLOCKING [0 BLOCKING / 2 NB / 2 OQ]; PROCEED-WITH-CAVEATS — A8 `:5180` smoke OWED)** | **The FIRST build of the LOCKED unified-authoring product.** **R1** `GET/PUT /v1/agent` (assemble + persist an Agent to the config plane; 422 on malformed; config-DB-only, never the seed). **R3** the **draft→grade loop** — `run_eval.run(ontology_path=)` threads one resolved source through BOTH committed-seed reads (`:109`+`:142`) + the BFF `_resolve_ontology_path` prefers the working copy, so a PUT-ed draft actually grades (**S-BS-26(b) CLOSED**; A2 = a real verdict flip `reject→needs_review`, seed byte-unchanged). **R0** the **immutable append-only AuditRecord/actor foundation** (`harness/audit.py`, §2B 9-field shape, INSERT-only `config_audit`, agent-row+audit-row in ONE transaction [N4], honest `{system,dev-default}` actor [N5]) wired onto every config write + `GET /v1/audit` + `GET /v1/runs/{id}/audit` (sync `PIPELINE_RUNS.get` projection, clean-404 on un-persisted [N1]). All **5 monitor plan-review notes folded** (N1–N5). **A5 monitor-re-verified:** `compliance_council` 0-delta + committed seeds 0-delta. A1–A7 PASS (monitor re-ran: ruff-clean on the 6 cycle files, default 267/11, debuglithrim 324/3, Vitest 37/37); **A8 PASS** (post-close `:5180` smoke 2026-06-04, Chrome MCP: Agent editor mounted + live, audit log live with §2B who/when/what/why+before/after). §10 ratified in-cycle (`a7edd25`). Opened **S-BS-52** (replay not auditable → UAP-3) + **S-BS-53** (OQ §2B before/after) + **S-BS-54** (ontology endpoints 404 on unknown) + **S-BS-55** (run-audit attaches stage-shared evidence per-judge → UAP-3) + **S-BS-56** (`/v1/run-eval` doesn't surface `pipeline_run_id` → UAP-3) + **S-BS-57** (stale `app.py` docstring). **CLOSED S-BS-26(b) + S-BS-51; S-BS-50 STAYS OPEN** (monitor correction — the hand-back over-claimed it; the *journey's* hardcode is untouched, journey frozen). Session `session-…UAP-1….json`; critique `critique-…UAP-1-2026-06-04.md`. |
| **UAP-2** — judge authoring via ontology-assignment (`JudgeEditor` + `/v1/judges` + the prompt↔ontology bridge, R2) | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (`86fa1d3..3e89ad5`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `a8c77571d1a911d6d`) NON-BLOCKING [0 BLOCKING / 3 NB / 2 OQ]; PROCEED-WITH-CAVEATS — A8 `:5180` smoke OWED)** | **The headline judge-creation gap shipped — a judge is now authorable as (role + ASSIGNED ontology-flag subset + model + execute-only validator refs).** **The bridge (load-bearing):** `judge_assignment.py render_role_questions` is LAYERED — the `council_roles/<role>.txt` seed base + an authored-refinement section from the assignment; `build_trio(ontology=, assignments=)` threads it into the in-process council so an authored judge re-votes with its authored lens (no WS-2 `:8002`). **A4 byte-parity proven for all 3 roles against the LIVE council loader** (`_load_role_prompts`) ⇒ **S-BS-11 safety prose preserved verbatim**, and `faithfulness_judge` (0 ontology questions) keeps its full 164-line base. **`GET/PUT /v1/judges`** + `harness/judges.py` doc-shim store (shared `audit.upsert_with_audit` txn; `config.save_agent` refactored onto it, no copy-paste; `target.type=judge` audit row). **The owner↔emit 422 gate keys off `LENS_BY_ROLE`, NOT the stale ontology `owner_roles`** (CITATION-DRIFT → S-BS-59: the seed's `owner_roles` are v1 roles w/ no `faithfulness_judge`; gating on them would 422 a correct `faithfulness←MISSING_ALLERGY`) + in-snapshot/gradeable (inv #1/#4); both adversarial cases tested + no inert-owner authoring (404 on `behavior_judge`); a guard test pins `LENS_BY_ROLE↔_TIER1_OWNERS`. **`JudgeEditor` gen-UI** (`tool-judge_editor`, KNOWN_TOOLS→8) — the **`$0` assignment→prompt→questions preview = demonstrability**, all fetches via `bff.js`. **MID-CYCLE (APPROVED, `74920df`):** the bridge extracted to a council-LIGHT `judge_assignment.py` so the BFF `$0` preview needs no `[council]` — **import-isolation monitor+critic-verified** (dspy/openai absent on default deps; S-BS-61 records the clean `judge_metric` import). **§10 ratified.** **A5 frozen 0-delta** (monitor+critic re-verified): `compliance_council` + `clinical_v1.json` + `data/config/agents` + `council_roles/*.txt` + the per-judge seam dict (the lone grep flag was a ruff reflow). **Suites monitor-re-ran:** default 277/12 · council 94/3 · BFF 30 · Vitest 40 (all `$0`, live env-gated). **CLOSED S-BS-57; opened S-BS-59** (re-snapshot stale ontology `owner_roles` — deferred, seed frozen) **+ S-BS-60** (question-text write-back deferred, §12.2 approved cut) **+ S-BS-61** (low/informational). Session `session-…UAP-2….json`; critique `critique-…UAP-2-2026-06-04.md`. **Post-close A8 `:5180` smoke (2026-06-04, Chrome MCP):** UAP-1 legs PASS in-browser (Agent editor mounted + live; audit log live). UAP-2 backend PASS live (`$0` — bridge renders risk_judge 5580→6712 on the `WRONG_DOSAGE` assign with S-BS-11 prose preserved; owner↔emit **422** on the non-owned `FABRICATED_CONSENT` via `LENS_BY_ROLE`; `target=judge` audit row). One PAID in_process v2-trio run (~`$0.20`, user-go'd) confirmed the live grade end-to-end (`BLOCK`/escalate; gpt-4.1/Mistral-`None`/Llama; calibration PASS; `pipeline_run_id:None` = S-BS-56 live). **Gaps:** JudgeEditor not UI-mounted → **S-BS-62** (fix task SPAWNED); authored assignments not threaded into `grade_inprocess` so authoring≠live-flip yet → **S-BS-63** (UAP-3). **A8: UAP-1 PASS; UAP-2 backend PASS, in-UI demonstrability + authored→flip deferred.** |
| **UAP-3** — processing surface + run-history + the authored→flip wiring (R4/R6, S-BS-63) | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (`5959e28..b3079d2`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `a98779f542ddee6c5`) NON-BLOCKING [0 BLOCKING / 5 NB / 3 OQ]; PROCEED-WITH-CAVEATS — A8 `:5180` smoke PASS [post-close 2026-06-04: UAP-1+UAP-3 in-browser; UAP-2 in-UI deferred S-BS-62; in_process paid flip ran but no visible flip on ws0_default + S-BS-66/69])** | **The authoring loop closes: an authored judge now re-votes with its authored lens through the in-process grade (S-BS-63).** **The seam (i), no orchestrator edit:** new `runtime/council/authored_stage.py build_authored_semantic_stage` builds the `build_trio(ontology=, assignments=)` trio ABOVE the frozen `_apply_consensus` and reuses `run_semantic(council_evaluate=)` for the consensus→StageResult mapping — so `_apply_consensus` is only CALLED, never modified; heavy deps lazy-imported inside the `--in-process` branch (A7 import-clean). **A1 proven $0/offline (debuglithrim):** assigning `WRONG_DOSAGE`→`risk_judge` flips composite non-reject→`reject`, the OTHER two roles don't move (the change is the AUTHORING, not the case); **preview↔live parity test-anchored** on the SAME `render_role_questions` the `$0` JudgeEditor preview returns (refinement #2) — critic independently traced the flip is causal, not trivially-passing. **A2 frozen-set = 0 diff lines** over the FULL set — `compliance_council`+`judges_dspy`+`judge_metric`+`clinical_v1.json`+`data/config/agents`+`council_roles/*.txt` (monitor + fresh-critic both re-ran `wc -l`=0; refinement #1). **Run-history real:** `/v1/run-eval` surfaces `pipeline_run_id` (S-BS-56), `GET /v1/runs` lists newest-first via the new `DocShimCollection.list_all`, replay+live+in_process ALL persist provenance now (S-BS-52) so the `$0` replay default is auditable + round-trips to `/v1/runs/{id}/audit`. **`POST /v1/eval-pack/run`** batches a pack (R6, replay/live). **`RunPanel` gen-UI** (`tool-run_panel`, KNOWN_TOOLS→9) triggers runs + shows verdict/votes/history, paid modes confirm-gated. **§10 ratified** (`8088d88`) + the row-shape widened to the shipped 6-field projection at close (S-BS-67). **Green bar monitor-re-ran ($0):** ruff-clean changed files (repo-wide 1297 = pre-existing port debt + `.claude/worktrees` inflation, S-BS-34, not a regression), default 284/13, debuglithrim uap3+bff+council 133/3, Vitest 44, import-clean, vite build clean. **CLOSED S-BS-63 + S-BS-56 + S-BS-52.** **Opened S-BS-64** (med — live `:8002` per-judge injection WS-2-gated; S-BS-63 closes in_process only) **+ S-BS-65** (low — eval-pack replay/live only) **+ S-BS-66** (low — authored in_process votes empty `model`+`reason`; INHERITED DSPy-seam property, judges_dspy 0-delta; **promote to a gate before the authored path is an audit exhibit**) **+ S-BS-67** (low — §10 row-shape, FIXED at close) **+ S-BS-68** (low — doc-shim `created_at` re-stamp vs §2B append-only; defer, fix scoped to the blob tier). **OWED:** the A8 `:5180` smoke incl. the PAID in_process authored-flip (Decision 3 batched it — the visceral finale). 8 atomic pathspec-only commits on `bench-salvage/ws6c-dspy`, NOT pushed. Session `session-…UAP-3….json`; critique `critique-…UAP-3-2026-06-04.md`. **Post-close A8 `:5180` smoke (2026-06-04, Chrome MCP):** **UAP-3 PASS** — RunPanel mounted (panes.jsx:172; Replay/Live/In-process modes); `$0` replay → `reject` + 3 judge votes + run-id `a57bd49d` surfaced (S-BS-56); **run-history 17 live** (S-BS-52); `POST /v1/run-eval` + `GET /v1/runs` both 200. **UAP-1 re-confirmed** (Agent editor + §2B "why·when·who·what" audit trail live). **UAP-2 in-UI NOT reachable** (S-BS-62 — `tool-judge_editor` is the only authoring widget NOT mounted in CenterPane). **PAID in_process authored run fired live** (assigned `WRONG_DOSAGE`→`risk_judge` via `PUT /v1/judges` since the JudgeEditor is unmounted; the `$0` preview showed the bridge render 5580→6712): the composite report renders in-browser (`live·paid`, `reject`, 6 active findings, **1 grounded suppression = the S-BS-7 floor**) BUT **(a) NO visible authored-flip on `ws0_default`** (no wrong-dosage defect in the case; risk correctly found none; verdict stayed `reject` from `FABRICATED_HISTORY`/`INCOMPLETE_DOCUMENTATION` — the constructed A1 flip doesn't reproduce on this already-reject case) and **(b) S-BS-66 is WORSE than recorded** → promoted. **DRIVE/COST NOTE:** the RunPanel paid gate is `window.confirm()` (RunPanel.jsx:57); a native dialog **freezes the renderer to CDP + can't be accepted via the MCP**, so the paid in_process run was fired via the BFF API instead (**1 call, ~`$0.10–0.20`, as scoped**; the gate works, no bypass) → **S-BS-69** (use an in-DOM modal). Additional in_process runs in that window were **user-fired** — an initial "auto-accept/overspend" read was WRONG (the automated click fired nothing: count stayed 17). **A8 verdict: UAP-1 + UAP-3 PASS; UAP-2 in-UI deferred to UAP-5a.** |
| **UAP-5a** — the in-UI authoring loop: mount `JudgeEditor` (S-BS-62) + legible authored audit (S-BS-66) + a by-construction live-flip case | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (`d7be919..2faf8d3` + log `e2c48a6`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `aafd47567039a9a19`) NON-BLOCKING [0 BLOCKING / 4 NB / 3 OQ]; PROCEED-WITH-CAVEATS — A4 live-flip FAIL + A8 `:5180` smoke OWED)** | **Two of the three release-critical gaps SHIPPED; the visceral live-flip did NOT reproduce (surfaced honestly, not fudged).** **A1 (S-BS-62 CLOSED):** `tool-judge_editor` mounted in `CenterPane` (`panes.jsx`, mirrors `tool-agent_editor`) — the `$0` assignment→prompt preview is reachable in-UI; Vitest 45/45 incl. a `panes.test.jsx` mount guard. **A2 (S-BS-66 CLOSED):** the authored (injected DSPy) in_process per-judge audit is now LEGIBLE through the SAME council-view + `/v1/runs/{id}/audit` surfaces — `stages.py` mapping-layer ONLY: `model` falls back to the seam's own `model` field (the role name), `reason` synthesized from decision + grounded finding codes (`_synth_reason`; a clean approve stays empty — HONEST, fresh-critic-verified not-fabricated), `council_config.judges` projected to the real roster off `["injected"]`. CONFIRMED live on a paid in_process run (rid `8f5bbcda`: `risk_judge {model:'risk_judge', reason:'reject — WRONG_DOSAGE', findings:['WRONG_DOSAGE']}`; was all-None/`["injected"]` before). **A3 frozen-set 0-delta** (monitor + fresh-critic both re-ran `git diff <frozen set> | wc -l`=0). **A4 (FAIL on the live half, user-pre-authorized):** the OFFLINE deterministic flip PASSES by construction (`test_uap5a_flip_demo.py`, injected marker-predictors, $0); the LIVE composite-flip is NOT reproduced — the base trio already returns BLOCK on the by-construction subtle `WRONG_DOSAGE` (`risk_judge` caught "zidovudine 450MG vs transcript 300MG", a 1.5× drift), so authoring SHARPENS an already-firing lens rather than flipping a miss. `WRONG_DOSAGE` is base-owned ⇒ no cheap calibration near-miss; surfaced honestly (NOT manufactured — CLAUDE.md labels-true-by-construction) → **S-BS-70**. The demo case is admissible (lint GREEN, fresh-critic re-ran `[OK] TIER_1 WRONG_DOSAGE`; owner `risk_judge` production-resident; the injector's deterministic output, not gamed). **Live spend ≈ $0.40** (2 in_process runs, under the ~$2–3 ceiling). **A5 green:** debuglithrim 264/0, default 255p/7s, Vitest 45/45, import-clean, ruff-clean new files (one pre-existing-class `UP006` in `stages.py` matching the file convention, S-BS-34). **The fresh-critic earned its keep:** D2-honesty HONEST + A4-integrity PRINCIPLED, and it caught the **refuted "base under-fires" premise stated as FACT** in the demo-agent note + generator docstring (the live run REFUTED it) → monitor-corrected at close. **CLOSED S-BS-62 + S-BS-66.** Opened **S-BS-70** (med — the visceral live verdict-flip needs a defect the base trio MISSES: a non-v2-base-owned code or the SEMANTIC axis = the grounding/withstands-gate moat; the paid "watch the verdict change" finale is still OWED) **+ S-BS-71** (low — authored-path `JudgeVote.model` duplicates `judge_role` since the FROZEN seam carries no deployment id; additive `authored_stage.py` follow-up). **A8 `:5180` ATTESTED LIVE 2026-06-06** (monitor, Chrome MCP + BFF API, ~$0.10–0.40, one paid `live=true :8002` call): `tool-judge_editor` is MOUNTED + renders the assign-lens + ontology refinement questions in `CenterPane` (the S-BS-62 gap — A1 visual PASS); the e2e run (`8a41ef3e`) returned a real verdict (risk_judge PASS / policy_judge + faithfulness_judge BLOCK → composite `needs_review`/WARN/0.45) with the **S-BS-7 grounding suppression firing LIVE** (`MEDICATION_NOT_IN_TRANSCRIPT` suppressed by `med-presence-check/v1` — zidovudine verbatim; BLOCK→WARN), persisted (run-history 31→32) + auditable (run→audit blob w/ KB evidence spans) + loaded back into the shell Run-provenance card. The SAFE/suppression direction — NOT the engineered correction-flip (that stays **S-BS-74**); no flip manufactured. Opened **S-BS-85** (low — `Load run` rebinds the center provenance card but not the right Evaluation-report pane). Un-exercised: the `$0` JudgeEditor toggle-updates-preview. Evidence `docs/research/RUN_uap5a_a8_live_2026-06-06.json`. 6 atomic pathspec-only commits on `bench-salvage/ws6c-dspy`, NOT pushed. Session `session-…UAP-5a….json`; critique `critique-…UAP-5a-2026-06-04.md`. |
| **UAP-3b** — THE MOAT: the §2A signals bus + the per-judge PRE-consensus withstands-gate | **✅ CLOSED-WITH-CAVEATS 2026-06-04 (`8cb388b..9eadf39`, parent `acc4973`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `ae312d9a358e1ca69`) NON-BLOCKING [0 BLOCKING / 5 NB / 3 OQ]; PROCEED-WITH-CAVEATS — D3/A6 + LLM-critique + LIVE attestation SPLIT to UAP-3b-2)** | **THE MOAT LANDED (offline/$0): a deterministic signal corrects a confidently-wrong LLM judge PRE-consensus → the composite verdict flips — the genuine "watch the verdict change" S-BS-70 said was needed.** **A2 (gate-attributable):** on a REAL clean-negative case (`expected==approve`), `risk_judge` confidently raises `PHI_DISCLOSURE_PRE_VERIFICATION` (Tier-1, OUTSIDE its lens, sole-owner `policy_judge` silent); **gate OFF** → the off-domain FP flows through (`ground()` has no contract for it) → the frozen seam's OWN off-domain-Tier-1 downgrade → MEDIUM → composite `reject`; **gate ON** → the ontology-rule signal rejects it pre-consensus → composite `approve` = the by-construction truth RESTORED. **SAME case + SAME predictor, only `apply_gate` differs ⇒ the flip is GATE-ATTRIBUTABLE** (the double-assertion; defeats the theater critique). The fresh-critic INDEPENDENTLY traced the baseline-reject rides the FROZEN seam's own logic (`PHI∉PHI_FALSE_POSITIVE_TYPES` → off-domain single-judge downgrade `:2107`), NOT test rigging. **Build:** `signals.py` (D1, per-judge typed signals = ontology rules [tier/owner via `LENS_BY_ROLE`, NOT the stale `owner_roles`] + validator outputs, REUSING `grounding.py` executors) + `withstands.py` (D2, `apply_withstands_gate`: suppress a validator-disproved finding [S-BS-7 per-judge] + reject an out-of-lens finding NO owning judge corroborates [the DSPy-3b over-fire]; down-rank ONLY when ALL blocking findings stripped) wired into `authored_stage._evaluator` BETWEEN the trio results and `_apply_consensus` (`apply_gate=True` DEFAULT = the moat is ON; `apply_gate=False` = the faithful no-gate baseline, monitor+critic confirmed the gate block is the only delta). **A2′ consensus-coherence:** the gate flips the FROZEN `_apply_consensus` decision itself (reject→approve, 2-judge corroborated MED-FP both suppressed) — what post-consensus `ground()` structurally can't do. **A3 frozen-set 0-delta** (monitor + critic both re-ran `wc -l`=0; test-pinned `test_frozen_seam_zero_delta`). **A4 audit:** `AuditLog.record` (action `withstand`/`flip`, the §2B `{signals_weighed, decision, what_failed}` ruling + run/case ids — the D4 primitive correction vs the driver's `upsert_with_audit`) + `build_withstands_correction` RLVR record. **A5 by-construction guard:** a true in-lens finding withstands; a corroborated-owner finding kept — the gate CANNOT relabel a true case (critic-verified under the worst adversarial case incl. the dormant-owner probe → defensible coverage choice consistent with S-BS-12). **Green:** full `tests/` 273 passed / 0 fail (critic re-ran), import-clean, ruff-clean 7 files. **D-E SPLIT (pre-authorized): A6 SKIP** — D3 (GroundingCheck first-class entities) + the LLM Ralph-Loop critique + the LIVE attestation → **UAP-3b-2** (stub driver written). Opened **S-BS-72** (low — withstands ruling not yet in the run-provenance BLOB, only AuditLog/config_audit + the NDJSON; needs an orchestrator seam above the frozen consensus → UAP-3b-2; critic-CONFIRMED principled split, not a dodge) + **S-BS-73** (low — the gate is ON by default on the authored in_process path; INTENDED [the moat] but changes live authored verdicts vs pre-UAP-3b, offline-tested only; validate on the UAP-3b-2 live attestation). 6 atomic pathspec-only commits, NOT pushed. Session `session-…UAP-3b….json`; critique `critique-…UAP-3b-2026-06-04.md`. |
| **UAP-3b-2** — complete the moat: the run-provenance withstands blob (S-BS-72) + GroundingChecks first-class (A6) + the LIVE attestation | **✅ CLOSED-WITH-CAVEATS 2026-06-05 (`662a1c0..24318a7`, parent `8a159c0`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `acd1046fff8c76e9a`) NON-BLOCKING [0 BLOCKING / 2 NB / 3 OQ]; PROCEED-WITH-CAVEATS — the live CORRECTION halt-to-surfaced honestly → S-BS-74)** | **The moat's operational + audit layers landed CLEAN; the live SAFE half is attested; the visceral live CORRECTION is honestly OWED (NOT manufactured).** **D2/S-BS-72 CLOSED:** the per-judge withstands ruling is embedded in the run-PROVENANCE blob via a post-save patch in `run_eval` (`_embed_withstands_in_blob`: `find_by_id` → embed `withstands_decisions` → re-insert the FULL doc; **ABOVE the frozen seam** — no `_apply_consensus`/`PipelineProvenance`-model edit, returned dict byte-identical, fk preserved by construction; `created_at` re-stamp disclosed S-BS-68-adjacent) + surfaced at `GET /v1/runs/{id}/audit` (a 1-line BFF `_run_audit_report` projection). **D3/A6:** GroundingChecks are first-class config-authored entities — `EvalProfile.grounding_checks` (additive, default `()` → every committed agent incl. `ws0_default` byte-unchanged) + per-entity `grounding_check` `AuditRecord`s (action `run`/`suppress`/`floor_block`, distinct from the gate's `withstand`/`flip`) projecting `ground()`'s partitions WITHOUT changing them (`ground()`/`composite()` byte-additively-identical for `clinical_v1`, test-pinned). **A4 frozen 0-delta** (monitor + critic both `wc -l`=0; the critic additionally confirmed `withstands`/`signals`/`authored_stage`/`grounding`/`report`/`audit` ALL 0-delta = REUSE-not-rewrite). **THE LIVE ATTESTATION (2 paid in_process runs, ~$0.20-0.40, driven via the BFF API per S-BS-69):** **Run A (PASS-honest, rid `087a59f8`):** D2/S-BS-72 attested LIVE on the REAL paid v2 trio — `/v1/runs/{id}/audit` carries the 3-judge ruling; all `decision=withstand`, verdict BLOCK → **S-BS-73 HOLDS (the on-by-default gate ADMITS a legitimate WRONG_DOSAGE finding, doesn't break it)**; the critic read this STRAIGHT FROM the sqlite blob, independent of the log. **Run B (HALT-TO-SURFACE, rid `2275720c`):** the authored DSPy trio raised only in-lens findings + risk PASSed despite a `MEDICATION_NOT_IN_TRANSCRIPT` assignment → NO live `decision=corrected`; the documented MED FP is a PROMPT-council artifact the better-calibrated authored DSPy trio doesn't reproduce (+ the out-of-lens flip can't be authored, `effective_lens=base+assigned`) → **surfaced honestly, NOT manufactured (the UAP-5a non-convergence lesson recurs)** → **S-BS-74**. **The executor HONORED the monitor D-B sharpening** (labeled Run A SAFE-direction, NOT 'the moat attested'). **Green:** default 270p/10s import-clean, debuglithrim 282p (incl. the S-BS-72 integration), Vitest 45/45, ruff-clean (critic re-ran all). **CLOSED S-BS-72.** Opened **S-BS-74** (med — the live CORRECTION needs a case ENGINEERED for the authored DSPy trio to over-fire = S-BS-70's visceral finale + S-BS-73's corrected-direction; a dedicated demo cycle vs accept the offline-mechanism + live-SAFE proof) + **S-BS-75** (low — the critic's OQ: the SAME `PresenceCheck` resolved differently across loci [pre-consensus gate `withstand` vs post-consensus `ground()` disproved] on Run A; verdict-neutral + inherited UAP-3b [`signals.py` 0-delta], but the §2A locus=BOTH coherence isn't guaranteed — the gate's `case_view` reconstruction vs `ground()`'s case). 5 atomic pathspec-only commits, NOT pushed. Session `session-…UAP-3b-2….json`; critique `critique-…UAP-3b-2-2026-06-05.md`. |
| **UAP-4** — the calibration-trainer OPTIMIZE loop, LIVE from the UI (R5): optimize→honest-Δ embedded in `JudgeEditor` (the bind/re-run round-trip SPLIT → UAP-4-opt) | **✅ CLOSED-WITH-CAVEATS 2026-06-05 (`d494ba9..faf06b5` + live-evidence + close-out, parent `e9f3b82`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `a057f68220fe14f85`) NON-BLOCKING [0 BLOCKING / 4 NB / 2 OQ]; PROCEED-WITH-CAVEATS — the live optimize Δ is an HONEST LOSS, S-BS-49 sharpened → S-BS-76)** | **The demonstrable-live optimize→honest-Δ loop landed; the live paid attestation rendered an HONEST LOSS — and the loss is the strongest evidence yet for the thesis (you cannot few-shot your way to grounding; the deterministic floor is the reliable lever, optimize is the honest MEASUREMENT instrument).** **RIGHT-SIZED v3 after a LIVE :5180 reassessment** (the user-requested live-UI + code-scan caught the v2 "full round-trip" as over-engineering: `calibration_chart` is a RELIABILITY diagram NOT a Δ [the Δ render is net-new]; the Run-evaluation/verdict surface already exists; optimize's Δ is negative so bind→re-run can't fire live → the round-trip SPLIT → **UAP-4-opt**). **Build:** BFF `POST /v1/judges/{role}/optimize` (cost-gated, 422 w/o `confirm`) + `optimizeJudge` client; optimize embedded in `JudgeEditor` (an in-DOM cost modal [S-BS-69, never `window.confirm`] → a NET-NEW inline Δ render, baseline→optimized precision/recall/graded WIN-OR-LOSS, a ≤0 Δ rendered EXPLICITLY as a loss [R1]); the S-BS-49 lead lever `order_positive_first` (a trainset REORDER only — `judge_metric` FROZEN, the accept-gate NEVER loosened) + corpus-widening 47→83 (positives 6→12/code, EVERY new positive pinned to `calibration` so the held-out `test` split is FROZEN [30 corpus / 10 risk_judge] — both arms measure the identical set, monitor condition #2) + a DURABLE committed superset-verify vs a frozen pre-widen fixture (monitor condition #1). **A4 frozen 0-delta** (monitor + critic both `wc -l`=0 over `_apply_consensus`+`judges_dspy`+`judge_metric`+`authored_stage`+seeds+roles); **NO round-trip leak** (`harness/judges.py` untouched, no `/bind`, no `compiled_demos` — critic-confirmed). **THE LIVE PAID RUN (ONE call, ~$0.26-0.40, 55s, via the JudgeEditor route `POST /v1/judges/risk_judge/optimize {confirm:true}`):** held-out (FROZEN, n=10, both arms identical) baseline graded 0.80 / P 0.7143 / R 0.7143 → optimized graded 0.70 / P 0.6250 / R 0.7143; **delta graded −0.10 = an HONEST LOSS.** The S-BS-49 selection fix WORKED (`n_positive_demos` 0→4 vs the all-silent WS-6c-DSPy-3b prior) but did NOT lift the held-out: the 4 positive WRONG_DOSAGE demos induced a NEW over-fire (clean negative `bench_scribe_v1_clean_negative_5074ab5a99d4` raised WRONG_DOSAGE = FP) → **S-BS-76 (positive few-shot demos over-fire — `SPEC_CALIBRATION_TRAINER §4.1`'s non-monotonic lever, now confirmed on the DEMO axis).** Gate NEVER loosened, NOT re-fired (no win-fishing). **A3 PASS-HONEST** (measured Δ), **A-LIVE FULLY ATTESTED LIVE** (route + DOM render: a user-authorized 2nd run via Chrome MCP drove the full loop and rendered the loss EXACTLY [graded 0.80→0.70, P 0.71→0.63, +1 FP, tp_fp_fn [5,2,2]→[5,3,2]], the honest-loss note verbatim, a fresh `result_dspy3b` artifact written; returned <8s vs 55s = LLM-cache → near-$0 incremental; ≤2 paid runs total; nothing owed). **A1/A2/A5 PASS** (loop reachable + one continuous surface; honest Δ win+loss Vitest-proven; green default 280/10 + debuglithrim 390/3 + Vitest + ruff + lint GREEN; critic re-ran). **UAP-4-opt** (bind→re-run) STAYS BLOCKED — no positive Δ to bind. Opened **S-BS-76** (med) + **S-BS-77** (low — `_demo_raises` tested but `run_optimize` inlines it; DRY) + **S-BS-78** (low — the win/loss headline keys on `delta.graded` only; mixed-metric honesty); **S-BS-49 stays open, SHARPENED by S-BS-76**; **S-BS-46 carried** (faithfulness coverage still gated by `VALUE_MISMATCH ×1`). 7 atomic pathspec-only commits + the live-evidence + the close-out, NOT pushed. Session `session-…UAP-4-2026-06-05.json`; critique `critique-…UAP-4-2026-06-05.md`; live evidence `docs/research/RUN_uap4_optimize_live_2026-06-05.json`. |
| **UAP-5b** — the conversational shell (R11, the CAPSTONE): the agent-loop SPINE + a 3-tool core (Python Agent SDK in the BFF, BYO-Claude, paid-gated) | **✅ CLOSED-WITH-CAVEATS 2026-06-05 (`336c69a..2944c55` + close-out, parent `590a330`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `a9e2c41ed28e24f9f`) NON-BLOCKING [0 BLOCKING / 4 NB / 2 OQ]; PROCEED-WITH-CAVEATS — A-LIVE owed/USER-RUN → S-BS-79)** | **The conversational capstone's SPINE landed: talk → the agent calls an in-process SDK-MCP tool → an audited config write → the EXISTING gen-UI card renders. The conversation IS the audit log (R0).** **D0 (the gating de-risk) PASSED:** a trivial `ClaudeSDKClient` + one `@tool` ran on **BYO-Claude/local-CLI auth with `ANTHROPIC_API_KEY` unset** (`claude-agent-sdk 0.2.90`, claude CLI 2.1.143) → **OQ-6=B + the licensing guardrail HOLD.** **Build:** `POST /v1/chat` (SSE) hosts `ClaudeSDKClient` over **3 core ops-as-tools** — `author_judge` (audited PUT-logic) + `get_judge` ($0 preview) + `run_eval` (**REPLAY-ONLY**); `apps/bff/agent/{tools,loop,adapter}.py` (additive, the wrapped ops FROZEN); the shell composer wired to a fetch-stream SSE client; tool-results adapt to the EXISTING `tool-judge_editor`/`tool-verdict_card` (no new cards); an in-DOM `CostModal` (never `window.confirm`) gates the human-only paid run. **A-SAFE AIRTIGHT** (the critic tried to break it 5 ways + failed): `RUN_EVAL_SCHEMA={"agent":str}` (no paid key), the handler hardcodes `run_eval_replay` ($0), `ChatRequest` no paid knob, `allowed_tools` = exactly the 3 `mcp__lithrim__*` (no Bash/Read/Write → `bypassPermissions` safe-by-construction); **the ONLY paid door is the human's `CostModal`-confirm** — the agent has NO code path to spend/relabel/bypass (an off-lens assignment 422s + is surfaced). **A6 frozen 0-delta** (council set all 0; `app.py` additive-only, 0 deletions on the existing handlers — monitor + critic both `wc -l`). **pydantic-settings 2.1→2.14 (the `[agent]`-extra transitive bump) NON-REGRESSING** (council+BFF 128p/3s re-run, monitor + critic). **import-isolated** (`claude_agent_sdk` lazy — absent from default-deps even after importing council/agent/app). **Green:** uap5b 8p, council+BFF 128p/3s, default 287p/11s, Vitest (chat 2p / 50 full), ruff + shell build clean. **A-LIVE = ATTESTED LIVE 2026-06-05 (monitor, Chrome MCP — S-BS-79 CLOSED):** the conversational slice ran end-to-end on BYO-Claude — talk → `author_judge` (audited write **VERIFIED against the BFF**: `risk_judge.assigned_flags=['WRONG_DOSAGE']` + an audit record actor=`dev-default`) + `run_eval` replay (verdict rendered); A-SAFE behavior live (the agent surfaces the 422 gate, never bypasses). The conversation IS the audit log — PROVEN, not claimed. **`get_judge` broke live → S-BS-82** (the offline tests missed it — the value of live attestation; the agent degraded gracefully). **The full Domain→Review-from-scratch journey → UAP-5c** (stub registered). Opened **S-BS-79** (med — the A-LIVE attestation owed) + **S-BS-80** (low — the pre-existing TopBar "Run live" still fires paid with NO modal, out of scope) + **S-BS-81** (low — critic NB-1: A-SAFE is safe-by-ALLOWLIST not safe-by-mode → UAP-5c MUST add an allowlist-bounds test). Carries to UAP-5c: `author_judge` is model-unbound (NB-2) + the verdict-card agreement/confidence projection is best-effort (NB-3). 6 atomic pathspec-only commits + the close-out, NOT pushed. Session `session-…UAP-5b-2026-06-05.json`; critique `critique-…UAP-5b-2026-06-05.md`. |
| **UAP-5c** — the FULL Domain→Judge→Flag→Run→Review-from-scratch conversational journey (R11): grow the UAP-5b spine over the journey-completing tools + close the two carried seams | **✅ CLOSED-WITH-CAVEATS 2026-06-05 (`a4e313d..9227b87`, parent `9cac025`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `ac63797ec49ec01ac`) NON-BLOCKING [0 BLOCKING / 1 NB / 1 OQ]; PROCEED-WITH-CAVEATS — A-LIVE owed/USER-RUN)** | **The full journey now runs from a blank agent over the proven spine; both carried seams CLOSED.** **3 new in-process SDK-MCP tools**, each a THIN wrapper over a FROZEN op → an EXISTING gen-UI card (no new types): `get_agent` (`$0` Domain read → `tool-agent_editor`) + `author_flag` (audited `PUT /v1/ontology`, the Flag leg, **EDIT-ONLY by construction** → `tool-flag_editor`) + `review_runs` (`$0` Review read → `tool-audit_log`). **S-BS-82 CLOSED:** the live `get_judge` break fixed in the WRAPPER (`_build_tool_context._get_judge` passes `assigned_flags=None`); `get_judge_endpoint` **BYTE-IDENTICAL** (critic AST-verified all 10 wrapped ops 0-delta). The regression test is **NON-VACUOUS** — the critic reverted the fix → the test's load-bearing line raises pre-fix; it drives the BOUND closure, not just the TestClient. Generalized to a binding-layer rule (memory `fastapi-endpoint-as-plain-call-fieldinfo-trap`). **S-BS-81 CLOSED:** A-SAFE is now **safe-by-explicit-bound** — the allowlist-bounds test asserts `allowed_tools` == exactly the `mcp__lithrim__*` set (no built-in, no wildcard) + no-paid-knob generalized across all 6 schemas + an `[agent]`-gated bind to the real `bypassPermissions` `ClaudeAgentOptions`. **A-SAFE cleanest-possible:** the D-F SPLIT (assemble_agent + eval-pack → UAP-5c-2) means NO new tool wraps a paid-capable op — the critic's grep for any agent-reachable paid path is **EMPTY**; the Review leg uses pure-read `tool-audit_log` after confirming `RunPanel.jsx:57` gates its paid Run with `window.confirm` (off the chat surface → the A-LIVE stays Chrome-MCP-driveable/in-DOM). **A6 frozen 0-delta** (council seam/judge_metric/authored_stage/seeds 0; all 10 wrapped BFF ops byte-identical; both `app.py` hunks strictly inside `_build_tool_context`). `max_turns 8→12` for the longer journey. **import-isolated** (default + the `[agent]` extra installed). **BYO-Claude no-key.** **Green:** default 293p/12s, debuglithrim (`[agent]`+council+bff+uap5b/5c) 143p/3s, Vitest 51p/11f, ruff clean — all monitor + critic re-run. **Opened S-BS-83** (low — `author_flag` is EDIT-ONLY; conversational flag CREATION needs `owner_roles` authoring + a snapshot refresh → stays a human act, the correct owner↔emit posture; tracks the from-scratch vision). **Deferred (carried):** NB-2 model-bind, NB-3 verdict-projection. **A-LIVE = ATTESTED LIVE 2026-06-05 (monitor, Chrome MCP, `$0` BYO-Claude):** the full conversational journey ran end-to-end on the live `:5180` shell — talk → `get_agent` + **`get_judge`** (the **S-BS-82 fix VERIFIED LIVE** — the exact UAP-5b `'Query' object has no attribute 'split'` crash is GONE; the agent reported the risk_judge config: lens `WRONG_DOSAGE`, model unbound, 6 questions) → **`author_flag`** (the audited `ontology:ws0_default` tier-2 edit **VERIFIED against the BFF `/v1/audit`** [actor `dev-default`, ts `2026-06-05T15:53:35`, why="Reclassify DURATION_FABRICATION to tier 2…"] — *the conversation IS the audit log, proven server-side not just claimed*) → `run_eval` replay (verdict card + run-provenance `a57bd49d`) → **`review_runs`** (the §2B audit_log "Audit trail" card rendered). **A-SAFE held live** (replay/`$0` + audited writes only; no paid run fired; the human-only Run buttons untouched). All 3 new tools + the S-BS-82 fix attested; A1–A4 + A-SAFE + A-FIXED all PASS live. **SPLIT → UAP-5c-2:** `assemble_agent` (`PUT /v1/agent`) + `run_eval_pack` (eval-pack batch). 7 atomic pathspec-only commits, NOT pushed. Session `session-…UAP-5c-2026-06-05.json`; critique `critique-…UAP-5c-2026-06-05.md`. |
| **UAP-5c-2** — complete the tool set (the UAP-5c D-F split): `run_eval_pack` (eval-pack batch) + `assemble_agent` (edit-one-facet) | **✅ CLOSED-WITH-CAVEATS 2026-06-05 (`3578905..1ff7ea8`, parent `b2a8c92`; monitor 7-item audit CLEAN [re-verified] + HARD-GATE genuinely-fresh-critic (agent `af82bf4b57ab6c3af`) NON-BLOCKING [0 BLOCKING / 2 NB / 1 OQ]; PROCEED-WITH-CAVEATS — A-LIVE ATTESTED LIVE 2026-06-05)** | **Both pre-authorized SPLIT tools shipped → the complete 8-tool surface; the `run_eval_pack`-alone fallback did NOT trigger (edit-one-facet stayed thin).** **`run_eval_pack`** (eval-pack BATCH) is the FIRST wrapper over a paid-capable op — its schema carries NO paid knob + `_run_eval_pack` **hardcodes `EvalPackRunRequest(..., live=False)`**; the load-bearing negative test injects `live=True`/`confirm`/`in_process` into the tool args + asserts `req.live is False` (**critic-verified NON-VACUOUS** — scratch-reverting `live=False`→`live=True` FAILS the test at line 229). **`assemble_agent`** (the FIRST agent-reachable Agent WRITE) is **EDIT-ONE-FACET** (loads via `get_agent` → edits only `eval_profile.judges` → puts; NEVER a full dict — the over-engineering trap avoided), `LENS_BY_ROLE`-guarded (unknown role → 404, never fabricates a judge), audited (`target_type=agent`). Both → EXISTING cards (`tool-audit_log` / `tool-agent_editor`; review/pack pure-read, no `run_panel`). **A-SAFE re-proven safe-by-explicit-bound:** the allowlist-bounds + no-paid-knob tests now cover all **8** tools (the `[agent]`-gated test binds the real `bypassPermissions` `ClaudeAgentOptions`). **A6 byte-frozen:** `put_agent_endpoint` + `eval_pack_run_endpoint` BYTE-IDENTICAL; council/seam/seeds 0-delta; all new code inside `_build_tool_context`. import-isolated + BYO-Claude no-key. **Green:** default 299p/12s, debuglithrim 149p/3s, Vitest 51p/11f, ruff clean — all monitor + critic re-run. **Opened S-BS-84** (low — `assemble_agent` roster REMOVE has no write-time owner↔emit/non-empty revalidation; the ADD path IS guarded; critic-probed = audited + reversible + no crash → NOT an A-SAFE escape; the empty-roster terminal case is a degraded verdict [critic sharpening] → OQ-1: should REMOVE refuse emptying?) **+ NB-1** (cosmetic: `ASSEMBLE_AGENT_SCHEMA` types add/remove_judge `str` but the closure defaults `None`). **Deferred (carried):** NB-2 model-bind, NB-3 verdict-projection. **A-LIVE = ATTESTED LIVE 2026-06-05 (monitor, Chrome MCP, `$0` BYO-Claude):** both new tools ran end-to-end on the live `:5180` shell — **`run_eval_pack`** batched `clinical_v1`/`ws0_default` (verdict `reject`, persisted) and the agent confirmed it **REPLAY-ONLY / `$0` / "can never fire a paid batch"** (the FIRST paid-capable wrapper stays replay-only LIVE — the `live=False` hardcode holds in practice; a live batch is the human's confirmed UI action) + **`assemble_agent`** did the audited roster round-trip (remove `policy_judge` → add back, **VERIFIED against the BFF `/v1/audit`**: 2 `target_type=agent` records, judges `[risk,faithful]`→`[risk,faithful,policy]`, actor `dev-default`) AND the **`LENS_BY_ROLE` guard refused a fabricated judge** (no write — **0 fabricated_judge audit records**, the agent surfaced "you can't invent a judge", no bypass). A1–A4 + A-SAFE all PASS live; the roster ends restored (ws0_default not degraded). 6 atomic pathspec-only commits, NOT pushed. Session `session-…UAP-5c-2-2026-06-05.json`; critique `critique-…UAP-5c-2-2026-06-05.md`. |

---

## Open seams

| ID | Title | Severity | Status |
|---|---|---|---|
| S-BS-1 | v1 pure-legacy vs findings-first | medium | **resolved** — findings-first w/ validated spans (pre-check P0) |
| S-BS-2 | v2 over-fire family (FABRICATED_CONSENT / INCOMPLETE_DOCUMENTATION / …) | medium | **open — CONFIRMED on live v2 by the WS-0 smoke test** (over-fires on the scribe note); ADDITIONAL grounding targets → WS-3 |
| S-BS-3 | blast radius of authoritative findings | high | **superseded/moot** — the aggregation fix it guarded doesn't land |
| S-BS-4 | SQLite shape (doc-shim vs relational) | medium | **resolved (WS-1)** — doc-shim applied (4 collections + config plane); rationale recorded in `collections.py`/`config.py` docstrings |
| S-BS-5 | local vector stub; sqlite-vec blocked | medium | open → WS-3 (numpy over rag receipts; or `:8002 /v1/kb/search`) |
| S-BS-6 | v1-vs-v2 as the canonical baseline | medium | **resolved/ratified (WS-1)** — compose-over-live-v2 stored in `ws0_default.eval_profile.council_config.disposition` (STORED only; injection = WS-2). Harness baseline is live-v2 + grounding cleans the over-fire. |
| S-BS-7 | MED FP is judge-calibration, not aggregation | **high** | **HYPOTHESIS confirmed (WS-0)** — the presence-check disproves+suppresses the live MED FP end-to-end (the WS-0 worked example landed). Generalize from one hardcoded contract to the ontology's verification_contract layer in WS-1; mid-loop (judge-calls-tool) in WS-3. |
| S-BS-8 | live `/v1/pipeline/evaluate` returns structural/artifact findings with **null taxonomy code** (4 in the WS-0 baseline; S-P1-16 generalization) | medium | **handled in WS-0** (skip-logged into the `ungrounded` bucket, retained in active, surfaced in report — never dropped). Upstream code-backfill in `lithrim-backend artifact_evaluator` remains out of harness scope. |
| S-BS-9 | same `case_id` carries divergent `expected_compliance_verdict` shape across pack files (string `'reject'` in `scribe_v1.jsonl` vs accept-set list `['needs_review','reject']` in `scribe_v1.n10.jsonl`) | low | **resolved (WS-1)** — code-level shape contract + `normalize_expected_verdict`/`expected_block`/`load_case` in `picklist.py`; collision order documented + deterministic (PACK_FILES n10-first, consumers pin via `load_case`). Residual (Q4.3): bare `resolve_case_fixtures` still n10-first by default → future pack-hygiene note. |
| S-BS-12 | S-BS-10 lint is **one-directional** — `gradeable_flags_outside_snapshot` (`seed_ontology.py:160`) catches "runtime tiers a flag the snapshot hasn't blessed" but NOT the reverse: a snapshot code the runtime stops tiering silently becomes `gradeable=false` with no lint. Per `CLAUDE.md` (snapshot = contract-of-record) the reverse is arguably the more dangerous divergence | medium | **open — opened WS-2 bench-side fresh-critic Q4.1; CONFIRMED.** Inert today (19==19). Recommend: lock the source-of-truth direction + a **bidirectional** lint (snapshot-code-not-gradeable ⇒ FAIL or explicit allowlist). Carry into the WS-2 backend driver or a small follow-up. Fix loc: `scripts/seed_ontology.py:152-160`. |
| S-BS-11 | `build_prompt` is hardcoded clinical prose, not templated from the taxonomy — truly domain-agnostic *prompting* (injected ontology generating the prompt) is a large rewrite of safety-critical prose | medium | **open — opened WS-2 D2 checkpoint; CONFIRMED.** `build_prompt:517-622` embeds HIPAA/FABRICATED_CONSENT/category-code rules as English; only taxonomy ref is "one of the codes from the SAFETY FLAG TAXONOMY below" — no tier-list templating. **WS-2 D2 scope LOCKED to the tractable seam:** instance-resolvable tier/owner/known-code LOOKUP (Surface A) + model selection (Surface B) + additive-block-only prompt threading; full prose rewrite deferred to **S-BS-11** (future cycle). Fix loc: `../lithrim-backend/app/services/compliance_council.py build_prompt:517 / build_source_message_prompt:1036`. |
| S-BS-10 | ontology flag-source fork — 4 flags (`FABRICATED_CONSENT_SCOPE`, `MALAFFI_CODE_PROPAGATION`, `MISSING_DUAL_CODING`, `WRONG_PATIENT_INFO`) seeded from council `safety_flags.py` are **absent from `taxonomy/taxonomy_snapshot.json`** (the CLAUDE.md contract-of-record) | medium | **DECIDED 2026-05-31 (user) = Option A: snapshot-authoritative + lint gate.** Diagnosis CONFIRMED: the 4 are *defined-but-untiered* in the backend itself (defined in `safety_flags.py:132/414/444/473`, absent from `compliance_council.py` TIER_1/2/3 + `_TIER1_OWNERS`) — already inert in grading. **Resolution (WS-2 deliverable-0, harness-only):** ontology gradeable set = the 19 tiered snapshot codes; `seed_ontology.py` cross-checks every seeded flag against `taxonomy_snapshot.json` and partitions **gradeable (in-snapshot, tier+owner)** vs **reference (out-of-snapshot, `tier=null, gradeable=false`)**; grounding/`_rescore` use gradeable-only; `safety_flags.py` stays a prose source for in-snapshot codes only. Lint FAILS if a *gradeable* flag is outside the snapshot. Rejected: backfill (Option B — needs a backend tier/owner clinical decision, separate cycle) and snapshot-as-prose-source (Option C — bigger schema change, noted as eventual cleanup to drop the `safety_flags.py` coupling entirely). |
| S-BS-13 | `run_eval` passes `http_client=None` → a declared floor uses the LIVE `:3031` path even under `--replay` (no offline floor-apply replay) | medium | **open → WS-4 (gate).** CONFIRMED (WS-3a). Inert today (clinical_v1 declares no floor) + the *test* path injects a fake client, so A3 holds. WS-4's eval-pack loop MUST add a floor-apply replay capture before any domain ships a floor contract. Fix loc: `scripts/run_eval.py run()`. |
| S-BS-14 | Floor declared without the `[verification]` extra raises ImportError at apply-time, not config-load | low | **open.** CONFIRMED (WS-3a). Cosmetic/DX — no early "install lithrim-bench[verification]" message. Fix loc: `grounding.py _run_floor` or ontology-load validation. |
| S-BS-15 | `_CONTRACT_EXECUTORS` is now the SUPPRESS-only registry but keeps its generic name (floor types live in the sibling `_FLOOR_CONTRACT_TYPES`) | low | **open.** Naming clarity only; rename to `_SUPPRESS_EXECUTORS` explicitly deferred this cycle (not worth the churn). Fix loc: `grounding.py`. |
| S-BS-16 | Floor-injected flag bypasses the gradeable/owner partition + severity validation — injected into `active` AFTER the per-finding loop (`grounding.py:355-365`), so `inject_flag_code`/`inject_severity` are read raw with no check that the code is a declared gradeable flag (owner per invariant #4) or the severity a key in `severity_map.weights` | **medium** | **open → follow-up cycle (WS-3b/WS-4), GATE before any floor-declaring ontology ships.** Fresh-critic Q4-3; touches the `CLAUDE.md` core invariant ("labels true by construction; never silently scored"). Inert for the shipped clinical ontology (no floor). A misconfigured floor could BLOCK on an ownerless code, or silently never fire on a typo'd severity (`weight_of`→0.0). Recommend: validate `inject_flag_code` ∈ `ontology.gradeable_flags()` + `inject_severity` ∈ `severity_map.weights` at ontology-load, raising loudly. Fix loc: `grounding.py:355-365` / ontology-load. |
| S-BS-17 | **Shell demo content is customer-support, not the clinical/Scribe/healthcare first domain.** The WS-5 skeleton's representative data (`apps/shell/src/data.jsx`, `app.jsx:14` "acme-support", `support_transcripts.jsonl`) diverges from `JOURNEY_brief` Phase 1 (Scribe agent + healthcare pack) and the harness `clinical_v1` ontology. Harmless as throwaway mock, but the shell, the journey, and the harness must agree on the demo domain before the journey port renders real content. | low | **RESOLVED/CLOSED (WS-5c).** Shell mock converged to clinical/Scribe (`acme-health` / Scribe Agent v4 / `scribe_transcripts.jsonl` / faithfulness·safety·structural / fabricated-allergy failure modes) — shell + journey + harness `clinical_v1` + `JOURNEY_brief` now agree. grep for support/customer/refund/`acme-support` → none. Fixed in `67492a0` (`apps/shell/src/data.jsx` + `app.jsx`/`panes.jsx`/`cards.jsx` prose). |
| S-BS-18 | **React↔BFF binding is manual-smoke-only.** The WS-5-BFF "first behavioral test" (`tests/test_ws5_bff.py`) guards the **BFF round-trip** (Python/TestClient), NOT the React side (`bff.js` fetch client + `ReportTab` real-composite render). A2/A3 rendered correctly but by manual smoke + a user screenshot, not an automated test. | low | **RESOLVED/CLOSED (WS-5c).** Vitest+RTL+jsdom infra stood up + the `bff.js`→`ReportTab` binding test (the S-BS-7 clinical composite renders through `artifact.jsx`: reject banner + FABRICATED_HISTORY active + MED suppression + calibration) `043ece4`; plus `app.test.jsx` Shell-`TopBar` guard `e9f863b`. 19/19 tests, 4 files. Fix loc was `apps/shell/src/{bff.test.jsx,app.test.jsx}` + `test/setup.js`. |
| S-BS-19 | gen-UI input widgets return via `onResult` but the scripted journey doesn't yet emit input `tool-parts` that mount them | low | **RESOLVED/CLOSED for the Shell host (WS-5d, `20474ed`).** `CenterPane` now mounts the 3 input tool-parts (FlagEditor/ContractBuilder/KbPicker), threading each `onResult` into local config-plane state; the datapoint `part.output` prop convention is **locked to flat-spread** (VerdictCard conformed, `{data}` wrapper dropped — the Ambiguity-2 lock). **Journey-act (jp1–jp4) mounting NOT done → carried to S-BS-26.** Fix loc was `apps/shell/src/panes.jsx` + `genui/VerdictCard.jsx` + `genui/registry.js`. |
| S-BS-20 | Radix Select/Slider interactions tested only on default/value-driven paths (jsdom has no layout engine) | low | **open → WS-5e.** CONFIRMED (WS-5c). A3 tests assert collect+return via default state + Input/Button paths (reliable in jsdom); opening a Radix Select popover or dragging a Slider is not exercised (needs pointer-capture/layout). Real interaction covered by manual smoke; a Playwright/E2E layer (WS-5e) closes it. Fix loc: `apps/shell/src/genui/*.test.jsx`. |
| S-BS-21 | **Backend test-rot: 2 collection errors** (`tests/test_agent.py:11` stale `app.agents.intent_quality_agent` import; `tests/test_council_evidence_extraction.py:12` removed `RetrievalMatch` from `hipaa_retrieval`) | low | **open** (WS-6a; backend repo; was S-WS6A-1/2; pre-existing, not WIP). Update/remove the dead imports. |
| S-BS-22 | `tests/services/pipeline/test_stages_phase_b.py:352` asserts a dict but `judge_votes` is a list (`AttributeError: 'list' object has no attribute 'keys'`); abuts the WS-6a pipeline surface but fails identically on clean HEAD | medium | **open** (WS-6a; was S-WS6A-3; pre-existing). Decide test-vs-shape drift. |
| S-BS-23 | 3 committed WIP ruff nits preserved as-authored (`models.py:10` F401 `typing.Sequence`; `stages.py:20` + `tests/routes/test_audit_view_surface_fixes.py:18` I001) | low | **open** (WS-6a; was S-WS6A-4; baseline-preserve, no drive-by reformat). `ruff --fix` in a cleanup. |
| S-BS-24 | **Backend `-m "not slow"` suite is RED on `mvp-ready`** (52 failed + 2 errors, Mongo up), pre-existing not WIP (failure-set identical clean-HEAD vs WIP); the suite is **NOT a green regression gate** | **medium** | **open** (WS-6a; was S-WS6A-5). **WS-6b MUST NOT assume a green suite**; recommend a dedicated backend test-health cycle. |
| S-BS-25 | **WS-6a critique Q2 test gaps:** PIPELINE_GRADING_AUDIT fix #3/#8 name sites not unit-tested — D-E `stages.py:585 _judge_votes_from_models` (e2e-only); D-K `stages.py:806/860` `council_error` derivation (`insufficient_valid_models` branch has neither unit nor e2e) + `pipeline.py:1047 _denormalise_audit_view` rendering | low/medium | **open** (WS-6a critique; backend test-health follow-up, pairs with S-BS-24). |
| S-BS-26 | **Two honest-close riders from WS-5d:** (a) **journey acts (jp1–jp4) mount no input tool-parts** — S-BS-19 is closed for the Shell `CenterPane` host only, but the 4-act journey (SPEC §3 "the conversation in the center journey writes the config plane") still emits none; (b) a **`PUT /v1/ontology` draft does NOT feed an eval run** — GET prefers the working copy so the UI shows the edit, but `run_eval.py:118` reads `agent.ontology_abspath()` (the committed seed), so "edit the ontology in the UI" ≠ "edits take effect in grading" | low | **(b) draft→grade CLOSED 2026-06-04 (UAP-1 R3** — `run_eval.run(ontology_path=)` threads the working copy through `run_eval.py:109`+`:142`; the BFF `_resolve_ontology_path` prefers it; A2 verdict flip, seed byte-unchanged). **(a) journey-act tool-part mounting STAYS OPEN → WS-5e** (frozen journey; UAP-1 mounts the parts in the Shell host, not the frozen acts). CONFIRMED (WS-5d fresh-critic, independently re-verified by the monitor). Folds the two fresh-critic OPEN-QUESTIONs for the spec author: *should ontology-edit drafts feed a grade?* + *is Shell-host-only the intended S-BS-19 close boundary?* Inert for the demo (drafts are UI-scoped; journey runs on `journeyData`). Fix loc: `apps/shell/src/journey/jp1-jp4.jsx` (journey-act mount) + the BFF working-copy → `run_eval` ontology source (`run_eval.py:118`). |
| S-BS-27 | **DSPy council prototype = validated prior art for the judge rebuild (WS-6c-DSPy).** The cross-provider DSPy council (`council_v2.py`, mirroring backend JUDGES_V2: gpt-4.1/Mistral/Llama via one Azure adapter) RAN on Azure and produced real verdict data (`v2_runs.ndjson` 28 rows; `smoke_result_v3_grounded.json` clean-negative reject→APPROVE flip). It deliberately dropped the consensus IP (`council_dspy.py:17-21` "DELIBERATE SIMPLIFICATION ... none of that is reproduced"). | low | **RESOLVED/CLOSED (WS-6c-DSPy 2026-06-02).** The §6 hybrid is now realized clean in-package (`runtime/council/judges_dspy.py`): a DSPy `risk_judge` emitting the conforming seam shape, wrapping the ported `_apply_consensus`. The throwaway worktree was **referenced, never imported** (critic-confirmed: no worktree DSPy import in the package). Its non-conforming shape (`violations:[str]`/singular `error`) + dropped consensus IP were the gap the rebuild closed. |
| S-BS-28 | **Port-source discipline for the council PORT (WS-6c).** A STALE parked council mirror exists at `lithrim_bench/runtime/council/compliance_council.py` (2733 lines, but `diff` DIFFERS from backend; snapshot pins `ba84608` vs backend `493b533`). **PORT FROM `lithrim-backend@493b533`, NOT the mirror.** Also: the tier/owner/pillar tables are SPLIT across `compliance_council.py` (tiers/owners) + `app/models/safety_flags.py` (flag prompt-text); grabbing only one silently loses Tier-1 owner-gating. | **medium** | **RESOLVED (WS-6c 2026-06-01).** Ported from `493b533` (both tables files: `compliance_council.py` + `app/models/safety_flags.py`); monitor re-diffed byte-faithful. The "stale mirror" drift was mechanical only (the 6 import lines + `_ROLE_PROMPTS_DIR`), not logical: the re-derive reproduced the parked bytes (zero git delta on the IP files). |
| S-BS-29 | **Stale taxonomy snapshot.** `taxonomy/taxonomy_snapshot.json` was pinned at `ba84608` vs backend `493b533`. | low | **RESOLVED (WS-6b ratify 2026-06-01).** Re-stamped `ba84608`→`493b533`; **no council-taxonomy drift** (TIER_1/2/3 + owners + judges set-identical, re-sorted only); `structural_codes` (5) preserved; admissibility lint GREEN (`proof_case` 7/7 + 5 scribe packs). Two `snapshot_taxonomy.py` bugs fixed en route (sys.modules/exec_module import crash + lossy structural_codes drop). |
| S-BS-30 | **`production_judges` is hardcoded** `["policy_judge","risk_judge","behavior_judge"]` in `snapshot_taxonomy.py:69` (not council-derived), but the v2 production trio is `risk/policy/faithfulness_judge` (`council_v2.py` V2_ROLES; backend JUDGES_V2). With **v2-only** locked (Q1), the recomposed council's trio is risk/policy/faithfulness. | medium | **RESOLVED (WS-6c 2026-06-01).** `snapshot_taxonomy.py` now DERIVES `production_judges` from the v2 council (decision 4a: instantiate-under-v2, offline, no network); re-snapshot gives `[risk_judge, policy_judge, faithfulness_judge]`, `declared_but_not_running` gains `behavior_judge`, structural_codes preserved. Surfaced the orphaned-owner consequence as **S-BS-31**. |
| S-BS-31 | **3 Tier-1 flags have no production-resident owner under v2-only.** Under the v2 trio (risk/policy/faithfulness), `MISSING_ALLERGY` / `FABRICATED_CONSENT` / `VALUE_MISMATCH` are owned only by `behavior_judge`/`source_message_judge` (both declared-but-not-running), so they never single-judge one-strike; only 2+ corroboration escalates them. 2 of 3 (`MISSING_ALLERGY`, `VALUE_MISMATCH`) are used in `examples/proof_case.jsonl`. Touches CLAUDE.md invariant #4; same snapshot↔runtime divergence class as **S-BS-12** + **S-BS-16**. `_TIER1_OWNERS` ported VERBATIM from `493b533` (PORT-not-rewrite/A2), so this is the backend's real v2 property, NOT a bench bug; the admissibility lint gates D1/D8 only (not owner-residency) so it stays green. | **medium** | **CLOSED 2026-06-02** (option (a) reassign-by-domain; backend `6720c70` @ mvp-ready + bench `903478e`; fresh-critic C2 verified owner↔emit pairing, **0 inert owners**). `faithfulness_judge` owns `MISSING_ALLERGY` + `VALUE_MISMATCH`; `policy_judge` owns `FABRICATED_CONSENT`. **`risk_judge` co-ownership of `MISSING_ALLERGY` was DROPPED at impl** — it would be inert (`risk_judge.txt` emits `FABRICATED_ALLERGY`, not `MISSING_ALLERGY`); `faithfulness_judge` alone (it emits `MISSING_ALLERGY`) restores one-strike. Restores single-judge Tier-1 one-strike under v2. Principled (not arbitrary): `faithfulness_judge.txt:14` already emits `MISSING_ALLERGY` (the v1 `behavior_judge` → v2 `faithfulness_judge` succession), and consent is policy's domain (`risk_judge.txt:26` + `policy_judge.txt:12`). Impl (WS-6c-AGENTIC D0, cross-repo): backend `_TIER1_OWNERS:232` + add `VALUE_MISMATCH` to `faithfulness_judge.txt` + `FABRICATED_CONSENT` to `policy_judge.txt`, re-port byte-faithfully to bench, re-snapshot taxonomy. Closes on D0; pair an owner-residency lint with S-BS-12. CONFIRMED (WS-6c). Fix loc: `../lithrim-backend/app/services/compliance_council.py _TIER1_OWNERS:232`. |
| S-BS-32 | **S-BS-31 footprint in the bench default-deps suite.** Under v2-only, 3 default-deps tests are RED, in **two distinct failure modes** (fresh-critic-corrected; the earlier "all 3 = ValueError" record was imprecise): (1) `tests/test_packager.py::test_multi_defect_worst_of_verdict` → `packager.py:153 ValueError: recipe.safety_flag 'MISSING_ALLERGY' has no production-owning judge`; (2+3) `tests/test_packager.py::test_single_defect_pack` (WRONG_DOSAGE) + `tests/test_triage_pack.py::test_missed_escalation_case_packages_as_tier1_reject` (MISSED_ESCALATION) → `AssertionError {'behavior_judge','risk_judge'} ⊄ {'risk_judge'}` (they hard-code the now-dormant `behavior_judge` as an expected production owner). The bench analogue of **S-BS-24** + the test-surface manifestation of **S-BS-31**. | medium | **CLOSED 2026-06-02 (WS-6c-AGENTIC D0, `903478e`).** Resolves in two ways, **fresh-critic C3 verified not gamed**: (a) the re-port + re-snapshot restores `faithfulness/policy` production ownership → mode-(1) `ValueError` clears GREEN; (b) a **principled assertion update** on the 2 mode-(2/3) tests — drop the stale `behavior_judge` expectation, assert the v2 production owner `risk_judge` (owns+emits both WRONG_DOSAGE + MISSED_ESCALATION) — GREENs them while KEEPING discriminating power (still requires the correct owner; the negative-control non-owner oracle test stays intact). 10/10 green; **NOT** a re-snapshot-alone fix (driver A0 corrected). Fix loc: the 2 test files + the re-snapshot. |
| S-BS-33 | **The per-judge seam doc omits the optional `citations_used` read.** `runtime/council/__init__.py:43-49` documents a 5-key seam (`model/decision/confidence/findings/errors`), but `_apply_consensus` also reads `result.get("citations_used")` at `compliance_council.py:1942` (the S29 citation→span merge, new-format branch). | low | **open → WS-6d-KB** (rerouted; WS-6c-AGENTIC scope explicitly deferred it). CONFIRMED (critic, from-source). Verdict-NEUTRAL + non-fatal (`.get()`, optional) + **inert** (no KB this cycle; the council emits no citations and the suppress/floor path doesn't use it). Becomes live once KB lands — a judge that should surface citations would need to emit this 6th key. **Decide `citations_used` emission at WS-6d-KB; update the seam doc then.** Fix loc: `runtime/council/__init__.py:43-49` (doc) + `judges_dspy.py` (emission, AGENTIC). |
| S-BS-34 | **`ruff check .` is not clean — 865 pre-existing errors** (`runtime/` byte-faithful port ~388 + `.claude/worktrees/` throwaway + `scripts/` ~30; `ruff.toml` has no `exclude`). WS-6c-AGENTIC introduced **ZERO new** (`compliance_council.py` 96 at base `6bae3c0` == 96 at HEAD; the executor's new files are ruff-clean — fresh-critic C5 verified). | low | **open (WS-6c-AGENTIC).** Fix = a `ruff.toml` `exclude` for the vendored port + worktrees + scripts, **NOT a reformat** (re-styling the byte-faithful port would break the A2/A4 invariant). Not a green gate (same posture as S-BS-24); A7 holds on the cycle's own code. Fix loc: `ruff.toml`. |
| S-BS-35 | **A5 live-evidence hygiene + grade-wire real-council in-suite coverage.** The WS-6c-AGENTIC A5 milestone (the council scores a real case live) is backed by a **self-reported** manual `grade_inprocess` run on `proof_case:drop_allergy` (session-log prose, **no committed execution artifact**); and `test_grade_wire.py` exercises the plumbing via **injected** stages (importorskip-gated, skipped on default-deps), so the **real v2 council path is not asserted in-suite**. fresh-critic C4/C6. | medium | **CLOSED 2026-06-02 (option B; addendum `9b2a9ce` fixture+gated-test + `5cf438b` log corrections; 1 cost-confirmed paid call).** All 4 parts delivered: (a) ONE live `grade_inprocess` v2-trio call on `proof_case:drop_allergy` → **committed fixture** `tests/fixtures/ws0/a5_live.drop_allergy.json` (BLOCK/`reject`, Mistral `None`, 29,679 tok); (b) env-gated real-council `grade_inprocess` test (no stage injection) — passes live / skips $0 by default = the in-suite real-council coverage C6 found missing; (c) session-log A5 narrative disambiguated from the separate `test_live_smoke.py` `ws6c_live_smoke_fabricated_history` fixture + `MISSING_ALLERGY` one-strike marked **offline-only** (live judges emit FABRICATED_ALLERGY/FABRICATED_HISTORY/…); (d) `run_eval.run` claim corrected → "extended backward-compatibly (`in_process=` added)". Monitor re-verified A4 intact post-addendum (0-delta `118af7d..HEAD`) + A7 187/7. |
| S-BS-36 | **Audio-DSP agent bodies deferred behind `[observation]` (tier-A+ boundary).** The recomposed `transcription`/`audio_analysis`/`technical_metrics` agents are interface-complete (Protocol-conformant, hoisted) but raise `ObservationExtraRequired` on real-audio paths — the whisper/torch/numpy/librosa bodies + a ported `transcript_parser` are NOT ported. The text path is fully functional. | low | **open (WS-6c-OBS).** Inert — **no harness consumer reads the KPI output yet**, so the text-path-real A+ is the correct scope (monitor disposition). Promote to a tier-B full-audio-port follow-up cycle ONLY when a KPI consumer materializes. Fix loc: `lithrim_bench/runtime/observation/agents/audio.py` + a full `[observation]` audio dep set. CONFIRMED. |
| S-BS-37 | **Ported observation models use deprecated `datetime.utcnow`.** `models.py` `CallKPI.created_at/updated_at` `default_factory=datetime.utcnow` (faithful to backend `call_kpi.py`) → 8 py3.12 DeprecationWarnings in the suite. | low | **open (WS-6c-OBS).** Cosmetic; no behavior impact. Fix = `default_factory=lambda: datetime.now(UTC)` when the backend port is reconciled. CONFIRMED. |
| S-BS-38 | **Persistence = two-tier blob+projection architecture (recorded direction 2026-06-02, NOT a defect).** Immutable JSON eval-output **blobs** = source-of-truth (fs/sqlite-blob desktop → **S3/ADLS** cloud; RLVR/lake-bound; `corpus.py` already "lake-bound via etlp file→S3") + a rebuildable relational **projection** of the reported numbers/meta (verdict/scores/ECE/`cost_tokens`/run-meta = the `report.py` output; SQLite columns desktop → **PG/Aurora** VPC, re-derivable from the blobs) — both behind the ONE `ProvenanceStore` Protocol. | info | **open → 2 future phases:** (P1) promote the reported numbers to query columns (the projection tier); (P2) S3/ADLS blob impl behind the Protocol (just another impl, like PG). **WS-6d builds the blob tier ONLY** (full `PipelineProvenance.model_dump` JSON into `PIPELINE_RUNS`; `sort_keys` deterministic); forward-compatible by construction. Do NOT add projection columns early — that's the relational-creep **S-BS-4** doc-shim-minimal forbids (WS-6d plan-review scoped it out). Memory: `persistence-blob-projection-architecture`. |
| S-BS-39 | **`debuglithrim` *full* suite has 12 pre-existing reds** (10× `test_ws5_bff` TestClient-version error + 2× WS-6c-OBS import-isolation tests that are default-deps-only **by design** — they assert no-heavy-dep-leak, so they "fail" where `[council]` deps exist). | low | **open (WS-6d).** Structurally NOT WS-6d (different subsystems — WS-5-BFF + OBS isolation); **identical failure-set at parent `f7b9a2d` → not a regression**. The env-analogue of **S-BS-24** for `debuglithrim`; NOT a green gate (WS-6d's own `tests/test_ws6d.py` = 6/6 on `debuglithrim`, 205/8 default deps). Fix = env-aware skips / a TestClient pin in a dedicated test-health pass. |
| S-BS-40 | **`debuglithrim` venv `starlette 0.27.0` × `httpx 0.28.1` breaks `fastapi.testclient.TestClient(app=…)`** → `TypeError` on **every** `tests/test_ws5_bff.py` test (incl. untouched pre-existing). | low | **open (WS-7b-py).** Pre-existing env break, NOT WS-7b-py; the env-analogue of **S-BS-39/S-BS-24** for the BFF test surface (new BFF KB tests proven by direct-handler call). Spawn-task filed to align the venv deps. *(The headless executor mis-filed this as "S-BS-16" — corrected.)* |
| S-BS-41 | **`run_eval` threads no `http_client` into `ground()`** → a *committed* `kb_grounding` (suppress) contract would hit **live `:8002` under `--replay`** (the S-BS-13 analog for the KB suppress path). | medium | **open (WS-7b-py) → GATE before any `kb_grounding` ships in a committed ontology.** Inert today (no `kb_grounding` declared in committed `clinical_v1`). Fix = replay `http_client` injection + validate at ontology-load; pairs with **S-BS-13** + **S-BS-16**. *(Executor mis-filed as "S-BS-17" — corrected.)* |
| S-BS-42 | **`policy_judge.txt` never names `PHI_DISCLOSURE_PRE_VERIFICATION` though policy is its sole Tier-1 owner** (`_TIER1_OWNERS:255`) — the live policy judge likely UNDER-raises it (code string absent from the prompt). | low | **open (WS-6c-DSPy-2).** A measurable recall gap the (deferred) live A/B surfaces, NOT a lens error — the lens correctly keeps the code (sole owner, in-snapshot). Fix = name the code in `council_roles/policy_judge.txt` (a judge-prompt-authoring edit on the calibration-trainer track). CONFIRMED (DSPy-2 fresh-critic + monitor). *(Executor first-filed as "S-BS-34" — collision with the open ruff-exclude seam; monitor renumbered at close.)* |
| S-BS-43 | **Owner-consistent lens (A) makes per-judge precision a LOWER BOUND** — a judge legitimately corroborating another owner's code (which its role prompt invites) scores as an out-of-lens FP. | low | **CLOSED (WS-6c-DSPy-3a `8e70331`).** The co-raise-aware lens landed in `judge_metric.py` (`co_raise_aware` kwarg; `neutral = raised & (expected−lens)`, `fp = raised − truth − neutral`) — **fresh-critic-verified correct in both directions** (neutral-on-corroboration + still-FP-on-genuine-overfire; a non-expected over-fire can never enter `neutral`). **Opt-in by default** (lower-bound stays the conservative default; 3b + precision reports pass `co_raise_aware=True`). *(Monitor #4 ruling was default-ON; executor shipped opt-in — accepted as a defensible judgment call, process-noted in the critique.)* CONFIRMED (`test_faithfulness_lens_in_scope_perfect_but_corroboration_is_out_of_lens_fp`: FABRICATED_ALLERGY corroboration → fp=1). *(Executor first-filed as "S-BS-35" — collision with the closed A5-hygiene seam; monitor renumbered at close.)* |
| S-BS-44 | **`judges_dspy.load_role_prompt` does not `.strip()`** the role-prompt text while the prompt-council's `_load_role_prompts` does (`compliance_council.py:527`) → a whitespace asymmetry between the DSPy judge's prompt and the prompt-council's, plus a docstring over-claim ("the SAME prompt text"). | low | **CLOSED (WS-6c-DSPy-2-live `4d67c93`).** The `.strip()` fix + the byte-parity test (`test_build_trio_role_prompts_byte_match_the_prompt_council`) landed before the live A/B; both arms now get byte-identical role prompts. CONFIRMED (`load_role_prompt` strips → matches `_load_role_prompts:527`; live A/B ran with parity). |
| S-BS-45 | **`policy_judge` (Mistral) elicitation-path divergence** — verbose under `build_prompt` (fp=22), **silent** under the DSPy `JudgeSignature` (fp=0); same model/deployment/`.strip()`-identical prompt. | low | **open (WS-6c-DSPy-2-live) → WS-6c-DSPy-3.** The first real prompt-vs-DSPy signal: the elicitation path, not the model, drives the difference. **Confirmed GENUINE, not error-induced** (monitor: zero non-empty `errors` arrays + 0 error indicators in `ab_result_dspy2_live_2026-06-03.json`). Open question for DSPy-3: is the DSPy signature *under*-eliciting policy, or is `build_prompt` *over*-eliciting out-of-lens noise — which is the calibrated target? NOT a paper claim (n=7, blind-spot-saturated). **ENABLED by WS-6c-DSPy-3a** — policy now has 12 by-construction positives to investigate against in 3b. |
| S-BS-46 | **VALUE_MISMATCH standalone corpus coverage = 1** — only one A1c-bearing scribe encounter in the sample Synthea cohort, so `judge_calib_v1` carries a single standalone VALUE_MISMATCH positive (the faithfulness/VALUE_MISMATCH lens is under-sampled). | low | **open (WS-6c-DSPy-3a) → widen the cohort before 3b's optimizer leans on that lens.** Cohort-capped + **logged, not padded** (`generate_judge_calib.py` SHORTFALLS) — the correct by-construction behavior; no case is inadmissible. Fix = a larger Synthea cohort (or a value-mismatch-rich base set). CONFIRMED (fresh-critic + lint `VALUE_MISMATCH x1`). |
| S-BS-47 | **`CLAUDE.md` invariant #3 is stale** — it says clean negatives have `injection_recipe: null` (singular), but the shipped schema is `injection_recipes: []` (plural list, multi-defect-capable), matching the packager + `proof_case.jsonl` + the admissibility lint. | low | **open (WS-6c-DSPy-3a).** The executor correctly followed the **shipped** contract (`[]`, lint-green); the doc text drifted (both the singular field name and `null` vs `[]`). Reconcile = update `CLAUDE.md` invariant #3 to match the schema (recommended; doc-only), or change the packager to emit `null` (touches a shared primitive — broader). CONFIRMED (fresh-critic: 8 cleans use `[]`). |
| S-BS-48 | **Bind compiled judge demos into the production trio by default.** WS-6c-DSPy-3b PROVED the bind-back mechanism (`bind_compiled_demos` + offline test) but does NOT flip `build_trio` to load compiled demos — that is a production-behavior change for its own cycle (WS-6c-DSPy-3c). | low | **open (WS-6c-DSPy-3b).** **Do NOT ship the current risk_judge demos** — their held-out Δ is negative (S-BS-49). Gate bind-back on a positive-exemplar demo set. Fix loc: `lithrim_bench/runtime/council/judges_dspy.py build_trio` (load demos onto each `Judge.predict.demos`). CONFIRMED (mechanism-only by driver §4 scope). |
| S-BS-49 | **BootstrapFewShot's exact-accept gate harvests only already-perfect (here: SILENT) demos on a small mixed corpus → held-out regression.** The trace-gate (`make_judge_metric` → `bool(exact)`, 0 FP ∧ 0 FN) only kept cases the un-compiled `risk_judge` already nailed; on `judge_calib_v1` those are the silent/approve cases, so all 4 demos carry empty findings + 0 positive exemplars. risk_judge regressed (precision 0.71→0.44; +3 spurious VALUE_MISMATCH FPs on held-out positives). | **medium** | **open (WS-6c-DSPy-3b) → GATE before the optimizer is trusted to improve a judge.** This is the actionable finding of the loop-closure (the loop itself is sound — the negative Δ is honestly reported, gate not loosened). Candidate fixes: a **coverage-aware demo-selection metric** that requires ≥1 positive exemplar, **MIPROv2**, a higher-raise-recall teacher, or more positives/code. Pairs with the standing `MISSED_ESCALATION`-vs-`SEVERITY_ESCALATION` baseline prompt confusion (orthogonal, both arms). CONFIRMED (verbatim 4-silent-demo dump + per-case regression rows in `score_optimized` + the REPORT). Fix loc: `judge_optimize.py compile_judge` demo-selection / `judge_metric.py` a coverage-aware gate. **UPDATE (UAP-4 live, 2026-06-05): coverage-aware selection (`order_positive_first`) FIXED the silent-demo harvest (`n_positive_demos` 0→4) — but the live held-out Δ stayed a LOSS (graded 0.80→0.70): the surfaced positive demos OVER-FIRE → S-BS-76. The SELECTION problem is solved; the PRECISION problem is not. Stays open with the measured Δ.** |
| S-BS-50 | **The journey hardcodes `BFF_URL = "http://localhost:8787"`** and fetches `/v1/case` + `/v1/run-eval` directly, bypassing `bff.js`'s `runEval` client + its `BASE = VITE_BFF_URL ?? ""` indirection. | low | **open (WS-7a) — STAYS OPEN; UAP-1 did NOT close it (monitor correction 2026-06-04: the UAP-1 hand-back over-claimed it closed).** UAP-1 honored the `bff.js` `BASE`/`VITE_BFF_URL` indirection for its OWN new code (A6 — `getAgent/putAgent/getAudit/getRunAudit` route through `BASE`, no hardcode), but the *journey's* existing hardcode is untouched (journey frozen). Works in dev (both hit `:8787` — the journey direct, `bff.js` via the vite proxy) but **breaks a packaged Tauri/VPC build** (no env-driven base URL). Fix = route the journey's case/grade fetches through `bff.js` (or at least the `VITE_BFF_URL` indirection); pairs with S-BS-51. → WS-5e packaging / a small journey-`bff.js` cleanup. CONFIRMED (WS-7a verify-and-close). Fix loc: `apps/shell/src/journey/JourneyApp.jsx:18,74,95`. |
| S-BS-51 | **The journey extended the §10-locked v1 BFF surface outside ratification** — `GET /v1/case` + a `council` view shaped onto `/v1/run-eval` (`_council_view` votes) + an `in_process` request field, all added by the parallel journey rework (bypassed devloop), not the WS-5-BFF §10 process. | medium | **CLOSED 2026-06-04 (UAP-1 commit `a7edd25`).** `SPEC_PRODUCT_SHELL §10` now admits the new UAP-1 routes (`/v1/agent`, `/v1/audit`, `/v1/runs/{id}/audit`) AND ratifies the prior WS-7a additions (`/v1/case` + the council view + `in_process`); the §10-locked surface is reconciled (fresh-critic confirmed). Residual (exposing `/v1/case`/`in_process` through `bff.js`) pairs with the still-open S-BS-50 → WS-5e. Originally open (WS-7a). Additive + non-regressing (the `composite` path stays green — `bff.test.jsx` in the 33/33 suite), and justified (the verify act needs the real case + a votes view + the in-process toggle), but the **locked v1 surface grew without §10 ratification** (the WS-7a driver's A3 "zero new BFF" deviation). Fix = reconcile/ratify the additions into `SPEC_PRODUCT_SHELL.md §10` + expose `/v1/case`/`in_process` through `bff.js` so clients don't hand-roll fetches (pairs with S-BS-50). CONFIRMED (WS-7a verify-and-close). Fix loc: `apps/bff/app.py:186` + `docs/specs/SPEC_PRODUCT_SHELL.md §10`. |
| S-BS-52 | **Replay runs are not auditable via `GET /v1/runs/{id}/audit`** — the $0 replay default persists no `PIPELINE_RUNS` blob (only `--in-process` constructs `SqliteProvenanceStore`), so the §2B run-provenance report is in-process-only; replay runs return a clean 404 | medium | **CLOSED 2026-06-04 (UAP-3)** — replay+live+in_process ALL persist a provenance blob now (`_persist_run_provenance`, `run_eval.py:104`), so the $0 replay default is auditable + listed in `GET /v1/runs` (round-trips to `/v1/runs/{id}/audit`). Idempotent on the fixed baseline id (one row/baseline; the `created_at` re-stamp → S-BS-68). Was: CONFIRMED (`run_eval.py:146` else→`grade_replay` had no `provenance_store`; only the `in_process` branch persisted; `test_run_provenance_unpersisted_run_is_404_not_500`). The N1 gap — accepted in scope (A4 tests the projection on a persisted/seeded blob). Fix loc: `run_eval.py` replay/live paths + `apps/bff/app.py /v1/run-eval`. |
| S-BS-53 | **OQ to spec author: §2B `AuditRecord` `why={rationale, before→after}` vs canonical top-level `before/after`** — §2B lists `before/after` as top-level fields AND describes the user-edit `why` as `{rationale, before→after}`, an ambiguity | low | **open (UAP-1).** UAP-1 RESOLVED it in code (top-level `before/after` canonical; `why={rationale}` — N2, not double-stored); the spec text should be reconciled so the next implementer doesn't duplicate the diff. CONFIRMED (fresh-critic Q4). Fix loc: `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md §2B`. |
| S-BS-54 | **`GET/PUT /v1/ontology` now load the agent (404 on unknown) before resolving the working copy** — sharing the sanctioned `_resolve_ontology_path` helper made the ontology endpoints 404 on an unknown agent even if a stray working copy exists | low | **open (UAP-1).** Strictly-more-correct + consistent; the existing ontology tests stay green (inside debuglithrim 324). Sanctioned-refactor fallout (decision 2), fresh-critic-judged acceptable; no fix needed, noted for the record. Fix loc: `apps/bff/app.py` `ontology_endpoint` + `put_ontology_endpoint`. |
| S-BS-55 | **Run-audit projection attaches the stage-shared `evidence` list to every judge** — `GET /v1/runs/{id}/audit` (`_run_audit_report`) gives each judge the same stage-level `evidence` rather than its own spans; per-judge truth survives in `findings[].evidence_spans` (no data loss) | low | **open (UAP-1, fresh-critic-found) → UAP-3.** Faithful-minimal per UAP-1 scope; the §2B "who saw what" projection reads coarser than the underlying blob. CONFIRMED (critic Q4, `app.py:428`). Refine when the run-history/report views land. Fix loc: `apps/bff/app.py _run_audit_report`. |
| S-BS-56 | **`/v1/run-eval` doesn't surface the `pipeline_run_id`** → the run→audit loop isn't closed in the UI: AuditView "Load run" needs an id the run response doesn't return, so a user fetches the latest from the collections DB by hand | low | **CLOSED 2026-06-04 (UAP-3)** — `/v1/run-eval` now surfaces `pipeline_run_id` + `GET /v1/runs` lists persisted runs newest-first via the new `DocShimCollection.list_all`; each id round-trips to `/v1/runs/{id}/audit`. Was: CONFIRMED (`run_eval.py:91-99` rebuilt a provenance dict WITHOUT `pipeline_run_id`; the orchestrator mints it at `orchestrator.py:337`). Pairs with S-BS-52/55. Fix = thread `pipeline_run_id` into the `/v1/run-eval` response (in-process path). Fix loc: `scripts/run_eval.py` provenance dict + `apps/bff/app.py /v1/run-eval`. |
| S-BS-57 | **Stale `apps/bff/app.py` module docstring** (~lines 26-28) — still says the working copy "does not feed an eval run … (`run_eval.py:118`)", which R3 wired up THIS cycle (and `:118` drifted to `:109`+`:142`) | low | **CLOSED 2026-06-04 (UAP-2 — fixed in `6f62fb1` while extending `app.py` for `/v1/judges`).** Doc-only, zero behavioral impact; the stale `run_eval.py:118` claim R3 invalidated is corrected. Fix loc: `apps/bff/app.py` module docstring. |
| S-BS-58 | **`load_ontology` `@lru_cache(path)` masked draft edits in a long-running process** — keyed on path ALONE, so the BFF reused the first-loaded ontology on the 2nd+ edit of the same working-copy draft; "edit the flag → see it grade" silently no-op'd after the first edit (the immutable committed seed was safe; the MUTABLE R3 draft was not — the whole iterative calibration loop). | medium | **CLOSED 2026-06-04 (UAP-1 R3 follow-up, `fix(harness)`).** Found by the A8 `:5180` smoke (long-running BFF) — the unit tests missed it (A2 only covered committed→draft = distinct cache keys; fresh-process runs always flip). Fix: key the cache on `(path, mtime_ns)` (`ontology.py:182`) — edits bust, committed seed still hits, API unchanged. Regression-guarded: `test_load_ontology_busts_cache_when_the_draft_changes` (unit, forced mtime) + `test_draft_re_edit_regrades_not_the_stale_cache` (BFF integration, draft→edit-same-draft). Default 269 / debuglithrim 326, **0 regressions**. CONFIRMED (offline-fresh flips reject→needs_review; long-running BFF didn't → cache). The `:5180` smoke earned its keep. |
| S-BS-59 | **Ontology `clinical_v1.json` `owner_roles` are stale v1 roles vs the v2 `_TIER1_OWNERS`** — the WS-1 seed's `owner_roles` (`behavior_judge`/`source_message_judge`) were never re-snapshotted to the v2 trio (`faithfulness_judge` is in ZERO ontology `owner_roles`); UAP-2's owner↔emit gate correctly bypasses them for `LENS_BY_ROLE` (CITATION-DRIFT vs the driver's `ontology.owners_of`) | medium | **open (UAP-2, monitor-confirmed diagnose-before-edit Finding 1).** Committed seed kept BYTE-UNCHANGED this cycle (re-snapshot out of scope). Ties to WS-2's deferred Q4.1 flag-source-authority decision. Fix = a re-snapshot pass aligning `owner_roles`→`_TIER1_OWNERS`. Fix loc: `data/ontology/clinical_v1.json` + a seed/snapshot step. |
| S-BS-60 | **`JudgeQuestion` text authoring → `ontology.questions` write-back deferred (R2 assignment-first)** — SPEC §12.2 expects the assigned flags' questions to write through to the ontology working copy; UAP-2 keeps questions READ-derived; editing question TEXT in the UI is a no-op as the model stands (questions are role-keyed, not flag-keyed) | low | **open (UAP-2, monitor-APPROVED scope cut, deviation c).** Recorded so the locked-spec §12.2 line isn't silently dropped (sequencing, not weakening). Fix loc: `PUT /v1/judges` (+ ontology working-copy write) / `JudgeEditor`. |
| S-BS-61 | **BFF judge prompt-preview transitively imports `openai`? — NO (verified clean)** — `render_role_questions` is council-light, but the BFF imports `LENS_BY_ROLE` from `judge_metric` (pure, no `openai` leak — verified) for the owner gate + role enum | low | **open (UAP-2, informational — no action).** Default-core import of `judge_metric.LENS_BY_ROLE` + `judge_assignment.render_role_questions` LEAKED: NONE. Recorded for completeness; revisit only if a pure-`[bff]` deploy ever drops `judge_metric`. Fix loc: `apps/bff/app.py` top imports. |
| S-BS-62 | **UAP-2 `JudgeEditor` widget not mounted in the running shell** — `tool-judge_editor` is registered (KNOWN_TOOLS→8) + Vitest-tested + its BFF backend fully live, but the shell's scripted demo conversation never instantiates it and the chat + ⌘K command bar are inert, so the assignment→prompt→questions demonstrability "aha" isn't reachable in-UI | medium | **CLOSED 2026-06-04 (UAP-5a).** `tool-judge_editor` mounted in `CenterPane` (`apps/shell/src/panes.jsx`, mirrors `tool-agent_editor`); the `$0` assignment→prompt preview is reachable in-UI; Vitest `panes.test.jsx` mount guard (45/45). [Originally: open (UAP-2, A8-smoke-found) → UAP-5 line-item] (the in-UI authoring-surface mount — the inert chat + ⌘K command bar = the conversational-driver seed **UAP-5a folds in**; user 2026-06-04). Wire `tool-judge_editor` into the shell flow like the Agent editor/FlagEditor, via `bff.js getJudge/putJudge`, $0 preview. Fix loc: `apps/shell/src/` (conversation/CenterPane + genui). |
| S-BS-63 | **Authored per-judge assignments not threaded into `grade_inprocess`** — `PUT /v1/judges` persists `assigned_flags`, the `build_trio(assignments=)` bridge + the `$0` GET-preview all exist, but `harness/grade.grade_inprocess` (and `run_eval.run`) build the council from the default config and never read the persisted `JudgeConfig`, so authoring a judge does NOT change the live verdict yet | medium | **CLOSED 2026-06-04 (UAP-3).** `authored_stage.build_authored_semantic_stage` threads loaded assignments → `build_trio(ontology=, assignments=)` into the in_process grade via the existing `grade_inprocess(semantic_stage=)` (seam (i), no orchestrator/`stages.py` edit, `grade.py` 0-touch). **A1 proven $0/offline:** `WRONG_DOSAGE`→`risk_judge` flips composite non-reject→`reject`, the OTHER roles don't move (the change is the authoring), preview↔live parity test-anchored. **in_process only** — live `:8002` per-judge injection → **S-BS-64**. |
| S-BS-64 | **Live `:8002` per-judge assignment-injection deferred (S-BS-63 closes in_process only)** | medium | **open (UAP-3).** An authored judge re-votes with its authored lens ONLY on the in_process path; the live `:8002` path needs the HARD-GATE-paused WS-2 backend (PipelineRequest per-judge council injection — `council_config`/`ontology` already inject, per-judge `assignments` don't). Gates the LIVE authored-flip. Fix loc: WS-2 backend + `scripts/run_eval.py` live branch. |
| S-BS-65 | **Eval-pack batch is replay/live only (`build_pack` has no in_process param)** | low | **open (UAP-3).** `POST /v1/eval-pack/run` can't batch the in_process authored trio; a batched-authored-flip-at-scale is a follow-on (single-case in_process is reachable via `/v1/run-eval`). Fix loc: `evalpack.build_pack` + `apps/bff` `EvalPackRunRequest`. |
| S-BS-66 | **Authored in_process per-judge projection is EMPTY in the realized view (role/decision/model/reason/findings all None)** | medium | **CLOSED 2026-06-04 (UAP-5a).** `stages.py` mapping-layer fix (frozen seam 0-delta): `model`→seam's own `model` field (role name), `reason`→`_synth_reason(decision, finding_codes)` (clean approve stays empty — HONEST, fresh-critic-verified), roster projected off `["injected"]`. CONFIRMED legible live (rid `8f5bbcda`). **Diagnosis nuance:** the A8 obs read "all None", but the static trace + fix showed role/decision/findings DO populate from the seam dict — `model`+`reason`+roster were the empty fields the fix addressed. INHERITED, judges_dspy 0-delta.** On the `council_evaluate` path `_model_lookup={}` (`stages.py:747`) → `JudgeVote.model=''`, and the DSPy seam dict has no `summary`/`rationale` → `JudgeVote.reason=''`. **A8 smoke 2026-06-04 (live in_process run):** BOTH the `/v1/run-eval` `council.votes` view AND the `/v1/runs/{id}/audit` judges projection returned `role=None, decision=None, model='', reason='', findings=[]` (only `confidence` survived: 1.0/None/1.0) — so the authored path's PER-JUDGE audit is unreadable, directly undercutting §2B for the headline authored path (the COMPOSITE verdict + 6 findings + the S-BS-7 grounded suppression DO survive + render). **GATE before the authored path is shown as an audit-fidelity exhibit** (the paid flip / UAP-5b). Fix loc: `stages.py` injected-evaluator branch (populate `_model_lookup` from the trio seam dicts + synthesize reason from findings) — outside the frozen A2 set. |
| S-BS-67 | **`/v1/runs` row returns 6 fields vs the §10-ratified 5** | low | **CLOSED 2026-06-04 (UAP-3 close; fresh-critic NB).** `_run_summary` (`app.py:735`) returns `verdict_flipped_by_stage` beyond the `8088d88` §10:147 shape; reconciled at close by widening §10 to the shipped projection. Fix loc was `docs/specs/SPEC_PRODUCT_SHELL.md §10`. |
| S-BS-68 | **Doc-shim upsert re-stamps `created_at` on `ON CONFLICT` → tension with §2B append-only blob tier** | low | **open (UAP-3; fresh-critic OQ).** `collections.py:67/74` recomputes `created_at` + `DO UPDATE SET created_at=excluded.created_at`, so re-persisting a deterministic-replay `pipeline_run_id` re-stamps it + bumps the run to the top of `created_at`-ordered history. Idempotent payload (low), but mutates a blob timestamp (cf. `persistence-blob-projection`). Fix must be SCOPED to the blob/provenance tier (config rows may legitimately want last-write-wins): preserve first-seen `created_at` for `PIPELINE_RUNS`. Fix loc: `collections.py` (a first-write-wins variant) / `_persist_run_provenance`. |
| S-BS-69 | **RunPanel paid-mode `window.confirm()` gate freezes the renderer to CDP — confirm-gated paid runs can't be DRIVEN via the Claude-in-Chrome MCP** | low | **open (UAP-3, A8-smoke-found).** `RunPanel.jsx:57` gates Live/In-process behind `window.confirm(COST_CONFIRM)`. For a HUMAN it works correctly; but a native dialog **freezes the renderer to CDP** (screenshots/clicks time out) and **can't be accepted via the MCP** — so the A8 smoke could NOT drive the paid in_process run in-browser; it was fired via the BFF API instead. The gate WORKS (it blocks — **NOT** a cost-bypass). **CORRECTION (diagnose-before-edit):** an earlier draft of this seam claimed CDP auto-accepts the dialog and the smoke overspent ~3 calls — WRONG; the automated click fired nothing (run count stayed 17 right after it), my actual spend was 1 deliberate API call, and the extra in_process runs in that window were USER-fired. Fix = an in-DOM confirm modal (driveable + observable by automation; better UX). Fix loc: `apps/shell/src/genui/RunPanel.jsx:57`. |
| S-BS-70 | **Visceral live verdict-flip not reproduced for a base-owned Tier-1 flag** — the paid "author a judge → watch the verdict change" finale (`judge-creation-must-be-demonstrable`) is unachieved with WRONG_DOSAGE: the base trio already BLOCKs the by-construction subtle drift, so authoring sharpens an already-firing lens instead of flipping a miss | **medium** | **open (UAP-5a; CONFIRMED).** LIVE: unassigned verdict=BLOCK (`risk_judge`: "zidovudine 450MG, transcript confirms 300MG"); assigned=BLOCK → no flip. **Strategic:** a genuine flip needs a defect the base trio MISSES — a non-v2-base-owned code (with the bridge-emit-outside-allowed-list + lone-Tier-2-corroboration questions resolved) OR a subtle SEMANTIC near-miss = the grounding / **withstands-gate (UAP-3b) moat**. So the demonstrability finale is ENTANGLED with the moat (motivates UAP-3b next) — OR a small dedicated demo cycle that constructs a base-missed flip case. **UPDATE (UAP-3b CLOSED 2026-06-04): the MECHANISM landed offline/$0** — the withstands-gate corrects a confidently-wrong out-of-lens judge PRE-consensus → the composite flips reject→approve, GATE-ATTRIBUTABLE (`test_MOAT_EXHIBIT…`, double-assertion). The genuine "watch the verdict change" S-BS-70 named is PROVEN as a mechanism; the LIVE paid "visceral" attestation (+ S-BS-73 on-by-default validation) is SPLIT to **UAP-3b-2**. So S-BS-70 is mechanism-DISCHARGED, live-attestation-PENDING. **UPDATE (UAP-3b-2): the live SAFE direction is ATTESTED (Run A); the live CORRECTION did NOT converge** — the authored DSPy trio doesn't over-fire the documented (prompt-council) MED FP + the out-of-lens flip can't be authored → S-BS-74. So S-BS-70 stays **live-CORRECTED-attestation-PENDING** (mechanism proven offline + live-SAFE attested). Fix loc: a dedicated S-BS-74 live-correction demo cycle (engineer a case the authored trio over-fires). |
| S-BS-71 | **Authored-path `JudgeVote.model` duplicates `judge_role` (no deployment id at the frozen seam)** | low | **open (UAP-5a; CONFIRMED).** The S-BS-66 fix falls `model` back to the seam's own `model` field, which on the DSPy path IS the role name (`judges_dspy.py:287` `{'model': self.role}`) — legible but redundant with `judge_role`; also contradicts the `models.py:42` "(NOT the role name)" comment (reconcile with this fix). The prompt-council path keeps its real deployment string. Additive follow-up: attach a `{role: deployment}` lookup to `authored_stage._evaluator`'s envelope + read it in `stages.py` (authored_stage is non-frozen). Fix loc: `lithrim_bench/runtime/council/authored_stage.py` + `stages.py` + `models.py:42`. |
| S-BS-72 | **Withstands ruling not yet embedded in the run-PROVENANCE blob** — it surfaces via `AuditLog`/config_audit (stream-1, `GET /v1/audit`) + the `build_withstands_correction` NDJSON, but NOT yet the `SqliteProvenanceStore` run-blob (`GET /v1/runs/{id}/audit`, stream-2) | low | **CLOSED 2026-06-05 (UAP-3b-2).** Embedded via a post-save patch in `run_eval` (`_embed_withstands_in_blob`: `find_by_id` → embed `withstands_decisions` → re-insert the FULL doc) + surfaced at `GET /v1/runs/{id}/audit` (BFF `_run_audit_report` projection); ABOVE the frozen seam (returned dict byte-identical, fk preserved); attested LIVE (Run A rid `087a59f8`). `created_at` re-stamp disclosed S-BS-68-adjacent (not fixed here). [was: open] `grade.py:162-170` `LocalPipelineBackend` builds+saves provenance INTERNALLY (no pre-save handle in `run_eval`), so embedding the ruling in the blob needs an orchestrator seam ABOVE the frozen consensus — out of bounds this cycle (would risk A3). Per lock-in #3 the executor split it + surfaced it rather than touch the seam; the certain audit (AuditLog.record + the NDJSON) landed + is test-pinned. Critic-CONFIRMED principled, not a dodge. Fix loc: the orchestrator provenance build → UAP-3b-2. |
| S-BS-73 | **The withstands-gate is ON by default on the authored in_process path (`apply_gate=True`)** — it will correct out-of-lens raises on REAL trio runs | low | **open (UAP-3b; CONFIRMED).** INTENDED (it IS the moat), but it changes live authored-path verdicts vs pre-UAP-3b + is offline-tested only this cycle (full suite 273 passed, no regression; the co-raise guard protects a corroborated out-of-lens raise, but a SOLO out-of-lens raise is now rejected by default). **Validate on the UAP-3b-2 LIVE attestation before treating the authored live path as a verdict source.** Fix loc: `authored_stage.build_authored_semantic_stage(apply_gate)` + the UAP-3b-2 live run. **UPDATE (UAP-3b-2): the SAFE direction is VALIDATED LIVE** (Run A rid `087a59f8`: the on-by-default gate ADMITS a legitimate `WRONG_DOSAGE` finding, verdict unchanged BLOCK — it does not break a true finding). The corrected-direction live validation is still owed → S-BS-74. |
| S-BS-74 | **The live withstands CORRECTION did not converge — the authored DSPy trio doesn't over-fire a presence-checkable/out-of-lens code** (so no live `decision=corrected`; S-BS-70's visceral finale stays owed) | medium | **open (UAP-3b-2; CONFIRMED).** Run B (rid `2275720c`): risk PASSed despite a `MEDICATION_NOT_IN_TRANSCRIPT` assignment; faithfulness raised in-lens findings only (all withstand). The documented MED FP is a PROMPT-council artifact the better-calibrated authored DSPy trio doesn't reproduce; the out-of-lens flip can't be authored (`effective_lens=base+assigned`). NOT manufactured (the UAP-5a non-convergence lesson). The offline A2 (UAP-3b moat exhibit + the live-on-real-trio S-BS-72 integration) stands as the mechanism proof; the live SAFE direction is attested (S-BS-73). **A live correction needs a case ENGINEERED for the authored trio to over-fire** — decide: a dedicated demo cycle vs accept the offline-mechanism + live-SAFE proof. Fix loc: a new by-construction case + (possibly) a calibration-degraded judge that the real trio over-fires. |
| S-BS-75 | **Pre-consensus gate vs post-consensus `ground()` resolve the SAME deterministic contract differently** — on Run A the `PresenceCheck` on `MEDICATION_NOT_IN_TRANSCRIPT` `withstood` at the gate but was disproved by `ground()` | low | **open (UAP-3b-2; fresh-critic OQ).** Verdict-neutral on Run A (`WRONG_DOSAGE` drove BLOCK regardless) + inherited UAP-3b behavior (`signals.py` 0-delta, not a regression). But the §2A locus=BOTH model assumes the pre/post-consensus deterministic checks are coherent — here they aren't: likely the gate's `case_view` reconstruction (transcript+artifact from the payload, `authored_stage`) differs from the `case` dict `ground()` reads, so `PresenceCheck` gets different inputs at the two loci. A case where the MED finding is the ONLY blocker would expose a consensus-vs-composite incoherence (the A2′ gap the gate claims to fix). Fix loc: reconcile the gate's `case_view` with `ground()`'s `case` (or document the locus contract). |
| S-BS-76 | **Positive few-shot demos OVER-FIRE — the non-monotonic lever, now confirmed on the DEMO axis.** The S-BS-49 coverage-aware selection WORKED (`n_positive_demos` 0→4), but the live held-out Δ was still a LOSS (graded 0.80→0.70): the 4 surfaced positive WRONG_DOSAGE demos induced a NEW clean-negative over-fire (`bench_scribe_v1_clean_negative_5074ab5a99d4` → WRONG_DOSAGE FP). So S-BS-49 is NOT merely "silent demos" — even correctly-selected positive few-shots degrade precision (`SPEC_CALIBRATION_TRAINER §4.1`, the demo axis). | **medium** | **open (UAP-4; CONFIRMED — verbatim from the live `score_optimized` + `docs/research/RUN_uap4_optimize_live_2026-06-05.json`).** Strengthens the moat thesis: you cannot few-shot your way to grounding — the deterministic withstands-gate/floor is the reliable lever, optimize is the honest MEASUREMENT instrument ([[critique-pass-precision-not-floor]]). Candidate next levers (a follow-on, NOT this cycle): demo-count tuning / clean-negative exemplars to counter the over-fire / MIPROv2 instruction-tuning / the floor UNDER the optimized judge. Pairs with S-BS-49. Fix loc: `judge_optimize.py` demo-selection (+ possibly clean-negative demos). |
| S-BS-77 | **`_demo_raises` is defined + tested but `run_optimize` inlines the equivalent check** (`sum(1 for d in demos if d.get("findings"))`) instead of calling it | low | **open (UAP-4; fresh-critic NB; CONFIRMED).** DRY cleanup — a tested helper duplicated inline; verdict-neutral. Fix loc: `lithrim_bench/runtime/council/judge_optimize.py` (`run_optimize` calls `_demo_raises`). |
| S-BS-78 | **The optimize win/loss headline keys on `delta.graded` only** — a graded-up/precision-down result would render "✓ improved" though precision regressed (the per-metric numbers ARE shown alongside, so it's not hidden) | low | **open (UAP-4; fresh-critic OQ→seam).** On the honesty-is-the-moat thesis even a subtle headline over-claim matters; the `improved` predicate should be mixed-metric-aware (no "improved" when any metric regressed, or an explicit "mixed" state). Fix loc: `apps/shell/src/genui/JudgeEditor.jsx` `OptimizeDelta`. |
| S-BS-79 | **The UAP-5b A-LIVE `:5180` conversational attestation is owed (BLOCKING, USER-RUN).** The loop is built fully driveable (in-DOM `CostModal`, live SSE) but the live "talk → author a judge + run" slice wasn't attested — the `:8787` BFF must be RESTARTED with the UAP-5b code + the `[agent]` extra + the `claude` CLI on PATH/desktop-auth (no autostart). | **medium** | **ATTESTED 2026-06-05 (monitor, Chrome MCP) — CLOSED.** Drove the live slice: composer "Assign WRONG_DOSAGE to risk_judge, then run a replay eval on ws0_default" → the BYO-Claude loop called the tools end-to-end. **`author_judge` audited write VERIFIED against the BFF** (`risk_judge.assigned_flags=['WRONG_DOSAGE']` + an audit record actor=`dev-default` — *the conversation IS the audit log*, proven live not just claimed) + **`run_eval` replay ran** (verdict card rendered) + **A-SAFE behavior live** (the agent declared it would surface a 422 rather than bypass). **`get_judge` BROKE live** (`'Query' object has no attribute 'split'`) — the agent surfaced it honestly + degraded gracefully → **S-BS-82** (the offline 8/8 tests missed it = the value of live attestation). [was: owed/USER-RUN] |
| S-BS-80 | **The pre-existing TopBar "Run live" still fires a paid `:8002` run with NO confirm modal** — found in passing during UAP-5b (the cost-gate is on the RunPanel/optimize/chat paths, not this legacy button) | low | **open (UAP-5b; PRE-EXISTING, out of scope).** NOT introduced by UAP-5b (the A-SAFE gate covers the agent + the new surfaces); the legacy TopBar button predates the in-DOM-modal convention (S-BS-69). Fix = route it through the same in-DOM `CostModal`. Fix loc: `apps/shell/src/` the TopBar "Run live" handler. |
| S-BS-81 | **A-SAFE is safe-by-ALLOWLIST, not safe-by-mode** (critic NB-1) — the agent's `permission_mode="bypassPermissions"` is safe ONLY because `allowed_tools` = exactly the 3 gate/replay-bounded `mcp__lithrim__*` tools (no Bash/Read/Write). The guarantee rests on `_TOOL_SPECS`/the allowlist staying bounded. | low | **CLOSED (UAP-5c).** The allowlist-bounds test ships (`tests/test_uap5c_journey.py`): `allowed_tools` == exactly the `mcp__lithrim__*` set derived from `_TOOL_SPECS` (no built-in tool, no wildcard) + a no-paid-knob assertion generalized across ALL 6 schemas + an `[agent]`-gated bind to the real `bypassPermissions` `ClaudeAgentOptions` (does NOT skip under debuglithrim). A-SAFE is now **safe-by-explicit-bound**, not safe-by-current-allowlist — fresh-critic CONFIRMED non-vacuous. The D-F split also means no new tool wraps a paid-capable op at all (the cleanest A-SAFE). Fix loc was `tests/test_uap5c_journey.py` + `apps/bff/agent/loop.py` (the `allowed_tools` construction). |
| S-BS-82 | **`get_judge` tool BROKEN on the live agent path** — `'Query' object has no attribute 'split'`. The tool wrapper calls the `get_judge` endpoint FUNCTION directly, so the FastAPI `assigned_flags: str = Query(None)` default arrives as a `Query` object (not a str) and the `.split(',')` parse crashes. **Caught by the UAP-5b A-LIVE attestation** (the agent's FIRST tool call); the offline 8/8 tests missed it (stub message-source / explicit args). | low/medium | **CLOSED (UAP-5c).** Fixed in the WRAPPER `_build_tool_context._get_judge` (passes `assigned_flags=None` so the direct call no longer inherits the `Query(None)` FieldInfo sentinel); `get_judge_endpoint` stays **BYTE-IDENTICAL** (A6 held — the fix is the binding, not the op; critic AST-verified). The regression test is **NON-VACUOUS** — it drives the BOUND `ctx.get_judge` (the live path), not the TestClient; the fresh-critic reverted the fix in a scratch tree and confirmed the test's load-bearing line raises pre-fix. Generalized: every binding closure now passes `Query`/`Header` params explicitly (memory `fastapi-endpoint-as-plain-call-fieldinfo-trap` — the in-process-tool-wraps-endpoint trap). Fix loc was `apps/bff/app.py` `_build_tool_context._get_judge` + `tests/test_uap5c_journey.py`. |
| S-BS-83 | **`author_flag` is EDIT-ONLY** — the conversational Flag tool edits an existing flag's `tier`/`gradeable` (the `FlagEditor` affordance) but cannot CREATE a new flag: a new gradeable flag needs `owner_roles` (production-judge-resident) the agent must NOT fabricate + a `taxonomy_snapshot.json` refresh (the `CLAUDE.md` owner↔emit invariant). | low | **open (UAP-5c; by-design boundary, user-approved at plan-review + fresh-critic-endorsed as the correct safety posture).** The "complete-from-scratch" journey is demonstrable on an EXISTING flag set; conversational flag CREATION (owner authoring + snapshot refresh) stays a human act → a future cycle if the from-scratch vision needs it (an agent fabricating owners is exactly what the invariant forbids). Fix loc: `apps/bff/agent/tools.py` `author_flag` + a snapshot-refresh / owner-authoring surface. |
| S-BS-84 | **`assemble_agent` roster REMOVE has no write-time owner↔emit / non-empty revalidation** — the conversational Domain-assembly tool can remove a judge from `eval_profile.judges` (or empty the roster) without checking that a remaining gradeable flag still has its production-judge owner, or that the roster stays non-empty. The ADD path IS guarded (`LENS_BY_ROLE` — refuses an unknown/fabricated role with a surfaced 404). | low | **open (UAP-5c-2; by-design, fresh-critic-probed + endorsed LOW).** NOT an A-SAFE escape: every write is AUDITED + attributed + reversible, REMOVE only narrows an existing roster (no fabrication/relabel/gate-bypass), and a `$0` replay on an empty roster does NOT crash (emits `reject`). **Critic sharpening:** the empty-roster terminal case is a DEGRADED verdict (zero judges), not merely "a narrower council" — the close prose was slightly optimistic. **OQ-1 (spec author):** should REMOVE refuse emptying / orphaning a gradeable flag's owner? Fix loc: `apps/bff/app.py` `_assemble_agent` (a remove-path revalidation) or `put_agent_endpoint`-load validation. **NB-1 (cosmetic):** `ASSEMBLE_AGENT_SCHEMA` (`tools.py`) types `add_judge`/`remove_judge` as `str` but the closure defaults them to `None` (harmless). |
| S-BS-85 | **`Load run` rebinds the center Run-provenance card but NOT the right Evaluation-report pane** — found during the UAP-5a A8 live attestation: loading run `8a41ef3e` populated the center provenance card (3 votes verbatim) but the right report kept a PRIOR live run (7 active/0 suppressed, led with `FABRICATED_HISTORY`) vs this run's 8 active/1 suppressed (`FABRICATED_CONSENT` + the suppressed `MEDICATION_NOT_IN_TRANSCRIPT`). | low | **open (UAP-5a A8 attestation 2026-06-06; CONFIRMED).** Pure UI wiring — the harness/API result is correct (CONFIRMED via `/v1/run-eval` + `/v1/runs/{id}/audit`); only the shell's report-pane binding doesn't follow `Load run`. Fix loc: `apps/shell/src/panes.jsx` / the RunPanel `Load run` handler → also set the report-pane run state. Evidence `docs/research/RUN_uap5a_a8_live_2026-06-06.json`. |
| S-BS-86 | **The active draft ontology (`ws0_default`) under-blocks — `HIGH=0.45 < block_at_or_above 0.5`, so NO semantic finding can BLOCK** — the root cause of the UAP-5a A8 live run landing `needs_review` not `reject`. Deterministic re-score of run `8a41ef3e`'s EXACT 8 findings (report.py:31 / ontology.py:86 algorithm): draft `{HIGH:0.45,MED:0.4}`→max 0.45→WARN vs committed `clinical_v1 {HIGH:1.0,MED:0.5}`→max 1.0→BLOCK — only the weights differ (6/8 findings are HIGH). | med | **open (UAP-5a A8 follow-up 2026-06-06; CONFIRMED via deterministic re-score + `/v1/audit`).** Two facets: **(a)** the config-plane draft is mis-calibrated (semantic findings can't block; a live demo on it under-rejects a fabrication-laden note the committed ontology BLOCKs); **(b)** the weights were lowered by undocumented `dev-default` UI edits (final HIGH=0.45/MED=0.4 @2026-06-04T22:05:57) with an **EMPTY `why` rationale on every edit** — the §2B why/when/who/what audit captured who/when/what but NOT why (a dogfooding gap: the authoring UI permits blocking-critical weight changes with no recorded justification). The composite MECHANISM is sound; this is a config/calibration + UI-process gap. Fix: restore the draft `severity_map` to committed (`HIGH≥0.5`) WITH a rationale; consider requiring a non-empty `why` on weight edits + a lint warning when no weight reaches `block_at_or_above`. Evidence `docs/research/RUN_uap5a_a8_live_2026-06-06.json` (`root_cause_analysis` + `remediation_live_confirmation`) + `/v1/audit`. **UPDATE 2026-06-06 — facet (a) REMEDIATED:** restored the draft `severity_map` to committed (`HIGH=1.0, MED=0.5`) via an audited `PUT /v1/ontology` (actor `monitor@s-bs-86-fix`, non-empty rationale — dogfoods facet b); the paid live re-run (`83ec5086`, SAME case + SAME 1-PASS/2-BLOCK council split) now returns **reject/BLOCK/1.0** and `calibration_check` flipped **0.0→1.0 (PASS)**, matching `expected_block`. Facet (b) (the empty-`why` weight-edit UI gap) stays OPEN. |
| S-BS-87 | **The conversational `/v1/chat` loop is STATELESS per message — no cross-turn memory** — found in a live from-scratch journey dry-run (Chrome MCP, `:5180`, `$0` BYO-Claude). CONFIRMED in code: `ChatRequest` is `{message, agent}` only (no history/session); `bff.js chatStream` sends no thread; `agent/loop.py:_real_source` does `client.query(message)` on a fresh `ClaudeSDKClient` (`max_turns=12` WITHIN one message). | med | **open (live journey dry-run 2026-06-06; CONFIRMED).** Within ONE message it chains the full Domain→Judge→Flag→Run→Review; ACROSS messages it carries nothing → the agent re-orients from live config each turn and can't track "what we just changed" (observed: turn-3 said "no config writes this session," missing the turn-2 audited `author_judge` write). Single-shot journey works; multi-turn conversation is not memory-coherent. Fix: thread conversation history into `ChatRequest` + replay to the loop (or persist a session). Evidence `docs/research/REPORT_live_journey_dryrun_2026-06-06.md`. |
| S-BS-88 | **Agent-id grounding nit in the chat loop** — mid-journey the agent said "`default` wasn't found in the config DB" before recovering on `ws0_default`. | low | **open (live journey dry-run 2026-06-06).** Cosmetic; a loop system-prompt fix (always use the bound agent id). The write still landed on the correct agent. Fix loc: `apps/bff/agent/loop.py` system prompt. |
| S-BS-89 | **Shell "New evaluation" (+) is a non-functional skeleton stub** (`apps/shell/src/panes.jsx:29`) — no blank-slate reset. | low | **CLOSED 2026-06-07 by UX-1** (`b7eee95`). The `+` now resets to a clean slate via an `App`-held `sessionKey` remount-by-key (`app.jsx:75/124/126`); combined with the clean empty-state default (the scripted 8-message preamble + fake chips gated behind opt-in "Show example"), the Shell chat surface opens blank — no seed clutter burying live turns. Non-vacuous regression test (`panes.chat.test.jsx:179-190` drives `<App>` send→reset→empty-state). CONFIRMED live `:5180`. |
| S-BS-90 | **The conversational `/v1/chat` loop's A-SAFE allowlist was NOT enforced — `permission_mode="bypassPermissions"` = "allow all tools", so `allowed_tools=[8 × mcp__lithrim__*]` was non-binding; the live agent spontaneously ran built-in `Bash` against the host** (found at the ONB-0 A-LIVE). | **high** | **RESOLVED 2026-06-06 by ASAFE-1** (`6446615..f56d6f2`). Fail-closed `PreToolUse` deny gate (`_deny_non_lithrim`) bounding the loop to `mcp__lithrim__*` + SDK isolation (`setting_sources=[]`/`skills=[]`); PROVEN LIVE both-directions (fresh-critic `a26d74ab` A/B: denied WITH the hook / host-username `aregee` leaked WITHOUT). The allowlist-VALUE tests were necessary-not-sufficient (the stub source never ran the real loop). Evidence `docs/research/FINDING_S-BS-90_asafe_bypass_2026-06-06.md` + `PROOF_bench-salvage_ASAFE-1_2026-06-06.md`. |
| S-BS-91 | **Post-deny agent UX** — when the floor denies a built-in, the agent treats it as a "spurious/injected" error and may retry/loop before recovering onto the Lithrim tools. | low | **open (ASAFE-1 2026-06-06).** Security is enforced (the deny holds); UX nit only. Fix: surface the bound in `_SYSTEM_PROMPT` (`apps/bff/agent/loop.py`). |
| S-BS-92 | **BYO-Claude as a first-class LLM provider (judges + generation; the model-composition lab)** — PROVEN live (5 spikes, `docs/research/REPORT_byo_claude_provider_2026-06-06.md`) but UNWIRED into the frozen council. Seam = `build_judge_lm` provider-awareness (`judges_dspy.py:205`) + the judge `model` field selector (`app.py:622`) + `LITHRIM_LLM_PROVIDER=claude-cli` (`settings.py:30`). | medium | **CLOSED 2026-06-07 by BYOC-1** (`5c54aaf..c05d8b3`). BYO-Claude is a first-class TOOL-LESS council provider: `build_judge_lm` provider-aware (Azure byte-identical) + `build_trio(models=)` + `ClaudeCliLM` (tool-less `dspy.BaseLM` over `claude -p`; `--tools ""` + neutral `--system-prompt` + isolation, never bypass/`--bare`, prompt on stdin) + per-judge `model` threading + global `LITHRIM_LLM_PROVIDER=claude-cli`. A6 byte-frozen (AST seam-freeze; fresh-critic `a6b5fbc4bd9b33e42` reproduced it); no-logprobs→`confidence=None` (honest, Mistral-consistent). Mechanism CLOSED; the conv-UI paid A-LIVE owed → S-BS-94. |
| S-BS-93 | **Persistent fake chrome left as-is by UX-1 (by design, R1)** — the TopBar crumb (`app.jsx:18` "Scribe Agent v4", test-locked by `app.test.jsx:20`), the LeftRail rail-footer (`panes.jsx:67-72` "Jordan Reyes / acme-health · Pro"), and the `StatusBar` (`app.jsx:42-58` "Run #218 / 1,488 / 2,400 / κ 0.88 / acc 92.4%") still render hardcoded demo values. | low | **open (UX-1 2026-06-07; monitor critique NB-1).** UX-1's clean-default was scoped to the CenterPane **chat surface** (R1); this app-level chrome is out of that scope. A later cycle should source the crumb/eval-name from the active eval, the rail-footer from real auth, and the StatusBar from the live run — and update `app.test.jsx:20` accordingly. Fix loc: `apps/shell/src/app.jsx` (TopBar + StatusBar) + `panes.jsx` LeftRail rail-foot. |
| S-BS-94 | **The conv-UI 1-Claude-2-Azure mixed-council paid A-LIVE is owed** (USER-RUN, cost-gated) — the BYOC-1 composition effect is proven offline ($0); the live attestation (talk → author a risk_judge on `byo-claude` → run → votes diverge vs all-Azure) is not yet run. | med | **CLOSED 2026-06-07 — A-LIVE ATTESTED** (monitor, user cost-authorized; D-F discharged). Live in-process contrast on `ws0_default`: risk=BYO-Claude (run `259c2121`) vs risk=gpt-4.1 (run `53b76c50`). PROOF the swap took effect: risk confidence `None` (BYO-Claude, no-logprobs) → `0.998808` (Azure), ISOLATED to the swapped seat (policy `None`→`None` Mistral; faith `1.0`→`1.0` Llama). Composition effect REAL: risk vote PASS↔BLOCK; gpt-4.1 raised FABRICATED_ALLERGY (a likely over-fire BYO-Claude avoided — not the case's gold defect). **Honest-Δ: verdict did NOT flip** (both BLOCK, faithfulness-driven) — no manufactured win. Capsule: `docs/research/PROOF_bench-salvage_BYOC-1_2026-06-07.md` + `RUN_byoc1_alive_2026-06-07.json` + `out/zyng_narrate/byoc1_composition_alive_narrated.mp4` (offline `$0`). Opened S-BS-97; baseline restored (risk=gpt-4.1). |
| S-BS-95 | **`LITHRIM_LLM_PROVIDER=claude-cli` wires only the DSPy judge path, not the OpenAI-direct client factory** — `build_judge_lm` reads the setting directly, but `llm_provider._current_provider` still raises on anything but `openai`/`azure`. | low | **open (BYOC-1 2026-06-07).** The global switch makes the council's DSPy judges run BYO-Claude; the v1 prompt-council / kb-search `get_sync_openai_client` factory still needs openai/azure. A `claude-cli` adapter there is a separate follow-on (reuses `ClaudeCliLM`; the jute-transform track). Fix loc: `runtime/council/llm_provider.py`. |
| S-BS-96 | **2 pre-existing order-fragile observation-isolation test failures + the devloop green-bar ran a `pytest tests/` subset** — `test_observation_pipeline.py::{test_importing_observation_does_not_load_compliance_modules, test_default_run_pulls_no_heavy_deps}` fail under canonical `pytest -q` (448/2/3) when an earlier test imports dspy/openai. | low | **open (BYOC-1 fresh-critic `a6b5fbc4bd9b33e42` 2026-06-07).** PRE-EXISTING + BYOC-1-independent (fail at parent `c4d33ab` 436/2 + baseline `acc4973` 369/2; BYOC-1 touched no observation test, +12 passing, same 2 failures). Fix: (a) run the 2 isolation tests in a subprocess (order-independent); (b) the devloop green-bar should run canonical `pytest -q` (pyproject `testpaths`), not `pytest tests/`. **SHARPENED by the CRUD-1 fresh-critic `a989dc135dc2d4d6a` 2026-06-07:** the failure SET is **order-non-deterministic** (at parent `8f59407` `pytest -q` gave a *shifting* 6-failure set across runs; the 2 observation tests PASS in isolation) — i.e. an order-dependent isolation FLOOR, not a fixed pair. Post-CRUD-1 HEAD is deterministic 460/2/3 (CRUD-1's new test file stabilized collection order). So "the same 2 failures" is robust only while collection order is stable; the real fix is (a) subprocess-isolation + deterministic pytest ordering. **GROUND-FLOOR-1 fresh-critic `a4e8ba1418e7cfd49` 2026-06-09 re-confirmed PRE-EXISTING (the same 2 fail at parent `8c45ffc` 524/2 + HEAD `1d5a4a5` 533/2/3); GROUND-FLOOR-1 added +9 passing, 0 new → canonical baseline now 533/2/3.** |
| S-BS-97 | **The run-provenance blob does not record the per-judge model/provider** — surfaced at the BYOC-1 A-LIVE: the BYO-Claude-vs-Azure swap had to be proven via the confidence delta + judge order, because the `judges[]` projection carries `model=<role-name>` (the S-BS-66 lossy projection), not the resolved provider/deployment. | low-med | **open (BYOC-1 A-LIVE 2026-06-07).** For the model-composition lab (and audit completeness) the per-judge resolved model/provider belongs in the provenance blob + the `/v1/runs/{id}/audit` projection. Fix loc: the in_process per-judge projection (council-view → blob; relates to S-BS-66). |
| S-BS-98 | **The judge-config store is GLOBAL (per-role), not per-agent** — `judges` is keyed `role TEXT PRIMARY KEY` (no agent column); `run_eval_endpoint` reads `list_judges` globally ([app.py:294](apps/bff/app.py:294)), never `agent.eval_profile.judges`. So a blank-slate agent is roster/Dataset/chat-clean but **shares whatever judge lenses/models are authored globally** — "clean state" is not judge-clean. | med | **open (CRUD-1 2026-06-07; honestly disclosed in 5 places incl. a shipped `bff.js` comment + the fresh-critic confirmed it true + not-overclaimed).** Relevant to FLAG-1's "clean" narrative + the "demonstration from clean state" objective. Candidate follow-on: a **per-agent judges store** (key `(agent, role)`) so each agent has its own roster — a real refactor (touches the config schema + `run_eval`'s `list_judges` read). Not blocking; the current global posture is the BYOC-1/UAP design. |
| S-BS-99 | **The flag-delete corpus-orphan guard is best-effort** — `_cases_emitting_flag` (`apps/bff/app.py`) scans `examples/*.jsonl` only (not `out/*.jsonl` generated packs) and silently skips a malformed JSON row. | low | **open (FLAG-1 2026-06-08; executor-opened, monitor + fresh-critic `aca2db88ac2ec11f8` confirmed).** Near-zero exposure: a reference (out-of-snapshot) code cannot legally appear in a golden pack (the golden lint rejects it) → the lint remains the real enforcer, and the endpoint documents it. Acceptable for FLAG-1's reference-only delete; revisit only if delete ever extends to scanning generated packs. |
| S-BS-100 | **The single-FP-on-clean live grounding-flip is hard to provoke; the live MED over-fire is context-primed** — S-BS-74 engineered a by-construction clean-negative to make the live council over-fire `MEDICATION_NOT_IN_TRANSCRIPT` (med verbatim in transcript) so `ground()` would flip BLOCK→PASS; the council UNANIMOUSLY APPROVED it (0 findings) → no FP to correct → no flip (honest LOSS, run `6c431bf2`). | low | **open (S-BS-74 2026-06-08; fresh-critic `aa9464d280c7e1c84` corroborated the diagnosis as evidence-backed).** The over-fire is primed by OTHER unsupported content (the multi-defect `8a41ef3e` artifact: injected diabetes + a 20-item PMH absent from a ~5-line transcript); a spotless case lacks the trigger. **Re-scopes S-BS-70** (the visceral live-flip): pursue (a) a near-miss surface-mismatch case (brand/generic or abbreviation the council mis-reads but that grounds clean), or (b) a held-block case where suppression flips WARN→PASS, or (c) accept the demo lives on the offline twin + `8a41ef3e`'s live suppression. The MECHANISM is proven (offline suppression-attributable + `8a41ef3e` BLOCK→WARN); only the single-FP-on-clean LIVE flip is unproven. Memory `live-overfire-context-primed`. **GROUND-FLOOR-1b driver WRITTEN + PARKED 2026-06-09** (`bench-salvage_phaseGROUND-FLOOR-1b_honest-live-flip_driver.md`) — the re-scoped honest-live-flip design+run; the DESIGN (D-A: near-miss / held-block / accept-offline) is the deliverable; activate only when a pitch needs the visceral live flip. |
| S-BS-101 | **Single-judge roster degenerates at the frozen consensus floor** — `build_trio roles=` (DOGFOOD-1) supports a subset roster, but `_apply_consensus` requires `min_valid=2` in full-council mode, so a 1-judge roster returns `insufficient_valid_models` (needs_review). | low | **open (DOGFOOD-1 2026-06-09; fresh-critic `a33d9235c4d470a4d`).** 2/3-judge rungs ship; the 1-judge rung is a deliberate consensus-policy decision, **not** a seam-edit (the frozen seam was untouched). True single-role support would re-open `_apply_consensus`/gate-mode — a separate gated decision. |
| S-BS-102 | **`pack_gate` silently skips orphan/missing outcome rows** — `reliability()`/`never_events()` iterate `outcomes` and `continue` on a case_id absent from `cases`. | low | **open (DOGFOOD-1 2026-06-09; fresh-critic NB).** Benign for engine-built packs (one paired outcome per case); a hand-authored/partially-built pack could under-count `total`. Defense-in-depth: assert `len(outcomes)==len(cases)` in `decide()`. |
| S-BS-103 | **The chat agent operates on `ws0_default`, not the rail-selected agent** — ROOT CAUSE CORRECTED (CHATBIND-1 authoring, re-grep): the binding chain is **already wired** (`app.jsx:183` `agent={activeAgent}` → `bff.js:182` POSTs `agent` → `app.py:1393` `_build_tool_context(req.agent)`). The real break is the BFF loop: `_SYSTEM_PROMPT` (`loop.py:28`) never names the active agent + the agent-scoped tools `_get_agent(name)`/`_run_eval_replay(agent)` (`app.py:1200/1189`) take the LLM's param (not `default_agent=req_agent` which the ToolContext already carries `:1365`), so the model defaults to `ws0_default`. | med | **✅ CLOSED (CHATBIND-1 2026-06-09; `574f98f`+`ef6e540`; HARD-GATE fresh-critic `a543783125606a12c` CLEAN, audit CLEAN).** Fixed at the BFF loop (NOT the shell): `_system_prompt(active_agent)` (`loop.py`) names the rail-selected agent so the model stops emitting `ws0_default` for the agent-scoped tools, + `_review_runs` (`app.py`) scopes the run history to `req_agent` (NON-VACUOUS — the filter key matches the `_run_summary` `agent_id→agent` projection; pre-fix the foreign run leaks). A-SAFE byte-identical (only `system_prompt=` changed; no paid path). The driver's closure-default was DROPPED as unreachable (`tools.py` resolves `args.get(...) or ctx.default_agent` first). A-LIVE owed (user-run `$0`). S-BS-98-adjacent (per-agent judges still global). |
| S-BS-104 | **Live council over-fires + the imported scribe artifact is free-text-not-JSON** — `imported_demo` cases grade reject correctly but the live council raised 12 findings where the case labels one (`HALLUCINATED_DETAIL`), and the structural layer flags "Artifact content is not valid JSON" (the scribe note is prose, not a JSON artifact). | low | **open (DOGFOOD-1 live dogfooding 2026-06-09).** The `imported_demo` cases are second-class (no structured recipe / JSON artifact); verdict-match holds but the finding-set is noisy. Import-fidelity/calibration note; promote a case to first-class (hand-authored recipe + JSON artifact) if a clean finding-set is needed for a demo. |
| S-BS-105 | **The shell UI cannot drive the `in_process` model-mix judge-set ladder** — `runEval` posts `in_process:false`; "Run live" = the `:8002` default all-Azure council. The Claude-vs-Azure composition contrast is `in_process`-only → CLI/SDK-only, not a UI capability. | med | **open (DOGFOOD-1 live dogfooding 2026-06-09).** The composition comparison (a selling point) lives only in the gate-CLI. Future shell enhancement: expose `in_process` + a judge-set picker so the ladder is clickable in the UI (the user's deferred "extend the shell" option). |
| S-BS-106 | **`review_runs` scoping has a 200-row global-window ceiling** — `_review_runs` (`app.py:1302-1303`) fetches the newest 200 runs globally then Python-filters to `req_agent`. If the global run history exceeds 200 AND the active agent's runs are all older than the newest 200 (a heavily-used shared collections DB), the active agent could show empty/short despite having runs. | low | **open (CHATBIND-1 2026-06-09; fresh-critic `a543783125606a12c` NB-2).** Acceptable for the single-tenant/demo posture + within the 500 cap; the proper fix is an **agent-filtered `list_all`** at the SQL level, which the CHATBIND-1 spec keeps OUT of scope as a frozen-op change. Follow-on if run volume grows. *(Plus a cosmetic nit, no seam: empty-string `default_agent` → an empty-backtick prompt token; can't occur via the typed defaults; optional hardening `active_agent or "the current agent"`.)* |
| S-BS-107 | **Pre-existing `ruff format` drift in two edited test files** — `tests/test_uap5c_journey.py` + `tests/test_crud_delete.py` carry format drift in regions CHATBIND-2 did not touch (confirmed at HEAD). | low | **open (CHATBIND-2 2026-06-09; CONFIRMED).** Left as-is (no drive-by reformatting); a future `ruff format` sweep cleans them in its own commit. |
| S-BS-108 | **ConfigTab may render the error state for a brand-new blank agent** — with the CHATBIND-2 `activeAgent` thread, `ConfigTab` self-fetches `getOntology(agent)`; a freshly created `eval-N` agent with no ontology working-copy yet may show ConfigTab's error state rather than an empty config. | low | **open (CHATBIND-2 2026-06-09; fresh-critic `a55dc0be504726cde` — renumbered from the session log's "S-BS-106", which collided with the review_runs seam).** HYPOTHESIS — the error branch already prevents a crash (cosmetic); verify in the A-LIVE. Fix: `apps/shell/src/artifact.jsx` ConfigTab empty-vs-error. |
| S-BS-109 | **The eval-pack *live* endpoint still routes to `:8002`** — `/v1/eval-pack/run` with `live=true` passes `live=req.live` to `evalpack.build_pack`, which has **no in_process param** (the endpoint docstring already calls a batched in_process path "a follow-on"). LAUNCH-PREP's `_resolve_run_backend` standalone default covers the single-agent `run_eval` path only. | low | **open (LAUNCH-PREP 2026-06-09; CONFIRMED; SHOULD-#1 residual).** NOT shell-exposed (no live-pack button; the chat's pack op is replay-only) + the offline `lithrim-bench-pack` gate is `$0`/standalone, so practical exposure = a direct CLI/SDK POST. Triaged post-v1 in `docs/RELEASE_v1.md`; a standalone **batched** live run needs `LITHRIM_COUNCIL_BACKEND=http` until the batched in_process follow-on. The single-agent standalone claim is unaffected. |
| S-BS-110 | **The artifact pane mislabeled an `in_process` PAID run as `replay · $0`** — `artifact.jsx` treated only `grade_path === "live"` as paid, so LAUNCH-PREP's new `in_process` "Run live" default fell through to the `$0` tag (a cost-label INVERSION that understates spend; surfaced live by the standalone A-LIVE). | med | **✅ FIXED (LAUNCH-PREP A-LIVE 2026-06-09; `0dd5613`).** Module-level `gradeTag(grade_path)` — only `"replay"` is `$0`; `"live"`/`"in_process"` are paid (`in-process · paid`), applied at both render sites. Non-vacuous regression test (`artifact.test.jsx`) + 76 shell green + a live `$0` replay confirmed the corrected render. |
| S-BS-111 | **The DSPy judge `max_tokens=1024` truncated the structured verdict on evidence-heavy cases — corrupting a vote** — on `ws0_default` (20-item PMH vs a 5-line transcript) `evidence_consensus_pillar_verdicts` truncated, making `risk_judge` under-call **PASS** so the verdict only reached BLOCK via a **semantic-stage rescue** (`flip_stage: semantic`). `judges_dspy.py:248`. Surfaced live by the LAUNCH-PREP A-LIVE. | med | **✅ FIXED (2026-06-09; `39e64e6`).** Bumped `max_tokens` → **4096** (caps, never forces — cost rises only on long output). **Validated standalone in_process:** the re-run kept the verdict (reject/BLOCK) but `risk_judge` now votes **BLOCK** directly (`flip_stage: None`, run `a416ecc4`) — judge quality corrected, headline stable. Council tests 98 passed. Evidence `RUN_ws0_refire_maxtokens4096_2026-06-09.json`. |
| S-BS-104 (corroboration) | **The in_process council over-fires on imported/second-class scribe cases** — fresh A-LIVE evidence: `imported_scribe_scribe_diabetes_soap_clean_compliant` (a CLEAN case) → **reject/BLOCK** (2 findings HALLUCINATED_DETAIL/INCOMPLETE_DOCUMENTATION, **0 grounded suppressions**), while the purpose-built `imported_scribe_council_v2_smoke_nka_clean` → **approve/PASS (0 findings)** and `ws0_default` (injected) → reject/BLOCK. The council **discriminates but imperfectly**; the floor covers neither over-fired flag. | low | **open (corroborates DOGFOOD-1 S-BS-104; ≥-strict in_process amplifies it).** Imported cases are free-text artifacts (structural stage `not_applicable`). The gap is exactly the grounding-floor/withstands-gate target. Evidence `RUN_clean_{nka_in_process,compliant_overfire_in_process}_2026-06-09.json`. |
| S-BS-108 (imported-replay) | **Imported/live-only agents 500 on `$0` replay** — `dataset.baseline is None` (no captured baseline) → `run_eval.run(replay)` → `agent.baseline_abspath()` → `Path(None)` → TypeError → 500 (`scripts/run_eval.py:292` / `config.py:93`). *(NB: shares the "S-BS-108" label with the ConfigTab-blank-agent seam — a CHATBIND-2 session-log numbering collision; this is the imported-replay one the SPEC_CONVERSATIONAL_CONTROL_PLANE P0 references.)* | med | **✅ FIXED (2026-06-09).** Guard in `run_eval.run`'s replay branch raises a clear `SystemExit` (BFF maps → **400** "no captured baseline … run it live/in_process") instead of the TypeError/500. Verified live (imported → 400, `ws0_default` → 200) + non-vacuous regression test (`test_ws2.py`) + ruff clean. **CAVEAT:** this makes baseline-less replay *fail cleanly*, it does NOT *enable* replay-demos of imported packs (they have no baseline) — that needs baseline-capture, or the live-journey path (no replay). |
| S-BS-113 | **`data/ontology/clinical_v1.json` is stale vs its seed sources — a clean `seed_ontology.py` re-seed sweeps 3 unrelated `owner_roles` additions** (policy_judge on FABRICATED_CONSENT, faithfulness_judge ×2 on MISSING_ALLERGY/VALUE_MISMATCH) absent from the committed JSON. GROUND-FLOOR-1 HAND-ADDED its contract (additive-only diff) + kept the seeder constant rather than regenerate (which would have swept the drift in). | med | **open (GROUND-FLOOR-1 2026-06-09; CONFIRMED + fresh-critic `a4e8ba1418e7cfd49` verified the hand-added contract is byte-identical to the seeder's `sort_keys` output).** The committed JSON is correct + the seeder is forward-correct (carries the new contract), BUT the seeder is NOT currently the byte-source-of-truth (a re-seed diverges by the 3 owner_roles). Fix = re-snapshot/owner-reconcile so a clean re-seed reproduces the committed JSON byte-identically (then A5 "re-seed survives" is unconditional). Until then, ontology edits stay additive-by-hand + the seeder constant tracks them. Relates to the CLAUDE.md "never hand-edit the snapshot / re-snapshot don't soft-pass" discipline (this is the ontology, not the taxonomy snapshot — the snapshot itself is byte-untouched). |
| S-BS-114 | **apps/shell FE fixtures carry a stale `ontology_path` literal** — `data/ontology/clinical_v1.json` appears in shell journey fixtures (layer-4 content), not a live load path. | low | **open (PACK-1 2026-06-09).** Frozen + green (FE fixtures, not exercised by the core load path); out of PACK-1's A1 grep scope (layer-1a = ontology/taxonomy load only). Sweeps in the layer-4 (journey) relocation. |
| S-BS-115 | **`docs/` + the `RUN_calib_progression` research script reference the old `taxonomy/taxonomy_snapshot.json` path** — prose/research references, not live load paths. | low | **open (PACK-1 2026-06-09).** A docs sweep / later-layer cleanup; no functional impact (the core resolves via the active pack). |
| S-BS-116 | **The 5 generator provenance labels stamp a now-dangling path** — `generate_{pack,judge_calib,proof_case,uap5a_flip_demo}.py` + `build_fhir_mini_pack.py` write `"taxonomy_snapshot": "taxonomy/taxonomy_snapshot.json"` into newly-generated manifests' provenance blocks; that path no longer exists on disk. | low | **open (PACK-1 2026-06-09; fresh-critic `a288ace3f4072d77b` NB-2).** Matches the locked "labels stay as historical record" decision (existing committed corpora honestly record their build path; this cycle is unaffected — metadata-write, not load-read). FORWARD-fix: when the corpus-regeneration layer comes, generators should stamp the **active-pack-resolved** snapshot path. Deferred follow-on, not a defect. |
| S-BS-117 | **`examples/judge_calib_v1.jsonl` carries a machine-ABSOLUTE `cohort_path`** — the determinism guard `test_uap4_corpus_superset::test_committed_corpus_matches_a_fresh_generator_run` regenerates a fresh corpus + compares to the committed one, but the committed `cohort_path` is an absolute machine path, so the guard can only pass on the machine/checkout where the corpus was generated (it FAILED in PACK-2's fresh-critic isolated worktree; PASSES in the canonical checkout). | low | **open (PACK-2 2026-06-09; fresh-critic `afda337585b683404` OQ-1).** Pre-existing, out of PACK-2 scope (the relocation never touched the corpus). Fix = record a relative/normalized `cohort_path` at corpus (re)generation so the guard is portable across checkouts/CI. Self-contained spin-off candidate. |
| S-BS-118 | **Packs-as-code trust surface** — PACK-3 makes a Pro pack ship in-process Python (`pack.load_pack_floors()` → `importlib…exec_module` from the manifest's `floors` ref). The loader fails-clean (missing → `FileNotFoundError`; no-`floors` → degrade to generic-only) and does not traverse beyond the declared ref, but a manifest declaring `floors: "../../x.py"` would load arbitrary Python. | low | **open (PACK-3 2026-06-09; fresh-critic `a24596f7030bc9a07` OQ-1).** ACCEPTED architectural consequence (the BE plugin half); license/sandbox is Phase-3-deferred + flagged in `SPEC_PLUGIN_ARCHITECTURE`. No action this cycle. (Executor labeled it `S-BENCH-SALVAGE-PACK3-1`.) |
| S-BS-119 | **Gitignored test-required fixtures** — `examples/proof_case.jsonl` (read by `test_ground_floor1`/`test_uap3b2_provenance`/`test_uap3b_withstands`/`test_grade_wire`) is gitignored + untracked, present only in the canonical checkout. Bare worktrees / CI / fresh clones lack it → ~3 spurious failures; this is why the PACK-2 & PACK-3 fresh-critic worktrees saw a different baseline than the executor's canonical run (both still 0-new at parent). | low | **open (PACK-3 2026-06-09; monitor ground-truthed via `git check-ignore`).** Recurring per-cycle cycle-tax. Fix = track the fixture, skip-clean on absence, or generate it in a conftest. Relates to S-BS-117 (both = non-portable test artifacts). Future critics: judge by *0-new at parent*, not absolute counts. |

---

## First move (next monitor action)

**▶▶ STRATEGIC FOUNDATIONAL NEXT — `healthcare-realm-as-pack`.** (User decision 2026-06-09 — peace-of-mind + *verifiable* boundaries.) Extract the ENTIRE clinical realm OUT of the core into a loadable `healthcare` pack → core = domain-agnostic CE, healthcare = Pro; boundary VERIFIED by grep (the core has no `clinical`/`snomed`/`scribe`) + load/unload. Reframes "clinical-as-first-plugin" → the *whole realm* as a pack; subsumes SCENARIO-1 + Plugin-Phase-1 + D-C into ONE verifiable target. **✅ PACK-1 (layer-1a) SHIPPED 2026-06-09** (HARD-GATE fresh-critic `a288ace3f4072d77b` NON-BLOCKING [0/2/0]; build `dd8b78d..953da37`): the ontology+taxonomy realm physically relocated (`git mv`, R100 byte-identical) into `packs/healthcare/`, the core loads via `active_pack()` (`harness/pack.py`), the boundary is grep-verified, an AST-parse consistency gate bridges the still-frozen council (**D-A held — `compliance_council.py` byte-untouched**; un-hardcoding `KNOWN_TAXONOMY_CODES` = layer **1b**, deferred behind explicit un-freeze). Full close ↓ in the history. Strangler-fig: ontology→judges→floors→journey→dataset, green at each step. The founder-dogfood (scribe+coding = healthcare-pack content; story-audit = a 2nd pack) validates the seam. Memory [[healthcare-realm-as-pack]]. **✅ PACK-2 (layer-2, judges) CLOSED 2026-06-09** (HARD-GATE fresh-critic `afda337585b683404` NON-BLOCKING [0/1/1]; build `11b3811..5efbc1c`): the 5 council role prompts relocated (`git mv` R100 byte-identical) into `packs/healthcare/council_roles/`; both readers resolve via `pack_prompts_path()`; the FROZEN council got an **authorized surgical path-only carve-out** at `compliance_council.py:470` (one `difflib`-guarded hunk; the roster/`LENS_BY_ROLE`/owners stay frozen = layer **2b**, deferred). **✅ PACK-3 (layer-3, floors) CLOSED 2026-06-09** (HARD-GATE fresh-critic `a24596f7030bc9a07` NON-BLOCKING [0/2/1]; build `ca54008..f5e6590`) — the FIRST packs-as-CODE step: the clinical grounding executors (`RecordPresence` + `DosageGroundingTool`/`InRowTool` + the SOAP/PMH/dose extractors) relocated **behavior-identical** into `packs/healthcare/floors.py` behind a pack executor-registration interface (`load_pack_floors()` importlib; lazy moat-visible `suppress_executors()`/`floor_executors()` merge, pack→core no cycle; `_run_floor` → registry-dispatch); the moat (`signals.py:183`) reads the pack-merged registry, proven non-vacuous (unregister → suppress vanishes). Opened S-BS-118 (packs-as-code trust surface) + S-BS-119 (gitignored fixtures). **NEXT = layer-4 (journey literals `apps/shell/src/data.jsx`/`cards.jsx` + the FE fixtures S-BS-114) → 5 (dataset); layer-2b (un-freeze the roster/lenses/owners) + layer-1b (un-freeze `KNOWN_TAXONOMY_CODES`) both deferred behind explicit authorization.** **MONITOR ACTION: author the layer-4 (journey) driver when ready.** OWED: the PAID before/after eval on `ws0_default` (user-fires; A2 already proven $0). Do NOT autostart. *(The grounding-stream fork below — GROUND-FLOOR-1b parked / TERMINOLOGY-1 / CONTRACT-AUTHOR — rides on top of this.)*

**✅ GROUND-FLOOR-1 CLOSED PROCEED-WITH-CAVEATS (2026-06-09) — the record-presence SUPPRESS floor shipped offline/$0; floor coverage 1/19 → 2/19; HARD-GATE fresh-critic NON-BLOCKING. ▶ NEXT = monitor's pick:** (a) **GROUND-FLOOR-1b** — re-scoped: DESIGN the honest live test (per [[live-overfire-context-primed]]/S-BS-74/S-BS-100 the clean twin is unanimously approved → a live `FABRICATED_HISTORY` flip likely won't reproduce; pursue a near-miss surface-mismatch or a held-block WARN→PASS case, honest-Δ) — ✍️ **DRIVER WRITTEN + 🅿️ PARKED 2026-06-09** (`.devloop/prompts/bench-salvage_phaseGROUND-FLOOR-1b_honest-live-flip_driver.md`; the DESIGN is the deliverable [D-A picks the case]; activate only when a pitch needs the visceral live flip — monitor lean: stay parked) | (b) **TERMINOLOGY-1** — Hermes code-based grounding (`subsumed_by`), unlocks the 4 coding flags | (c) the carried **owner-gated push** | (d) **CONTRACT-AUTHOR** — generated contracts (the `jute_gen` validator + `jute_dspy` + the proven pure-JUTE transformer = ONE primitive → a new `jute_transform` contract-type that generates/freezes the EXTRACTION side too, not just validators). A **REGISTERED FUTURE PHASE** (`SPEC_GROUNDING_TOOL_LAYER.md` §Phased-Plan #5 + memory `jute-generated-contracts-unification`), **gated / brought-in AT TIME — NOT now**: the de-risk spike (does `jute_dspy` converge on a transformer?) is anytime on a structured artifact; the full type matures POST-TERMINOLOGY-1 + (narrative instance) after the locked `SCENARIO-1 → FHIR → StoryWorld` order + the **label model** (RESOLVED 2026-06-09 → `SPEC_EVAL_SCENARIOS` **D-C**: the floor IS the by-construction label; the narrative CE-wedge NARR sub-sequence is specced there, gated behind v1 ship — Pro = the healthcare pack, CE = StoryWorld/narrative). Full GROUND-FLOOR-1 close ↓ in the history. *(The grounding-floor text below was GROUND-FLOOR-1's kickoff context, now DONE.)*

**▶ (was NEXT, now DONE) the GROUNDING FLOOR — the moat.** (2026-06-09 user direction: "tackle grounding in a fresh session.") This session COMPLETED the **conversational journey** — all 4 acts of the frozen demo are now demonstrable LIVE through chat (compose-not-build): **CHATBIND-3** (the Case tab + inline Case Summary card via `show_case` — the SOURCE input, generic across artifact types, the readable FHIR note decoded; `238e4fc`+`01b6e50`), **CHATBIND-4** (the consented live-run hand-off `propose_live_run` — agent proposes → human confirms the in-DOM CostModal → the paid council runs; **A-SAFE held**, A-LIVE attested: user confirmed → `in_process` run `54b46132`; `d57f05d`), and **Act-3 calibration** via the existing JudgeEditor Optimize button (`get_judge` + a system-prompt nudge — NO new tool; `a045ace`), live-verified ("calibrate the risk judge" → the JudgeEditor card w/ Optimize + ATTACH VALIDATORS). **14 chat tools; A-SAFE intact** (allowlist = the `_TOOL_SPECS` set; the agent NEVER spends — only the human's confirm does). **The grounding work** = the moat: the runtime floor is THIN (**1 of 19** gradeable flags has a `verification_contract`); the diabetes over-fire (S-BS-104, clean→BLOCK, 0 suppressions) is the motivating case (`FABRICATED_HISTORY` has no validator). **Generate Jute validators for the flags** via WS-3's `lithrim_bench/verification/` (`jute_gen`/`jute_dspy`, `[verification]` extra, live-bench-gated vs `:3031`); attach via the JudgeEditor ATTACH VALIDATORS row (the withstands-gate). **Optimize is ALREADY grounded** (by-construction `recipe=label` corpus) — DON'T touch it; the FLOOR is the gap. Memory `grounding-floor-is-the-moat-next` carries the full setup. **Also owed (carried):** the owner-gated push (the unpushed stack grew: LAUNCH-PREP → CHATBIND-3/4 → calibration). New seam **S-BS-112** (low — the JudgeEditor "ATTACH VALIDATORS" lists validators but most flags have no real contract; the grounding-floor target). Do NOT autostart; the user authorizes paid runs (optimize is paid). [[grounding-floor-is-the-moat-next]] [[jute-for-data-transformations]] *Below = the LAUNCH-PREP/v1-launch history.*

**🚀 v1-LAUNCH FOCUS (user directive 2026-06-09: "launch the first version ASAP, don't digress").** **▶ NEXT: the owner-gated push** (`docs/RELEASE_v1.md` checklist — the owner runs `git push`/PR/tag; the monitor prepares nothing further). **The standalone A-LIVE is ✅ ATTESTED PASS (2026-06-09, monitor-driven via Chrome MCP, owner paid-authorized, no autostart):** `:8002` STOPPED + `LITHRIM_COUNCIL_BACKEND` unset → shell "Run live" on `ws0_default` → `provenance.grade_path: **in_process**` (source-of-truth blob, written at the run ts), 3 real council votes (risk PASS `0.99932` logprob / policy WARN null / faithfulness BLOCK) → **reject/BLOCK**, ~18s latency — **v1 runs with no lithrim-backend/`:8002`/Mongo.** The A-LIVE caught + the session FIXED **two defects** now in the unpushed stack: **S-BS-110** (the in_process PAID run mislabeled `replay · $0` — cost-label inversion; `0dd5613`, +76 shell green) and **S-BS-111** (the DSPy judge `max_tokens=1024` truncated the structured verdict → `risk_judge` under-called PASS; bumped → 4096, `39e64e6`; validated re-run kept reject/BLOCK but `risk_judge` now votes BLOCK directly, council 98 green). Plus fresh **S-BS-104** corroboration (the clean `diabetes_soap_clean_compliant` over-fired to BLOCK while `nka_clean` correctly approved 0 findings — the council discriminates imperfectly; all standalone in_process). Proof capsule: [PROOF doc](docs/research/PROOF_bench-salvage_LAUNCH-PREP_alive_2026-06-09.md) + preserved blob/audit (`docs/research/RUN_launch_prep_alive_*.json`); zyng video deferred (owner). **The RELEASE pre-push gate is satisfied — the push is the owner's call. Do NOT push without explicit owner approval; do NOT autostart.** Reassessment: **v1 is ~built** — the clinical conversational, tool-grounded eval platform (3-pane shell + 12 chat tools + the multi-provider v2 council + the **grounding-floor moat** + the audit trail + the CI/CD gate), live-attested + CHATBIND-2 + LAUNCH-PREP. **CUT to the post-v1 roadmap — specs WRITTEN + PARKED, nothing lost; STOP speccing these until v1 ships:** SCENARIO-1 + FHIR/StoryWorld (`docs/specs/SPEC_EVAL_SCENARIOS.md`) · IMPORT-1 (data-ingestion) · the provider picker / Claude-council UI (GAP B / S-BS-105) · the plugin refactor (`docs/specs/SPEC_PLUGIN_ARCHITECTURE.md`, OQ-1..3 resolved). **Critical path: CHATBIND-2 (✅) → LAUNCH-PREP (✅ CLOSED) → the standalone A-LIVE (user-run, NEXT) → push/release (owner-gated).** **PENDING the user's v1-scope-line confirm:** pitch/early-access (clinical, seeded, you/FDE drive — FASTEST, the cut holds; monitor rec) **vs** self-serve (re-adds IMPORT-1 + the provider picker INTO v1, slower). **CHATBIND-2 A-LIVE now DONE** (attested `0793a8c`); the Claude-council live demo stays post-v1. Do NOT autostart.

**✅ PACK-1 (healthcare-realm-as-pack, layer-1a) CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the ontology+taxonomy realm physically relocated OUT of the core into a loadable `healthcare` pack; the core↔domain boundary is now grep-verifiable; NEXT = layer-2 (judges); do NOT autostart.** The clinical `ontology` + `taxonomy_snapshot` were `git mv`'d (**R100 byte-identical**) into `packs/healthcare/{ontology,taxonomy_snapshot}.json`; the core resolves them via a new active-pack resolver (`harness/pack.py`: `active_pack()` default `healthcare`, `LITHRIM_BENCH_PACK`-overridable) instead of the two hardcodes (`ontology.py:38` / `taxonomy.py:13`). A consistency gate **AST-parses** the FROZEN council's `KNOWN_TAXONOMY_CODES` (no import — the core/OSS env has no openai) and **fails closed** on an out-of-council code, so 1a ships ABOVE the frozen seam (**D-A held — `compliance_council.py` byte-untouched**; un-hardcoding the council = layer **1b**, deferred behind explicit un-freeze). Build `dd8b78d..953da37` (5 atomic pathspec-only, NOT pushed). **Two plan-review decisions (user-locked):** **D-B = RELOCATE** (not reference-in-place, the cosmetic boundary) — the 8 agent seeds get a **behavior-preserving `ontology_path`-only edit** (A5 RE-SCOPED from "seed files frozen" → "seed BEHAVIOR frozen"; the council + content byte-identity stay frozen); + the **5 generator provenance labels stay as historical record** (live loads are pack-resolved; A1 = "no old path in the live LOAD path"). **Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic `a288ace3f4072d77b` NON-BLOCKING [0 BLOCKING / 2 NB / 0 OQ]** — SHA256-verified the snapshot + ontology + council byte-identical pre/post; **defeated the moat-guard 6 ways** (owner_roles / severity-weight / gradeable-flip / flag-remove on `packs/healthcare/ontology.json` ALL caught by `assert_clinical_ontology_seam_frozen` [now reads baseline from `acc4973:data/ontology/clinical_v1.json`]; + the new `assert_seed_ontology_path_relocated_only` caught a non-path seed edit); confirmed `pack.py` imports + the gate run in the **no-openai env** (does NOT import the council; `council_known_codes()` AST = the 19 ground-truth codes); A1 boundary clean (the only core `healthcare` literal is the env-overridable `DEFAULT_PACK`); **A2 byte-behavior-identical** (the typed `Ontology` is pydantic-deep-equal pre/post, incl. `severity_map`); every `+/-` line in the 48-file footprint is a path-relocation (no hidden logic). **Green bar:** default 410→**420** (the new `test_pack_layer1a.py` 10/10); debuglithrim 533→**543** (the 2f = the pre-existing **S-BS-96** observation pair, **0 new**); CLAUDE.md coupling-point relocated to `packs/healthcare/taxonomy_snapshot.json` ("the invariant *moves* into the pack, it does not weaken"; the re-snapshot `--out` + the never-hand-edit rule preserved); golden lint green against the new location. **Footprint note (transparently flagged):** the move touched **48 files** — 2 R100 renames + `pack.py`/`pack.json`/the proof test + 5 core load sites + the 8 path-only seeds + 6 scripts + ~20 test-fixture path updates + the 2nd frozen-seam guard reconcile (`test_uap3b_withstands` — the guard diffs `acc4973..HEAD`, so it only surfaced once the seed edit hit HEAD) — bigger than the GO's "8 seeds + 4 scripts" estimate but every line verified path-only. **OWED:** the PAID before/after eval on `ws0_default` (user-fires; A2 already proven $0 via byte + typed equality). Opened **S-BS-114** (FE fixtures stale path, layer-4), **S-BS-115** (docs sweep), **S-BS-116** (the 5 generator provenance labels → forward-fix at the corpus-regen layer). Pre-existing (neither introduced nor fixed): **S-BS-96** (the 2 observation failures), **S-BS-113** (`seed_ontology --check` STALE — byte-identical to pre-move). Session `session-bench-salvage-phasePACK-1-2026-06-09.json`; critique `critique-bench-salvage-PACK-1-2026-06-09.md`. *Below = the GROUND-FLOOR-1 close + the prior history.*

**✅ GROUND-FLOOR-1 CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the record-presence SUPPRESS floor (the S-BS-7 suppress exhibit generalized from transcript to patient record); floor coverage 1/19 → 2/19; NEXT = GROUND-FLOOR-1b (re-scoped live test) or monitor's pick; do NOT autostart.** Wired the existing-but-unwired `InRowTool` as a `record_presence` SUPPRESS executor keyed to `FABRICATED_HISTORY`: a legitimately carried-forward PMH grounded in `patient_profile.conditions` (`conforms is True`) → `Verdict(disproved=True)` → the false finding leaves `active` → rescore flips BLOCK→PASS; a true fabrication (ANY ungrounded item) STANDS; non-SOAP / empty-PMH → never suppress. String-match (`snomed_core`) only — Hermes/code-based = TERMINOLOGY-1, out of scope (honest caveat: sound only because the synthetic bench mints note-PMH and `conditions` from identical FSN strings). Build `8c45ffc..1d5a4a5` (6 atomic pathspec-only, NOT pushed): D1/D2 `RecordPresence` + plaintext FHIR-DocumentReference decode (`9997b45`), D3 the additive `FABRICATED_HISTORY` contract via the seeder constant (`286d724`), D4 9 offline tests (`9fbc810`), D6 spec (`1548756`), + the moat-guard carve-out (`aecbba4`, `tests/_seam_freeze.py`) + the session log (`1d5a4a5`). **Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic `a4e8ba1418e7cfd49` NON-BLOCKING [0 BLOCKING / 2 NB / 1 OQ]** — independently ran **5 adversarial attacks** on the by-construction guard (an injected ungrounded leukemia bullet, a `_core` qualifier-strip collision "Hypertensive crisis"≠"Hypertension", partial N-1 grounding) ALL REPELLED + **5 non-additive `clinical_v1.json` mutations** (owner_roles / med-contract version / contract-remove / gradeable-flip / severity_map weight) ALL CAUGHT by `assert_clinical_ontology_seam_frozen` + proved the flip NON-VACUOUS (pre-suppress verdict is BLOCK) + the empty guard's `items_checked` real (`tools.py:140`, pre-existing) + S-BS-113 byte-identity + import isolation (`leaked: []`). **Two GO-approved deviations, both critic-verified sound:** (i) **A5 re-seed flip → S-BS-113** — a clean re-seed would sweep 3 unrelated pre-existing `owner_roles` additions, so the executor HAND-ADDED the contract (additive-only diff) + kept the seeder constant (forward-correct) + opened the seam; the hand-added JSON is byte-identical to the seeder's `sort_keys` emit. (ii) **the moat-freeze carve-out** — `clinical_v1.json` was whole-file-pinned by the UAP-3b/3b2 guards; reconciled BYOC-1-style (`assert_clinical_ontology_seam_frozen`: flags/owners/severity byte-frozen vs `acc4973`, only `verification_contracts` may grow ADDITIVELY, existing contracts immutable — defeated 5 ways, held). **The monitor D-B correction was honored** — the decode is plaintext (the corpus stores SOAP as plaintext per `injectors/_soap.py`; base64 deliberately deferred to FHIR-1) + the required **empty-extract guard** (`items_checked==0 → disproved=False`, "nothing to check ≠ all grounded"). FROZEN set 0-delta (`_apply_consensus` / `judges_dspy` consensus seam / `judge_metric` / `ws0_default` seed / `taxonomy_snapshot`). **Green bar (canonical reconciled):** `test_ground_floor1.py` 9/9; combined ground-floor + uap3b 22/22; full debuglithrim **533p / 2f / 3-skip** (the 2f = the pre-existing **S-BS-96** observation-isolation pair, **0 new** — +9 passing); ruff clean on all 6 touched + golden lint green on both corpus files. **A7 SKIP — D5 the paid live run SPLIT → GROUND-FLOOR-1b** (D-C, user-approved; the offline mechanism is fully proven, the live verdict-flip is deferred-NOT-dropped — re-scoped because the captured over-fire is a *different* case [`imported_..._diabetes_soap_clean_compliant`] firing HALLUCINATED_DETAIL/INCOMPLETE_DOCUMENTATION, and S-BS-74 shows the clean twin is unanimously approved → a live FH flip likely won't reproduce; honest-Δ). No proof capsule (offline mechanism; the capsule is GROUND-FLOOR-1b's if/when the live test fires). Opened **S-BS-113** (med — the pre-existing `clinical_v1.json` seed↔source staleness). Session `session-bench-salvage-phaseGROUND-FLOOR-1-2026-06-09.json`; critique `critique-bench-salvage-GROUND-FLOOR-1-2026-06-09.md`. *Below = the LAUNCH-PREP close + the prior history.*

**✅ LAUNCH-PREP CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the OSS core runs standalone (no `:8002`/lithrim-backend/Mongo); NEXT = the standalone A-LIVE (user-run) → owner-gated push.** Packaging, not features: **D1** (`apps/bff/app.py`) a net-new `_resolve_run_backend(req) → (live_http, in_process)` reads `LITHRIM_COUNCIL_BACKEND` (default `in_process`) and `run_eval_endpoint` swaps `live=req.live, in_process=req.in_process` for the resolved pair — a human's "Run live" now defaults to the bundled in-process v2 council (BYO Azure/Claude key); `http` is the opt-in. Shell touch = tooltip + comment only. **D2** `docs/QUICKSTART.md` (clean-clone product walkthrough). **D4** `docs/RELEASE_v1.md` (OSS-core scope + airgapped/no-us-hosted framing + owner-executed push checklist + draft notes). **D3** the 8-seam triage (no HARD launch-blocker). Build `84c3c82`+`4dbc417`+`ec526f4` (+ log `9b6d3c1`; concurrent `0793a8c` CHATBIND-2 capsule interleaved above — pathspec-only kept it out; branch `bench-salvage/ws6c-dspy`, NOT pushed). **Monitor 7-item audit CLEAN + inline 4-question critique** (ROUTINE-with-care, NO fresh-critic — D1 doesn't touch the deny-hook, as scoped at GO): **A-SAFE byte-identical** (`git diff 84c3c82~1 ec526f4 -- loop.py` = EMPTY; the plan-review MUST folded — the chat's `_run_eval_replay` calls `run_eval_endpoint` directly, so the proof is the resolver invariant `live=False,in_process=False → replay` regardless of env, pinned by `test_replay_stays_replay` under both env values + unset); **in_process self-containment re-CONFIRMED** (Mongo/backend greps empty over the in_process chain); resolver matches the approved design (env read gated inside `if req.live`). Green: monitor re-ran the new test = 5/5 on the real `…/3.10.15/envs/debuglithrim/bin/python`; executor full suite 522/2(S-BS-96, pre-existing)/3(cred-gated). Opened **S-BS-109** (low — the eval-pack *live* path still needs `:8002`; `build_pack` has no in_process param; SHOULD-#1 residual, triaged in RELEASE). CITATION-DRIFT logged (`PYENV_VERSION=debuglithrim` → 3.12.8; use the explicit interpreter path — memory `council-runtime-test-env` updated). **A-LIVE ✅ ATTESTED PASS (2026-06-09):** `:8002` STOPPED + `LITHRIM_COUNCIL_BACKEND` unset → "Run live" → `grade_path:in_process` (blob ground-truth), 3 real votes → reject/BLOCK; the load-bearing standalone proof + RELEASE pre-push gate satisfied. Caught + FIXED **S-BS-110** (in_process PAID run mislabeled `replay · $0`; `0dd5613`, non-vacuous test + 76 shell green). Proof `PROOF_bench-salvage_LAUNCH-PREP_alive_2026-06-09.md` + `RUN_launch_prep_alive_*.json`. Session `session-bench-salvage-LAUNCH-PREP-2026-06-09.json`; critique `critique-bench-salvage-LAUNCH-PREP-2026-06-09.md`. *Below = the CHATBIND-2 close + the prior history.*

**✅ CHATBIND-2 CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the chat drives the artifact pane.** `focus_artifact({tab})` `$0` directive → the shell opens/focuses report/judges/config/corpus; a `run_result` SSE event lifts the chat's `$0` replay into the shared `runResult` (the focused Report/Judge tab shows THIS run); the ConfigTab `activeAgent` thread in + `ref` dropped (per GO). Build `e7b0806`+`4bf3c89`+`d8d9e70` (+ log `961140d`; my `1ba6291` spec interleaved; branch `bench-salvage/ws6c-dspy`, NOT pushed). **Monitor 7-item audit CLEAN** (re-ran 17/17 chatbind2+asafe+chatbind1 on debuglithrim; ruff clean) **+ HARD-GATE genuinely-fresh-critic `a55dc0be504726cde` NON-BLOCKING [0 BLOCKING / 1 NB / 1 nit]** — C1 loop.py A-SAFE byte-identity (full-`_build_options` diff = 0), C2 no paid path (`emit_run` from the single replay-only `run_eval_handler`; the `run_result` event can't carry a paid run), C3 directive non-vacuous + never a card (not in `KNOWN_TOOLS`), C4 lift byte-same-shape + ConfigTab active-agent, C5 `app.py`/frozen-ops byte-untouched. Green: 408 BFF (with creds) / 17 hermetic + 75 shell. Opened **S-BS-107** (ruff drift) + **S-BS-108** (ConfigTab blank-agent; renumbered from the session log's collided "S-BS-106"). **A-LIVE owed (user-run `$0`).** Session `session-bench-salvage-phaseCHATBIND-2-2026-06-09.json`; critique `critique-bench-salvage-CHATBIND-2-2026-06-09.md`. *Below = the CHATBIND-1 close + the prior history.*

**✅ CHATBIND-1 CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the chat loop is bound to the active agent; NEXT = the OWED narrated-video walkthrough (now unblocked) OR a user-resolved gate; do NOT autostart.** CHATBIND-1 binds the conversational chat to the rail-selected agent (the corrected root cause = the BFF loop, NOT the shell). **D1** (`loop.py`): a net-new `_system_prompt(active_agent)` builder appended to the static base, naming the active agent + instructing default-targeting — the model stops emitting a stale `ws0_default` for `get_agent`/`run_eval`/`review_runs`. **D2** (`app.py`): `_review_runs` scopes the run history to `req_agent` (NON-VACUOUS — the filter key matches the `_run_summary` `agent_id→agent` projection; pre-fix the foreign run leaks). The driver's closure-default was DROPPED as unreachable (the executor's diagnose-before-edit catch; `tools.py` resolves `args.get(...) or ctx.default_agent` before the closure). Build `574f98f`+`ef6e540` (+ log `e19b7b0`; parent `6e19e5a`, branch `bench-salvage/ws6c-dspy`, NOT pushed). **Monitor 7-item audit CLEAN** (re-ran `test_chatbind_active_agent.py`+`test_asafe_tool_gate.py` = 10/10 on debuglithrim, none skipped; ruff clean on the 3 touched `.py`; loop.py byte-identity + no-paid-knob + review_runs non-vacuity all verified) + **HARD-GATE genuinely-fresh-critic `a543783125606a12c` CLEAN [0 BLOCKING / 2 NB / 0 OQ]** — independently confirmed C1 loop.py A-SAFE byte-identity (only `system_prompt=` changed; deny-hook/`bypassPermissions`/`setting_sources=[]`/`skills=[]`/allowlist/`max_turns` byte-identical), C2 no paid path (`run_eval_replay` `live=in_process=False`; `tools.py` UNCHANGED; no `PAID_KEYS` added), **C3 review_runs non-vacuous IN PRODUCTION + the vacuity trap AVOIDED** (chased the exact `agent` vs `agent_id` trap → `_run_summary:1035` re-keys `agent_id`→`agent`, the test seeds via a REAL replay not a fixture, pre-fix the assertion FAILS), C4 prompt parameterized, C5 scope held / frozen ops untouched. **Closed S-BS-103; opened S-BS-106** (low — `review_runs` 200-row global-window ceiling; the SQL-level agent-filter fix is a frozen-op change, out of CHATBIND-1 scope). **A-LIVE ATTESTED 2026-06-09 (live `:5180`, Chrome MCP, `$0`):** with an imported case rail-selected the chat named THAT case (`imported_scribe_scribe_diabetes_soap_clean_violation`) via `get_agent`+`review_runs` (run `0343ca99`→BLOCK) + refused the paid run — NOT `ws0_default`. **Proof capsule committed `e9f4d8c`** ([PROOF doc](docs/research/PROOF_bench-salvage_CHATBIND-1_2026-06-09.md) + the `compose_lesson` journey spec; `$0` silent+captioned video `out/zyng_narrate/chatbind1_capsule_preview.mp4`, gitignored — **owner chose the silent cut, voiced ElevenLabs cut deferred/re-renderable**; deferred polish = a scroll-to-prose framing step). This unlocks the **fully-chat-driven imported-case demo**. Session `session-bench-salvage-CHATBIND-1-2026-06-09.json`; critique `critique-bench-salvage-CHATBIND-1-2026-06-09.md`. **THE FORK (user-chosen):** (1) **the proof-capsule video — `$0` silent+captioned cut SHIPPED** (`e9f4d8c`; `compose_lesson` thesis→demo→moat→arch→GTM, 106s; the voiced ElevenLabs cut + a broader pitch walkthrough remain optional follow-ups): live-capture `:5180` via Chrome MCP + concept slides → zyng (`record_walkthrough`/`compose_lesson`/`render_presentation`; offline `$0`, ElevenLabs=paid voice; honest-Δ; reconnect-check the zyng MCP first); ideally do the A-LIVE `$0` chat smoke first. (2) **GATED on the user resolving `SPEC_PLUGIN_ARCHITECTURE` OQ-1..3 (esp. OQ-1 the Core/Pro line):** author the **HPACK-1** terminology-floor driver (reconned, agent `a52c03ea`) + the **Plugin Phase-1** registry-refactor driver. (3) **PARKED:** S-BS-49-WIN (optimize-win; driver ready). *Below = the DOGFOOD-1 close + S-BS-74 + FLAG-1 + CRUD-1 + BYOC-1 + UX-1 + UAP history.*

**✅ DOGFOOD-1 CLOSED PROCEED-WITH-CAVEATS, 2026-06-09 — the platform runs real evals on real cases LIVE (A-LIVE attested via UI dogfooding); NEXT = CHATBIND-1 (user-chosen); do NOT autostart.** DOGFOOD-1 proved the generic eval platform end-to-end on real lithrim-backend cases: import (taxonomy-linted `imported_demo`, second-class) → judge-set ladder (call-time model-mix `models=` + `build_trio roles=` for 2/3 rosters, NO S-BS-98 refactor) → `in_process` pack → the `lithrim-bench-pack` **CI/CD gate** (`reliability ≥ threshold AND never_events == 0`, exit 0/1 = the **lithrim-sdk parity, Mongo-free**). Build `6557fb1..f4738ac` (4 atomic, parent `b692678`, NOT pushed). Monitor 7-item audit CLEAN + **HARD-GATE genuinely-fresh-critic `a33d9235c4d470a4d` NON-BLOCKING [0 BLOCKING / 2 NB / 0 OQ]** — proved the gate NON-VACUOUS (passing→exit 0; verdict-mismatch→exit 1; **Tier-1 false-alarm at 100% reliability→exit 1**, tiers read from the frozen snapshot), frozen engine byte-untouched, imported cases second-class (PHI-clean), roster ladder honest about `min_valid=2`. Green: **506/2(S-BS-96)/3**, ruff clean. **A-LIVE ATTESTED — live UI dogfooding (monitor-driven, Chrome MCP, paid-authorized; `:8002`/`:8787` health-checked, no autostart):** the 5 imported cases seeded into the running BFF; 2 `Run live` (`:8002`) evals — `…nka_clean` → **approve** (0 findings; the v2 council correctly did NOT false-flag FABRICATED_ALLERGY) + `…clean_violation` → **reject** (BLOCK, 12 findings, **1 grounding-suppression visible live**). The platform performs real evals on real cases live, grounding floor visibly correcting; honest results (2 screenshots). **Still owed:** the model-mix composition contrast (`in_process`-only; the shell can't drive it = **S-BS-105**; CLI/SDK-only). **Opened S-BS-101..105** (the dogfood findings): single-judge roster (101) · pack orphan-guard (102) · **the chat agent binds `ws0_default` not the active agent (103, med → CHATBIND-1)** · council over-fires + free-text artifact on imported cases (104) · the UI can't do the `in_process` ladder (105). Deviations (approved at plan-review): gate-ON-for-all confound fix + the `test_crud_delete` subset edit. **No proof capsule yet — the OWED narrated-video walkthrough** (deferred until CHATBIND-1 enables the fully-chat-driven demo; zyng pipeline). Session `session-bench-salvage-DOGFOOD-1-2026-06-09.json`; critique `critique-bench-salvage-DOGFOOD-1-2026-06-09.md` (+ the post-close live-dogfooding addendum). **NEXT = CHATBIND-1** (user-chosen): bind the chat tool-context to the rail-selected agent so the conversation operates on the SELECTED case (enables "demo what cases we're evaluating" from chat) — a small shell-side fix (thread `activeAgent` → `POST /v1/chat`; the BFF `_build_tool_context(req_agent)` already honors it). Then the narrated video. Parked: HPACK-1 (gated on the plugin-arch spec `SPEC_PLUGIN_ARCHITECTURE`, OQ-1..3 open), S-BS-49-WIN. *Below = the S-BS-74 close + FLAG-1 + CRUD-1 + BYOC-1 + UX-1 + UAP history.*

**🧭 (prior — S-BS-74 CLOSED PROCEED-WITH-CAVEATS, 2026-06-08) — the live grounding-correction flip is an HONEST DOCUMENTED LOSS (a PASS per the honest-Δ law); the mechanism still stands.** S-BS-74 engineered a by-construction clean-negative scribe case to make the live council over-fire `MEDICATION_NOT_IN_TRANSCRIPT` (med verbatim in transcript) so the post-hoc `ground()` floor would suppress it and flip BLOCK→PASS — the visceral "watch grounding correct a confidently-wrong judge" demo. **The flip did NOT reproduce:** the live council (risk/policy/faithfulness, all v2) UNANIMOUSLY APPROVED the clean case with 0 findings → no confidently-wrong judge for the floor to correct (run `6c431bf2`, 1 paid run ~$0.01, no iteration). Recorded as an HONEST LOSS — no manufactured win. **The mechanism still stands** (carefully scoped, NOT overclaimed): proven offline on this exact case (suppression-attributable — `ground` ON suppresses zidovudine → approve vs the contract-removed OFF → reject; the fresh-critic perturbed it 4 ways, the flip vanished each time) + live in `8a41ef3e` (the same `med-presence-check/v1` suppressed the same MED FP, BLOCK→WARN, not a full flip). Build `622ca5e..db1cf9e` (4 executor commits `3c48cb2..19e4e8c` + a monitor ruff-format close-out `db1cf9e`, NOT pushed): the clean-negative case + an additive demo agent + the offline floor-flip test (both loci) + the live run blob + the honest proof capsule. Monitor 7-item audit CLEAN + **HARD-GATE genuinely-fresh-critic `aa9464d280c7e1c84` NON-BLOCKING [0 BLOCKING / 1 NB / 1 OQ]** — its INVERSE mandate (verify the LOSS is honest) CONFIRMED all 5 honesty checks: the offline flip is non-vacuous + suppression-attributable, the live loss is real (1 run, 0 findings), the case is genuinely clean (not gamed), the "mechanism stands" claim is earned + scoped (no sentence claims a live flip), frozen seam + `clinical_v1` + snapshot byte-untouched. Green bar: offline 3/3, full debuglithrim **481/2/3** (the 2 = the pre-existing S-BS-96 observation pair, **0 new**), ruff check clean (format fixed `db1cf9e` = NB-1). **Proof capsule SHIPPED** (`PROOF_bench-salvage_S-BS-74_2026-06-08.md`); video honestly NOT produced (no flip to narrate). Opened **S-BS-100** (low — the single-FP-on-clean live flip is hard to provoke; the over-fire is context-primed → re-scopes S-BS-70) + memory `live-overfire-context-primed`. Deviations (both logged + monitor-approved): the cost-gate citation-drift (the executor caught the driver's misread of `OptimizeRequest.confirm`; corrected `622ca5e` — run-eval has NO `confirm` field, fired `{live:true}`) + the 1-line `test_crud_delete.py` seed-manifest update (additive, `+s_bs_74_demo`). **The honest-loss outcome is itself a proof point for the honesty-is-the-moat thesis** — the system reported a non-result it had every incentive to dress up, and didn't. Session `session-bench-salvage-S-BS-74-2026-06-08.json`; critique `critique-bench-salvage-S-BS-74-2026-06-08.md`. **NEXT-CYCLE FORK (monitor's pick):** S-BS-46/49 corpus-deepening (a real optimize WIN) | the S-BS-70/S-BS-100 near-miss surface-mismatch flip (the re-scoped visceral demo) | S-BS-98 per-agent judges | the self-asserting withstands-loop over the audit blobs. *Below = the FLAG-1 close + CRUD-1 + BYOC-1 + UX-1 + UAP history.*

**🧭 (prior — FLAG-1 CLOSED CLEAN, 2026-06-08) — ALL THREE 2026-06-07 OBJECTIVES DONE (UX-1 + BYOC-1 + CRUD-from-clean); next = monitor's pick from the open fork; do NOT autostart.** FLAG-1 shipped the flags half of objective #2: local reference-flag CREATE + DELETE (guarded, audited, reversible) + the HONEST refusal of gradeable-from-clean (the core invariant — a scoreable flag's code comes ONLY from a lithrim-backend re-snapshot). Build `6592a2a..67d3025` (5 atomic + log, parent `6592a2a`, NOT pushed): `_create_flag` (gradeable=False/tier=None/owner_roles=[] **HARDCODED**, NO gradeable knob in `CREATE_FLAG_SCHEMA`, audited via the FROZEN `put_ontology_endpoint` → action=edit/target=ontology) + the **10th** conv tool `create_flag` + `DELETE /v1/ontology/flags/{code}` with the three orphan guards (gradeable/in-snapshot, judge-assigned, case-emitted) **IN THE ENDPOINT** + the **11th** conv tool `delete_flag` (binds-and-forwards only) → action=delete/target=flag audit + the sharpened `_validate_ontology` 422 (names `snapshot_taxonomy.py --backend-path`, keeps `{offenders}`) + `docs/ONTOLOGY_FLAG_LIFECYCLE.md`. **Approved deviation (logged):** delete is the 11th conv tool (not route-only; count 9→11) — ratified on the merits (CRUD-1 risk-tiering: reversible/low-blast-radius → conversational; guard-bounded by the endpoint per PIN 1). Monitor 7-item audit CLEAN + **HARD-GATE genuinely-fresh-critic `aca2db88ac2ec11f8` NON-BLOCKING [0 BLOCKING / 1 NB / 1 OQ-resolved]** — every required perturbation flipped a real test (add `gradeable`/`live` to a schema → A-SAFE FAILS; flip the `_create_flag` hardcode → the create 422s; disable GUARD 2/3 → the guard test FAILS); **PIN 1 airtight** (no raw `put_ontology` in the ToolContext → no agent PUT backdoor); snapshot byte-untouched; golden lint OK; frozen-seam 0-delta. **Baseline reconciled (monitor, diagnose-before-edit):** canonical main-checkout debuglithrim = **478/2/3** (the 2 = the S-BS-96 observation pair); the critic's worktree "6 failures" = a **worktree-isolation artifact** (the 4 extra tests pass in the main checkout, 15/15 — a fresh worktree lacks the untracked generated artifacts they need) → folded into S-BS-96. Plain 365/0/27; ruff clean on all 7 touched. **A1–A6 PASS** incl. LIVE on `:8787` (422 non-vacuous) + `:5180` (the BYO-Claude agent refused gradeable-create conversationally — *"the manufactured-win failure mode this product exists to prevent"*). Opened **S-BS-99** (low — corpus-orphan guard best-effort) + NB-1 (cosmetic stale test names). No proof capsule (CRUD-mechanics). Session `session-bench-salvage-FLAG-1-2026-06-08.json`; critique `critique-bench-salvage-FLAG-1-2026-06-08.md`. **NEXT-CYCLE FORK (monitor's pick):** S-BS-46/49 corpus-deepening (a real optimize WIN) | S-BS-74 live-correction demo | S-BS-98 per-agent judges refactor (completes "clean state") | the self-asserting withstands-loop over the audit blobs. *Below = the FLAG-1 in-flight + CRUD-1 + BYOC-1 + UX-1 + UAP history.*

**🧭 (prior — FLAG-1 was in-flight, 2026-06-07) — plan-review POSTED + monitor-APPROVED; the executor was AWAITING THE "GO".** The prior monitor session handed off the role; the full handoff (incl. the paste-ready FLAG-1 go-block + the close protocol) is `**.devloop/sessions/HANDOFF_bench-salvage_objectives-flag1-inflight_2026-06-07.md**` — read it first. **Immediate action:** relay the FLAG-1 go (it's in the handoff) to the executor. **The arc:** UX-1 ✅ + BYOC-1 ✅ (A-LIVE attested) + CRUD-1 ✅ (judges/agents) are CLOSED; **FLAG-1 (reference-flag CRUD local; gradeable refused cross-repo) is the LAST objective-#2 piece** — after it closes, all 3 user objectives are done. FLAG-1 audit verdict = **APPROVE** with: (1) the executor's CITATION-DRIFT correction is right — flag-delete uses the `AuditRecord` shape + a file-rewrite, NOT `delete_with_audit` (SQL-row-shaped); the driver §1 is superseded on that point; (2) accept the create=ontology-edit / delete=flag-delete audit asymmetry (A6-clean); (3) D-B route-only (count stays 10), D-C `docs/ONTOLOGY_FLAG_LIFECYCLE.md`, D-D `owner_roles=[]`, D-E no create-if-missing — all confirmed; (4) load-bearing: A2 gradeable-refusal non-vacuous (keep `{offenders}`), A-SAFE `create_flag` can't make gradeable (hardcoded false, non-vacuous), `taxonomy_snapshot.json` byte-untouched; (5) the 3 count-test bumps (9→10) are sanctioned. **At close:** 7-item + HARD-GATE fresh-critic (A2 + A-SAFE non-vacuity, delete orphan-guards, snapshot-untouched, frozen-seam 0-delta), then close. HEAD `0f14a72`, branch `bench-salvage/ws6c-dspy`, NOT pushed; tree clean except the concurrent `root.jsx` (M) + 3 foreign untracked — pathspec-only. *Below = the CRUD-1 close + BYOC-1 + UX-1 + UAP history.*

**🧭 (prior — CRUD-1 CLOSED CLEAN, 2026-06-07) — judges + agents now have full guarded CRUD + runnable blank-slate; next = FLAG-1 (the last objective-#2 piece); do NOT autostart.** CRUD-1 = the judges+agents half of the user's 3rd objective ([[three-objectives-ux-crud-byoc-2026-06]]). Build `893d7a7..eff4911` (5 atomic + log, parent `8f59407`, NOT pushed): harness audited delete primitives (`delete_with_audit` + `delete_judge` revert-to-default + `delete_agent` + `list_agents`, `action="delete"`, change-only) + BFF `DELETE /v1/judges/{role}` + `DELETE /v1/agent` + `GET /v1/agents` (guards: 422 seed-default + 422 last-agent, 404 unknown; audited who/when/what/why) + the **9th** conversational tool `delete_judge` (revert-only; **agent-delete stays human-only** — no `delete_agent` on the ToolContext) + the shell blank-slate New-eval (creates+switches a **runnable** agent — empty roster + the committed Dataset+ontology) + the rail agents list. Monitor 7-item audit CLEAN + **HARD-GATE genuinely-fresh-critic `a989dc135dc2d4d6a` NON-BLOCKING [0 BLOCKING / 2 NB / 2 OQ]** — independently reproduced the A-SAFE 9-tool bound non-vacuity (2 scratch-perturbation proofs), the deny-hook byte-identity (loop.py = prompt only), the delete guards/404/idempotent-revert/change-only-audit live, the frozen-seam 0-delta + no run-delete. **Green bar:** debuglithrim 460/2/3 (the 2 = pre-existing observation-isolation, CRUD-1 introduced none + is more stable), plain 347/0/27, vitest 69/69, ruff clean on touched files. **A7 :5180 smoke** (light+dark): create→switch→delete `eval-1`, rail↔BFF in sync, `ws0_default` shows no delete (the 422 guard reflected in the UI). Opened **S-BS-98** (the judge store is GLOBAL not per-agent → "clean state" is roster/Dataset/chat-clean, NOT judge-clean; honestly disclosed + critic-confirmed) + sharpened **S-BS-96** (the isolation debt is order-non-deterministic, not a fixed pair). NB-1 (session-log diff-stat miscount 14→15 / shell ×4→×5, corrected in the critique). No proof capsule (CRUD-mechanics, not a capability A-LIVE). **NEXT = FLAG-1** — reference-flag create/delete local + the gradeable-create cross-repo precondition gate (driver authored at `8f59407`): `/devloop-kickoff bench-salvage FLAG-1` (HARD-GATE — touches the ontology/taxonomy invariant). Session `session-bench-salvage-CRUD-1-2026-06-07.json`; critique `critique-bench-salvage-CRUD-1-2026-06-07.md`. *Below = the BYOC-1 close + the A-LIVE + UX-1 + the UAP history.*

**🧭 (prior — BYOC-1 CLOSED PROCEED-WITH-CAVEATS, 2026-06-07) — Claude is now a first-class tool-less council provider; next = CRUD-from-clean (the 3rd user objective); do NOT autostart.** BYOC-1 was the 2nd of the user's 3 objectives ([[three-objectives-ux-crud-byoc-2026-06]]). Build `5c54aaf..c05d8b3` (6 atomic pathspec, parent `c4d33ab`, NOT pushed): D1 `ClaudeCliLM` (tool-less `dspy.BaseLM` over `claude -p` — `--tools ""` + neutral `--system-prompt` + isolation, never bypass/`--bare`, prompt on stdin, import-isolated, injectable runner) + D2 `build_judge_lm` provider-aware (Azure byte-identical) + `build_trio(models=)` + global `LITHRIM_LLM_PROVIDER=claude-cli` + D3 per-judge `model` threading (authored_stage→run_eval→BFF→tools; resolves NB-2) + D4 12 hermetic `$0` tests + D5 design note + the moat-guard reconciliation (`1fd4482`, per-symbol freeze in `tests/_seam_freeze.py`). **S-BS-92 CLOSED** (mechanism). Monitor 7-item audit CLEAN + **HARD-GATE genuinely-fresh-critic `a6b5fbc4bd9b33e42` NON-BLOCKING [0 BLOCKING / 2 NB / 1 OQ]** — independently reproduced the consensus-seam byte-freeze (AST), the guard non-vacuity (perturb→fail), the A-SAFE tool-less non-vacuity, no-faked-confidence, and the composition flip is consensus-driven (not hardcoded). **Green-bar CORRECTION (NB-1, monitor-verified):** the canonical `pytest -q` is **448 passed / 2 failed / 3 skipped** (debuglithrim), NOT the executor-reported 339/0 (that was a `pytest tests/` subset); the 2 failures are PRE-EXISTING + BYOC-1-independent (fail at parent `c4d33ab` + baseline `acc4973`; BYOC-1 adds +12 passing, same 2) → S-BS-96. **A-LIVE ATTESTED 2026-06-07 → S-BS-94 CLOSED** (monitor, user cost-authorized): live in-process contrast on `ws0_default` — risk=BYO-Claude (run `259c2121`) vs risk=gpt-4.1 (run `53b76c50`); risk confidence `None`→`0.998808` ISOLATED to the swapped seat = the model swap took effect live; composition effect REAL (risk vote PASS↔BLOCK; gpt-4.1's FABRICATED_ALLERGY = a likely over-fire BYO-Claude avoided); **honest-Δ: verdict did NOT flip** (both BLOCK, faithfulness-driven — no manufactured win). Opened S-BS-95 (low) + S-BS-96 (low) + **S-BS-97** (low-med — provenance doesn't tag the per-judge model); OQ-1 (authored_stage in §4 frozen-list) RESOLVED (the D-D="Both" approval). **Honest proof capsule SHIPPED:** `docs/research/PROOF_bench-salvage_BYOC-1_2026-06-07.md` + `RUN_byoc1_alive_2026-06-07.json` + `out/zyng_narrate/byoc1_composition_alive_narrated.mp4` (offline `$0`). **NEXT = CRUD-from-clean** — the monitor authors `CRUD-1`/`FLAG-1` (guarded CRUD + admissibility gates; DELETE absent, flag-create S-BS-83, blank-slate closed; HARD-GATE — touches the core invariant). Session `session-bench-salvage-phaseBYOC-1-2026-06-07.json`; critique `critique-bench-salvage-phaseBYOC-1-2026-06-07.md`. *Below = the UX-1 close + the ONB-0/ASAFE-1 + UAP history.*

**🧭 (prior — UX-1 CLOSED CLEAN, 2026-06-07) — the shell chat surface is now usable (markdown + cadence + clean default); next = BYOC-1, then CRUD-from-clean; do NOT autostart.** UX-1 was the FIRST of the user's three 2026-06-07 objectives ([[three-objectives-ux-crud-byoc-2026-06]]): make the conversational shell actually usable. Shipped shell-only, 5 atomic pathspec commits `c58d7f2..f8bed47` (parent `c58d7f2`, NOT pushed): **markdown** rendering for assistant text (`cb6a9dd` — `react-markdown@9` + `remark-gfm@4`, new `Markdown.jsx` with NO `rehype-raw` = raw HTML inert by construction, hardened links) + **cadence** (`b6dff1e` — near-bottom autoscroll + "↓ latest" pill, composer auto-grow cap 200px, dead attach/layers removed) + **clean default + demo opt-in** (`b7eee95` — empty-state with suggestions that FILL the composer, the scripted 8-message preamble + fake chips gated behind "Show example", neutral You/Lithrim live identity, New-evaluation remount-by-key reset = **S-BS-89 CLOSED**) + **tests** (`16ebf0f` — `Markdown.test.jsx` non-vacuous sanitization [mixes `**bold**` + `<script>` + `<img onerror>`, asserts rendered AND no live element AND `window.__xss` undefined] + the UX-1 suite) + bookkeeping (`f8bed47`). Monitor 7-item audit CLEAN (re-verified: 11 files, BFF/journey/bff.js/genui 0-touch, ONB-0 history contract intact `:107`, foreign files never swept, **61/61 re-run green**, build clean) + inline critique NON-BLOCKING [0/2/0] + **independent `:5180` re-smoke** (monitor, Chrome MCP: A2 clean empty-state + A3 dead-buttons-gone + A4 journey-unchanged CONFIRMED live in light; dark stays executor-verified — the theme toggle is untouched chrome). Opened **S-BS-93** (low — persistent fake chrome [TopBar crumb + rail-footer + StatusBar] left as-is, by R1; A2 scoped to the chat surface). No proof capsule (UX-polish cycle, not a capability A-LIVE). **NEXT (the 3-objective sequence): (1) BYOC-1** — Claude as a native provider for all LLM use cases (`/devloop-kickoff bench-salvage BYOC-1`, driver READY, HARD-GATE); **(2) CRUD-from-clean** — guarded CRUD + admissibility gates (monitor authors `CRUD-1`/`FLAG-1`; DELETE absent, flag-create S-BS-83, blank-slate now closed). Session `session-bench-salvage-phaseUX-1-2026-06-07.json`; critique `critique-bench-salvage-phaseUX-1-2026-06-07.md`; A7 evidence `A7-smoke-bench-salvage-phaseUX-1-2026-06-07.md`. *Below = the ONB-0/ASAFE-1 close + the UAP history.*

**🧭 (prior — UAP-5c-2 CLOSED PROCEED-WITH-CAVEATS, 2026-06-05) — the conversational tool set is COMPLETE (8 tools); A-SAFE re-proven as the surface added its first paid-capable wrapper + first Agent WRITE; next = pick the next cycle (fork below); do NOT autostart.** UAP-5c-2 landed the pre-authorized D-F split IN FULL: **`run_eval_pack`** (the eval-pack BATCH — the FIRST paid-capable wrapper; schema carries no paid knob + `_run_eval_pack` HARDCODES `EvalPackRunRequest(..., live=False)`; the negative test is **critic-verified NON-VACUOUS** — scratch-reverting the hardcode FAILS it) + **`assemble_agent`** (the FIRST agent-reachable Agent WRITE — **EDIT-ONE-FACET** on `eval_profile.judges`, `LENS_BY_ROLE`-guarded, audited `target_type=agent`; never a full dict). Both → EXISTING cards (pure-read `tool-audit_log` / `tool-agent_editor`; no `run_panel`, no new types). **A-SAFE safe-by-explicit-bound:** the allowlist-bounds + no-paid-knob tests cover all 8 tools (the `[agent]`-gated test binds the real `bypassPermissions` options). **A6 byte-frozen:** `put_agent_endpoint` + `eval_pack_run_endpoint` byte-identical; all new code inside `_build_tool_context`. Build `3578905..1ff7ea8` (6 atomic pathspec-only, parent `b2a8c92`, NOT pushed). Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic (agent `af82bf4b57ab6c3af`) NON-BLOCKING [0/2/1] — the critic re-ran the suite (299/149/51), proved the live=False test non-vacuous by scratch-revert, and independently probed S-BS-84 (audited + reversible + no crash → LOW). **NEXT-CYCLE FORK (monitor's choice):** (1) an **S-BS-46/49 corpus-deepening** pass (clean-negative cohort → a real optimize WIN — the path UAP-4's honest LOSS pointed to) — **candidate lean.** (2) a small **S-BS-74 live-correction** demo (the visceral S-BS-70 flip, for a pitch). (3) **UAP-5a-assist** (R10 LLM author-assist) | **UAP-3b-3** (LLM Ralph-Loop critique). **A-LIVE = ATTESTED LIVE 2026-06-05 (monitor, Chrome MCP, `$0`):** `run_eval_pack` batched live + the agent confirmed it REPLAY-ONLY ("can never fire a paid batch" — the first paid-capable wrapper stays replay-only) + `assemble_agent` did the audited roster round-trip (BFF-verified: 2 `target_type=agent` records) + the `LENS_BY_ROLE` guard refused a fabricated judge (0 audit records). A1–A4 + A-SAFE PASS live. **A8 (UAP-5a) ATTESTED LIVE 2026-06-06** — run `8a41ef3e`, live `:8002` e2e, the S-BS-7 grounding suppression fired live; opened S-BS-85 (Load-run report-pane rebind). **Opened S-BS-84** (low — roster-remove no revalidation; OQ-1 + NB-1). *Below = the UAP-5c close + the prior forks + history.*

**🧭 (prior — UAP-5c CLOSED PROCEED-WITH-CAVEATS, 2026-06-05) — the FULL Domain→Judge→Flag→Run→Review conversational journey runs from a blank agent; both carried seams CLOSED; next was pick the next cycle (fork below); do NOT autostart.** UAP-5c grew the proven UAP-5b spine with 3 journey-completing tools — `get_agent` (`$0` Domain read) + `author_flag` (audited `PUT /v1/ontology`, **EDIT-ONLY** by construction) + `review_runs` (`$0` Review read) — each a THIN wrapper over a FROZEN op → an EXISTING gen-UI card (no new types). **Both carries CLOSED: S-BS-82** (the live `get_judge` break — fixed in the WRAPPER `_get_judge` w/ `assigned_flags=None`; `get_judge_endpoint` BYTE-IDENTICAL; the regression test is critic-verified NON-VACUOUS — it drives the BOUND closure, and reverting the fix makes its load-bearing line raise) **+ S-BS-81** (A-SAFE safe-by-explicit-bound — the allowlist-bounds + no-paid-knob-across-all-6 + `[agent]`-gated `bypassPermissions` bind tests ship). **A-SAFE is cleanest-possible:** the D-F SPLIT (assemble_agent + eval-pack → UAP-5c-2) means NO new tool wraps a paid-capable op — the critic's grep for any agent-reachable paid path is EMPTY; the Review leg uses pure-read `tool-audit_log` (after confirming `RunPanel.jsx:57` gates with `window.confirm`, off the chat surface → Chrome-MCP-driveable). Build `a4e313d..9227b87` (7 atomic pathspec-only, parent `9cac025`, NOT pushed). Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic (agent `ac63797ec49ec01ac`) NON-BLOCKING [0/1/1] — the critic re-ran the full suite (293 default / 143 debuglithrim / 51 Vitest), AST-verified all 10 wrapped BFF ops 0-delta, and proved the S-BS-82 test non-vacuous by reverting the fix. **NEXT-CYCLE FORK (monitor's choice):** (1) **UAP-5c-2** — the pre-authorized SPLIT: `assemble_agent` (`PUT /v1/agent`, audited WRITE — the heavier Domain-assembly) + `run_eval_pack` (eval-pack batch; the wrapper MUST hardcode `live=False`) → completes the "complete tool set"; `/devloop-expand-driver bench-salvage UAP-5c-2` — **candidate lean.** (2) an **S-BS-46/49 corpus-deepening** pass (clean-negative cohort → a real optimize WIN). (3) a small **S-BS-74 live-correction** demo (the visceral S-BS-70 flip, for a pitch). **A-LIVE = ATTESTED LIVE 2026-06-05 (monitor, Chrome MCP):** the full journey ran on `:5180` — `get_agent`/`get_judge` (**S-BS-82 fix VERIFIED LIVE**) + `author_flag` (audited edit VERIFIED against the BFF `/v1/audit`, `2026-06-05T15:53:35`) + `run_eval` replay + `review_runs` (the audit_log card); A-SAFE held (`$0` replay only). **OWED (carried):** the older **UAP-5a A8** `:5180` smoke. **Opened S-BS-83** (low — `author_flag` EDIT-ONLY; conversational flag creation stays a human act per owner↔emit). *Below = the UAP-3b-2 close + the prior forks + history.*

**🧭 (prior — UAP-3b-2 CLOSED PROCEED-WITH-CAVEATS, 2026-06-05) — the moat is operationally COMPLETE + auditable + live-SAFE; next was pick the next cycle (fork below); do NOT autostart.** UAP-3b-2 closed the moat's operational + audit layers: the withstands ruling now embeds in the run-provenance blob (**S-BS-72 CLOSED**, post-save patch above the frozen seam, attested live) + GroundingChecks are first-class config-authored entities (**A6**) + the gate's SAFE direction is **VALIDATED LIVE** (Run A: the on-by-default gate ADMITS a legitimate finding, doesn't break it — S-BS-73). **The visceral live CORRECTION is honestly OWED → S-BS-74:** Run B halt-to-surfaced (the authored DSPy trio doesn't over-fire the documented MED FP; the out-of-lens flip can't be authored) — NOT manufactured; the offline A2 + the live-SAFE attestation stand as proof. Build `662a1c0..24318a7` (5 atomic pathspec-only, parent `8a159c0`, NOT pushed). Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic (agent `acd1046fff8c76e9a`) NON-BLOCKING [0/2/3]; the critic re-ran the full suite (282 debuglithrim / 270 default), re-verified A4=0 + the REUSE-not-rewrite 0-delta across `signals`/`withstands`/`authored_stage`/`grounding`, and read the live rulings STRAIGHT FROM the sqlite blob (Run A safe / Run B genuine non-firing). **NEXT-CYCLE FORK (monitor's choice):** (1) **UAP-4** — optimize/calibration in-UI (R5, gated S-BS-49) = the calibration trainer, THE product (`calibration-trainer-is-the-product`); `/devloop-expand-driver bench-salvage UAP-4` — **a candidate lean (advance the product; the moat is proven + operational + live-safe).** (2) a small **S-BS-74 live-correction demo cycle** — engineer a case the authored trio over-fires → the gate corrects LIVE → the visceral S-BS-70 flip (for a pitch/demo; polish on an already-proven mechanism). (3) **UAP-3b-3** — the LLM Ralph-Loop critique (the split; stub written; lowest-value/uncertain per `critique-pass-precision-not-floor`). (4) **UAP-5a-assist** — R10 LLM author-assist. **OWED (carried):** the UAP-5a A8 `:5180` smoke; the optional `test_MOAT_EXHIBIT` comment (NB). *Below = the UAP-3b close + the prior fork + history.*

**🧭 (prior — UAP-3b CLOSED PROCEED-WITH-CAVEATS, 2026-06-04) — THE MOAT LANDED; next was pick the next cycle (fork below); do NOT autostart.** UAP-3b shipped the §2A withstands-gate: a deterministic signal corrects a confidently-wrong LLM judge PRE-consensus → the composite flips reject→approve, **GATE-ATTRIBUTABLE** (the A2 double-assertion: SAME case + SAME predictor, only `apply_gate` differs) — the genuine "watch the verdict change" **S-BS-70** named, now PROVEN offline/$0. Build `8cb388b..9eadf39` (6 atomic pathspec-only, parent `acc4973`, NOT pushed). Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic (agent `ae312d9a358e1ca69`) NON-BLOCKING [0/5/3]; the critic re-ran the full suite (273 passed), re-verified A3=0, INDEPENDENTLY traced the flip rides the FROZEN seam's own off-domain-Tier-1 logic (not rigging), and confirmed A5 (cannot relabel a true case) holds under the worst adversarial case. `signals.py` + `withstands.py` wired ABOVE the byte-0-delta `_apply_consensus`, ON by default (`apply_gate=True`). **SPLIT (pre-authorized D-E):** D3 (GroundingCheck first-class entities, A6) + the LLM Ralph-Loop critique + the **LIVE attestation** → **UAP-3b-2** (stub driver written; A6 SKIP this cycle). Opened **S-BS-72** (provenance-blob embedding → UAP-3b-2, principled split) + **S-BS-73** (gate ON-by-default → validate on the live attestation). **NEXT-CYCLE FORK (monitor's choice):** (1) **UAP-3b-2** — complete the moat: the LIVE paid attestation (discharges S-BS-70's visceral demo + S-BS-73 validation) + GroundingCheck entities (A6) + the LLM critique + the provenance-blob embedding (S-BS-72); `/devloop-expand-driver bench-salvage UAP-3b-2` (stub exists) — **monitor lean** (completes the moat + lands the live demo). (2) **UAP-4** — optimize/calibration in-UI (R5, gated S-BS-49). (3) **UAP-5a-assist** — R10 LLM author-assist. **OWED (carried):** the UAP-5a A8 `:5180` smoke. **Carried critique note (NB, optional):** add a one-line comment in `test_MOAT_EXHIBIT…` naming the implicit baseline-reject dependency (`_TIER1_OWNERS` off-domain downgrade) so a future seam change can't silently degrade the exhibit. *Below = the UAP-5a close + the prior fork + history.*

**🧭 (prior — UAP-5a CLOSED PROCEED-WITH-CAVEATS, 2026-06-04) — next monitor action was pick the next cycle (fork below); do NOT autostart.** UAP-5a shipped the two clean release-critical gaps — **JudgeEditor mounted in-UI (S-BS-62 CLOSED)** + **legible authored per-judge audit (S-BS-66 CLOSED)** — but the **visceral live verdict-flip did NOT reproduce (A4 live-FAIL, honest, user-pre-authorized → S-BS-70):** the base trio already BLOCKs the by-construction subtle `WRONG_DOSAGE` (base-owned ⇒ no cheap calibration near-miss; authoring sharpens an already-firing lens). Build `d7be919..2faf8d3` + log `e2c48a6` (6 atomic pathspec-only, NOT pushed). Monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic (agent `aafd47567039a9a19`) NON-BLOCKING [0/4/3]; the critic re-verified A3=0, lint-GREEN, D2-HONEST, A4-PRINCIPLED, and caught the **refuted "base under-fires" premise stated as fact** in the demo artifacts (monitor-corrected at close). **THE S-BS-70 FINDING IS STRATEGIC:** a genuine "watch the verdict change" demo needs a defect the base trio MISSES — the SEMANTIC axis / grounding / the **withstands-gate (UAP-3b, the moat)** — so the demonstrability finale is now ENTANGLED with the moat, strengthening UAP-3b-next. **NEXT-CYCLE FORK (monitor's choice):** (1) **UAP-3b** — the withstands-gate / signals orchestration (the moat); `/devloop-expand-driver bench-salvage UAP-3b` — now doubly-motivated (it's where the genuine flip lives) — **monitor lean.** (2) **UAP-5a-assist** — the SPLIT-OUT R10 LLM author-assist (describe→card, zyng `LLMClient`), `blocked_by: UAP-5a` now cleared — a $0-to-author demonstrability fast-follow. (3) a small **S-BS-70 demo cycle** — construct a base-missed (semantic / non-v2-owned) flip case to land the paid finale. **OWED:** the A8 `:5180` visual smoke (A1, user-run; on attestation UAP-5a A8→PASS). **Runtime caveat (NOT git):** the executor reset the judge store to `[]` + re-seeded the config DB so `uap5a_flip_demo` is BFF-loadable on `:8787`; `PUT /v1/judges/risk_judge {assigned_flags:[]}` for a clean `ws0_default` baseline. *Below = the UAP-5a kickoff + the UAP-3 close + UAP-1/2 history.*

**🧭 (prior — UAP-5a driver authored + kicked off, 2026-06-04) — DECIDED: lead with UAP-5a; the action was `/devloop-kickoff bench-salvage UAP-5a`.** UAP-3 closed PROCEED-WITH-CAVEATS + the A8 smoke ran (UAP-1+UAP-3 PASS in-browser; the in_process authored path runs live but **S-BS-66** leaves the per-judge audit empty + `ws0_default` shows no flip — both addressed by UAP-5a). The user chose **5a over 3b** (release-critical: the in-UI authoring loop is the gap a user hits; the moat 3b builds on top). **Driver authored + READY** (`4bd23d0`, `bench-salvage_phaseUAP-5a_authoring-loop_driver.md`; **HARD-GATE**; index `status: ready`; pack backfilled → 27 tasks): the RE-CUT = **D1** mount the JudgeEditor in CenterPane (S-BS-62) + **D2** fix S-BS-66 (legible authored per-judge audit; `stages.py` mapping only, A3 frozen-seam 0-delta) + **D3** a by-construction live-flip case + agent (the visceral paid finale, verified live + reproduced offline/`$0`). **R10 author-assist SPLIT → UAP-5a-assist** (P1 fast-follow, `blocked_by: UAP-5a`) so the loop ships LLM-free. **NEXT MONITOR ACTION:** `/devloop-kickoff bench-salvage UAP-5a` → paste into a fresh executor; plan-review resolves the 4 decisions (**D3 the flip-case construction is the hard one** — no `wrong_dosage` injector exists). At close: fresh-critic (`/devloop-critique bench-salvage UAP-5a`). **All committed on `bench-salvage/ws6c-dspy`, NOT pushed:** `c455356`→`266fc4b` (UAP-3 close incl. the A8-smoke cost-narrative correction) + `4bd23d0` (the UAP-5a driver). **Runtime caveat (NOT git):** risk_judge carries a smoke `WRONG_DOSAGE` assignment in the BFF config DB — `PUT /v1/judges/risk_judge {assigned_flags:[]}` to reset `ws0_default`. *Below = the UAP-3 close + the fork analysis + UAP-1/2 history.*

**🧭 (prior — UAP-3 close, 2026-06-04) — UAP-3 CLOSED PROCEED-WITH-CAVEATS; the next-cycle fork (UAP-3b vs UAP-5a) was resolved to 5a (above).** The authoring loop is closed end-to-end: an authored judge now re-votes with its authored lens through the in-process grade (**S-BS-63**), proven `$0`/offline — `WRONG_DOSAGE`→`risk_judge` flips composite non-reject→`reject`, the other roles don't move (the change is the AUTHORING); preview↔live parity test-anchored (the fresh-critic independently traced the flip is causal, NOT trivially-passing). Build `5959e28..b3079d2` (8 atomic, pathspec-only on `bench-salvage/ws6c-dspy`, **NOT pushed**); monitor 7-item audit CLEAN (suites monitor-re-ran `$0`: default 284/13 · debuglithrim uap3+bff+council 133/3 · Vitest 44 · **A2 frozen-set 0-delta**, ruff-clean changed files) + HARD-GATE **genuinely-fresh-critic** (agent `a98779f542ddee6c5`) NON-BLOCKING (0 BLOCKING / 5 NB / 3 OQ). **Closed S-BS-63 + S-BS-56 + S-BS-52.** Opened **S-BS-64** (med — live `:8002` per-judge injection WS-2-backend-gated; S-BS-63 closes in_process only) + **S-BS-65/66/67/68** (low; **S-BS-67 FIXED at close** = the §10 `/v1/runs` row-shape widened to the shipped projection; **S-BS-66 = PROMOTE to a gate before the authored path is shown as an audit exhibit** — empty `model`+`reason` on authored votes, INHERITED DSPy-seam property; **S-BS-68 = `created_at` re-stamp vs §2B append-only, scoped blob-tier fix**). Run-history + `pipeline_run_id` + `POST /v1/eval-pack/run` + the `RunPanel` gen-UI shipped; §10 ratified (`8088d88`). **OWED: the A8 `:5180` smoke incl. the PAID in_process authored-flip** (Decision 3 batched it — the visceral demonstrability finale; on attestation flip UAP-1/2/3 A8→PASS). **NEXT-CYCLE FORK:** `/devloop-expand-driver bench-salvage UAP-3b` (the withstands-gate / signals orchestration = **the moat**) **OR** pull **UAP-5a** forward (authoring-assist describe→card + the **S-BS-62 JudgeEditor-mount** as the first line-item) for a `$0` demonstrability win — per the handoff, UAP-5a MAY pull forward. Session `session-…UAP-3….json`; critique `critique-…UAP-3-2026-06-04.md`. *Below = the UAP-2 close + UAP-1 + reframe history.*

**🧭 (prior — UAP-2 close, 2026-06-04) — UAP-2 CLOSED PROCEED-WITH-CAVEATS; next monitor action = `/devloop-expand-driver bench-salvage UAP-3`.** Judge authoring via ontology-assignment shipped + closed (`86fa1d3..3e89ad5`; monitor 7-item audit CLEAN + HARD-GATE genuinely-fresh-critic agent `a8c77571d1a911d6d` NON-BLOCKING [0/3/2]; suites monitor-re-ran default 277/12 · council 94/3 · BFF 30 · Vitest 40, all `$0`). The layered prompt↔ontology bridge (`judge_assignment.py`; A4 byte-parity all 3 roles vs the LIVE council loader — **S-BS-11 preserved**, faithfulness keeps its 164-line base), `GET/PUT /v1/judges` + the `LENS_BY_ROLE` owner↔emit 422 (CITATION-DRIFT: NOT the stale ontology `owner_roles` → S-BS-59), the `JudgeEditor` `$0` assignment→prompt preview (demonstrability), the council-light `judge_assignment.py` extraction (import-isolation verified). **The close WAITED for the concurrent UAP-5a/5b commit (`111ac3c`) then landed clean** (user-elected; pathspec-only). **NEXT = UAP-3** (`RunPanel`/processing + eval-pack + run-history, R4/R6); **UAP-5a authoring-assist MAY pull forward** for a `$0` demonstrability win. **OWED: the batched UAP-1 + UAP-2 `:5180` A8 visual smoke** (user-run). Opened S-BS-59/60/61; closed S-BS-57. **Post-close A8 `:5180` smoke (2026-06-04, Chrome MCP):** UAP-1 A8 → **PASS** (Agent editor + audit live); UAP-2 backend → PASS live (`$0` bridge/owner-gate/audit) + one PAID `$0.20` in_process trio confirmed the live grade (`BLOCK`). Two gaps: JudgeEditor not UI-mounted → **S-BS-62** (fix task spawned); authored→live-grade wiring → **S-BS-63** (UAP-3; monitor held a misleading paid spend after diagnose-before-edit). Session `session-…UAP-2….json`; critique `critique-…UAP-2-2026-06-04.md`. *Below = the UAP-2 kickoff + UAP-1 close history.*

**🧭 (prior — session 2 close, 2026-06-04) — the UAP-2 driver was AUTHORED + READY; next monitor action = `/devloop-kickoff bench-salvage UAP-2`.** This session: (1) **closed UAP-1** PROCEED-WITH-CAVEATS (`b89110f`); (2) drove the A8 `:5180` smoke via the Preview MCP (Chrome ext was down) — R1+R0 confirmed in-browser, and the R3 draft→grade flip **found + fixed S-BS-58** (a `load_ontology @lru_cache(path)` defect that silently dropped iterative draft edits in the long-running BFF — the core "edit → see it grade" loop; `78929e2`, mtime-keyed, regression-guarded, **0 regressions**, recorded `d7f65c6`); (3) shipped **BFF watch-mode** (`c2d4495`, uvicorn `--reload` scoped); (4) **authored the UAP-2 driver** (`43d162c`) — judge creation via ontology-assignment + the prompt↔ontology bridge (render `JudgeSignature.role_key_questions` from `ontology.questions_for(role)` ⇒ the **static→live close for judges**, no WS-2 `:8002` backend), with **demonstrability first-class** (user: "judge creation must be demonstrable for people" — the assignment→prompt→questions link instant + `$0`; the verdict-change is the paid finale; memory `judge-creation-must-be-demonstrable`). **Next:** `/devloop-resume` → `/devloop-kickoff bench-salvage UAP-2` (driver `bench-salvage_phaseUAP-2_judge-authoring_driver.md`, status ready; 6 plan-review decisions, the bridge-render is load-bearing — A4 parity guard so safety-critical judge prose doesn't drift). All on `bench-salvage/ws6c-dspy`, **NOTHING PUSHED**; pathspec-only (foreign `.claude/`/`KICKOFF_CRITIC`/`REPORT_fhir…` untracked). *Below = the UAP-1 close + the reframe history.*

**✅ UAP-1 CLOSED 2026-06-04 — the FIRST build of the LOCKED unified-authoring product shipped (`6bdf975..a7edd25` + log `c2e01ae`; PROCEED-WITH-CAVEATS).** Monitor 7-item audit CLEAN (re-verified: A5 frozen-seam + seeds 0-delta; ruff-clean on the 6 cycle files; default 267/11, debuglithrim 324/3, Vitest 37/37) + HARD-GATE **genuinely-fresh-critic** (agent `a62e561dc12854912`) NON-BLOCKING (0 BLOCKING / 2 NB / 2 OQ). R0/R1/R3 landed: `/v1/agent` config-plane write-path + the draft→grade loop (**S-BS-26(b) closed**) + the immutable append-only AuditRecord/actor foundation (the §2B audit-is-the-product floor). §10 ratified in-cycle (**S-BS-51 closed**). All 5 monitor plan-review notes folded (N1–N5). **The ONE OWED gate = the A8 `:5180` visual smoke** (user-run, no-autostart) — the close is PROCEED-WITH-CAVEATS until attested (WS-5c posture). **Monitor correction:** the hand-back claimed S-BS-50 closed — it is NOT; UAP-1 honored the `bff.js` indirection for its own new code, but the *journey's* hardcode is untouched (frozen) → **S-BS-50 stays OPEN**. Opened **S-BS-52/55/56** (replay-not-auditable · per-judge evidence projection · `run_eval` doesn't surface `pipeline_run_id` — all UAP-3) + **S-BS-53/54/57** (low/doc). **🎯 NEXT = the UAP-2 driver** — `JudgeEditor` + `/v1/judges` via **ontology-assignment** (R2; the headline judge-creation gap, OQ-1 decided). Run `/devloop-expand-driver bench-salvage UAP-2` (re-grep `judges_dspy.py`/`judge_metric.py`/`ontology.py questions_for`/`council_roles/*.txt`/`app.py`; the prompt↔ontology bridge is the load-bearing decision). Session `session-bench-salvage-phaseUAP-1-2026-06-04.json`; critique `critique-bench-salvage-phaseUAP-1-2026-06-04.md`; handoff `HANDOFF_bench-salvage_phaseUAP-2_kickoff_2026-06-04.md`. **Post-close 2026-06-04: the A8 `:5180` smoke FOUND + FIXED `S-BS-58`** — a `load_ontology @lru_cache(path)` defect that silently dropped iterative draft edits in the long-running BFF (R3's core "edit → see it grade" loop). Fixed via an `(path, mtime)`-keyed cache + 2 regression tests (the A2 committed→draft blind spot closed); 0 regressions. A8 R1/R0 confirmed in-browser; the R3 flip is now correct (was masked by this cache). The smoke earned its keep.

**🎯 STRATEGIC REFRAME — 2026-06-04 (user, STANDING): unify the two tracks; FREEZE the journey; the target is a complete UI-driven authoring+processing product.** The 4-act **journey is FROZEN** — a pitch/onboarding demo; its improvements are deferred *into* the product, not standalone rework. The **DSPy-judge/calibration track + the shell/product track UNIFY into ONE program**: a complete product whose **UI itself drives** (1) **judge creation** (DSPy judges `runtime/council/{judges_dspy,judge_optimize,judge_metric}` + the calibration trainer `SPEC_CALIBRATION_TRAINER.md`), (2) **flag creation** (ontology flags via the WS-5c gen-UI `FlagEditor`/contract-builder + `PUT /v1/ontology` WS-5d), and (3) **processing** (`run_eval`/`harness/grade` + the merged `dosage_grounding` floor + `KbRagTool`, `dc1cb7c`). The building blocks already exist — WS-1 SQLite **config plane** (`{judges, council_config, ontology, tools, kb_bindings}`) + the BFF + the shell + the gen-UI authoring widgets — so the next step is to **compose them into the authoring+processing UI**, NOT more demo polish. **WS-7b reframes:** floor+KB "Align" belongs in the product's **processing surface**, not Act-3 journey rework. **✅ SPEC LOCKED 2026-06-04 (user) — `docs/specs/SPEC_UNIFIED_AUTHORING_PRODUCT.md`** (OQ-1..5 resolved: judges = ASSIGNED ontologies + execute-not-generate validators; the Ralph-Loop **withstands-gate** [locus = BOTH]; first-class why/when/who/what **audit trail** §2B. **NEXT = the UAP-1 driver** — config-plane write-path `/v1/agent` + draft→grade + the audit-record/actor foundation). Grounded against the real surfaces (`harness/config.py` `Agent`/`EvalProfile`; `harness/ontology.py`; the 7-route BFF + the gaps; the gen-UI widgets). **Headline finding: the judge-creation surface is the real gap** — flags have `FlagEditor`+`PUT /v1/ontology`, but judges have NO gen-UI widget + NO BFF create/persist route (the ontology's `JudgeQuestion` is data, but runtime judges read `council_roles/*.txt` — the one new architectural decision = the prompt↔ontology bridge, OQ-1). **Proposed build sequence: UAP-1** (config-plane write-path `/v1/agent` + the draft→grade loop S-BS-26b) → **UAP-2** (`JudgeEditor` + `/v1/judges` + the bridge) → **UAP-3** (`RunPanel`/processing + eval-pack + run-history) → **UAP-4** (optimize/calibration in-UI, gated on S-BS-49). Memory: `unified-authoring-product-frozen-journey`. *Everything below is the track history this reframe unifies — WS-7a was the last journey-targeted cycle.*

**✅ WS-6c-DSPy-3b CLOSED 2026-06-03 — the DSPy judge loop is CLOSED + MEASURED (risk_judge); the held-out Δ is NEGATIVE and honest.** The optimize→measure→bind-back stages landed on the 3a corpus (`781abc4..820cf88`; monitor audit CLEAN + **spawned fresh-critic CLEAN** + 1 OQ; ~$0.26 one run). `BootstrapFewShot` compiled `risk_judge` on the 12-case calibration split; both arms scored on the 10-case held-out split via the same `score_judge` → **precision 0.71→0.44, recall 0.71→0.57, graded 0.80→0.70**. Per the driver, a *measured* Δ (incl. ≤0, gate NOT loosened) IS the loop-closure — and the negative result is the load-bearing honest finding. **CONFIRMED root cause (→ S-BS-49):** the exact-accept gate harvested only the 4 already-perfect cases, which on this small mixed corpus are all SILENT (empty findings) → 0 positive exemplars → the compiled judge over-fired out-of-lens VALUE_MISMATCH. A1 frozen 0-delta ×3 (monitor + critic both re-ran the diff). **Next on the DSPy track (PAID, not the ship-path):** **S-BS-49 first** — a coverage-aware demo-selection metric (require ≥1 positive exemplar) / MIPROv2 / higher-recall teacher, BEFORE any **S-BS-48** bind-back-by-default (do NOT ship the current negative-Δ demos). Trio optimization (policy + faithfulness) + **S-BS-42** (PHI recall) + **S-BS-45** (policy elicitation) are now data-enabled but wait on the S-BS-49 gate. *This whole track is the engine the user's hand-run `RUN_calib_progression` demo points at — the product capability that must one day reproduce that demo natively.*

**🧬 WS-6c-DSPy-3a CLOSED 2026-06-03 — the judge-calibration corpus landed ($0); the optimizer (WS-6c-DSPy-3b) is now well-posed.** Re-sequenced ahead of the optimizer once DSPy-2-live proved it **data-blocked** (≤1 council-judge positive/judge, 0 for policy). Landed offline/$0 (`c847501..dad8b6f`; monitor audit CLEAN + **spawned fresh-critic CLEAN**): the net-new **`FABRICATED_CONSENT` injector** + **`examples/judge_calib_v1.jsonl`** (47 cases, lint-GREEN, byte-deterministic, **policy 0→12**, splits 17/30) + the **co-raise-aware lens** (**S-BS-43 closed**, opt-in). The adversarial fresh-critic independently confirmed every recipe self-evidently warrants its label + owner-correctness across all 47 cases (ran the lint + tests + a full regen). **Next = WS-6c-DSPy-3b (PAID — needs a cost-go):** BootstrapFewShot climbing `make_judge_metric` (pass `co_raise_aware=True`) on `examples/judge_calib_v1.jsonl` (calibration split = trainset, test = held-out) — and with policy now carrying 12 positives, **S-BS-42** (PHI recall) + **S-BS-45** (policy elicitation divergence) become measurable/investigable. **Watch:** **S-BS-46** (VALUE_MISMATCH 1/6 — widen the cohort before the faithfulness/VALUE_MISMATCH lens is leaned on) + **S-BS-47** (`CLAUDE.md` inv#3 doc drift). The transcript-only blind spot remains the **grounding-track** motivation — only patient-record grounding flips the clean-negative false-blocks. *This whole track is the engine the user's hand-run `RUN_calib_progression` demo points at — the product capability that must one day reproduce that demo natively.*

**⚡ Product-completion pivot (2026-06-02→04) — current lead. LANDED 2026-06-04: the concurrent calibration vertical is committed + ws7b-kb merged.** Roadmap pivoted from packaging-first to **WS-7 product feature-completion** (the 4-act journey on REAL service calls; packaging **WS-5e/WS-6e deferred to the last mile**). **The concurrent calibration session's vertical is now COMMITTED** — 7 atomic pathspec'd commits **`a6424da..4b46ad4`** (preserve-not-bless): the **`dosage_grounding` deterministic floor + `DosageGroundingTool`** (offline, flips verdict via WRONG_DOSAGE injection; `test_dosage_floor` 6/6) + **`SPEC_CALIBRATION_TRAINER.md`** + the `RUN_calib_progression` demo + the **semantic-moat-pitch journey rework** (Acts 1-4) + a **`/v1/case` BFF** route + the UX/design critique + the **semantic-moat research** (`REPORT_semantic_moat_proof` + `SYNTHESIS_…`; the `PAPER_OUTLINE` amendment is the user-ratified 2026-06-02 record, **§1 UNCHANGED**) + devstack tooling + the council **`extra=ignore`** env fix. **WS-7b-py MERGED @ `dc1cb7c`** (additive union — `kb_grounding` suppress + `dosage_grounding` floor coexist; `KbRagTool` + `DosageGroundingTool`; `/v1/kb` + `/v1/case`; re-verified **`tests/verification/` 79 passed** on debuglithrim + the default-core leverage gate holds — no httpx/onnx/pinecone/openai leak). Closes the WS-7b-py MERGE-PENDING item (audit `.devloop/sessions/audit-bench-salvage-WS-7b-py-2026-06-03.md`; **S-BS-40 + S-BS-41 stand**). **WS-7a is now DONE-IN-TREE, NOT a pending build:** the landed journey (`26e479f`) already wires Phase-2 Verify to the live grade — `runVerify`→`POST /v1/run-eval {live:false}` ($0 replay default = A2✓), real composite/votes via `gradeResult` + `GET /v1/case` (A1✓), `:8787` BFF with a `bffDown` fixture fallback (A4✓). **BUT it overshot the WS-7a driver** (added the `/v1/case` BFF endpoint — the driver forbade new BFF endpoints, an **A3 leverage-gate deviation** — and reworked Acts 1/3/4 vs Phase-2-only) **and bypassed devloop** (no D4 wiring test, no `:5180` visual smoke, no session log/critique). **WS-7a verify-and-CLOSED 2026-06-04** (`0b1d2d0` D4 test + `critique-…WS-7a-2026-06-04.md` inline): audit done (A1/A2/A4 met + tested), the **D4 test** added (the journey's first; **33/33** shell + `vite build` clean), **A3 deviated** (the journey extended the BFF — `/v1/case` + council view + `in_process` — additive/justified but outside §10 → **S-BS-51**; it also hardcodes `BFF_URL` → **S-BS-50**). **The one OWED gate = the `:5180` visual-parity smoke** (user-run, no-autostart) — the close is conditional on it (WS-5c posture). WS-7b (Align: floor + KB into Act 3) is the next product act. **Both the calibration vertical AND WS-7a bypassed the cycle (preserve-not-bless) → each warrants its own close pass** (the floor's S-BS-16 inject-param gate; the journey/research un-audited). A concurrent session owns the **calibration trainer** (the Phase-3 *product*, `docs/specs/SPEC_CALIBRATION_TRAINER.md`); the dosage floor + KB grounding are its Act-3 engines (`[[calibration-trainer-is-the-product]]`). *The WS-5e/WS-6e "Next" framing in item 1-2 below is SUPERSEDED by this pivot.*

**Backend vertical complete (WS-0→WS-4a); PRODUCT DIRECTION SET 2026-06-01. Shell program: WS-5 skeleton + WS-5b journey + WS-5-BFF vertical + WS-5c generative-UI/Tailwind foundation + WS-5d artifacts-pane-wired ALL DONE (2026-06-01) — the React→Python stack is PROVEN on one real artifact (SPEC §9 honest-bet paid down: live verdict == replay, S-BS-7 holds on live data); the §5b gen-UI registry + Tailwind/`@theme` foundation are LOCKED contracts; and the locked v1 BFF API now carries the `PUT /v1/ontology` write (clobber-safe working copy) on top of the ratified reads.** The shell is a real React/Tauri product **program** + a **strangler-fig that retires Mongo**. Specs: `docs/specs/SPEC_PRODUCT_SHELL.md` (§8 FE-first + §10 BFF-surface ratified 2026-06-01) + `docs/specs/SPEC_PRODUCT_SERVICE_TOPOLOGY.md` (sequencing B). All FIVE built shell phases **audited CLEAN + critiqued (WS-5/5b inline NB; WS-5-BFF + WS-5c + WS-5d fresh-critic NB) + closed** (`apps/shell/` + `apps/bff/`; `842db03`/`d3ddd47`, `460b677..00c68b9`, `f19dee4..4549a12`, `efefaa8..e9f863b`, `38e4071..b065f14`+`5518ffb`). **WS-5e (packaging) is the last shell phase.**

> **⚠️ One human-eyeball step still OWED on WS-5c (carried, not blocking):** the 60s `:5180` visual-parity smoke (light/dark × Shell/Journey). De-risked by the new `app.test.jsx` Shell-`TopBar` guard (the stray-`}` regression is now test-covered) but the light/dark Preflight-bleed parity is still un-eyeballed. The fresh-critic caught a *visible* defect that build + 18 tests missed — treat the `:5180` smoke as load-bearing for every shell phase going forward, not optional. User runs it (no-autostart).

1. **PRODUCT-0 — shell program: WS-5 / WS-5b / WS-5-BFF / WS-5c / WS-5d all DONE (2026-06-01). Lead = WS-5e, the last shell phase (do NOT autostart):**
   - **WS-5e** (HARD-GATE — the lead) — Tauri installers + the **deferred Tauri sidecar** (from WS-5-BFF) + VPC packaging + offline-license + **S-BS-20** (Playwright/E2E for Radix popover/drag interactions) + (fold or sequence) **S-BS-26(a)** journey-act tool-part mounting if not split into a small WS-5d-follow. `/devloop-expand-driver bench-salvage WS-5e`.
   - **WS-5d-follow** (optional, small) — **S-BS-26**: (a) wire jp1–jp4 to emit input `tool-parts` (the journey is the real activation surface; CenterPane is wired, the acts are not); (b) decide whether a `PUT /v1/ontology` draft should feed an eval run — today the working copy is decoupled from `run_eval.py:118` (committed-seed source) by design. Both inert for the demo; both are fresh-critic OPEN-QUESTIONs for the spec author.
2. **Parallel tracks (monitor's choice):** **WS-6** = Python-Layer consolidation. **WS-6a ✅ CLOSED**; **WS-6b RECOMPOSITION PLAN landed 2026-06-01** (`9f8b907`; `docs/specs/RECOMPOSITION_PLAN_ws6.md`, 8 sections, all citations CONFIRMED via the 8-agent audit). The vision: **salvage the enabling infra + recompose the LangGraph agentic layer in-process + PORT the council consensus IP + DSPy-rebuild the judges/taxonomy + repository-interface persistence; Mongo retired; ONE clean Python-Layer service powers the bench.** **WS-6b ✅ RATIFIED 2026-06-01** (decisions locked: v2-only Azure trio, recompose 6 KPI agents / drop dead `evaluation_agent` / park LiveKit sim, defer KB→6d-KB; taxonomy re-snapshotted @ `493b533` — no drift, lint GREEN, S-BS-29 resolved; `snapshot_taxonomy.py` 2 bugs fixed; new seam S-BS-30). **WS-6c council-PORT ✅ CLOSED 2026-06-01** (`12281e1..6bae3c0`; inline critique NON-BLOCKING, HARD-GATE user-elected; A5 live PASS with Mistral `confidence=None` end-to-end; **S-BS-28 + S-BS-30 closed**, **S-BS-31** opened). **WS-6c-DSPy ✅ CLOSED 2026-06-02** (`95174c0..4e1d3cc`; audit CLEAN 7/7, critique NON-BLOCKING; HARD-GATE inline-by-continuation; risk_judge rebuilt on DSPy emitting the §6 seam, ported consensus byte-ZERO-delta, D2 logprobs bridge live-validated $0.0067; **closed S-BS-27**, opened **S-BS-32 + S-BS-33**; NOT wired into grade). **WS-6c-AGENTIC ✅ CLOSED 2026-06-02** (bench `903478e..5cf438b` + backend `6720c70`@`mvp-ready`; HARD-GATE **genuinely-fresh-critic** [6-agent workflow `wf_fdb93c9c-776`] NON-BLOCKING; **the council scores real cases through the grade seam** — live v2 trio on `proof_case:drop_allergy` → `reject`, Mistral `None`, on-disk fixture `a5_live.drop_allergy.json` + env-gated real-council test; D0 S-BS-31 fix cross-repo, M1 spine→v2, `grade_inprocess` behind the frozen seam; test-integrity PRINCIPLED, consensus byte-frozen; **closed S-BS-31 + S-BS-32 + S-BS-35**, opened **S-BS-34**; the **D2 reframe** — orchestrator = `pipeline/` PRIMITIVE, always-run-council is verdict-equivalent-or-STRICTER than the backend's approve-only gate — is load-bearing for OBS). **WS-6c-OBS ✅ CLOSED 2026-06-02** (`2321090..3cda9df`; greenfield observation/KPI recompose into `lithrim_bench/runtime/observation/`, tier A+; audit CLEAN + inline critique/monitor-deep-read NON-BLOCKING; LLM-port scoring+prompt fidelity verified from source; opened **S-BS-36** [audio bodies deferred] + **S-BS-37** [`datetime.utcnow`]; DRIFT-2b driver self-correction). **WS-6d ✅ CLOSED 2026-06-02** (`f898790..a211773`; the in-process grade path is now **Mongo-free** — `SqliteProvenanceStore` = the S-BS-38 blob tier behind the frozen `save` seam; A3 frozen-contract PASS+principled on `debuglithrim`; opened **S-BS-39**; the executor's paper-file contamination was rewrite-decontaminated + monitor-verified clean). **SHIP-PATH chosen (user 2026-06-02): retire Mongo → packaging.** **Next: `WS-5e`** (Tauri installer + offline-license + VPC — the shippable artifact, HARD-GATE; 6d was its precondition, now met; `clinical_v1`'s floor is inert so it ships without `:3031`) ∥ **`WS-6e`** (ETLP JVM sidecar — the co-pole, needed once a floor-declaring domain exercises `:3031`). **WS-6d-KB** deferred (post-v1). **Off the ship-path (parallel):** the **remaining two judges** + the A/B harness (**WS-6c-DSPy-2**, calibration/paper-aligned) · **WS-4b** locked calibration gate · the **semantic-moat proof** (note: `REPORT_semantic_moat_proof` is in-tree — the deferred axis has activity). **MUST heed S-BS-24** (suite not green). · **paper-1-copilot** §2/§4/§6 (unblocked, no live cost).
2. **Next cycle (monitor's choice — do NOT autostart):**
   - **WS-4b** — eval-pack loop + LOCKED preregistered calibration gate + external-SDK `/eval-runs/compare` regression-parity (cross-repo, HARD-GATE). Builds on WS-4a's corpus + thin eval-pack. **Gated** on S-BS-13 (floor-apply replay) + S-BS-16 (inject-param validation) BEFORE any floor-declaring ontology ships; picks role-aware-vs-eligible-raiser ownership (WS-1 Q4.2: `_TIER1_OWNERS`-only = 8/23 flags). Run `/devloop-expand-driver bench-salvage WS-4b`.
   - **WS-3b** — KB/RAG/ONNX stack (S-BS-5 + pinecone via `:8002 /v1/kb/search`) + judge-calls-tool MID-LOOP into the live council (mid-loop half waits on WS-2's paused backend; KB half does not). Natural home for **S-BS-16** + the S-BS-15 rename + S-BS-14 DX fix + stale `tools.py` comments / dangling `TOOL_KB_RAG`.
3. **WS-2 backend half (separately gated, unrelated to WS-3):** still blocked on (a) user lands/stashes the in-flight `../lithrim-backend/` WIP (8 files) and (b) user sign-off on the every-`:8002`-caller blast radius; backend commits need their OWN fresh-critic pass. Carry S-BS-12 (bidirectional lint) into that driver.
4. **Paper feed:** WS-3a now settles `paper-1-copilot` **S-P1-22** (§2/§4 reframe) — the DSPy bench-gated generator is the disclosed mechanism for §4/§6/§7. Resolve S-P1-22 once the commits land.
5. **Carry WS-1 Q4.3:** `resolve_case_fixtures` n10-first default → future pack-hygiene note.
6. **Carry from the WS-6a close (2026-06-01):** (a) **`council_error_rate`** rollup deferred to **WS-4b** (fix #8 partially met; the per-run `council_error` flag shipped is its prerequisite; treat `council_error=None` as excluded from the denominator). (b) **`judge_votes[*].confidence` is now `float | null`** (D-E): **DISCHARGED at WS-5d** — JudgeTab renders realized votes `null`-tolerant (`4363f05`); the harness composite reader was already null-tolerant. (c) **S-P1-15 attribution:** WS-6a's committed code is PIPELINE_GRADING_AUDIT D-E/D-J/D-K + `43b20f6` P0-1, **NOT** S-P1-15 (the driver's guess); reconcile the S-P1-15 references in `STREAM_paper-1-copilot.md` (cross-stream; paper-1 monitor). (d) **S-BS-16 symmetry:** WS-6a `_check_severity` silently falls back on an unrecognized validator severity (critique Q4 OQ); same loud-validation argument as the floor-inject case.

---

## References

- Persona: `.devloop/personas/MONITOR.md`
- Task pack: `.devloop/tasks/TASK_PACK_bench-salvage.json` (M1 + COUNCIL-EVIDENCE-GATE history; WS-0..WS-6)
- Memory: `walking-skeleton-architecture`, `etlp-ecosystem-capability-map`
- Live baseline: `out/scribe_v1.live_pipeline_evaluate.bench_scribe_v1_inject_condition_1bd0f10dc7b5.json`
- Falsification (S-BS-7 origin): `docs/research/REPORT_r3d_precheck_falsification_2026-05-30.md`
- Paired publication stream: `.devloop/state/STREAM_paper-1-copilot.md`
