# STREAM: `bench-salvage` (Bench Salvage: salvage-and-compose the shipped council + pipeline into an in-process local stack)

> **Live monitor state for the bench-salvage stream** (the PRODUCT/ENGINE track
> that runs alongside, and feeds, the publication track `STREAM paper-1-copilot`;
> they share one substrate, the vendored v1 compliance council in
> `lithrim_bench/runtime/council/` and the `scribe_v1` pack).
>
> **Stream owner:** monitor
> **Created:** 2026-05-30
> **North star:** salvage-and-compose the shipped Lithrim verification stack
> (compliance council + sync `/v1/pipeline/evaluate` orchestrator) into a local,
> in-process engine (no Mongo/Pinecone/Celery/etlp-mapper) that reproduces our
> own paper eval-run findings on the product path, then layer the SME-Question
> Ontology, local vector retrieval, the Clojure server, and the desktop shell on
> top. Acceptance test: reproduce the paper findings on the salvaged
> in-process analyze -> council -> eval path. PAPER GUARDRAIL: none of this
> weakens `PAPER_OUTLINE`'s locked one-claim; scribe is semantic-only by
> construction (paper semantic-baseline characterization, never thesis
> validation, HL7 owns the structural-floor demonstration).

## Authority docs

- `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md`, M1 spine close-out; the CONFIRMED reject==reject case-1 result + the confirmed `MEDICATION_NOT_IN_TRANSCRIPT` false positive that motivates the NEXT cycle; run command; key decisions 1/4/6; open questions
- `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md`, the R3(d) fix design (output-contract + aggregation change), validation protocol, confidence ledger; status: spec only, no council code edited (the persisted-reasoning enabler §6 did land)
- `docs/research/REPORT_council_overfire_convergence_2026-05-28.md`, the v2 cross-provider fabrication-family over-fire characterization + R1-R4 candidate fixes; R3(d) is the ADDITIVE new R3(d) (code-level companion-flag bypass) that composes with R3(a) pre-stage normalization
- `docs/PAPER_OUTLINE.md`, locked one-claim paper outline (preserve; the salvage touches the semantic/council axis + false-positive characterization, NOT the structural-floor claim / HL7 demonstration / silent-confident-certification headline)
- `lithrim_bench/runtime/council/compliance_council.py`, the vendored v1 compliance council (the shared substrate; `_normalize_result` :1461, `_apply_consensus` :1853, the legacy violations_found branch :1962-1971, the derive-from-findings guard :1530)
- `lithrim_bench/backends/local_pipeline.py` + `lithrim_bench/eval_runner.py`, the in-process `LocalPipelineBackend` + NDJSON eval runner (persisted per-judge reasoning surface that made offline root-cause possible)
- `scripts/run_local_scribe.py`, the M1 acceptance-test runner (`--limit`, `--n`); reproduces the case-1 FP live with BYOK Azure gpt-4.1 + `COMPLIANCE_COUNCIL_VERSION=v1`

---

## Section structure (per M1: locked)

| § | Title | Source material | Status |
|---|---|---|---|
| §1 | M1 spine, salvaged council + sync orchestrator run in-process on the shipped path | `lithrim_bench/runtime/{council,pipeline,services}/`, `backends/local_pipeline.py`, `scripts/run_local_scribe.py`; `out/scribe_v1.local.ndjson` | **DONE 2026-05-30**, case-1 reject==reject (1/1); `MEDICATION_NOT_IN_TRANSCRIPT` confirmed FP |
| §2 | Persisted-judge-reasoning enabler (the no-regrets change that made offline root-cause possible) | `backends/base.py:32` `JudgeOutput.reason`; `local_pipeline.py:118`; `eval_runner.py:62` per-judge reason + `:79-80` findings_rich | **DONE**, captured in the M1 spine; 82 tests green |
| §3 | Council-evidence-gate fix (R3(d)), close the legacy `violations_found` per-flag bypass in consensus | `SPEC_consensus_evidence_gate_bypass_2026-05-30.md`; `compliance_council.py` build_prompt + `_normalize_result` :1530 + `_apply_consensus` :1962-1971 | **⛔ HALTED / FALSIFIED 2026-05-30** — spec §2b falsified by pre-check P0; FP is calibration not aggregation; re-routed to §4 (P1) as a presence-check question; spec banner-corrected |
| §4 | SQLite data layer + SME-Question Ontology (the product's IP) **+ the first SME presence-check question (the re-routed scribe_v1 MED FP, S-BS-7)** | the 3 ontology bindings (prompt-shaping / per-question KB retrieval / reporting dimension); 4 Mongo collections -> SQLite slim path | **READY** (unblocked: the FP fix now lives here, not in aggregation) |
| §5 | Local vector + per-question retrieval (replace the `retrieval.py` stub) | `retrieval.py` stub (empty matches); numpy brute-force over `lithrim-rag-via-etlp` receipts | queued, depends on §4; carries the S-BS-7 presence-check tool |
| §6 | Salvage the remaining clusters, analyze flow, audio, eval/promote/compare, reporting | ~28-module analyze flow (no Celery); eval/promote/compare loop; reporting/dimensions off the ontology; audio sub-track | queued, depends on §5 |
| §7 | Consolidated Clojure server (uberjar) + KB ingest -> local vector | etlp-mapper copilot/engine.clj; etlp-base protocols; lithrim-rag-via-etlp ingest; NEW local-vector destination | queued, depends on §6 |
| §8 | v1-vs-v2 council fidelity for full paper reproduction | M1+R3(d) use v1 (3x gpt-4.1); prod default is v2 cross-provider; folds in the v2-over-fire HYPOTHESIS | deferred, depends on §3 |
| §9 | Desktop shell + calibration journey UI + demo voice agent | Tauri sidecars; TS+Tailwind+shadcn journey UI (lithrim-ui behavior-reference only); generative authoring agent; demo voice agent | deferred, depends on §7 |
| §10 | Packaging/installer + licensed-production separability | uberjar + JRE + PyInstaller bundle; same services as container images on ACA/ECS; state = swappable boundary | deferred, depends on §9 |

---

## Current cycle

| Cycle | Title | Status | Blocked by |
|---|---|---|---|
| **M1** | M1 spine, salvaged council + sync `/v1/pipeline/evaluate` orchestrator run fully in-process under `LocalPipelineBackend` (no Mongo/Pinecone/Celery/etlp-mapper), semantic-only stage, BYOK Azure gpt-4.1, council v1 (3 judges policy/risk/behavior). ALSO landed (no-regrets enabler, SPEC §6): persisted per-judge reasoning surface (`JudgeOutput.reason` in `backends/base.py:32`, captured in `local_pipeline.py:118`, persisted by `eval_runner.py:62` as `per_judge[*].reason` + `findings_rich`) so calibration misses are diagnosable offline from the standard NDJSON row WITHOUT re-issuing paid council calls. This is the paper acceptance test on the product path. **DONE, closed 2026-05-30.** CONFIRMED result: `out/scribe_v1.local.ndjson` row `bench_scribe_v1_inject_condition_1bd0f10dc7b5` got=reject exp=reject (1/1 match); `FABRICATED_HISTORY` correctly caught; `MEDICATION_NOT_IN_TRANSCRIPT` emitted as a CONFIRMED false positive (med `zidovudine 300 MG` is verbatim in the transcript, never mutated, only injected defect is the diabetes PMH), the calibration target that motivates the NEXT cycle. Tests: `pytest -q` => 82 passed (debuglithrim/py3.10); persistence-capture test `tests/test_lithrim_pipeline_confidence_capture.py` => 2 passed. NOTE: only the council/orchestrator slice is vendored; grounding is empty (retrieval stubbed at `lithrim_bench/runtime/pipeline/retrieval.py`); `eval_runner` writes NDJSON to `out/`, no data layer yet. Artifacts: `out/scribe_v1.local.ndjson` (baseline), `out/scribe_v1.local.guardfix.ndjson` (reasoning-captured re-run). Handoff: `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md`. | **done** | none |
| **COUNCIL-EVIDENCE-GATE** | **⛔ HALTED / BLOCKED-AND-RE-SCOPE — closed 2026-05-30, 0 commits, no code changed.** Pre-check P0 ($0.10, `--limit 1`, deterministic) falsified the specified R3(d) fix BEFORE any code or the ~$5 sweep: the `MEDICATION_NOT_IN_TRANSCRIPT` FP is findings-first + fully-evidenced + semantically-validated (overlap 1.000), symmetric with the true defect on every aggregation-readable signal — the judge cites the transcript line that *contains* zidovudine as proof of its *absence*. SPEC §2b "legacy span-less bypass" is FALSIFIED (the persisted `spans=[]` was an NDJSON projection artifact; `:1962-1971` never executes — judges are findings-first → `:1907`). All 3 authorized levers (`:1530` / `:1962-1971` / evidence-aware `:2170`) inert; OMISSION-code shortcut ruled out. Root cause = judge calibration; re-routed to §4/§5 (P1/P1-VEC) as the first SME presence-check question (S-BS-7). Falsification record: `docs/research/REPORT_r3d_precheck_falsification_2026-05-30.md`; raw evidence (tracked): `docs/research/r3d_precheck_P0_raw_evidence_2026-05-30.ndjson`; session log: `.devloop/sessions/session-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.json`; critique (HARD-GATE fresh-critic waived, no-code halt + monitor re-verified raw evidence): `.devloop/sessions/critique-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.md`. ORIGINAL CYCLE INTENT (retained for trace): Council-evidence-gate fix (R3(d)), close the legacy `violations_found` per-flag bypass in consensus. **THE FIRST EXECUTABLE CYCLE AFTER M1.** Spec: `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md`. Make the evidenced `findings[]` the AUTHORITATIVE violation set and retire the legacy path's ability to introduce un-evidenced companion codes. Two coordinated changes: (1) OUTPUT CONTRACT (prompt, `build_prompt`): require findings-first, every flag MUST be a `findings[]` entry with its own `evidence_spans`; state bare `violations_found` is DERIVED, not authoritative (removes the build_prompt ~:742/:749 dual-shape ambiguity that lets a judge return a padded flat list; the evidence gate already exists at :685). (2) AGGREGATION (`_normalize_result` :1530 + `_apply_consensus` :1962-1971): always reconcile `violations_found = [c for c in violations_found if c in evidenced_codes]` (new var `evidenced_codes`, CONFIRMED absent from the tree) rather than the current `if findings and not violations_found:` empty-only guard; a legacy-only code with no per-code evidence never drives a BLOCK vote (mirrors 2026-05-28 Finding-2: structured-detail asymmetry must not drive BLOCK). Fix lives in consensus aggregation, so it is judge-agnostic and composes with R3(a). Diagnose-before-edit gate applies (verbatim evidence + CONFIRMED/INFERRED/HYPOTHESIS tags). NOTE: prompt-only fix already tested-and-rejected (a behavior-judge carryforward guard was added 2026-05-30, the FP persisted because the judge already knows the med is present, `behavior_judge.reason` 'zidovudine were mentioned', guard reverted, CONFIRMED absent). Driver: `.devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md` (stub, run `/devloop-expand-driver bench-salvage COUNCIL-EVIDENCE-GATE`). Validation folds into the deferred pack-level `scribe_v1` sweep (~90 gpt-4.1 calls for the full 30-case run; the per-(case,judge) seed `sha256(case_id\|judge_role)` at `compliance_council.py:63` is independent of prompt/consensus code, so the A/B is clean for free). | **⛔ halted / re-scoped** | M1 (closed) |
| **P1** | Phase 1, SQLite data layer + SME-Question Ontology. Stand up the SQLite document-store and the SME-Question Ontology (the product's IP, today latent + hardcoded in three disconnected places). Wire the 3 bindings: prompt-shaping, per-question KB retrieval policy, reporting dimension. Data-drive the hardcoded council constants + reliability dims off the ontology. 4 Mongo collections -> SQLite on the slim path only (conversation_item, conversation_session, call_kpi, compliance_report; ~14 writes), scribe-first. PRIMARY OPEN DECISION: SQLite data layer = Mongo-shaped DOCUMENT SHIM vs RELATIONAL TABLES (recommended: document shim; gates the vendoring shape of the ~28 analyze-flow modules in P-CLUSTERS). **NOW CARRIES the re-routed scribe_v1 MED FP (S-BS-7) as its first concrete SME presence-check question** — "is `<med>` actually in the transcript?" before `MEDICATION_NOT_IN_TRANSCRIPT` fires. UNBLOCKED 2026-05-30: COUNCIL-EVIDENCE-GATE halted, and the FP fix is calibration that lives HERE, not in aggregation, so the "build on corrected aggregation" gate no longer applies. | **ready** | none (COUNCIL-EVIDENCE-GATE closed; S-BS-4 doc-shim-vs-relational is a decision made *during* P1, not a blocker) |
| **P1-VEC** | Local vector + per-question retrieval (replace the `retrieval.py` stub which returns empty matches). Spike = numpy brute-force over the existing `lithrim-rag-via-etlp` receipts (vectors precomputed, no new embedding cost). Closes the one consistent engine gap; sqlite-vec is blocked (neither python3 3.12.8 nor debuglithrim 3.10.15 has `enable_load_extension`). Wires the per-question retrieval policy binding from P1 to actual grounding so judges have evidence spans. Productized local-vector store choice DEFERRED. | **queued** | P1 |
| **P-CLUSTERS** | Salvage the remaining clusters, vendor the ~28-module analyze flow in-process (no Celery); the eval/promote/compare loop; data-drive reporting/dimensions off the P1 ontology; `analyzeWithAudio` is its own sub-track (local whisper loader + audio-bytes use-after-delete fix + `TRANSCRIPTION_PROVIDER=local\|cloud`). The internal eval runner already loops `ObservationWorkflow` per case (F2 unifier). Audio DEFERRED to M2 (~1.1GB whisper/pyannote). | **queued** | P1-VEC |
| **P2** | Phase 2, consolidated Clojure server (uberjar, ~60-80% exists) + KB ingest -> local vector. Jute mappings + AI copilot `POST /mappings/generate` + ETLP processor trigger (HTTP trigger is net-new; CLI exists) + KB->embeddings populate. NEW local-vector destination. Productized local-vector destination choice likely settled here (Clojure-side xerial sqlite-jdbc supports extensions). | **queued** | P-CLUSTERS |
| **P2-COUNCIL** | v1 vs v2 council fidelity for full paper reproduction. M1 + R3(d) use v1 (3x gpt-4.1); prod default is v2 (cross-provider: risk gpt-4.1 / policy mistral / faithfulness llama) and requires Azure Mistral/Llama deployments. Folds in the open HYPOTHESIS from COUNCIL-EVIDENCE-GATE: whether the 2026-05-28 v2 over-fires ride the same legacy span-less path (then R3(d) partially fixes v2 too) or carry real evidenced spans (then R3(a) is still needed). Defer the paid v2 sweep until the R3(d) v1 batch + the cheap v2-NDJSON legacy-path check decide whether v2 is even needed. | **deferred** | COUNCIL-EVIDENCE-GATE |
| **P3** | Phase 3, desktop shell (Tauri sidecars on localhost) + calibration journey UI (TS+Tailwind+shadcn; lithrim-ui is BEHAVIOR REFERENCE ONLY, do not code-salvage; conversation-detail reimplemented from `ConversationDetail.jsx`, NOT `CallKPISideSheet` which is dead UI) + the conversational/generative authoring agent + intentional-miscalibration seeding + demo scribe voice agent (live, must cite THIS run not a fixture) + observability importers. Acceptance: journey steps 5/10 run on the salvaged in-process analyze->council->eval path, reproducing paper findings. UI is a FRESH BUILD, not code-salvaged. | **deferred** | P2 |
| **P4** | Phase 4, packaging/installer (uberjar + JRE + PyInstaller; GraalVM native-image later) + licensed-production separability (same services as container images on ACA/ECS; UI is a pure HTTP/WSS client; state = swappable boundary). One artifact serves both desktop-download and licensed-cloud. The P1 document-shim is what makes the swap-back-to-Mongo-for-cloud separability clean. | **deferred** | P3 |

---

## Open seams

| ID | Title | Severity | Fix loc | Opened in | Status |
|---|---|---|---|---|---|
| S-BS-1 | **v1 judges emitted PURE-LEGACY output (flat `violations_found`, no `findings[]`) vs findings-first that got fully discarded by the spans-gate, unproven.** The council does not persist each judge's raw LLM JSON (only the mapped `JudgeVote` + consensus findings), so which shape each judge emitted is unproven. The CONFIRMED part is only that the flag surfaced via the judge-level-evidence legacy path (empty consensus spans + `(judges=N)` detail). SPEC §2b lines 86-90 / confidence ledger line 201. **Cheap test:** add a one-line debug log of the raw per-judge `findings[]`/`violations_found` on a single re-run (no extra paid calls, seed is deterministic). Non-empty `findings[]` with spans => findings-first discard; only `violations_found` => pure-legacy confirmed. | medium | one-line debug log in the council judge-output capture path; settled on the COUNCIL-EVIDENCE-GATE validating re-run | COUNCIL-EVIDENCE-GATE planning | **RESOLVED 2026-05-30** — NOT pure-legacy. Pre-check P0 raw capture: all 3 judges findings-first with per-code `evidence_spans`; the persisted `spans=[]` was an NDJSON projection artifact (`evidence_spans` 0× in NDJSON). See `REPORT_r3d_precheck_falsification_2026-05-30.md` §2a/§2b. |
| S-BS-2 | **v2 over-fires (`FABRICATED_HISTORY`/`FABRICATED_CONSENT`/`IMPLICIT_CONFIRMATION_OF_RECORD` on clean structured artifacts) in `REPORT_council_overfire_convergence_2026-05-28` may also ride the legacy un-evidenced bypass, rather than being genuinely evidenced findings.** If span-less, R3(d) partially fixes both; if they carry real spans, R3(a) pre-stage normalization is still required for the structured-detail-asymmetry class. SPEC §3 lines 110-113 / confidence ledger line 203. **Cheap test:** re-inspect the existing 2026-05-28 v2 NDJSON (`out/p1_canonical_n5_pilot.ndjson` C1 dumps, `out/fhir_mini_harness_results.ndjson` case-A dump) for per-finding `evidence_spans` on the over-fired codes, no new run; now that per-judge reason + consensus spans are persisted the check is purely offline. | medium | ~~offline re-inspection~~ → raw `R3D_DEBUG_RAW` capture under `COMPLIANCE_COUNCIL_VERSION=v2` (a P2-COUNCIL paid step) | COUNCIL-EVIDENCE-GATE planning | **STILL OPEN — offline check INCONCLUSIVE 2026-05-30.** The v2 NDJSON has the same projection-lossiness as scribe (`judge_votes[*].findings` flat code strings, 0 with spans; `semantic_findings` has no `evidence_spans` field). Cannot be answered offline; needs v2 raw capture under P2-COUNCIL. Free signal: same FP over-fires harder under v2, Mistral `policy_judge` the consistent over-firer (INFERRED: calibration class). |
| S-BS-3 | **BLAST RADIUS: making findings authoritative changes the flag set on EVERY case.** Must verify in the batch that no v1 judge emits legitimate flags ONLY via legacy `violations_found` with no `findings[]` (those would drop too, a true-defect regression). Needs the multi-case batch, not a 1-case check. SPEC §4 blast-radius lines 147-150. | **high** | the COUNCIL-EVIDENCE-GATE validating batch run (full 30-case `scribe_v1` or the stratified subset: 2 cleans + S1/S3 true-FABRICATED_HISTORY + the FP case) | COUNCIL-EVIDENCE-GATE planning | **SUPERSEDED / MOOT 2026-05-30** — the make-findings-authoritative fix whose blast radius this guarded does NOT land (R3(d) falsified, no aggregation change). No batch run needed for this cycle. If a future aggregation change is ever attempted, this blast-radius concern re-activates. |
| S-BS-4 | **SQLite data layer shape (Mongo-shaped DOCUMENT SHIM vs RELATIONAL TABLES) undecided.** Document shim = vendored code runs with import-path rewrites only, swaps back to Mongo for cloud; relational = queryable for reporting but rewrites every DB call. Recommended: document shim, add indexed columns for reporting later. This decision GATES the vendoring shape of the ~28 analyze-flow modules (P-CLUSTERS) and makes P4 licensed-production separability clean. Did NOT block M1/COUNCIL-EVIDENCE-GATE (eval_runner writes NDJSON). HANDOFF Key-decision 6. | medium | `lithrim_bench/runtime/services/` data layer; the SQLite document-store stood up in P1 | M1 close-out 2026-05-29 | open, must be made consciously in P1, do not let it ride |
| S-BS-5 | **Local vector store is a stub; sqlite-vec blocked.** `lithrim_bench/runtime/pipeline/retrieval.py` returns empty matches (council tolerates empty grounding); neither python3 (3.12.8) nor debuglithrim (3.10.15) has `enable_load_extension`. Productized local-vector (sqlite-vec via apsw / LanceDB / Qdrant / Clojure-side xerial sqlite-jdbc) deferred; the P1-VEC spike uses numpy brute-force over precomputed receipts. HANDOFF Key-decision 4. | medium | `lithrim_bench/runtime/pipeline/retrieval.py` (numpy spike in P1-VEC); productized choice likely settled in P2 (Clojure host) | M1 close-out 2026-05-29 | open (future work) |
| S-BS-6 | **v1-vs-v2 council as the canonical reproduction config undecided.** M1 + R3(d) use v1 (3x gpt-4.1, simplicity); prod default is v2 (cross-provider) needing Azure Mistral/Llama. Tied to S-BS-2: whether v2 over-fires share the legacy bypass. Cheap first step: re-check the existing 2026-05-28 v2 NDJSON for span-less companion flags BEFORE committing to any paid v2 run. HANDOFF open-questions. | medium | `compliance_council.py` v2 config; resolved in P2-COUNCIL after the cheap v2-NDJSON check | M1 close-out 2026-05-29 | open (documented limitation, v2 fidelity is a full-reproduction question, not an M1 blocker) |
| S-BS-7 | **`MEDICATION_NOT_IN_TRANSCRIPT` FP is a judge-calibration error, not an aggregation bypass.** Findings-first + fully-evidenced + semantically-validated (overlap 1.000) + symmetric with the true defect on every aggregation-readable signal (judge_count, evidence_judge_count, validation survival). No aggregation lever (`:1530` reconcile / `:1962-1971` legacy gate / evidence-aware `:2170`) separates it from `FABRICATED_HISTORY`; the OMISSION-code corpus-narrowing shortcut also fails (the self-refuting span — the transcript zidovudine line — is transcript text and survives transcript-only validation). The judge cites the line that *contains* the med as proof of its *absence*: a reasoning error wearing valid evidence. CONFIRMED. | **high** | **P1 / P1-VEC** — a per-question presence-check / KB-query tool ("is `<med>` in the transcript?") that lets a judge falsify the flag before emitting. This is P1's first concrete SME presence-check question. HYPOTHESIS that the tool closes the FP (REPORT §8) must be falsifier-tested on this case in P1-VEC, not assumed. | COUNCIL-EVIDENCE-GATE pre-check P0 2026-05-30 | open — re-routed to P1, the calibration fix |

---

## First move (next monitor action)

**COUNCIL-EVIDENCE-GATE HALTED / re-scoped 2026-05-30 (BLOCKED-AND-RE-SCOPE, 0 commits).** Pre-check P0 ($0.10, `--limit 1`, deterministic) falsified the specified R3(d) fix BEFORE any code or the ~$5 sweep. The `MEDICATION_NOT_IN_TRANSCRIPT` FP is findings-first + fully-evidenced + semantically-validated (overlap 1.000), symmetric with the true defect on every aggregation-readable signal — the judge cites the transcript line that *contains* zidovudine as proof of its *absence*. SPEC §2b "legacy span-less bypass" is FALSIFIED (banner-corrected); the persisted `spans=[]` was an NDJSON projection artifact; the legacy branch `:1962-1971` never executes (judges are findings-first → `:1907`). All 3 authorized levers inert; OMISSION-code shortcut ruled out. **Root cause = judge calibration, not consensus aggregation.** The whole gate exists to test fixes against trustworthy aggregation — the bench worked: for $0.10 it isolated the miss to judge reasoning and saved a wrong fix from landing. Falsification record: `docs/research/REPORT_r3d_precheck_falsification_2026-05-30.md`; raw evidence (tracked): `docs/research/r3d_precheck_P0_raw_evidence_2026-05-30.ndjson`. The persisted-judge-reasoning enabler (M1, SPEC §6) is what made the $0.10 offline root-cause possible.

1. **PRIORITY-0: P1 is now READY and is the next cycle.** The blocker premise inverted — the FP fix does NOT live in aggregation, it lives in P1 as the **first concrete SME presence-check question** (S-BS-7): a per-question presence-check / KB-query tool ("is `<med>` actually in the transcript?") that lets a judge falsify `MEDICATION_NOT_IN_TRANSCRIPT` before emitting. Run `/devloop-expand-driver bench-salvage P1` to author the driver. P1 wires the SME-Question Ontology's 3 bindings (prompt-shaping / per-question KB retrieval policy / reporting dimension) + the 4-collection SQLite slim path; this FP is the worked example that motivates the per-question retrieval binding.

2. **PRIORITY-0 (decide consciously IN P1, do not let it ride): S-BS-4 doc-shim vs relational SQLite shape.** Recommended document-shim. It is the linchpin gating the P-CLUSTERS vendoring shape and P4 separability.

3. **The S-BS-7 HYPOTHESIS must be falsifier-tested, not assumed.** "A presence-check tool closes this FP" is HYPOTHESIS (REPORT §8). The P1-VEC presence-check prototype run on this exact case (`scribe_v1` case 1, deterministic seed) is the falsifier. Keep the diagnose-before-edit discipline: this is where independent scrutiny re-enters (the HARD-GATE fresh-critic was waived only because COUNCIL-EVIDENCE-GATE landed no code).

4. **PRIORITY-2: S-BS-2 (v2 over-fire span class) needs v2 raw capture under P2-COUNCIL.** The offline check was INCONCLUSIVE (v2 NDJSON has the same projection-lossiness). When P2-COUNCIL runs, capture raw per-judge `findings[]` under `COMPLIANCE_COUNCIL_VERSION=v2` (the same `R3D_DEBUG_RAW` shape) to decide whether the v2 over-fires are evidenced (calibration, like v1) or span-less. Free signal so far points to calibration (Mistral `policy_judge` consistent over-firer).

5. **PRIORITY-3 (cheap-first late-phase rule): defer every paid v2/cross-provider sweep (P2-COUNCIL) until needed; keep P3 demos live (cite this run) but on-demand, never bulk.** P2-COUNCIL and P3 demos are the expensive surfaces.

> **Reframing the next monitor must hold (user, this cycle):** the bench-salvage stack is a *calibratable research instrument* — tweak judge prompts + give judges per-question KB-query tools — not just paper-reproduction. "Reproduce paper findings" is the acceptance test, not the goal. The COUNCIL-EVIDENCE-GATE falsification is the first proof the instrument works: it isolated a calibration miss the aggregation layer could not have fixed.

---

## References

- Persona: `.devloop/personas/MONITOR.md`
- Task pack: `.devloop/tasks/TASK_PACK_bench-salvage.json`
- Fix spec: `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md`
- M1 handoff: `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md`
- Convergence report (R1-R4 context): `docs/research/REPORT_council_overfire_convergence_2026-05-28.md`
- Paper outline (locked one-claim guardrail): `docs/PAPER_OUTLINE.md`
- Paired publication stream: `.devloop/state/STREAM_paper-1-copilot.md`
- Vendored council substrate: `lithrim_bench/runtime/council/compliance_council.py`
- In-process backend + runner: `lithrim_bench/backends/local_pipeline.py`, `scripts/run_local_scribe.py`
