{
  "session_id": "session-bench-salvage-phaseFAUTH-3-2026-06-20",
  "stream": "bench-salvage",
  "phase": "FAUTH-3 (+ FAUTH-3a)",
  "driver_bundle_id": "bench-salvage-phaseFAUTH-3-assist-prose-to-params-driver",
  "executor": "monitor (completed in-session) after the devloop-executor subagent was sandbox-blocked at gate 0 twice",
  "target_repos": ["lithrim-bench"],

  "execution_note": "The FAUTH-3 executor subagent was sandbox-blocked (Bash/python denied at the runner gate). The monitor completed the cycle in-session (FAUTH-2a precedent), keeping tests-first + MOAT-frozen + A-LIVE. Scoped via the fauth3-scope workflow (wf_99e8ae66-6e0, HIGH confidence; SNOMED-authoring-absent finding → split to FAUTH-3b). The cycle iterated through TWO honest A-LIVE findings before landing.",

  "commits": [
    {"sha": "177e691", "subject": "test(fauth): FAUTH-3 assist prose→params — spine guard + suggestion shape — red"},
    {"sha": "11cace0", "subject": "feat(bff): author_contract suggests presence_check params (emit-only) — the kb_context-cite assist (FAUTH-3)"},
    {"sha": "db3d88d", "subject": "feat(shell): ContractBuilder pre-fills from suggested_params, editable, Save unchanged (FAUTH-3)"},
    {"sha": "d66479e", "subject": "fix(bff): steer the FAUTH-3 assist to the deterministic source_hint path (correct keys)"},
    {"sha": "54679cb", "subject": "test(fauth): FAUTH-3a default-fill the presence_check skeleton for a named flag — red"},
    {"sha": "c21816a", "subject": "fix(bff): FAUTH-3a — author_contract default-fills the deterministic presence_check skeleton"},
    {"sha": "69b2204", "subject": "test(fauth): FAUTH-3a — empty-dict suggested_params must still default-fill — red"},
    {"sha": "1efd4b0", "subject": "fix(bff): FAUTH-3a — treat empty-dict suggested_params as no-suggestion (default-fill)"}
  ],

  "the_two_alive_findings": [
    "FAUTH-3a (the param-key hallucination, design R1 realized): given freedom to compose suggested_params, the live agent produced WRONG keys (contract_type/needle/haystack_path, not med_source/dosage_regex) — spine-safe but defeats zero-hand-JSON. Fix: author_contract DEFAULT-fills the deterministic presence_check skeleton for a named flag (correct keys by construction), independent of the agent passing source_hint.",
    "The empty-dict SDK trap (the inert-card root cause): the SDK-MCP layer passes {} (not None) for an omitted dict-typed schema param, so a real named-flag call arrived as suggested_params={}; the `suggested is None` check skipped the default-fill and contract_builder_part dropped the empty dict → inert card. Fix: normalize {} -> None. Reproduced deterministically (test_author_contract_empty_suggested_params_still_default_fills) before the fix."
  ],

  "tests": {
    "tests_first": true,
    "red_then_green": "Each behavior change was RED-first (177e691, 54679cb, 69b2204) then GREEN. The spine guard (A1) is non-vacuous both ways: the assist sequence writes nothing + ground() sees no contract until an EXPLICIT Save.",
    "green": "tests/bff/test_fauth3_assist.py + tests/bff/test_contract_builder_part.py = 13 passed; full suite 28 failed / 702 passed / 23 skipped (28==28 vs parent 457c4ae, 0 net new — the one delta is the documented test_pack_dist/test_byoc order-flap); ruff clean; vitest ContractBuilder 9 passed; npm build clean."
  },

  "acceptance": [
    {"criterion": "A1 spine guard (load-bearing, non-vacuous)", "verdict": "PASS"},
    {"criterion": "A2 kb_context still inert", "verdict": "PASS (unchanged; retrieval-only)"},
    {"criterion": "A3 author_contract emit-only with suggestions", "verdict": "PASS"},
    {"criterion": "A4 deterministic prose→params suggestion shape", "verdict": "PASS"},
    {"criterion": "A5 UI pre-fill from suggested_params, no auto-save", "verdict": "PASS"},
    {"criterion": "A6 end-to-end (Save→ground pickup)", "verdict": "PASS (Save path proven in the FAUTH-2 A-LIVE; not re-mutated this cycle)"},
    {"criterion": "A7 A-SAFE (no PAID_KEY; tool count 20)", "verdict": "PASS"},
    {"criterion": "A8 frozen + green bar", "verdict": "PASS (MOAT + FAUTH-2 gate + kb_context retrieval 0-diff)"}
  ],

  "a_live": "PROVEN LIVE on clinverdict_clean (healthcare), after a BFF restart (the dev --reload missed the final commit; watchfiles dedupes). Prose → the agent cited the org policy via kb_context → author_contract surfaced the ContractBuilder PRE-FILLED with the CORRECT deterministic presence_check keys (med_source=transcript.text, dosage_regex=\\b\\d+(?:\\.\\d+)?\\s*(?:%|x)\\b, token_min_len=4, noise_tokens). Spine narration verbatim: 'Your Save is the only write … this floor never decides a verdict on its own; it gives the faithfulness judge a deterministic oracle to withstand.' Zero hand-JSON. The earlier FAUTH-3 attempts (wrong keys / inert) drove the two fixes above.",

  "moat": "0-diff vs pre-FAUTH-3 457c4ae: harness/grounding.py (incl. PresenceCheck/ground/accessors), runtime/council/{compliance_council,signals,withstands}.py, judge_metric, apps/bff/app.py (the FAUTH-2 gate), lithrim_bench/verification/tools.py (kb_context retrieval). No new SDK-MCP tool (author_contract extended; tool count 20). add_grounding_contract handler unchanged.",

  "seams_opened": [
    {"id": "S-BS-FAUTH3-1", "title": "the live agent doesn't reliably pass source_hint; mitigated by default-fill, but med_source defaults to a generic path the human must confirm for non-transcript checks", "severity": "low", "fix": "stronger source-path inference or a small per-flag default map (future)"},
    {"id": "S-BS-FAUTH3-2", "title": "dev --reload (watchfiles) missed the final tools.py commit; a touch won't force it — needs devstack restart bff", "severity": "low", "fix": "environment; not repo code"}
  ],
  "seams_deferred": [
    {"id": "FAUTH-3b", "title": "SNOMED authoring-time terminology tool (ground-by-code only); no in-product Hermes authoring path exists today"}
  ],

  "verdict": "DONE (deterministic green + A-LIVE proven); HARD-GATE cold critic OWED (subagent runner sandbox-flapping)",
  "next": "Spawn the fresh cold critic on 457c4ae..1efd4b0 when the runner is healthy. Then FAUTH-3b (SNOMED authoring) or FAUTH-4 (NARR-FLOOR-1, the case-10 flip)."
}
