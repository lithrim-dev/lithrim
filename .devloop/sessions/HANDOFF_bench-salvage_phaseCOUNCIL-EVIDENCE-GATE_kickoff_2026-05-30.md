# HANDOFF: `bench-salvage` phase `M1` → phase `COUNCIL-EVIDENCE-GATE` kickoff

> Written by the monitor on cycle close (2026-05-30). This bridges the just-finished M1 spine + FP-diagnosis session to a FRESH `lithrim-bench` executor session that will run the first `.devloop/` cycle of the `bench-salvage` stream: `COUNCIL-EVIDENCE-GATE`.
> **Path:** `.devloop/sessions/HANDOFF_bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_kickoff_2026-05-30.md`
> **Repo:** `/Users/aregee/Workspace/github.com/lithrim-bench`
> Diagnose-before-edit discipline held throughout: every causal claim below is tagged CONFIRMED / INFERRED / HYPOTHESIS, and every `file:line` was re-grepped against current code on 2026-05-30.

## TL;DR

The `bench-salvage` stream salvages the cloud compliance council onto a local, in-process stack so the paper's eval-run findings reproduce on the shipped path. **M1 proved the spine** (salvaged council + sync `/v1/pipeline/evaluate` orchestrator runs fully in-process under `LocalPipelineBackend`, BYOK Azure gpt-4.1, council v1, semantic-only, no Mongo/Pinecone/Celery/etlp-mapper) and reproduced one `scribe_v1` case end to end: `compliance_verdict=reject == expected reject`, `FABRICATED_HISTORY` correctly caught. This session also **landed the persisted-judge-reasoning enabler** (a no-regrets change), **CONFIRMED a two-part false-positive root cause** for `MEDICATION_NOT_IN_TRANSCRIPT` on that same case, **wrote the R3(d) fix SPEC** (spec-only, no council code edited), and **tested-and-rejected a prompt-only fix** (a behavior-judge carryforward guard, added then reverted; FP persisted). Tests are green (82 passed). The next cycle, `COUNCIL-EVIDENCE-GATE`, applies the R3(d) aggregation fix the SPEC designed. Nothing here weakens the paper's locked one-claim: scribe is semantic-only by construction, so this is paper semantic-baseline characterization, never thesis validation (HL7 owns the structural-floor claim).

## What just landed

- **Closed phase:** `M1`, salvaged council runs in-process on the shipped path. NOT YET COMMITTED as a `.devloop/` close (this stream is bootstrapping; M1 work + the diagnosis session predate the stream's formal cycle ledger). The artifacts below are the proof-of-work; the `bench-salvage` STREAM doc + task pack + driver bundle are being stood up alongside this handoff. M1 proof (CONFIRMED): `out/scribe_v1.local.ndjson` row `bench_scribe_v1_inject_condition_1bd0f10dc7b5`, `compliance_verdict=reject == expected reject` (1/1 match).
  - **Persistence enabler landed** (the no-regrets change that made offline root-cause possible, CONFIRMED): `JudgeOutput.reason` at `lithrim_bench/backends/base.py:32` (docstring `:28-31`); captured `reason=jv.reason or ""` in `_map_result` at `lithrim_bench/backends/local_pipeline.py:118` (def `:103`, `per_judge` dict `:112-121`); persisted per-judge `{verdict,flags,confidence,reason}` by `_verdict_row` at `lithrim_bench/eval_runner.py:62` (def `:47`) plus `findings_rich` + `structural_findings_rich` at `:79-80`.
  - **FP root cause CONFIRMED (two parts):**
    - *Part 1, it is a true false positive.* `MEDICATION_NOT_IN_TRANSCRIPT` fired on case 1, but the med (`zidovudine 300 MG`) is verbatim in the transcript and was never mutated (the only injected defect is the diabetes PMH). The judge itself knows this: `out/scribe_v1.local.guardfix.ndjson` `behavior_judge.reason` reads verbatim *"The artifact contains fabricated medical history and conditions not discussed in the transcript. Only sprain and zidovudine were mentioned; all other history is invented."* A judge that names the med as mentioned, yet still emits the flag, means the flag is a padding/aggregation artifact, not a presence-reasoning error.
    - *Part 2, it rode the legacy judge-level-evidence consensus path.* The reasoning-captured re-run shows `findings_rich`: `MEDICATION_NOT_IN_TRANSCRIPT (judges=2)` with `spans=[]` and `FABRICATED_HISTORY (judges=3)` with `spans=[]`; `per_judge` now carries `reason` + `confidence=1.0`. The M1 baseline `out/scribe_v1.local.ndjson` had `per-judge reason absent/None` and `findings_rich=None`, confirming the enabler is what surfaced this. Empty consensus spans + the `(judges=N)` detail shape is the legacy `violations_found` path signature.
  - **SPEC written (spec-only, no council code edited, CONFIRMED):** `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` (§1-§8) carries the R3(d) fix design, the validation protocol, and the confidence ledger. Status line (file `:10-11`): *"spec only, no council code edited (per decision: spec the fix, defer the validating run). The persisted-reasoning enabler (§6) did land."*
  - **Prompt-only fix tested and REJECTED (CONFIRMED):** a behavior-judge carryforward guard ("clinical incongruity is not transcript absence") was added and re-run 2026-05-30; the FP persisted; the guard was reverted. Verified reverted: grep for `clinical incongruity` / `transcript absence` / `carryforward` in `compliance_council.py` returns no matches (guard text absent from current code). A prompt cannot close a gate that lives in aggregation code.
  - **Tests green:** `pytest -q` => 82 passed in 0.20s (`debuglithrim` / py3.10). The persistence-capture test `tests/test_lithrim_pipeline_confidence_capture.py` => 2 passed. NOTE: the SPEC and the prior 2026-05-29 handoff cite `38/38`, which was the suite count when the enabler landed; the current full suite is 82.
- **Critique verdict:** N/A for M1, no `.devloop/` critique was run (M1 + diagnosis predate the stream's formal cycle ledger). `COUNCIL-EVIDENCE-GATE` is a HARD GATE (it touches a council output-contract + aggregation behavior) and therefore REQUIRES a fresh-critic session + user confirmation before the one-way-door change lands; see "What's next".
- **Audit verdict:** N/A for M1 (no `.devloop/` mechanical audit run). The substitute proof-of-work is the SPEC's diagnose-before-edit evidence blocks and the green test suite.
- **Session log:** No `.devloop/` session-log JSON was produced for the diagnosis session. The narrative record is the prior handoff `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md` (M1 spine, lines 91-112 carry the reject==reject proof) and the SPEC. The first formal session log will be `.devloop/sessions/session-bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-2026-05-30.json`, written by the executor of the next cycle.

## What's next

- **Next phase:** `COUNCIL-EVIDENCE-GATE`, make the evidenced `findings[]` the AUTHORITATIVE violation set and retire the legacy `violations_found` path's ability to introduce un-evidenced companion codes (this is the new R3(d) from the convergence report). The fix the SPEC designed (CONFIRMED fix-site citations, all re-grepped 2026-05-30):
  - *(1) Output contract (prompt, `build_prompt`).* The prompt currently requests BOTH shapes, the evidence-gated `findings[]` block (`A finding WITHOUT evidence_spans is INVALID and will be discarded` at `compliance_council.py:685`) AND a bare `violations_found` list at `:742`, with the backward-compat reconciliation note at `:748-750` (*"the 'findings' array is the PRIMARY output... the 'violations_found' array should list the taxonomy_codes from your findings (for backward compatibility)"*). That dual-shape ambiguity is what lets a judge return a padded flat list. Fix: state every flag MUST be a `findings[]` entry with its own `evidence_spans`, and that bare `violations_found` is DERIVED, not authoritative. (Scribe prompt branch selector is the `elif agent_category == "scribe"` at `:645`; `build_prompt` def at `:517`.)
  - *(2) Aggregation.* In `_normalize_result` (def `compliance_council.py:1461`; `violations_found = raw.get("violations_found", [])` passthrough at `:1466`; spans-discard for findings-first at `:1518-1525`): change the derive-from-findings guard at `:1530` from the empty-only `if findings and not violations_found:` to ALWAYS reconcile, introduce `evidenced_codes = {f.taxonomy_code for f in validated_findings}` (the name `evidenced_codes` is CONFIRMED ABSENT in the tree, a clean new var) and set `violations_found = [c for c in violations_found if c in evidenced_codes]`. The return passes `violations_found` through ungated at `:1555`. Then in `_apply_consensus` (def `:1853`): the format selector `if raw_findings and isinstance(raw_findings, list):` at `:1907` routes to the per-finding-evidence branch; the LEGACY branch at `:1962-1971` (`violations = set(result.get("violations_found") or [])`, `has_evidence = bool(primary_ev) or bool(supporting_ctx)`, one judge-level flag broadcast to all violations with `violation_spans[v] = []`) is where the un-gated companion flag currently survives. A legacy-only code with no per-code evidence must NOT drive a BLOCK vote (mirrors 2026-05-28 Finding-2: structured-detail asymmetry must not drive a BLOCK vote); flag-only / non-decision-driving at most. The fix is judge-agnostic (lives in consensus), so it composes with R3(a) pre-stage normalization.
- **Driver bundle:** status `stub`, to be expanded into a paste-ready bundle. Bundle ID: `bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-consensus-evidence-gate-driver`.
- **Driver path:** `.devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md` (does not exist yet; expand the stub via `/devloop-expand-driver bench-salvage COUNCIL-EVIDENCE-GATE`, which will re-grep all the `:1461 / :1530 / :1907 / :1962-1971 / :685 / :742 / :748-750` citations against current code at authoring time per the citation-discipline rule).
- **Blocked by:** none for authoring the driver. The validating batch run is gated by user "go" (LLM-cost-conscious; ~90 gpt-4.1 calls), and the one-way-door code change is gated by a fresh-critic session + user confirmation (HARD GATE).
- **How to kick it off (per conventions.kickoff_mechanism):** the USER (not an automated runner) opens a FRESH Claude Code session in this repo and pastes the driver's embedded `## KICKOFF` block verbatim. The monitor only hands the paste-ready block to the user; it never auto-starts the cycle. The block follows the generic `prompts/KICKOFF_EXECUTOR.md` form: first line `You are the EXECUTOR for the .devloop/ cycle: bench-salvage phase COUNCIL-EVIDENCE-GATE (consensus-evidence-gate).`, then reading order (1 `.devloop/personas/EXECUTOR.md`, 2 the driver doc, 3 the driver's §1 pre-flight `file:line` list, N `.devloop/state/STREAM_bench-salvage.md`), then post a plan-review and write NO code until the user says "go". Ends with `Bundle ID: bench-salvage-phaseCOUNCIL-EVIDENCE-GATE-consensus-evidence-gate-driver` and `Driver doc: .devloop/prompts/bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_consensus_evidence_gate_driver.md`. Slash-command equivalent: `/devloop-kickoff bench-salvage COUNCIL-EVIDENCE-GATE` prints the paste-ready block; `/devloop-critique bench-salvage COUNCIL-EVIDENCE-GATE` triggers the HARD-GATE critic.

## Open seams for `bench-salvage`

| ID | Title | Severity | Fix loc | Opened in | Status |
|---|---|---|---|---|---|
| S-BS-1 | **Raw per-judge output shape unproven**: pure-legacy flat `violations_found` (no `findings[]`) vs findings-first that the spans-gate discarded at `:1518-1525`. The only CONFIRMED part is that the flag rode the judge-level-evidence legacy path (empty consensus spans + `(judges=N)` detail). | low | one-line raw-shape debug log on one deterministic re-run (SPEC §2b) | `COUNCIL-EVIDENCE-GATE` planning | open, cheap pre-check, gates the paid sweep |
| S-BS-2 | **v2 over-fires may share the legacy span-less bypass**: the 2026-05-28 `FABRICATED_HISTORY`/`FABRICATED_CONSENT`/`IMPLICIT_CONFIRMATION_OF_RECORD` over-fires on clean structured artifacts, not yet checked for per-finding spans. If span-less, R3(d) partially fixes v2; if evidenced, R3(a) pre-stage normalization is still needed. | medium | offline re-inspect `out/p1_canonical_n5_pilot.ndjson` + `out/fhir_mini_harness_results.ndjson` (SPEC §3) | `COUNCIL-EVIDENCE-GATE` planning | open, offline, no new calls |
| S-BS-3 | **Blast radius**: making `findings[]` authoritative changes the flag set on EVERY case. Must verify no v1 judge emits legitimate flags ONLY via legacy `violations_found` with no `findings[]` (those would drop too, a true-defect regression). | **high** | the `COUNCIL-EVIDENCE-GATE` validating batch (SPEC §4 lines 147-150) | `COUNCIL-EVIDENCE-GATE` planning | open, gates the cycle's acceptance |
| S-BS-4 | **SQLite data layer shape undecided** (Mongo-shaped doc-shim vs relational tables); `eval_runner` writes NDJSON to `out/` for now. Gates the ~28-module analyze-flow vendoring shape (P-CLUSTERS) + P4 separability. | medium | data layer, decided consciously in P1 | M1 close-out 2026-05-29 | open, decide in P1, does not block COUNCIL-EVIDENCE-GATE |
| S-BS-5 | **Local vector is a stub; sqlite-vec blocked**: `retrieval.py` returns empty matches (council tolerates empty grounding); neither python3 3.12.8 nor debuglithrim 3.10.15 has `enable_load_extension`. P1-VEC numpy spike over precomputed receipts. | medium | `lithrim_bench/runtime/pipeline/retrieval.py` (P1-VEC) | M1 close-out 2026-05-29 | open (future work) |
| S-BS-6 | **v1-vs-v2 council fidelity** for full paper reproduction; M1+R3(d) use v1 (3x gpt-4.1), prod default is v2 (cross-provider Mistral/Llama/gpt-4.1) needing Azure deployments. Tied to S-BS-2. | medium | council config (P2-COUNCIL) | M1 close-out 2026-05-29 | open (future work) |

## Load-bearing context the next monitor MUST know

1. **The fix lives in aggregation code, and a prompt cannot reach it, this is settled, not a hunch.** A behavior-judge carryforward guard was added and re-run on 2026-05-30; the FP persisted (CONFIRMED) because the judge already knows the med is present (`behavior_judge.reason`: *"zidovudine were mentioned"*), so the flag is a padding/aggregation artifact. The guard was reverted (verified absent from `compliance_council.py`). Do not relitigate prompt-only approaches for `COUNCIL-EVIDENCE-GATE`; the two coordinated changes (output contract + aggregation reconcile at `:1530` and `:1962-1971`) are both required, and the aggregation change is the load-bearing one.

2. **Blast radius: making `findings[]` authoritative changes the flag set on EVERY case, so this needs a multi-case batch, never a 1-case check.** It will correctly drop un-evidenced companion flags, but if any v1 judge emits legitimate flags ONLY via legacy `violations_found` with no `findings[]`, those drop too (SPEC §4, lines 147-150). The validation is therefore the deferred ~90 gpt-4.1-call full 30-case `scribe_v1` sweep, NOT a quick re-run. That one batch serves three purposes at once: (a) validates R3(d), case-1 `MEDICATION_NOT_IN_TRANSCRIPT` gone, `FABRICATED_HISTORY` retained, verdict stays reject, no true-defect regression on S1/S3; (b) produces the deferred pack-level numbers (verdict accuracy, flag precision/recall, false-block rate on cleans, the metric the FP most affects); (c) gives the paper its semantic-baseline characterization on scribe. The A/B is clean for free because the per-(case,judge) seed `sha256(case_id|judge_role)` (`compliance_council.py:63`, used at `:2539`) is independent of the prompt/consensus code R3(d) touches.

3. **User preferences that persist (never override):** (a) LLM-cost-conscious, do offline analysis before any paid run, inspect 1 before batching N, run once and inspect before rerunning; the two near-zero-cost pre-checks that gate the paid sweep are: log raw per-judge `findings[]`/`violations_found` on one deterministic re-run to confirm the pure-legacy HYPOTHESIS (S-BS-1), and re-inspect the EXISTING 2026-05-28 v2 NDJSON for span-less companion flags (no new calls, S-BS-2). (b) Don't autostart services, the user runs them; this cycle is in-process BYOK so no service start is needed, but confirm before any backend dependency. (c) Don't auto-commit / no push/tag/publish without explicit owner approval; per-repo atomic commits with conventional-commit prefixes (`fix(council)` for the aggregation change, `docs(research)` for spec edits), body references the bundle id + session log. (d) Reproducibility bar = verdict + flag-set + confidence, NOT byte-identical reasoning. (e) No em dashes in drafted content.

## How to resume

1. Read `.devloop/personas/MONITOR.md` (monitor role: roadmap/specs/drivers/state + discipline/audit/handoff only; never implements).
2. Read `.devloop/state/STREAM_bench-salvage.md` (current stream state + open seams; being stood up alongside this handoff).
3. Read this handoff (`.devloop/sessions/HANDOFF_bench-salvage_phaseCOUNCIL-EVIDENCE-GATE_kickoff_2026-05-30.md`).
4. Read the most recent narrative record: `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md` (M1 spine, lines 91-112) and the fix SPEC `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` (§1-§8).
5. Run `git log --oneline -10` to see the current commit head before authoring/expanding the driver (re-grep every cited `file:line` at authoring time per the citation-discipline rule).
6. Wait for user input. The monitor hands the paste-ready executor kickoff block to the user; it does NOT auto-start the `COUNCIL-EVIDENCE-GATE` cycle.

## References

- **Stream state:** `.devloop/state/STREAM_bench-salvage.md` (current cycle table + open-seams table; `bench-salvage` entry in `.devloop/state/streams.json`)
- **Prior cycle session log (narrative):** `docs/HANDOFF_BENCH_SALVAGE_2026-05-29.md` (M1 spine proven; reject==reject proof at lines 91-112); supporting findings `docs/SALVAGE_FINDINGS_2026-05-29.md`
- **Prior cycle critique:** none for M1 (no `.devloop/` critique run; M1 + diagnosis predate the formal cycle ledger). `COUNCIL-EVIDENCE-GATE` requires a fresh-critic session (HARD GATE).
- **Fix SPEC (the next cycle's authority doc):** `docs/research/SPEC_consensus_evidence_gate_bypass_2026-05-30.md` (R3(d) design, validation protocol, confidence ledger; status line `:10-11` "spec only, no council code edited")
- **Overfire report (R1-R4 context; R3(d) is additive):** `docs/research/REPORT_council_overfire_convergence_2026-05-28.md` (v2 cross-provider substantive over-fire; this stream's R3(d) is the code-level companion-flag bypass on v1)
- **Run artifacts:** `out/scribe_v1.local.ndjson` (M1 baseline, `per-judge reason absent/None`, `findings_rich=None`); `out/scribe_v1.local.guardfix.ndjson` (reasoning-captured re-run, `behavior_judge.reason` populated, `findings_rich` spans `[]`); v2 NDJSON to re-inspect offline: `out/p1_canonical_n5_pilot.ndjson`, `out/fhir_mini_harness_results.ndjson`
- **Patched / load-bearing files:** `lithrim_bench/backends/base.py:32` (`JudgeOutput.reason`), `lithrim_bench/backends/local_pipeline.py:118` (`_map_result` captures `reason`), `lithrim_bench/eval_runner.py:62` + `:79-80` (`_verdict_row` persists per-judge reason + `findings_rich`); the `COUNCIL-EVIDENCE-GATE` fix targets `lithrim_bench/runtime/council/compliance_council.py:1530` + `:1962-1971` (aggregation) and `:685` / `:742` / `:748-750` (prompt output contract); deterministic seed at `:63` / `:2539`
- **Tests:** `tests/test_lithrim_pipeline_confidence_capture.py` (persistence-capture, 2 passed); full suite 82 passed
- **Roadmap context:** the deferred phases (`P1` SQLite data layer + SME-Question Ontology → `P1-VEC` local vector + per-question retrieval → `P-CLUSTERS` analyze/audio/eval-promote-compare/reporting → `P2` Clojure uberjar + KB ingest → `P2-COUNCIL` v1-vs-v2 fidelity → `P3` desktop shell + calibration journey UI → `P4` packaging + licensed-production separability) live in `.devloop/state/STREAM_bench-salvage.md`. Ordering principle: offline-first, batch-then-validate; `COUNCIL-EVIDENCE-GATE` lands before `P1` so the data-driven flag/dimension bindings are built on corrected aggregation, not a known-FP baseline. Paper guardrail: none of this weakens `PAPER_OUTLINE`'s locked one-claim (scribe is semantic-only = semantic-baseline characterization; HL7 owns the structural-floor demonstration); R3(d) is additive to the convergence report's R1-R4 and sharpens the false-block-on-cleans measurement.
