# Inline critique — `bench-salvage` LAUNCH-PREP (v1 standalone + release prep)

> **Mode:** ROUTINE-with-care → monitor 7-item audit + inline 4-question critique (NOT HARD-GATE — D1 does not touch the A-SAFE deny-hook/allowlist, as scoped at plan-review; no fresh-critic).
> **Date:** 2026-06-09 · **Verdict: CLEAN → PROCEED-WITH-CAVEATS** (the one caveat = the standalone A-LIVE is an owed user-run; it is also the RELEASE pre-push gate). All offline acceptance green; the resolver is byte-verified against the approved design.

## What landed
The OSS core is now self-contained. **D1** (`apps/bff/app.py`): a net-new `_resolve_run_backend(req) -> (live_http, in_process)` reads `LITHRIM_COUNCIL_BACKEND` (default `in_process`) and the `run_eval_endpoint` swaps `live=req.live, in_process=req.in_process` for the resolved pair — so a human's "Run live" defaults to the bundled in-process v2 council (BYO Azure/Claude key), **no `:8002`/lithrim-backend, no Mongo**; `http` is the opt-in for a backend deployment. Shell touch is comment/tooltip-only (`app.jsx` "Run live" tooltip + `bff.js` comment go backend-agnostic). **D2** (`docs/QUICKSTART.md`): a clean-clone product walkthrough (BYO-key, `$0` replay default, the standalone framing). **D4** (`docs/RELEASE_v1.md`): OSS-core scope + the trust-wedge framing (BYO-key/airgapped/no-us-hosted) + an owner-executed push checklist + draft notes. **D3** (`RELEASE_v1.md` append): the 8-seam triage table — no HARD launch-blocker.

## Monitor 7-item audit (independent)
1. **Commits** — `84c3c82` (bff+shell+test) + `4dbc417` (QUICKSTART/RELEASE/README pointers) + `ec526f4` (seam-triage) on `bench-salvage/ws6c-dspy`; 3 contiguous functional commits + the session log `9b6d3c1`. A concurrent session's `0793a8c` (CHATBIND-2 A-LIVE capsule) interleaved ABOVE the stack — pathspec-only kept it out of every executor commit. ✓
2. **Files** — 8, all planned: `apps/bff/app.py` (+30), `apps/shell/src/{app.jsx,bff.js}` (2/5 lines), `tests/test_launch_standalone.py` (new), `docs/{QUICKSTART,RELEASE_v1}.md`, `README.md`+`apps/bff/README.md` pointers. No scenarios/FHIR/IMPORT-1/provider-picker/plugin file; **no `runtime/observation/` touch**. ✓
3. **Tests** — monitor re-ran `tests/test_launch_standalone.py` on the REAL `…/3.10.15/envs/debuglithrim/bin/python` = **5 passed**. Executor full suite **522 passed / 2 failed / 3 skipped**; the 2 = the pre-existing S-BS-96 order-non-deterministic observation-isolation pair (pass 13/13 standalone; structurally independent — the diff touches no observation file), the 3 = credential-gated council/optimizer (BYO key). ✓ (monitor spot-verified the new test + the S-BS-96 independence rather than re-running all 522 — the blast radius is one pure function + docs.)
4. **Scope / A-SAFE** — `git diff 84c3c82~1 ec526f4 -- apps/bff/agent/loop.py` is **EMPTY** (the deny-hook/allowlist/`_build_options` byte-identical). The only `app.py` change is the resolver function + the 3-line endpoint wiring; the chat-gate surface is untouched. ✓
5. **Prefs** — pathspec-only commits; A-LIVE deferred to the user-run (no autostart, no paid run fired by the executor); push owner-gated (PREPARED only). ✓
6. **Session log** — `session-bench-salvage-LAUNCH-PREP-2026-06-09.json` present + well-formed. ✓
7. **Deviations** — both APPROVED-AT-PLAN (D1 achieved purely in the BFF → shell needs no logic, only copy; one new test file). The CITATION-DRIFT (`PYENV_VERSION=debuglithrim` → 3.12.8) is an environment note, not introduced code. ✓

## Inline 4-question critique
- **Q1 — does it meet the driver's D1–D4 / acceptance?** YES. D1 standalone topology (A1 resolver test green), D2 QUICKSTART, D3 green-bar+seam-triage, D4 RELEASE checklist all landed. A-LIVE is the single owed item (a user-run paid smoke; also the RELEASE pre-push checkbox).
- **Q2 — is the scope clean?** YES. 8 files, all planned; packaging-only; no parked-roadmap bleed; the foreign `root.jsx`/untracked files stayed out of every commit.
- **Q3 — are the load-bearing claims evidenced (diagnose-before-edit)?** YES — and monitor-reverified. **CONFIRMED** in_process self-containment (re-ran the `pymongo|motor|MongoClient` + `lithrim_backend|from app.` greps over the in_process chain → empty). **CONFIRMED** A-SAFE byte-identity (the load-bearing fact is now a behavioral invariant on the resolver, not "untouched endpoint" — see below — and the test pins it). **CONFIRMED** the resolver matches the approved design: `os.environ.get(...)` is gated INSIDE `if req.live:`, so the chat's `live=False, in_process=False` never reaches the env read.
- **Q4 — what's deferred / new seams?** The standalone A-LIVE (owed user-run). New seam **S-BS-109** (low — the eval-pack *live* path still needs `:8002`; SHOULD-#1 residual, now triaged in RELEASE). Carried: S-BS-96/98/101–108.

## The A-SAFE proof — corrected framing (the plan-review MUST, folded)
The plan-review framed safety as "the chat loop calls `_run_eval_replay` and is untouched → byte-identical." Half-precise: `loop.py` IS byte-untouched (✓), but `_run_eval_replay` calls `run_eval_endpoint` **directly** (`apps/bff/agent/loop.py:1189`), so the chat's `$0` replay now **flows through** the new `_resolve_run_backend`. The real A-SAFE guarantee is therefore a behavioral invariant: `_resolve_run_backend(live=False, in_process=False) == (False, False)` = replay, **regardless of `LITHRIM_COUNCIL_BACKEND`**. The executor folded exactly the required test — `test_replay_stays_replay` asserts the chat's default inputs resolve to replay under **both** env values (`http` AND the `in_process` default) **and** unset (`tests/test_launch_standalone.py:66-73`), with a documented non-vacuity mutation. This forecloses the only paid-leak this cycle could introduce (an env read before the `req.live` gate would silently fire paid in-process runs from chat). **Verified correct as committed.**

## Finding — S-BS-109 (low) the second live path
`/v1/eval-pack/run` with `live=true` still routes to `:8002` — `build_pack` has no `in_process` param (a code-acknowledged follow-on in the endpoint docstring). Single-agent "Run live" is standalone; batched live is not. **Not shell-exposed** (no live-pack button; the chat's pack op is replay-only), and the offline `lithrim-bench-pack` gate is `$0`/standalone — so practical exposure is a direct CLI/SDK POST. Triaged post-v1 + noted in RELEASE so "standalone" does not overclaim. Until the batched in_process follow-on, a standalone batched live run needs `LITHRIM_COUNCIL_BACKEND=http`.

## Two executor notes (acknowledged)
- **CITATION-DRIFT** — `PYENV_VERSION=debuglithrim python` resolved to pyenv **3.12.8** (no openai/dspy → council tests spuriously skip). The real interpreter is `~/.pyenv/versions/3.10.15/envs/debuglithrim/bin/python`. Monitor hit + used the explicit path. Memory `council-runtime-test-env` updated with the explicit-path caveat; kickoff guidance should cite the full path, not the bare env name.
- **Concurrent-session commit** `0793a8c` — the documented dirty-index hazard; handled correctly (pathspec-only). The 3 functional commits are intact/contiguous.

## Caveats at close
1. **The standalone A-LIVE is owed** (user-run, paid, BYO key) — on `:5180` with lithrim-backend STOPPED + `LITHRIM_COUNCIL_BACKEND` unset, "Run live" completes an in-process council run with a real verdict + audit record. This is the load-bearing live proof that v1 runs standalone, and the RELEASE pre-push gate. **Honest-Δ:** the acceptance is "a real grounded verdict completed backend-stopped + an audit blob persisted," NOT "verdict == :8002" — in_process is ≥-strict (WS-6c-AGENTIC); document any delta, do not assert parity.
2. S-BS-109 (low) + carried S-BS-96/98/101–108 — none HARD launch-blocking (triaged in RELEASE).
3. Push/PR/tag is owner-gated — PREPARED (the checklist), not done.
