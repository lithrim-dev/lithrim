{
  "session_id": "session-bench-salvage-CONFIG-PERSIST-1-2026-06-25",
  "stream": "bench-salvage",
  "phase": "CONFIG-PERSIST-1",
  "driver_bundle_id": "bench-salvage-CONFIG-PERSIST-1-driver",
  "started_at": "2026-06-25T13:40:00Z",
  "ended_at": "2026-06-25T14:24:55Z",
  "executor": "claude-code",
  "target_repos": ["lithrim-bench"],

  "plan_review": {
    "posted_at": "2026-06-25T13:40:00Z",
    "user_approved_at": "2026-06-25T13:40:00Z",
    "deviations": [
      {
        "kind": "APPROVED-AT-PLAN",
        "summary": "Driver is the standing approval (autonomous mode); executed as specified.",
        "evidence": ".devloop/prompts/DRIVER_config_persist_1.md",
        "decided_at": "2026-06-25T13:40:00Z"
      },
      {
        "kind": "APPROVED-MID-CYCLE",
        "summary": "Round-trip MUTATION reworked from a repo-root read (non-hermetic; the dev worktree carries a real .provider_env) to a fresh empty-dir read — same bug-direction proof, no repo-root dependency.",
        "evidence": "tests/bff/test_provider_env_persistence.py::test_persistence_round_trip_volume_kept_container_gone (wiped_layer empty dir)",
        "decided_at": "2026-06-25T14:05:00Z"
      }
    ]
  },

  "commits": [
    {
      "hash": "675ca6cb",
      "repo": "lithrim-bench",
      "title": "test(bff): provider-env persists across down/up (LITHRIM_PROVIDER_ENV_DIR) — red",
      "summary": "RED acceptance tests for the resolver + persistence round-trip + app/loop agreement + boot restore + byte-compat."
    },
    {
      "hash": "d9669e98",
      "repo": "lithrim-bench",
      "title": "fix(bff): persist UI-configured provider keys + bindings across down/up — green",
      "summary": "Relocate .provider_env + .models_registry.json to LITHRIM_PROVIDER_ENV_DIR (default REPO_ROOT; /app/out in compose); resolvers at call time; loop.py reads the env var directly; boot restore confirmed; compose + .env.example documented."
    }
  ],

  "tests": {
    "tests_first": true,
    "tests_first_note": "RED committed first (675ca6c) before any app.py/loop.py edit.",
    "red_command": "PYENV_VERSION=debuglithrim python -m pytest -q tests/bff/test_provider_env_persistence.py",
    "red_evidence": "9 failed, 3 warnings. AttributeError: module 'app' has no attribute '_provider_env_dir'. AssertionError: the write did not land in <tmp>/.provider_env (the named volume) — _persist_and_reload_provider wrote to REPO_ROOT, not the relocated dir (the live bug). Behavior genuinely absent (no resolver, no relocation), not import/typo.",
    "green_command": "PYENV_VERSION=debuglithrim python -m pytest -q  (full suite) ; ruff check .",
    "green_evidence": "871 passed, 158 skipped, 5 warnings in 15.88s (0 failed). ruff: All checks passed! Round-trip in isolation: 1 passed. Byte-compat probe: unset → _provider_env_path()==_PROVIDER_ENV_PATH==REPO_ROOT/.provider_env; env=/app/out → /app/out/.provider_env."
  },

  "acceptance": [
    {
      "criterion": "Resolver honors LITHRIM_PROVIDER_ENV_DIR, falls back to REPO_ROOT unset, non-vacuous",
      "test": "tests/bff/test_provider_env_persistence.py::test_provider_env_dir_honors_the_env / _falls_back_to_repo_root_when_unset / _non_vacuous",
      "verdict": "PASS",
      "evidence": "3 passed; _provider_env_dir/_provider_env_path/_models_registry_path resolve via the env at call time."
    },
    {
      "criterion": "Persistence round-trip (the regression for the live bug): write to <tmp>/.provider_env, fresh read returns the vars (container-gone-volume-kept); MUTATION → empty",
      "test": "tests/bff/test_provider_env_persistence.py::test_persistence_round_trip_volume_kept_container_gone",
      "verdict": "PASS",
      "evidence": "1 passed; write lands in <tmp>/.provider_env, fresh _parse_env_file returns OPENAI_API_KEY + the bindings; the wiped empty-dir read returns {}."
    },
    {
      "criterion": "App + loop agree: loop.py's .provider_env fallback resolves to the same dir; a LITHRIM_CHAT_* bind round-trips to _chat_provider_config() reading the relocated file",
      "test": "tests/bff/test_provider_env_persistence.py::test_app_and_loop_agree_on_the_relocated_provider_env / _loop_keeps_reading_dev_env_files_at_repo_root",
      "verdict": "PASS",
      "evidence": "2 passed; cfg{provider,model,api_key} read from the relocated file with os.environ cleared; .env/.live_env stay repo-root, _relocatable_provider_env_dir follows the env var."
    },
    {
      "criterion": "Boot restore: _load_provider_env() repopulates os.environ from the persisted file (up-restores-config)",
      "test": "tests/bff/test_provider_env_persistence.py::test_boot_restore_repopulates_os_environ_from_persisted_file",
      "verdict": "PASS",
      "evidence": "1 passed; OPENAI_API_KEY + LITHRIM_LLM_PROVIDER_RISK + LITHRIM_CHAT_PROVIDER reappear in os.environ after the startup boot path."
    },
    {
      "criterion": "Back-compat (unset → byte-identical) + secret hygiene unchanged (relocated file write-only, no key in a response)",
      "test": "tests/bff/test_provider_env_persistence.py::test_back_compat_unset_is_byte_identical_to_today / _relocated_file_is_still_write_only_no_key_in_a_response",
      "verdict": "PASS",
      "evidence": "2 passed; unset resolvers == REPO_ROOT constants; POST /v1/provider/config writes the key to <tmp>/.provider_env, NEVER in resp.text."
    }
  ],

  "diagnostic_stats": {
    "full_suite": "871 passed / 158 skipped / 0 failed",
    "provider_registry_suites_after_refactor": "71 passed (test_provider_config + model_registry + roles_bind + provider_center_types + azure_api_version + assistant_plane + the new persistence file)",
    "call_sites_rerouted_in_app_py": "all _PROVIDER_ENV_PATH / _PROVIDER_STATUS_PATH / _MODELS_REGISTRY_PATH uses now route through the call-time resolvers; the constants survive only as the unset-default anchors existing tests monkeypatch",
    "files_touched": "apps/bff/app.py, apps/bff/agent/loop.py, docker-compose.yml, .env.example, tests/bff/test_provider_env_persistence.py (no apps/shell, no compliance_council, no app.jsx)"
  },

  "seams_opened": [
    {
      "id": "S-BENCH-SALVAGE-CONFIG-PERSIST-LIVE",
      "title": "Real docker compose down/up smoke is owed (offline tests simulate the volume-kept/container-gone boundary)",
      "severity": "low",
      "fix_location": "owner desktop: docker compose up --build → UI-configure Azure + bind judges/chat → down → up → assert keys+bindings present; down -v → assert cleared",
      "impact": "The persistence is proven offline (tmp dir + LITHRIM_PROVIDER_ENV_DIR) and via the compose default; a real container round-trip confirms /app/out write permissions + the boot-restore in the actual image.",
      "confidence": "INFERRED",
      "evidence": "docker-compose.yml sets LITHRIM_PROVIDER_ENV_DIR=${LITHRIM_PROVIDER_ENV_DIR:-/app/out}; no service started this cycle (standing rule: no autostart)."
    }
  ],

  "seams_closed": [],

  "verdict": "CLEAN",

  "report": "CONFIG-PERSIST-1 fixed: .provider_env (provider keys + per-role LITHRIM_LLM_*_<ROLE> + chat LITHRIM_CHAT_*) and the .models_registry.json sidecar now resolve through LITHRIM_PROVIDER_ENV_DIR (default REPO_ROOT, byte-identical to today), defaulted to /app/out (the named volume) in docker-compose so UI-configured keys/bindings survive docker compose down/up (wiped only by down -v). WHAT is written and the write-only/secret hygiene are unchanged — only WHERE the file lives. loop.py reads the env var directly (it can't import app.py); .env/.live_env stay repo-root. Boot restore is confirmed via _load_provider_env() at @app.on_event(startup). Full suite 871 passed / 0 failed, ruff clean. Two commits (RED 675ca6c, GREEN d9669e9). On audit, the monitor should check: (1) the round-trip MUTATION is the wiped-empty-dir variant (hermetic, not a repo-root read); (2) the isolate_provider_env fixture (it caught + fixed a real LITHRIM_CHAT_PROVIDER leak into the chat tests — a state-ordering regression I introduced and resolved); (3) a real down/up smoke is still owed live.",

  "next_session_hint": "monitor should run /devloop-audit bench-salvage CONFIG-PERSIST-1 next; the live down/up smoke is owed to the owner."
}
