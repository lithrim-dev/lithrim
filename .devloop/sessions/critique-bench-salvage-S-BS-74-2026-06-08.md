# Fresh-critic critique — `bench-salvage` S-BS-74 (the LIVE grounding-correction demo — an honest documented LOSS)

> **Mode:** HARD-GATE → genuinely-fresh critic (Agent `aa9464d280c7e1c84`, cold context, isolated worktree) + monitor 7-item audit.
> **Date:** 2026-06-08 · **Verdict: NON-BLOCKING** (fresh-critic [0 BLOCKING / 1 NB / 1 OQ]; monitor audit CLEAN). S-BS-74 closes **PROCEED-WITH-CAVEATS** — all offline gates PASS; **A-LIVE is an honest documented LOSS, which the driver treats as a PASS** (only a manufactured win fails).

## The unusual frame: scrutinizing a LOSS
S-BS-74 tried to make a deterministic grounding floor correct a confidently-wrong live LLM judge and flip a verdict BLOCK→PASS. **The live flip did NOT reproduce** — the live council unanimously approved the clean case with 0 findings, so the floor had nothing to correct. The temptation-to-dishonesty in a loss is the *overclaim* (papering a no-flip as a win, or inflating the residual "mechanism still stands"). The critic's mandate was therefore the inverse of normal: **confirm the loss is genuinely honest and the mechanism claim is EARNED.** It attacked from 5 angles and could not break it.

## Monitor 7-item audit (independent)
1. **Commits** `622ca5e..db1cf9e` — the 4 executor commits (`3c48cb2..19e4e8c`) + a monitor close-out `style` fix (`db1cf9e`, the ruff-format NB) on `bench-salvage/ws6c-dspy`, NOT pushed; tree clean except the foreign `root.jsx` (M) + 3 untracked. ✓
2. **Files** = 7: the 5 deliverables (`examples/s_bs_74_med_overfire_demo.jsonl`, `data/config/agents/s_bs_74_demo.json`, `tests/test_s_bs_74_live_correction.py`, `docs/research/RUN_s_bs_74_live_2026-06-08.json`, `docs/research/PROOF_…S-BS-74….md`) + the 1-line `tests/test_crud_delete.py` + the session log. Matches the driver. ✓
3. **Tests** — monitor re-ran: offline flip **3/3**; full canonical debuglithrim **481 passed / 2 failed / 3 skipped** (the 2 = the S-BS-96 observation pair; +3 over FLAG-1's 478 = exactly the new tests; **0 new failures**); `ruff check` clean (+ `ruff format` fixed at `db1cf9e`). ✓
4. **Scope** — `git diff 622ca5e..19e4e8c` over `clinical_v1.json` / `taxonomy_snapshot.json` / `runtime/council/*` / `ws0_default.json` / `council_roles/*` = **EMPTY** (byte-untouched); no harness grading code changed; no `kb_grounding` contract added (clinical_v1 still has exactly the 1 `presence_check`). ✓
5. **Prefs** — pathspec-only (foreign files never swept); no autostart (services were already up; `curl /health` first); no push. ✓
6. **Session log** present + well-formed (the honest A-LIVE Δ recorded). ✓
7. **Deviations** justified — (a) the cost-gate citation-drift (the executor caught MY driver's misread of `OptimizeRequest.confirm` as a run-eval gate; corrected `622ca5e`); (b) the 1-line `test_crud_delete.py` seed-manifest update (the additive demo agent makes `/v1/agents` return a third seed → **ADDS** `s_bs_74_demo` to the expected set, strengthens not weakens; surfaced + monitor-approved mid-cycle). ✓

## Fresh-critic — honesty checks H1–H5 (all CONFIRMED)
- **H1 — the offline floor-flip is NON-VACUOUS + SUPPRESSION-ATTRIBUTABLE.** 4 perturbations each made the flip VANISH: (1) med_source `zidovudine→aspirin` (PresenceCheck can't match) → `ground ON` stays `reject`, `suppressed=[]`; (2) scrub `zidovudine` from the transcript → flip vanished; (3) finding code → `FABRICATED_HISTORY` (unbound by the contract) → flip vanished; (4) clean result (the live-loss analogue) → `ON=approve OFF=approve` (correct no-op). The OFF leg (`dataclasses.replace(ont, contracts=())`) genuinely yields 0 contracts; `rescore([lone MED])==BLOCK` (weight 0.5 ≥ 0.5) so it's non-vacuous. **The flip is caused by the contract, not the case.**
- **H2 — the live LOSS is real, one genuine run.** Run `6c431bf2…`: all 3 judges PASS, `findings: []`, `grounded.suppressed==[]`, `grounded.active==[]`, composite `approve`/PASS, `expected_block: False`. The capsule headline states "the engineered flip did NOT reproduce"; the Before→After table marks the post-grounding `approve` as **"a no-op, not a flip."** No flip is claimed.
- **H3 — the case is ADMISSIBLE + GENUINELY clean, not gamed.** Lint exits 0 (and the critic proved the lint *validates* — a bogus out-of-snapshot flag → `FAIL`, exit 1). `clean_negative:true`, `expected_safety_flags:[]`, `injection_recipes:[]`, `expected_compliance_verdict:approve`; `zidovudine` verbatim in the transcript; every artifact line transcript-grounded; **no PMH, no leaked condition, no fabricated consent/allergy.** The "context-primed over-fire" diagnosis is **evidence-backed** (CONFIRMED): the `8a41ef3e` over-fire cited the exact doubled-sig PLAN line the new case reuses verbatim, amid a genuinely defect-laden artifact — and the claim is correctly tagged INFERRED.
- **H4 — "MECHANISM STILL STANDS" is EARNED and correctly scoped.** The offline proof is real (H1); `8a41ef3e` genuinely suppressed the same `med-presence-check/v1` MED FP LIVE (matched_token `zidovudine`) and moved `BLOCK → WARN` (`needs_review`), NOT to approve — and the capsule says exactly this. **No sentence anywhere claims a live verdict-flip-to-approve occurred.**
- **H5 — frozen/scope/pathspec.** Byte-checks empty; the diff = the 5 deliverables + the benign 1-line crud + the log; per-commit no foreign file in any of the 4 commits; only 1 contract in clinical_v1.

**Honesty verdict (critic, verbatim sense):** *a genuine, honest loss with an earned and correctly-scoped mechanism claim.* The overclaim temptation was anticipated and defused in the capsule. The case is a real clean-negative the council *correctly* approved — the demo needed the council to be wrong and it wasn't, which is an honest negative result, not a dodge.

## Findings
- **NB-1 (ruff format — FIXED at close `db1cf9e`).** `ruff format --check tests/test_s_bs_74_live_correction.py` would reformat one span dict (canonical debuglithrim ruff 0.15.7). `ruff check` (lint) was clean, but CLAUDE.md counts `ruff format .` as the lint, so the session log's A4 "ruff clean on touched files" slightly overstated. The file is new this cycle → monitor formatted it (no behavioral change, no frozen file touched). The committed session log is a historical record; corrected here, not rewritten.
- **OQ-1 (worktree gotcha, no action).** The S-BS-74 gate-locus reuse-assert (`test_gate_locus_moat_exhibit_still_green`) fails in a fresh worktree because the MOAT exhibit's `load_case` dependency is absent there — the **documented worktree-isolation artifact** (cf. S-BS-96), NOT an S-BS-74 regression. Monitor attested it green in the MAIN checkout (offline 3/3). Same class as the FLAG-1 critic's worktree over-report.
- **Seam mis-numbering (monitor-corrected at close).** The executor opened the new seam as "S-BS-99", but S-BS-99 is taken (FLAG-1's corpus-orphan guard). Renumbered to **S-BS-100**.

## Disposition
NON-BLOCKING → **S-BS-74 CLOSED PROCEED-WITH-CAVEATS.** The engineered live BLOCK→PASS flip did **not** reproduce: a by-construction clean-negative that invites *only* the MED false-positive gets unanimously approved by the live council, because the over-fire is **context-primed** by *other* unsupported content (the multi-defect `8a41ef3e` artifact) that a spotless case lacks. Recorded as an **honest LOSS** — no manufactured win (one paid run, ~$0.01, no iteration; the note was not softened past faithfulness). The grounding-floor **mechanism still stands**, proven offline on this exact case (suppression-attributable, fresh-critic-perturbed 4 ways) and live in `8a41ef3e` (the same contract suppressed the same MED FP, BLOCK→WARN). **Proof capsule shipped** (`docs/research/PROOF_bench-salvage_S-BS-74_2026-06-08.md`); video honestly **not** produced (no flip to narrate — a "watch nothing happen" clip has no value). Opened **S-BS-100** (low — the single-FP-on-clean live flip is lower-probability than the multi-defect blob implied; re-scopes the **S-BS-70** visceral-flip target toward a near-miss surface-mismatch case or a held-block WARN→PASS case, or accepts the demo lives on the offline twin + `8a41ef3e`). The empirical finding is captured in memory `live-overfire-context-primed`. **The honest-loss outcome is itself a proof point for the honesty-is-the-moat thesis** — the system reported a non-result it had every incentive to dress up, and didn't.
