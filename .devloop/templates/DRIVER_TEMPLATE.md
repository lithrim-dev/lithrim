# Driver — `<stream>` phase `<N>`: `<scope>`

> **Generated by the monitor in a planning session. The executor pastes
> the KICKOFF block (top) into a fresh Claude Code session, reads the
> rest, posts plan-review, then implements.**
>
> **Bundle ID:** `<stream>-phase<N>-<scope>-driver`
> **Version:** v1
> **Authored:** YYYY-MM-DD
> **Last re-verified against code:** YYYY-MM-DD (per MONITOR §Phase 1a)

---

## KICKOFF (paste this block into a fresh executor session)

```
You are the EXECUTOR for the .devloop/ cycle: <stream> phase <N> (<scope>).

Read in this order:
  1. .devloop/personas/EXECUTOR.md       (your role)
  2. .devloop/prompts/<this-driver>.md   (this doc; you are reading the kickoff already)
  3. <pre-flight doc 1>
  4. <pre-flight doc 2>
  ...

Then post your plan-review per EXECUTOR.md §"Plan-review (non-negotiable)".
Do not write code until I say "go".

Bundle ID: <stream>-phase<N>-<scope>-driver
Driver doc: .devloop/prompts/<this-driver>.md
```

---

## 1. Pre-flight reading (ordered)

Open these in order before posting plan-review:

1. `<path/to/spec.md>` — the spec section this cycle implements
2. `<path/to/relevant/code.py>:<line-range>` — the integration site
3. `<path/to/related-tests.py>` — existing test patterns to extend
4. `.devloop/state/STREAM_<stream>.md` — current stream state + open seams
5. `<other context>` — ...

> **Citation discipline:** every file:line above must match current
> code as of the "Last re-verified" date in the metadata. If you spot
> drift on read, halt and surface it (per EXECUTOR.md §"Citation drift").

---

## 2. Deliverables (file-by-file)

1. **`<file/path/1.py>`** — `<one-line description of the change>`
   - Add `<symbol>` with signature `<sig>`
   - Wire into `<call site>`

2. **`<file/path/2.py>`** — `<one-line description>`
   - ...

3. **`<test/path/1.py>`** — tests for deliverable 1 + 2
   - Covers acceptance criteria A1, A2, A3 below

4. **`<docs/whatever.md>`** — `<one-line description>` (if docs in scope)

---

## 3. Plan-review checkpoint (non-negotiable)

Per `.devloop/personas/EXECUTOR.md`, post a plan with:
- Understanding (one paragraph)
- File-by-file changes
- Test plan
- Risks
- Any proposed deviations from this driver

Wait for "go" before writing code.

---

## 4. Scope guardrails — NOT in scope

The following are tempting but OUT of scope for this cycle:

- `<thing 1>` — handled by phase `<M>`; do not touch
- `<thing 2>` — separate stream; flag as seam if surfaced
- `<thing 3>` — refactor opportunity; defer
- Drive-by formatting / dependency bumps / "while I was here" edits

If something feels load-bearing-but-not-listed, halt and surface it
in plan-review.

---

## 5. Acceptance criteria

The cycle is done when ALL of these are PASS:

- **A1.** `<criterion 1>` — verify via `<command or test>`
- **A2.** `<criterion 2>` — verify via `<command or test>`
- **A3.** `<criterion 3>` — verify via `<command or test>`

Each gets a row in the session log's `acceptance` array.

### Diagnostic stats (NOT acceptance gates)

If the driver measures things that are useful to report but not
load-bearing gates, list them here so the acceptance/diagnostic
distinction stays clean:

- `<diagnostic 1>` — reported in REPORT; NOT a gate
- `<diagnostic 2>` — reported in REPORT; NOT a gate

---

## 6. Commit structure + message templates

Atomic commits per logical unit. Suggested order:

1. **`feat(<scope>): <deliverable 1 title>`**
   - Body explains why; references this driver bundle ID
2. **`feat(<scope>): <deliverable 2 title>`**
3. **`test(<scope>): <test title>`**
4. **`docs(<scope>): <docs title>`** (if in scope)

Each commit body should end with:

```
Executed per <bundle-id> by session-YYYY-MM-DD-N.
See .devloop/sessions/session-<stream>-phase<N>-YYYY-MM-DD.json
```

(Add the user's Co-Authored-By trailer if used.)

---

## 7. Verification checklist (run at end, before writing session log)

- [ ] All acceptance criteria PASS (per §5)
- [ ] All commits exist; working tree clean
- [ ] Tests pass + linters clean (re-run; record commands used)
- [ ] No file in the diff outside §2 deliverables (scope held)
- [ ] No services were autostarted (user preference)
- [ ] No pushes / publishes / tags (user preference)
- [ ] Session log written per `.devloop/templates/SESSION_LOG_TEMPLATE.json`

---

## 8. First move

1. Read `.devloop/personas/EXECUTOR.md` end-to-end.
2. Read §1 pre-flight docs in order.
3. Re-read §2-§7 of this driver.
4. Post plan-review per §3.

---

## 9. References

- Spec: `<path/to/spec.md>` §`<section>`
- Stream state: `.devloop/state/STREAM_<stream>.md`
- Prior cycle session log: `.devloop/sessions/session-<stream>-phase<N-1>-YYYY-MM-DD.json`
- Prior cycle critique (if HARD GATE): `.devloop/sessions/critique-<stream>-phase<N-1>-YYYY-MM-DD.md`

---

## Hardness

- [ ] Routine cycle — monitor performs inline critique at close
- [ ] **HARD GATE** — fresh-critic session required; trigger via `/devloop-critique <stream> <N>`
